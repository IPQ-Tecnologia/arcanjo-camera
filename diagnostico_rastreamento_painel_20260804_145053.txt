============================================================
DIAGNÓSTICO DE RASTREAMENTO E PAINEL
Data: ter 04 ago 2026 14:50:53 -03
Pasta: /home/matheus-gomes/Documentos/minha-api-lab
============================================================

===== ARQUIVOS DO PROJETO =====
./alarm_detection_payload.py
./analise_atributos_eventos/comparar_dahua.py
./analise_noite/galeria.html
./app/adapters/cameras/base.py
app/adapters/cameras/base.py
./app/adapters/cameras/dahua.py
app/adapters/cameras/dahua.py
./app/adapters/cameras/factory.py
app/adapters/cameras/factory.py
./app/adapters/cameras/hikvision.py
app/adapters/cameras/hikvision.py
./app/adapters/cameras/__init__.py
app/adapters/cameras/__init__.py
./app/adapters/__init__.py
app/adapters/__init__.py
./app/api/__init__.py
app/api/__init__.py
./app/api/routes/__init__.py
app/api/routes/__init__.py
./app/core/config_backup_etapa10.py
app/core/config_backup_etapa10.py
./app/core/config.py
app/core/config.py
./app/core/__init__.py
app/core/__init__.py
./app/domain/__init__.py
app/domain/__init__.py
./app/domain/models/alarm_detection_message.py
app/domain/models/alarm_detection_message.py
./app/domain/models/camera_event.py
app/domain/models/camera_event.py
./app/domain/models/__init__.py
app/domain/models/__init__.py
./app/__init__.py
app/__init__.py
./app/messaging/__init__.py
app/messaging/__init__.py
./app/messaging/kafka_producer_melhorado.py
app/messaging/kafka_producer_melhorado.py
./app/messaging/kafka_producer.py
app/messaging/kafka_producer.py
./app/services/alarm_detection_payload_backup_antes_multiplas_pessoas.py
app/services/alarm_detection_payload_backup_antes_multiplas_pessoas.py
./app/services/alarm_detection_payload.py
app/services/alarm_detection_payload.py
./app/services/appearance_memory.py
app/services/appearance_memory.py
./app/services/camera_event_pipeline_backup_amostras_repetidas.py
app/services/camera_event_pipeline_backup_amostras_repetidas.py
./app/services/camera_event_pipeline_backup_analise_visual.py
app/services/camera_event_pipeline_backup_analise_visual.py
./app/services/camera_event_pipeline_backup_antes_analise.py
app/services/camera_event_pipeline_backup_antes_analise.py
./app/services/camera_event_pipeline_backup_antes_movimento.py
app/services/camera_event_pipeline_backup_antes_movimento.py
./app/services/camera_event_pipeline_backup_antes_multiplas_pessoas.py
app/services/camera_event_pipeline_backup_antes_multiplas_pessoas.py
./app/services/camera_event_pipeline_backup_antes_saida.py
app/services/camera_event_pipeline_backup_antes_saida.py
./app/services/camera_event_pipeline_backup_atualizacao_visual.py
app/services/camera_event_pipeline_backup_atualizacao_visual.py
./app/services/camera_event_pipeline_backup_cena_renderizada.py
app/services/camera_event_pipeline_backup_cena_renderizada.py
./app/services/camera_event_pipeline_backup_contexto_cena.py
app/services/camera_event_pipeline_backup_contexto_cena.py
./app/services/camera_event_pipeline_backup_front.py
app/services/camera_event_pipeline_backup_front.py
./app/services/camera_event_pipeline_backup_recorte.py
app/services/camera_event_pipeline_backup_recorte.py
./app/services/camera_event_pipeline_melhorado.py
app/services/camera_event_pipeline_melhorado.py
./app/services/camera_event_pipeline.py
app/services/camera_event_pipeline.py
./app/services/event_hub.py
app/services/event_hub.py
./app/services/__init__.py
app/services/__init__.py
./app/services/person_detector_yolo.py
app/services/person_detector_yolo.py
./app/services/person_movement.py
app/services/person_movement.py
./app/services/person_tracker_backup_antes_15s.py
app/services/person_tracker_backup_antes_15s.py
./app/services/person_tracker_backup_antes_continuidade.py
app/services/person_tracker_backup_antes_continuidade.py
./app/services/person_tracker_backup_antes_lote.py
app/services/person_tracker_backup_antes_lote.py
./app/services/person_tracker.py
app/services/person_tracker.py
./app/services/scene_analyzer_backup_antes_cores.py
app/services/scene_analyzer_backup_antes_cores.py
./app/services/scene_analyzer_backup_antes_cor_preta.py
app/services/scene_analyzer_backup_antes_cor_preta.py
./app/services/scene_analyzer.py
app/services/scene_analyzer.py
./app/services/scene_context_analyzer_backup_integracao.py
app/services/scene_context_analyzer_backup_integracao.py
./app/services/scene_context_analyzer.py
app/services/scene_context_analyzer.py
./app/services/scene_renderer.py
app/services/scene_renderer.py
./app/static/index_backup_analise_visual.html
app/static/index_backup_analise_visual.html
./app/static/index_backup_antes_analise.html
app/static/index_backup_antes_analise.html
./app/static/index_backup_antes_atualizacao.html
app/static/index_backup_antes_atualizacao.html
./app/static/index_backup_antes_contadores.html
app/static/index_backup_antes_contadores.html
./app/static/index_backup_antes_contexto_cena.html
app/static/index_backup_antes_contexto_cena.html
./app/static/index_backup_antes_duas_imagens.html
app/static/index_backup_antes_duas_imagens.html
./app/static/index_backup_antes_movimento.html
app/static/index_backup_antes_movimento.html
./app/static/index_backup_atualizacao_visual.html
app/static/index_backup_atualizacao_visual.html
./app/static/index_backup_recorte.html
app/static/index_backup_recorte.html
./app/static/index.html
app/static/index.html
./camera_event_pipeline(1).py
./diagnosticar_balanco_cor.py
./diagnosticar_cores_eventos.py
./diagnosticar_preto_atual.py
./inspecionar_dahua.py
./main_antes_background.py
./main_backup_antes_xml.py
./main_kafka_melhorado.py
./main_kafka_teste_backup_antes_comentar_rotas_20260723-181656.py
./main_kafka_teste_backup_antes_rota_device.py
./main_kafka_teste_backup_rota_device_20260723-180654.py
./main_kafka_teste.py
./main.py
./main_refatorado_teste.py
./organizar_base_dahua.py
./test_alarm_payload.py
./test_appearance_memory.py
./testar_cor_segmentada.py
./testar_detector_pessoas.py
./testar_estabilidade_cor.py
./test_kafka_connection.py
./test_kafka_publisher.py
./test_person_movement.py
./test_person_tracker_continuidade.py
./test_person_tracker_lote.py
./test_person_tracker.py
./test_publicar_alarm_kafka.py
./test_scene_analyzer_colors.py
./test_scene_analyzer.py
./test_scene_context_analyzer.py
./test_scene_renderer.py
./workers/__init__.py

===== REFERÊNCIAS AO RASTREAMENTO =====
app/services/camera_event_pipeline_melhorado.py:16:from app.services.person_tracker import (
app/services/camera_event_pipeline_melhorado.py:18:    TrackingDecision,
app/services/camera_event_pipeline_melhorado.py:19:    person_tracker,
app/services/camera_event_pipeline_melhorado.py:89:            await asyncio.wait_for(self.queue.join(), timeout=10)
app/services/camera_event_pipeline_melhorado.py:108:        await person_tracker.limpar()
app/services/camera_event_pipeline_melhorado.py:124:                saidas = await person_tracker.coletar_saidas()
app/services/camera_event_pipeline_melhorado.py:128:                        saida.pessoa_id
app/services/camera_event_pipeline_melhorado.py:131:                        saida.pessoa_id
app/services/camera_event_pipeline_melhorado.py:140:                        "pessoa_id": saida.pessoa_id,
app/services/camera_event_pipeline_melhorado.py:141:                        "status": "exited",
app/services/camera_event_pipeline_melhorado.py:167:                        saida.pessoa_id,
app/services/camera_event_pipeline_melhorado.py:268:        pessoa_id: str,
app/services/camera_event_pipeline_melhorado.py:300:                    pessoa_id,
app/services/camera_event_pipeline_melhorado.py:307:                    pessoa_id
app/services/camera_event_pipeline_melhorado.py:330:                    pessoa_id,
app/services/camera_event_pipeline_melhorado.py:335:                    pessoa_id
app/services/camera_event_pipeline_melhorado.py:339:                pessoa_id=pessoa_id,
app/services/camera_event_pipeline_melhorado.py:346:                pessoa_id,
app/services/camera_event_pipeline_melhorado.py:358:                pessoa_id,
app/services/camera_event_pipeline_melhorado.py:444:        decisao: TrackingDecision,
app/services/camera_event_pipeline_melhorado.py:456:            "pessoa_id": decisao.pessoa_id,
app/services/camera_event_pipeline_melhorado.py:457:            "status": "appearance_updated",
app/services/camera_event_pipeline_melhorado.py:493:            decisao.pessoa_id,
app/services/camera_event_pipeline_melhorado.py:530:        decisao: TrackingDecision,
app/services/camera_event_pipeline_melhorado.py:541:            pessoa_id=decisao.pessoa_id,
app/services/camera_event_pipeline_melhorado.py:546:            pessoa_id=decisao.pessoa_id,
app/services/camera_event_pipeline_melhorado.py:551:            "tendencia=%s velocidade=%spx_s distancia_total=%spx "
app/services/camera_event_pipeline_melhorado.py:554:            decisao.pessoa_id,
app/services/camera_event_pipeline_melhorado.py:557:            movimento.tendencia_distancia,
app/services/camera_event_pipeline_melhorado.py:559:            movimento.distancia_total_pixels,
app/services/camera_event_pipeline_melhorado.py:584:                        decisao.pessoa_id,
app/services/camera_event_pipeline_melhorado.py:604:                decisao.pessoa_id,
app/services/camera_event_pipeline_melhorado.py:637:                decisao.pessoa_id,
app/services/camera_event_pipeline_melhorado.py:644:            "pessoa_id": decisao.pessoa_id,
app/services/camera_event_pipeline_melhorado.py:684:            decisao.pessoa_id,
app/services/camera_event_pipeline_melhorado.py:695:                decisao.pessoa_id,
app/services/camera_event_pipeline_melhorado.py:714:            decisao.pessoa_id,
app/services/camera_event_pipeline_melhorado.py:805:                caixas_rastreamento = [
app/services/camera_event_pipeline_melhorado.py:814:                decisoes = await person_tracker.registrar_lote(
app/services/camera_event_pipeline_melhorado.py:817:                    bboxes=caixas_rastreamento,
app/services/camera_event_pipeline_melhorado.py:831:                        (decisao.pessoa_id, decisao.status)
app/services/camera_event_pipeline_backup_antes_analise.py:13:from app.services.person_tracker import (
app/services/camera_event_pipeline_backup_antes_analise.py:15:    person_tracker,
app/services/camera_event_pipeline_backup_antes_analise.py:92:                timeout=10,
app/services/camera_event_pipeline_backup_antes_analise.py:150:                    await person_tracker.coletar_saidas()
app/services/camera_event_pipeline_backup_antes_analise.py:162:                        "pessoa_id": saida.pessoa_id,
app/services/camera_event_pipeline_backup_antes_analise.py:163:                        "status": "exited",
app/services/camera_event_pipeline_backup_antes_analise.py:181:                        saida.pessoa_id,
app/services/camera_event_pipeline_backup_antes_analise.py:258:                caixa_rastreamento = DetectionBox(
app/services/camera_event_pipeline_backup_antes_analise.py:265:                decisao = await person_tracker.registrar(
app/services/camera_event_pipeline_backup_antes_analise.py:268:                    bbox=caixa_rastreamento,
app/services/camera_event_pipeline_backup_antes_analise.py:276:                    decisao.pessoa_id,
app/services/camera_event_pipeline_backup_antes_analise.py:288:                        decisao.pessoa_id,
app/services/camera_event_pipeline_backup_antes_analise.py:308:                    "pessoa_id": decisao.pessoa_id,
app/services/camera_event_pipeline_backup_front.py:81:                timeout=10,
app/services/camera_event_pipeline_backup_antes_saida.py:12:from app.services.person_tracker import (
app/services/camera_event_pipeline_backup_antes_saida.py:14:    person_tracker,
app/services/camera_event_pipeline_backup_antes_saida.py:85:                timeout=10,
app/services/camera_event_pipeline_backup_antes_saida.py:152:                # Eventos sem imagem não entram no rastreamento.
app/services/camera_event_pipeline_backup_antes_saida.py:183:                    await person_tracker.coletar_saidas()
app/services/camera_event_pipeline_backup_antes_saida.py:191:                        saida.pessoa_id,
app/services/camera_event_pipeline_backup_antes_saida.py:196:                caixa_rastreamento = DetectionBox(
app/services/camera_event_pipeline_backup_antes_saida.py:203:                decisao = await person_tracker.registrar(
app/services/camera_event_pipeline_backup_antes_saida.py:206:                    bbox=caixa_rastreamento,
app/services/camera_event_pipeline_backup_antes_saida.py:214:                    decisao.pessoa_id,
app/services/camera_event_pipeline_backup_antes_saida.py:226:                        decisao.pessoa_id,
app/services/camera_event_pipeline_backup_antes_saida.py:246:                    "pessoa_id": decisao.pessoa_id,
app/services/camera_event_pipeline_backup_cena_renderizada.py:22:from app.services.person_tracker import (
app/services/camera_event_pipeline_backup_cena_renderizada.py:24:    person_tracker,
app/services/camera_event_pipeline_backup_cena_renderizada.py:113:                timeout=10,
app/services/camera_event_pipeline_backup_cena_renderizada.py:179:                    await person_tracker
app/services/camera_event_pipeline_backup_cena_renderizada.py:187:                            saida.pessoa_id
app/services/camera_event_pipeline_backup_cena_renderizada.py:206:                        "pessoa_id": (
app/services/camera_event_pipeline_backup_cena_renderizada.py:207:                            saida.pessoa_id
app/services/camera_event_pipeline_backup_cena_renderizada.py:209:                        "status": "exited",
app/services/camera_event_pipeline_backup_cena_renderizada.py:236:                        saida.pessoa_id,
app/services/camera_event_pipeline_backup_cena_renderizada.py:269:        pessoa_id: str,
app/services/camera_event_pipeline_backup_cena_renderizada.py:320:                    pessoa_id=pessoa_id,
app/services/camera_event_pipeline_backup_cena_renderizada.py:331:                pessoa_id,
app/services/camera_event_pipeline_backup_cena_renderizada.py:523:                caixa_rastreamento = (
app/services/camera_event_pipeline_backup_cena_renderizada.py:537:                    await person_tracker
app/services/camera_event_pipeline_backup_cena_renderizada.py:544:                            caixa_rastreamento
app/services/camera_event_pipeline_backup_cena_renderizada.py:554:                    decisao.pessoa_id,
app/services/camera_event_pipeline_backup_cena_renderizada.py:569:                        pessoa_id=(
app/services/camera_event_pipeline_backup_cena_renderizada.py:570:                            decisao.pessoa_id
app/services/camera_event_pipeline_backup_cena_renderizada.py:605:                            "pessoa_id": (
app/services/camera_event_pipeline_backup_cena_renderizada.py:606:                                decisao.pessoa_id
app/services/camera_event_pipeline_backup_cena_renderizada.py:609:                                "appearance_updated"
app/services/camera_event_pipeline_backup_cena_renderizada.py:647:                            decisao.pessoa_id,
app/services/camera_event_pipeline_backup_cena_renderizada.py:666:                        decisao.pessoa_id,
app/services/camera_event_pipeline_backup_cena_renderizada.py:698:                    "pessoa_id": (
app/services/camera_event_pipeline_backup_cena_renderizada.py:699:                        decisao.pessoa_id
app/services/person_detector_yolo.py.backup-duplicado:61:    iou: float = 0.30,
app/services/person_detector_yolo.py.backup-duplicado:91:            iou=iou,
app/services/person_detector_yolo.py.backup-duplicado:213:    iou = (
app/services/person_detector_yolo.py.backup-duplicado:227:        iou >= 0.30
app/services/person_tracker_backup_antes_continuidade.py:10:    "entered",
app/services/person_tracker_backup_antes_continuidade.py:11:    "updated",
app/services/person_tracker_backup_antes_continuidade.py:12:    "suppressed",
app/services/person_tracker_backup_antes_continuidade.py:13:    "exited",
app/services/person_tracker_backup_antes_continuidade.py:47:    def iou(
app/services/person_tracker_backup_antes_continuidade.py:107:    def distancia_centros(
app/services/person_tracker_backup_antes_continuidade.py:129:    pessoa_id: str
app/services/person_tracker_backup_antes_continuidade.py:142:class TrackingDecision:
app/services/person_tracker_backup_antes_continuidade.py:143:    pessoa_id: str
app/services/person_tracker_backup_antes_continuidade.py:154:class PersonTracker:
app/services/person_tracker_backup_antes_continuidade.py:159:        limite_iou: float = 0.25,
app/services/person_tracker_backup_antes_continuidade.py:160:        limite_distancia: float = 0.75,
app/services/person_tracker_backup_antes_continuidade.py:173:        if limite_iou < 0:
app/services/person_tracker_backup_antes_continuidade.py:175:                "limite_iou não pode ser negativo"
app/services/person_tracker_backup_antes_continuidade.py:178:        if limite_distancia < 0:
app/services/person_tracker_backup_antes_continuidade.py:180:                "limite_distancia não pode "
app/services/person_tracker_backup_antes_continuidade.py:192:        self.limite_iou = limite_iou
app/services/person_tracker_backup_antes_continuidade.py:193:        self.limite_distancia = limite_distancia
app/services/person_tracker_backup_antes_continuidade.py:212:    ) -> TrackingDecision:
app/services/person_tracker_backup_antes_continuidade.py:233:    ) -> list[TrackingDecision]:
app/services/person_tracker_backup_antes_continuidade.py:274:                TrackingDecision
app/services/person_tracker_backup_antes_continuidade.py:303:                        TrackingDecision(
app/services/person_tracker_backup_antes_continuidade.py:304:                            pessoa_id=(
app/services/person_tracker_backup_antes_continuidade.py:305:                                pessoa.pessoa_id
app/services/person_tracker_backup_antes_continuidade.py:311:                            status="entered",
app/services/person_tracker_backup_antes_continuidade.py:340:    ) -> list[TrackingDecision]:
app/services/person_tracker_backup_antes_continuidade.py:349:                TrackingDecision
app/services/person_tracker_backup_antes_continuidade.py:355:                pessoa_id,
app/services/person_tracker_backup_antes_continuidade.py:370:                    TrackingDecision(
app/services/person_tracker_backup_antes_continuidade.py:371:                        pessoa_id=(
app/services/person_tracker_backup_antes_continuidade.py:372:                            pessoa.pessoa_id
app/services/person_tracker_backup_antes_continuidade.py:378:                        status="exited",
app/services/person_tracker_backup_antes_continuidade.py:389:                    pessoa_id
app/services/person_tracker_backup_antes_continuidade.py:392:            for pessoa_id in ids_para_remover:
app/services/person_tracker_backup_antes_continuidade.py:394:                    pessoa_id,
app/services/person_tracker_backup_antes_continuidade.py:446:                        pessoa.pessoa_id,
app/services/person_tracker_backup_antes_continuidade.py:452:        # O índice e o pessoa_id deixam o resultado
app/services/person_tracker_backup_antes_continuidade.py:473:            pessoa_id,
app/services/person_tracker_backup_antes_continuidade.py:478:            if pessoa_id in pessoas_utilizadas:
app/services/person_tracker_backup_antes_continuidade.py:482:                pessoa_id
app/services/person_tracker_backup_antes_continuidade.py:495:                pessoa_id
app/services/person_tracker_backup_antes_continuidade.py:505:        sobreposicao = bbox.iou(
app/services/person_tracker_backup_antes_continuidade.py:509:        distancia = bbox.distancia_centros(
app/services/person_tracker_backup_antes_continuidade.py:521:        distancia_normalizada = (
app/services/person_tracker_backup_antes_continuidade.py:522:            distancia
app/services/person_tracker_backup_antes_continuidade.py:527:            distancia_normalizada
app/services/person_tracker_backup_antes_continuidade.py:528:            <= self.limite_distancia
app/services/person_tracker_backup_antes_continuidade.py:533:            >= self.limite_iou
app/services/person_tracker_backup_antes_continuidade.py:542:        pontuacao_distancia = max(
app/services/person_tracker_backup_antes_continuidade.py:544:            1.0 - distancia_normalizada,
app/services/person_tracker_backup_antes_continuidade.py:549:            + pontuacao_distancia
app/services/person_tracker_backup_antes_continuidade.py:558:    ) -> TrackingDecision:
app/services/person_tracker_backup_antes_continuidade.py:575:            return TrackingDecision(
app/services/person_tracker_backup_antes_continuidade.py:576:                pessoa_id=pessoa.pessoa_id,
app/services/person_tracker_backup_antes_continuidade.py:579:                status="updated",
app/services/person_tracker_backup_antes_continuidade.py:587:        return TrackingDecision(
app/services/person_tracker_backup_antes_continuidade.py:588:            pessoa_id=pessoa.pessoa_id,
app/services/person_tracker_backup_antes_continuidade.py:591:            status="suppressed",
app/services/person_tracker_backup_antes_continuidade.py:652:        pessoa_id = (
app/services/person_tracker_backup_antes_continuidade.py:658:            pessoa_id=pessoa_id,
app/services/person_tracker_backup_antes_continuidade.py:669:            pessoa_id
app/services/person_tracker_backup_antes_continuidade.py:700:person_tracker = PersonTracker(
app/services/camera_event_pipeline_backup_contexto_cena.py:22:from app.services.person_tracker import (
app/services/camera_event_pipeline_backup_contexto_cena.py:24:    person_tracker,
app/services/camera_event_pipeline_backup_contexto_cena.py:104:                timeout=10,
app/services/camera_event_pipeline_backup_contexto_cena.py:164:                    await person_tracker.coletar_saidas()
app/services/camera_event_pipeline_backup_contexto_cena.py:170:                            saida.pessoa_id
app/services/camera_event_pipeline_backup_contexto_cena.py:182:                        "pessoa_id": saida.pessoa_id,
app/services/camera_event_pipeline_backup_contexto_cena.py:183:                        "status": "exited",
app/services/camera_event_pipeline_backup_contexto_cena.py:207:                        saida.pessoa_id,
app/services/camera_event_pipeline_backup_contexto_cena.py:236:        pessoa_id: str,
app/services/camera_event_pipeline_backup_contexto_cena.py:274:                    pessoa_id=pessoa_id,
app/services/camera_event_pipeline_backup_contexto_cena.py:285:                pessoa_id,
app/services/camera_event_pipeline_backup_contexto_cena.py:379:                caixa_rastreamento = DetectionBox(
app/services/camera_event_pipeline_backup_contexto_cena.py:386:                decisao = await person_tracker.registrar(
app/services/camera_event_pipeline_backup_contexto_cena.py:389:                    bbox=caixa_rastreamento,
app/services/camera_event_pipeline_backup_contexto_cena.py:397:                    decisao.pessoa_id,
app/services/camera_event_pipeline_backup_contexto_cena.py:408:                        pessoa_id=decisao.pessoa_id,
app/services/camera_event_pipeline_backup_contexto_cena.py:424:                            "pessoa_id": (
app/services/camera_event_pipeline_backup_contexto_cena.py:425:                                decisao.pessoa_id
app/services/camera_event_pipeline_backup_contexto_cena.py:428:                                "appearance_updated"
app/services/camera_event_pipeline_backup_contexto_cena.py:452:                            decisao.pessoa_id,
app/services/camera_event_pipeline_backup_contexto_cena.py:468:                        decisao.pessoa_id,
app/services/camera_event_pipeline_backup_contexto_cena.py:495:                    "pessoa_id": decisao.pessoa_id,
app/services/camera_event_pipeline_backup_atualizacao_visual.py:22:from app.services.person_tracker import (
app/services/camera_event_pipeline_backup_atualizacao_visual.py:24:    person_tracker,
app/services/camera_event_pipeline_backup_atualizacao_visual.py:104:                timeout=10,
app/services/camera_event_pipeline_backup_atualizacao_visual.py:164:                    await person_tracker.coletar_saidas()
app/services/camera_event_pipeline_backup_atualizacao_visual.py:170:                            saida.pessoa_id
app/services/camera_event_pipeline_backup_atualizacao_visual.py:182:                        "pessoa_id": saida.pessoa_id,
app/services/camera_event_pipeline_backup_atualizacao_visual.py:183:                        "status": "exited",
app/services/camera_event_pipeline_backup_atualizacao_visual.py:207:                        saida.pessoa_id,
app/services/camera_event_pipeline_backup_atualizacao_visual.py:236:        pessoa_id: str,
app/services/camera_event_pipeline_backup_atualizacao_visual.py:274:                    pessoa_id=pessoa_id,
app/services/camera_event_pipeline_backup_atualizacao_visual.py:285:                pessoa_id,
app/services/camera_event_pipeline_backup_atualizacao_visual.py:379:                caixa_rastreamento = DetectionBox(
app/services/camera_event_pipeline_backup_atualizacao_visual.py:386:                decisao = await person_tracker.registrar(
app/services/camera_event_pipeline_backup_atualizacao_visual.py:389:                    bbox=caixa_rastreamento,
app/services/camera_event_pipeline_backup_atualizacao_visual.py:397:                    decisao.pessoa_id,
app/services/camera_event_pipeline_backup_atualizacao_visual.py:409:                        pessoa_id=decisao.pessoa_id,
app/services/camera_event_pipeline_backup_atualizacao_visual.py:421:                        decisao.pessoa_id,
app/services/camera_event_pipeline_backup_atualizacao_visual.py:447:                    "pessoa_id": decisao.pessoa_id,
app/services/scene_context_analyzer.py:45:    distancia_normalizada: float
app/services/scene_context_analyzer.py:432:            distancia = math.sqrt(
app/services/scene_context_analyzer.py:439:                    distancia
app/services/scene_context_analyzer.py:451:                    distancia_normalizada=(
app/services/scene_context_analyzer.py:453:                            distancia,
app/services/scene_context_analyzer.py:467:    distancia_normalizada: float,
app/services/scene_context_analyzer.py:476:    if distancia_normalizada <= 0.15:
app/services/scene_context_analyzer.py:479:    if distancia_normalizada <= 0.30:
app/services/person_tracker.py:10:    "entered",
app/services/person_tracker.py:11:    "updated",
app/services/person_tracker.py:12:    "suppressed",
app/services/person_tracker.py:13:    "exited",
app/services/person_tracker.py:47:    def iou(
app/services/person_tracker.py:104:    def distancia_centros(
app/services/person_tracker.py:126:    pessoa_id: str
app/services/person_tracker.py:139:class TrackingDecision:
app/services/person_tracker.py:140:    pessoa_id: str
app/services/person_tracker.py:151:class PersonTracker:
app/services/person_tracker.py:156:        limite_iou: float = 0.25,
app/services/person_tracker.py:157:        limite_distancia: float = 0.75,
app/services/person_tracker.py:159:        limite_distancia_flexivel: float = 2.5,
app/services/person_tracker.py:174:        if limite_iou < 0:
app/services/person_tracker.py:176:                "limite_iou não pode ser negativo"
app/services/person_tracker.py:179:        if limite_distancia < 0:
app/services/person_tracker.py:181:                "limite_distancia não pode ser negativo"
app/services/person_tracker.py:191:            limite_distancia_flexivel
app/services/person_tracker.py:192:            < limite_distancia
app/services/person_tracker.py:195:                "limite_distancia_flexivel deve ser "
app/services/person_tracker.py:196:                "maior ou igual ao limite_distancia"
app/services/person_tracker.py:216:        self.limite_iou = limite_iou
app/services/person_tracker.py:217:        self.limite_distancia = limite_distancia
app/services/person_tracker.py:223:        self.limite_distancia_flexivel = (
app/services/person_tracker.py:224:            limite_distancia_flexivel
app/services/person_tracker.py:252:    ) -> TrackingDecision:
app/services/person_tracker.py:273:    ) -> list[TrackingDecision]:
app/services/person_tracker.py:319:                TrackingDecision
app/services/person_tracker.py:348:                        TrackingDecision(
app/services/person_tracker.py:349:                            pessoa_id=(
app/services/person_tracker.py:350:                                pessoa.pessoa_id
app/services/person_tracker.py:356:                            status="entered",
app/services/person_tracker.py:383:    ) -> list[TrackingDecision]:
app/services/person_tracker.py:392:                TrackingDecision
app/services/person_tracker.py:398:                pessoa_id,
app/services/person_tracker.py:413:                    TrackingDecision(
app/services/person_tracker.py:414:                        pessoa_id=(
app/services/person_tracker.py:415:                            pessoa.pessoa_id
app/services/person_tracker.py:421:                        status="exited",
app/services/person_tracker.py:432:                    pessoa_id
app/services/person_tracker.py:435:            for pessoa_id in ids_para_remover:
app/services/person_tracker.py:437:                    pessoa_id,
app/services/person_tracker.py:489:                        pessoa.pessoa_id,
app/services/person_tracker.py:512:            pessoa_id,
app/services/person_tracker.py:517:            if pessoa_id in pessoas_utilizadas:
app/services/person_tracker.py:521:                pessoa_id
app/services/person_tracker.py:534:                pessoa_id
app/services/person_tracker.py:559:        Assim, a regra não interfere no rastreamento
app/services/person_tracker.py:633:        distancia = bbox.distancia_centros(
app/services/person_tracker.py:645:        distancia_normalizada = (
app/services/person_tracker.py:646:            distancia
app/services/person_tracker.py:651:            self.limite_distancia_flexivel,
app/services/person_tracker.py:653:                self.limite_distancia,
app/services/person_tracker.py:660:            distancia_normalizada
app/services/person_tracker.py:666:            distancia_normalizada
app/services/person_tracker.py:686:        sobreposicao = bbox.iou(
app/services/person_tracker.py:690:        distancia = bbox.distancia_centros(
app/services/person_tracker.py:702:        distancia_normalizada = (
app/services/person_tracker.py:703:            distancia
app/services/person_tracker.py:708:            distancia_normalizada
app/services/person_tracker.py:709:            <= self.limite_distancia
app/services/person_tracker.py:714:            >= self.limite_iou
app/services/person_tracker.py:723:        pontuacao_distancia = max(
app/services/person_tracker.py:725:            1.0 - distancia_normalizada,
app/services/person_tracker.py:730:            + pontuacao_distancia
app/services/person_tracker.py:739:    ) -> TrackingDecision:
app/services/person_tracker.py:756:            return TrackingDecision(
app/services/person_tracker.py:757:                pessoa_id=pessoa.pessoa_id,
app/services/person_tracker.py:760:                status="updated",
app/services/person_tracker.py:768:        return TrackingDecision(
app/services/person_tracker.py:769:            pessoa_id=pessoa.pessoa_id,
app/services/person_tracker.py:772:            status="suppressed",
app/services/person_tracker.py:833:        pessoa_id = (
app/services/person_tracker.py:839:            pessoa_id=pessoa_id,
app/services/person_tracker.py:850:            pessoa_id
app/services/person_tracker.py:879:person_tracker = PersonTracker(
app/services/person_tracker.py:882:    limite_iou=0.25,
app/services/person_tracker.py:883:    limite_distancia=0.75,
app/services/person_tracker.py:885:    limite_distancia_flexivel=2.5,
app/services/camera_event_pipeline.py:16:from app.services.person_tracker import (
app/services/camera_event_pipeline.py:18:    TrackingDecision,
app/services/camera_event_pipeline.py:19:    person_tracker,
app/services/camera_event_pipeline.py:85:            await asyncio.wait_for(self.queue.join(), timeout=10)
app/services/camera_event_pipeline.py:104:        await person_tracker.limpar()
app/services/camera_event_pipeline.py:120:                saidas = await person_tracker.coletar_saidas()
app/services/camera_event_pipeline.py:124:                        saida.pessoa_id
app/services/camera_event_pipeline.py:127:                        saida.pessoa_id
app/services/camera_event_pipeline.py:136:                        "pessoa_id": saida.pessoa_id,
app/services/camera_event_pipeline.py:137:                        "status": "exited",
app/services/camera_event_pipeline.py:163:                        saida.pessoa_id,
app/services/camera_event_pipeline.py:264:        pessoa_id: str,
app/services/camera_event_pipeline.py:296:                    pessoa_id,
app/services/camera_event_pipeline.py:303:                    pessoa_id
app/services/camera_event_pipeline.py:326:                    pessoa_id,
app/services/camera_event_pipeline.py:331:                    pessoa_id
app/services/camera_event_pipeline.py:335:                pessoa_id=pessoa_id,
app/services/camera_event_pipeline.py:342:                pessoa_id,
app/services/camera_event_pipeline.py:354:                pessoa_id,
app/services/camera_event_pipeline.py:440:        decisao: TrackingDecision,
app/services/camera_event_pipeline.py:452:            "pessoa_id": decisao.pessoa_id,
app/services/camera_event_pipeline.py:453:            "status": "appearance_updated",
app/services/camera_event_pipeline.py:489:            decisao.pessoa_id,
app/services/camera_event_pipeline.py:526:        decisao: TrackingDecision,
app/services/camera_event_pipeline.py:537:            pessoa_id=decisao.pessoa_id,
app/services/camera_event_pipeline.py:542:            pessoa_id=decisao.pessoa_id,
app/services/camera_event_pipeline.py:547:            "tendencia=%s velocidade=%spx_s distancia_total=%spx "
app/services/camera_event_pipeline.py:550:            decisao.pessoa_id,
app/services/camera_event_pipeline.py:553:            movimento.tendencia_distancia,
app/services/camera_event_pipeline.py:555:            movimento.distancia_total_pixels,
app/services/camera_event_pipeline.py:580:                        decisao.pessoa_id,
app/services/camera_event_pipeline.py:600:                decisao.pessoa_id,
app/services/camera_event_pipeline.py:633:                decisao.pessoa_id,
app/services/camera_event_pipeline.py:640:            "pessoa_id": decisao.pessoa_id,
app/services/camera_event_pipeline.py:680:            decisao.pessoa_id,
app/services/camera_event_pipeline.py:691:                decisao.pessoa_id,
app/services/camera_event_pipeline.py:710:            decisao.pessoa_id,
app/services/camera_event_pipeline.py:764:                caixas_rastreamento = [
app/services/camera_event_pipeline.py:773:                decisoes = await person_tracker.registrar_lote(
app/services/camera_event_pipeline.py:776:                    bboxes=caixas_rastreamento,
app/services/camera_event_pipeline.py:790:                        (decisao.pessoa_id, decisao.status)
app/services/camera_event_pipeline_backup_antes_multiplas_pessoas.py:22:from app.services.person_tracker import (
app/services/camera_event_pipeline_backup_antes_multiplas_pessoas.py:24:    person_tracker,
app/services/camera_event_pipeline_backup_antes_multiplas_pessoas.py:110:                timeout=10,
app/services/camera_event_pipeline_backup_antes_multiplas_pessoas.py:171:                    await person_tracker
app/services/camera_event_pipeline_backup_antes_multiplas_pessoas.py:179:                            saida.pessoa_id
app/services/camera_event_pipeline_backup_antes_multiplas_pessoas.py:196:                        "pessoa_id": saida.pessoa_id,
app/services/camera_event_pipeline_backup_antes_multiplas_pessoas.py:197:                        "status": "exited",
app/services/camera_event_pipeline_backup_antes_multiplas_pessoas.py:227:                        saida.pessoa_id,
app/services/camera_event_pipeline_backup_antes_multiplas_pessoas.py:256:        pessoa_id: str,
app/services/camera_event_pipeline_backup_antes_multiplas_pessoas.py:286:                    pessoa_id=pessoa_id,
app/services/camera_event_pipeline_backup_antes_multiplas_pessoas.py:297:                pessoa_id,
app/services/camera_event_pipeline_backup_antes_multiplas_pessoas.py:544:                caixa_rastreamento = DetectionBox(
app/services/camera_event_pipeline_backup_antes_multiplas_pessoas.py:551:                decisao = await person_tracker.registrar(
app/services/camera_event_pipeline_backup_antes_multiplas_pessoas.py:554:                    bbox=caixa_rastreamento,
app/services/camera_event_pipeline_backup_antes_multiplas_pessoas.py:562:                    decisao.pessoa_id,
app/services/camera_event_pipeline_backup_antes_multiplas_pessoas.py:571:                        pessoa_id=decisao.pessoa_id,
app/services/camera_event_pipeline_backup_antes_multiplas_pessoas.py:596:                        "pessoa_id": decisao.pessoa_id,
app/services/camera_event_pipeline_backup_antes_multiplas_pessoas.py:597:                        "status": "appearance_updated",
app/services/camera_event_pipeline_backup_antes_multiplas_pessoas.py:636:                        decisao.pessoa_id,
app/services/camera_event_pipeline_backup_antes_multiplas_pessoas.py:657:                        decisao.pessoa_id,
app/services/camera_event_pipeline_backup_antes_multiplas_pessoas.py:684:                    "pessoa_id": decisao.pessoa_id,
app/services/appearance_memory.py:79:        pessoa_id: str,
app/services/appearance_memory.py:82:        if not pessoa_id:
app/services/appearance_memory.py:84:                "pessoa_id não pode ser vazio"
app/services/appearance_memory.py:89:                pessoa_id
app/services/appearance_memory.py:96:                    pessoa_id
app/services/appearance_memory.py:139:        pessoa_id: str,
app/services/appearance_memory.py:143:                pessoa_id
app/services/appearance_memory.py:155:        pessoa_id: str,
app/services/appearance_memory.py:164:                pessoa_id,
app/services/camera_event_pipeline_backup_analise_visual.py:13:from app.services.person_tracker import (
app/services/camera_event_pipeline_backup_analise_visual.py:15:    person_tracker,
app/services/camera_event_pipeline_backup_analise_visual.py:92:                timeout=10,
app/services/camera_event_pipeline_backup_analise_visual.py:150:                    await person_tracker.coletar_saidas()
app/services/camera_event_pipeline_backup_analise_visual.py:162:                        "pessoa_id": saida.pessoa_id,
app/services/camera_event_pipeline_backup_analise_visual.py:163:                        "status": "exited",
app/services/camera_event_pipeline_backup_analise_visual.py:181:                        saida.pessoa_id,
app/services/camera_event_pipeline_backup_analise_visual.py:258:                caixa_rastreamento = DetectionBox(
app/services/camera_event_pipeline_backup_analise_visual.py:265:                decisao = await person_tracker.registrar(
app/services/camera_event_pipeline_backup_analise_visual.py:268:                    bbox=caixa_rastreamento,
app/services/camera_event_pipeline_backup_analise_visual.py:276:                    decisao.pessoa_id,
app/services/camera_event_pipeline_backup_analise_visual.py:288:                        decisao.pessoa_id,
app/services/camera_event_pipeline_backup_analise_visual.py:308:                    "pessoa_id": decisao.pessoa_id,
app/services/person_detector_yolo.py:94:    iou: float,
app/services/person_detector_yolo.py:111:        round(iou, 4),
app/services/person_detector_yolo.py:119:    iou: float,
app/services/person_detector_yolo.py:125:        iou=iou,
app/services/person_detector_yolo.py:168:            iou=iou,
app/services/person_detector_yolo.py:240:    iou: float = 0.30,
app/services/person_detector_yolo.py:253:        iou=iou,
app/services/person_detector_yolo.py:412:    valor_iou = (
app/services/person_detector_yolo.py:429:        valor_iou,
app/services/person_detector_yolo.py:442:    iou: float = 0.30,
app/services/person_detector_yolo.py:470:        iou=iou,
app/services/person_detector_yolo.py:565:            valor_iou,
app/services/person_detector_yolo.py:574:            valor_iou >= 0.15
app/services/person_detector_yolo.py:584:                valor_iou,
app/services/person_detector_yolo.py:589:            valor_iou,
app/services/person_detector_yolo.py:851:    valor_iou = (
app/services/person_detector_yolo.py:866:        valor_iou >= 0.30
app/services/camera_event_pipeline_backup_antes_movimento.py:15:from app.services.person_tracker import (
app/services/camera_event_pipeline_backup_antes_movimento.py:17:    TrackingDecision,
app/services/camera_event_pipeline_backup_antes_movimento.py:18:    person_tracker,
app/services/camera_event_pipeline_backup_antes_movimento.py:83:            await asyncio.wait_for(self.queue.join(), timeout=10)
app/services/camera_event_pipeline_backup_antes_movimento.py:101:        await person_tracker.limpar()
app/services/camera_event_pipeline_backup_antes_movimento.py:117:                saidas = await person_tracker.coletar_saidas()
app/services/camera_event_pipeline_backup_antes_movimento.py:121:                        saida.pessoa_id
app/services/camera_event_pipeline_backup_antes_movimento.py:130:                        "pessoa_id": saida.pessoa_id,
app/services/camera_event_pipeline_backup_antes_movimento.py:131:                        "status": "exited",
app/services/camera_event_pipeline_backup_antes_movimento.py:150:                        saida.pessoa_id,
app/services/camera_event_pipeline_backup_antes_movimento.py:241:        pessoa_id: str,
app/services/camera_event_pipeline_backup_antes_movimento.py:261:                pessoa_id=pessoa_id,
app/services/camera_event_pipeline_backup_antes_movimento.py:268:                pessoa_id,
app/services/camera_event_pipeline_backup_antes_movimento.py:280:                pessoa_id,
app/services/camera_event_pipeline_backup_antes_movimento.py:366:        decisao: TrackingDecision,
app/services/camera_event_pipeline_backup_antes_movimento.py:376:            "pessoa_id": decisao.pessoa_id,
app/services/camera_event_pipeline_backup_antes_movimento.py:377:            "status": "appearance_updated",
app/services/camera_event_pipeline_backup_antes_movimento.py:406:            decisao.pessoa_id,
app/services/camera_event_pipeline_backup_antes_movimento.py:428:        decisao: TrackingDecision,
app/services/camera_event_pipeline_backup_antes_movimento.py:439:            pessoa_id=decisao.pessoa_id,
app/services/camera_event_pipeline_backup_antes_movimento.py:457:                decisao.pessoa_id,
app/services/camera_event_pipeline_backup_antes_movimento.py:490:                decisao.pessoa_id,
app/services/camera_event_pipeline_backup_antes_movimento.py:497:            "pessoa_id": decisao.pessoa_id,
app/services/camera_event_pipeline_backup_antes_movimento.py:529:            decisao.pessoa_id,
app/services/camera_event_pipeline_backup_antes_movimento.py:540:                decisao.pessoa_id,
app/services/camera_event_pipeline_backup_antes_movimento.py:555:            decisao.pessoa_id,
app/services/camera_event_pipeline_backup_antes_movimento.py:601:                caixas_rastreamento = [
app/services/camera_event_pipeline_backup_antes_movimento.py:610:                decisoes = await person_tracker.registrar_lote(
app/services/camera_event_pipeline_backup_antes_movimento.py:613:                    bboxes=caixas_rastreamento,
app/services/camera_event_pipeline_backup_antes_movimento.py:627:                        (decisao.pessoa_id, decisao.status)
app/services/camera_event_pipeline_backup_amostras_repetidas.py:22:from app.services.person_tracker import (
app/services/camera_event_pipeline_backup_amostras_repetidas.py:24:    person_tracker,
app/services/camera_event_pipeline_backup_amostras_repetidas.py:110:                timeout=10,
app/services/camera_event_pipeline_backup_amostras_repetidas.py:171:                    await person_tracker
app/services/camera_event_pipeline_backup_amostras_repetidas.py:179:                            saida.pessoa_id
app/services/camera_event_pipeline_backup_amostras_repetidas.py:198:                        "pessoa_id": (
app/services/camera_event_pipeline_backup_amostras_repetidas.py:199:                            saida.pessoa_id
app/services/camera_event_pipeline_backup_amostras_repetidas.py:201:                        "status": "exited",
app/services/camera_event_pipeline_backup_amostras_repetidas.py:227:                        saida.pessoa_id,
app/services/camera_event_pipeline_backup_amostras_repetidas.py:322:                caixa_rastreamento = (
app/services/camera_event_pipeline_backup_amostras_repetidas.py:336:                    await person_tracker.registrar(
app/services/camera_event_pipeline_backup_amostras_repetidas.py:342:                            caixa_rastreamento
app/services/camera_event_pipeline_backup_amostras_repetidas.py:352:                    decisao.pessoa_id,
app/services/camera_event_pipeline_backup_amostras_repetidas.py:367:                        decisao.pessoa_id,
app/services/camera_event_pipeline_backup_amostras_repetidas.py:403:                                pessoa_id=(
app/services/camera_event_pipeline_backup_amostras_repetidas.py:404:                                    decisao.pessoa_id
app/services/camera_event_pipeline_backup_amostras_repetidas.py:419:                            decisao.pessoa_id,
app/services/camera_event_pipeline_backup_amostras_repetidas.py:469:                    "pessoa_id": (
app/services/camera_event_pipeline_backup_amostras_repetidas.py:470:                        decisao.pessoa_id
app/services/scene_context_analyzer_backup_integracao.py:45:    distancia_normalizada: float
app/services/scene_context_analyzer_backup_integracao.py:432:            distancia = math.sqrt(
app/services/scene_context_analyzer_backup_integracao.py:439:                    distancia
app/services/scene_context_analyzer_backup_integracao.py:451:                    distancia_normalizada=(
app/services/scene_context_analyzer_backup_integracao.py:453:                            distancia,
app/services/scene_context_analyzer_backup_integracao.py:467:    distancia_normalizada: float,
app/services/scene_context_analyzer_backup_integracao.py:476:    if distancia_normalizada <= 0.15:
app/services/scene_context_analyzer_backup_integracao.py:479:    if distancia_normalizada <= 0.30:
app/services/person_tracker_backup_antes_15s.py:10:    "entered",
app/services/person_tracker_backup_antes_15s.py:11:    "updated",
app/services/person_tracker_backup_antes_15s.py:12:    "suppressed",
app/services/person_tracker_backup_antes_15s.py:13:    "exited",
app/services/person_tracker_backup_antes_15s.py:47:    def iou(self, outra: "DetectionBox") -> float:
app/services/person_tracker_backup_antes_15s.py:85:    def distancia_centros(
app/services/person_tracker_backup_antes_15s.py:100:    pessoa_id: str
app/services/person_tracker_backup_antes_15s.py:111:class TrackingDecision:
app/services/person_tracker_backup_antes_15s.py:112:    pessoa_id: str
app/services/person_tracker_backup_antes_15s.py:121:class PersonTracker:
app/services/person_tracker_backup_antes_15s.py:126:        limite_iou: float = 0.25,
app/services/person_tracker_backup_antes_15s.py:127:        limite_distancia: float = 0.75,
app/services/person_tracker_backup_antes_15s.py:144:        self.limite_iou = limite_iou
app/services/person_tracker_backup_antes_15s.py:145:        self.limite_distancia = limite_distancia
app/services/person_tracker_backup_antes_15s.py:160:    ) -> TrackingDecision:
app/services/person_tracker_backup_antes_15s.py:181:                return TrackingDecision(
app/services/person_tracker_backup_antes_15s.py:182:                    pessoa_id=pessoa.pessoa_id,
app/services/person_tracker_backup_antes_15s.py:185:                    status="entered",
app/services/person_tracker_backup_antes_15s.py:207:                return TrackingDecision(
app/services/person_tracker_backup_antes_15s.py:208:                    pessoa_id=pessoa.pessoa_id,
app/services/person_tracker_backup_antes_15s.py:211:                    status="updated",
app/services/person_tracker_backup_antes_15s.py:219:            return TrackingDecision(
app/services/person_tracker_backup_antes_15s.py:220:                pessoa_id=pessoa.pessoa_id,
app/services/person_tracker_backup_antes_15s.py:223:                status="suppressed",
app/services/person_tracker_backup_antes_15s.py:234:    ) -> list[TrackingDecision]:
app/services/person_tracker_backup_antes_15s.py:243:                TrackingDecision
app/services/person_tracker_backup_antes_15s.py:248:            for pessoa_id, pessoa in self._pessoas.items():
app/services/person_tracker_backup_antes_15s.py:261:                    TrackingDecision(
app/services/person_tracker_backup_antes_15s.py:262:                        pessoa_id=pessoa.pessoa_id,
app/services/person_tracker_backup_antes_15s.py:267:                        status="exited",
app/services/person_tracker_backup_antes_15s.py:276:                ids_para_remover.append(pessoa_id)
app/services/person_tracker_backup_antes_15s.py:278:            for pessoa_id in ids_para_remover:
app/services/person_tracker_backup_antes_15s.py:280:                    pessoa_id,
app/services/person_tracker_backup_antes_15s.py:298:            sobreposicao = bbox.iou(pessoa.bbox)
app/services/person_tracker_backup_antes_15s.py:300:            distancia = bbox.distancia_centros(
app/services/person_tracker_backup_antes_15s.py:312:            distancia_normalizada = (
app/services/person_tracker_backup_antes_15s.py:313:                distancia / maior_dimensao
app/services/person_tracker_backup_antes_15s.py:317:                distancia_normalizada
app/services/person_tracker_backup_antes_15s.py:318:                <= self.limite_distancia
app/services/person_tracker_backup_antes_15s.py:322:                sobreposicao >= self.limite_iou
app/services/person_tracker_backup_antes_15s.py:331:            pontuacao_distancia = max(
app/services/person_tracker_backup_antes_15s.py:333:                1.0 - distancia_normalizada,
app/services/person_tracker_backup_antes_15s.py:338:                + pontuacao_distancia
app/services/person_tracker_backup_antes_15s.py:364:        pessoa_id = (
app/services/person_tracker_backup_antes_15s.py:370:            pessoa_id=pessoa_id,
app/services/person_tracker_backup_antes_15s.py:380:        self._pessoas[pessoa_id] = pessoa
app/services/person_tracker_backup_antes_15s.py:385:person_tracker = PersonTracker(
app/services/person_movement.py:9:from app.services.person_tracker import DetectionBox
app/services/person_movement.py:36:    pessoa_id: str
app/services/person_movement.py:40:    tendencia_distancia: TendenciaDistancia
app/services/person_movement.py:44:    distancia_pixels: float
app/services/person_movement.py:49:    distancia_total_pixels: float
app/services/person_movement.py:61:    pessoa_id: str
app/services/person_movement.py:69:    distancia_total_pixels: float
app/services/person_movement.py:120:        pessoa_id: str,
app/services/person_movement.py:124:        if not pessoa_id.strip():
app/services/person_movement.py:126:                "pessoa_id não pode ser vazio"
app/services/person_movement.py:146:                pessoa_id
app/services/person_movement.py:151:                    pessoa_id=pessoa_id,
app/services/person_movement.py:154:                self._estados[pessoa_id] = (
app/services/person_movement.py:156:                        pessoa_id=pessoa_id,
app/services/person_movement.py:161:                        distancia_total_pixels=0.0,
app/services/person_movement.py:178:            estado.distancia_total_pixels = (
app/services/person_movement.py:179:                analise.distancia_total_pixels
app/services/person_movement.py:188:        pessoa_id: str,
app/services/person_movement.py:192:                pessoa_id
app/services/person_movement.py:202:        pessoa_id: str,
app/services/person_movement.py:206:                pessoa_id,
app/services/person_movement.py:225:        pessoa_id: str,
app/services/person_movement.py:228:            pessoa_id=pessoa_id,
app/services/person_movement.py:231:            tendencia_distancia="inicial",
app/services/person_movement.py:234:            distancia_pixels=0.0,
app/services/person_movement.py:237:            distancia_total_pixels=0.0,
app/services/person_movement.py:264:        distancia_pixels = math.hypot(
app/services/person_movement.py:276:            distancia_pixels
app/services/person_movement.py:326:        tendencia_distancia = (
app/services/person_movement.py:327:            self._classificar_tendencia_distancia(
app/services/person_movement.py:337:        distancia_total = (
app/services/person_movement.py:338:            estado.distancia_total_pixels
app/services/person_movement.py:339:            + distancia_pixels
app/services/person_movement.py:355:            tendencia_distancia=(
app/services/person_movement.py:356:                tendencia_distancia
app/services/person_movement.py:362:            pessoa_id=estado.pessoa_id,
app/services/person_movement.py:369:            tendencia_distancia=(
app/services/person_movement.py:370:                tendencia_distancia
app/services/person_movement.py:380:            distancia_pixels=round(
app/services/person_movement.py:381:                distancia_pixels,
app/services/person_movement.py:392:            distancia_total_pixels=round(
app/services/person_movement.py:393:                distancia_total,
app/services/person_movement.py:432:    def _classificar_tendencia_distancia(
app/services/person_movement.py:453:        tendencia_distancia: TendenciaDistancia,
app/services/person_movement.py:507:            f"{tendencias[tendencia_distancia]}, "
app/services/scene_analyzer_backup_antes_cor_preta.py:291:    menor_distancia = math.inf
app/services/scene_analyzer_backup_antes_cor_preta.py:296:        distancia = math.sqrt(
app/services/scene_analyzer_backup_antes_cor_preta.py:311:        if distancia < menor_distancia:
app/services/scene_analyzer_backup_antes_cor_preta.py:312:            menor_distancia = distancia
app/services/person_tracker_backup_antes_lote.py:10:    "entered",
app/services/person_tracker_backup_antes_lote.py:11:    "updated",
app/services/person_tracker_backup_antes_lote.py:12:    "suppressed",
app/services/person_tracker_backup_antes_lote.py:13:    "exited",
app/services/person_tracker_backup_antes_lote.py:47:    def iou(self, outra: "DetectionBox") -> float:
app/services/person_tracker_backup_antes_lote.py:85:    def distancia_centros(
app/services/person_tracker_backup_antes_lote.py:100:    pessoa_id: str
app/services/person_tracker_backup_antes_lote.py:111:class TrackingDecision:
app/services/person_tracker_backup_antes_lote.py:112:    pessoa_id: str
app/services/person_tracker_backup_antes_lote.py:121:class PersonTracker:
app/services/person_tracker_backup_antes_lote.py:126:        limite_iou: float = 0.25,
app/services/person_tracker_backup_antes_lote.py:127:        limite_distancia: float = 0.75,
app/services/person_tracker_backup_antes_lote.py:144:        self.limite_iou = limite_iou
app/services/person_tracker_backup_antes_lote.py:145:        self.limite_distancia = limite_distancia
app/services/person_tracker_backup_antes_lote.py:160:    ) -> TrackingDecision:
app/services/person_tracker_backup_antes_lote.py:181:                return TrackingDecision(
app/services/person_tracker_backup_antes_lote.py:182:                    pessoa_id=pessoa.pessoa_id,
app/services/person_tracker_backup_antes_lote.py:185:                    status="entered",
app/services/person_tracker_backup_antes_lote.py:207:                return TrackingDecision(
app/services/person_tracker_backup_antes_lote.py:208:                    pessoa_id=pessoa.pessoa_id,
app/services/person_tracker_backup_antes_lote.py:211:                    status="updated",
app/services/person_tracker_backup_antes_lote.py:219:            return TrackingDecision(
app/services/person_tracker_backup_antes_lote.py:220:                pessoa_id=pessoa.pessoa_id,
app/services/person_tracker_backup_antes_lote.py:223:                status="suppressed",
app/services/person_tracker_backup_antes_lote.py:234:    ) -> list[TrackingDecision]:
app/services/person_tracker_backup_antes_lote.py:243:                TrackingDecision
app/services/person_tracker_backup_antes_lote.py:248:            for pessoa_id, pessoa in self._pessoas.items():
app/services/person_tracker_backup_antes_lote.py:261:                    TrackingDecision(
app/services/person_tracker_backup_antes_lote.py:262:                        pessoa_id=pessoa.pessoa_id,
app/services/person_tracker_backup_antes_lote.py:267:                        status="exited",
app/services/person_tracker_backup_antes_lote.py:276:                ids_para_remover.append(pessoa_id)
app/services/person_tracker_backup_antes_lote.py:278:            for pessoa_id in ids_para_remover:
app/services/person_tracker_backup_antes_lote.py:280:                    pessoa_id,
app/services/person_tracker_backup_antes_lote.py:298:            sobreposicao = bbox.iou(pessoa.bbox)
app/services/person_tracker_backup_antes_lote.py:300:            distancia = bbox.distancia_centros(
app/services/person_tracker_backup_antes_lote.py:312:            distancia_normalizada = (
app/services/person_tracker_backup_antes_lote.py:313:                distancia / maior_dimensao
app/services/person_tracker_backup_antes_lote.py:317:                distancia_normalizada
app/services/person_tracker_backup_antes_lote.py:318:                <= self.limite_distancia
app/services/person_tracker_backup_antes_lote.py:322:                sobreposicao >= self.limite_iou
app/services/person_tracker_backup_antes_lote.py:331:            pontuacao_distancia = max(
app/services/person_tracker_backup_antes_lote.py:333:                1.0 - distancia_normalizada,
app/services/person_tracker_backup_antes_lote.py:338:                + pontuacao_distancia
app/services/person_tracker_backup_antes_lote.py:364:        pessoa_id = (
app/services/person_tracker_backup_antes_lote.py:370:            pessoa_id=pessoa_id,
app/services/person_tracker_backup_antes_lote.py:380:        self._pessoas[pessoa_id] = pessoa
app/services/person_tracker_backup_antes_lote.py:385:person_tracker = PersonTracker(
app/services/camera_event_pipeline_backup_recorte.py:15:from app.services.person_tracker import (
app/services/camera_event_pipeline_backup_recorte.py:17:    TrackingDecision,
app/services/camera_event_pipeline_backup_recorte.py:18:    person_tracker,
app/services/camera_event_pipeline_backup_recorte.py:84:            await asyncio.wait_for(self.queue.join(), timeout=10)
app/services/camera_event_pipeline_backup_recorte.py:103:        await person_tracker.limpar()
app/services/camera_event_pipeline_backup_recorte.py:119:                saidas = await person_tracker.coletar_saidas()
app/services/camera_event_pipeline_backup_recorte.py:123:                        saida.pessoa_id
app/services/camera_event_pipeline_backup_recorte.py:126:                        saida.pessoa_id
app/services/camera_event_pipeline_backup_recorte.py:135:                        "pessoa_id": saida.pessoa_id,
app/services/camera_event_pipeline_backup_recorte.py:136:                        "status": "exited",
app/services/camera_event_pipeline_backup_recorte.py:161:                        saida.pessoa_id,
app/services/camera_event_pipeline_backup_recorte.py:262:        pessoa_id: str,
app/services/camera_event_pipeline_backup_recorte.py:282:                pessoa_id=pessoa_id,
app/services/camera_event_pipeline_backup_recorte.py:289:                pessoa_id,
app/services/camera_event_pipeline_backup_recorte.py:301:                pessoa_id,
app/services/camera_event_pipeline_backup_recorte.py:387:        decisao: TrackingDecision,
app/services/camera_event_pipeline_backup_recorte.py:398:            "pessoa_id": decisao.pessoa_id,
app/services/camera_event_pipeline_backup_recorte.py:399:            "status": "appearance_updated",
app/services/camera_event_pipeline_backup_recorte.py:434:            decisao.pessoa_id,
app/services/camera_event_pipeline_backup_recorte.py:471:        decisao: TrackingDecision,
app/services/camera_event_pipeline_backup_recorte.py:482:            pessoa_id=decisao.pessoa_id,
app/services/camera_event_pipeline_backup_recorte.py:487:            pessoa_id=decisao.pessoa_id,
app/services/camera_event_pipeline_backup_recorte.py:492:            "tendencia=%s velocidade=%spx_s distancia_total=%spx "
app/services/camera_event_pipeline_backup_recorte.py:495:            decisao.pessoa_id,
app/services/camera_event_pipeline_backup_recorte.py:498:            movimento.tendencia_distancia,
app/services/camera_event_pipeline_backup_recorte.py:500:            movimento.distancia_total_pixels,
app/services/camera_event_pipeline_backup_recorte.py:520:                decisao.pessoa_id,
app/services/camera_event_pipeline_backup_recorte.py:553:                decisao.pessoa_id,
app/services/camera_event_pipeline_backup_recorte.py:560:            "pessoa_id": decisao.pessoa_id,
app/services/camera_event_pipeline_backup_recorte.py:593:            decisao.pessoa_id,
app/services/camera_event_pipeline_backup_recorte.py:604:                decisao.pessoa_id,
app/services/camera_event_pipeline_backup_recorte.py:619:            decisao.pessoa_id,
app/services/camera_event_pipeline_backup_recorte.py:665:                caixas_rastreamento = [
app/services/camera_event_pipeline_backup_recorte.py:674:                decisoes = await person_tracker.registrar_lote(
app/services/camera_event_pipeline_backup_recorte.py:677:                    bboxes=caixas_rastreamento,
app/services/camera_event_pipeline_backup_recorte.py:691:                        (decisao.pessoa_id, decisao.status)
app/services/scene_analyzer_backup_antes_cores.py:445:    menor_distancia = math.inf
app/services/scene_analyzer_backup_antes_cores.py:450:        distancia = math.sqrt(
app/services/scene_analyzer_backup_antes_cores.py:465:        if distancia < menor_distancia:
app/services/scene_analyzer_backup_antes_cores.py:466:            menor_distancia = distancia
app/messaging/kafka_producer.py:80:            "request_timeout_ms": (
app/messaging/kafka_producer.py:81:                settings.kafka_request_timeout_ms
app/messaging/kafka_producer_melhorado.py:82:            "request_timeout_ms": (
app/messaging/kafka_producer_melhorado.py:83:                settings.kafka_request_timeout_ms
app/core/config_backup_antes_integracao.p:17:    kafka_request_timeout_ms: int = 10000
app/core/config.py:17:    kafka_request_timeout_ms: int = 10000
app/static/index_backup_analise_visual.html:153:        .evento-status.entered {
app/static/index_backup_analise_visual.html:158:        .evento-status.updated {
app/static/index_backup_analise_visual.html:163:        .evento-status.exited {
app/static/index_backup_analise_visual.html:285:                entered: "Entrou",
app/static/index_backup_analise_visual.html:286:                updated: "Atualizado",
app/static/index_backup_analise_visual.html:287:                suppressed: "Repetido",
app/static/index_backup_analise_visual.html:288:                exited: "Saiu"
app/static/index_backup_analise_visual.html:325:                if (novoStatus === "exited") {
app/static/index_backup_analise_visual.html:336:                statusAnterior !== "exited"
app/static/index_backup_analise_visual.html:337:                && novoStatus === "exited"
app/static/index_backup_analise_visual.html:352:                statusAnterior === "exited"
app/static/index_backup_analise_visual.html:353:                && novoStatus !== "exited"
app/static/index_backup_analise_visual.html:449:            if (evento.status === "suppressed") {
app/static/index_backup_analise_visual.html:454:                evento.pessoa_id
app/static/index_backup_analise_visual.html:474:                evento.status === "exited"
app/static/index_backup_analise_visual.html:481:                evento.status || "entered";
app/static/index_backup_analise_visual.html:672:            if (evento.status === "exited") {
app/static/index_backup_analise_visual.html:684:                    evento.pessoa_id || "-"
app/static/index_backup_analise_visual.html:697:            if (evento.status === "updated") {
app/static/index_backup_analise_visual.html:717:            if (evento.status === "exited") {
app/static/index_backup_analise_visual.html:740:                    if (statusRemovido === "exited") {
app/static/index.html:195:        .evento-status.entered {
app/static/index.html:200:        .evento-status.updated {
app/static/index.html:205:        .evento-status.exited {
app/static/index.html:394:                entered: "Entrou",
app/static/index.html:395:                updated: "Atualizado",
app/static/index.html:396:                suppressed: "Repetido",
app/static/index.html:397:                exited: "Saiu"
app/static/index.html:433:                if (novoStatus === "exited") {
app/static/index.html:441:                statusAnterior !== "exited"
app/static/index.html:442:                && novoStatus === "exited"
app/static/index.html:453:                statusAnterior === "exited"
app/static/index.html:454:                && novoStatus !== "exited"
app/static/index.html:561:                === "appearance_updated"
app/static/index.html:572:                === "suppressed"
app/static/index.html:578:                evento.pessoa_id
app/static/index.html:596:                evento.status === "exited"
app/static/index.html:604:                evento.status || "entered"
app/static/index.html:747:                    '[data-campo="movimento-distancia"]'
app/static/index.html:792:                evento.pessoa_id
app/static/index.html:1185:                    "movimento-distancia"
app/static/index.html:1566:                            .tendencia_distancia
app/static/index.html:1583:                            .distancia_total_pixels
app/static/index.html:1761:                evento.status === "exited"
app/static/index.html:1777:                    evento.pessoa_id || "-"
app/static/index.html:1813:                evento.status === "updated"
app/static/index.html:1834:                evento.status === "exited"
app/static/index.html:1866:                        === "exited"
app/static/index_backup_antes_contexto_cena.html:153:        .evento-status.entered {
app/static/index_backup_antes_contexto_cena.html:158:        .evento-status.updated {
app/static/index_backup_antes_contexto_cena.html:163:        .evento-status.exited {
app/static/index_backup_antes_contexto_cena.html:329:                entered: "Entrou",
app/static/index_backup_antes_contexto_cena.html:330:                updated: "Atualizado",
app/static/index_backup_antes_contexto_cena.html:331:                suppressed: "Repetido",
app/static/index_backup_antes_contexto_cena.html:332:                exited: "Saiu"
app/static/index_backup_antes_contexto_cena.html:368:                if (novoStatus === "exited") {
app/static/index_backup_antes_contexto_cena.html:376:                statusAnterior !== "exited"
app/static/index_backup_antes_contexto_cena.html:377:                && novoStatus === "exited"
app/static/index_backup_antes_contexto_cena.html:388:                statusAnterior === "exited"
app/static/index_backup_antes_contexto_cena.html:389:                && novoStatus !== "exited"
app/static/index_backup_antes_contexto_cena.html:501:                === "appearance_updated"
app/static/index_backup_antes_contexto_cena.html:512:                === "suppressed"
app/static/index_backup_antes_contexto_cena.html:518:                evento.pessoa_id
app/static/index_backup_antes_contexto_cena.html:536:                evento.status === "exited"
app/static/index_backup_antes_contexto_cena.html:544:                evento.status || "entered"
app/static/index_backup_antes_contexto_cena.html:569:                evento.pessoa_id
app/static/index_backup_antes_contexto_cena.html:1082:                evento.status === "exited"
app/static/index_backup_antes_contexto_cena.html:1097:                    evento.pessoa_id || "-"
app/static/index_backup_antes_contexto_cena.html:1127:                evento.status === "updated"
app/static/index_backup_antes_contexto_cena.html:1148:                evento.status === "exited"
app/static/index_backup_antes_contexto_cena.html:1180:                        === "exited"
app/static/index_backup_atualizacao_visual.html:153:        .evento-status.entered {
app/static/index_backup_atualizacao_visual.html:158:        .evento-status.updated {
app/static/index_backup_atualizacao_visual.html:163:        .evento-status.exited {
app/static/index_backup_atualizacao_visual.html:329:                entered: "Entrou",
app/static/index_backup_atualizacao_visual.html:330:                updated: "Atualizado",
app/static/index_backup_atualizacao_visual.html:331:                suppressed: "Repetido",
app/static/index_backup_atualizacao_visual.html:332:                exited: "Saiu"
app/static/index_backup_atualizacao_visual.html:368:                if (novoStatus === "exited") {
app/static/index_backup_atualizacao_visual.html:376:                statusAnterior !== "exited"
app/static/index_backup_atualizacao_visual.html:377:                && novoStatus === "exited"
app/static/index_backup_atualizacao_visual.html:388:                statusAnterior === "exited"
app/static/index_backup_atualizacao_visual.html:389:                && novoStatus !== "exited"
app/static/index_backup_atualizacao_visual.html:496:                === "suppressed"
app/static/index_backup_atualizacao_visual.html:502:                evento.pessoa_id
app/static/index_backup_atualizacao_visual.html:520:                evento.status === "exited"
app/static/index_backup_atualizacao_visual.html:528:                evento.status || "entered"
app/static/index_backup_atualizacao_visual.html:897:                evento.status === "exited"
app/static/index_backup_atualizacao_visual.html:907:                `Pessoa: ${evento.pessoa_id || "-"}`;
app/static/index_backup_atualizacao_visual.html:991:                evento.status === "updated"
app/static/index_backup_atualizacao_visual.html:1012:                evento.status === "exited"
app/static/index_backup_atualizacao_visual.html:1044:                        === "exited"
app/static/index_backup_recorte.html:195:        .evento-status.entered {
app/static/index_backup_recorte.html:200:        .evento-status.updated {
app/static/index_backup_recorte.html:205:        .evento-status.exited {
app/static/index_backup_recorte.html:394:                entered: "Entrou",
app/static/index_backup_recorte.html:395:                updated: "Atualizado",
app/static/index_backup_recorte.html:396:                suppressed: "Repetido",
app/static/index_backup_recorte.html:397:                exited: "Saiu"
app/static/index_backup_recorte.html:433:                if (novoStatus === "exited") {
app/static/index_backup_recorte.html:441:                statusAnterior !== "exited"
app/static/index_backup_recorte.html:442:                && novoStatus === "exited"
app/static/index_backup_recorte.html:453:                statusAnterior === "exited"
app/static/index_backup_recorte.html:454:                && novoStatus !== "exited"
app/static/index_backup_recorte.html:561:                === "appearance_updated"
app/static/index_backup_recorte.html:572:                === "suppressed"
app/static/index_backup_recorte.html:578:                evento.pessoa_id
app/static/index_backup_recorte.html:596:                evento.status === "exited"
app/static/index_backup_recorte.html:604:                evento.status || "entered"
app/static/index_backup_recorte.html:747:                    '[data-campo="movimento-distancia"]'
app/static/index_backup_recorte.html:792:                evento.pessoa_id
app/static/index_backup_recorte.html:1178:                    "movimento-distancia"
app/static/index_backup_recorte.html:1559:                            .tendencia_distancia
app/static/index_backup_recorte.html:1576:                            .distancia_total_pixels
app/static/index_backup_recorte.html:1754:                evento.status === "exited"
app/static/index_backup_recorte.html:1770:                    evento.pessoa_id || "-"
app/static/index_backup_recorte.html:1806:                evento.status === "updated"
app/static/index_backup_recorte.html:1827:                evento.status === "exited"
app/static/index_backup_recorte.html:1859:                        === "exited"
app/static/index_backup_antes_duas_imagens.html:153:        .evento-status.entered {
app/static/index_backup_antes_duas_imagens.html:158:        .evento-status.updated {
app/static/index_backup_antes_duas_imagens.html:163:        .evento-status.exited {
app/static/index_backup_antes_duas_imagens.html:336:                entered: "Entrou",
app/static/index_backup_antes_duas_imagens.html:337:                updated: "Atualizado",
app/static/index_backup_antes_duas_imagens.html:338:                suppressed: "Repetido",
app/static/index_backup_antes_duas_imagens.html:339:                exited: "Saiu"
app/static/index_backup_antes_duas_imagens.html:375:                if (novoStatus === "exited") {
app/static/index_backup_antes_duas_imagens.html:383:                statusAnterior !== "exited"
app/static/index_backup_antes_duas_imagens.html:384:                && novoStatus === "exited"
app/static/index_backup_antes_duas_imagens.html:395:                statusAnterior === "exited"
app/static/index_backup_antes_duas_imagens.html:396:                && novoStatus !== "exited"
app/static/index_backup_antes_duas_imagens.html:510:                === "appearance_updated"
app/static/index_backup_antes_duas_imagens.html:521:                === "suppressed"
app/static/index_backup_antes_duas_imagens.html:527:                evento.pessoa_id
app/static/index_backup_antes_duas_imagens.html:545:                evento.status === "exited"
app/static/index_backup_antes_duas_imagens.html:553:                evento.status || "entered"
app/static/index_backup_antes_duas_imagens.html:662:                evento.pessoa_id
app/static/index_backup_antes_duas_imagens.html:1245:                evento.status === "exited"
app/static/index_backup_antes_duas_imagens.html:1261:                    evento.pessoa_id || "-"
app/static/index_backup_antes_duas_imagens.html:1295:                evento.status === "updated"
app/static/index_backup_antes_duas_imagens.html:1316:                evento.status === "exited"
app/static/index_backup_antes_duas_imagens.html:1348:                        === "exited"
app/static/index_backup_antes_analise.html:153:        .evento-status.entered {
app/static/index_backup_antes_analise.html:158:        .evento-status.updated {
app/static/index_backup_antes_analise.html:163:        .evento-status.exited {
app/static/index_backup_antes_analise.html:285:                entered: "Entrou",
app/static/index_backup_antes_analise.html:286:                updated: "Atualizado",
app/static/index_backup_antes_analise.html:287:                suppressed: "Repetido",
app/static/index_backup_antes_analise.html:288:                exited: "Saiu"
app/static/index_backup_antes_analise.html:325:                if (novoStatus === "exited") {
app/static/index_backup_antes_analise.html:336:                statusAnterior !== "exited"
app/static/index_backup_antes_analise.html:337:                && novoStatus === "exited"
app/static/index_backup_antes_analise.html:352:                statusAnterior === "exited"
app/static/index_backup_antes_analise.html:353:                && novoStatus !== "exited"
app/static/index_backup_antes_analise.html:449:            if (evento.status === "suppressed") {
app/static/index_backup_antes_analise.html:454:                evento.pessoa_id
app/static/index_backup_antes_analise.html:474:                evento.status === "exited"
app/static/index_backup_antes_analise.html:481:                evento.status || "entered";
app/static/index_backup_antes_analise.html:672:            if (evento.status === "exited") {
app/static/index_backup_antes_analise.html:684:                    evento.pessoa_id || "-"
app/static/index_backup_antes_analise.html:697:            if (evento.status === "updated") {
app/static/index_backup_antes_analise.html:717:            if (evento.status === "exited") {
app/static/index_backup_antes_analise.html:740:                    if (statusRemovido === "exited") {
app/static/index_backup_antes_movimento.html:195:        .evento-status.entered {
app/static/index_backup_antes_movimento.html:200:        .evento-status.updated {
app/static/index_backup_antes_movimento.html:205:        .evento-status.exited {
app/static/index_backup_antes_movimento.html:389:                entered: "Entrou",
app/static/index_backup_antes_movimento.html:390:                updated: "Atualizado",
app/static/index_backup_antes_movimento.html:391:                suppressed: "Repetido",
app/static/index_backup_antes_movimento.html:392:                exited: "Saiu"
app/static/index_backup_antes_movimento.html:428:                if (novoStatus === "exited") {
app/static/index_backup_antes_movimento.html:436:                statusAnterior !== "exited"
app/static/index_backup_antes_movimento.html:437:                && novoStatus === "exited"
app/static/index_backup_antes_movimento.html:448:                statusAnterior === "exited"
app/static/index_backup_antes_movimento.html:449:                && novoStatus !== "exited"
app/static/index_backup_antes_movimento.html:556:                === "appearance_updated"
app/static/index_backup_antes_movimento.html:567:                === "suppressed"
app/static/index_backup_antes_movimento.html:573:                evento.pessoa_id
app/static/index_backup_antes_movimento.html:591:                evento.status === "exited"
app/static/index_backup_antes_movimento.html:599:                evento.status || "entered"
app/static/index_backup_antes_movimento.html:751:                evento.pessoa_id
app/static/index_backup_antes_movimento.html:1480:                evento.status === "exited"
app/static/index_backup_antes_movimento.html:1496:                    evento.pessoa_id || "-"
app/static/index_backup_antes_movimento.html:1525:                evento.status === "updated"
app/static/index_backup_antes_movimento.html:1546:                evento.status === "exited"
app/static/index_backup_antes_movimento.html:1578:                        === "exited"
app/static/index_backup_antes_contadores.html:153:        .evento-status.entered {
app/static/index_backup_antes_contadores.html:158:        .evento-status.updated {
app/static/index_backup_antes_contadores.html:163:        .evento-status.exited {
app/static/index_backup_antes_contadores.html:245:                entered: "Entrou",
app/static/index_backup_antes_contadores.html:246:                updated: "Atualizado",
app/static/index_backup_antes_contadores.html:247:                suppressed: "Repetido",
app/static/index_backup_antes_contadores.html:248:                exited: "Saiu"
app/static/index_backup_antes_contadores.html:325:            if (evento.status === "suppressed") {
app/static/index_backup_antes_contadores.html:330:                evento.pessoa_id
app/static/index_backup_antes_contadores.html:341:                evento.status === "exited"
app/static/index_backup_antes_contadores.html:538:            if (evento.status === "exited") {
app/static/index_backup_antes_contadores.html:550:                    evento.pessoa_id || "-"
app/static/index_backup_antes_contadores.html:563:            if (evento.status === "updated") {
app/static/index_backup_antes_contadores.html:580:            if (evento.status === "exited") {

===== ARQUIVOS RELACIONADOS AO RASTREAMENTO =====

------------------------------------------------------------
ARQUIVO: app/services/appearance_memory.py
------------------------------------------------------------
import asyncio
from collections import Counter
from dataclasses import asdict, dataclass, field

from app.services.scene_analyzer import (
    PersonVisualAnalysis,
)


@dataclass(frozen=True)
class StableAppearance:
    cor_roupa_predominante: str

    rgb_medio: tuple[
        int,
        int,
        int,
    ]

    posicao_atual: str
    tamanho_predominante: str
    percentual_medio_quadro: float
    quantidade_amostras: int
    descricao: str

    def to_dict(self) -> dict:
        dados = asdict(self)

        dados["rgb_medio"] = list(
            self.rgb_medio
        )

        return dados


@dataclass
class _AppearanceState:
    cores: Counter[str] = field(
        default_factory=Counter
    )

    tamanhos: Counter[str] = field(
        default_factory=Counter
    )

    soma_vermelho: int = 0
    soma_verde: int = 0
    soma_azul: int = 0

    soma_percentual: float = 0.0
    quantidade_amostras: int = 0

    ultima_cor: str = "indefinida"

    # Cor apresentada como resultado estável.
    # É separada da última leitura para evitar
    # alternância causada por iluminação e sombra.
    cor_estavel: str = "indefinida"

    ultima_posicao: str = "centro"
    ultimo_tamanho: str = "medio"


class AppearanceMemory:
    def __init__(self) -> None:
        self._sessoes: dict[
            str,
            _AppearanceState,
        ] = {}

        self._lock = asyncio.Lock()

    @property
    def quantidade_sessoes(self) -> int:
        return len(self._sessoes)

    async def registrar(
        self,
        pessoa_id: str,
        analise: PersonVisualAnalysis,
    ) -> StableAppearance:
        if not pessoa_id:
            raise ValueError(
                "pessoa_id não pode ser vazio"
            )

        async with self._lock:
            estado = self._sessoes.get(
                pessoa_id
            )

            if estado is None:
                estado = _AppearanceState()

                self._sessoes[
                    pessoa_id
                ] = estado

            estado.cores[
                analise.cor_roupa_aproximada
            ] += 1

            estado.tamanhos[
                analise.tamanho_no_quadro
            ] += 1

            vermelho, verde, azul = (
                analise.rgb_representativo
            )

            estado.soma_vermelho += vermelho
            estado.soma_verde += verde
            estado.soma_azul += azul

            estado.soma_percentual += (
                analise.percentual_quadro
            )

            estado.quantidade_amostras += 1

            estado.ultima_cor = (
                analise.cor_roupa_aproximada
            )

            estado.ultima_posicao = (
                analise.posicao_horizontal
            )

            estado.ultimo_tamanho = (
                analise.tamanho_no_quadro
            )

            return self._montar_resultado(
                estado
            )

    async def obter(
        self,
        pessoa_id: str,
    ) -> StableAppearance | None:
        async with self._lock:
            estado = self._sessoes.get(
                pessoa_id
            )

            if estado is None:
                return None

            return self._montar_resultado(
                estado
            )

    async def finalizar(
        self,
        pessoa_id: str,
    ) -> StableAppearance | None:
        """
        Retorna a aparência final e remove a sessão
        daquela pessoa da memória.
        """

        async with self._lock:
            estado = self._sessoes.pop(
                pessoa_id,
                None,
            )

            if estado is None:
                return None

            return self._montar_resultado(
                estado
            )

    async def limpar(self) -> None:
        async with self._lock:
            self._sessoes.clear()

    def _montar_resultado(
        self,
        estado: _AppearanceState,
    ) -> StableAppearance:
        quantidade = max(
            1,
            estado.quantidade_amostras,
        )

        cor_calculada = (
            self._selecionar_cor_predominante(
                estado
            )
        )

        maior_quantidade_cor = max(
            estado.cores.values(),
            default=0,
        )

        # Confirma a cor depois de duas leituras
        # iguais. Com mais amostras, exige pelo menos
        # 60% de concordância.
        cor_consistente = (
            quantidade >= 2
            and maior_quantidade_cor >= 2
            and (
                maior_quantidade_cor * 5
                >= quantidade * 3
            )
        )

        # A roupa não muda durante uma passagem curta.
        # Depois que a cor fica estável, leituras
        # isoladas diferentes não apagam o resultado.
        if (
            estado.cor_estavel
            != "indefinida"
        ):
            cor_predominante = (
                estado.cor_estavel
            )

        elif cor_consistente:
            cor_predominante = (
                cor_calculada
            )

            estado.cor_estavel = (
                cor_predominante
            )

        else:
            cor_predominante = (
                "indefinida"
            )

        tamanho_predominante = (
            self._selecionar_predominante(
                contagem=estado.tamanhos,
                valor_mais_recente=(
                    estado.ultimo_tamanho
                ),
            )
        )

        rgb_medio = (
            round(
                estado.soma_vermelho
                / quantidade
            ),
            round(
                estado.soma_verde
                / quantidade
            ),
            round(
                estado.soma_azul
                / quantidade
            ),
        )

        percentual_medio = round(
            estado.soma_percentual
            / quantidade,
            2,
        )

        descricao = self._montar_descricao(
            cor=cor_predominante,
            posicao=estado.ultima_posicao,
            tamanho=tamanho_predominante,
            quantidade=quantidade,
        )

        return StableAppearance(
            cor_roupa_predominante=(
                cor_predominante
            ),
            rgb_medio=rgb_medio,
            posicao_atual=(
                estado.ultima_posicao
            ),
            tamanho_predominante=(
                tamanho_predominante
            ),
            percentual_medio_quadro=(
                percentual_medio
            ),
            quantidade_amostras=quantidade,
            descricao=descricao,
        )

    def _selecionar_cor_predominante(
        self,
        estado: _AppearanceState,
    ) -> str:
        """
        Estabiliza a classificação de roupas escuras
        que alternam entre preta e uma cor escura
        devido à iluminação, distância ou sombra.
        """

        contagem = estado.cores

        if not contagem:
            return estado.ultima_cor

        resultado_padrao = (
            self._selecionar_predominante(
                contagem=contagem,
                valor_mais_recente=(
                    estado.ultima_cor
                ),
            )
        )

        cores_escuras = (
            "azul-escura",
            "verde-escura",
            "vermelha-escura",
            "roxa-escura",
        )

        quantidade_preta = contagem.get(
            "preta",
            0,
        )

        candidatas = [
            (
                cor,
                contagem.get(cor, 0),
            )
            for cor in cores_escuras
            if contagem.get(cor, 0) > 0
        ]

        if (
            quantidade_preta == 0
            or not candidatas
        ):
            return resultado_padrao

        maior_quantidade_escura = max(
            quantidade
            for _, quantidade in candidatas
        )

        empatadas = [
            cor
            for cor, quantidade in candidatas
            if (
                quantidade
                == maior_quantidade_escura
            )
        ]

        if estado.cor_estavel in empatadas:
            cor_escura = estado.cor_estavel
        elif estado.ultima_cor in empatadas:
            cor_escura = estado.ultima_cor
        else:
            cor_escura = empatadas[0]

        cores_do_conflito = (
            set(cores_escuras)
            | {"preta"}
        )

        maior_outra_cor = max(
            (
                quantidade
                for cor, quantidade
                in contagem.items()
                if cor not in cores_do_conflito
            ),
            default=0,
        )

        if maior_outra_cor > max(
            quantidade_preta,
            maior_quantidade_escura,
        ):
            return resultado_padrao

        # Já estabilizou em azul-escura, verde-escura
        # ou outra cor escura. Um quadro preto isolado
        # não deve alterar imediatamente o resultado.
        if estado.cor_estavel in cores_escuras:
            cor_estavel = estado.cor_estavel

            quantidade_cor_estavel = (
                contagem.get(
                    cor_estavel,
                    0,
                )
            )

            # Permite mudar entre cores escuras apenas
            # quando a nova cor estiver duas amostras
            # à frente da atual.
            if (
                cor_escura != cor_estavel
                and maior_quantidade_escura
                >= quantidade_cor_estavel + 2
            ):
                cor_estavel = cor_escura
                quantidade_cor_estavel = (
                    maior_quantidade_escura
                )

            # Volta para preta somente quando as
            # leituras pretas estiverem duas amostras
            # à frente da cor escura estabilizada.
            if (
                quantidade_preta
                >= quantidade_cor_estavel + 2
            ):
                return "preta"

            return cor_estavel

        # Estava estabilizado como preto. Exige duas
        # leituras da cor escura e empate ou vantagem
        # para realizar a mudança.
        if estado.cor_estavel == "preta":
            if (
                maior_quantidade_escura >= 2
                and maior_quantidade_escura
                >= quantidade_preta
            ):
                return cor_escura

            return "preta"

        # Estado inicial, antes de uma cor estável.
        if (
            maior_quantidade_escura >= 2
            and maior_quantidade_escura
            >= quantidade_preta
        ):
            return cor_escura

        return resultado_padrao


    @staticmethod
    def _selecionar_predominante(
        contagem: Counter[str],
        valor_mais_recente: str,
    ) -> str:
        if not contagem:
            return valor_mais_recente

        maior_quantidade = max(
            contagem.values()
        )

        empatados = [
            valor
            for valor, quantidade
            in contagem.items()
            if quantidade == maior_quantidade
        ]

        # Em caso de empate, utiliza o valor
        # observado mais recentemente.
        if valor_mais_recente in empatados:
            return valor_mais_recente

        return empatados[0]

    @staticmethod
    def _montar_descricao(
        cor: str,
        posicao: str,
        tamanho: str,
        quantidade: int,
    ) -> str:
        posicoes = {
            "esquerda": "à esquerda",
            "centro": "no centro",
            "direita": "à direita",
        }

        tamanhos = {
            "pequeno": "pequeno",
            "medio": "médio",
            "grande": "grande",
        }

        posicao_formatada = posicoes.get(
            posicao,
            posicao,
        )

        tamanho_formatado = tamanhos.get(
            tamanho,
            tamanho,
        )

        return (
            "Pessoa com roupa predominantemente "
            f"{cor}, localizada atualmente "
            f"{posicao_formatada} da cena, "
            f"com tamanho aparente "
            f"{tamanho_formatado}. "
            f"Resultado baseado em "
            f"{quantidade} amostra(s)."
        )


appearance_memory = AppearanceMemory()
------------------------------------------------------------
ARQUIVO: app/services/camera_event_pipeline_backup_amostras_repetidas.py
------------------------------------------------------------
import asyncio
import logging
from datetime import datetime, timezone

from app.adapters.cameras.factory import (
    camera_adapter_factory,
)
from app.core.config import settings
from app.domain.models.camera_event import (
    RawCameraPackage,
)
from app.messaging.kafka_producer import (
    KafkaPublisher,
)
from app.services.alarm_detection_payload import (
    montar_alarm_detection_message,
)
from app.services.appearance_memory import (
    appearance_memory,
)
from app.services.event_hub import event_hub
from app.services.person_tracker import (
    DetectionBox,
    person_tracker,
)
from app.services.scene_analyzer import (
    analisar_pessoa,
)


logger = logging.getLogger(__name__)


class CameraEventPipeline:
    def __init__(
        self,
        publisher: KafkaPublisher,
        topic: str,
        maxsize: int = 1000,
        worker_count: int = 4,
    ) -> None:
        self.publisher = publisher
        self.topic = topic
        self.worker_count = worker_count

        self.queue: asyncio.Queue[
            tuple[RawCameraPackage, bytes]
        ] = asyncio.Queue(maxsize=maxsize)

        self._worker_tasks: list[
            asyncio.Task
        ] = []

        self._exit_task: (
            asyncio.Task | None
        ) = None

        self._iniciado = False

    @property
    def tamanho_fila(self) -> int:
        return self.queue.qsize()

    @property
    def capacidade_fila(self) -> int:
        return self.queue.maxsize

    async def start(self) -> None:
        if self._iniciado:
            return

        if settings.kafka_enabled:
            await self.publisher.start()
        else:
            logger.warning(
                "Kafka desativado: eventos serão "
                "processados, mas não publicados."
            )

        self._worker_tasks = [
            asyncio.create_task(
                self._worker(numero),
                name=f"camera-worker-{numero}",
            )
            for numero in range(
                1,
                self.worker_count + 1,
            )
        ]

        self._exit_task = asyncio.create_task(
            self._monitorar_saidas(),
            name="person-exit-monitor",
        )

        self._iniciado = True

        logger.info(
            "Pipeline iniciado com %s workers",
            self.worker_count,
        )

    async def stop(self) -> None:
        if not self._iniciado:
            return

        try:
            await asyncio.wait_for(
                self.queue.join(),
                timeout=10,
            )

        except TimeoutError:
            logger.warning(
                "Encerrando com %s itens na fila",
                self.queue.qsize(),
            )

        tarefas: list[asyncio.Task] = [
            *self._worker_tasks,
        ]

        if self._exit_task is not None:
            tarefas.append(
                self._exit_task
            )

        for tarefa in tarefas:
            tarefa.cancel()

        if tarefas:
            await asyncio.gather(
                *tarefas,
                return_exceptions=True,
            )

        if self.publisher.iniciado:
            await self.publisher.stop()

        await appearance_memory.limpar()

        self._worker_tasks.clear()
        self._exit_task = None
        self._iniciado = False

        logger.info(
            "Pipeline encerrado"
        )

    def adicionar(
        self,
        pacote: RawCameraPackage,
        body: bytes,
    ) -> None:
        self.queue.put_nowait(
            (pacote, body)
        )

    async def _monitorar_saidas(
        self,
    ) -> None:
        logger.info(
            "Monitor automático de saídas iniciado"
        )

        while True:
            try:
                await asyncio.sleep(1)

                saidas = (
                    await person_tracker
                    .coletar_saidas()
                )

                for saida in saidas:
                    aparencia_final = (
                        await appearance_memory
                        .finalizar(
                            saida.pessoa_id
                        )
                    )

                    momento_saida = (
                        datetime.now(
                            timezone.utc
                        )
                        .isoformat()
                        .replace(
                            "+00:00",
                            "Z",
                        )
                    )

                    evento_painel = {
                        "evento_id": (
                            saida.evento_id
                        ),
                        "pessoa_id": (
                            saida.pessoa_id
                        ),
                        "status": "exited",
                        "quantidade_deteccoes": (
                            saida
                            .quantidade_deteccoes
                        ),
                        "camera": saida.camera,
                        "tipo": None,
                        "datetime": momento_saida,
                        "imagem": None,
                        "aparencia": (
                            aparencia_final.to_dict()
                            if aparencia_final
                            is not None
                            else None
                        ),
                    }

                    await event_hub.publicar(
                        evento_painel
                    )

                    logger.info(
                        "[RASTREAMENTO] Pessoa saiu: "
                        "pessoa=%s camera=%s "
                        "deteccoes=%s amostras=%s "
                        "paineis=%s",
                        saida.pessoa_id,
                        saida.camera,
                        (
                            saida
                            .quantidade_deteccoes
                        ),
                        (
                            aparencia_final
                            .quantidade_amostras
                            if aparencia_final
                            is not None
                            else 0
                        ),
                        event_hub.total_conexoes,
                    )

            except asyncio.CancelledError:
                logger.info(
                    "Monitor automático de "
                    "saídas encerrado"
                )
                raise

            except Exception:
                logger.exception(
                    "Erro no monitor automático "
                    "de saídas"
                )

    async def _worker(
        self,
        numero: int,
    ) -> None:
        logger.info(
            "Worker %s iniciado",
            numero,
        )

        while True:
            pacote, body = (
                await self.queue.get()
            )

            try:
                adapter = (
                    camera_adapter_factory
                    .encontrar_adapter(
                        content_type=(
                            pacote.content_type
                        ),
                        body=body,
                    )
                )

                logger.info(
                    "[%s] Worker %s usando %s",
                    pacote.evento_id,
                    numero,
                    adapter.__class__.__name__,
                )

                evento = await asyncio.to_thread(
                    adapter.normalizar,
                    pacote,
                    body,
                )

                if evento.imagem is None:
                    logger.info(
                        "[%s] Evento ignorado: "
                        "não possui imagem",
                        pacote.evento_id,
                    )
                    continue

                bounding_box = (
                    evento
                    .bounding_box_escolhida
                )

                if bounding_box is None:
                    logger.info(
                        "[%s] Evento ignorado: "
                        "não possui bounding box",
                        pacote.evento_id,
                    )
                    continue

                camera = (
                    evento.nome_camera
                    or evento.camera_id
                    or evento.ip_camera
                    or "Camera desconhecida"
                )

                caixa_rastreamento = (
                    DetectionBox(
                        x=bounding_box.x,
                        y=bounding_box.y,
                        largura=(
                            bounding_box.largura
                        ),
                        altura=(
                            bounding_box.altura
                        ),
                    )
                )

                decisao = (
                    await person_tracker.registrar(
                        camera=camera,
                        evento_id=(
                            evento.evento_id
                        ),
                        bbox=(
                            caixa_rastreamento
                        ),
                    )
                )

                logger.info(
                    "[%s] Rastreamento: "
                    "pessoa=%s status=%s "
                    "deteccoes=%s",
                    pacote.evento_id,
                    decisao.pessoa_id,
                    decisao.status,
                    (
                        decisao
                        .quantidade_deteccoes
                    ),
                )

                # Eventos repetidos em intervalos
                # curtos param aqui.
                if not decisao.deve_processar:
                    logger.info(
                        "[%s] Evento repetido "
                        "suprimido: pessoa=%s",
                        pacote.evento_id,
                        decisao.pessoa_id,
                    )
                    continue

                aparencia_estavel = None

                try:
                    caminho_original = (
                        evento
                        .imagem
                        .caminho_original
                    )

                    if caminho_original:
                        analise_visual = (
                            await asyncio.to_thread(
                                analisar_pessoa,
                                caminho_imagem=(
                                    caminho_original
                                ),
                                x=bounding_box.x,
                                y=bounding_box.y,
                                largura=(
                                    bounding_box
                                    .largura
                                ),
                                altura=(
                                    bounding_box
                                    .altura
                                ),
                            )
                        )

                        aparencia_estavel = (
                            await appearance_memory
                            .registrar(
                                pessoa_id=(
                                    decisao.pessoa_id
                                ),
                                analise=(
                                    analise_visual
                                ),
                            )
                        )

                        logger.info(
                            "[%s] Aparência estável: "
                            "pessoa=%s cor=%s "
                            "posição=%s tamanho=%s "
                            "área_média=%s%% "
                            "amostras=%s",
                            pacote.evento_id,
                            decisao.pessoa_id,
                            (
                                aparencia_estavel
                                .cor_roupa_predominante
                            ),
                            (
                                aparencia_estavel
                                .posicao_atual
                            ),
                            (
                                aparencia_estavel
                                .tamanho_predominante
                            ),
                            (
                                aparencia_estavel
                                .percentual_medio_quadro
                            ),
                            (
                                aparencia_estavel
                                .quantidade_amostras
                            ),
                        )

                except Exception:
                    logger.exception(
                        "[%s] Não foi possível "
                        "estabilizar a aparência",
                        pacote.evento_id,
                    )

                try:
                    mensagem = (
                        await asyncio.to_thread(
                            montar_alarm_detection_message,
                            evento,
                        )
                    )

                except ValueError as erro:
                    logger.info(
                        "[%s] Evento ignorado: %s",
                        pacote.evento_id,
                        erro,
                    )
                    continue

                evento_painel = {
                    "evento_id": (
                        evento.evento_id
                    ),
                    "pessoa_id": (
                        decisao.pessoa_id
                    ),
                    "status": decisao.status,
                    "quantidade_deteccoes": (
                        decisao
                        .quantidade_deteccoes
                    ),
                    "camera": (
                        mensagem.device.name
                    ),
                    "tipo": (
                        mensagem.event.type
                    ),
                    "datetime": (
                        mensagem.event.datetime
                    ),
                    "imagem": (
                        mensagem
                        .event
                        .images
                        .detection
                    ),
                    "aparencia": (
                        aparencia_estavel.to_dict()
                        if aparencia_estavel
                        is not None
                        else None
                    ),
                }

                await event_hub.publicar(
                    evento_painel
                )

                logger.info(
                    "[%s] Evento enviado para "
                    "%s painel(is)",
                    pacote.evento_id,
                    event_hub.total_conexoes,
                )

                if not settings.kafka_enabled:
                    logger.info(
                        "[%s] Payload pronto; "
                        "Kafka desativado",
                        pacote.evento_id,
                    )
                    continue

                # O payload do Kafka permanece
                # no formato solicitado.
                resultado = (
                    await self.publisher.publicar(
                        topico=self.topic,
                        evento_id=(
                            evento.evento_id
                        ),
                        dados=(
                            mensagem.model_dump(
                                mode="json"
                            )
                        ),
                    )
                )

                logger.info(
                    "[%s] Publicado em %s "
                    "p=%s offset=%s",
                    pacote.evento_id,
                    resultado["topico"],
                    resultado["particao"],
                    resultado["offset"],
                )

            except asyncio.CancelledError:
                raise

            except Exception:
                logger.exception(
                    "[%s] Erro no processamento",
                    pacote.evento_id,
                )

            finally:
                self.queue.task_done()
------------------------------------------------------------
ARQUIVO: app/services/camera_event_pipeline_backup_analise_visual.py
------------------------------------------------------------
import asyncio
import logging
from datetime import datetime, timezone

from app.adapters.cameras.factory import camera_adapter_factory
from app.core.config import settings
from app.domain.models.camera_event import RawCameraPackage
from app.messaging.kafka_producer import KafkaPublisher
from app.services.alarm_detection_payload import (
    montar_alarm_detection_message,
)
from app.services.event_hub import event_hub
from app.services.person_tracker import (
    DetectionBox,
    person_tracker,
)


logger = logging.getLogger(__name__)


class CameraEventPipeline:
    def __init__(
        self,
        publisher: KafkaPublisher,
        topic: str,
        maxsize: int = 1000,
        worker_count: int = 4,
    ) -> None:
        self.publisher = publisher
        self.topic = topic
        self.worker_count = worker_count

        self.queue: asyncio.Queue[
            tuple[RawCameraPackage, bytes]
        ] = asyncio.Queue(maxsize=maxsize)

        self._worker_tasks: list[asyncio.Task] = []
        self._exit_task: asyncio.Task | None = None
        self._iniciado = False

    @property
    def tamanho_fila(self) -> int:
        return self.queue.qsize()

    @property
    def capacidade_fila(self) -> int:
        return self.queue.maxsize

    async def start(self) -> None:
        if self._iniciado:
            return

        if settings.kafka_enabled:
            await self.publisher.start()
        else:
            logger.warning(
                "Kafka desativado: eventos serão processados, "
                "mas não publicados."
            )

        self._worker_tasks = [
            asyncio.create_task(
                self._worker(numero),
                name=f"camera-worker-{numero}",
            )
            for numero in range(
                1,
                self.worker_count + 1,
            )
        ]

        self._exit_task = asyncio.create_task(
            self._monitorar_saidas(),
            name="person-exit-monitor",
        )

        self._iniciado = True

        logger.info(
            "Pipeline iniciado com %s workers",
            self.worker_count,
        )

    async def stop(self) -> None:
        if not self._iniciado:
            return

        try:
            await asyncio.wait_for(
                self.queue.join(),
                timeout=10,
            )

        except TimeoutError:
            logger.warning(
                "Encerrando com %s itens na fila",
                self.queue.qsize(),
            )

        tarefas: list[asyncio.Task] = [
            *self._worker_tasks,
        ]

        if self._exit_task is not None:
            tarefas.append(self._exit_task)

        for tarefa in tarefas:
            tarefa.cancel()

        if tarefas:
            await asyncio.gather(
                *tarefas,
                return_exceptions=True,
            )

        if self.publisher.iniciado:
            await self.publisher.stop()

        self._worker_tasks.clear()
        self._exit_task = None
        self._iniciado = False

        logger.info("Pipeline encerrado")

    def adicionar(
        self,
        pacote: RawCameraPackage,
        body: bytes,
    ) -> None:
        self.queue.put_nowait(
            (pacote, body)
        )

    async def _monitorar_saidas(self) -> None:
        """
        Verifica a cada segundo quais pessoas estão há mais
        de oito segundos sem receber uma nova detecção.
        """

        logger.info(
            "Monitor automático de saídas iniciado"
        )

        while True:
            try:
                await asyncio.sleep(1)

                saidas = (
                    await person_tracker.coletar_saidas()
                )

                for saida in saidas:
                    momento_saida = (
                        datetime.now(timezone.utc)
                        .isoformat()
                        .replace("+00:00", "Z")
                    )

                    evento_painel = {
                        "evento_id": saida.evento_id,
                        "pessoa_id": saida.pessoa_id,
                        "status": "exited",
                        "quantidade_deteccoes": (
                            saida.quantidade_deteccoes
                        ),
                        "camera": saida.camera,
                        "tipo": None,
                        "datetime": momento_saida,
                        "imagem": None,
                    }

                    await event_hub.publicar(
                        evento_painel
                    )

                    logger.info(
                        "[RASTREAMENTO] Pessoa saiu: "
                        "pessoa=%s camera=%s "
                        "deteccoes=%s paineis=%s",
                        saida.pessoa_id,
                        saida.camera,
                        saida.quantidade_deteccoes,
                        event_hub.total_conexoes,
                    )

            except asyncio.CancelledError:
                logger.info(
                    "Monitor automático de saídas encerrado"
                )
                raise

            except Exception:
                logger.exception(
                    "Erro no monitor automático de saídas"
                )

    async def _worker(
        self,
        numero: int,
    ) -> None:
        logger.info(
            "Worker %s iniciado",
            numero,
        )

        while True:
            pacote, body = await self.queue.get()

            try:
                adapter = (
                    camera_adapter_factory.encontrar_adapter(
                        content_type=pacote.content_type,
                        body=body,
                    )
                )

                logger.info(
                    "[%s] Worker %s usando %s",
                    pacote.evento_id,
                    numero,
                    adapter.__class__.__name__,
                )

                evento = await asyncio.to_thread(
                    adapter.normalizar,
                    pacote,
                    body,
                )

                if evento.imagem is None:
                    logger.info(
                        "[%s] Evento ignorado: "
                        "não possui imagem",
                        pacote.evento_id,
                    )
                    continue

                bounding_box = (
                    evento.bounding_box_escolhida
                )

                if bounding_box is None:
                    logger.info(
                        "[%s] Evento ignorado: "
                        "não possui bounding box",
                        pacote.evento_id,
                    )
                    continue

                camera = (
                    evento.nome_camera
                    or evento.camera_id
                    or evento.ip_camera
                    or "Camera desconhecida"
                )

                caixa_rastreamento = DetectionBox(
                    x=bounding_box.x,
                    y=bounding_box.y,
                    largura=bounding_box.largura,
                    altura=bounding_box.altura,
                )

                decisao = await person_tracker.registrar(
                    camera=camera,
                    evento_id=evento.evento_id,
                    bbox=caixa_rastreamento,
                )

                logger.info(
                    "[%s] Rastreamento: "
                    "pessoa=%s status=%s "
                    "deteccoes=%s",
                    pacote.evento_id,
                    decisao.pessoa_id,
                    decisao.status,
                    decisao.quantidade_deteccoes,
                )

                # Eventos repetidos em menos de cinco segundos
                # não geram Base64, painel ou Kafka.
                if not decisao.deve_processar:
                    logger.info(
                        "[%s] Evento repetido suprimido: "
                        "pessoa=%s",
                        pacote.evento_id,
                        decisao.pessoa_id,
                    )
                    continue

                try:
                    mensagem = await asyncio.to_thread(
                        montar_alarm_detection_message,
                        evento,
                    )

                except ValueError as erro:
                    logger.info(
                        "[%s] Evento ignorado: %s",
                        pacote.evento_id,
                        erro,
                    )
                    continue

                evento_painel = {
                    "evento_id": evento.evento_id,
                    "pessoa_id": decisao.pessoa_id,
                    "status": decisao.status,
                    "quantidade_deteccoes": (
                        decisao.quantidade_deteccoes
                    ),
                    "camera": mensagem.device.name,
                    "tipo": mensagem.event.type,
                    "datetime": mensagem.event.datetime,
                    "imagem": (
                        mensagem.event.images.detection
                    ),
                }

                await event_hub.publicar(
                    evento_painel
                )

                logger.info(
                    "[%s] Evento enviado para "
                    "%s painel(is)",
                    pacote.evento_id,
                    event_hub.total_conexoes,
                )

                if not settings.kafka_enabled:
                    logger.info(
                        "[%s] Payload pronto; "
                        "Kafka desativado",
                        pacote.evento_id,
                    )
                    continue

                resultado = (
                    await self.publisher.publicar(
                        topico=self.topic,
                        evento_id=evento.evento_id,
                        dados=mensagem.model_dump(
                            mode="json"
                        ),
                    )
                )

                logger.info(
                    "[%s] Publicado em %s "
                    "p=%s offset=%s",
                    pacote.evento_id,
                    resultado["topico"],
                    resultado["particao"],
                    resultado["offset"],
                )

            except asyncio.CancelledError:
                raise

            except Exception:
                logger.exception(
                    "[%s] Erro no processamento",
                    pacote.evento_id,
                )

            finally:
                self.queue.task_done()
------------------------------------------------------------
ARQUIVO: app/services/camera_event_pipeline_backup_antes_analise.py
------------------------------------------------------------
import asyncio
import logging
from datetime import datetime, timezone

from app.adapters.cameras.factory import camera_adapter_factory
from app.core.config import settings
from app.domain.models.camera_event import RawCameraPackage
from app.messaging.kafka_producer import KafkaPublisher
from app.services.alarm_detection_payload import (
    montar_alarm_detection_message,
)
from app.services.event_hub import event_hub
from app.services.person_tracker import (
    DetectionBox,
    person_tracker,
)


logger = logging.getLogger(__name__)


class CameraEventPipeline:
    def __init__(
        self,
        publisher: KafkaPublisher,
        topic: str,
        maxsize: int = 1000,
        worker_count: int = 4,
    ) -> None:
        self.publisher = publisher
        self.topic = topic
        self.worker_count = worker_count

        self.queue: asyncio.Queue[
            tuple[RawCameraPackage, bytes]
        ] = asyncio.Queue(maxsize=maxsize)

        self._worker_tasks: list[asyncio.Task] = []
        self._exit_task: asyncio.Task | None = None
        self._iniciado = False

    @property
    def tamanho_fila(self) -> int:
        return self.queue.qsize()

    @property
    def capacidade_fila(self) -> int:
        return self.queue.maxsize

    async def start(self) -> None:
        if self._iniciado:
            return

        if settings.kafka_enabled:
            await self.publisher.start()
        else:
            logger.warning(
                "Kafka desativado: eventos serão processados, "
                "mas não publicados."
            )

        self._worker_tasks = [
            asyncio.create_task(
                self._worker(numero),
                name=f"camera-worker-{numero}",
            )
            for numero in range(
                1,
                self.worker_count + 1,
            )
        ]

        self._exit_task = asyncio.create_task(
            self._monitorar_saidas(),
            name="person-exit-monitor",
        )

        self._iniciado = True

        logger.info(
            "Pipeline iniciado com %s workers",
            self.worker_count,
        )

    async def stop(self) -> None:
        if not self._iniciado:
            return

        try:
            await asyncio.wait_for(
                self.queue.join(),
                timeout=10,
            )

        except TimeoutError:
            logger.warning(
                "Encerrando com %s itens na fila",
                self.queue.qsize(),
            )

        tarefas: list[asyncio.Task] = [
            *self._worker_tasks,
        ]

        if self._exit_task is not None:
            tarefas.append(self._exit_task)

        for tarefa in tarefas:
            tarefa.cancel()

        if tarefas:
            await asyncio.gather(
                *tarefas,
                return_exceptions=True,
            )

        if self.publisher.iniciado:
            await self.publisher.stop()

        self._worker_tasks.clear()
        self._exit_task = None
        self._iniciado = False

        logger.info("Pipeline encerrado")

    def adicionar(
        self,
        pacote: RawCameraPackage,
        body: bytes,
    ) -> None:
        self.queue.put_nowait(
            (pacote, body)
        )

    async def _monitorar_saidas(self) -> None:
        """
        Verifica a cada segundo quais pessoas estão há mais
        de oito segundos sem receber uma nova detecção.
        """

        logger.info(
            "Monitor automático de saídas iniciado"
        )

        while True:
            try:
                await asyncio.sleep(1)

                saidas = (
                    await person_tracker.coletar_saidas()
                )

                for saida in saidas:
                    momento_saida = (
                        datetime.now(timezone.utc)
                        .isoformat()
                        .replace("+00:00", "Z")
                    )

                    evento_painel = {
                        "evento_id": saida.evento_id,
                        "pessoa_id": saida.pessoa_id,
                        "status": "exited",
                        "quantidade_deteccoes": (
                            saida.quantidade_deteccoes
                        ),
                        "camera": saida.camera,
                        "tipo": None,
                        "datetime": momento_saida,
                        "imagem": None,
                    }

                    await event_hub.publicar(
                        evento_painel
                    )

                    logger.info(
                        "[RASTREAMENTO] Pessoa saiu: "
                        "pessoa=%s camera=%s "
                        "deteccoes=%s paineis=%s",
                        saida.pessoa_id,
                        saida.camera,
                        saida.quantidade_deteccoes,
                        event_hub.total_conexoes,
                    )

            except asyncio.CancelledError:
                logger.info(
                    "Monitor automático de saídas encerrado"
                )
                raise

            except Exception:
                logger.exception(
                    "Erro no monitor automático de saídas"
                )

    async def _worker(
        self,
        numero: int,
    ) -> None:
        logger.info(
            "Worker %s iniciado",
            numero,
        )

        while True:
            pacote, body = await self.queue.get()

            try:
                adapter = (
                    camera_adapter_factory.encontrar_adapter(
                        content_type=pacote.content_type,
                        body=body,
                    )
                )

                logger.info(
                    "[%s] Worker %s usando %s",
                    pacote.evento_id,
                    numero,
                    adapter.__class__.__name__,
                )

                evento = await asyncio.to_thread(
                    adapter.normalizar,
                    pacote,
                    body,
                )

                if evento.imagem is None:
                    logger.info(
                        "[%s] Evento ignorado: "
                        "não possui imagem",
                        pacote.evento_id,
                    )
                    continue

                bounding_box = (
                    evento.bounding_box_escolhida
                )

                if bounding_box is None:
                    logger.info(
                        "[%s] Evento ignorado: "
                        "não possui bounding box",
                        pacote.evento_id,
                    )
                    continue

                camera = (
                    evento.nome_camera
                    or evento.camera_id
                    or evento.ip_camera
                    or "Camera desconhecida"
                )

                caixa_rastreamento = DetectionBox(
                    x=bounding_box.x,
                    y=bounding_box.y,
                    largura=bounding_box.largura,
                    altura=bounding_box.altura,
                )

                decisao = await person_tracker.registrar(
                    camera=camera,
                    evento_id=evento.evento_id,
                    bbox=caixa_rastreamento,
                )

                logger.info(
                    "[%s] Rastreamento: "
                    "pessoa=%s status=%s "
                    "deteccoes=%s",
                    pacote.evento_id,
                    decisao.pessoa_id,
                    decisao.status,
                    decisao.quantidade_deteccoes,
                )

                # Eventos repetidos em menos de cinco segundos
                # não geram Base64, painel ou Kafka.
                if not decisao.deve_processar:
                    logger.info(
                        "[%s] Evento repetido suprimido: "
                        "pessoa=%s",
                        pacote.evento_id,
                        decisao.pessoa_id,
                    )
                    continue

                try:
                    mensagem = await asyncio.to_thread(
                        montar_alarm_detection_message,
                        evento,
                    )

                except ValueError as erro:
                    logger.info(
                        "[%s] Evento ignorado: %s",
                        pacote.evento_id,
                        erro,
                    )
                    continue

                evento_painel = {
                    "evento_id": evento.evento_id,
                    "pessoa_id": decisao.pessoa_id,
                    "status": decisao.status,
                    "quantidade_deteccoes": (
                        decisao.quantidade_deteccoes
                    ),
                    "camera": mensagem.device.name,
                    "tipo": mensagem.event.type,
                    "datetime": mensagem.event.datetime,
                    "imagem": (
                        mensagem.event.images.detection
                    ),
                }

                await event_hub.publicar(
                    evento_painel
                )

                logger.info(
                    "[%s] Evento enviado para "
                    "%s painel(is)",
                    pacote.evento_id,
                    event_hub.total_conexoes,
                )

                if not settings.kafka_enabled:
                    logger.info(
                        "[%s] Payload pronto; "
                        "Kafka desativado",
                        pacote.evento_id,
                    )
                    continue

                resultado = (
                    await self.publisher.publicar(
                        topico=self.topic,
                        evento_id=evento.evento_id,
                        dados=mensagem.model_dump(
                            mode="json"
                        ),
                    )
                )

                logger.info(
                    "[%s] Publicado em %s "
                    "p=%s offset=%s",
                    pacote.evento_id,
                    resultado["topico"],
                    resultado["particao"],
                    resultado["offset"],
                )

            except asyncio.CancelledError:
                raise

            except Exception:
                logger.exception(
                    "[%s] Erro no processamento",
                    pacote.evento_id,
                )

            finally:
                self.queue.task_done()
------------------------------------------------------------
ARQUIVO: app/services/camera_event_pipeline_backup_antes_movimento.py
------------------------------------------------------------
import asyncio
import logging
from datetime import datetime, timezone

from app.adapters.cameras.factory import camera_adapter_factory
from app.core.config import settings
from app.domain.models.camera_event import BoundingBox, CameraEvent, RawCameraPackage
from app.messaging.kafka_producer import KafkaPublisher
from app.services.alarm_detection_payload import (
    arquivo_para_base64,
    montar_alarm_detection_message,
)
from app.services.appearance_memory import appearance_memory
from app.services.event_hub import event_hub
from app.services.person_tracker import (
    DetectionBox,
    TrackingDecision,
    person_tracker,
)
from app.services.scene_analyzer import analisar_pessoa
from app.services.scene_context_analyzer import analisar_contexto_cena
from app.services.scene_renderer import renderizar_cena_com_boxes

logger = logging.getLogger(__name__)


class CameraEventPipeline:
    def __init__(
        self,
        publisher: KafkaPublisher,
        topic: str,
        maxsize: int = 1000,
        worker_count: int = 4,
    ) -> None:
        self.publisher = publisher
        self.topic = topic
        self.worker_count = worker_count
        self.queue: asyncio.Queue[tuple[RawCameraPackage, bytes]] = asyncio.Queue(
            maxsize=maxsize
        )
        self._worker_tasks: list[asyncio.Task] = []
        self._exit_task: asyncio.Task | None = None
        self._iniciado = False

    @property
    def tamanho_fila(self) -> int:
        return self.queue.qsize()

    @property
    def capacidade_fila(self) -> int:
        return self.queue.maxsize

    async def start(self) -> None:
        if self._iniciado:
            return

        if settings.kafka_enabled:
            await self.publisher.start()
        else:
            logger.warning(
                "Kafka desativado: eventos serão processados, mas não publicados."
            )

        self._worker_tasks = [
            asyncio.create_task(
                self._worker(numero),
                name=f"camera-worker-{numero}",
            )
            for numero in range(1, self.worker_count + 1)
        ]
        self._exit_task = asyncio.create_task(
            self._monitorar_saidas(),
            name="person-exit-monitor",
        )
        self._iniciado = True
        logger.info("Pipeline iniciado com %s workers", self.worker_count)

    async def stop(self) -> None:
        if not self._iniciado:
            return

        try:
            await asyncio.wait_for(self.queue.join(), timeout=10)
        except TimeoutError:
            logger.warning("Encerrando com %s itens na fila", self.queue.qsize())

        tarefas: list[asyncio.Task] = [*self._worker_tasks]
        if self._exit_task is not None:
            tarefas.append(self._exit_task)

        for tarefa in tarefas:
            tarefa.cancel()

        if tarefas:
            await asyncio.gather(*tarefas, return_exceptions=True)

        if self.publisher.iniciado:
            await self.publisher.stop()

        await appearance_memory.limpar()
        await person_tracker.limpar()

        self._worker_tasks.clear()
        self._exit_task = None
        self._iniciado = False
        logger.info("Pipeline encerrado")

    def adicionar(self, pacote: RawCameraPackage, body: bytes) -> None:
        self.queue.put_nowait((pacote, body))

    async def _monitorar_saidas(self) -> None:
        logger.info("Monitor automático de saídas iniciado")

        while True:
            try:
                await asyncio.sleep(1)
                saidas = await person_tracker.coletar_saidas()

                for saida in saidas:
                    aparencia_final = await appearance_memory.finalizar(
                        saida.pessoa_id
                    )
                    momento_saida = (
                        datetime.now(timezone.utc)
                        .isoformat()
                        .replace("+00:00", "Z")
                    )
                    evento_painel = {
                        "evento_id": saida.evento_id,
                        "pessoa_id": saida.pessoa_id,
                        "status": "exited",
                        "quantidade_deteccoes": saida.quantidade_deteccoes,
                        "camera": saida.camera,
                        "tipo": None,
                        "datetime": momento_saida,
                        "imagem": None,
                        "aparencia": (
                            aparencia_final.to_dict()
                            if aparencia_final is not None
                            else None
                        ),
                        "contexto_cena": None,
                        "imagem_cena": None,
                        "quantidade_boxes_cena": None,
                    }
                    await event_hub.publicar(evento_painel)
                    logger.info(
                        "[RASTREAMENTO] Pessoa saiu: pessoa=%s camera=%s "
                        "deteccoes=%s amostras=%s paineis=%s",
                        saida.pessoa_id,
                        saida.camera,
                        saida.quantidade_deteccoes,
                        (
                            aparencia_final.quantidade_amostras
                            if aparencia_final is not None
                            else 0
                        ),
                        event_hub.total_conexoes,
                    )

            except asyncio.CancelledError:
                logger.info("Monitor automático de saídas encerrado")
                raise
            except Exception:
                logger.exception("Erro no monitor automático de saídas")

    def _selecionar_boxes_pessoas(
        self,
        evento: CameraEvent,
    ) -> list[BoundingBox]:
        """Remove caixas inválidas, duplicadas e a caixa da imagem inteira."""

        if evento.imagem is None:
            return []

        largura_imagem = evento.imagem.largura
        altura_imagem = evento.imagem.altura
        if not largura_imagem or not altura_imagem:
            return []

        area_imagem = largura_imagem * altura_imagem
        assinaturas: set[tuple[int, int, int, int]] = set()
        caixas_validas: list[BoundingBox] = []

        for caixa in evento.bounding_boxes or []:
            if caixa.largura <= 0 or caixa.altura <= 0:
                continue

            x1 = max(0, min(caixa.x, largura_imagem - 1))
            y1 = max(0, min(caixa.y, altura_imagem - 1))
            x2 = max(x1 + 1, min(caixa.x2, largura_imagem))
            y2 = max(y1 + 1, min(caixa.y2, altura_imagem))
            largura = x2 - x1
            altura = y2 - y1
            percentual = largura * altura / area_imagem * 100

            if percentual >= 60 or percentual < 0.05:
                continue

            assinatura = (x1, y1, x2, y2)
            if assinatura in assinaturas:
                continue
            assinaturas.add(assinatura)

            if (
                x1 == caixa.x
                and y1 == caixa.y
                and largura == caixa.largura
                and altura == caixa.altura
            ):
                caixa_ajustada = caixa
            else:
                caixa_ajustada = caixa.model_copy(
                    update={
                        "x": x1,
                        "y": y1,
                        "largura": largura,
                        "altura": altura,
                        "x2": x2,
                        "y2": y2,
                    }
                )

            caixas_validas.append(caixa_ajustada)

        if not caixas_validas and evento.bounding_box_escolhida is not None:
            caixas_validas.append(evento.bounding_box_escolhida)

        caixas_validas.sort(
            key=lambda caixa: (
                caixa.x + caixa.largura / 2,
                caixa.y + caixa.altura / 2,
            )
        )
        return caixas_validas

    async def _analisar_aparencia(
        self,
        evento: CameraEvent,
        bounding_box: BoundingBox,
        pessoa_id: str,
        pacote_id: str,
    ):
        try:
            if evento.imagem is None or not evento.imagem.caminho_original:
                logger.info(
                    "[%s] Imagem sem caminho original; aparência não analisada",
                    pacote_id,
                )
                return None

            analise_visual = await asyncio.to_thread(
                analisar_pessoa,
                caminho_imagem=evento.imagem.caminho_original,
                x=bounding_box.x,
                y=bounding_box.y,
                largura=bounding_box.largura,
                altura=bounding_box.altura,
            )
            aparencia_estavel = await appearance_memory.registrar(
                pessoa_id=pessoa_id,
                analise=analise_visual,
            )
            logger.info(
                "[%s] Aparência estável: pessoa=%s cor=%s posição=%s "
                "tamanho=%s área_média=%s%% amostras=%s",
                pacote_id,
                pessoa_id,
                aparencia_estavel.cor_roupa_predominante,
                aparencia_estavel.posicao_atual,
                aparencia_estavel.tamanho_predominante,
                aparencia_estavel.percentual_medio_quadro,
                aparencia_estavel.quantidade_amostras,
            )
            return aparencia_estavel
        except Exception:
            logger.exception(
                "[%s] Não foi possível analisar a aparência da pessoa=%s",
                pacote_id,
                pessoa_id,
            )
            return None

    async def _analisar_contexto_cena(
        self,
        evento: CameraEvent,
        bounding_boxes: list[BoundingBox],
        pacote_id: str,
    ):
        try:
            if evento.imagem is None:
                return None

            largura_imagem = evento.imagem.largura
            altura_imagem = evento.imagem.altura
            if not largura_imagem or not altura_imagem:
                logger.info(
                    "[%s] Contexto não analisado: dimensões ausentes",
                    pacote_id,
                )
                return None

            contexto = await asyncio.to_thread(
                analisar_contexto_cena,
                largura_imagem=largura_imagem,
                altura_imagem=altura_imagem,
                bounding_boxes=bounding_boxes,
            )
            logger.info(
                "[%s] Contexto da cena: pessoas=%s esquerda=%s centro=%s "
                "direita=%s muito_proximos=%s proximos=%s separados=%s",
                pacote_id,
                contexto.quantidade_pessoas,
                contexto.pessoas_esquerda,
                contexto.pessoas_centro,
                contexto.pessoas_direita,
                contexto.pares_muito_proximos,
                contexto.pares_proximos,
                contexto.pares_separados,
            )
            logger.info("[%s] Descrição da cena: %s", pacote_id, contexto.descricao)
            return contexto
        except Exception:
            logger.exception("[%s] Não foi possível analisar a cena", pacote_id)
            return None

    async def _renderizar_cena(
        self,
        evento: CameraEvent,
        bounding_boxes: list[BoundingBox],
        pacote_id: str,
    ):
        try:
            if evento.imagem is None or not evento.imagem.caminho_original:
                return None
            if not bounding_boxes:
                return None

            resultado = await asyncio.to_thread(
                renderizar_cena_com_boxes,
                caminho_imagem=evento.imagem.caminho_original,
                bounding_boxes=bounding_boxes,
            )
            if resultado.quantidade_boxes == 0:
                return None

            logger.info(
                "[%s] Cena renderizada: boxes=%s dimensoes=%sx%s base64=%s caracteres",
                pacote_id,
                resultado.quantidade_boxes,
                resultado.largura_imagem,
                resultado.altura_imagem,
                len(resultado.imagem_base64),
            )
            return resultado
        except Exception:
            logger.exception(
                "[%s] Não foi possível renderizar a cena com boxes",
                pacote_id,
            )
            return None

    async def _publicar_atualizacao_parcial(
        self,
        camera: str,
        decisao: TrackingDecision,
        aparencia_estavel,
        contexto_cena,
        cena_renderizada,
        indice_na_cena: int,
        total_pessoas_cena: int,
        pacote_id: str,
    ) -> None:
        atualizacao_parcial = {
            "evento_id": decisao.evento_id,
            "pessoa_id": decisao.pessoa_id,
            "status": "appearance_updated",
            "quantidade_deteccoes": decisao.quantidade_deteccoes,
            "camera": camera,
            "aparencia": (
                aparencia_estavel.to_dict()
                if aparencia_estavel is not None
                else None
            ),
            "contexto_cena": (
                contexto_cena.to_dict() if contexto_cena is not None else None
            ),
            "imagem_cena": (
                cena_renderizada.imagem_base64
                if cena_renderizada is not None
                else None
            ),
            "quantidade_boxes_cena": (
                cena_renderizada.quantidade_boxes
                if cena_renderizada is not None
                else 0
            ),
            "indice_na_cena": indice_na_cena,
            "total_pessoas_cena": total_pessoas_cena,
        }
        await event_hub.publicar(atualizacao_parcial)
        logger.info(
            "[%s] Atualização parcial: pessoa=%s indice=%s/%s "
            "deteccoes=%s amostras=%s boxes=%s paineis=%s",
            pacote_id,
            decisao.pessoa_id,
            indice_na_cena,
            total_pessoas_cena,
            decisao.quantidade_deteccoes,
            (
                aparencia_estavel.quantidade_amostras
                if aparencia_estavel is not None
                else 0
            ),
            (
                cena_renderizada.quantidade_boxes
                if cena_renderizada is not None
                else 0
            ),
            event_hub.total_conexoes,
        )

    async def _processar_pessoa(
        self,
        evento: CameraEvent,
        camera: str,
        bounding_box: BoundingBox,
        decisao: TrackingDecision,
        contexto_cena,
        cena_renderizada,
        indice_na_cena: int,
        total_pessoas_cena: int,
        pacote_id: str,
        imagem_background_base64: str | None,
    ) -> str | None:
        aparencia_estavel = await self._analisar_aparencia(
            evento=evento,
            bounding_box=bounding_box,
            pessoa_id=decisao.pessoa_id,
            pacote_id=pacote_id,
        )

        if not decisao.deve_processar:
            await self._publicar_atualizacao_parcial(
                camera=camera,
                decisao=decisao,
                aparencia_estavel=aparencia_estavel,
                contexto_cena=contexto_cena,
                cena_renderizada=cena_renderizada,
                indice_na_cena=indice_na_cena,
                total_pessoas_cena=total_pessoas_cena,
                pacote_id=pacote_id,
            )
            logger.info(
                "[%s] Evento repetido suprimido: pessoa=%s indice=%s/%s amostras=%s",
                pacote_id,
                decisao.pessoa_id,
                indice_na_cena,
                total_pessoas_cena,
                (
                    aparencia_estavel.quantidade_amostras
                    if aparencia_estavel is not None
                    else 0
                ),
            )
            return imagem_background_base64

        if evento.imagem is None or not evento.imagem.caminho_original:
            logger.info("[%s] Pessoa ignorada: caminho original ausente", pacote_id)
            return imagem_background_base64

        if imagem_background_base64 is None:
            imagem_background_base64 = await asyncio.to_thread(
                arquivo_para_base64,
                evento.imagem.caminho_original,
            )

        try:
            mensagem = await asyncio.to_thread(
                montar_alarm_detection_message,
                evento,
                bounding_box=bounding_box,
                evento_id=decisao.evento_id,
                imagem_background_base64=imagem_background_base64,
            )
        except ValueError as erro:
            logger.info(
                "[%s] Pessoa ignorada: pessoa=%s erro=%s",
                pacote_id,
                decisao.pessoa_id,
                erro,
            )
            return imagem_background_base64

        evento_painel = {
            "evento_id": decisao.evento_id,
            "pessoa_id": decisao.pessoa_id,
            "status": decisao.status,
            "quantidade_deteccoes": decisao.quantidade_deteccoes,
            "camera": mensagem.device.name,
            "tipo": mensagem.event.type,
            "datetime": mensagem.event.datetime,
            "imagem": mensagem.event.images.detection,
            "aparencia": (
                aparencia_estavel.to_dict()
                if aparencia_estavel is not None
                else None
            ),
            "contexto_cena": (
                contexto_cena.to_dict() if contexto_cena is not None else None
            ),
            "imagem_cena": (
                cena_renderizada.imagem_base64
                if cena_renderizada is not None
                else None
            ),
            "quantidade_boxes_cena": (
                cena_renderizada.quantidade_boxes
                if cena_renderizada is not None
                else 0
            ),
            "indice_na_cena": indice_na_cena,
            "total_pessoas_cena": total_pessoas_cena,
        }
        await event_hub.publicar(evento_painel)
        logger.info(
            "[%s] Pessoa enviada ao painel: pessoa=%s status=%s indice=%s/%s paineis=%s",
            pacote_id,
            decisao.pessoa_id,
            decisao.status,
            indice_na_cena,
            total_pessoas_cena,
            event_hub.total_conexoes,
        )

        if not settings.kafka_enabled:
            logger.info(
                "[%s] Payload pronto; Kafka desativado: pessoa=%s",
                pacote_id,
                decisao.pessoa_id,
            )
            return imagem_background_base64

        resultado = await self.publisher.publicar(
            topico=self.topic,
            evento_id=decisao.evento_id,
            dados=mensagem.model_dump(mode="json"),
        )
        logger.info(
            "[%s] Pessoa publicada em %s p=%s offset=%s pessoa=%s indice=%s/%s",
            pacote_id,
            resultado["topico"],
            resultado["particao"],
            resultado["offset"],
            decisao.pessoa_id,
            indice_na_cena,
            total_pessoas_cena,
        )
        return imagem_background_base64

    async def _worker(self, numero: int) -> None:
        logger.info("Worker %s iniciado", numero)

        while True:
            pacote, body = await self.queue.get()

            try:
                adapter = camera_adapter_factory.encontrar_adapter(
                    content_type=pacote.content_type,
                    body=body,
                )
                logger.info(
                    "[%s] Worker %s usando %s",
                    pacote.evento_id,
                    numero,
                    adapter.__class__.__name__,
                )

                evento = await asyncio.to_thread(adapter.normalizar, pacote, body)
                if evento.imagem is None:
                    logger.info(
                        "[%s] Evento ignorado: não possui imagem",
                        pacote.evento_id,
                    )
                    continue

                bounding_boxes = self._selecionar_boxes_pessoas(evento)
                if not bounding_boxes:
                    logger.info(
                        "[%s] Evento ignorado: não possui boxes válidas de pessoas",
                        pacote.evento_id,
                    )
                    continue

                camera = (
                    evento.nome_camera
                    or evento.camera_id
                    or evento.ip_camera
                    or "Camera desconhecida"
                )
                caixas_rastreamento = [
                    DetectionBox(
                        x=caixa.x,
                        y=caixa.y,
                        largura=caixa.largura,
                        altura=caixa.altura,
                    )
                    for caixa in bounding_boxes
                ]
                decisoes = await person_tracker.registrar_lote(
                    camera=camera,
                    evento_id=evento.evento_id,
                    bboxes=caixas_rastreamento,
                )

                if len(decisoes) != len(bounding_boxes):
                    raise RuntimeError(
                        "Quantidade de decisões diferente da quantidade de boxes"
                    )

                total_pessoas = len(bounding_boxes)
                logger.info(
                    "[%s] Rastreamento em lote: pessoas=%s ids=%s",
                    pacote.evento_id,
                    total_pessoas,
                    [
                        (decisao.pessoa_id, decisao.status)
                        for decisao in decisoes
                    ],
                )

                contexto_cena = await self._analisar_contexto_cena(
                    evento=evento,
                    bounding_boxes=bounding_boxes,
                    pacote_id=pacote.evento_id,
                )
                cena_renderizada = await self._renderizar_cena(
                    evento=evento,
                    bounding_boxes=bounding_boxes,
                    pacote_id=pacote.evento_id,
                )

                imagem_background_base64: str | None = None
                for indice, (bounding_box, decisao) in enumerate(
                    zip(bounding_boxes, decisoes, strict=True),
                    start=1,
                ):
                    imagem_background_base64 = await self._processar_pessoa(
                        evento=evento,
                        camera=camera,
                        bounding_box=bounding_box,
                        decisao=decisao,
                        contexto_cena=contexto_cena,
                        cena_renderizada=cena_renderizada,
                        indice_na_cena=indice,
                        total_pessoas_cena=total_pessoas,
                        pacote_id=pacote.evento_id,
                        imagem_background_base64=imagem_background_base64,
                    )

            except asyncio.CancelledError:
                raise
            except Exception:
                logger.exception("[%s] Erro no processamento", pacote.evento_id)
            finally:
                self.queue.task_done()

------------------------------------------------------------
ARQUIVO: app/services/camera_event_pipeline_backup_antes_multiplas_pessoas.py
------------------------------------------------------------
import asyncio
import logging
from datetime import datetime, timezone

from app.adapters.cameras.factory import (
    camera_adapter_factory,
)
from app.core.config import settings
from app.domain.models.camera_event import (
    RawCameraPackage,
)
from app.messaging.kafka_producer import (
    KafkaPublisher,
)
from app.services.alarm_detection_payload import (
    montar_alarm_detection_message,
)
from app.services.appearance_memory import (
    appearance_memory,
)
from app.services.event_hub import event_hub
from app.services.person_tracker import (
    DetectionBox,
    person_tracker,
)
from app.services.scene_analyzer import (
    analisar_pessoa,
)
from app.services.scene_context_analyzer import (
    analisar_contexto_cena,
)
from app.services.scene_renderer import (
    renderizar_cena_com_boxes,
)


logger = logging.getLogger(__name__)


class CameraEventPipeline:
    def __init__(
        self,
        publisher: KafkaPublisher,
        topic: str,
        maxsize: int = 1000,
        worker_count: int = 4,
    ) -> None:
        self.publisher = publisher
        self.topic = topic
        self.worker_count = worker_count

        self.queue: asyncio.Queue[
            tuple[RawCameraPackage, bytes]
        ] = asyncio.Queue(maxsize=maxsize)

        self._worker_tasks: list[asyncio.Task] = []
        self._exit_task: asyncio.Task | None = None
        self._iniciado = False

    @property
    def tamanho_fila(self) -> int:
        return self.queue.qsize()

    @property
    def capacidade_fila(self) -> int:
        return self.queue.maxsize

    async def start(self) -> None:
        if self._iniciado:
            return

        if settings.kafka_enabled:
            await self.publisher.start()
        else:
            logger.warning(
                "Kafka desativado: eventos serão "
                "processados, mas não publicados."
            )

        self._worker_tasks = [
            asyncio.create_task(
                self._worker(numero),
                name=f"camera-worker-{numero}",
            )
            for numero in range(
                1,
                self.worker_count + 1,
            )
        ]

        self._exit_task = asyncio.create_task(
            self._monitorar_saidas(),
            name="person-exit-monitor",
        )

        self._iniciado = True

        logger.info(
            "Pipeline iniciado com %s workers",
            self.worker_count,
        )

    async def stop(self) -> None:
        if not self._iniciado:
            return

        try:
            await asyncio.wait_for(
                self.queue.join(),
                timeout=10,
            )

        except TimeoutError:
            logger.warning(
                "Encerrando com %s itens na fila",
                self.queue.qsize(),
            )

        tarefas: list[asyncio.Task] = [
            *self._worker_tasks,
        ]

        if self._exit_task is not None:
            tarefas.append(
                self._exit_task
            )

        for tarefa in tarefas:
            tarefa.cancel()

        if tarefas:
            await asyncio.gather(
                *tarefas,
                return_exceptions=True,
            )

        if self.publisher.iniciado:
            await self.publisher.stop()

        await appearance_memory.limpar()

        self._worker_tasks.clear()
        self._exit_task = None
        self._iniciado = False

        logger.info(
            "Pipeline encerrado"
        )

    def adicionar(
        self,
        pacote: RawCameraPackage,
        body: bytes,
    ) -> None:
        self.queue.put_nowait(
            (pacote, body)
        )

    async def _monitorar_saidas(
        self,
    ) -> None:
        logger.info(
            "Monitor automático de saídas iniciado"
        )

        while True:
            try:
                await asyncio.sleep(1)

                saidas = (
                    await person_tracker
                    .coletar_saidas()
                )

                for saida in saidas:
                    aparencia_final = (
                        await appearance_memory
                        .finalizar(
                            saida.pessoa_id
                        )
                    )

                    momento_saida = (
                        datetime.now(
                            timezone.utc
                        )
                        .isoformat()
                        .replace(
                            "+00:00",
                            "Z",
                        )
                    )

                    evento_painel = {
                        "evento_id": saida.evento_id,
                        "pessoa_id": saida.pessoa_id,
                        "status": "exited",
                        "quantidade_deteccoes": (
                            saida.quantidade_deteccoes
                        ),
                        "camera": saida.camera,
                        "tipo": None,
                        "datetime": momento_saida,
                        "imagem": None,
                        "aparencia": (
                            aparencia_final.to_dict()
                            if aparencia_final is not None
                            else None
                        ),
                        "contexto_cena": None,

                        # O painel manterá a última
                        # cena recebida no cartão.
                        "imagem_cena": None,
                        "quantidade_boxes_cena": None,
                    }

                    await event_hub.publicar(
                        evento_painel
                    )

                    logger.info(
                        "[RASTREAMENTO] Pessoa saiu: "
                        "pessoa=%s camera=%s "
                        "deteccoes=%s amostras=%s "
                        "paineis=%s",
                        saida.pessoa_id,
                        saida.camera,
                        saida.quantidade_deteccoes,
                        (
                            aparencia_final
                            .quantidade_amostras
                            if aparencia_final is not None
                            else 0
                        ),
                        event_hub.total_conexoes,
                    )

            except asyncio.CancelledError:
                logger.info(
                    "Monitor automático de "
                    "saídas encerrado"
                )
                raise

            except Exception:
                logger.exception(
                    "Erro no monitor automático "
                    "de saídas"
                )

    async def _analisar_aparencia(
        self,
        evento,
        bounding_box,
        pessoa_id: str,
        pacote_id: str,
    ):
        try:
            if evento.imagem is None:
                return None

            caminho_original = (
                evento.imagem.caminho_original
            )

            if not caminho_original:
                logger.info(
                    "[%s] Imagem sem caminho "
                    "original; aparência não analisada",
                    pacote_id,
                )
                return None

            analise_visual = await asyncio.to_thread(
                analisar_pessoa,
                caminho_imagem=caminho_original,
                x=bounding_box.x,
                y=bounding_box.y,
                largura=bounding_box.largura,
                altura=bounding_box.altura,
            )

            aparencia_estavel = (
                await appearance_memory.registrar(
                    pessoa_id=pessoa_id,
                    analise=analise_visual,
                )
            )

            logger.info(
                "[%s] Aparência estável: "
                "pessoa=%s cor=%s posição=%s "
                "tamanho=%s área_média=%s%% "
                "amostras=%s",
                pacote_id,
                pessoa_id,
                (
                    aparencia_estavel
                    .cor_roupa_predominante
                ),
                aparencia_estavel.posicao_atual,
                (
                    aparencia_estavel
                    .tamanho_predominante
                ),
                (
                    aparencia_estavel
                    .percentual_medio_quadro
                ),
                (
                    aparencia_estavel
                    .quantidade_amostras
                ),
            )

            return aparencia_estavel

        except Exception:
            logger.exception(
                "[%s] Não foi possível analisar "
                "ou estabilizar a aparência",
                pacote_id,
            )

            return None

    async def _analisar_contexto_cena(
        self,
        evento,
        pacote_id: str,
    ):
        try:
            if evento.imagem is None:
                return None

            largura_imagem = (
                evento.imagem.largura
            )

            altura_imagem = (
                evento.imagem.altura
            )

            if (
                not largura_imagem
                or not altura_imagem
            ):
                logger.info(
                    "[%s] Contexto não analisado: "
                    "dimensões da imagem ausentes",
                    pacote_id,
                )
                return None

            bounding_boxes = (
                evento.bounding_boxes
                or []
            )

            contexto = await asyncio.to_thread(
                analisar_contexto_cena,
                largura_imagem=largura_imagem,
                altura_imagem=altura_imagem,
                bounding_boxes=bounding_boxes,
            )

            logger.info(
                "[%s] Contexto da cena: "
                "pessoas=%s esquerda=%s "
                "centro=%s direita=%s "
                "muito_proximos=%s "
                "proximos=%s separados=%s",
                pacote_id,
                contexto.quantidade_pessoas,
                contexto.pessoas_esquerda,
                contexto.pessoas_centro,
                contexto.pessoas_direita,
                contexto.pares_muito_proximos,
                contexto.pares_proximos,
                contexto.pares_separados,
            )

            logger.info(
                "[%s] Descrição da cena: %s",
                pacote_id,
                contexto.descricao,
            )

            return contexto

        except Exception:
            logger.exception(
                "[%s] Não foi possível analisar "
                "o contexto da cena",
                pacote_id,
            )

            return None

    async def _renderizar_cena(
        self,
        evento,
        pacote_id: str,
    ):
        """
        Desenha as bounding boxes reais da câmera
        sobre a imagem original.

        A imagem resultante é enviada somente ao
        painel e não altera o payload do Kafka.
        """

        try:
            if evento.imagem is None:
                return None

            caminho_original = (
                evento.imagem.caminho_original
            )

            if not caminho_original:
                logger.info(
                    "[%s] Cena não renderizada: "
                    "caminho original ausente",
                    pacote_id,
                )
                return None

            bounding_boxes = (
                evento.bounding_boxes
                or []
            )

            if not bounding_boxes:
                logger.info(
                    "[%s] Cena não renderizada: "
                    "nenhuma bounding box recebida",
                    pacote_id,
                )
                return None

            resultado = await asyncio.to_thread(
                renderizar_cena_com_boxes,
                caminho_imagem=caminho_original,
                bounding_boxes=bounding_boxes,
            )

            if resultado.quantidade_boxes == 0:
                logger.info(
                    "[%s] Cena não renderizada: "
                    "nenhuma bounding box válida",
                    pacote_id,
                )
                return None

            logger.info(
                "[%s] Cena renderizada: "
                "boxes=%s dimensoes=%sx%s "
                "base64=%s caracteres",
                pacote_id,
                resultado.quantidade_boxes,
                resultado.largura_imagem,
                resultado.altura_imagem,
                len(resultado.imagem_base64),
            )

            return resultado

        except Exception:
            logger.exception(
                "[%s] Não foi possível renderizar "
                "a cena com bounding boxes",
                pacote_id,
            )

            return None

    async def _worker(
        self,
        numero: int,
    ) -> None:
        logger.info(
            "Worker %s iniciado",
            numero,
        )

        while True:
            pacote, body = (
                await self.queue.get()
            )

            try:
                adapter = (
                    camera_adapter_factory
                    .encontrar_adapter(
                        content_type=(
                            pacote.content_type
                        ),
                        body=body,
                    )
                )

                logger.info(
                    "[%s] Worker %s usando %s",
                    pacote.evento_id,
                    numero,
                    adapter.__class__.__name__,
                )

                evento = await asyncio.to_thread(
                    adapter.normalizar,
                    pacote,
                    body,
                )

                if evento.imagem is None:
                    logger.info(
                        "[%s] Evento ignorado: "
                        "não possui imagem",
                        pacote.evento_id,
                    )
                    continue

                bounding_box = (
                    evento.bounding_box_escolhida
                )

                if bounding_box is None:
                    logger.info(
                        "[%s] Evento ignorado: "
                        "não possui bounding box",
                        pacote.evento_id,
                    )
                    continue

                camera = (
                    evento.nome_camera
                    or evento.camera_id
                    or evento.ip_camera
                    or "Camera desconhecida"
                )

                caixa_rastreamento = DetectionBox(
                    x=bounding_box.x,
                    y=bounding_box.y,
                    largura=bounding_box.largura,
                    altura=bounding_box.altura,
                )

                decisao = await person_tracker.registrar(
                    camera=camera,
                    evento_id=evento.evento_id,
                    bbox=caixa_rastreamento,
                )

                logger.info(
                    "[%s] Rastreamento: "
                    "pessoa=%s status=%s "
                    "deteccoes=%s",
                    pacote.evento_id,
                    decisao.pessoa_id,
                    decisao.status,
                    decisao.quantidade_deteccoes,
                )

                aparencia_estavel = (
                    await self._analisar_aparencia(
                        evento=evento,
                        bounding_box=bounding_box,
                        pessoa_id=decisao.pessoa_id,
                        pacote_id=pacote.evento_id,
                    )
                )

                contexto_cena = (
                    await self._analisar_contexto_cena(
                        evento=evento,
                        pacote_id=pacote.evento_id,
                    )
                )

                cena_renderizada = (
                    await self._renderizar_cena(
                        evento=evento,
                        pacote_id=pacote.evento_id,
                    )
                )

                # Evento repetido:
                # atualiza o cartão, mas não vai
                # novamente para o Kafka.
                if not decisao.deve_processar:
                    atualizacao_parcial = {
                        "evento_id": evento.evento_id,
                        "pessoa_id": decisao.pessoa_id,
                        "status": "appearance_updated",
                        "quantidade_deteccoes": (
                            decisao.quantidade_deteccoes
                        ),
                        "camera": camera,
                        "aparencia": (
                            aparencia_estavel.to_dict()
                            if aparencia_estavel is not None
                            else None
                        ),
                        "contexto_cena": (
                            contexto_cena.to_dict()
                            if contexto_cena is not None
                            else None
                        ),
                        "imagem_cena": (
                            cena_renderizada.imagem_base64
                            if cena_renderizada is not None
                            else None
                        ),
                        "quantidade_boxes_cena": (
                            cena_renderizada.quantidade_boxes
                            if cena_renderizada is not None
                            else 0
                        ),
                    }

                    await event_hub.publicar(
                        atualizacao_parcial
                    )

                    logger.info(
                        "[%s] Aparência, contexto e "
                        "cena atualizados em "
                        "%s painel(is): pessoa=%s "
                        "deteccoes=%s amostras=%s "
                        "boxes=%s",
                        pacote.evento_id,
                        event_hub.total_conexoes,
                        decisao.pessoa_id,
                        decisao.quantidade_deteccoes,
                        (
                            aparencia_estavel
                            .quantidade_amostras
                            if aparencia_estavel is not None
                            else 0
                        ),
                        (
                            cena_renderizada
                            .quantidade_boxes
                            if cena_renderizada is not None
                            else 0
                        ),
                    )

                    logger.info(
                        "[%s] Evento repetido "
                        "suprimido: pessoa=%s "
                        "amostras=%s",
                        pacote.evento_id,
                        decisao.pessoa_id,
                        (
                            aparencia_estavel
                            .quantidade_amostras
                            if aparencia_estavel is not None
                            else 0
                        ),
                    )

                    continue

                try:
                    mensagem = await asyncio.to_thread(
                        montar_alarm_detection_message,
                        evento,
                    )

                except ValueError as erro:
                    logger.info(
                        "[%s] Evento ignorado: %s",
                        pacote.evento_id,
                        erro,
                    )
                    continue

                evento_painel = {
                    "evento_id": evento.evento_id,
                    "pessoa_id": decisao.pessoa_id,
                    "status": decisao.status,
                    "quantidade_deteccoes": (
                        decisao.quantidade_deteccoes
                    ),
                    "camera": mensagem.device.name,
                    "tipo": mensagem.event.type,
                    "datetime": mensagem.event.datetime,

                    # Recorte individual da pessoa.
                    "imagem": (
                        mensagem
                        .event
                        .images
                        .detection
                    ),

                    "aparencia": (
                        aparencia_estavel.to_dict()
                        if aparencia_estavel is not None
                        else None
                    ),

                    "contexto_cena": (
                        contexto_cena.to_dict()
                        if contexto_cena is not None
                        else None
                    ),

                    # Imagem completa com todas as
                    # bounding boxes válidas.
                    "imagem_cena": (
                        cena_renderizada.imagem_base64
                        if cena_renderizada is not None
                        else None
                    ),

                    "quantidade_boxes_cena": (
                        cena_renderizada.quantidade_boxes
                        if cena_renderizada is not None
                        else 0
                    ),
                }

                await event_hub.publicar(
                    evento_painel
                )

                logger.info(
                    "[%s] Evento enviado para "
                    "%s painel(is)",
                    pacote.evento_id,
                    event_hub.total_conexoes,
                )

                if not settings.kafka_enabled:
                    logger.info(
                        "[%s] Payload pronto; "
                        "Kafka desativado",
                        pacote.evento_id,
                    )
                    continue

                # O formato do Kafka permanece igual.
                # imagem_cena e contexto_cena ficam
                # apenas no painel/WebSocket.
                resultado = await self.publisher.publicar(
                    topico=self.topic,
                    evento_id=evento.evento_id,
                    dados=mensagem.model_dump(
                        mode="json"
                    ),
                )

                logger.info(
                    "[%s] Publicado em %s "
                    "p=%s offset=%s",
                    pacote.evento_id,
                    resultado["topico"],
                    resultado["particao"],
                    resultado["offset"],
                )

            except asyncio.CancelledError:
                raise

            except Exception:
                logger.exception(
                    "[%s] Erro no processamento",
                    pacote.evento_id,
                )

            finally:
                self.queue.task_done()
------------------------------------------------------------
ARQUIVO: app/services/camera_event_pipeline_backup_antes_saida.py
------------------------------------------------------------
import asyncio
import logging

from app.adapters.cameras.factory import camera_adapter_factory
from app.core.config import settings
from app.domain.models.camera_event import RawCameraPackage
from app.messaging.kafka_producer import KafkaPublisher
from app.services.alarm_detection_payload import (
    montar_alarm_detection_message,
)
from app.services.event_hub import event_hub
from app.services.person_tracker import (
    DetectionBox,
    person_tracker,
)


logger = logging.getLogger(__name__)


class CameraEventPipeline:
    def __init__(
        self,
        publisher: KafkaPublisher,
        topic: str,
        maxsize: int = 1000,
        worker_count: int = 4,
    ) -> None:
        self.publisher = publisher
        self.topic = topic
        self.worker_count = worker_count

        self.queue: asyncio.Queue[
            tuple[RawCameraPackage, bytes]
        ] = asyncio.Queue(maxsize=maxsize)

        self._worker_tasks: list[asyncio.Task] = []
        self._iniciado = False

    @property
    def tamanho_fila(self) -> int:
        return self.queue.qsize()

    @property
    def capacidade_fila(self) -> int:
        return self.queue.maxsize

    async def start(self) -> None:
        if self._iniciado:
            return

        if settings.kafka_enabled:
            await self.publisher.start()
        else:
            logger.warning(
                "Kafka desativado: eventos serão processados, "
                "mas não publicados."
            )

        self._worker_tasks = [
            asyncio.create_task(
                self._worker(numero),
                name=f"camera-worker-{numero}",
            )
            for numero in range(
                1,
                self.worker_count + 1,
            )
        ]

        self._iniciado = True

        logger.info(
            "Pipeline iniciado com %s workers",
            self.worker_count,
        )

    async def stop(self) -> None:
        if not self._iniciado:
            return

        try:
            await asyncio.wait_for(
                self.queue.join(),
                timeout=10,
            )

        except TimeoutError:
            logger.warning(
                "Encerrando com %s itens na fila",
                self.queue.qsize(),
            )

        for task in self._worker_tasks:
            task.cancel()

        await asyncio.gather(
            *self._worker_tasks,
            return_exceptions=True,
        )

        if self.publisher.iniciado:
            await self.publisher.stop()

        self._worker_tasks.clear()
        self._iniciado = False

        logger.info("Pipeline encerrado")

    def adicionar(
        self,
        pacote: RawCameraPackage,
        body: bytes,
    ) -> None:
        self.queue.put_nowait(
            (pacote, body)
        )

    async def _worker(
        self,
        numero: int,
    ) -> None:
        logger.info(
            "Worker %s iniciado",
            numero,
        )

        while True:
            pacote, body = await self.queue.get()

            try:
                adapter = (
                    camera_adapter_factory.encontrar_adapter(
                        content_type=pacote.content_type,
                        body=body,
                    )
                )

                logger.info(
                    "[%s] Worker %s usando %s",
                    pacote.evento_id,
                    numero,
                    adapter.__class__.__name__,
                )

                evento = await asyncio.to_thread(
                    adapter.normalizar,
                    pacote,
                    body,
                )

                # Eventos sem imagem não entram no rastreamento.
                if evento.imagem is None:
                    logger.info(
                        "[%s] Evento ignorado: "
                        "não possui imagem",
                        pacote.evento_id,
                    )
                    continue

                bounding_box = (
                    evento.bounding_box_escolhida
                )

                if bounding_box is None:
                    logger.info(
                        "[%s] Evento ignorado: "
                        "não possui bounding box",
                        pacote.evento_id,
                    )
                    continue

                camera = (
                    evento.nome_camera
                    or evento.camera_id
                    or evento.ip_camera
                    or "Camera desconhecida"
                )

                # Remove sessões que estão há mais de
                # 8 segundos sem novas detecções.
                saidas = (
                    await person_tracker.coletar_saidas()
                )

                for saida in saidas:
                    logger.info(
                        "[RASTREAMENTO] Pessoa saiu: "
                        "pessoa=%s camera=%s "
                        "deteccoes=%s",
                        saida.pessoa_id,
                        saida.camera,
                        saida.quantidade_deteccoes,
                    )

                caixa_rastreamento = DetectionBox(
                    x=bounding_box.x,
                    y=bounding_box.y,
                    largura=bounding_box.largura,
                    altura=bounding_box.altura,
                )

                decisao = await person_tracker.registrar(
                    camera=camera,
                    evento_id=evento.evento_id,
                    bbox=caixa_rastreamento,
                )

                logger.info(
                    "[%s] Rastreamento: "
                    "pessoa=%s status=%s "
                    "deteccoes=%s",
                    pacote.evento_id,
                    decisao.pessoa_id,
                    decisao.status,
                    decisao.quantidade_deteccoes,
                )

                # Eventos repetidos em menos de 5 segundos
                # param aqui. Não geram Base64, painel ou Kafka.
                if not decisao.deve_processar:
                    logger.info(
                        "[%s] Evento repetido suprimido: "
                        "pessoa=%s",
                        pacote.evento_id,
                        decisao.pessoa_id,
                    )
                    continue

                try:
                    mensagem = await asyncio.to_thread(
                        montar_alarm_detection_message,
                        evento,
                    )

                except ValueError as erro:
                    logger.info(
                        "[%s] Evento ignorado: %s",
                        pacote.evento_id,
                        erro,
                    )
                    continue

                evento_painel = {
                    "evento_id": evento.evento_id,
                    "pessoa_id": decisao.pessoa_id,
                    "status": decisao.status,
                    "quantidade_deteccoes": (
                        decisao.quantidade_deteccoes
                    ),
                    "camera": mensagem.device.name,
                    "tipo": mensagem.event.type,
                    "datetime": mensagem.event.datetime,
                    "imagem": (
                        mensagem.event.images.detection
                    ),
                }

                await event_hub.publicar(
                    evento_painel
                )

                logger.info(
                    "[%s] Evento enviado para "
                    "%s painel(is)",
                    pacote.evento_id,
                    event_hub.total_conexoes,
                )

                if not settings.kafka_enabled:
                    logger.info(
                        "[%s] Payload pronto; "
                        "Kafka desativado",
                        pacote.evento_id,
                    )
                    continue

                resultado = (
                    await self.publisher.publicar(
                        topico=self.topic,
                        evento_id=evento.evento_id,
                        dados=mensagem.model_dump(
                            mode="json"
                        ),
                    )
                )

                logger.info(
                    "[%s] Publicado em %s "
                    "p=%s offset=%s",
                    pacote.evento_id,
                    resultado["topico"],
                    resultado["particao"],
                    resultado["offset"],
                )

            except Exception:
                logger.exception(
                    "[%s] Erro no processamento",
                    pacote.evento_id,
                )

            finally:
                self.queue.task_done()
------------------------------------------------------------
ARQUIVO: app/services/camera_event_pipeline_backup_atualizacao_visual.py
------------------------------------------------------------
import asyncio
import logging
from datetime import datetime, timezone

from app.adapters.cameras.factory import (
    camera_adapter_factory,
)
from app.core.config import settings
from app.domain.models.camera_event import (
    RawCameraPackage,
)
from app.messaging.kafka_producer import (
    KafkaPublisher,
)
from app.services.alarm_detection_payload import (
    montar_alarm_detection_message,
)
from app.services.appearance_memory import (
    appearance_memory,
)
from app.services.event_hub import event_hub
from app.services.person_tracker import (
    DetectionBox,
    person_tracker,
)
from app.services.scene_analyzer import (
    analisar_pessoa,
)


logger = logging.getLogger(__name__)


class CameraEventPipeline:
    def __init__(
        self,
        publisher: KafkaPublisher,
        topic: str,
        maxsize: int = 1000,
        worker_count: int = 4,
    ) -> None:
        self.publisher = publisher
        self.topic = topic
        self.worker_count = worker_count

        self.queue: asyncio.Queue[
            tuple[RawCameraPackage, bytes]
        ] = asyncio.Queue(maxsize=maxsize)

        self._worker_tasks: list[asyncio.Task] = []
        self._exit_task: asyncio.Task | None = None
        self._iniciado = False

    @property
    def tamanho_fila(self) -> int:
        return self.queue.qsize()

    @property
    def capacidade_fila(self) -> int:
        return self.queue.maxsize

    async def start(self) -> None:
        if self._iniciado:
            return

        if settings.kafka_enabled:
            await self.publisher.start()
        else:
            logger.warning(
                "Kafka desativado: eventos serão "
                "processados, mas não publicados."
            )

        self._worker_tasks = [
            asyncio.create_task(
                self._worker(numero),
                name=f"camera-worker-{numero}",
            )
            for numero in range(
                1,
                self.worker_count + 1,
            )
        ]

        self._exit_task = asyncio.create_task(
            self._monitorar_saidas(),
            name="person-exit-monitor",
        )

        self._iniciado = True

        logger.info(
            "Pipeline iniciado com %s workers",
            self.worker_count,
        )

    async def stop(self) -> None:
        if not self._iniciado:
            return

        try:
            await asyncio.wait_for(
                self.queue.join(),
                timeout=10,
            )

        except TimeoutError:
            logger.warning(
                "Encerrando com %s itens na fila",
                self.queue.qsize(),
            )

        tarefas: list[asyncio.Task] = [
            *self._worker_tasks,
        ]

        if self._exit_task is not None:
            tarefas.append(self._exit_task)

        for tarefa in tarefas:
            tarefa.cancel()

        if tarefas:
            await asyncio.gather(
                *tarefas,
                return_exceptions=True,
            )

        if self.publisher.iniciado:
            await self.publisher.stop()

        await appearance_memory.limpar()

        self._worker_tasks.clear()
        self._exit_task = None
        self._iniciado = False

        logger.info("Pipeline encerrado")

    def adicionar(
        self,
        pacote: RawCameraPackage,
        body: bytes,
    ) -> None:
        self.queue.put_nowait(
            (pacote, body)
        )

    async def _monitorar_saidas(self) -> None:
        """
        Verifica a cada segundo quais pessoas estão
        há pelo menos 15 segundos sem nova imagem.
        """

        logger.info(
            "Monitor automático de saídas iniciado"
        )

        while True:
            try:
                await asyncio.sleep(1)

                saidas = (
                    await person_tracker.coletar_saidas()
                )

                for saida in saidas:
                    aparencia_final = (
                        await appearance_memory.finalizar(
                            saida.pessoa_id
                        )
                    )

                    momento_saida = (
                        datetime.now(timezone.utc)
                        .isoformat()
                        .replace("+00:00", "Z")
                    )

                    evento_painel = {
                        "evento_id": saida.evento_id,
                        "pessoa_id": saida.pessoa_id,
                        "status": "exited",
                        "quantidade_deteccoes": (
                            saida.quantidade_deteccoes
                        ),
                        "camera": saida.camera,
                        "tipo": None,
                        "datetime": momento_saida,
                        "imagem": None,
                        "aparencia": (
                            aparencia_final.to_dict()
                            if aparencia_final is not None
                            else None
                        ),
                    }

                    await event_hub.publicar(
                        evento_painel
                    )

                    logger.info(
                        "[RASTREAMENTO] Pessoa saiu: "
                        "pessoa=%s camera=%s "
                        "deteccoes=%s amostras=%s "
                        "paineis=%s",
                        saida.pessoa_id,
                        saida.camera,
                        saida.quantidade_deteccoes,
                        (
                            aparencia_final
                            .quantidade_amostras
                            if aparencia_final is not None
                            else 0
                        ),
                        event_hub.total_conexoes,
                    )

            except asyncio.CancelledError:
                logger.info(
                    "Monitor automático de saídas "
                    "encerrado"
                )
                raise

            except Exception:
                logger.exception(
                    "Erro no monitor automático "
                    "de saídas"
                )

    async def _analisar_aparencia(
        self,
        evento,
        bounding_box,
        pessoa_id: str,
        pacote_id: str,
    ):
        """
        Analisa a imagem e registra uma nova amostra
        visual para a mesma pessoa.

        Esta função também é executada para eventos
        repetidos que serão suprimidos.
        """

        try:
            if evento.imagem is None:
                return None

            caminho_original = (
                evento.imagem.caminho_original
            )

            if not caminho_original:
                logger.info(
                    "[%s] Imagem sem caminho original; "
                    "aparência não analisada",
                    pacote_id,
                )
                return None

            analise_visual = await asyncio.to_thread(
                analisar_pessoa,
                caminho_imagem=caminho_original,
                x=bounding_box.x,
                y=bounding_box.y,
                largura=bounding_box.largura,
                altura=bounding_box.altura,
            )

            aparencia_estavel = (
                await appearance_memory.registrar(
                    pessoa_id=pessoa_id,
                    analise=analise_visual,
                )
            )

            logger.info(
                "[%s] Aparência estável: "
                "pessoa=%s cor=%s posição=%s "
                "tamanho=%s área_média=%s%% "
                "amostras=%s",
                pacote_id,
                pessoa_id,
                (
                    aparencia_estavel
                    .cor_roupa_predominante
                ),
                aparencia_estavel.posicao_atual,
                (
                    aparencia_estavel
                    .tamanho_predominante
                ),
                (
                    aparencia_estavel
                    .percentual_medio_quadro
                ),
                (
                    aparencia_estavel
                    .quantidade_amostras
                ),
            )

            return aparencia_estavel

        except Exception:
            logger.exception(
                "[%s] Não foi possível analisar "
                "ou estabilizar a aparência",
                pacote_id,
            )

            return None

    async def _worker(
        self,
        numero: int,
    ) -> None:
        logger.info(
            "Worker %s iniciado",
            numero,
        )

        while True:
            pacote, body = await self.queue.get()

            try:
                adapter = (
                    camera_adapter_factory
                    .encontrar_adapter(
                        content_type=(
                            pacote.content_type
                        ),
                        body=body,
                    )
                )

                logger.info(
                    "[%s] Worker %s usando %s",
                    pacote.evento_id,
                    numero,
                    adapter.__class__.__name__,
                )

                evento = await asyncio.to_thread(
                    adapter.normalizar,
                    pacote,
                    body,
                )

                if evento.imagem is None:
                    logger.info(
                        "[%s] Evento ignorado: "
                        "não possui imagem",
                        pacote.evento_id,
                    )
                    continue

                bounding_box = (
                    evento.bounding_box_escolhida
                )

                if bounding_box is None:
                    logger.info(
                        "[%s] Evento ignorado: "
                        "não possui bounding box",
                        pacote.evento_id,
                    )
                    continue

                camera = (
                    evento.nome_camera
                    or evento.camera_id
                    or evento.ip_camera
                    or "Camera desconhecida"
                )

                caixa_rastreamento = DetectionBox(
                    x=bounding_box.x,
                    y=bounding_box.y,
                    largura=bounding_box.largura,
                    altura=bounding_box.altura,
                )

                decisao = await person_tracker.registrar(
                    camera=camera,
                    evento_id=evento.evento_id,
                    bbox=caixa_rastreamento,
                )

                logger.info(
                    "[%s] Rastreamento: "
                    "pessoa=%s status=%s "
                    "deteccoes=%s",
                    pacote.evento_id,
                    decisao.pessoa_id,
                    decisao.status,
                    decisao.quantidade_deteccoes,
                )

                # A análise acontece antes da supressão.
                # Assim, imagens repetidas contam como
                # amostras da aparência da mesma pessoa.
                aparencia_estavel = (
                    await self._analisar_aparencia(
                        evento=evento,
                        bounding_box=bounding_box,
                        pessoa_id=decisao.pessoa_id,
                        pacote_id=pacote.evento_id,
                    )
                )

                # O evento repetido atualiza a memória,
                # mas não gera Base64, painel ou Kafka.
                if not decisao.deve_processar:
                    logger.info(
                        "[%s] Evento repetido suprimido: "
                        "pessoa=%s amostras=%s",
                        pacote.evento_id,
                        decisao.pessoa_id,
                        (
                            aparencia_estavel
                            .quantidade_amostras
                            if aparencia_estavel is not None
                            else 0
                        ),
                    )
                    continue

                try:
                    mensagem = await asyncio.to_thread(
                        montar_alarm_detection_message,
                        evento,
                    )

                except ValueError as erro:
                    logger.info(
                        "[%s] Evento ignorado: %s",
                        pacote.evento_id,
                        erro,
                    )
                    continue

                evento_painel = {
                    "evento_id": evento.evento_id,
                    "pessoa_id": decisao.pessoa_id,
                    "status": decisao.status,
                    "quantidade_deteccoes": (
                        decisao.quantidade_deteccoes
                    ),
                    "camera": mensagem.device.name,
                    "tipo": mensagem.event.type,
                    "datetime": mensagem.event.datetime,
                    "imagem": (
                        mensagem
                        .event
                        .images
                        .detection
                    ),
                    "aparencia": (
                        aparencia_estavel.to_dict()
                        if aparencia_estavel is not None
                        else None
                    ),
                }

                await event_hub.publicar(
                    evento_painel
                )

                logger.info(
                    "[%s] Evento enviado para "
                    "%s painel(is)",
                    pacote.evento_id,
                    event_hub.total_conexoes,
                )

                if not settings.kafka_enabled:
                    logger.info(
                        "[%s] Payload pronto; "
                        "Kafka desativado",
                        pacote.evento_id,
                    )
                    continue

                resultado = (
                    await self.publisher.publicar(
                        topico=self.topic,
                        evento_id=evento.evento_id,
                        dados=mensagem.model_dump(
                            mode="json"
                        ),
                    )
                )

                logger.info(
                    "[%s] Publicado em %s "
                    "p=%s offset=%s",
                    pacote.evento_id,
                    resultado["topico"],
                    resultado["particao"],
                    resultado["offset"],
                )

            except asyncio.CancelledError:
                raise

            except Exception:
                logger.exception(
                    "[%s] Erro no processamento",
                    pacote.evento_id,
                )

            finally:
                self.queue.task_done()
------------------------------------------------------------
ARQUIVO: app/services/camera_event_pipeline_backup_cena_renderizada.py
------------------------------------------------------------
import asyncio
import logging
from datetime import datetime, timezone

from app.adapters.cameras.factory import (
    camera_adapter_factory,
)
from app.core.config import settings
from app.domain.models.camera_event import (
    RawCameraPackage,
)
from app.messaging.kafka_producer import (
    KafkaPublisher,
)
from app.services.alarm_detection_payload import (
    montar_alarm_detection_message,
)
from app.services.appearance_memory import (
    appearance_memory,
)
from app.services.event_hub import event_hub
from app.services.person_tracker import (
    DetectionBox,
    person_tracker,
)
from app.services.scene_analyzer import (
    analisar_pessoa,
)
from app.services.scene_context_analyzer import (
    analisar_contexto_cena,
)


logger = logging.getLogger(__name__)


class CameraEventPipeline:
    def __init__(
        self,
        publisher: KafkaPublisher,
        topic: str,
        maxsize: int = 1000,
        worker_count: int = 4,
    ) -> None:
        self.publisher = publisher
        self.topic = topic
        self.worker_count = worker_count

        self.queue: asyncio.Queue[
            tuple[RawCameraPackage, bytes]
        ] = asyncio.Queue(maxsize=maxsize)

        self._worker_tasks: list[
            asyncio.Task
        ] = []

        self._exit_task: (
            asyncio.Task | None
        ) = None

        self._iniciado = False

    @property
    def tamanho_fila(self) -> int:
        return self.queue.qsize()

    @property
    def capacidade_fila(self) -> int:
        return self.queue.maxsize

    async def start(self) -> None:
        if self._iniciado:
            return

        if settings.kafka_enabled:
            await self.publisher.start()
        else:
            logger.warning(
                "Kafka desativado: eventos serão "
                "processados, mas não publicados."
            )

        self._worker_tasks = [
            asyncio.create_task(
                self._worker(numero),
                name=f"camera-worker-{numero}",
            )
            for numero in range(
                1,
                self.worker_count + 1,
            )
        ]

        self._exit_task = asyncio.create_task(
            self._monitorar_saidas(),
            name="person-exit-monitor",
        )

        self._iniciado = True

        logger.info(
            "Pipeline iniciado com %s workers",
            self.worker_count,
        )

    async def stop(self) -> None:
        if not self._iniciado:
            return

        try:
            await asyncio.wait_for(
                self.queue.join(),
                timeout=10,
            )

        except TimeoutError:
            logger.warning(
                "Encerrando com %s itens na fila",
                self.queue.qsize(),
            )

        tarefas: list[asyncio.Task] = [
            *self._worker_tasks,
        ]

        if self._exit_task is not None:
            tarefas.append(
                self._exit_task
            )

        for tarefa in tarefas:
            tarefa.cancel()

        if tarefas:
            await asyncio.gather(
                *tarefas,
                return_exceptions=True,
            )

        if self.publisher.iniciado:
            await self.publisher.stop()

        await appearance_memory.limpar()

        self._worker_tasks.clear()
        self._exit_task = None
        self._iniciado = False

        logger.info(
            "Pipeline encerrado"
        )

    def adicionar(
        self,
        pacote: RawCameraPackage,
        body: bytes,
    ) -> None:
        self.queue.put_nowait(
            (pacote, body)
        )

    async def _monitorar_saidas(
        self,
    ) -> None:
        """
        Verifica quais pessoas estão há pelo menos
        15 segundos sem receber uma nova imagem.
        """

        logger.info(
            "Monitor automático de saídas iniciado"
        )

        while True:
            try:
                await asyncio.sleep(1)

                saidas = (
                    await person_tracker
                    .coletar_saidas()
                )

                for saida in saidas:
                    aparencia_final = (
                        await appearance_memory
                        .finalizar(
                            saida.pessoa_id
                        )
                    )

                    momento_saida = (
                        datetime.now(
                            timezone.utc
                        )
                        .isoformat()
                        .replace(
                            "+00:00",
                            "Z",
                        )
                    )

                    evento_painel = {
                        "evento_id": (
                            saida.evento_id
                        ),
                        "pessoa_id": (
                            saida.pessoa_id
                        ),
                        "status": "exited",
                        "quantidade_deteccoes": (
                            saida
                            .quantidade_deteccoes
                        ),
                        "camera": saida.camera,
                        "tipo": None,
                        "datetime": momento_saida,
                        "imagem": None,
                        "aparencia": (
                            aparencia_final.to_dict()
                            if aparencia_final
                            is not None
                            else None
                        ),
                        "contexto_cena": None,
                    }

                    await event_hub.publicar(
                        evento_painel
                    )

                    logger.info(
                        "[RASTREAMENTO] Pessoa saiu: "
                        "pessoa=%s camera=%s "
                        "deteccoes=%s amostras=%s "
                        "paineis=%s",
                        saida.pessoa_id,
                        saida.camera,
                        (
                            saida
                            .quantidade_deteccoes
                        ),
                        (
                            aparencia_final
                            .quantidade_amostras
                            if aparencia_final
                            is not None
                            else 0
                        ),
                        event_hub.total_conexoes,
                    )

            except asyncio.CancelledError:
                logger.info(
                    "Monitor automático de "
                    "saídas encerrado"
                )
                raise

            except Exception:
                logger.exception(
                    "Erro no monitor automático "
                    "de saídas"
                )

    async def _analisar_aparencia(
        self,
        evento,
        bounding_box,
        pessoa_id: str,
        pacote_id: str,
    ):
        """
        Analisa a imagem e registra uma amostra
        visual para a pessoa.

        Também é executado para eventos repetidos
        que serão suprimidos.
        """

        try:
            if evento.imagem is None:
                return None

            caminho_original = (
                evento
                .imagem
                .caminho_original
            )

            if not caminho_original:
                logger.info(
                    "[%s] Imagem sem caminho "
                    "original; aparência não "
                    "analisada",
                    pacote_id,
                )

                return None

            analise_visual = (
                await asyncio.to_thread(
                    analisar_pessoa,
                    caminho_imagem=(
                        caminho_original
                    ),
                    x=bounding_box.x,
                    y=bounding_box.y,
                    largura=(
                        bounding_box.largura
                    ),
                    altura=(
                        bounding_box.altura
                    ),
                )
            )

            aparencia_estavel = (
                await appearance_memory
                .registrar(
                    pessoa_id=pessoa_id,
                    analise=analise_visual,
                )
            )

            logger.info(
                "[%s] Aparência estável: "
                "pessoa=%s cor=%s posição=%s "
                "tamanho=%s área_média=%s%% "
                "amostras=%s",
                pacote_id,
                pessoa_id,
                (
                    aparencia_estavel
                    .cor_roupa_predominante
                ),
                (
                    aparencia_estavel
                    .posicao_atual
                ),
                (
                    aparencia_estavel
                    .tamanho_predominante
                ),
                (
                    aparencia_estavel
                    .percentual_medio_quadro
                ),
                (
                    aparencia_estavel
                    .quantidade_amostras
                ),
            )

            return aparencia_estavel

        except Exception:
            logger.exception(
                "[%s] Não foi possível analisar "
                "ou estabilizar a aparência",
                pacote_id,
            )

            return None

    async def _analisar_contexto_cena(
        self,
        evento,
        pacote_id: str,
    ):
        """
        Analisa todas as bounding boxes válidas
        recebidas no evento da câmera.
        """

        try:
            if evento.imagem is None:
                return None

            largura_imagem = (
                evento.imagem.largura
            )

            altura_imagem = (
                evento.imagem.altura
            )

            if (
                not largura_imagem
                or not altura_imagem
            ):
                logger.info(
                    "[%s] Contexto não analisado: "
                    "dimensões da imagem ausentes",
                    pacote_id,
                )

                return None

            bounding_boxes = (
                evento.bounding_boxes
                or []
            )

            contexto = (
                await asyncio.to_thread(
                    analisar_contexto_cena,
                    largura_imagem=(
                        largura_imagem
                    ),
                    altura_imagem=(
                        altura_imagem
                    ),
                    bounding_boxes=(
                        bounding_boxes
                    ),
                )
            )

            logger.info(
                "[%s] Contexto da cena: "
                "pessoas=%s esquerda=%s "
                "centro=%s direita=%s "
                "muito_proximos=%s "
                "proximos=%s separados=%s",
                pacote_id,
                contexto.quantidade_pessoas,
                contexto.pessoas_esquerda,
                contexto.pessoas_centro,
                contexto.pessoas_direita,
                (
                    contexto
                    .pares_muito_proximos
                ),
                contexto.pares_proximos,
                contexto.pares_separados,
            )

            logger.info(
                "[%s] Descrição da cena: %s",
                pacote_id,
                contexto.descricao,
            )

            return contexto

        except Exception:
            logger.exception(
                "[%s] Não foi possível analisar "
                "o contexto da cena",
                pacote_id,
            )

            return None

    async def _worker(
        self,
        numero: int,
    ) -> None:
        logger.info(
            "Worker %s iniciado",
            numero,
        )

        while True:
            pacote, body = (
                await self.queue.get()
            )

            try:
                adapter = (
                    camera_adapter_factory
                    .encontrar_adapter(
                        content_type=(
                            pacote.content_type
                        ),
                        body=body,
                    )
                )

                logger.info(
                    "[%s] Worker %s usando %s",
                    pacote.evento_id,
                    numero,
                    adapter.__class__.__name__,
                )

                evento = (
                    await asyncio.to_thread(
                        adapter.normalizar,
                        pacote,
                        body,
                    )
                )

                if evento.imagem is None:
                    logger.info(
                        "[%s] Evento ignorado: "
                        "não possui imagem",
                        pacote.evento_id,
                    )
                    continue

                bounding_box = (
                    evento
                    .bounding_box_escolhida
                )

                if bounding_box is None:
                    logger.info(
                        "[%s] Evento ignorado: "
                        "não possui bounding box",
                        pacote.evento_id,
                    )
                    continue

                camera = (
                    evento.nome_camera
                    or evento.camera_id
                    or evento.ip_camera
                    or "Camera desconhecida"
                )

                caixa_rastreamento = (
                    DetectionBox(
                        x=bounding_box.x,
                        y=bounding_box.y,
                        largura=(
                            bounding_box.largura
                        ),
                        altura=(
                            bounding_box.altura
                        ),
                    )
                )

                decisao = (
                    await person_tracker
                    .registrar(
                        camera=camera,
                        evento_id=(
                            evento.evento_id
                        ),
                        bbox=(
                            caixa_rastreamento
                        ),
                    )
                )

                logger.info(
                    "[%s] Rastreamento: "
                    "pessoa=%s status=%s "
                    "deteccoes=%s",
                    pacote.evento_id,
                    decisao.pessoa_id,
                    decisao.status,
                    (
                        decisao
                        .quantidade_deteccoes
                    ),
                )

                aparencia_estavel = (
                    await self
                    ._analisar_aparencia(
                        evento=evento,
                        bounding_box=(
                            bounding_box
                        ),
                        pessoa_id=(
                            decisao.pessoa_id
                        ),
                        pacote_id=(
                            pacote.evento_id
                        ),
                    )
                )

                contexto_cena = (
                    await self
                    ._analisar_contexto_cena(
                        evento=evento,
                        pacote_id=(
                            pacote.evento_id
                        ),
                    )
                )

                # A captura repetida não vai ao
                # Kafka e não é contada como um
                # evento novo no painel.
                #
                # Ela envia somente uma atualização
                # parcial da aparência e do contexto.
                if not decisao.deve_processar:
                    if (
                        aparencia_estavel
                        is not None
                        or contexto_cena
                        is not None
                    ):
                        atualizacao_aparencia = {
                            "evento_id": (
                                evento.evento_id
                            ),
                            "pessoa_id": (
                                decisao.pessoa_id
                            ),
                            "status": (
                                "appearance_updated"
                            ),
                            "quantidade_deteccoes": (
                                decisao
                                .quantidade_deteccoes
                            ),
                            "camera": camera,
                            "aparencia": (
                                aparencia_estavel
                                .to_dict()
                                if aparencia_estavel
                                is not None
                                else None
                            ),
                            "contexto_cena": (
                                contexto_cena
                                .to_dict()
                                if contexto_cena
                                is not None
                                else None
                            ),
                        }

                        await event_hub.publicar(
                            atualizacao_aparencia
                        )

                        logger.info(
                            "[%s] Aparência e "
                            "contexto atualizados em "
                            "%s painel(is): "
                            "pessoa=%s deteccoes=%s "
                            "amostras=%s",
                            pacote.evento_id,
                            (
                                event_hub
                                .total_conexoes
                            ),
                            decisao.pessoa_id,
                            (
                                decisao
                                .quantidade_deteccoes
                            ),
                            (
                                aparencia_estavel
                                .quantidade_amostras
                                if aparencia_estavel
                                is not None
                                else 0
                            ),
                        )

                    logger.info(
                        "[%s] Evento repetido "
                        "suprimido: pessoa=%s "
                        "amostras=%s",
                        pacote.evento_id,
                        decisao.pessoa_id,
                        (
                            aparencia_estavel
                            .quantidade_amostras
                            if aparencia_estavel
                            is not None
                            else 0
                        ),
                    )

                    continue

                try:
                    mensagem = (
                        await asyncio.to_thread(
                            montar_alarm_detection_message,
                            evento,
                        )
                    )

                except ValueError as erro:
                    logger.info(
                        "[%s] Evento ignorado: %s",
                        pacote.evento_id,
                        erro,
                    )
                    continue

                evento_painel = {
                    "evento_id": (
                        evento.evento_id
                    ),
                    "pessoa_id": (
                        decisao.pessoa_id
                    ),
                    "status": decisao.status,
                    "quantidade_deteccoes": (
                        decisao
                        .quantidade_deteccoes
                    ),
                    "camera": (
                        mensagem.device.name
                    ),
                    "tipo": (
                        mensagem.event.type
                    ),
                    "datetime": (
                        mensagem.event.datetime
                    ),
                    "imagem": (
                        mensagem
                        .event
                        .images
                        .detection
                    ),
                    "aparencia": (
                        aparencia_estavel.to_dict()
                        if aparencia_estavel
                        is not None
                        else None
                    ),
                    "contexto_cena": (
                        contexto_cena.to_dict()
                        if contexto_cena
                        is not None
                        else None
                    ),
                }

                await event_hub.publicar(
                    evento_painel
                )

                logger.info(
                    "[%s] Evento enviado para "
                    "%s painel(is)",
                    pacote.evento_id,
                    event_hub.total_conexoes,
                )

                if not settings.kafka_enabled:
                    logger.info(
                        "[%s] Payload pronto; "
                        "Kafka desativado",
                        pacote.evento_id,
                    )
                    continue

                # A análise de contexto não é
                # adicionada ao Kafka nesta etapa.
                # O formato solicitado pelo
                # supervisor permanece igual.
                resultado = (
                    await self.publisher.publicar(
                        topico=self.topic,
                        evento_id=(
                            evento.evento_id
                        ),
                        dados=(
                            mensagem.model_dump(
                                mode="json"
                            )
                        ),
                    )
                )

                logger.info(
                    "[%s] Publicado em %s "
                    "p=%s offset=%s",
                    pacote.evento_id,
                    resultado["topico"],
                    resultado["particao"],
                    resultado["offset"],
                )

            except asyncio.CancelledError:
                raise

            except Exception:
                logger.exception(
                    "[%s] Erro no processamento",
                    pacote.evento_id,
                )

            finally:
                self.queue.task_done()
------------------------------------------------------------
ARQUIVO: app/services/camera_event_pipeline_backup_contexto_cena.py
------------------------------------------------------------
import asyncio
import logging
from datetime import datetime, timezone

from app.adapters.cameras.factory import (
    camera_adapter_factory,
)
from app.core.config import settings
from app.domain.models.camera_event import (
    RawCameraPackage,
)
from app.messaging.kafka_producer import (
    KafkaPublisher,
)
from app.services.alarm_detection_payload import (
    montar_alarm_detection_message,
)
from app.services.appearance_memory import (
    appearance_memory,
)
from app.services.event_hub import event_hub
from app.services.person_tracker import (
    DetectionBox,
    person_tracker,
)
from app.services.scene_analyzer import (
    analisar_pessoa,
)


logger = logging.getLogger(__name__)


class CameraEventPipeline:
    def __init__(
        self,
        publisher: KafkaPublisher,
        topic: str,
        maxsize: int = 1000,
        worker_count: int = 4,
    ) -> None:
        self.publisher = publisher
        self.topic = topic
        self.worker_count = worker_count

        self.queue: asyncio.Queue[
            tuple[RawCameraPackage, bytes]
        ] = asyncio.Queue(maxsize=maxsize)

        self._worker_tasks: list[asyncio.Task] = []
        self._exit_task: asyncio.Task | None = None
        self._iniciado = False

    @property
    def tamanho_fila(self) -> int:
        return self.queue.qsize()

    @property
    def capacidade_fila(self) -> int:
        return self.queue.maxsize

    async def start(self) -> None:
        if self._iniciado:
            return

        if settings.kafka_enabled:
            await self.publisher.start()
        else:
            logger.warning(
                "Kafka desativado: eventos serão "
                "processados, mas não publicados."
            )

        self._worker_tasks = [
            asyncio.create_task(
                self._worker(numero),
                name=f"camera-worker-{numero}",
            )
            for numero in range(
                1,
                self.worker_count + 1,
            )
        ]

        self._exit_task = asyncio.create_task(
            self._monitorar_saidas(),
            name="person-exit-monitor",
        )

        self._iniciado = True

        logger.info(
            "Pipeline iniciado com %s workers",
            self.worker_count,
        )

    async def stop(self) -> None:
        if not self._iniciado:
            return

        try:
            await asyncio.wait_for(
                self.queue.join(),
                timeout=10,
            )

        except TimeoutError:
            logger.warning(
                "Encerrando com %s itens na fila",
                self.queue.qsize(),
            )

        tarefas: list[asyncio.Task] = [
            *self._worker_tasks,
        ]

        if self._exit_task is not None:
            tarefas.append(self._exit_task)

        for tarefa in tarefas:
            tarefa.cancel()

        if tarefas:
            await asyncio.gather(
                *tarefas,
                return_exceptions=True,
            )

        if self.publisher.iniciado:
            await self.publisher.stop()

        await appearance_memory.limpar()

        self._worker_tasks.clear()
        self._exit_task = None
        self._iniciado = False

        logger.info("Pipeline encerrado")

    def adicionar(
        self,
        pacote: RawCameraPackage,
        body: bytes,
    ) -> None:
        self.queue.put_nowait(
            (pacote, body)
        )

    async def _monitorar_saidas(self) -> None:
        """
        Verifica quais pessoas estão há pelo menos
        15 segundos sem receber uma nova imagem.
        """

        logger.info(
            "Monitor automático de saídas iniciado"
        )

        while True:
            try:
                await asyncio.sleep(1)

                saidas = (
                    await person_tracker.coletar_saidas()
                )

                for saida in saidas:
                    aparencia_final = (
                        await appearance_memory.finalizar(
                            saida.pessoa_id
                        )
                    )

                    momento_saida = (
                        datetime.now(timezone.utc)
                        .isoformat()
                        .replace("+00:00", "Z")
                    )

                    evento_painel = {
                        "evento_id": saida.evento_id,
                        "pessoa_id": saida.pessoa_id,
                        "status": "exited",
                        "quantidade_deteccoes": (
                            saida.quantidade_deteccoes
                        ),
                        "camera": saida.camera,
                        "tipo": None,
                        "datetime": momento_saida,
                        "imagem": None,
                        "aparencia": (
                            aparencia_final.to_dict()
                            if aparencia_final is not None
                            else None
                        ),
                    }

                    await event_hub.publicar(
                        evento_painel
                    )

                    logger.info(
                        "[RASTREAMENTO] Pessoa saiu: "
                        "pessoa=%s camera=%s "
                        "deteccoes=%s amostras=%s "
                        "paineis=%s",
                        saida.pessoa_id,
                        saida.camera,
                        saida.quantidade_deteccoes,
                        (
                            aparencia_final
                            .quantidade_amostras
                            if aparencia_final is not None
                            else 0
                        ),
                        event_hub.total_conexoes,
                    )

            except asyncio.CancelledError:
                logger.info(
                    "Monitor automático de saídas "
                    "encerrado"
                )
                raise

            except Exception:
                logger.exception(
                    "Erro no monitor automático "
                    "de saídas"
                )

    async def _analisar_aparencia(
        self,
        evento,
        bounding_box,
        pessoa_id: str,
        pacote_id: str,
    ):
        """
        Analisa a imagem e registra uma nova amostra
        visual para a pessoa.

        Esta função também é executada para eventos
        repetidos que serão suprimidos.
        """

        try:
            if evento.imagem is None:
                return None

            caminho_original = (
                evento.imagem.caminho_original
            )

            if not caminho_original:
                logger.info(
                    "[%s] Imagem sem caminho original; "
                    "aparência não analisada",
                    pacote_id,
                )
                return None

            analise_visual = await asyncio.to_thread(
                analisar_pessoa,
                caminho_imagem=caminho_original,
                x=bounding_box.x,
                y=bounding_box.y,
                largura=bounding_box.largura,
                altura=bounding_box.altura,
            )

            aparencia_estavel = (
                await appearance_memory.registrar(
                    pessoa_id=pessoa_id,
                    analise=analise_visual,
                )
            )

            logger.info(
                "[%s] Aparência estável: "
                "pessoa=%s cor=%s posição=%s "
                "tamanho=%s área_média=%s%% "
                "amostras=%s",
                pacote_id,
                pessoa_id,
                (
                    aparencia_estavel
                    .cor_roupa_predominante
                ),
                aparencia_estavel.posicao_atual,
                (
                    aparencia_estavel
                    .tamanho_predominante
                ),
                (
                    aparencia_estavel
                    .percentual_medio_quadro
                ),
                (
                    aparencia_estavel
                    .quantidade_amostras
                ),
            )

            return aparencia_estavel

        except Exception:
            logger.exception(
                "[%s] Não foi possível analisar "
                "ou estabilizar a aparência",
                pacote_id,
            )

            return None

    async def _worker(
        self,
        numero: int,
    ) -> None:
        logger.info(
            "Worker %s iniciado",
            numero,
        )

        while True:
            pacote, body = await self.queue.get()

            try:
                adapter = (
                    camera_adapter_factory
                    .encontrar_adapter(
                        content_type=(
                            pacote.content_type
                        ),
                        body=body,
                    )
                )

                logger.info(
                    "[%s] Worker %s usando %s",
                    pacote.evento_id,
                    numero,
                    adapter.__class__.__name__,
                )

                evento = await asyncio.to_thread(
                    adapter.normalizar,
                    pacote,
                    body,
                )

                if evento.imagem is None:
                    logger.info(
                        "[%s] Evento ignorado: "
                        "não possui imagem",
                        pacote.evento_id,
                    )
                    continue

                bounding_box = (
                    evento.bounding_box_escolhida
                )

                if bounding_box is None:
                    logger.info(
                        "[%s] Evento ignorado: "
                        "não possui bounding box",
                        pacote.evento_id,
                    )
                    continue

                camera = (
                    evento.nome_camera
                    or evento.camera_id
                    or evento.ip_camera
                    or "Camera desconhecida"
                )

                caixa_rastreamento = DetectionBox(
                    x=bounding_box.x,
                    y=bounding_box.y,
                    largura=bounding_box.largura,
                    altura=bounding_box.altura,
                )

                decisao = await person_tracker.registrar(
                    camera=camera,
                    evento_id=evento.evento_id,
                    bbox=caixa_rastreamento,
                )

                logger.info(
                    "[%s] Rastreamento: "
                    "pessoa=%s status=%s "
                    "deteccoes=%s",
                    pacote.evento_id,
                    decisao.pessoa_id,
                    decisao.status,
                    decisao.quantidade_deteccoes,
                )

                # A aparência é analisada antes da
                # verificação de supressão.
                aparencia_estavel = (
                    await self._analisar_aparencia(
                        evento=evento,
                        bounding_box=bounding_box,
                        pessoa_id=decisao.pessoa_id,
                        pacote_id=pacote.evento_id,
                    )
                )

                # A captura repetida não vai ao Kafka
                # e não é contada como um evento novo.
                #
                # Ela envia somente uma atualização
                # parcial da aparência para o cartão.
                if not decisao.deve_processar:
                    if aparencia_estavel is not None:
                        atualizacao_aparencia = {
                            "evento_id": (
                                evento.evento_id
                            ),
                            "pessoa_id": (
                                decisao.pessoa_id
                            ),
                            "status": (
                                "appearance_updated"
                            ),
                            "quantidade_deteccoes": (
                                decisao
                                .quantidade_deteccoes
                            ),
                            "camera": camera,
                            "aparencia": (
                                aparencia_estavel
                                .to_dict()
                            ),
                        }

                        await event_hub.publicar(
                            atualizacao_aparencia
                        )

                        logger.info(
                            "[%s] Aparência atualizada "
                            "em %s painel(is): "
                            "pessoa=%s deteccoes=%s "
                            "amostras=%s",
                            pacote.evento_id,
                            event_hub.total_conexoes,
                            decisao.pessoa_id,
                            (
                                decisao
                                .quantidade_deteccoes
                            ),
                            (
                                aparencia_estavel
                                .quantidade_amostras
                            ),
                        )

                    logger.info(
                        "[%s] Evento repetido "
                        "suprimido: pessoa=%s "
                        "amostras=%s",
                        pacote.evento_id,
                        decisao.pessoa_id,
                        (
                            aparencia_estavel
                            .quantidade_amostras
                            if aparencia_estavel is not None
                            else 0
                        ),
                    )

                    continue

                try:
                    mensagem = await asyncio.to_thread(
                        montar_alarm_detection_message,
                        evento,
                    )

                except ValueError as erro:
                    logger.info(
                        "[%s] Evento ignorado: %s",
                        pacote.evento_id,
                        erro,
                    )
                    continue

                evento_painel = {
                    "evento_id": evento.evento_id,
                    "pessoa_id": decisao.pessoa_id,
                    "status": decisao.status,
                    "quantidade_deteccoes": (
                        decisao.quantidade_deteccoes
                    ),
                    "camera": mensagem.device.name,
                    "tipo": mensagem.event.type,
                    "datetime": mensagem.event.datetime,
                    "imagem": (
                        mensagem
                        .event
                        .images
                        .detection
                    ),
                    "aparencia": (
                        aparencia_estavel.to_dict()
                        if aparencia_estavel is not None
                        else None
                    ),
                }

                await event_hub.publicar(
                    evento_painel
                )

                logger.info(
                    "[%s] Evento enviado para "
                    "%s painel(is)",
                    pacote.evento_id,
                    event_hub.total_conexoes,
                )

                if not settings.kafka_enabled:
                    logger.info(
                        "[%s] Payload pronto; "
                        "Kafka desativado",
                        pacote.evento_id,
                    )
                    continue

                resultado = (
                    await self.publisher.publicar(
                        topico=self.topic,
                        evento_id=evento.evento_id,
                        dados=mensagem.model_dump(
                            mode="json"
                        ),
                    )
                )

                logger.info(
                    "[%s] Publicado em %s "
                    "p=%s offset=%s",
                    pacote.evento_id,
                    resultado["topico"],
                    resultado["particao"],
                    resultado["offset"],
                )

            except asyncio.CancelledError:
                raise

            except Exception:
                logger.exception(
                    "[%s] Erro no processamento",
                    pacote.evento_id,
                )

            finally:
                self.queue.task_done()
------------------------------------------------------------
ARQUIVO: app/services/camera_event_pipeline_backup_recorte.py
------------------------------------------------------------
import asyncio
import logging
from datetime import datetime, timezone

from app.adapters.cameras.factory import camera_adapter_factory
from app.core.config import settings
from app.domain.models.camera_event import BoundingBox, CameraEvent, RawCameraPackage
from app.messaging.kafka_producer import KafkaPublisher
from app.services.alarm_detection_payload import (
    arquivo_para_base64,
    montar_alarm_detection_message,
)
from app.services.appearance_memory import appearance_memory
from app.services.event_hub import event_hub
from app.services.person_tracker import (
    DetectionBox,
    TrackingDecision,
    person_tracker,
)
from app.services.person_movement import person_movement_memory
from app.services.scene_analyzer import analisar_pessoa
from app.services.scene_context_analyzer import analisar_contexto_cena
from app.services.scene_renderer import renderizar_cena_com_boxes

logger = logging.getLogger(__name__)


class CameraEventPipeline:
    def __init__(
        self,
        publisher: KafkaPublisher,
        topic: str,
        maxsize: int = 1000,
        worker_count: int = 4,
    ) -> None:
        self.publisher = publisher
        self.topic = topic
        self.worker_count = worker_count
        self.queue: asyncio.Queue[tuple[RawCameraPackage, bytes]] = asyncio.Queue(
            maxsize=maxsize
        )
        self._worker_tasks: list[asyncio.Task] = []
        self._exit_task: asyncio.Task | None = None
        self._iniciado = False

    @property
    def tamanho_fila(self) -> int:
        return self.queue.qsize()

    @property
    def capacidade_fila(self) -> int:
        return self.queue.maxsize

    async def start(self) -> None:
        if self._iniciado:
            return

        if settings.kafka_enabled:
            await self.publisher.start()
        else:
            logger.warning(
                "Kafka desativado: eventos serão processados, mas não publicados."
            )

        self._worker_tasks = [
            asyncio.create_task(
                self._worker(numero),
                name=f"camera-worker-{numero}",
            )
            for numero in range(1, self.worker_count + 1)
        ]
        self._exit_task = asyncio.create_task(
            self._monitorar_saidas(),
            name="person-exit-monitor",
        )
        self._iniciado = True
        logger.info("Pipeline iniciado com %s workers", self.worker_count)

    async def stop(self) -> None:
        if not self._iniciado:
            return

        try:
            await asyncio.wait_for(self.queue.join(), timeout=10)
        except TimeoutError:
            logger.warning("Encerrando com %s itens na fila", self.queue.qsize())

        tarefas: list[asyncio.Task] = [*self._worker_tasks]
        if self._exit_task is not None:
            tarefas.append(self._exit_task)

        for tarefa in tarefas:
            tarefa.cancel()

        if tarefas:
            await asyncio.gather(*tarefas, return_exceptions=True)

        if self.publisher.iniciado:
            await self.publisher.stop()

        await appearance_memory.limpar()
        await person_movement_memory.limpar()
        await person_tracker.limpar()

        self._worker_tasks.clear()
        self._exit_task = None
        self._iniciado = False
        logger.info("Pipeline encerrado")

    def adicionar(self, pacote: RawCameraPackage, body: bytes) -> None:
        self.queue.put_nowait((pacote, body))

    async def _monitorar_saidas(self) -> None:
        logger.info("Monitor automático de saídas iniciado")

        while True:
            try:
                await asyncio.sleep(1)
                saidas = await person_tracker.coletar_saidas()

                for saida in saidas:
                    aparencia_final = await appearance_memory.finalizar(
                        saida.pessoa_id
                    )
                    movimento_final = await person_movement_memory.finalizar(
                        saida.pessoa_id
                    )
                    momento_saida = (
                        datetime.now(timezone.utc)
                        .isoformat()
                        .replace("+00:00", "Z")
                    )
                    evento_painel = {
                        "evento_id": saida.evento_id,
                        "pessoa_id": saida.pessoa_id,
                        "status": "exited",
                        "quantidade_deteccoes": saida.quantidade_deteccoes,
                        "camera": saida.camera,
                        "tipo": None,
                        "datetime": momento_saida,
                        "imagem": None,
                        "aparencia": (
                            aparencia_final.to_dict()
                            if aparencia_final is not None
                            else None
                        ),
                        "movimento": (
                            movimento_final.to_dict()
                            if movimento_final is not None
                            else None
                        ),
                        "contexto_cena": None,
                        "imagem_cena": None,
                        "quantidade_boxes_cena": None,
                    }
                    await event_hub.publicar(evento_painel)
                    logger.info(
                        "[RASTREAMENTO] Pessoa saiu: pessoa=%s camera=%s "
                        "deteccoes=%s amostras_visuais=%s amostras_movimento=%s "
                        "tempo_observado=%ss paineis=%s",
                        saida.pessoa_id,
                        saida.camera,
                        saida.quantidade_deteccoes,
                        (
                            aparencia_final.quantidade_amostras
                            if aparencia_final is not None
                            else 0
                        ),
                        (
                            movimento_final.quantidade_amostras
                            if movimento_final is not None
                            else 0
                        ),
                        (
                            movimento_final.tempo_observado_segundos
                            if movimento_final is not None
                            else 0
                        ),
                        event_hub.total_conexoes,
                    )

            except asyncio.CancelledError:
                logger.info("Monitor automático de saídas encerrado")
                raise
            except Exception:
                logger.exception("Erro no monitor automático de saídas")

    def _selecionar_boxes_pessoas(
        self,
        evento: CameraEvent,
    ) -> list[BoundingBox]:
        """Remove caixas inválidas, duplicadas e a caixa da imagem inteira."""

        if evento.imagem is None:
            return []

        largura_imagem = evento.imagem.largura
        altura_imagem = evento.imagem.altura
        if not largura_imagem or not altura_imagem:
            return []

        area_imagem = largura_imagem * altura_imagem
        assinaturas: set[tuple[int, int, int, int]] = set()
        caixas_validas: list[BoundingBox] = []

        for caixa in evento.bounding_boxes or []:
            if caixa.largura <= 0 or caixa.altura <= 0:
                continue

            x1 = max(0, min(caixa.x, largura_imagem - 1))
            y1 = max(0, min(caixa.y, altura_imagem - 1))
            x2 = max(x1 + 1, min(caixa.x2, largura_imagem))
            y2 = max(y1 + 1, min(caixa.y2, altura_imagem))
            largura = x2 - x1
            altura = y2 - y1
            percentual = largura * altura / area_imagem * 100

            if percentual >= 60 or percentual < 0.05:
                continue

            assinatura = (x1, y1, x2, y2)
            if assinatura in assinaturas:
                continue
            assinaturas.add(assinatura)

            if (
                x1 == caixa.x
                and y1 == caixa.y
                and largura == caixa.largura
                and altura == caixa.altura
            ):
                caixa_ajustada = caixa
            else:
                caixa_ajustada = caixa.model_copy(
                    update={
                        "x": x1,
                        "y": y1,
                        "largura": largura,
                        "altura": altura,
                        "x2": x2,
                        "y2": y2,
                    }
                )

            caixas_validas.append(caixa_ajustada)

        if not caixas_validas and evento.bounding_box_escolhida is not None:
            caixas_validas.append(evento.bounding_box_escolhida)

        caixas_validas.sort(
            key=lambda caixa: (
                caixa.x + caixa.largura / 2,
                caixa.y + caixa.altura / 2,
            )
        )
        return caixas_validas

    async def _analisar_aparencia(
        self,
        evento: CameraEvent,
        bounding_box: BoundingBox,
        pessoa_id: str,
        pacote_id: str,
    ):
        try:
            if evento.imagem is None or not evento.imagem.caminho_original:
                logger.info(
                    "[%s] Imagem sem caminho original; aparência não analisada",
                    pacote_id,
                )
                return None

            analise_visual = await asyncio.to_thread(
                analisar_pessoa,
                caminho_imagem=evento.imagem.caminho_original,
                x=bounding_box.x,
                y=bounding_box.y,
                largura=bounding_box.largura,
                altura=bounding_box.altura,
            )
            aparencia_estavel = await appearance_memory.registrar(
                pessoa_id=pessoa_id,
                analise=analise_visual,
            )
            logger.info(
                "[%s] Aparência estável: pessoa=%s cor=%s posição=%s "
                "tamanho=%s área_média=%s%% amostras=%s",
                pacote_id,
                pessoa_id,
                aparencia_estavel.cor_roupa_predominante,
                aparencia_estavel.posicao_atual,
                aparencia_estavel.tamanho_predominante,
                aparencia_estavel.percentual_medio_quadro,
                aparencia_estavel.quantidade_amostras,
            )
            return aparencia_estavel
        except Exception:
            logger.exception(
                "[%s] Não foi possível analisar a aparência da pessoa=%s",
                pacote_id,
                pessoa_id,
            )
            return None

    async def _analisar_contexto_cena(
        self,
        evento: CameraEvent,
        bounding_boxes: list[BoundingBox],
        pacote_id: str,
    ):
        try:
            if evento.imagem is None:
                return None

            largura_imagem = evento.imagem.largura
            altura_imagem = evento.imagem.altura
            if not largura_imagem or not altura_imagem:
                logger.info(
                    "[%s] Contexto não analisado: dimensões ausentes",
                    pacote_id,
                )
                return None

            contexto = await asyncio.to_thread(
                analisar_contexto_cena,
                largura_imagem=largura_imagem,
                altura_imagem=altura_imagem,
                bounding_boxes=bounding_boxes,
            )
            logger.info(
                "[%s] Contexto da cena: pessoas=%s esquerda=%s centro=%s "
                "direita=%s muito_proximos=%s proximos=%s separados=%s",
                pacote_id,
                contexto.quantidade_pessoas,
                contexto.pessoas_esquerda,
                contexto.pessoas_centro,
                contexto.pessoas_direita,
                contexto.pares_muito_proximos,
                contexto.pares_proximos,
                contexto.pares_separados,
            )
            logger.info("[%s] Descrição da cena: %s", pacote_id, contexto.descricao)
            return contexto
        except Exception:
            logger.exception("[%s] Não foi possível analisar a cena", pacote_id)
            return None

    async def _renderizar_cena(
        self,
        evento: CameraEvent,
        bounding_boxes: list[BoundingBox],
        pacote_id: str,
    ):
        try:
            if evento.imagem is None or not evento.imagem.caminho_original:
                return None
            if not bounding_boxes:
                return None

            resultado = await asyncio.to_thread(
                renderizar_cena_com_boxes,
                caminho_imagem=evento.imagem.caminho_original,
                bounding_boxes=bounding_boxes,
            )
            if resultado.quantidade_boxes == 0:
                return None

            logger.info(
                "[%s] Cena renderizada: boxes=%s dimensoes=%sx%s base64=%s caracteres",
                pacote_id,
                resultado.quantidade_boxes,
                resultado.largura_imagem,
                resultado.altura_imagem,
                len(resultado.imagem_base64),
            )
            return resultado
        except Exception:
            logger.exception(
                "[%s] Não foi possível renderizar a cena com boxes",
                pacote_id,
            )
            return None

    async def _publicar_atualizacao_parcial(
        self,
        camera: str,
        decisao: TrackingDecision,
        aparencia_estavel,
        movimento,
        contexto_cena,
        cena_renderizada,
        indice_na_cena: int,
        total_pessoas_cena: int,
        pacote_id: str,
    ) -> None:
        atualizacao_parcial = {
            "evento_id": decisao.evento_id,
            "pessoa_id": decisao.pessoa_id,
            "status": "appearance_updated",
            "quantidade_deteccoes": decisao.quantidade_deteccoes,
            "camera": camera,
            "aparencia": (
                aparencia_estavel.to_dict()
                if aparencia_estavel is not None
                else None
            ),
            "movimento": (
                movimento.to_dict()
                if movimento is not None
                else None
            ),
            "contexto_cena": (
                contexto_cena.to_dict() if contexto_cena is not None else None
            ),
            "imagem_cena": (
                cena_renderizada.imagem_base64
                if cena_renderizada is not None
                else None
            ),
            "quantidade_boxes_cena": (
                cena_renderizada.quantidade_boxes
                if cena_renderizada is not None
                else 0
            ),
            "indice_na_cena": indice_na_cena,
            "total_pessoas_cena": total_pessoas_cena,
        }
        await event_hub.publicar(atualizacao_parcial)
        logger.info(
            "[%s] Atualização parcial: pessoa=%s indice=%s/%s "
            "deteccoes=%s amostras=%s movimento=%s/%s velocidade=%spx_s "
            "boxes=%s paineis=%s",
            pacote_id,
            decisao.pessoa_id,
            indice_na_cena,
            total_pessoas_cena,
            decisao.quantidade_deteccoes,
            (
                aparencia_estavel.quantidade_amostras
                if aparencia_estavel is not None
                else 0
            ),
            (
                movimento.movimento_horizontal
                if movimento is not None
                else "-"
            ),
            (
                movimento.movimento_vertical
                if movimento is not None
                else "-"
            ),
            (
                movimento.velocidade_pixels_segundo
                if movimento is not None
                else 0
            ),
            (
                cena_renderizada.quantidade_boxes
                if cena_renderizada is not None
                else 0
            ),
            event_hub.total_conexoes,
        )

    async def _processar_pessoa(
        self,
        evento: CameraEvent,
        camera: str,
        bounding_box: BoundingBox,
        decisao: TrackingDecision,
        contexto_cena,
        cena_renderizada,
        indice_na_cena: int,
        total_pessoas_cena: int,
        pacote_id: str,
        imagem_background_base64: str | None,
    ) -> str | None:
        aparencia_estavel = await self._analisar_aparencia(
            evento=evento,
            bounding_box=bounding_box,
            pessoa_id=decisao.pessoa_id,
            pacote_id=pacote_id,
        )

        movimento = await person_movement_memory.registrar(
            pessoa_id=decisao.pessoa_id,
            bbox=decisao.bbox,
        )
        logger.info(
            "[%s] Movimento: pessoa=%s horizontal=%s vertical=%s "
            "tendencia=%s velocidade=%spx_s distancia_total=%spx "
            "tempo=%ss amostras=%s",
            pacote_id,
            decisao.pessoa_id,
            movimento.movimento_horizontal,
            movimento.movimento_vertical,
            movimento.tendencia_distancia,
            movimento.velocidade_pixels_segundo,
            movimento.distancia_total_pixels,
            movimento.tempo_observado_segundos,
            movimento.quantidade_amostras,
        )

        if not decisao.deve_processar:
            await self._publicar_atualizacao_parcial(
                camera=camera,
                decisao=decisao,
                aparencia_estavel=aparencia_estavel,
                movimento=movimento,
                contexto_cena=contexto_cena,
                cena_renderizada=cena_renderizada,
                indice_na_cena=indice_na_cena,
                total_pessoas_cena=total_pessoas_cena,
                pacote_id=pacote_id,
            )
            logger.info(
                "[%s] Evento repetido suprimido: pessoa=%s indice=%s/%s amostras=%s",
                pacote_id,
                decisao.pessoa_id,
                indice_na_cena,
                total_pessoas_cena,
                (
                    aparencia_estavel.quantidade_amostras
                    if aparencia_estavel is not None
                    else 0
                ),
            )
            return imagem_background_base64

        if evento.imagem is None or not evento.imagem.caminho_original:
            logger.info("[%s] Pessoa ignorada: caminho original ausente", pacote_id)
            return imagem_background_base64

        if imagem_background_base64 is None:
            imagem_background_base64 = await asyncio.to_thread(
                arquivo_para_base64,
                evento.imagem.caminho_original,
            )

        try:
            mensagem = await asyncio.to_thread(
                montar_alarm_detection_message,
                evento,
                bounding_box=bounding_box,
                evento_id=decisao.evento_id,
                imagem_background_base64=imagem_background_base64,
            )
        except ValueError as erro:
            logger.info(
                "[%s] Pessoa ignorada: pessoa=%s erro=%s",
                pacote_id,
                decisao.pessoa_id,
                erro,
            )
            return imagem_background_base64

        evento_painel = {
            "evento_id": decisao.evento_id,
            "pessoa_id": decisao.pessoa_id,
            "status": decisao.status,
            "quantidade_deteccoes": decisao.quantidade_deteccoes,
            "camera": mensagem.device.name,
            "tipo": mensagem.event.type,
            "datetime": mensagem.event.datetime,
            "imagem": mensagem.event.images.detection,
            "aparencia": (
                aparencia_estavel.to_dict()
                if aparencia_estavel is not None
                else None
            ),
            "movimento": movimento.to_dict(),
            "contexto_cena": (
                contexto_cena.to_dict() if contexto_cena is not None else None
            ),
            "imagem_cena": (
                cena_renderizada.imagem_base64
                if cena_renderizada is not None
                else None
            ),
            "quantidade_boxes_cena": (
                cena_renderizada.quantidade_boxes
                if cena_renderizada is not None
                else 0
            ),
            "indice_na_cena": indice_na_cena,
            "total_pessoas_cena": total_pessoas_cena,
        }
        await event_hub.publicar(evento_painel)
        logger.info(
            "[%s] Pessoa enviada ao painel: pessoa=%s status=%s indice=%s/%s paineis=%s",
            pacote_id,
            decisao.pessoa_id,
            decisao.status,
            indice_na_cena,
            total_pessoas_cena,
            event_hub.total_conexoes,
        )

        if not settings.kafka_enabled:
            logger.info(
                "[%s] Payload pronto; Kafka desativado: pessoa=%s",
                pacote_id,
                decisao.pessoa_id,
            )
            return imagem_background_base64

        resultado = await self.publisher.publicar(
            topico=self.topic,
            evento_id=decisao.evento_id,
            dados=mensagem.model_dump(mode="json"),
        )
        logger.info(
            "[%s] Pessoa publicada em %s p=%s offset=%s pessoa=%s indice=%s/%s",
            pacote_id,
            resultado["topico"],
            resultado["particao"],
            resultado["offset"],
            decisao.pessoa_id,
            indice_na_cena,
            total_pessoas_cena,
        )
        return imagem_background_base64

    async def _worker(self, numero: int) -> None:
        logger.info("Worker %s iniciado", numero)

        while True:
            pacote, body = await self.queue.get()

            try:
                adapter = camera_adapter_factory.encontrar_adapter(
                    content_type=pacote.content_type,
                    body=body,
                )
                logger.info(
                    "[%s] Worker %s usando %s",
                    pacote.evento_id,
                    numero,
                    adapter.__class__.__name__,
                )

                evento = await asyncio.to_thread(adapter.normalizar, pacote, body)
                if evento.imagem is None:
                    logger.info(
                        "[%s] Evento ignorado: não possui imagem",
                        pacote.evento_id,
                    )
                    continue

                bounding_boxes = self._selecionar_boxes_pessoas(evento)
                if not bounding_boxes:
                    logger.info(
                        "[%s] Evento ignorado: não possui boxes válidas de pessoas",
                        pacote.evento_id,
                    )
                    continue

                camera = (
                    evento.nome_camera
                    or evento.camera_id
                    or evento.ip_camera
                    or "Camera desconhecida"
                )
                caixas_rastreamento = [
                    DetectionBox(
                        x=caixa.x,
                        y=caixa.y,
                        largura=caixa.largura,
                        altura=caixa.altura,
                    )
                    for caixa in bounding_boxes
                ]
                decisoes = await person_tracker.registrar_lote(
                    camera=camera,
                    evento_id=evento.evento_id,
                    bboxes=caixas_rastreamento,
                )

                if len(decisoes) != len(bounding_boxes):
                    raise RuntimeError(
                        "Quantidade de decisões diferente da quantidade de boxes"
                    )

                total_pessoas = len(bounding_boxes)
                logger.info(
                    "[%s] Rastreamento em lote: pessoas=%s ids=%s",
                    pacote.evento_id,
                    total_pessoas,
                    [
                        (decisao.pessoa_id, decisao.status)
                        for decisao in decisoes
                    ],
                )

                contexto_cena = await self._analisar_contexto_cena(
                    evento=evento,
                    bounding_boxes=bounding_boxes,
                    pacote_id=pacote.evento_id,
                )
                cena_renderizada = await self._renderizar_cena(
                    evento=evento,
                    bounding_boxes=bounding_boxes,
                    pacote_id=pacote.evento_id,
                )

                imagem_background_base64: str | None = None
                for indice, (bounding_box, decisao) in enumerate(
                    zip(bounding_boxes, decisoes, strict=True),
                    start=1,
                ):
                    imagem_background_base64 = await self._processar_pessoa(
                        evento=evento,
                        camera=camera,
                        bounding_box=bounding_box,
                        decisao=decisao,
                        contexto_cena=contexto_cena,
                        cena_renderizada=cena_renderizada,
                        indice_na_cena=indice,
                        total_pessoas_cena=total_pessoas,
                        pacote_id=pacote.evento_id,
                        imagem_background_base64=imagem_background_base64,
                    )

            except asyncio.CancelledError:
                raise
            except Exception:
                logger.exception("[%s] Erro no processamento", pacote.evento_id)
            finally:
                self.queue.task_done()

------------------------------------------------------------
ARQUIVO: app/services/camera_event_pipeline_melhorado.py
------------------------------------------------------------
import asyncio
import logging
from datetime import datetime, timezone

from app.adapters.cameras.factory import camera_adapter_factory
from app.core.config import settings
from app.domain.models.camera_event import BoundingBox, CameraEvent, RawCameraPackage
from app.messaging.kafka_producer_melhorado import KafkaPublisher
from app.services.alarm_detection_payload import (
    arquivo_para_base64,
    montar_alarm_detection_message,
    recortar_deteccao_base64,
)
from app.services.appearance_memory import appearance_memory
from app.services.event_hub import event_hub
from app.services.person_tracker import (
    DetectionBox,
    TrackingDecision,
    person_tracker,
)
from app.services.person_movement import person_movement_memory
from app.services.person_detector_yolo import (
    combinar_bounding_boxes,
    detectar_pessoas_yolo,
)
from app.services.scene_analyzer import analisar_pessoa
from app.services.scene_context_analyzer import analisar_contexto_cena
from app.services.scene_renderer import renderizar_cena_com_boxes

logger = logging.getLogger(__name__)


class CameraEventPipeline:
    def __init__(
        self,
        publisher: KafkaPublisher,
        topic: str,
        maxsize: int = 1000,
        worker_count: int = 4,
    ) -> None:
        self.publisher = publisher
        self.topic = topic
        self.worker_count = worker_count
        self.queue: asyncio.Queue[tuple[RawCameraPackage, bytes]] = asyncio.Queue(
            maxsize=maxsize
        )
        self._worker_tasks: list[asyncio.Task] = []
        self._exit_task: asyncio.Task | None = None
        self._iniciado = False

    @property
    def tamanho_fila(self) -> int:
        return self.queue.qsize()

    @property
    def capacidade_fila(self) -> int:
        return self.queue.maxsize

    async def start(self) -> None:
        if self._iniciado:
            return

        if settings.kafka_enabled:
            await self.publisher.start()
        else:
            logger.warning(
                "Kafka desativado: eventos serão processados, mas não publicados."
            )

        self._worker_tasks = [
            asyncio.create_task(
                self._worker(numero),
                name=f"camera-worker-{numero}",
            )
            for numero in range(1, self.worker_count + 1)
        ]
        self._exit_task = asyncio.create_task(
            self._monitorar_saidas(),
            name="person-exit-monitor",
        )
        self._iniciado = True
        logger.info("Pipeline iniciado com %s workers", self.worker_count)

    async def stop(self) -> None:
        if not self._iniciado:
            return

        try:
            await asyncio.wait_for(self.queue.join(), timeout=10)
        except TimeoutError:
            logger.warning("Encerrando com %s itens na fila", self.queue.qsize())

        tarefas: list[asyncio.Task] = [*self._worker_tasks]
        if self._exit_task is not None:
            tarefas.append(self._exit_task)

        for tarefa in tarefas:
            tarefa.cancel()

        if tarefas:
            await asyncio.gather(*tarefas, return_exceptions=True)

        if self.publisher.iniciado:
            await self.publisher.stop()

        await appearance_memory.limpar()
        await person_movement_memory.limpar()
        await person_tracker.limpar()

        self._worker_tasks.clear()
        self._exit_task = None
        self._iniciado = False
        logger.info("Pipeline encerrado")

    def adicionar(self, pacote: RawCameraPackage, body: bytes) -> None:
        self.queue.put_nowait((pacote, body))

    async def _monitorar_saidas(self) -> None:
        logger.info("Monitor automático de saídas iniciado")

        while True:
            try:
                await asyncio.sleep(1)
                saidas = await person_tracker.coletar_saidas()

                for saida in saidas:
                    aparencia_final = await appearance_memory.finalizar(
                        saida.pessoa_id
                    )
                    movimento_final = await person_movement_memory.finalizar(
                        saida.pessoa_id
                    )
                    momento_saida = (
                        datetime.now(timezone.utc)
                        .isoformat()
                        .replace("+00:00", "Z")
                    )
                    evento_painel = {
                        "evento_id": saida.evento_id,
                        "pessoa_id": saida.pessoa_id,
                        "status": "exited",
                        "quantidade_deteccoes": saida.quantidade_deteccoes,
                        "camera": saida.camera,
                        "tipo": None,
                        "datetime": momento_saida,
                        "attributes": None,
                        "imagem": None,
                        "aparencia": (
                            aparencia_final.to_dict()
                            if aparencia_final is not None
                            else None
                        ),
                        "movimento": (
                            movimento_final.to_dict()
                            if movimento_final is not None
                            else None
                        ),
                        "contexto_cena": None,
                        "imagem_cena": None,
                        "quantidade_boxes_cena": None,
                    }
                    await event_hub.publicar(evento_painel)
                    logger.info(
                        "[RASTREAMENTO] Pessoa saiu: pessoa=%s camera=%s "
                        "deteccoes=%s amostras_visuais=%s amostras_movimento=%s "
                        "tempo_observado=%ss paineis=%s",
                        saida.pessoa_id,
                        saida.camera,
                        saida.quantidade_deteccoes,
                        (
                            aparencia_final.quantidade_amostras
                            if aparencia_final is not None
                            else 0
                        ),
                        (
                            movimento_final.quantidade_amostras
                            if movimento_final is not None
                            else 0
                        ),
                        (
                            movimento_final.tempo_observado_segundos
                            if movimento_final is not None
                            else 0
                        ),
                        event_hub.total_conexoes,
                    )

            except asyncio.CancelledError:
                logger.info("Monitor automático de saídas encerrado")
                raise
            except Exception:
                logger.exception("Erro no monitor automático de saídas")

    def _selecionar_boxes_pessoas(
        self,
        evento: CameraEvent,
    ) -> list[BoundingBox]:
        """Remove caixas inválidas, duplicadas e a caixa da imagem inteira."""

        if evento.imagem is None:
            return []

        largura_imagem = evento.imagem.largura
        altura_imagem = evento.imagem.altura
        if not largura_imagem or not altura_imagem:
            return []

        area_imagem = largura_imagem * altura_imagem
        assinaturas: set[tuple[int, int, int, int]] = set()
        caixas_validas: list[BoundingBox] = []

        for caixa in evento.bounding_boxes or []:
            if caixa.largura <= 0 or caixa.altura <= 0:
                continue

            x1 = max(0, min(caixa.x, largura_imagem - 1))
            y1 = max(0, min(caixa.y, altura_imagem - 1))
            x2 = max(x1 + 1, min(caixa.x2, largura_imagem))
            y2 = max(y1 + 1, min(caixa.y2, altura_imagem))
            largura = x2 - x1
            altura = y2 - y1
            percentual = largura * altura / area_imagem * 100

            if percentual >= 60 or percentual < 0.05:
                continue

            assinatura = (x1, y1, x2, y2)
            if assinatura in assinaturas:
                continue
            assinaturas.add(assinatura)

            if (
                x1 == caixa.x
                and y1 == caixa.y
                and largura == caixa.largura
                and altura == caixa.altura
            ):
                caixa_ajustada = caixa
            else:
                caixa_ajustada = caixa.model_copy(
                    update={
                        "x": x1,
                        "y": y1,
                        "largura": largura,
                        "altura": altura,
                        "x2": x2,
                        "y2": y2,
                    }
                )

            caixas_validas.append(caixa_ajustada)

        if not caixas_validas and evento.bounding_box_escolhida is not None:
            caixas_validas.append(evento.bounding_box_escolhida)

        caixas_validas.sort(
            key=lambda caixa: (
                caixa.x + caixa.largura / 2,
                caixa.y + caixa.altura / 2,
            )
        )
        return caixas_validas

    async def _analisar_aparencia(
        self,
        evento: CameraEvent,
        bounding_box: BoundingBox,
        pessoa_id: str,
        pacote_id: str,
    ):
        try:
            if evento.imagem is None or not evento.imagem.caminho_original:
                logger.info(
                    "[%s] Imagem sem caminho original; aparência não analisada",
                    pacote_id,
                )
                return None

            percentual_caixa = (
                float(
                    bounding_box.proporcao_imagem
                    or 0.0
                )
                * 100
            )

            # Uma pessoa muito pequena não possui
            # pixels de roupa suficientes para uma
            # classificação confiável.
            if (
                bounding_box.largura < 60
                or bounding_box.altura < 120
                or percentual_caixa < 1.00
            ):
                logger.info(
                    "[%s] Aparência ignorada: "
                    "caixa pequena pessoa=%s "
                    "dimensoes=%sx%s area=%.2f%%",
                    pacote_id,
                    pessoa_id,
                    bounding_box.largura,
                    bounding_box.altura,
                    percentual_caixa,
                )

                return await appearance_memory.obter(
                    pessoa_id
                )

            analise_visual = await asyncio.to_thread(
                analisar_pessoa,
                caminho_imagem=evento.imagem.caminho_original,
                x=bounding_box.x,
                y=bounding_box.y,
                largura=bounding_box.largura,
                altura=bounding_box.altura,
            )
            # Uma leitura ambígua não entra na
            # votação da memória. Caso já exista uma
            # aparência confiável, ela é preservada.
            if (
                analise_visual.cor_roupa_aproximada
                == "escura-indefinida"
            ):
                logger.info(
                    "[%s] Cor escura indefinida: "
                    "pessoa=%s rgb=%s; "
                    "mantendo aparência anterior",
                    pacote_id,
                    pessoa_id,
                    analise_visual.rgb_representativo,
                )

                return await appearance_memory.obter(
                    pessoa_id
                )

            aparencia_estavel = await appearance_memory.registrar(
                pessoa_id=pessoa_id,
                analise=analise_visual,
            )
            logger.info(
                "[%s] Aparência estável: pessoa=%s cor=%s posição=%s "
                "tamanho=%s área_média=%s%% amostras=%s",
                pacote_id,
                pessoa_id,
                aparencia_estavel.cor_roupa_predominante,
                aparencia_estavel.posicao_atual,
                aparencia_estavel.tamanho_predominante,
                aparencia_estavel.percentual_medio_quadro,
                aparencia_estavel.quantidade_amostras,
            )
            return aparencia_estavel
        except Exception:
            logger.exception(
                "[%s] Não foi possível analisar a aparência da pessoa=%s",
                pacote_id,
                pessoa_id,
            )
            return None

    async def _analisar_contexto_cena(
        self,
        evento: CameraEvent,
        bounding_boxes: list[BoundingBox],
        pacote_id: str,
    ):
        try:
            if evento.imagem is None:
                return None

            largura_imagem = evento.imagem.largura
            altura_imagem = evento.imagem.altura
            if not largura_imagem or not altura_imagem:
                logger.info(
                    "[%s] Contexto não analisado: dimensões ausentes",
                    pacote_id,
                )
                return None

            contexto = await asyncio.to_thread(
                analisar_contexto_cena,
                largura_imagem=largura_imagem,
                altura_imagem=altura_imagem,
                bounding_boxes=bounding_boxes,
            )
            logger.info(
                "[%s] Contexto da cena: pessoas=%s esquerda=%s centro=%s "
                "direita=%s muito_proximos=%s proximos=%s separados=%s",
                pacote_id,
                contexto.quantidade_pessoas,
                contexto.pessoas_esquerda,
                contexto.pessoas_centro,
                contexto.pessoas_direita,
                contexto.pares_muito_proximos,
                contexto.pares_proximos,
                contexto.pares_separados,
            )
            logger.info("[%s] Descrição da cena: %s", pacote_id, contexto.descricao)
            return contexto
        except Exception:
            logger.exception("[%s] Não foi possível analisar a cena", pacote_id)
            return None

    async def _renderizar_cena(
        self,
        evento: CameraEvent,
        bounding_boxes: list[BoundingBox],
        pacote_id: str,
    ):
        try:
            if evento.imagem is None or not evento.imagem.caminho_original:
                return None
            if not bounding_boxes:
                return None

            resultado = await asyncio.to_thread(
                renderizar_cena_com_boxes,
                caminho_imagem=evento.imagem.caminho_original,
                bounding_boxes=bounding_boxes,
            )
            if resultado.quantidade_boxes == 0:
                return None

            logger.info(
                "[%s] Cena renderizada: boxes=%s dimensoes=%sx%s base64=%s caracteres",
                pacote_id,
                resultado.quantidade_boxes,
                resultado.largura_imagem,
                resultado.altura_imagem,
                len(resultado.imagem_base64),
            )
            return resultado
        except Exception:
            logger.exception(
                "[%s] Não foi possível renderizar a cena com boxes",
                pacote_id,
            )
            return None

    async def _publicar_atualizacao_parcial(
        self,
        camera: str,
        decisao: TrackingDecision,
        aparencia_estavel,
        movimento,
        contexto_cena,
        cena_renderizada,
        imagem_recorte_base64: str | None,
        indice_na_cena: int,
        total_pessoas_cena: int,
        pacote_id: str,
    ) -> None:
        atualizacao_parcial = {
            "evento_id": decisao.evento_id,
            "pessoa_id": decisao.pessoa_id,
            "status": "appearance_updated",
            "quantidade_deteccoes": decisao.quantidade_deteccoes,
            "camera": camera,
            "imagem": imagem_recorte_base64,
            "aparencia": (
                aparencia_estavel.to_dict()
                if aparencia_estavel is not None
                else None
            ),
            "movimento": (
                movimento.to_dict()
                if movimento is not None
                else None
            ),
            "contexto_cena": (
                contexto_cena.to_dict() if contexto_cena is not None else None
            ),
            "imagem_cena": (
                cena_renderizada.imagem_base64
                if cena_renderizada is not None
                else None
            ),
            "quantidade_boxes_cena": (
                cena_renderizada.quantidade_boxes
                if cena_renderizada is not None
                else 0
            ),
            "indice_na_cena": indice_na_cena,
            "total_pessoas_cena": total_pessoas_cena,
        }
        await event_hub.publicar(atualizacao_parcial)
        logger.info(
            "[%s] Atualização parcial: pessoa=%s indice=%s/%s "
            "deteccoes=%s amostras=%s movimento=%s/%s velocidade=%spx_s "
            "boxes=%s paineis=%s",
            pacote_id,
            decisao.pessoa_id,
            indice_na_cena,
            total_pessoas_cena,
            decisao.quantidade_deteccoes,
            (
                aparencia_estavel.quantidade_amostras
                if aparencia_estavel is not None
                else 0
            ),
            (
                movimento.movimento_horizontal
                if movimento is not None
                else "-"
            ),
            (
                movimento.movimento_vertical
                if movimento is not None
                else "-"
            ),
            (
                movimento.velocidade_pixels_segundo
                if movimento is not None
                else 0
            ),
            (
                cena_renderizada.quantidade_boxes
                if cena_renderizada is not None
                else 0
            ),
            event_hub.total_conexoes,
        )

    async def _processar_pessoa(
        self,
        evento: CameraEvent,
        camera: str,
        bounding_box: BoundingBox,
        decisao: TrackingDecision,
        contexto_cena,
        cena_renderizada,
        indice_na_cena: int,
        total_pessoas_cena: int,
        pacote_id: str,
        imagem_background_base64: str | None,
    ) -> str | None:
        aparencia_estavel = await self._analisar_aparencia(
            evento=evento,
            bounding_box=bounding_box,
            pessoa_id=decisao.pessoa_id,
            pacote_id=pacote_id,
        )

        movimento = await person_movement_memory.registrar(
            pessoa_id=decisao.pessoa_id,
            bbox=decisao.bbox,
        )
        logger.info(
            "[%s] Movimento: pessoa=%s horizontal=%s vertical=%s "
            "tendencia=%s velocidade=%spx_s distancia_total=%spx "
            "tempo=%ss amostras=%s",
            pacote_id,
            decisao.pessoa_id,
            movimento.movimento_horizontal,
            movimento.movimento_vertical,
            movimento.tendencia_distancia,
            movimento.velocidade_pixels_segundo,
            movimento.distancia_total_pixels,
            movimento.tempo_observado_segundos,
            movimento.quantidade_amostras,
        )

        if not decisao.deve_processar:
            imagem_recorte_base64 = None

            if (
                evento.imagem is not None
                and evento.imagem.caminho_original
            ):
                try:
                    imagem_recorte_base64 = (
                        await asyncio.to_thread(
                            recortar_deteccao_base64,
                            evento.imagem.caminho_original,
                            bounding_box,
                        )
                    )
                except Exception:
                    logger.exception(
                        "[%s] Não foi possível atualizar "
                        "o recorte da pessoa=%s",
                        pacote_id,
                        decisao.pessoa_id,
                    )

            await self._publicar_atualizacao_parcial(
                camera=camera,
                decisao=decisao,
                aparencia_estavel=aparencia_estavel,
                movimento=movimento,
                contexto_cena=contexto_cena,
                cena_renderizada=cena_renderizada,
                imagem_recorte_base64=(
                    imagem_recorte_base64
                ),
                indice_na_cena=indice_na_cena,
                total_pessoas_cena=total_pessoas_cena,
                pacote_id=pacote_id,
            )
            logger.info(
                "[%s] Evento repetido suprimido: pessoa=%s indice=%s/%s amostras=%s",
                pacote_id,
                decisao.pessoa_id,
                indice_na_cena,
                total_pessoas_cena,
                (
                    aparencia_estavel.quantidade_amostras
                    if aparencia_estavel is not None
                    else 0
                ),
            )
            return imagem_background_base64

        if evento.imagem is None or not evento.imagem.caminho_original:
            logger.info("[%s] Pessoa ignorada: caminho original ausente", pacote_id)
            return imagem_background_base64

        if imagem_background_base64 is None:
            imagem_background_base64 = await asyncio.to_thread(
                arquivo_para_base64,
                evento.imagem.caminho_original,
            )

        try:
            mensagem = await asyncio.to_thread(
                montar_alarm_detection_message,
                evento,
                bounding_box=bounding_box,
                evento_id=decisao.evento_id,
                imagem_background_base64=imagem_background_base64,
            )
        except ValueError as erro:
            logger.info(
                "[%s] Pessoa ignorada: pessoa=%s erro=%s",
                pacote_id,
                decisao.pessoa_id,
                erro,
            )
            return imagem_background_base64

        evento_painel = {
            "evento_id": decisao.evento_id,
            "pessoa_id": decisao.pessoa_id,
            "status": decisao.status,
            "quantidade_deteccoes": decisao.quantidade_deteccoes,
            "camera": mensagem.device.name,
            "tipo": mensagem.event.type,
            "datetime": mensagem.event.datetime,
            "attributes": (
                mensagem.event.attributes.model_dump(
                    mode="json"
                )
                if mensagem.event.attributes is not None
                else None
            ),
            "imagem": mensagem.event.images.detection,
            "aparencia": (
                aparencia_estavel.to_dict()
                if aparencia_estavel is not None
                else None
            ),
            "movimento": movimento.to_dict(),
            "contexto_cena": (
                contexto_cena.to_dict() if contexto_cena is not None else None
            ),
            "imagem_cena": (
                cena_renderizada.imagem_base64
                if cena_renderizada is not None
                else None
            ),
            "quantidade_boxes_cena": (
                cena_renderizada.quantidade_boxes
                if cena_renderizada is not None
                else 0
            ),
            "indice_na_cena": indice_na_cena,
            "total_pessoas_cena": total_pessoas_cena,
        }
        await event_hub.publicar(evento_painel)
        logger.info(
            "[%s] Pessoa enviada ao painel: pessoa=%s status=%s indice=%s/%s paineis=%s",
            pacote_id,
            decisao.pessoa_id,
            decisao.status,
            indice_na_cena,
            total_pessoas_cena,
            event_hub.total_conexoes,
        )

        if not settings.kafka_enabled:
            logger.info(
                "[%s] Payload pronto; Kafka desativado: pessoa=%s",
                pacote_id,
                decisao.pessoa_id,
            )
            return imagem_background_base64

        resultado = await self.publisher.publicar(
            topico=self.topic,
            evento_id=decisao.evento_id,
            dados=mensagem.model_dump(
                mode="json",
                by_alias=True,
                exclude_none=True,
            ),
        )
        logger.info(
            "[%s] Pessoa publicada em %s p=%s offset=%s pessoa=%s indice=%s/%s",
            pacote_id,
            resultado["topico"],
            resultado["particao"],
            resultado["offset"],
            decisao.pessoa_id,
            indice_na_cena,
            total_pessoas_cena,
        )
        return imagem_background_base64

    async def _worker(self, numero: int) -> None:
        logger.info("Worker %s iniciado", numero)

        while True:
            pacote, body = await self.queue.get()

            try:
                adapter = camera_adapter_factory.encontrar_adapter(
                    content_type=pacote.content_type,
                    body=body,
                )
                logger.info(
                    "[%s] Worker %s usando %s",
                    pacote.evento_id,
                    numero,
                    adapter.__class__.__name__,
                )

                evento = await asyncio.to_thread(adapter.normalizar, pacote, body)

                logger.info(
                    "[%s] OBJETO NORMALIZADO (%s):\n%s",
                    pacote.evento_id,
                    evento.fabricante,
                    evento.model_dump_json(indent=2, by_alias=True, exclude_none=True),
                )

                if evento.imagem is None:
                    logger.info(
                        "[%s] Evento ignorado: não possui imagem",
                        pacote.evento_id,
                    )
                    continue

                bounding_boxes_camera = (
                    self._selecionar_boxes_pessoas(
                        evento
                    )
                )

                bounding_boxes_yolo: list[
                    BoundingBox
                ] = []

                if evento.imagem.caminho_original:
                    try:
                        bounding_boxes_yolo = (
                            await asyncio.to_thread(
                                detectar_pessoas_yolo,
                                evento.imagem.caminho_original,
                            )
                        )
                    except Exception:
                        logger.exception(
                            "[%s] Falha no YOLO; "
                            "usando caixas da câmera",
                            pacote.evento_id,
                        )

                bounding_boxes = combinar_bounding_boxes(
                    caixas_camera=bounding_boxes_camera,
                    caixas_yolo=bounding_boxes_yolo,
                )

                logger.info(
                    "[%s] Boxes: câmera=%s "
                    "yolo=%s final=%s",
                    pacote.evento_id,
                    len(bounding_boxes_camera),
                    len(bounding_boxes_yolo),
                    len(bounding_boxes),
                )
                if not bounding_boxes:
                    logger.info(
                        "[%s] Evento ignorado: não possui boxes válidas de pessoas",
                        pacote.evento_id,
                    )
                    continue

                camera = (
                    evento.nome_camera
                    or evento.camera_id
                    or evento.ip_camera
                    or "Camera desconhecida"
                )
                caixas_rastreamento = [
                    DetectionBox(
                        x=caixa.x,
                        y=caixa.y,
                        largura=caixa.largura,
                        altura=caixa.altura,
                    )
                    for caixa in bounding_boxes
                ]
                decisoes = await person_tracker.registrar_lote(
                    camera=camera,
                    evento_id=evento.evento_id,
                    bboxes=caixas_rastreamento,
                )

                if len(decisoes) != len(bounding_boxes):
                    raise RuntimeError(
                        "Quantidade de decisões diferente da quantidade de boxes"
                    )

                total_pessoas = len(bounding_boxes)
                logger.info(
                    "[%s] Rastreamento em lote: pessoas=%s ids=%s",
                    pacote.evento_id,
                    total_pessoas,
                    [
                        (decisao.pessoa_id, decisao.status)
                        for decisao in decisoes
                    ],
                )

                contexto_cena = await self._analisar_contexto_cena(
                    evento=evento,
                    bounding_boxes=bounding_boxes,
                    pacote_id=pacote.evento_id,
                )
                cena_renderizada = await self._renderizar_cena(
                    evento=evento,
                    bounding_boxes=bounding_boxes,
                    pacote_id=pacote.evento_id,
                )

                imagem_background_base64: str | None = None
                for indice, (bounding_box, decisao) in enumerate(
                    zip(bounding_boxes, decisoes, strict=True),
                    start=1,
                ):
                    imagem_background_base64 = await self._processar_pessoa(
                        evento=evento,
                        camera=camera,
                        bounding_box=bounding_box,
                        decisao=decisao,
                        contexto_cena=contexto_cena,
                        cena_renderizada=cena_renderizada,
                        indice_na_cena=indice,
                        total_pessoas_cena=total_pessoas,
                        pacote_id=pacote.evento_id,
                        imagem_background_base64=imagem_background_base64,
                    )

            except asyncio.CancelledError:
                raise
            except Exception:
                logger.exception("[%s] Erro no processamento", pacote.evento_id)
            finally:
                self.queue.task_done()

------------------------------------------------------------
ARQUIVO: app/services/camera_event_pipeline.py
------------------------------------------------------------
import asyncio
import logging
from datetime import datetime, timezone

from app.adapters.cameras.factory import camera_adapter_factory
from app.core.config import settings
from app.domain.models.camera_event import BoundingBox, CameraEvent, RawCameraPackage
from app.messaging.kafka_producer import KafkaPublisher
from app.services.alarm_detection_payload import (
    arquivo_para_base64,
    montar_alarm_detection_message,
    recortar_deteccao_base64,
)
from app.services.appearance_memory import appearance_memory
from app.services.event_hub import event_hub
from app.services.person_tracker import (
    DetectionBox,
    TrackingDecision,
    person_tracker,
)
from app.services.person_movement import person_movement_memory
from app.services.scene_analyzer import analisar_pessoa
from app.services.scene_context_analyzer import analisar_contexto_cena
from app.services.scene_renderer import renderizar_cena_com_boxes

logger = logging.getLogger(__name__)


class CameraEventPipeline:
    def __init__(
        self,
        publisher: KafkaPublisher,
        topic: str,
        maxsize: int = 1000,
        worker_count: int = 4,
    ) -> None:
        self.publisher = publisher
        self.topic = topic
        self.worker_count = worker_count
        self.queue: asyncio.Queue[tuple[RawCameraPackage, bytes]] = asyncio.Queue(
            maxsize=maxsize
        )
        self._worker_tasks: list[asyncio.Task] = []
        self._exit_task: asyncio.Task | None = None
        self._iniciado = False

    @property
    def tamanho_fila(self) -> int:
        return self.queue.qsize()

    @property
    def capacidade_fila(self) -> int:
        return self.queue.maxsize

    async def start(self) -> None:
        if self._iniciado:
            return

        if settings.kafka_enabled:
            await self.publisher.start()
        else:
            logger.warning(
                "Kafka desativado: eventos serão processados, mas não publicados."
            )

        self._worker_tasks = [
            asyncio.create_task(
                self._worker(numero),
                name=f"camera-worker-{numero}",
            )
            for numero in range(1, self.worker_count + 1)
        ]
        self._exit_task = asyncio.create_task(
            self._monitorar_saidas(),
            name="person-exit-monitor",
        )
        self._iniciado = True
        logger.info("Pipeline iniciado com %s workers", self.worker_count)

    async def stop(self) -> None:
        if not self._iniciado:
            return

        try:
            await asyncio.wait_for(self.queue.join(), timeout=10)
        except TimeoutError:
            logger.warning("Encerrando com %s itens na fila", self.queue.qsize())

        tarefas: list[asyncio.Task] = [*self._worker_tasks]
        if self._exit_task is not None:
            tarefas.append(self._exit_task)

        for tarefa in tarefas:
            tarefa.cancel()

        if tarefas:
            await asyncio.gather(*tarefas, return_exceptions=True)

        if self.publisher.iniciado:
            await self.publisher.stop()

        await appearance_memory.limpar()
        await person_movement_memory.limpar()
        await person_tracker.limpar()

        self._worker_tasks.clear()
        self._exit_task = None
        self._iniciado = False
        logger.info("Pipeline encerrado")

    def adicionar(self, pacote: RawCameraPackage, body: bytes) -> None:
        self.queue.put_nowait((pacote, body))

    async def _monitorar_saidas(self) -> None:
        logger.info("Monitor automático de saídas iniciado")

        while True:
            try:
                await asyncio.sleep(1)
                saidas = await person_tracker.coletar_saidas()

                for saida in saidas:
                    aparencia_final = await appearance_memory.finalizar(
                        saida.pessoa_id
                    )
                    movimento_final = await person_movement_memory.finalizar(
                        saida.pessoa_id
                    )
                    momento_saida = (
                        datetime.now(timezone.utc)
                        .isoformat()
                        .replace("+00:00", "Z")
                    )
                    evento_painel = {
                        "evento_id": saida.evento_id,
                        "pessoa_id": saida.pessoa_id,
                        "status": "exited",
                        "quantidade_deteccoes": saida.quantidade_deteccoes,
                        "camera": saida.camera,
                        "tipo": None,
                        "datetime": momento_saida,
                        "attributes": None,
                        "imagem": None,
                        "aparencia": (
                            aparencia_final.to_dict()
                            if aparencia_final is not None
                            else None
                        ),
                        "movimento": (
                            movimento_final.to_dict()
                            if movimento_final is not None
                            else None
                        ),
                        "contexto_cena": None,
                        "imagem_cena": None,
                        "quantidade_boxes_cena": None,
                    }
                    await event_hub.publicar(evento_painel)
                    logger.info(
                        "[RASTREAMENTO] Pessoa saiu: pessoa=%s camera=%s "
                        "deteccoes=%s amostras_visuais=%s amostras_movimento=%s "
                        "tempo_observado=%ss paineis=%s",
                        saida.pessoa_id,
                        saida.camera,
                        saida.quantidade_deteccoes,
                        (
                            aparencia_final.quantidade_amostras
                            if aparencia_final is not None
                            else 0
                        ),
                        (
                            movimento_final.quantidade_amostras
                            if movimento_final is not None
                            else 0
                        ),
                        (
                            movimento_final.tempo_observado_segundos
                            if movimento_final is not None
                            else 0
                        ),
                        event_hub.total_conexoes,
                    )

            except asyncio.CancelledError:
                logger.info("Monitor automático de saídas encerrado")
                raise
            except Exception:
                logger.exception("Erro no monitor automático de saídas")

    def _selecionar_boxes_pessoas(
        self,
        evento: CameraEvent,
    ) -> list[BoundingBox]:
        """Remove caixas inválidas, duplicadas e a caixa da imagem inteira."""

        if evento.imagem is None:
            return []

        largura_imagem = evento.imagem.largura
        altura_imagem = evento.imagem.altura
        if not largura_imagem or not altura_imagem:
            return []

        area_imagem = largura_imagem * altura_imagem
        assinaturas: set[tuple[int, int, int, int]] = set()
        caixas_validas: list[BoundingBox] = []

        for caixa in evento.bounding_boxes or []:
            if caixa.largura <= 0 or caixa.altura <= 0:
                continue

            x1 = max(0, min(caixa.x, largura_imagem - 1))
            y1 = max(0, min(caixa.y, altura_imagem - 1))
            x2 = max(x1 + 1, min(caixa.x2, largura_imagem))
            y2 = max(y1 + 1, min(caixa.y2, altura_imagem))
            largura = x2 - x1
            altura = y2 - y1
            percentual = largura * altura / area_imagem * 100

            if percentual >= 60 or percentual < 0.05:
                continue

            assinatura = (x1, y1, x2, y2)
            if assinatura in assinaturas:
                continue
            assinaturas.add(assinatura)

            if (
                x1 == caixa.x
                and y1 == caixa.y
                and largura == caixa.largura
                and altura == caixa.altura
            ):
                caixa_ajustada = caixa
            else:
                caixa_ajustada = caixa.model_copy(
                    update={
                        "x": x1,
                        "y": y1,
                        "largura": largura,
                        "altura": altura,
                        "x2": x2,
                        "y2": y2,
                    }
                )

            caixas_validas.append(caixa_ajustada)

        if not caixas_validas and evento.bounding_box_escolhida is not None:
            caixas_validas.append(evento.bounding_box_escolhida)

        caixas_validas.sort(
            key=lambda caixa: (
                caixa.x + caixa.largura / 2,
                caixa.y + caixa.altura / 2,
            )
        )
        return caixas_validas

    async def _analisar_aparencia(
        self,
        evento: CameraEvent,
        bounding_box: BoundingBox,
        pessoa_id: str,
        pacote_id: str,
    ):
        try:
            if evento.imagem is None or not evento.imagem.caminho_original:
                logger.info(
                    "[%s] Imagem sem caminho original; aparência não analisada",
                    pacote_id,
                )
                return None

            percentual_caixa = (
                float(
                    bounding_box.proporcao_imagem
                    or 0.0
                )
                * 100
            )

            # Uma pessoa muito pequena não possui
            # pixels de roupa suficientes para uma
            # classificação confiável.
            if (
                bounding_box.largura < 60
                or bounding_box.altura < 120
                or percentual_caixa < 1.00
            ):
                logger.info(
                    "[%s] Aparência ignorada: "
                    "caixa pequena pessoa=%s "
                    "dimensoes=%sx%s area=%.2f%%",
                    pacote_id,
                    pessoa_id,
                    bounding_box.largura,
                    bounding_box.altura,
                    percentual_caixa,
                )

                return await appearance_memory.obter(
                    pessoa_id
                )

            analise_visual = await asyncio.to_thread(
                analisar_pessoa,
                caminho_imagem=evento.imagem.caminho_original,
                x=bounding_box.x,
                y=bounding_box.y,
                largura=bounding_box.largura,
                altura=bounding_box.altura,
            )
            # Uma leitura ambígua não entra na
            # votação da memória. Caso já exista uma
            # aparência confiável, ela é preservada.
            if (
                analise_visual.cor_roupa_aproximada
                == "escura-indefinida"
            ):
                logger.info(
                    "[%s] Cor escura indefinida: "
                    "pessoa=%s rgb=%s; "
                    "mantendo aparência anterior",
                    pacote_id,
                    pessoa_id,
                    analise_visual.rgb_representativo,
                )

                return await appearance_memory.obter(
                    pessoa_id
                )

            aparencia_estavel = await appearance_memory.registrar(
                pessoa_id=pessoa_id,
                analise=analise_visual,
            )
            logger.info(
                "[%s] Aparência estável: pessoa=%s cor=%s posição=%s "
                "tamanho=%s área_média=%s%% amostras=%s",
                pacote_id,
                pessoa_id,
                aparencia_estavel.cor_roupa_predominante,
                aparencia_estavel.posicao_atual,
                aparencia_estavel.tamanho_predominante,
                aparencia_estavel.percentual_medio_quadro,
                aparencia_estavel.quantidade_amostras,
            )
            return aparencia_estavel
        except Exception:
            logger.exception(
                "[%s] Não foi possível analisar a aparência da pessoa=%s",
                pacote_id,
                pessoa_id,
            )
            return None

    async def _analisar_contexto_cena(
        self,
        evento: CameraEvent,
        bounding_boxes: list[BoundingBox],
        pacote_id: str,
    ):
        try:
            if evento.imagem is None:
                return None

            largura_imagem = evento.imagem.largura
            altura_imagem = evento.imagem.altura
            if not largura_imagem or not altura_imagem:
                logger.info(
                    "[%s] Contexto não analisado: dimensões ausentes",
                    pacote_id,
                )
                return None

            contexto = await asyncio.to_thread(
                analisar_contexto_cena,
                largura_imagem=largura_imagem,
                altura_imagem=altura_imagem,
                bounding_boxes=bounding_boxes,
            )
            logger.info(
                "[%s] Contexto da cena: pessoas=%s esquerda=%s centro=%s "
                "direita=%s muito_proximos=%s proximos=%s separados=%s",
                pacote_id,
                contexto.quantidade_pessoas,
                contexto.pessoas_esquerda,
                contexto.pessoas_centro,
                contexto.pessoas_direita,
                contexto.pares_muito_proximos,
                contexto.pares_proximos,
                contexto.pares_separados,
            )
            logger.info("[%s] Descrição da cena: %s", pacote_id, contexto.descricao)
            return contexto
        except Exception:
            logger.exception("[%s] Não foi possível analisar a cena", pacote_id)
            return None

    async def _renderizar_cena(
        self,
        evento: CameraEvent,
        bounding_boxes: list[BoundingBox],
        pacote_id: str,
    ):
        try:
            if evento.imagem is None or not evento.imagem.caminho_original:
                return None
            if not bounding_boxes:
                return None

            resultado = await asyncio.to_thread(
                renderizar_cena_com_boxes,
                caminho_imagem=evento.imagem.caminho_original,
                bounding_boxes=bounding_boxes,
            )
            if resultado.quantidade_boxes == 0:
                return None

            logger.info(
                "[%s] Cena renderizada: boxes=%s dimensoes=%sx%s base64=%s caracteres",
                pacote_id,
                resultado.quantidade_boxes,
                resultado.largura_imagem,
                resultado.altura_imagem,
                len(resultado.imagem_base64),
            )
            return resultado
        except Exception:
            logger.exception(
                "[%s] Não foi possível renderizar a cena com boxes",
                pacote_id,
            )
            return None

    async def _publicar_atualizacao_parcial(
        self,
        camera: str,
        decisao: TrackingDecision,
        aparencia_estavel,
        movimento,
        contexto_cena,
        cena_renderizada,
        imagem_recorte_base64: str | None,
        indice_na_cena: int,
        total_pessoas_cena: int,
        pacote_id: str,
    ) -> None:
        atualizacao_parcial = {
            "evento_id": decisao.evento_id,
            "pessoa_id": decisao.pessoa_id,
            "status": "appearance_updated",
            "quantidade_deteccoes": decisao.quantidade_deteccoes,
            "camera": camera,
            "imagem": imagem_recorte_base64,
            "aparencia": (
                aparencia_estavel.to_dict()
                if aparencia_estavel is not None
                else None
            ),
            "movimento": (
                movimento.to_dict()
                if movimento is not None
                else None
            ),
            "contexto_cena": (
                contexto_cena.to_dict() if contexto_cena is not None else None
            ),
            "imagem_cena": (
                cena_renderizada.imagem_base64
                if cena_renderizada is not None
                else None
            ),
            "quantidade_boxes_cena": (
                cena_renderizada.quantidade_boxes
                if cena_renderizada is not None
                else 0
            ),
            "indice_na_cena": indice_na_cena,
            "total_pessoas_cena": total_pessoas_cena,
        }
        await event_hub.publicar(atualizacao_parcial)
        logger.info(
            "[%s] Atualização parcial: pessoa=%s indice=%s/%s "
            "deteccoes=%s amostras=%s movimento=%s/%s velocidade=%spx_s "
            "boxes=%s paineis=%s",
            pacote_id,
            decisao.pessoa_id,
            indice_na_cena,
            total_pessoas_cena,
            decisao.quantidade_deteccoes,
            (
                aparencia_estavel.quantidade_amostras
                if aparencia_estavel is not None
                else 0
            ),
            (
                movimento.movimento_horizontal
                if movimento is not None
                else "-"
            ),
            (
                movimento.movimento_vertical
                if movimento is not None
                else "-"
            ),
            (
                movimento.velocidade_pixels_segundo
                if movimento is not None
                else 0
            ),
            (
                cena_renderizada.quantidade_boxes
                if cena_renderizada is not None
                else 0
            ),
            event_hub.total_conexoes,
        )

    async def _processar_pessoa(
        self,
        evento: CameraEvent,
        camera: str,
        bounding_box: BoundingBox,
        decisao: TrackingDecision,
        contexto_cena,
        cena_renderizada,
        indice_na_cena: int,
        total_pessoas_cena: int,
        pacote_id: str,
        imagem_background_base64: str | None,
    ) -> str | None:
        aparencia_estavel = await self._analisar_aparencia(
            evento=evento,
            bounding_box=bounding_box,
            pessoa_id=decisao.pessoa_id,
            pacote_id=pacote_id,
        )

        movimento = await person_movement_memory.registrar(
            pessoa_id=decisao.pessoa_id,
            bbox=decisao.bbox,
        )
        logger.info(
            "[%s] Movimento: pessoa=%s horizontal=%s vertical=%s "
            "tendencia=%s velocidade=%spx_s distancia_total=%spx "
            "tempo=%ss amostras=%s",
            pacote_id,
            decisao.pessoa_id,
            movimento.movimento_horizontal,
            movimento.movimento_vertical,
            movimento.tendencia_distancia,
            movimento.velocidade_pixels_segundo,
            movimento.distancia_total_pixels,
            movimento.tempo_observado_segundos,
            movimento.quantidade_amostras,
        )

        if not decisao.deve_processar:
            imagem_recorte_base64 = None

            if (
                evento.imagem is not None
                and evento.imagem.caminho_original
            ):
                try:
                    imagem_recorte_base64 = (
                        await asyncio.to_thread(
                            recortar_deteccao_base64,
                            evento.imagem.caminho_original,
                            bounding_box,
                        )
                    )
                except Exception:
                    logger.exception(
                        "[%s] Não foi possível atualizar "
                        "o recorte da pessoa=%s",
                        pacote_id,
                        decisao.pessoa_id,
                    )

            await self._publicar_atualizacao_parcial(
                camera=camera,
                decisao=decisao,
                aparencia_estavel=aparencia_estavel,
                movimento=movimento,
                contexto_cena=contexto_cena,
                cena_renderizada=cena_renderizada,
                imagem_recorte_base64=(
                    imagem_recorte_base64
                ),
                indice_na_cena=indice_na_cena,
                total_pessoas_cena=total_pessoas_cena,
                pacote_id=pacote_id,
            )
            logger.info(
                "[%s] Evento repetido suprimido: pessoa=%s indice=%s/%s amostras=%s",
                pacote_id,
                decisao.pessoa_id,
                indice_na_cena,
                total_pessoas_cena,
                (
                    aparencia_estavel.quantidade_amostras
                    if aparencia_estavel is not None
                    else 0
                ),
            )
            return imagem_background_base64

        if evento.imagem is None or not evento.imagem.caminho_original:
            logger.info("[%s] Pessoa ignorada: caminho original ausente", pacote_id)
            return imagem_background_base64

        if imagem_background_base64 is None:
            imagem_background_base64 = await asyncio.to_thread(
                arquivo_para_base64,
                evento.imagem.caminho_original,
            )

        try:
            mensagem = await asyncio.to_thread(
                montar_alarm_detection_message,
                evento,
                bounding_box=bounding_box,
                evento_id=decisao.evento_id,
                imagem_background_base64=imagem_background_base64,
            )
        except ValueError as erro:
            logger.info(
                "[%s] Pessoa ignorada: pessoa=%s erro=%s",
                pacote_id,
                decisao.pessoa_id,
                erro,
            )
            return imagem_background_base64

        evento_painel = {
            "evento_id": decisao.evento_id,
            "pessoa_id": decisao.pessoa_id,
            "status": decisao.status,
            "quantidade_deteccoes": decisao.quantidade_deteccoes,
            "camera": mensagem.device.name,
            "tipo": mensagem.event.type,
            "datetime": mensagem.event.datetime,
            "attributes": (
                mensagem.event.attributes.model_dump(
                    mode="json"
                )
                if mensagem.event.attributes is not None
                else None
            ),
            "imagem": mensagem.event.images.detection,
            "aparencia": (
                aparencia_estavel.to_dict()
                if aparencia_estavel is not None
                else None
            ),
            "movimento": movimento.to_dict(),
            "contexto_cena": (
                contexto_cena.to_dict() if contexto_cena is not None else None
            ),
            "imagem_cena": (
                cena_renderizada.imagem_base64
                if cena_renderizada is not None
                else None
            ),
            "quantidade_boxes_cena": (
                cena_renderizada.quantidade_boxes
                if cena_renderizada is not None
                else 0
            ),
            "indice_na_cena": indice_na_cena,
            "total_pessoas_cena": total_pessoas_cena,
        }
        await event_hub.publicar(evento_painel)
        logger.info(
            "[%s] Pessoa enviada ao painel: pessoa=%s status=%s indice=%s/%s paineis=%s",
            pacote_id,
            decisao.pessoa_id,
            decisao.status,
            indice_na_cena,
            total_pessoas_cena,
            event_hub.total_conexoes,
        )

        if not settings.kafka_enabled:
            logger.info(
                "[%s] Payload pronto; Kafka desativado: pessoa=%s",
                pacote_id,
                decisao.pessoa_id,
            )
            return imagem_background_base64

        resultado = await self.publisher.publicar(
            topico=self.topic,
            evento_id=decisao.evento_id,
            dados=mensagem.model_dump(
                mode="json",
                by_alias=True,
                exclude_none=True,
            ),
        )
        logger.info(
            "[%s] Pessoa publicada em %s p=%s offset=%s pessoa=%s indice=%s/%s",
            pacote_id,
            resultado["topico"],
            resultado["particao"],
            resultado["offset"],
            decisao.pessoa_id,
            indice_na_cena,
            total_pessoas_cena,
        )
        return imagem_background_base64

    async def _worker(self, numero: int) -> None:
        logger.info("Worker %s iniciado", numero)

        while True:
            pacote, body = await self.queue.get()

            try:
                adapter = camera_adapter_factory.encontrar_adapter(
                    content_type=pacote.content_type,
                    body=body,
                )
                logger.info(
                    "[%s] Worker %s usando %s",
                    pacote.evento_id,
                    numero,
                    adapter.__class__.__name__,
                )

                evento = await asyncio.to_thread(adapter.normalizar, pacote, body)

                logger.info(
                    "[%s] OBJETO NORMALIZADO (%s):\n%s",
                    pacote.evento_id,
                    evento.fabricante,
                    evento.model_dump_json(indent=2, by_alias=True, exclude_none=True),
                )

                if evento.imagem is None:
                    logger.info(
                        "[%s] Evento ignorado: não possui imagem",
                        pacote.evento_id,
                    )
                    continue

                bounding_boxes = self._selecionar_boxes_pessoas(evento)
                if not bounding_boxes:
                    logger.info(
                        "[%s] Evento ignorado: não possui boxes válidas de pessoas",
                        pacote.evento_id,
                    )
                    continue

                camera = (
                    evento.nome_camera
                    or evento.camera_id
                    or evento.ip_camera
                    or "Camera desconhecida"
                )
                caixas_rastreamento = [
                    DetectionBox(
                        x=caixa.x,
                        y=caixa.y,
                        largura=caixa.largura,
                        altura=caixa.altura,
                    )
                    for caixa in bounding_boxes
                ]
                decisoes = await person_tracker.registrar_lote(
                    camera=camera,
                    evento_id=evento.evento_id,
                    bboxes=caixas_rastreamento,
                )

                if len(decisoes) != len(bounding_boxes):
                    raise RuntimeError(
                        "Quantidade de decisões diferente da quantidade de boxes"
                    )

                total_pessoas = len(bounding_boxes)
                logger.info(
                    "[%s] Rastreamento em lote: pessoas=%s ids=%s",
                    pacote.evento_id,
                    total_pessoas,
                    [
                        (decisao.pessoa_id, decisao.status)
                        for decisao in decisoes
                    ],
                )

                contexto_cena = await self._analisar_contexto_cena(
                    evento=evento,
                    bounding_boxes=bounding_boxes,
                    pacote_id=pacote.evento_id,
                )
                cena_renderizada = await self._renderizar_cena(
                    evento=evento,
                    bounding_boxes=bounding_boxes,
                    pacote_id=pacote.evento_id,
                )

                imagem_background_base64: str | None = None
                for indice, (bounding_box, decisao) in enumerate(
                    zip(bounding_boxes, decisoes, strict=True),
                    start=1,
                ):
                    imagem_background_base64 = await self._processar_pessoa(
                        evento=evento,
                        camera=camera,
                        bounding_box=bounding_box,
                        decisao=decisao,
                        contexto_cena=contexto_cena,
                        cena_renderizada=cena_renderizada,
                        indice_na_cena=indice,
                        total_pessoas_cena=total_pessoas,
                        pacote_id=pacote.evento_id,
                        imagem_background_base64=imagem_background_base64,
                    )

            except asyncio.CancelledError:
                raise
            except Exception:
                logger.exception("[%s] Erro no processamento", pacote.evento_id)
            finally:
                self.queue.task_done()

------------------------------------------------------------
ARQUIVO: app/services/person_movement.py
------------------------------------------------------------
from __future__ import annotations

import asyncio
import math
import time
from dataclasses import asdict, dataclass
from typing import Literal

from app.services.person_tracker import DetectionBox


MovimentoHorizontal = Literal[
    "inicial",
    "parado",
    "esquerda",
    "direita",
]

MovimentoVertical = Literal[
    "inicial",
    "parado",
    "cima",
    "baixo",
]

TendenciaDistancia = Literal[
    "inicial",
    "estavel",
    "aproximando",
    "afastando",
]


@dataclass(frozen=True)
class PersonMovementAnalysis:
    pessoa_id: str

    movimento_horizontal: MovimentoHorizontal
    movimento_vertical: MovimentoVertical
    tendencia_distancia: TendenciaDistancia

    deslocamento_x: float
    deslocamento_y: float
    distancia_pixels: float

    velocidade_pixels_segundo: float
    variacao_area_percentual: float

    distancia_total_pixels: float
    tempo_observado_segundos: float
    quantidade_amostras: int

    descricao: str

    def to_dict(self) -> dict:
        return asdict(self)


@dataclass
class _MovementState:
    pessoa_id: str

    primeira_amostra_em: float
    ultima_amostra_em: float

    ultima_bbox: DetectionBox

    quantidade_amostras: int
    distancia_total_pixels: float

    ultima_analise: PersonMovementAnalysis


class PersonMovementMemory:
    def __init__(
        self,
        limite_movimento_percentual: float = 0.05,
        limite_movimento_minimo_pixels: float = 6.0,
        limite_variacao_area_percentual: float = 12.0,
    ) -> None:
        if limite_movimento_percentual < 0:
            raise ValueError(
                "limite_movimento_percentual "
                "não pode ser negativo"
            )

        if limite_movimento_minimo_pixels < 0:
            raise ValueError(
                "limite_movimento_minimo_pixels "
                "não pode ser negativo"
            )

        if limite_variacao_area_percentual < 0:
            raise ValueError(
                "limite_variacao_area_percentual "
                "não pode ser negativo"
            )

        self.limite_movimento_percentual = (
            limite_movimento_percentual
        )

        self.limite_movimento_minimo_pixels = (
            limite_movimento_minimo_pixels
        )

        self.limite_variacao_area_percentual = (
            limite_variacao_area_percentual
        )

        self._estados: dict[
            str,
            _MovementState,
        ] = {}

        self._lock = asyncio.Lock()

    async def registrar(
        self,
        pessoa_id: str,
        bbox: DetectionBox,
        agora: float | None = None,
    ) -> PersonMovementAnalysis:
        if not pessoa_id.strip():
            raise ValueError(
                "pessoa_id não pode ser vazio"
            )

        if (
            bbox.largura <= 0
            or bbox.altura <= 0
        ):
            raise ValueError(
                "A bounding box deve possuir "
                "largura e altura positivas"
            )

        momento = (
            time.monotonic()
            if agora is None
            else agora
        )

        async with self._lock:
            estado = self._estados.get(
                pessoa_id
            )

            if estado is None:
                analise = self._criar_primeira_analise(
                    pessoa_id=pessoa_id,
                )

                self._estados[pessoa_id] = (
                    _MovementState(
                        pessoa_id=pessoa_id,
                        primeira_amostra_em=momento,
                        ultima_amostra_em=momento,
                        ultima_bbox=bbox,
                        quantidade_amostras=1,
                        distancia_total_pixels=0.0,
                        ultima_analise=analise,
                    )
                )

                return analise

            analise = self._calcular_movimento(
                estado=estado,
                bbox=bbox,
                agora=momento,
            )

            estado.ultima_amostra_em = momento
            estado.ultima_bbox = bbox
            estado.quantidade_amostras += 1

            estado.distancia_total_pixels = (
                analise.distancia_total_pixels
            )

            estado.ultima_analise = analise

            return analise

    async def obter(
        self,
        pessoa_id: str,
    ) -> PersonMovementAnalysis | None:
        async with self._lock:
            estado = self._estados.get(
                pessoa_id
            )

            if estado is None:
                return None

            return estado.ultima_analise

    async def finalizar(
        self,
        pessoa_id: str,
    ) -> PersonMovementAnalysis | None:
        async with self._lock:
            estado = self._estados.pop(
                pessoa_id,
                None,
            )

            if estado is None:
                return None

            return estado.ultima_analise

    async def limpar(self) -> None:
        async with self._lock:
            self._estados.clear()

    @property
    def quantidade_pessoas(self) -> int:
        return len(self._estados)

    def _criar_primeira_analise(
        self,
        pessoa_id: str,
    ) -> PersonMovementAnalysis:
        return PersonMovementAnalysis(
            pessoa_id=pessoa_id,
            movimento_horizontal="inicial",
            movimento_vertical="inicial",
            tendencia_distancia="inicial",
            deslocamento_x=0.0,
            deslocamento_y=0.0,
            distancia_pixels=0.0,
            velocidade_pixels_segundo=0.0,
            variacao_area_percentual=0.0,
            distancia_total_pixels=0.0,
            tempo_observado_segundos=0.0,
            quantidade_amostras=1,
            descricao=(
                "Primeira posição da pessoa "
                "registrada."
            ),
        )

    def _calcular_movimento(
        self,
        estado: _MovementState,
        bbox: DetectionBox,
        agora: float,
    ) -> PersonMovementAnalysis:
        bbox_anterior = estado.ultima_bbox

        deslocamento_x = (
            bbox.centro_x
            - bbox_anterior.centro_x
        )

        deslocamento_y = (
            bbox.centro_y
            - bbox_anterior.centro_y
        )

        distancia_pixels = math.hypot(
            deslocamento_x,
            deslocamento_y,
        )

        intervalo_segundos = max(
            0.001,
            agora
            - estado.ultima_amostra_em,
        )

        velocidade = (
            distancia_pixels
            / intervalo_segundos
        )

        limite_horizontal = max(
            self.limite_movimento_minimo_pixels,
            max(
                bbox.largura,
                bbox_anterior.largura,
            )
            * self.limite_movimento_percentual,
        )

        limite_vertical = max(
            self.limite_movimento_minimo_pixels,
            max(
                bbox.altura,
                bbox_anterior.altura,
            )
            * self.limite_movimento_percentual,
        )

        movimento_horizontal = (
            self._classificar_movimento_horizontal(
                deslocamento_x=deslocamento_x,
                limite=limite_horizontal,
            )
        )

        movimento_vertical = (
            self._classificar_movimento_vertical(
                deslocamento_y=deslocamento_y,
                limite=limite_vertical,
            )
        )

        area_anterior = max(
            1,
            bbox_anterior.area,
        )

        variacao_area_percentual = (
            (
                bbox.area
                - area_anterior
            )
            / area_anterior
            * 100
        )

        tendencia_distancia = (
            self._classificar_tendencia_distancia(
                variacao_area_percentual
            )
        )

        quantidade_amostras = (
            estado.quantidade_amostras
            + 1
        )

        distancia_total = (
            estado.distancia_total_pixels
            + distancia_pixels
        )

        tempo_observado = max(
            0.0,
            agora
            - estado.primeira_amostra_em,
        )

        descricao = self._montar_descricao(
            movimento_horizontal=(
                movimento_horizontal
            ),
            movimento_vertical=(
                movimento_vertical
            ),
            tendencia_distancia=(
                tendencia_distancia
            ),
            velocidade=velocidade,
        )

        return PersonMovementAnalysis(
            pessoa_id=estado.pessoa_id,
            movimento_horizontal=(
                movimento_horizontal
            ),
            movimento_vertical=(
                movimento_vertical
            ),
            tendencia_distancia=(
                tendencia_distancia
            ),
            deslocamento_x=round(
                deslocamento_x,
                2,
            ),
            deslocamento_y=round(
                deslocamento_y,
                2,
            ),
            distancia_pixels=round(
                distancia_pixels,
                2,
            ),
            velocidade_pixels_segundo=round(
                velocidade,
                2,
            ),
            variacao_area_percentual=round(
                variacao_area_percentual,
                2,
            ),
            distancia_total_pixels=round(
                distancia_total,
                2,
            ),
            tempo_observado_segundos=round(
                tempo_observado,
                2,
            ),
            quantidade_amostras=(
                quantidade_amostras
            ),
            descricao=descricao,
        )

    def _classificar_movimento_horizontal(
        self,
        deslocamento_x: float,
        limite: float,
    ) -> MovimentoHorizontal:
        if abs(deslocamento_x) < limite:
            return "parado"

        if deslocamento_x > 0:
            return "direita"

        return "esquerda"

    def _classificar_movimento_vertical(
        self,
        deslocamento_y: float,
        limite: float,
    ) -> MovimentoVertical:
        if abs(deslocamento_y) < limite:
            return "parado"

        if deslocamento_y > 0:
            return "baixo"

        return "cima"

    def _classificar_tendencia_distancia(
        self,
        variacao_area_percentual: float,
    ) -> TendenciaDistancia:
        limite = (
            self
            .limite_variacao_area_percentual
        )

        if variacao_area_percentual >= limite:
            return "aproximando"

        if variacao_area_percentual <= -limite:
            return "afastando"

        return "estavel"

    def _montar_descricao(
        self,
        movimento_horizontal: MovimentoHorizontal,
        movimento_vertical: MovimentoVertical,
        tendencia_distancia: TendenciaDistancia,
        velocidade: float,
    ) -> str:
        movimentos: list[str] = []

        if movimento_horizontal == "direita":
            movimentos.append(
                "para a direita"
            )

        elif movimento_horizontal == "esquerda":
            movimentos.append(
                "para a esquerda"
            )

        if movimento_vertical == "cima":
            movimentos.append(
                "para cima"
            )

        elif movimento_vertical == "baixo":
            movimentos.append(
                "para baixo"
            )

        if not movimentos:
            descricao_movimento = (
                "Pessoa praticamente parada"
            )

        else:
            descricao_movimento = (
                "Pessoa se movimentando "
                + " e ".join(movimentos)
            )

        tendencias = {
            "inicial": "",
            "estavel": (
                " mantendo distância "
                "aparente estável"
            ),
            "aproximando": (
                " e aparentemente se aproximando "
                "da câmera"
            ),
            "afastando": (
                " e aparentemente se afastando "
                "da câmera"
            ),
        }

        return (
            f"{descricao_movimento}"
            f"{tendencias[tendencia_distancia]}, "
            f"com velocidade aproximada de "
            f"{velocidade:.2f} pixels por segundo."
        )


person_movement_memory = (
    PersonMovementMemory()
)
------------------------------------------------------------
ARQUIVO: app/services/person_tracker_backup_antes_15s.py
------------------------------------------------------------
import asyncio
import math
import time
import uuid
from dataclasses import dataclass
from typing import Literal


TrackingStatus = Literal[
    "entered",
    "updated",
    "suppressed",
    "exited",
]


@dataclass(frozen=True)
class DetectionBox:
    x: int
    y: int
    largura: int
    altura: int

    @property
    def x2(self) -> int:
        return self.x + self.largura

    @property
    def y2(self) -> int:
        return self.y + self.altura

    @property
    def centro_x(self) -> float:
        return self.x + self.largura / 2

    @property
    def centro_y(self) -> float:
        return self.y + self.altura / 2

    @property
    def area(self) -> int:
        return max(0, self.largura) * max(
            0,
            self.altura,
        )

    def iou(self, outra: "DetectionBox") -> float:
        """
        Calcula a sobreposição entre duas caixas.

        0.0 significa nenhuma sobreposição.
        1.0 significa caixas iguais.
        """

        intersecao_x1 = max(self.x, outra.x)
        intersecao_y1 = max(self.y, outra.y)
        intersecao_x2 = min(self.x2, outra.x2)
        intersecao_y2 = min(self.y2, outra.y2)

        largura_intersecao = max(
            0,
            intersecao_x2 - intersecao_x1,
        )

        altura_intersecao = max(
            0,
            intersecao_y2 - intersecao_y1,
        )

        area_intersecao = (
            largura_intersecao * altura_intersecao
        )

        area_uniao = (
            self.area
            + outra.area
            - area_intersecao
        )

        if area_uniao <= 0:
            return 0.0

        return area_intersecao / area_uniao

    def distancia_centros(
        self,
        outra: "DetectionBox",
    ) -> float:
        diferenca_x = self.centro_x - outra.centro_x
        diferenca_y = self.centro_y - outra.centro_y

        return math.hypot(
            diferenca_x,
            diferenca_y,
        )


@dataclass
class TrackedPerson:
    pessoa_id: str
    camera: str
    primeira_deteccao: float
    ultima_deteccao: float
    ultimo_processamento: float
    bbox: DetectionBox
    quantidade_deteccoes: int
    ultimo_evento_id: str


@dataclass(frozen=True)
class TrackingDecision:
    pessoa_id: str
    camera: str
    evento_id: str
    status: TrackingStatus
    deve_processar: bool
    quantidade_deteccoes: int
    bbox: DetectionBox


class PersonTracker:
    def __init__(
        self,
        intervalo_reprocessamento: float = 5.0,
        tempo_para_saida: float = 8.0,
        limite_iou: float = 0.25,
        limite_distancia: float = 0.75,
    ) -> None:
        if intervalo_reprocessamento <= 0:
            raise ValueError(
                "intervalo_reprocessamento deve ser positivo"
            )

        if tempo_para_saida <= 0:
            raise ValueError(
                "tempo_para_saida deve ser positivo"
            )

        self.intervalo_reprocessamento = (
            intervalo_reprocessamento
        )

        self.tempo_para_saida = tempo_para_saida
        self.limite_iou = limite_iou
        self.limite_distancia = limite_distancia

        self._pessoas: dict[str, TrackedPerson] = {}
        self._lock = asyncio.Lock()

    @property
    def quantidade_ativas(self) -> int:
        return len(self._pessoas)

    async def registrar(
        self,
        camera: str,
        evento_id: str,
        bbox: DetectionBox,
        agora: float | None = None,
    ) -> TrackingDecision:
        momento = (
            time.monotonic()
            if agora is None
            else agora
        )

        async with self._lock:
            pessoa = self._encontrar_pessoa(
                camera=camera,
                bbox=bbox,
            )

            if pessoa is None:
                pessoa = self._criar_pessoa(
                    camera=camera,
                    evento_id=evento_id,
                    bbox=bbox,
                    agora=momento,
                )

                return TrackingDecision(
                    pessoa_id=pessoa.pessoa_id,
                    camera=camera,
                    evento_id=evento_id,
                    status="entered",
                    deve_processar=True,
                    quantidade_deteccoes=1,
                    bbox=bbox,
                )

            pessoa.ultima_deteccao = momento
            pessoa.bbox = bbox
            pessoa.ultimo_evento_id = evento_id
            pessoa.quantidade_deteccoes += 1

            tempo_desde_processamento = (
                momento
                - pessoa.ultimo_processamento
            )

            if (
                tempo_desde_processamento
                >= self.intervalo_reprocessamento
            ):
                pessoa.ultimo_processamento = momento

                return TrackingDecision(
                    pessoa_id=pessoa.pessoa_id,
                    camera=camera,
                    evento_id=evento_id,
                    status="updated",
                    deve_processar=True,
                    quantidade_deteccoes=(
                        pessoa.quantidade_deteccoes
                    ),
                    bbox=bbox,
                )

            return TrackingDecision(
                pessoa_id=pessoa.pessoa_id,
                camera=camera,
                evento_id=evento_id,
                status="suppressed",
                deve_processar=False,
                quantidade_deteccoes=(
                    pessoa.quantidade_deteccoes
                ),
                bbox=bbox,
            )

    async def coletar_saidas(
        self,
        agora: float | None = None,
    ) -> list[TrackingDecision]:
        momento = (
            time.monotonic()
            if agora is None
            else agora
        )

        async with self._lock:
            pessoas_encerradas: list[
                TrackingDecision
            ] = []

            ids_para_remover: list[str] = []

            for pessoa_id, pessoa in self._pessoas.items():
                tempo_sem_deteccao = (
                    momento
                    - pessoa.ultima_deteccao
                )

                if (
                    tempo_sem_deteccao
                    < self.tempo_para_saida
                ):
                    continue

                pessoas_encerradas.append(
                    TrackingDecision(
                        pessoa_id=pessoa.pessoa_id,
                        camera=pessoa.camera,
                        evento_id=(
                            pessoa.ultimo_evento_id
                        ),
                        status="exited",
                        deve_processar=False,
                        quantidade_deteccoes=(
                            pessoa.quantidade_deteccoes
                        ),
                        bbox=pessoa.bbox,
                    )
                )

                ids_para_remover.append(pessoa_id)

            for pessoa_id in ids_para_remover:
                self._pessoas.pop(
                    pessoa_id,
                    None,
                )

            return pessoas_encerradas

    def _encontrar_pessoa(
        self,
        camera: str,
        bbox: DetectionBox,
    ) -> TrackedPerson | None:
        melhor_pessoa: TrackedPerson | None = None
        melhor_pontuacao = -1.0

        for pessoa in self._pessoas.values():
            if pessoa.camera != camera:
                continue

            sobreposicao = bbox.iou(pessoa.bbox)

            distancia = bbox.distancia_centros(
                pessoa.bbox
            )

            maior_dimensao = max(
                bbox.largura,
                bbox.altura,
                pessoa.bbox.largura,
                pessoa.bbox.altura,
                1,
            )

            distancia_normalizada = (
                distancia / maior_dimensao
            )

            caixas_proximas = (
                distancia_normalizada
                <= self.limite_distancia
            )

            caixas_sobrepostas = (
                sobreposicao >= self.limite_iou
            )

            if not (
                caixas_proximas
                or caixas_sobrepostas
            ):
                continue

            pontuacao_distancia = max(
                0.0,
                1.0 - distancia_normalizada,
            )

            pontuacao = (
                sobreposicao
                + pontuacao_distancia
            )

            if pontuacao > melhor_pontuacao:
                melhor_pontuacao = pontuacao
                melhor_pessoa = pessoa

        return melhor_pessoa

    def _criar_pessoa(
        self,
        camera: str,
        evento_id: str,
        bbox: DetectionBox,
        agora: float,
    ) -> TrackedPerson:
        camera_normalizada = "".join(
            caractere.lower()
            if caractere.isalnum()
            else "-"
            for caractere in camera
        ).strip("-")

        if not camera_normalizada:
            camera_normalizada = "camera"

        pessoa_id = (
            f"{camera_normalizada}-"
            f"{uuid.uuid4().hex[:8]}"
        )

        pessoa = TrackedPerson(
            pessoa_id=pessoa_id,
            camera=camera,
            primeira_deteccao=agora,
            ultima_deteccao=agora,
            ultimo_processamento=agora,
            bbox=bbox,
            quantidade_deteccoes=1,
            ultimo_evento_id=evento_id,
        )

        self._pessoas[pessoa_id] = pessoa

        return pessoa


person_tracker = PersonTracker(
    intervalo_reprocessamento=5.0,
    tempo_para_saida=8.0,
)
------------------------------------------------------------
ARQUIVO: app/services/person_tracker_backup_antes_continuidade.py
------------------------------------------------------------
import asyncio
import math
import time
import uuid
from dataclasses import dataclass
from typing import Literal


TrackingStatus = Literal[
    "entered",
    "updated",
    "suppressed",
    "exited",
]


@dataclass(frozen=True)
class DetectionBox:
    x: int
    y: int
    largura: int
    altura: int

    @property
    def x2(self) -> int:
        return self.x + self.largura

    @property
    def y2(self) -> int:
        return self.y + self.altura

    @property
    def centro_x(self) -> float:
        return self.x + self.largura / 2

    @property
    def centro_y(self) -> float:
        return self.y + self.altura / 2

    @property
    def area(self) -> int:
        return (
            max(0, self.largura)
            * max(0, self.altura)
        )

    def iou(
        self,
        outra: "DetectionBox",
    ) -> float:
        """
        Calcula a sobreposição entre duas caixas.

        0.0 significa nenhuma sobreposição.
        1.0 significa caixas iguais.
        """

        intersecao_x1 = max(
            self.x,
            outra.x,
        )

        intersecao_y1 = max(
            self.y,
            outra.y,
        )

        intersecao_x2 = min(
            self.x2,
            outra.x2,
        )

        intersecao_y2 = min(
            self.y2,
            outra.y2,
        )

        largura_intersecao = max(
            0,
            intersecao_x2 - intersecao_x1,
        )

        altura_intersecao = max(
            0,
            intersecao_y2 - intersecao_y1,
        )

        area_intersecao = (
            largura_intersecao
            * altura_intersecao
        )

        area_uniao = (
            self.area
            + outra.area
            - area_intersecao
        )

        if area_uniao <= 0:
            return 0.0

        return (
            area_intersecao
            / area_uniao
        )

    def distancia_centros(
        self,
        outra: "DetectionBox",
    ) -> float:
        diferenca_x = (
            self.centro_x
            - outra.centro_x
        )

        diferenca_y = (
            self.centro_y
            - outra.centro_y
        )

        return math.hypot(
            diferenca_x,
            diferenca_y,
        )


@dataclass
class TrackedPerson:
    pessoa_id: str
    camera: str

    primeira_deteccao: float
    ultima_deteccao: float
    ultimo_processamento: float

    bbox: DetectionBox
    quantidade_deteccoes: int
    ultimo_evento_id: str


@dataclass(frozen=True)
class TrackingDecision:
    pessoa_id: str
    camera: str
    evento_id: str

    status: TrackingStatus
    deve_processar: bool

    quantidade_deteccoes: int
    bbox: DetectionBox


class PersonTracker:
    def __init__(
        self,
        intervalo_reprocessamento: float = 5.0,
        tempo_para_saida: float = 15.0,
        limite_iou: float = 0.25,
        limite_distancia: float = 0.75,
    ) -> None:
        if intervalo_reprocessamento <= 0:
            raise ValueError(
                "intervalo_reprocessamento "
                "deve ser positivo"
            )

        if tempo_para_saida <= 0:
            raise ValueError(
                "tempo_para_saida deve ser positivo"
            )

        if limite_iou < 0:
            raise ValueError(
                "limite_iou não pode ser negativo"
            )

        if limite_distancia < 0:
            raise ValueError(
                "limite_distancia não pode "
                "ser negativo"
            )

        self.intervalo_reprocessamento = (
            intervalo_reprocessamento
        )

        self.tempo_para_saida = (
            tempo_para_saida
        )

        self.limite_iou = limite_iou
        self.limite_distancia = limite_distancia

        self._pessoas: dict[
            str,
            TrackedPerson,
        ] = {}

        self._lock = asyncio.Lock()

    @property
    def quantidade_ativas(self) -> int:
        return len(self._pessoas)

    async def registrar(
        self,
        camera: str,
        evento_id: str,
        bbox: DetectionBox,
        agora: float | None = None,
    ) -> TrackingDecision:
        """
        Mantém compatibilidade com o pipeline antigo,
        que registra somente uma pessoa por chamada.
        """

        decisoes = await self.registrar_lote(
            camera=camera,
            evento_id=evento_id,
            bboxes=[bbox],
            agora=agora,
        )

        return decisoes[0]

    async def registrar_lote(
        self,
        camera: str,
        evento_id: str,
        bboxes: list[DetectionBox],
        agora: float | None = None,
    ) -> list[TrackingDecision]:
        """
        Registra todas as detecções de um mesmo quadro.

        Cada pessoa ativa pode ser associada a apenas
        uma bounding box do quadro atual.

        Cada bounding box também pode ser associada
        a apenas uma pessoa ativa.
        """

        if not bboxes:
            return []

        for bbox in bboxes:
            if (
                bbox.largura <= 0
                or bbox.altura <= 0
            ):
                raise ValueError(
                    "Todas as bounding boxes devem "
                    "possuir largura e altura positivas"
                )

        momento = (
            time.monotonic()
            if agora is None
            else agora
        )

        async with self._lock:
            associacoes = (
                self._associar_deteccoes_em_lote(
                    camera=camera,
                    bboxes=bboxes,
                )
            )

            total_deteccoes = len(bboxes)

            decisoes: list[
                TrackingDecision
            ] = []

            for indice, bbox in enumerate(
                bboxes
            ):
                evento_individual_id = (
                    self._criar_evento_individual_id(
                        evento_id=evento_id,
                        indice=indice,
                        total=total_deteccoes,
                    )
                )

                pessoa = associacoes.get(
                    indice
                )

                if pessoa is None:
                    pessoa = self._criar_pessoa(
                        camera=camera,
                        evento_id=(
                            evento_individual_id
                        ),
                        bbox=bbox,
                        agora=momento,
                    )

                    decisoes.append(
                        TrackingDecision(
                            pessoa_id=(
                                pessoa.pessoa_id
                            ),
                            camera=camera,
                            evento_id=(
                                evento_individual_id
                            ),
                            status="entered",
                            deve_processar=True,
                            quantidade_deteccoes=1,
                            bbox=bbox,
                        )
                    )

                    continue

                decisao = (
                    self._atualizar_pessoa(
                        pessoa=pessoa,
                        evento_id=(
                            evento_individual_id
                        ),
                        bbox=bbox,
                        agora=momento,
                    )
                )

                decisoes.append(
                    decisao
                )

            return decisoes

    async def coletar_saidas(
        self,
        agora: float | None = None,
    ) -> list[TrackingDecision]:
        momento = (
            time.monotonic()
            if agora is None
            else agora
        )

        async with self._lock:
            pessoas_encerradas: list[
                TrackingDecision
            ] = []

            ids_para_remover: list[str] = []

            for (
                pessoa_id,
                pessoa,
            ) in self._pessoas.items():
                tempo_sem_deteccao = (
                    momento
                    - pessoa.ultima_deteccao
                )

                if (
                    tempo_sem_deteccao
                    < self.tempo_para_saida
                ):
                    continue

                pessoas_encerradas.append(
                    TrackingDecision(
                        pessoa_id=(
                            pessoa.pessoa_id
                        ),
                        camera=pessoa.camera,
                        evento_id=(
                            pessoa.ultimo_evento_id
                        ),
                        status="exited",
                        deve_processar=False,
                        quantidade_deteccoes=(
                            pessoa
                            .quantidade_deteccoes
                        ),
                        bbox=pessoa.bbox,
                    )
                )

                ids_para_remover.append(
                    pessoa_id
                )

            for pessoa_id in ids_para_remover:
                self._pessoas.pop(
                    pessoa_id,
                    None,
                )

            return pessoas_encerradas

    async def limpar(self) -> None:
        async with self._lock:
            self._pessoas.clear()

    def _associar_deteccoes_em_lote(
        self,
        camera: str,
        bboxes: list[DetectionBox],
    ) -> dict[int, TrackedPerson]:
        """
        Cria todas as combinações possíveis entre
        as caixas atuais e as pessoas ativas.

        Depois seleciona as melhores combinações,
        sem reutilizar pessoa ou bounding box.
        """

        candidatos: list[
            tuple[
                float,
                int,
                str,
            ]
        ] = []

        for indice, bbox in enumerate(
            bboxes
        ):
            for pessoa in self._pessoas.values():
                if pessoa.camera != camera:
                    continue

                pontuacao = (
                    self._calcular_pontuacao(
                        bbox=bbox,
                        pessoa=pessoa,
                    )
                )

                if pontuacao is None:
                    continue

                candidatos.append(
                    (
                        pontuacao,
                        indice,
                        pessoa.pessoa_id,
                    )
                )

        # Maior pontuação primeiro.
        #
        # O índice e o pessoa_id deixam o resultado
        # determinístico quando há empate.
        candidatos.sort(
            key=lambda item: (
                -item[0],
                item[1],
                item[2],
            )
        )

        deteccoes_utilizadas: set[int] = set()
        pessoas_utilizadas: set[str] = set()

        associacoes: dict[
            int,
            TrackedPerson,
        ] = {}

        for (
            _,
            indice,
            pessoa_id,
        ) in candidatos:
            if indice in deteccoes_utilizadas:
                continue

            if pessoa_id in pessoas_utilizadas:
                continue

            pessoa = self._pessoas.get(
                pessoa_id
            )

            if pessoa is None:
                continue

            associacoes[indice] = pessoa

            deteccoes_utilizadas.add(
                indice
            )

            pessoas_utilizadas.add(
                pessoa_id
            )

        return associacoes

    def _calcular_pontuacao(
        self,
        bbox: DetectionBox,
        pessoa: TrackedPerson,
    ) -> float | None:
        sobreposicao = bbox.iou(
            pessoa.bbox
        )

        distancia = bbox.distancia_centros(
            pessoa.bbox
        )

        maior_dimensao = max(
            bbox.largura,
            bbox.altura,
            pessoa.bbox.largura,
            pessoa.bbox.altura,
            1,
        )

        distancia_normalizada = (
            distancia
            / maior_dimensao
        )

        caixas_proximas = (
            distancia_normalizada
            <= self.limite_distancia
        )

        caixas_sobrepostas = (
            sobreposicao
            >= self.limite_iou
        )

        if not (
            caixas_proximas
            or caixas_sobrepostas
        ):
            return None

        pontuacao_distancia = max(
            0.0,
            1.0 - distancia_normalizada,
        )

        return (
            sobreposicao
            + pontuacao_distancia
        )

    def _atualizar_pessoa(
        self,
        pessoa: TrackedPerson,
        evento_id: str,
        bbox: DetectionBox,
        agora: float,
    ) -> TrackingDecision:
        pessoa.ultima_deteccao = agora
        pessoa.bbox = bbox
        pessoa.ultimo_evento_id = evento_id
        pessoa.quantidade_deteccoes += 1

        tempo_desde_processamento = (
            agora
            - pessoa.ultimo_processamento
        )

        if (
            tempo_desde_processamento
            >= self.intervalo_reprocessamento
        ):
            pessoa.ultimo_processamento = agora

            return TrackingDecision(
                pessoa_id=pessoa.pessoa_id,
                camera=pessoa.camera,
                evento_id=evento_id,
                status="updated",
                deve_processar=True,
                quantidade_deteccoes=(
                    pessoa.quantidade_deteccoes
                ),
                bbox=bbox,
            )

        return TrackingDecision(
            pessoa_id=pessoa.pessoa_id,
            camera=pessoa.camera,
            evento_id=evento_id,
            status="suppressed",
            deve_processar=False,
            quantidade_deteccoes=(
                pessoa.quantidade_deteccoes
            ),
            bbox=bbox,
        )

    def _encontrar_pessoa(
        self,
        camera: str,
        bbox: DetectionBox,
    ) -> TrackedPerson | None:
        """
        Mantido para compatibilidade e testes
        individuais.
        """

        melhor_pessoa: (
            TrackedPerson | None
        ) = None

        melhor_pontuacao = -1.0

        for pessoa in self._pessoas.values():
            if pessoa.camera != camera:
                continue

            pontuacao = (
                self._calcular_pontuacao(
                    bbox=bbox,
                    pessoa=pessoa,
                )
            )

            if pontuacao is None:
                continue

            if pontuacao > melhor_pontuacao:
                melhor_pontuacao = pontuacao
                melhor_pessoa = pessoa

        return melhor_pessoa

    def _criar_pessoa(
        self,
        camera: str,
        evento_id: str,
        bbox: DetectionBox,
        agora: float,
    ) -> TrackedPerson:
        camera_normalizada = "".join(
            caractere.lower()
            if caractere.isalnum()
            else "-"
            for caractere in camera
        ).strip("-")

        if not camera_normalizada:
            camera_normalizada = "camera"

        pessoa_id = (
            f"{camera_normalizada}-"
            f"{uuid.uuid4().hex[:8]}"
        )

        pessoa = TrackedPerson(
            pessoa_id=pessoa_id,
            camera=camera,
            primeira_deteccao=agora,
            ultima_deteccao=agora,
            ultimo_processamento=agora,
            bbox=bbox,
            quantidade_deteccoes=1,
            ultimo_evento_id=evento_id,
        )

        self._pessoas[
            pessoa_id
        ] = pessoa

        return pessoa

    def _criar_evento_individual_id(
        self,
        evento_id: str,
        indice: int,
        total: int,
    ) -> str:
        """
        Um quadro com uma única pessoa mantém o ID
        original.

        Quadros com várias pessoas recebem IDs como:

        evento123-01
        evento123-02
        evento123-03
        """

        if total <= 1:
            return evento_id

        return (
            f"{evento_id}-"
            f"{indice + 1:02d}"
        )


person_tracker = PersonTracker(
    intervalo_reprocessamento=5.0,
    tempo_para_saida=15.0,
)
------------------------------------------------------------
ARQUIVO: app/services/person_tracker_backup_antes_lote.py
------------------------------------------------------------
import asyncio
import math
import time
import uuid
from dataclasses import dataclass
from typing import Literal


TrackingStatus = Literal[
    "entered",
    "updated",
    "suppressed",
    "exited",
]


@dataclass(frozen=True)
class DetectionBox:
    x: int
    y: int
    largura: int
    altura: int

    @property
    def x2(self) -> int:
        return self.x + self.largura

    @property
    def y2(self) -> int:
        return self.y + self.altura

    @property
    def centro_x(self) -> float:
        return self.x + self.largura / 2

    @property
    def centro_y(self) -> float:
        return self.y + self.altura / 2

    @property
    def area(self) -> int:
        return max(0, self.largura) * max(
            0,
            self.altura,
        )

    def iou(self, outra: "DetectionBox") -> float:
        """
        Calcula a sobreposição entre duas caixas.

        0.0 significa nenhuma sobreposição.
        1.0 significa caixas iguais.
        """

        intersecao_x1 = max(self.x, outra.x)
        intersecao_y1 = max(self.y, outra.y)
        intersecao_x2 = min(self.x2, outra.x2)
        intersecao_y2 = min(self.y2, outra.y2)

        largura_intersecao = max(
            0,
            intersecao_x2 - intersecao_x1,
        )

        altura_intersecao = max(
            0,
            intersecao_y2 - intersecao_y1,
        )

        area_intersecao = (
            largura_intersecao * altura_intersecao
        )

        area_uniao = (
            self.area
            + outra.area
            - area_intersecao
        )

        if area_uniao <= 0:
            return 0.0

        return area_intersecao / area_uniao

    def distancia_centros(
        self,
        outra: "DetectionBox",
    ) -> float:
        diferenca_x = self.centro_x - outra.centro_x
        diferenca_y = self.centro_y - outra.centro_y

        return math.hypot(
            diferenca_x,
            diferenca_y,
        )


@dataclass
class TrackedPerson:
    pessoa_id: str
    camera: str
    primeira_deteccao: float
    ultima_deteccao: float
    ultimo_processamento: float
    bbox: DetectionBox
    quantidade_deteccoes: int
    ultimo_evento_id: str


@dataclass(frozen=True)
class TrackingDecision:
    pessoa_id: str
    camera: str
    evento_id: str
    status: TrackingStatus
    deve_processar: bool
    quantidade_deteccoes: int
    bbox: DetectionBox


class PersonTracker:
    def __init__(
        self,
        intervalo_reprocessamento: float = 5.0,
        tempo_para_saida: float = 8.0,
        limite_iou: float = 0.25,
        limite_distancia: float = 0.75,
    ) -> None:
        if intervalo_reprocessamento <= 0:
            raise ValueError(
                "intervalo_reprocessamento deve ser positivo"
            )

        if tempo_para_saida <= 0:
            raise ValueError(
                "tempo_para_saida deve ser positivo"
            )

        self.intervalo_reprocessamento = (
            intervalo_reprocessamento
        )

        self.tempo_para_saida = tempo_para_saida
        self.limite_iou = limite_iou
        self.limite_distancia = limite_distancia

        self._pessoas: dict[str, TrackedPerson] = {}
        self._lock = asyncio.Lock()

    @property
    def quantidade_ativas(self) -> int:
        return len(self._pessoas)

    async def registrar(
        self,
        camera: str,
        evento_id: str,
        bbox: DetectionBox,
        agora: float | None = None,
    ) -> TrackingDecision:
        momento = (
            time.monotonic()
            if agora is None
            else agora
        )

        async with self._lock:
            pessoa = self._encontrar_pessoa(
                camera=camera,
                bbox=bbox,
            )

            if pessoa is None:
                pessoa = self._criar_pessoa(
                    camera=camera,
                    evento_id=evento_id,
                    bbox=bbox,
                    agora=momento,
                )

                return TrackingDecision(
                    pessoa_id=pessoa.pessoa_id,
                    camera=camera,
                    evento_id=evento_id,
                    status="entered",
                    deve_processar=True,
                    quantidade_deteccoes=1,
                    bbox=bbox,
                )

            pessoa.ultima_deteccao = momento
            pessoa.bbox = bbox
            pessoa.ultimo_evento_id = evento_id
            pessoa.quantidade_deteccoes += 1

            tempo_desde_processamento = (
                momento
                - pessoa.ultimo_processamento
            )

            if (
                tempo_desde_processamento
                >= self.intervalo_reprocessamento
            ):
                pessoa.ultimo_processamento = momento

                return TrackingDecision(
                    pessoa_id=pessoa.pessoa_id,
                    camera=camera,
                    evento_id=evento_id,
                    status="updated",
                    deve_processar=True,
                    quantidade_deteccoes=(
                        pessoa.quantidade_deteccoes
                    ),
                    bbox=bbox,
                )

            return TrackingDecision(
                pessoa_id=pessoa.pessoa_id,
                camera=camera,
                evento_id=evento_id,
                status="suppressed",
                deve_processar=False,
                quantidade_deteccoes=(
                    pessoa.quantidade_deteccoes
                ),
                bbox=bbox,
            )

    async def coletar_saidas(
        self,
        agora: float | None = None,
    ) -> list[TrackingDecision]:
        momento = (
            time.monotonic()
            if agora is None
            else agora
        )

        async with self._lock:
            pessoas_encerradas: list[
                TrackingDecision
            ] = []

            ids_para_remover: list[str] = []

            for pessoa_id, pessoa in self._pessoas.items():
                tempo_sem_deteccao = (
                    momento
                    - pessoa.ultima_deteccao
                )

                if (
                    tempo_sem_deteccao
                    < self.tempo_para_saida
                ):
                    continue

                pessoas_encerradas.append(
                    TrackingDecision(
                        pessoa_id=pessoa.pessoa_id,
                        camera=pessoa.camera,
                        evento_id=(
                            pessoa.ultimo_evento_id
                        ),
                        status="exited",
                        deve_processar=False,
                        quantidade_deteccoes=(
                            pessoa.quantidade_deteccoes
                        ),
                        bbox=pessoa.bbox,
                    )
                )

                ids_para_remover.append(pessoa_id)

            for pessoa_id in ids_para_remover:
                self._pessoas.pop(
                    pessoa_id,
                    None,
                )

            return pessoas_encerradas

    def _encontrar_pessoa(
        self,
        camera: str,
        bbox: DetectionBox,
    ) -> TrackedPerson | None:
        melhor_pessoa: TrackedPerson | None = None
        melhor_pontuacao = -1.0

        for pessoa in self._pessoas.values():
            if pessoa.camera != camera:
                continue

            sobreposicao = bbox.iou(pessoa.bbox)

            distancia = bbox.distancia_centros(
                pessoa.bbox
            )

            maior_dimensao = max(
                bbox.largura,
                bbox.altura,
                pessoa.bbox.largura,
                pessoa.bbox.altura,
                1,
            )

            distancia_normalizada = (
                distancia / maior_dimensao
            )

            caixas_proximas = (
                distancia_normalizada
                <= self.limite_distancia
            )

            caixas_sobrepostas = (
                sobreposicao >= self.limite_iou
            )

            if not (
                caixas_proximas
                or caixas_sobrepostas
            ):
                continue

            pontuacao_distancia = max(
                0.0,
                1.0 - distancia_normalizada,
            )

            pontuacao = (
                sobreposicao
                + pontuacao_distancia
            )

            if pontuacao > melhor_pontuacao:
                melhor_pontuacao = pontuacao
                melhor_pessoa = pessoa

        return melhor_pessoa

    def _criar_pessoa(
        self,
        camera: str,
        evento_id: str,
        bbox: DetectionBox,
        agora: float,
    ) -> TrackedPerson:
        camera_normalizada = "".join(
            caractere.lower()
            if caractere.isalnum()
            else "-"
            for caractere in camera
        ).strip("-")

        if not camera_normalizada:
            camera_normalizada = "camera"

        pessoa_id = (
            f"{camera_normalizada}-"
            f"{uuid.uuid4().hex[:8]}"
        )

        pessoa = TrackedPerson(
            pessoa_id=pessoa_id,
            camera=camera,
            primeira_deteccao=agora,
            ultima_deteccao=agora,
            ultimo_processamento=agora,
            bbox=bbox,
            quantidade_deteccoes=1,
            ultimo_evento_id=evento_id,
        )

        self._pessoas[pessoa_id] = pessoa

        return pessoa


person_tracker = PersonTracker(
    intervalo_reprocessamento=5.0,
    tempo_para_saida=15.0,
)
------------------------------------------------------------
ARQUIVO: app/services/person_tracker.py
------------------------------------------------------------
import asyncio
import math
import time
import uuid
from dataclasses import dataclass
from typing import Literal


TrackingStatus = Literal[
    "entered",
    "updated",
    "suppressed",
    "exited",
]


@dataclass(frozen=True)
class DetectionBox:
    x: int
    y: int
    largura: int
    altura: int

    @property
    def x2(self) -> int:
        return self.x + self.largura

    @property
    def y2(self) -> int:
        return self.y + self.altura

    @property
    def centro_x(self) -> float:
        return self.x + self.largura / 2

    @property
    def centro_y(self) -> float:
        return self.y + self.altura / 2

    @property
    def area(self) -> int:
        return (
            max(0, self.largura)
            * max(0, self.altura)
        )

    def iou(
        self,
        outra: "DetectionBox",
    ) -> float:
        """
        Calcula a sobreposição entre duas caixas.

        0.0 significa nenhuma sobreposição.
        1.0 significa caixas iguais.
        """

        intersecao_x1 = max(
            self.x,
            outra.x,
        )

        intersecao_y1 = max(
            self.y,
            outra.y,
        )

        intersecao_x2 = min(
            self.x2,
            outra.x2,
        )

        intersecao_y2 = min(
            self.y2,
            outra.y2,
        )

        largura_intersecao = max(
            0,
            intersecao_x2 - intersecao_x1,
        )

        altura_intersecao = max(
            0,
            intersecao_y2 - intersecao_y1,
        )

        area_intersecao = (
            largura_intersecao
            * altura_intersecao
        )

        area_uniao = (
            self.area
            + outra.area
            - area_intersecao
        )

        if area_uniao <= 0:
            return 0.0

        return area_intersecao / area_uniao

    def distancia_centros(
        self,
        outra: "DetectionBox",
    ) -> float:
        diferenca_x = (
            self.centro_x
            - outra.centro_x
        )

        diferenca_y = (
            self.centro_y
            - outra.centro_y
        )

        return math.hypot(
            diferenca_x,
            diferenca_y,
        )


@dataclass
class TrackedPerson:
    pessoa_id: str
    camera: str

    primeira_deteccao: float
    ultima_deteccao: float
    ultimo_processamento: float

    bbox: DetectionBox
    quantidade_deteccoes: int
    ultimo_evento_id: str


@dataclass(frozen=True)
class TrackingDecision:
    pessoa_id: str
    camera: str
    evento_id: str

    status: TrackingStatus
    deve_processar: bool

    quantidade_deteccoes: int
    bbox: DetectionBox


class PersonTracker:
    def __init__(
        self,
        intervalo_reprocessamento: float = 5.0,
        tempo_para_saida: float = 15.0,
        limite_iou: float = 0.25,
        limite_distancia: float = 0.75,
        janela_continuidade_flexivel: float = 4.0,
        limite_distancia_flexivel: float = 2.5,
        limite_razao_area_flexivel: float = 3.0,
        limite_velocidade_normalizada: float = 1.25,
    ) -> None:
        if intervalo_reprocessamento <= 0:
            raise ValueError(
                "intervalo_reprocessamento "
                "deve ser positivo"
            )

        if tempo_para_saida <= 0:
            raise ValueError(
                "tempo_para_saida deve ser positivo"
            )

        if limite_iou < 0:
            raise ValueError(
                "limite_iou não pode ser negativo"
            )

        if limite_distancia < 0:
            raise ValueError(
                "limite_distancia não pode ser negativo"
            )

        if janela_continuidade_flexivel <= 0:
            raise ValueError(
                "janela_continuidade_flexivel "
                "deve ser positiva"
            )

        if (
            limite_distancia_flexivel
            < limite_distancia
        ):
            raise ValueError(
                "limite_distancia_flexivel deve ser "
                "maior ou igual ao limite_distancia"
            )

        if limite_razao_area_flexivel < 1:
            raise ValueError(
                "limite_razao_area_flexivel deve ser "
                "maior ou igual a 1"
            )

        if limite_velocidade_normalizada <= 0:
            raise ValueError(
                "limite_velocidade_normalizada "
                "deve ser positivo"
            )

        self.intervalo_reprocessamento = (
            intervalo_reprocessamento
        )

        self.tempo_para_saida = tempo_para_saida
        self.limite_iou = limite_iou
        self.limite_distancia = limite_distancia

        self.janela_continuidade_flexivel = (
            janela_continuidade_flexivel
        )

        self.limite_distancia_flexivel = (
            limite_distancia_flexivel
        )

        self.limite_razao_area_flexivel = (
            limite_razao_area_flexivel
        )

        self.limite_velocidade_normalizada = (
            limite_velocidade_normalizada
        )

        self._pessoas: dict[
            str,
            TrackedPerson,
        ] = {}

        self._lock = asyncio.Lock()

    @property
    def quantidade_ativas(self) -> int:
        return len(self._pessoas)

    async def registrar(
        self,
        camera: str,
        evento_id: str,
        bbox: DetectionBox,
        agora: float | None = None,
    ) -> TrackingDecision:
        """
        Mantém compatibilidade com chamadas que
        enviam somente uma bounding box.
        """

        decisoes = await self.registrar_lote(
            camera=camera,
            evento_id=evento_id,
            bboxes=[bbox],
            agora=agora,
        )

        return decisoes[0]

    async def registrar_lote(
        self,
        camera: str,
        evento_id: str,
        bboxes: list[DetectionBox],
        agora: float | None = None,
    ) -> list[TrackingDecision]:
        """
        Registra todas as pessoas encontradas
        no mesmo quadro.

        Cada pessoa ativa pode ser associada
        somente a uma bounding box do quadro.
        """

        if not bboxes:
            return []

        for bbox in bboxes:
            if (
                bbox.largura <= 0
                or bbox.altura <= 0
            ):
                raise ValueError(
                    "Todas as bounding boxes devem "
                    "possuir largura e altura positivas"
                )

        momento = (
            time.monotonic()
            if agora is None
            else agora
        )

        async with self._lock:
            associacoes = (
                self._associar_deteccoes_em_lote(
                    camera=camera,
                    bboxes=bboxes,
                )
            )

            self._aplicar_continuidade_flexivel(
                camera=camera,
                bboxes=bboxes,
                associacoes=associacoes,
                agora=momento,
            )

            total_deteccoes = len(bboxes)

            decisoes: list[
                TrackingDecision
            ] = []

            for indice, bbox in enumerate(
                bboxes
            ):
                evento_individual_id = (
                    self._criar_evento_individual_id(
                        evento_id=evento_id,
                        indice=indice,
                        total=total_deteccoes,
                    )
                )

                pessoa = associacoes.get(
                    indice
                )

                if pessoa is None:
                    pessoa = self._criar_pessoa(
                        camera=camera,
                        evento_id=(
                            evento_individual_id
                        ),
                        bbox=bbox,
                        agora=momento,
                    )

                    decisoes.append(
                        TrackingDecision(
                            pessoa_id=(
                                pessoa.pessoa_id
                            ),
                            camera=camera,
                            evento_id=(
                                evento_individual_id
                            ),
                            status="entered",
                            deve_processar=True,
                            quantidade_deteccoes=1,
                            bbox=bbox,
                        )
                    )

                    continue

                decisao = self._atualizar_pessoa(
                    pessoa=pessoa,
                    evento_id=(
                        evento_individual_id
                    ),
                    bbox=bbox,
                    agora=momento,
                )

                decisoes.append(
                    decisao
                )

            return decisoes

    async def coletar_saidas(
        self,
        agora: float | None = None,
    ) -> list[TrackingDecision]:
        momento = (
            time.monotonic()
            if agora is None
            else agora
        )

        async with self._lock:
            pessoas_encerradas: list[
                TrackingDecision
            ] = []

            ids_para_remover: list[str] = []

            for (
                pessoa_id,
                pessoa,
            ) in self._pessoas.items():
                tempo_sem_deteccao = (
                    momento
                    - pessoa.ultima_deteccao
                )

                if (
                    tempo_sem_deteccao
                    < self.tempo_para_saida
                ):
                    continue

                pessoas_encerradas.append(
                    TrackingDecision(
                        pessoa_id=(
                            pessoa.pessoa_id
                        ),
                        camera=pessoa.camera,
                        evento_id=(
                            pessoa.ultimo_evento_id
                        ),
                        status="exited",
                        deve_processar=False,
                        quantidade_deteccoes=(
                            pessoa
                            .quantidade_deteccoes
                        ),
                        bbox=pessoa.bbox,
                    )
                )

                ids_para_remover.append(
                    pessoa_id
                )

            for pessoa_id in ids_para_remover:
                self._pessoas.pop(
                    pessoa_id,
                    None,
                )

            return pessoas_encerradas

    async def limpar(self) -> None:
        async with self._lock:
            self._pessoas.clear()

    def _associar_deteccoes_em_lote(
        self,
        camera: str,
        bboxes: list[DetectionBox],
    ) -> dict[int, TrackedPerson]:
        """
        Busca as melhores associações entre as
        bounding boxes e as pessoas ativas.

        Uma pessoa não pode ser usada duas vezes
        dentro do mesmo quadro.
        """

        candidatos: list[
            tuple[
                float,
                int,
                str,
            ]
        ] = []

        for indice, bbox in enumerate(
            bboxes
        ):
            for pessoa in self._pessoas.values():
                if pessoa.camera != camera:
                    continue

                pontuacao = (
                    self._calcular_pontuacao(
                        bbox=bbox,
                        pessoa=pessoa,
                    )
                )

                if pontuacao is None:
                    continue

                candidatos.append(
                    (
                        pontuacao,
                        indice,
                        pessoa.pessoa_id,
                    )
                )

        candidatos.sort(
            key=lambda item: (
                -item[0],
                item[1],
                item[2],
            )
        )

        deteccoes_utilizadas: set[int] = set()
        pessoas_utilizadas: set[str] = set()

        associacoes: dict[
            int,
            TrackedPerson,
        ] = {}

        for (
            _,
            indice,
            pessoa_id,
        ) in candidatos:
            if indice in deteccoes_utilizadas:
                continue

            if pessoa_id in pessoas_utilizadas:
                continue

            pessoa = self._pessoas.get(
                pessoa_id
            )

            if pessoa is None:
                continue

            associacoes[indice] = pessoa

            deteccoes_utilizadas.add(
                indice
            )

            pessoas_utilizadas.add(
                pessoa_id
            )

        return associacoes

    def _aplicar_continuidade_flexivel(
        self,
        camera: str,
        bboxes: list[DetectionBox],
        associacoes: dict[int, TrackedPerson],
        agora: float,
    ) -> None:
        """
        Corrige o caso em que uma pessoa atravessa
        rapidamente a imagem e a nova caixa fica
        distante da caixa anterior.

        Esta regra somente é aplicada quando:

        - existe uma única bounding box;
        - existe uma única pessoa ativa na câmera;
        - a associação normal falhou;
        - o intervalo entre as imagens é curto;
        - o tamanho das caixas continua plausível.

        Assim, a regra não interfere no rastreamento
        em lote de várias pessoas.
        """

        if len(bboxes) != 1:
            return

        if associacoes:
            return

        pessoas_camera = [
            pessoa
            for pessoa in self._pessoas.values()
            if pessoa.camera == camera
        ]

        if len(pessoas_camera) != 1:
            return

        pessoa = pessoas_camera[0]
        bbox = bboxes[0]

        continuidade_valida = (
            self._pode_usar_continuidade_flexivel(
                pessoa=pessoa,
                bbox=bbox,
                agora=agora,
            )
        )

        if continuidade_valida:
            associacoes[0] = pessoa

    def _pode_usar_continuidade_flexivel(
        self,
        pessoa: TrackedPerson,
        bbox: DetectionBox,
        agora: float,
    ) -> bool:
        tempo_sem_deteccao = max(
            0.0,
            agora - pessoa.ultima_deteccao,
        )

        if (
            tempo_sem_deteccao
            > self.janela_continuidade_flexivel
        ):
            return False

        menor_area = max(
            1,
            min(
                bbox.area,
                pessoa.bbox.area,
            ),
        )

        maior_area = max(
            bbox.area,
            pessoa.bbox.area,
        )

        razao_area = (
            maior_area
            / menor_area
        )

        if (
            razao_area
            > self.limite_razao_area_flexivel
        ):
            return False

        distancia = bbox.distancia_centros(
            pessoa.bbox
        )

        maior_dimensao = max(
            bbox.largura,
            bbox.altura,
            pessoa.bbox.largura,
            pessoa.bbox.altura,
            1,
        )

        distancia_normalizada = (
            distancia
            / maior_dimensao
        )

        limite_dinamico = min(
            self.limite_distancia_flexivel,
            max(
                self.limite_distancia,
                1.25
                + tempo_sem_deteccao * 0.45,
            ),
        )

        if (
            distancia_normalizada
            > limite_dinamico
        ):
            return False

        velocidade_normalizada = (
            distancia_normalizada
            / max(
                tempo_sem_deteccao,
                0.25,
            )
        )

        if (
            velocidade_normalizada
            > self.limite_velocidade_normalizada
        ):
            return False

        return True

    def _calcular_pontuacao(
        self,
        bbox: DetectionBox,
        pessoa: TrackedPerson,
    ) -> float | None:
        sobreposicao = bbox.iou(
            pessoa.bbox
        )

        distancia = bbox.distancia_centros(
            pessoa.bbox
        )

        maior_dimensao = max(
            bbox.largura,
            bbox.altura,
            pessoa.bbox.largura,
            pessoa.bbox.altura,
            1,
        )

        distancia_normalizada = (
            distancia
            / maior_dimensao
        )

        caixas_proximas = (
            distancia_normalizada
            <= self.limite_distancia
        )

        caixas_sobrepostas = (
            sobreposicao
            >= self.limite_iou
        )

        if not (
            caixas_proximas
            or caixas_sobrepostas
        ):
            return None

        pontuacao_distancia = max(
            0.0,
            1.0 - distancia_normalizada,
        )

        return (
            sobreposicao
            + pontuacao_distancia
        )

    def _atualizar_pessoa(
        self,
        pessoa: TrackedPerson,
        evento_id: str,
        bbox: DetectionBox,
        agora: float,
    ) -> TrackingDecision:
        pessoa.ultima_deteccao = agora
        pessoa.bbox = bbox
        pessoa.ultimo_evento_id = evento_id
        pessoa.quantidade_deteccoes += 1

        tempo_desde_processamento = (
            agora
            - pessoa.ultimo_processamento
        )

        if (
            tempo_desde_processamento
            >= self.intervalo_reprocessamento
        ):
            pessoa.ultimo_processamento = agora

            return TrackingDecision(
                pessoa_id=pessoa.pessoa_id,
                camera=pessoa.camera,
                evento_id=evento_id,
                status="updated",
                deve_processar=True,
                quantidade_deteccoes=(
                    pessoa.quantidade_deteccoes
                ),
                bbox=bbox,
            )

        return TrackingDecision(
            pessoa_id=pessoa.pessoa_id,
            camera=pessoa.camera,
            evento_id=evento_id,
            status="suppressed",
            deve_processar=False,
            quantidade_deteccoes=(
                pessoa.quantidade_deteccoes
            ),
            bbox=bbox,
        )

    def _encontrar_pessoa(
        self,
        camera: str,
        bbox: DetectionBox,
    ) -> TrackedPerson | None:
        """
        Mantido para compatibilidade com testes
        individuais.
        """

        melhor_pessoa: (
            TrackedPerson | None
        ) = None

        melhor_pontuacao = -1.0

        for pessoa in self._pessoas.values():
            if pessoa.camera != camera:
                continue

            pontuacao = (
                self._calcular_pontuacao(
                    bbox=bbox,
                    pessoa=pessoa,
                )
            )

            if pontuacao is None:
                continue

            if pontuacao > melhor_pontuacao:
                melhor_pontuacao = pontuacao
                melhor_pessoa = pessoa

        return melhor_pessoa

    def _criar_pessoa(
        self,
        camera: str,
        evento_id: str,
        bbox: DetectionBox,
        agora: float,
    ) -> TrackedPerson:
        camera_normalizada = "".join(
            caractere.lower()
            if caractere.isalnum()
            else "-"
            for caractere in camera
        ).strip("-")

        if not camera_normalizada:
            camera_normalizada = "camera"

        pessoa_id = (
            f"{camera_normalizada}-"
            f"{uuid.uuid4().hex[:8]}"
        )

        pessoa = TrackedPerson(
            pessoa_id=pessoa_id,
            camera=camera,
            primeira_deteccao=agora,
            ultima_deteccao=agora,
            ultimo_processamento=agora,
            bbox=bbox,
            quantidade_deteccoes=1,
            ultimo_evento_id=evento_id,
        )

        self._pessoas[
            pessoa_id
        ] = pessoa

        return pessoa

    def _criar_evento_individual_id(
        self,
        evento_id: str,
        indice: int,
        total: int,
    ) -> str:
        """
        Um quadro com uma pessoa mantém o ID original.

        Quadros com várias pessoas recebem IDs como:

        evento123-01
        evento123-02
        """

        if total <= 1:
            return evento_id

        return (
            f"{evento_id}-"
            f"{indice + 1:02d}"
        )


person_tracker = PersonTracker(
    intervalo_reprocessamento=5.0,
    tempo_para_saida=15.0,
    limite_iou=0.25,
    limite_distancia=0.75,
    janela_continuidade_flexivel=12.0,
    limite_distancia_flexivel=2.5,
    limite_razao_area_flexivel=12.0,
    limite_velocidade_normalizada=1.25,
)
------------------------------------------------------------
ARQUIVO: app/static/index_backup_analise_visual.html
------------------------------------------------------------
<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">

    <meta
        name="viewport"
        content="width=device-width, initial-scale=1.0"
    >

    <title>Painel de Eventos</title>

    <style>
        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            font-family: Arial, sans-serif;
            background: #10131a;
            color: #f3f4f6;
        }

        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px 30px;
            background: #191e29;
            border-bottom: 1px solid #2d3443;
        }

        h1 {
            margin: 0;
            font-size: 24px;
        }

        .status-conexao {
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .status-bolinha {
            width: 12px;
            height: 12px;
            border-radius: 50%;
            background: #f59e0b;
        }

        .status-bolinha.conectado {
            background: #22c55e;
        }

        .status-bolinha.desconectado {
            background: #ef4444;
        }

        main {
            padding: 30px;
        }

        .resumo {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
            margin-bottom: 25px;
        }

        .resumo-card {
            min-width: 180px;
            padding: 18px;
            background: #191e29;
            border: 1px solid #2d3443;
            border-radius: 10px;
        }

        .resumo-card span {
            display: block;
            color: #9ca3af;
            font-size: 14px;
        }

        .resumo-card strong {
            display: block;
            margin-top: 8px;
            font-size: 28px;
        }

        #eventos {
            display: grid;
            grid-template-columns: repeat(
                auto-fill,
                minmax(280px, 1fr)
            );
            gap: 20px;
        }

        .evento-card {
            overflow: hidden;
            background: #191e29;
            border: 1px solid #2d3443;
            border-radius: 10px;
            transition:
                border-color 0.2s,
                transform 0.2s,
                opacity 0.2s;
        }

        .evento-card.atualizado {
            border-color: #3b82f6;
            transform: translateY(-2px);
        }

        .evento-card.saiu {
            opacity: 0.55;
            border-color: #6b7280;
        }

        .evento-card img {
            width: 100%;
            height: 230px;
            object-fit: contain;
            background: #05070a;
        }

        .evento-dados {
            padding: 15px;
        }

        .evento-cabecalho {
            display: flex;
            justify-content: space-between;
            align-items: center;
            gap: 10px;
            margin-bottom: 12px;
        }

        .evento-cabecalho h2 {
            margin: 0;
            font-size: 18px;
        }

        .evento-status {
            padding: 5px 9px;
            border-radius: 999px;
            background: #374151;
            font-size: 12px;
            font-weight: bold;
        }

        .evento-status.entered {
            background: #166534;
            color: #dcfce7;
        }

        .evento-status.updated {
            background: #1e40af;
            color: #dbeafe;
        }

        .evento-status.exited {
            background: #4b5563;
            color: #f3f4f6;
        }

        .evento-dados p {
            margin: 6px 0;
            color: #cbd5e1;
            font-size: 14px;
        }

        .vazio {
            padding: 40px;
            color: #9ca3af;
            text-align: center;
            background: #191e29;
            border: 1px dashed #374151;
            border-radius: 10px;
        }

        @media (max-width: 700px) {
            header {
                align-items: flex-start;
                flex-direction: column;
                gap: 15px;
                padding: 20px;
            }

            main {
                padding: 20px;
            }

            .resumo-card {
                flex: 1;
                min-width: 140px;
            }
        }
    </style>
</head>

<body>
    <header>
        <h1>Painel de Eventos das Câmeras</h1>

        <div class="status-conexao">
            <div
                id="status-bolinha"
                class="status-bolinha"
            ></div>

            <span id="status-texto">
                Conectando...
            </span>
        </div>
    </header>

    <main>
        <section class="resumo">
            <div class="resumo-card">
                <span>Pessoas ativas</span>
                <strong id="pessoas-ativas">0</strong>
            </div>

            <div class="resumo-card">
                <span>Pessoas que saíram</span>
                <strong id="pessoas-sairam">0</strong>
            </div>

            <div class="resumo-card">
                <span>Total exibido</span>
                <strong id="total-pessoas">0</strong>
            </div>

            <div class="resumo-card">
                <span>Eventos processados</span>
                <strong id="total-eventos">0</strong>
            </div>
        </section>

        <section id="eventos">
            <div id="mensagem-vazia" class="vazio">
                Aguardando eventos das câmeras...
            </div>
        </section>
    </main>

    <script>
        const statusBolinha =
            document.getElementById("status-bolinha");

        const statusTexto =
            document.getElementById("status-texto");

        const pessoasAtivasElemento =
            document.getElementById("pessoas-ativas");

        const pessoasSairamElemento =
            document.getElementById("pessoas-sairam");

        const totalPessoasElemento =
            document.getElementById("total-pessoas");

        const totalEventosElemento =
            document.getElementById("total-eventos");

        const eventosElemento =
            document.getElementById("eventos");

        /*
         * Guarda o cartão e o último estado de cada pessoa.
         */
        const cartoesPorPessoa = new Map();
        const statusPorPessoa = new Map();

        let pessoasAtivas = 0;
        let pessoasSairam = 0;
        let totalPessoas = 0;
        let totalEventos = 0;
        let heartbeatId = null;

        function traduzirStatus(status) {
            const traducoes = {
                entered: "Entrou",
                updated: "Atualizado",
                suppressed: "Repetido",
                exited: "Saiu"
            };

            return (
                traducoes[status]
                || status
                || "Detectado"
            );
        }

        function atualizarResumo() {
            pessoasAtivasElemento.textContent =
                pessoasAtivas;

            pessoasSairamElemento.textContent =
                pessoasSairam;

            totalPessoasElemento.textContent =
                totalPessoas;

            totalEventosElemento.textContent =
                totalEventos;
        }

        function registrarMudancaStatus(
            pessoaId,
            novoStatus
        ) {
            const statusAnterior =
                statusPorPessoa.get(pessoaId);

            /*
             * Primeira mensagem recebida dessa pessoa.
             */
            if (!statusAnterior) {
                totalPessoas += 1;

                if (novoStatus === "exited") {
                    pessoasSairam += 1;
                } else {
                    pessoasAtivas += 1;
                }
            }

            /*
             * A pessoa estava ativa e agora saiu.
             */
            else if (
                statusAnterior !== "exited"
                && novoStatus === "exited"
            ) {
                pessoasAtivas = Math.max(
                    0,
                    pessoasAtivas - 1
                );

                pessoasSairam += 1;
            }

            /*
             * Segurança caso o mesmo identificador volte
             * a receber um evento ativo.
             */
            else if (
                statusAnterior === "exited"
                && novoStatus !== "exited"
            ) {
                pessoasSairam = Math.max(
                    0,
                    pessoasSairam - 1
                );

                pessoasAtivas += 1;
            }

            statusPorPessoa.set(
                pessoaId,
                novoStatus
            );

            atualizarResumo();
        }

        function conectarWebSocket() {
            const protocolo =
                window.location.protocol === "https:"
                    ? "wss"
                    : "ws";

            const endereco =
                `${protocolo}://${window.location.host}/ws/eventos`;

            const websocket =
                new WebSocket(endereco);

            websocket.onopen = function () {
                statusTexto.textContent =
                    "Conectado";

                statusBolinha.className =
                    "status-bolinha conectado";

                if (heartbeatId) {
                    clearInterval(heartbeatId);
                }

                heartbeatId = setInterval(
                    function () {
                        if (
                            websocket.readyState
                            === WebSocket.OPEN
                        ) {
                            websocket.send("ping");
                        }
                    },
                    20000
                );
            };

            websocket.onmessage = function (mensagem) {
                try {
                    const evento =
                        JSON.parse(mensagem.data);

                    processarEvento(evento);
                } catch (erro) {
                    console.error(
                        "Erro ao interpretar evento:",
                        erro
                    );
                }
            };

            websocket.onclose = function () {
                statusTexto.textContent =
                    "Desconectado — reconectando...";

                statusBolinha.className =
                    "status-bolinha desconectado";

                if (heartbeatId) {
                    clearInterval(heartbeatId);
                    heartbeatId = null;
                }

                setTimeout(
                    conectarWebSocket,
                    2000
                );
            };

            websocket.onerror = function () {
                websocket.close();
            };
        }

        function processarEvento(evento) {
            /*
             * O backend já bloqueia eventos repetidos.
             * Esta verificação é uma proteção adicional.
             */
            if (evento.status === "suppressed") {
                return;
            }

            const pessoaId =
                evento.pessoa_id
                || evento.evento_id;

            if (!pessoaId) {
                console.warn(
                    "Evento recebido sem identificador",
                    evento
                );

                return;
            }

            const cartaoExistente =
                cartoesPorPessoa.get(pessoaId);

            /*
             * Não cria cartão vazio quando o painel
             * conecta somente depois da saída da pessoa.
             */
            if (
                evento.status === "exited"
                && !cartaoExistente
            ) {
                return;
            }

            const novoStatus =
                evento.status || "entered";

            registrarMudancaStatus(
                pessoaId,
                novoStatus
            );

            totalEventos += 1;
            atualizarResumo();

            if (cartaoExistente) {
                atualizarCartao(
                    cartaoExistente,
                    evento
                );

                return;
            }

            criarCartao(
                pessoaId,
                evento
            );
        }

        function criarCartao(
            pessoaId,
            evento
        ) {
            const mensagemVazia =
                document.getElementById(
                    "mensagem-vazia"
                );

            if (mensagemVazia) {
                mensagemVazia.remove();
            }

            const card =
                document.createElement("article");

            card.className = "evento-card";
            card.dataset.pessoaId = pessoaId;

            const imagem =
                document.createElement("img");

            imagem.alt = "Recorte da pessoa";
            imagem.dataset.campo = "imagem";

            const dados =
                document.createElement("div");

            dados.className = "evento-dados";

            const cabecalho =
                document.createElement("div");

            cabecalho.className =
                "evento-cabecalho";

            const titulo =
                document.createElement("h2");

            titulo.dataset.campo = "camera";

            const status =
                document.createElement("span");

            status.dataset.campo = "status";
            status.className = "evento-status";

            const tipo =
                document.createElement("p");

            tipo.dataset.campo = "tipo";

            const horario =
                document.createElement("p");

            horario.dataset.campo = "horario";

            const identificadorPessoa =
                document.createElement("p");

            identificadorPessoa.dataset.campo =
                "pessoa-id";

            const quantidade =
                document.createElement("p");

            quantidade.dataset.campo =
                "quantidade";

            cabecalho.appendChild(titulo);
            cabecalho.appendChild(status);

            dados.appendChild(cabecalho);
            dados.appendChild(tipo);
            dados.appendChild(horario);
            dados.appendChild(
                identificadorPessoa
            );
            dados.appendChild(quantidade);

            card.appendChild(imagem);
            card.appendChild(dados);

            eventosElemento.prepend(card);

            cartoesPorPessoa.set(
                pessoaId,
                card
            );

            atualizarCartao(
                card,
                evento
            );

            limitarQuantidadeCartoes();
        }

        function atualizarCartao(
            card,
            evento
        ) {
            const imagem =
                card.querySelector(
                    '[data-campo="imagem"]'
                );

            const titulo =
                card.querySelector(
                    '[data-campo="camera"]'
                );

            const status =
                card.querySelector(
                    '[data-campo="status"]'
                );

            const tipo =
                card.querySelector(
                    '[data-campo="tipo"]'
                );

            const horario =
                card.querySelector(
                    '[data-campo="horario"]'
                );

            const pessoaId =
                card.querySelector(
                    '[data-campo="pessoa-id"]'
                );

            const quantidade =
                card.querySelector(
                    '[data-campo="quantidade"]'
                );

            /*
             * O evento de saída não envia uma imagem.
             * A última captura permanece no cartão.
             */
            if (evento.imagem) {
                imagem.src =
                    `data:image/jpeg;base64,${evento.imagem}`;
            }

            if (evento.camera) {
                titulo.textContent =
                    evento.camera;
            }

            status.textContent =
                traduzirStatus(evento.status);

            status.className =
                `evento-status ${evento.status || ""}`;

            /*
             * O evento de saída não possui um novo tipo.
             * Mantemos o último tipo exibido.
             */
            if (evento.tipo) {
                tipo.textContent =
                    `Evento: ${evento.tipo}`;
            }

            if (evento.status === "exited") {
                horario.textContent =
                    `Saída: ${evento.datetime || "-"}`;
            } else {
                horario.textContent =
                    `Última captura: ${
                        evento.datetime || "-"
                    }`;
            }

            pessoaId.textContent =
                `Pessoa: ${
                    evento.pessoa_id || "-"
                }`;

            quantidade.textContent =
                `Detecções: ${
                    evento.quantidade_deteccoes || 1
                }`;

            card.classList.remove(
                "atualizado",
                "saiu"
            );

            if (evento.status === "updated") {
                card.classList.add(
                    "atualizado"
                );

                /*
                 * Move a pessoa atualizada para o início.
                 */
                eventosElemento.prepend(card);

                setTimeout(
                    function () {
                        card.classList.remove(
                            "atualizado"
                        );
                    },
                    1500
                );
            }

            if (evento.status === "exited") {
                card.classList.add("saiu");
            }
        }

        function limitarQuantidadeCartoes() {
            while (
                eventosElemento.children.length > 50
            ) {
                const ultimo =
                    eventosElemento.lastElementChild;

                if (!ultimo) {
                    break;
                }

                const pessoaId =
                    ultimo.dataset.pessoaId;

                if (pessoaId) {
                    const statusRemovido =
                        statusPorPessoa.get(pessoaId);

                    if (statusRemovido === "exited") {
                        pessoasSairam = Math.max(
                            0,
                            pessoasSairam - 1
                        );
                    } else if (statusRemovido) {
                        pessoasAtivas = Math.max(
                            0,
                            pessoasAtivas - 1
                        );
                    }

                    totalPessoas = Math.max(
                        0,
                        totalPessoas - 1
                    );

                    cartoesPorPessoa.delete(
                        pessoaId
                    );

                    statusPorPessoa.delete(
                        pessoaId
                    );
                }

                ultimo.remove();
            }

            atualizarResumo();
        }

        atualizarResumo();
        conectarWebSocket();
    </script>
</body>
</html>
------------------------------------------------------------
ARQUIVO: app/static/index_backup_antes_analise.html
------------------------------------------------------------
<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">

    <meta
        name="viewport"
        content="width=device-width, initial-scale=1.0"
    >

    <title>Painel de Eventos</title>

    <style>
        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            font-family: Arial, sans-serif;
            background: #10131a;
            color: #f3f4f6;
        }

        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px 30px;
            background: #191e29;
            border-bottom: 1px solid #2d3443;
        }

        h1 {
            margin: 0;
            font-size: 24px;
        }

        .status-conexao {
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .status-bolinha {
            width: 12px;
            height: 12px;
            border-radius: 50%;
            background: #f59e0b;
        }

        .status-bolinha.conectado {
            background: #22c55e;
        }

        .status-bolinha.desconectado {
            background: #ef4444;
        }

        main {
            padding: 30px;
        }

        .resumo {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
            margin-bottom: 25px;
        }

        .resumo-card {
            min-width: 180px;
            padding: 18px;
            background: #191e29;
            border: 1px solid #2d3443;
            border-radius: 10px;
        }

        .resumo-card span {
            display: block;
            color: #9ca3af;
            font-size: 14px;
        }

        .resumo-card strong {
            display: block;
            margin-top: 8px;
            font-size: 28px;
        }

        #eventos {
            display: grid;
            grid-template-columns: repeat(
                auto-fill,
                minmax(280px, 1fr)
            );
            gap: 20px;
        }

        .evento-card {
            overflow: hidden;
            background: #191e29;
            border: 1px solid #2d3443;
            border-radius: 10px;
            transition:
                border-color 0.2s,
                transform 0.2s,
                opacity 0.2s;
        }

        .evento-card.atualizado {
            border-color: #3b82f6;
            transform: translateY(-2px);
        }

        .evento-card.saiu {
            opacity: 0.55;
            border-color: #6b7280;
        }

        .evento-card img {
            width: 100%;
            height: 230px;
            object-fit: contain;
            background: #05070a;
        }

        .evento-dados {
            padding: 15px;
        }

        .evento-cabecalho {
            display: flex;
            justify-content: space-between;
            align-items: center;
            gap: 10px;
            margin-bottom: 12px;
        }

        .evento-cabecalho h2 {
            margin: 0;
            font-size: 18px;
        }

        .evento-status {
            padding: 5px 9px;
            border-radius: 999px;
            background: #374151;
            font-size: 12px;
            font-weight: bold;
        }

        .evento-status.entered {
            background: #166534;
            color: #dcfce7;
        }

        .evento-status.updated {
            background: #1e40af;
            color: #dbeafe;
        }

        .evento-status.exited {
            background: #4b5563;
            color: #f3f4f6;
        }

        .evento-dados p {
            margin: 6px 0;
            color: #cbd5e1;
            font-size: 14px;
        }

        .vazio {
            padding: 40px;
            color: #9ca3af;
            text-align: center;
            background: #191e29;
            border: 1px dashed #374151;
            border-radius: 10px;
        }

        @media (max-width: 700px) {
            header {
                align-items: flex-start;
                flex-direction: column;
                gap: 15px;
                padding: 20px;
            }

            main {
                padding: 20px;
            }

            .resumo-card {
                flex: 1;
                min-width: 140px;
            }
        }
    </style>
</head>

<body>
    <header>
        <h1>Painel de Eventos das Câmeras</h1>

        <div class="status-conexao">
            <div
                id="status-bolinha"
                class="status-bolinha"
            ></div>

            <span id="status-texto">
                Conectando...
            </span>
        </div>
    </header>

    <main>
        <section class="resumo">
            <div class="resumo-card">
                <span>Pessoas ativas</span>
                <strong id="pessoas-ativas">0</strong>
            </div>

            <div class="resumo-card">
                <span>Pessoas que saíram</span>
                <strong id="pessoas-sairam">0</strong>
            </div>

            <div class="resumo-card">
                <span>Total exibido</span>
                <strong id="total-pessoas">0</strong>
            </div>

            <div class="resumo-card">
                <span>Eventos processados</span>
                <strong id="total-eventos">0</strong>
            </div>
        </section>

        <section id="eventos">
            <div id="mensagem-vazia" class="vazio">
                Aguardando eventos das câmeras...
            </div>
        </section>
    </main>

    <script>
        const statusBolinha =
            document.getElementById("status-bolinha");

        const statusTexto =
            document.getElementById("status-texto");

        const pessoasAtivasElemento =
            document.getElementById("pessoas-ativas");

        const pessoasSairamElemento =
            document.getElementById("pessoas-sairam");

        const totalPessoasElemento =
            document.getElementById("total-pessoas");

        const totalEventosElemento =
            document.getElementById("total-eventos");

        const eventosElemento =
            document.getElementById("eventos");

        /*
         * Guarda o cartão e o último estado de cada pessoa.
         */
        const cartoesPorPessoa = new Map();
        const statusPorPessoa = new Map();

        let pessoasAtivas = 0;
        let pessoasSairam = 0;
        let totalPessoas = 0;
        let totalEventos = 0;
        let heartbeatId = null;

        function traduzirStatus(status) {
            const traducoes = {
                entered: "Entrou",
                updated: "Atualizado",
                suppressed: "Repetido",
                exited: "Saiu"
            };

            return (
                traducoes[status]
                || status
                || "Detectado"
            );
        }

        function atualizarResumo() {
            pessoasAtivasElemento.textContent =
                pessoasAtivas;

            pessoasSairamElemento.textContent =
                pessoasSairam;

            totalPessoasElemento.textContent =
                totalPessoas;

            totalEventosElemento.textContent =
                totalEventos;
        }

        function registrarMudancaStatus(
            pessoaId,
            novoStatus
        ) {
            const statusAnterior =
                statusPorPessoa.get(pessoaId);

            /*
             * Primeira mensagem recebida dessa pessoa.
             */
            if (!statusAnterior) {
                totalPessoas += 1;

                if (novoStatus === "exited") {
                    pessoasSairam += 1;
                } else {
                    pessoasAtivas += 1;
                }
            }

            /*
             * A pessoa estava ativa e agora saiu.
             */
            else if (
                statusAnterior !== "exited"
                && novoStatus === "exited"
            ) {
                pessoasAtivas = Math.max(
                    0,
                    pessoasAtivas - 1
                );

                pessoasSairam += 1;
            }

            /*
             * Segurança caso o mesmo identificador volte
             * a receber um evento ativo.
             */
            else if (
                statusAnterior === "exited"
                && novoStatus !== "exited"
            ) {
                pessoasSairam = Math.max(
                    0,
                    pessoasSairam - 1
                );

                pessoasAtivas += 1;
            }

            statusPorPessoa.set(
                pessoaId,
                novoStatus
            );

            atualizarResumo();
        }

        function conectarWebSocket() {
            const protocolo =
                window.location.protocol === "https:"
                    ? "wss"
                    : "ws";

            const endereco =
                `${protocolo}://${window.location.host}/ws/eventos`;

            const websocket =
                new WebSocket(endereco);

            websocket.onopen = function () {
                statusTexto.textContent =
                    "Conectado";

                statusBolinha.className =
                    "status-bolinha conectado";

                if (heartbeatId) {
                    clearInterval(heartbeatId);
                }

                heartbeatId = setInterval(
                    function () {
                        if (
                            websocket.readyState
                            === WebSocket.OPEN
                        ) {
                            websocket.send("ping");
                        }
                    },
                    20000
                );
            };

            websocket.onmessage = function (mensagem) {
                try {
                    const evento =
                        JSON.parse(mensagem.data);

                    processarEvento(evento);
                } catch (erro) {
                    console.error(
                        "Erro ao interpretar evento:",
                        erro
                    );
                }
            };

            websocket.onclose = function () {
                statusTexto.textContent =
                    "Desconectado — reconectando...";

                statusBolinha.className =
                    "status-bolinha desconectado";

                if (heartbeatId) {
                    clearInterval(heartbeatId);
                    heartbeatId = null;
                }

                setTimeout(
                    conectarWebSocket,
                    2000
                );
            };

            websocket.onerror = function () {
                websocket.close();
            };
        }

        function processarEvento(evento) {
            /*
             * O backend já bloqueia eventos repetidos.
             * Esta verificação é uma proteção adicional.
             */
            if (evento.status === "suppressed") {
                return;
            }

            const pessoaId =
                evento.pessoa_id
                || evento.evento_id;

            if (!pessoaId) {
                console.warn(
                    "Evento recebido sem identificador",
                    evento
                );

                return;
            }

            const cartaoExistente =
                cartoesPorPessoa.get(pessoaId);

            /*
             * Não cria cartão vazio quando o painel
             * conecta somente depois da saída da pessoa.
             */
            if (
                evento.status === "exited"
                && !cartaoExistente
            ) {
                return;
            }

            const novoStatus =
                evento.status || "entered";

            registrarMudancaStatus(
                pessoaId,
                novoStatus
            );

            totalEventos += 1;
            atualizarResumo();

            if (cartaoExistente) {
                atualizarCartao(
                    cartaoExistente,
                    evento
                );

                return;
            }

            criarCartao(
                pessoaId,
                evento
            );
        }

        function criarCartao(
            pessoaId,
            evento
        ) {
            const mensagemVazia =
                document.getElementById(
                    "mensagem-vazia"
                );

            if (mensagemVazia) {
                mensagemVazia.remove();
            }

            const card =
                document.createElement("article");

            card.className = "evento-card";
            card.dataset.pessoaId = pessoaId;

            const imagem =
                document.createElement("img");

            imagem.alt = "Recorte da pessoa";
            imagem.dataset.campo = "imagem";

            const dados =
                document.createElement("div");

            dados.className = "evento-dados";

            const cabecalho =
                document.createElement("div");

            cabecalho.className =
                "evento-cabecalho";

            const titulo =
                document.createElement("h2");

            titulo.dataset.campo = "camera";

            const status =
                document.createElement("span");

            status.dataset.campo = "status";
            status.className = "evento-status";

            const tipo =
                document.createElement("p");

            tipo.dataset.campo = "tipo";

            const horario =
                document.createElement("p");

            horario.dataset.campo = "horario";

            const identificadorPessoa =
                document.createElement("p");

            identificadorPessoa.dataset.campo =
                "pessoa-id";

            const quantidade =
                document.createElement("p");

            quantidade.dataset.campo =
                "quantidade";

            cabecalho.appendChild(titulo);
            cabecalho.appendChild(status);

            dados.appendChild(cabecalho);
            dados.appendChild(tipo);
            dados.appendChild(horario);
            dados.appendChild(
                identificadorPessoa
            );
            dados.appendChild(quantidade);

            card.appendChild(imagem);
            card.appendChild(dados);

            eventosElemento.prepend(card);

            cartoesPorPessoa.set(
                pessoaId,
                card
            );

            atualizarCartao(
                card,
                evento
            );

            limitarQuantidadeCartoes();
        }

        function atualizarCartao(
            card,
            evento
        ) {
            const imagem =
                card.querySelector(
                    '[data-campo="imagem"]'
                );

            const titulo =
                card.querySelector(
                    '[data-campo="camera"]'
                );

            const status =
                card.querySelector(
                    '[data-campo="status"]'
                );

            const tipo =
                card.querySelector(
                    '[data-campo="tipo"]'
                );

            const horario =
                card.querySelector(
                    '[data-campo="horario"]'
                );

            const pessoaId =
                card.querySelector(
                    '[data-campo="pessoa-id"]'
                );

            const quantidade =
                card.querySelector(
                    '[data-campo="quantidade"]'
                );

            /*
             * O evento de saída não envia uma imagem.
             * A última captura permanece no cartão.
             */
            if (evento.imagem) {
                imagem.src =
                    `data:image/jpeg;base64,${evento.imagem}`;
            }

            if (evento.camera) {
                titulo.textContent =
                    evento.camera;
            }

            status.textContent =
                traduzirStatus(evento.status);

            status.className =
                `evento-status ${evento.status || ""}`;

            /*
             * O evento de saída não possui um novo tipo.
             * Mantemos o último tipo exibido.
             */
            if (evento.tipo) {
                tipo.textContent =
                    `Evento: ${evento.tipo}`;
            }

            if (evento.status === "exited") {
                horario.textContent =
                    `Saída: ${evento.datetime || "-"}`;
            } else {
                horario.textContent =
                    `Última captura: ${
                        evento.datetime || "-"
                    }`;
            }

            pessoaId.textContent =
                `Pessoa: ${
                    evento.pessoa_id || "-"
                }`;

            quantidade.textContent =
                `Detecções: ${
                    evento.quantidade_deteccoes || 1
                }`;

            card.classList.remove(
                "atualizado",
                "saiu"
            );

            if (evento.status === "updated") {
                card.classList.add(
                    "atualizado"
                );

                /*
                 * Move a pessoa atualizada para o início.
                 */
                eventosElemento.prepend(card);

                setTimeout(
                    function () {
                        card.classList.remove(
                            "atualizado"
                        );
                    },
                    1500
                );
            }

            if (evento.status === "exited") {
                card.classList.add("saiu");
            }
        }

        function limitarQuantidadeCartoes() {
            while (
                eventosElemento.children.length > 50
            ) {
                const ultimo =
                    eventosElemento.lastElementChild;

                if (!ultimo) {
                    break;
                }

                const pessoaId =
                    ultimo.dataset.pessoaId;

                if (pessoaId) {
                    const statusRemovido =
                        statusPorPessoa.get(pessoaId);

                    if (statusRemovido === "exited") {
                        pessoasSairam = Math.max(
                            0,
                            pessoasSairam - 1
                        );
                    } else if (statusRemovido) {
                        pessoasAtivas = Math.max(
                            0,
                            pessoasAtivas - 1
                        );
                    }

                    totalPessoas = Math.max(
                        0,
                        totalPessoas - 1
                    );

                    cartoesPorPessoa.delete(
                        pessoaId
                    );

                    statusPorPessoa.delete(
                        pessoaId
                    );
                }

                ultimo.remove();
            }

            atualizarResumo();
        }

        atualizarResumo();
        conectarWebSocket();
    </script>
</body>
</html>
------------------------------------------------------------
ARQUIVO: app/static/index_backup_antes_contadores.html
------------------------------------------------------------
<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">

    <meta
        name="viewport"
        content="width=device-width, initial-scale=1.0"
    >

    <title>Painel de Eventos</title>

    <style>
        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            font-family: Arial, sans-serif;
            background: #10131a;
            color: #f3f4f6;
        }

        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px 30px;
            background: #191e29;
            border-bottom: 1px solid #2d3443;
        }

        h1 {
            margin: 0;
            font-size: 24px;
        }

        .status-conexao {
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .status-bolinha {
            width: 12px;
            height: 12px;
            border-radius: 50%;
            background: #f59e0b;
        }

        .status-bolinha.conectado {
            background: #22c55e;
        }

        .status-bolinha.desconectado {
            background: #ef4444;
        }

        main {
            padding: 30px;
        }

        .resumo {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
            margin-bottom: 25px;
        }

        .resumo-card {
            min-width: 180px;
            padding: 18px;
            background: #191e29;
            border: 1px solid #2d3443;
            border-radius: 10px;
        }

        .resumo-card span {
            display: block;
            color: #9ca3af;
            font-size: 14px;
        }

        .resumo-card strong {
            display: block;
            margin-top: 8px;
            font-size: 28px;
        }

        #eventos {
            display: grid;
            grid-template-columns: repeat(
                auto-fill,
                minmax(280px, 1fr)
            );
            gap: 20px;
        }

        .evento-card {
            overflow: hidden;
            background: #191e29;
            border: 1px solid #2d3443;
            border-radius: 10px;
            transition:
                border-color 0.2s,
                transform 0.2s,
                opacity 0.2s;
        }

        .evento-card.atualizado {
            border-color: #3b82f6;
            transform: translateY(-2px);
        }

        .evento-card.saiu {
            opacity: 0.55;
            border-color: #6b7280;
        }

        .evento-card img {
            width: 100%;
            height: 230px;
            object-fit: contain;
            background: #05070a;
        }

        .evento-dados {
            padding: 15px;
        }

        .evento-cabecalho {
            display: flex;
            justify-content: space-between;
            align-items: center;
            gap: 10px;
            margin-bottom: 12px;
        }

        .evento-cabecalho h2 {
            margin: 0;
            font-size: 18px;
        }

        .evento-status {
            padding: 5px 9px;
            border-radius: 999px;
            background: #374151;
            font-size: 12px;
            font-weight: bold;
        }

        .evento-status.entered {
            background: #166534;
            color: #dcfce7;
        }

        .evento-status.updated {
            background: #1e40af;
            color: #dbeafe;
        }

        .evento-status.exited {
            background: #4b5563;
            color: #f3f4f6;
        }

        .evento-dados p {
            margin: 6px 0;
            color: #cbd5e1;
            font-size: 14px;
        }

        .vazio {
            padding: 40px;
            color: #9ca3af;
            text-align: center;
            background: #191e29;
            border: 1px dashed #374151;
            border-radius: 10px;
        }
    </style>
</head>

<body>
    <header>
        <h1>Painel de Eventos das Câmeras</h1>

        <div class="status-conexao">
            <div
                id="status-bolinha"
                class="status-bolinha"
            ></div>

            <span id="status-texto">
                Conectando...
            </span>
        </div>
    </header>

    <main>
        <section class="resumo">
            <div class="resumo-card">
                <span>Pessoas exibidas</span>
                <strong id="total-pessoas">0</strong>
            </div>

            <div class="resumo-card">
                <span>Eventos processados</span>
                <strong id="total-eventos">0</strong>
            </div>
        </section>

        <section id="eventos">
            <div id="mensagem-vazia" class="vazio">
                Aguardando eventos das câmeras...
            </div>
        </section>
    </main>

    <script>
        const statusBolinha =
            document.getElementById("status-bolinha");

        const statusTexto =
            document.getElementById("status-texto");

        const totalPessoasElemento =
            document.getElementById("total-pessoas");

        const totalEventosElemento =
            document.getElementById("total-eventos");

        const eventosElemento =
            document.getElementById("eventos");

        const cartoesPorPessoa = new Map();

        let totalPessoas = 0;
        let totalEventos = 0;
        let heartbeatId = null;

        function traduzirStatus(status) {
            const traducoes = {
                entered: "Entrou",
                updated: "Atualizado",
                suppressed: "Repetido",
                exited: "Saiu"
            };

            return (
                traducoes[status]
                || status
                || "Detectado"
            );
        }

        function conectarWebSocket() {
            const protocolo =
                window.location.protocol === "https:"
                    ? "wss"
                    : "ws";

            const endereco =
                `${protocolo}://${window.location.host}/ws/eventos`;

            const websocket =
                new WebSocket(endereco);

            websocket.onopen = function () {
                statusTexto.textContent =
                    "Conectado";

                statusBolinha.className =
                    "status-bolinha conectado";

                if (heartbeatId) {
                    clearInterval(heartbeatId);
                }

                heartbeatId = setInterval(
                    function () {
                        if (
                            websocket.readyState
                            === WebSocket.OPEN
                        ) {
                            websocket.send("ping");
                        }
                    },
                    20000
                );
            };

            websocket.onmessage = function (mensagem) {
                const evento =
                    JSON.parse(mensagem.data);

                processarEvento(evento);
            };

            websocket.onclose = function () {
                statusTexto.textContent =
                    "Desconectado — reconectando...";

                statusBolinha.className =
                    "status-bolinha desconectado";

                if (heartbeatId) {
                    clearInterval(heartbeatId);
                    heartbeatId = null;
                }

                setTimeout(
                    conectarWebSocket,
                    2000
                );
            };

            websocket.onerror = function () {
                websocket.close();
            };
        }

        function processarEvento(evento) {
            if (evento.status === "suppressed") {
                return;
            }

            const pessoaId =
                evento.pessoa_id
                || evento.evento_id;

            const cartaoExistente =
                cartoesPorPessoa.get(pessoaId);

            /*
             * Não cria um cartão vazio caso o painel tenha
             * conectado somente depois da saída da pessoa.
             */
            if (
                evento.status === "exited"
                && !cartaoExistente
            ) {
                return;
            }

            totalEventos += 1;

            totalEventosElemento.textContent =
                totalEventos;

            if (cartaoExistente) {
                atualizarCartao(
                    cartaoExistente,
                    evento
                );

                return;
            }

            criarCartao(
                pessoaId,
                evento
            );
        }

        function criarCartao(
            pessoaId,
            evento
        ) {
            const mensagemVazia =
                document.getElementById(
                    "mensagem-vazia"
                );

            if (mensagemVazia) {
                mensagemVazia.remove();
            }

            const card =
                document.createElement("article");

            card.className = "evento-card";
            card.dataset.pessoaId = pessoaId;

            const imagem =
                document.createElement("img");

            imagem.alt = "Recorte da pessoa";
            imagem.dataset.campo = "imagem";

            const dados =
                document.createElement("div");

            dados.className = "evento-dados";

            const cabecalho =
                document.createElement("div");

            cabecalho.className =
                "evento-cabecalho";

            const titulo =
                document.createElement("h2");

            titulo.dataset.campo = "camera";

            const status =
                document.createElement("span");

            status.dataset.campo = "status";
            status.className = "evento-status";

            const tipo =
                document.createElement("p");

            tipo.dataset.campo = "tipo";

            const horario =
                document.createElement("p");

            horario.dataset.campo = "horario";

            const identificadorPessoa =
                document.createElement("p");

            identificadorPessoa.dataset.campo =
                "pessoa-id";

            const quantidade =
                document.createElement("p");

            quantidade.dataset.campo =
                "quantidade";

            cabecalho.appendChild(titulo);
            cabecalho.appendChild(status);

            dados.appendChild(cabecalho);
            dados.appendChild(tipo);
            dados.appendChild(horario);
            dados.appendChild(
                identificadorPessoa
            );
            dados.appendChild(quantidade);

            card.appendChild(imagem);
            card.appendChild(dados);

            eventosElemento.prepend(card);

            cartoesPorPessoa.set(
                pessoaId,
                card
            );

            totalPessoas += 1;

            totalPessoasElemento.textContent =
                totalPessoas;

            atualizarCartao(
                card,
                evento
            );

            limitarQuantidadeCartoes();
        }

        function atualizarCartao(
            card,
            evento
        ) {
            const imagem =
                card.querySelector(
                    '[data-campo="imagem"]'
                );

            const titulo =
                card.querySelector(
                    '[data-campo="camera"]'
                );

            const status =
                card.querySelector(
                    '[data-campo="status"]'
                );

            const tipo =
                card.querySelector(
                    '[data-campo="tipo"]'
                );

            const horario =
                card.querySelector(
                    '[data-campo="horario"]'
                );

            const pessoaId =
                card.querySelector(
                    '[data-campo="pessoa-id"]'
                );

            const quantidade =
                card.querySelector(
                    '[data-campo="quantidade"]'
                );

            /*
             * No evento de saída não existe uma nova imagem.
             * Por isso, a última imagem continua no cartão.
             */
            if (evento.imagem) {
                imagem.src =
                    `data:image/jpeg;base64,${evento.imagem}`;
            }

            if (evento.camera) {
                titulo.textContent =
                    evento.camera;
            }

            status.textContent =
                traduzirStatus(evento.status);

            status.className =
                `evento-status ${evento.status || ""}`;

            /*
             * No evento de saída o tipo não é enviado.
             * O painel mantém o último tipo mostrado.
             */
            if (evento.tipo) {
                tipo.textContent =
                    `Evento: ${evento.tipo}`;
            }

            if (evento.status === "exited") {
                horario.textContent =
                    `Saída: ${evento.datetime || "-"}`;
            } else {
                horario.textContent =
                    `Última captura: ${
                        evento.datetime || "-"
                    }`;
            }

            pessoaId.textContent =
                `Pessoa: ${
                    evento.pessoa_id || "-"
                }`;

            quantidade.textContent =
                `Detecções: ${
                    evento.quantidade_deteccoes || 1
                }`;

            card.classList.remove(
                "atualizado",
                "saiu"
            );

            if (evento.status === "updated") {
                card.classList.add(
                    "atualizado"
                );

                eventosElemento.prepend(card);

                setTimeout(
                    function () {
                        card.classList.remove(
                            "atualizado"
                        );
                    },
                    1500
                );
            }

            if (evento.status === "exited") {
                card.classList.add("saiu");
            }
        }

        function limitarQuantidadeCartoes() {
            while (
                eventosElemento.children.length > 50
            ) {
                const ultimo =
                    eventosElemento.lastElementChild;

                const pessoaId =
                    ultimo.dataset.pessoaId;

                if (pessoaId) {
                    cartoesPorPessoa.delete(
                        pessoaId
                    );
                }

                ultimo.remove();

                totalPessoas = Math.max(
                    0,
                    totalPessoas - 1
                );

                totalPessoasElemento.textContent =
                    totalPessoas;
            }
        }

        conectarWebSocket();
    </script>
</body>
</html>
------------------------------------------------------------
ARQUIVO: app/static/index_backup_antes_contexto_cena.html
------------------------------------------------------------
<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">

    <meta
        name="viewport"
        content="width=device-width, initial-scale=1.0"
    >

    <title>Painel de Eventos</title>

    <style>
        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            font-family: Arial, sans-serif;
            background: #10131a;
            color: #f3f4f6;
        }

        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px 30px;
            background: #191e29;
            border-bottom: 1px solid #2d3443;
        }

        h1 {
            margin: 0;
            font-size: 24px;
        }

        .status-conexao {
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .status-bolinha {
            width: 12px;
            height: 12px;
            border-radius: 50%;
            background: #f59e0b;
        }

        .status-bolinha.conectado {
            background: #22c55e;
        }

        .status-bolinha.desconectado {
            background: #ef4444;
        }

        main {
            padding: 30px;
        }

        .resumo {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
            margin-bottom: 25px;
        }

        .resumo-card {
            min-width: 180px;
            padding: 18px;
            background: #191e29;
            border: 1px solid #2d3443;
            border-radius: 10px;
        }

        .resumo-card span {
            display: block;
            color: #9ca3af;
            font-size: 14px;
        }

        .resumo-card strong {
            display: block;
            margin-top: 8px;
            font-size: 28px;
        }

        #eventos {
            display: grid;
            grid-template-columns: repeat(
                auto-fill,
                minmax(310px, 1fr)
            );
            gap: 20px;
        }

        .evento-card {
            overflow: hidden;
            background: #191e29;
            border: 1px solid #2d3443;
            border-radius: 10px;
            transition:
                border-color 0.2s,
                transform 0.2s,
                opacity 0.2s;
        }

        .evento-card.atualizado {
            border-color: #3b82f6;
            transform: translateY(-2px);
        }

        .evento-card.saiu {
            opacity: 0.55;
            border-color: #6b7280;
        }

        .evento-card img {
            width: 100%;
            height: 230px;
            object-fit: contain;
            background: #05070a;
        }

        .evento-dados {
            padding: 15px;
        }

        .evento-cabecalho {
            display: flex;
            justify-content: space-between;
            align-items: center;
            gap: 10px;
            margin-bottom: 12px;
        }

        .evento-cabecalho h2 {
            margin: 0;
            font-size: 18px;
        }

        .evento-status {
            padding: 5px 9px;
            border-radius: 999px;
            background: #374151;
            font-size: 12px;
            font-weight: bold;
        }

        .evento-status.entered {
            background: #166534;
            color: #dcfce7;
        }

        .evento-status.updated {
            background: #1e40af;
            color: #dbeafe;
        }

        .evento-status.exited {
            background: #4b5563;
            color: #f3f4f6;
        }

        .evento-dados p {
            margin: 6px 0;
            color: #cbd5e1;
            font-size: 14px;
        }

        .aparencia-visual {
            display: none;
            margin-top: 14px;
            padding-top: 12px;
            border-top: 1px solid #2d3443;
        }

        .aparencia-titulo {
            margin: 0 0 10px;
            font-size: 14px;
            color: #93c5fd;
        }

        .descricao {
            line-height: 1.45;
        }

        .cor-amostra {
            display: inline-block;
            width: 12px;
            height: 12px;
            margin-right: 6px;
            border: 1px solid #64748b;
            border-radius: 50%;
            vertical-align: -1px;
        }

        .vazio {
            padding: 40px;
            color: #9ca3af;
            text-align: center;
            background: #191e29;
            border: 1px dashed #374151;
            border-radius: 10px;
        }

        @media (max-width: 700px) {
            header {
                align-items: flex-start;
                flex-direction: column;
                gap: 15px;
                padding: 20px;
            }

            main {
                padding: 20px;
            }

            .resumo-card {
                flex: 1;
                min-width: 140px;
            }
        }
    </style>
</head>

<body>
    <header>
        <h1>Painel de Eventos das Câmeras</h1>

        <div class="status-conexao">
            <div
                id="status-bolinha"
                class="status-bolinha"
            ></div>

            <span id="status-texto">
                Conectando...
            </span>
        </div>
    </header>

    <main>
        <section class="resumo">
            <div class="resumo-card">
                <span>Pessoas ativas</span>
                <strong id="pessoas-ativas">0</strong>
            </div>

            <div class="resumo-card">
                <span>Pessoas que saíram</span>
                <strong id="pessoas-sairam">0</strong>
            </div>

            <div class="resumo-card">
                <span>Total exibido</span>
                <strong id="total-pessoas">0</strong>
            </div>

            <div class="resumo-card">
                <span>Eventos processados</span>
                <strong id="total-eventos">0</strong>
            </div>
        </section>

        <section id="eventos">
            <div
                id="mensagem-vazia"
                class="vazio"
            >
                Aguardando eventos das câmeras...
            </div>
        </section>
    </main>

    <script>
        const statusBolinha =
            document.getElementById(
                "status-bolinha"
            );

        const statusTexto =
            document.getElementById(
                "status-texto"
            );

        const pessoasAtivasElemento =
            document.getElementById(
                "pessoas-ativas"
            );

        const pessoasSairamElemento =
            document.getElementById(
                "pessoas-sairam"
            );

        const totalPessoasElemento =
            document.getElementById(
                "total-pessoas"
            );

        const totalEventosElemento =
            document.getElementById(
                "total-eventos"
            );

        const eventosElemento =
            document.getElementById(
                "eventos"
            );

        const cartoesPorPessoa =
            new Map();

        const statusPorPessoa =
            new Map();

        let pessoasAtivas = 0;
        let pessoasSairam = 0;
        let totalPessoas = 0;
        let totalEventos = 0;
        let heartbeatId = null;

        function traduzirStatus(status) {
            const traducoes = {
                entered: "Entrou",
                updated: "Atualizado",
                suppressed: "Repetido",
                exited: "Saiu"
            };

            return (
                traducoes[status]
                || status
                || "Detectado"
            );
        }

        function atualizarResumo() {
            pessoasAtivasElemento.textContent =
                pessoasAtivas;

            pessoasSairamElemento.textContent =
                pessoasSairam;

            totalPessoasElemento.textContent =
                totalPessoas;

            totalEventosElemento.textContent =
                totalEventos;
        }

        function registrarMudancaStatus(
            pessoaId,
            novoStatus
        ) {
            const statusAnterior =
                statusPorPessoa.get(
                    pessoaId
                );

            if (!statusAnterior) {
                totalPessoas += 1;

                if (novoStatus === "exited") {
                    pessoasSairam += 1;
                } else {
                    pessoasAtivas += 1;
                }
            }

            else if (
                statusAnterior !== "exited"
                && novoStatus === "exited"
            ) {
                pessoasAtivas = Math.max(
                    0,
                    pessoasAtivas - 1
                );

                pessoasSairam += 1;
            }

            else if (
                statusAnterior === "exited"
                && novoStatus !== "exited"
            ) {
                pessoasSairam = Math.max(
                    0,
                    pessoasSairam - 1
                );

                pessoasAtivas += 1;
            }

            statusPorPessoa.set(
                pessoaId,
                novoStatus
            );

            atualizarResumo();
        }

        function conectarWebSocket() {
            const protocolo =
                window.location.protocol === "https:"
                    ? "wss"
                    : "ws";

            const endereco =
                `${protocolo}://${window.location.host}/ws/eventos`;

            const websocket =
                new WebSocket(endereco);

            websocket.onopen = function () {
                statusTexto.textContent =
                    "Conectado";

                statusBolinha.className =
                    "status-bolinha conectado";

                if (heartbeatId) {
                    clearInterval(
                        heartbeatId
                    );
                }

                heartbeatId = setInterval(
                    function () {
                        if (
                            websocket.readyState
                            === WebSocket.OPEN
                        ) {
                            websocket.send(
                                "ping"
                            );
                        }
                    },
                    20000
                );
            };

            websocket.onmessage =
                function (mensagem) {
                    try {
                        const evento =
                            JSON.parse(
                                mensagem.data
                            );

                        processarEvento(evento);

                    } catch (erro) {
                        console.error(
                            "Erro ao interpretar evento:",
                            erro
                        );
                    }
                };

            websocket.onclose = function () {
                statusTexto.textContent =
                    "Desconectado — reconectando...";

                statusBolinha.className =
                    "status-bolinha desconectado";

                if (heartbeatId) {
                    clearInterval(
                        heartbeatId
                    );

                    heartbeatId = null;
                }

                setTimeout(
                    conectarWebSocket,
                    2000
                );
            };

            websocket.onerror = function () {
                websocket.close();
            };
        }

        function processarEvento(evento) {
            /*
             * Atualização parcial gerada por uma
             * captura que foi suprimida.
             *
             * Não altera os contadores e não muda
             * o status Entrou, Atualizado ou Saiu.
             */
            if (
                evento.status
                === "appearance_updated"
            ) {
                atualizarSomenteAparencia(
                    evento
                );

                return;
            }

            if (
                evento.status
                === "suppressed"
            ) {
                return;
            }

            const pessoaId =
                evento.pessoa_id
                || evento.evento_id;

            if (!pessoaId) {
                console.warn(
                    "Evento sem identificador",
                    evento
                );

                return;
            }

            const cartaoExistente =
                cartoesPorPessoa.get(
                    pessoaId
                );

            if (
                evento.status === "exited"
                && !cartaoExistente
            ) {
                return;
            }

            registrarMudancaStatus(
                pessoaId,
                evento.status || "entered"
            );

            totalEventos += 1;
            atualizarResumo();

            if (cartaoExistente) {
                atualizarCartao(
                    cartaoExistente,
                    evento
                );

                return;
            }

            criarCartao(
                pessoaId,
                evento
            );
        }

        function atualizarSomenteAparencia(
            evento
        ) {
            const pessoaId =
                evento.pessoa_id
                || evento.evento_id;

            const card =
                cartoesPorPessoa.get(
                    pessoaId
                );

            /*
             * Não cria um cartão novo quando o painel
             * não recebeu o evento inicial da pessoa.
             */
            if (!card) {
                console.warn(
                    "Atualização recebida sem cartão:",
                    pessoaId
                );

                return;
            }

            const quantidade =
                card.querySelector(
                    '[data-campo="quantidade"]'
                );

            const aparenciaVisual =
                card.querySelector(
                    '[data-campo="aparencia"]'
                );

            const corRoupa =
                card.querySelector(
                    '[data-campo="cor-roupa"]'
                );

            const posicao =
                card.querySelector(
                    '[data-campo="posicao"]'
                );

            const tamanho =
                card.querySelector(
                    '[data-campo="tamanho"]'
                );

            const percentual =
                card.querySelector(
                    '[data-campo="percentual"]'
                );

            const quantidadeAmostras =
                card.querySelector(
                    '[data-campo="quantidade-amostras"]'
                );

            const descricao =
                card.querySelector(
                    '[data-campo="descricao"]'
                );

            quantidade.textContent =
                `Detecções: ${
                    evento.quantidade_deteccoes
                    || 1
                }`;

            if (evento.aparencia) {
                aparenciaVisual.style.display =
                    "block";

                preencherAparencia(
                    evento.aparencia,
                    corRoupa,
                    posicao,
                    tamanho,
                    percentual,
                    quantidadeAmostras,
                    descricao
                );
            }

            /*
             * Faz o cartão piscar sem alterar
             * o texto atual do status.
             */
            card.classList.add(
                "atualizado"
            );

            setTimeout(
                function () {
                    card.classList.remove(
                        "atualizado"
                    );
                },
                1000
            );
        }

        function criarCartao(
            pessoaId,
            evento
        ) {
            const mensagemVazia =
                document.getElementById(
                    "mensagem-vazia"
                );

            if (mensagemVazia) {
                mensagemVazia.remove();
            }

            const card =
                document.createElement(
                    "article"
                );

            card.className =
                "evento-card";

            card.dataset.pessoaId =
                pessoaId;

            const imagem =
                document.createElement(
                    "img"
                );

            imagem.alt =
                "Recorte da pessoa";

            imagem.dataset.campo =
                "imagem";

            const dados =
                document.createElement(
                    "div"
                );

            dados.className =
                "evento-dados";

            const cabecalho =
                document.createElement(
                    "div"
                );

            cabecalho.className =
                "evento-cabecalho";

            const titulo =
                document.createElement(
                    "h2"
                );

            titulo.dataset.campo =
                "camera";

            const status =
                document.createElement(
                    "span"
                );

            status.dataset.campo =
                "status";

            status.className =
                "evento-status";

            const tipo =
                document.createElement(
                    "p"
                );

            tipo.dataset.campo =
                "tipo";

            const horario =
                document.createElement(
                    "p"
                );

            horario.dataset.campo =
                "horario";

            const identificadorPessoa =
                document.createElement(
                    "p"
                );

            identificadorPessoa.dataset.campo =
                "pessoa-id";

            const quantidade =
                document.createElement(
                    "p"
                );

            quantidade.dataset.campo =
                "quantidade";

            const aparencia =
                document.createElement(
                    "section"
                );

            aparencia.className =
                "aparencia-visual";

            aparencia.dataset.campo =
                "aparencia";

            const aparenciaTitulo =
                document.createElement(
                    "h3"
                );

            aparenciaTitulo.className =
                "aparencia-titulo";

            aparenciaTitulo.textContent =
                "Aparência estável";

            const corRoupa =
                document.createElement(
                    "p"
                );

            corRoupa.dataset.campo =
                "cor-roupa";

            const posicao =
                document.createElement(
                    "p"
                );

            posicao.dataset.campo =
                "posicao";

            const tamanho =
                document.createElement(
                    "p"
                );

            tamanho.dataset.campo =
                "tamanho";

            const percentual =
                document.createElement(
                    "p"
                );

            percentual.dataset.campo =
                "percentual";

            const quantidadeAmostras =
                document.createElement(
                    "p"
                );

            quantidadeAmostras.dataset.campo =
                "quantidade-amostras";

            const descricao =
                document.createElement(
                    "p"
                );

            descricao.dataset.campo =
                "descricao";

            descricao.className =
                "descricao";

            cabecalho.appendChild(titulo);
            cabecalho.appendChild(status);

            dados.appendChild(cabecalho);
            dados.appendChild(tipo);
            dados.appendChild(horario);

            dados.appendChild(
                identificadorPessoa
            );

            dados.appendChild(
                quantidade
            );

            aparencia.appendChild(
                aparenciaTitulo
            );

            aparencia.appendChild(
                corRoupa
            );

            aparencia.appendChild(
                posicao
            );

            aparencia.appendChild(
                tamanho
            );

            aparencia.appendChild(
                percentual
            );

            aparencia.appendChild(
                quantidadeAmostras
            );

            aparencia.appendChild(
                descricao
            );

            dados.appendChild(
                aparencia
            );

            card.appendChild(imagem);
            card.appendChild(dados);

            eventosElemento.prepend(
                card
            );

            cartoesPorPessoa.set(
                pessoaId,
                card
            );

            atualizarCartao(
                card,
                evento
            );

            limitarQuantidadeCartoes();
        }

        function preencherAparencia(
            aparencia,
            corRoupa,
            posicao,
            tamanho,
            percentual,
            quantidadeAmostras,
            descricao
        ) {
            const rgb =
                aparencia.rgb_medio;

            let amostraCor = "";

            if (
                Array.isArray(rgb)
                && rgb.length === 3
            ) {
                amostraCor =
                    `<span
                        class="cor-amostra"
                        style="
                            background-color:
                            rgb(
                                ${rgb[0]},
                                ${rgb[1]},
                                ${rgb[2]}
                            );
                        "
                    ></span>`;
            }

            corRoupa.innerHTML =
                `${amostraCor}Cor predominante: ${
                    aparencia
                        .cor_roupa_predominante
                    || "-"
                }`;

            posicao.textContent =
                `Posição atual: ${
                    aparencia.posicao_atual
                    || "-"
                }`;

            tamanho.textContent =
                `Tamanho predominante: ${
                    aparencia
                        .tamanho_predominante
                    || "-"
                }`;

            percentual.textContent =
                `Área média ocupada: ${
                    aparencia
                        .percentual_medio_quadro
                    ?? "-"
                }%`;

            quantidadeAmostras.textContent =
                `Amostras analisadas: ${
                    aparencia
                        .quantidade_amostras
                    || 1
                }`;

            descricao.textContent =
                `Descrição estável: ${
                    aparencia.descricao
                    || "-"
                }`;
        }

        function atualizarCartao(
            card,
            evento
        ) {
            const imagem =
                card.querySelector(
                    '[data-campo="imagem"]'
                );

            const titulo =
                card.querySelector(
                    '[data-campo="camera"]'
                );

            const status =
                card.querySelector(
                    '[data-campo="status"]'
                );

            const tipo =
                card.querySelector(
                    '[data-campo="tipo"]'
                );

            const horario =
                card.querySelector(
                    '[data-campo="horario"]'
                );

            const pessoaId =
                card.querySelector(
                    '[data-campo="pessoa-id"]'
                );

            const quantidade =
                card.querySelector(
                    '[data-campo="quantidade"]'
                );

            const aparenciaVisual =
                card.querySelector(
                    '[data-campo="aparencia"]'
                );

            const corRoupa =
                card.querySelector(
                    '[data-campo="cor-roupa"]'
                );

            const posicao =
                card.querySelector(
                    '[data-campo="posicao"]'
                );

            const tamanho =
                card.querySelector(
                    '[data-campo="tamanho"]'
                );

            const percentual =
                card.querySelector(
                    '[data-campo="percentual"]'
                );

            const quantidadeAmostras =
                card.querySelector(
                    '[data-campo="quantidade-amostras"]'
                );

            const descricao =
                card.querySelector(
                    '[data-campo="descricao"]'
                );

            if (evento.imagem) {
                imagem.src =
                    `data:image/jpeg;base64,${evento.imagem}`;
            }

            if (evento.camera) {
                titulo.textContent =
                    evento.camera;
            }

            status.textContent =
                traduzirStatus(
                    evento.status
                );

            status.className =
                `evento-status ${evento.status || ""}`;

            if (evento.tipo) {
                tipo.textContent =
                    `Evento: ${evento.tipo}`;
            }

            if (
                evento.status === "exited"
            ) {
                horario.textContent =
                    `Saída: ${
                        evento.datetime || "-"
                    }`;
            } else {
                horario.textContent =
                    `Última captura: ${
                        evento.datetime || "-"
                    }`;
            }

            pessoaId.textContent =
                `Pessoa: ${
                    evento.pessoa_id || "-"
                }`;

            quantidade.textContent =
                `Detecções: ${
                    evento.quantidade_deteccoes
                    || 1
                }`;

            if (evento.aparencia) {
                aparenciaVisual.style.display =
                    "block";

                preencherAparencia(
                    evento.aparencia,
                    corRoupa,
                    posicao,
                    tamanho,
                    percentual,
                    quantidadeAmostras,
                    descricao
                );
            }

            card.classList.remove(
                "atualizado",
                "saiu"
            );

            if (
                evento.status === "updated"
            ) {
                card.classList.add(
                    "atualizado"
                );

                eventosElemento.prepend(
                    card
                );

                setTimeout(
                    function () {
                        card.classList.remove(
                            "atualizado"
                        );
                    },
                    1500
                );
            }

            if (
                evento.status === "exited"
            ) {
                card.classList.add(
                    "saiu"
                );
            }
        }

        function limitarQuantidadeCartoes() {
            while (
                eventosElemento.children.length
                > 50
            ) {
                const ultimo =
                    eventosElemento
                    .lastElementChild;

                if (!ultimo) {
                    break;
                }

                const pessoaId =
                    ultimo.dataset.pessoaId;

                if (pessoaId) {
                    const statusRemovido =
                        statusPorPessoa.get(
                            pessoaId
                        );

                    if (
                        statusRemovido
                        === "exited"
                    ) {
                        pessoasSairam =
                            Math.max(
                                0,
                                pessoasSairam - 1
                            );
                    }

                    else if (
                        statusRemovido
                    ) {
                        pessoasAtivas =
                            Math.max(
                                0,
                                pessoasAtivas - 1
                            );
                    }

                    totalPessoas =
                        Math.max(

------------------------------------------------------------
ARQUIVO: app/static/index_backup_antes_duas_imagens.html
------------------------------------------------------------
<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">

    <meta
        name="viewport"
        content="width=device-width, initial-scale=1.0"
    >

    <title>Painel de Eventos</title>

    <style>
        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            font-family: Arial, sans-serif;
            background: #10131a;
            color: #f3f4f6;
        }

        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px 30px;
            background: #191e29;
            border-bottom: 1px solid #2d3443;
        }

        h1 {
            margin: 0;
            font-size: 24px;
        }

        .status-conexao {
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .status-bolinha {
            width: 12px;
            height: 12px;
            border-radius: 50%;
            background: #f59e0b;
        }

        .status-bolinha.conectado {
            background: #22c55e;
        }

        .status-bolinha.desconectado {
            background: #ef4444;
        }

        main {
            padding: 30px;
        }

        .resumo {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
            margin-bottom: 25px;
        }

        .resumo-card {
            min-width: 180px;
            padding: 18px;
            background: #191e29;
            border: 1px solid #2d3443;
            border-radius: 10px;
        }

        .resumo-card span {
            display: block;
            color: #9ca3af;
            font-size: 14px;
        }

        .resumo-card strong {
            display: block;
            margin-top: 8px;
            font-size: 28px;
        }

        #eventos {
            display: grid;
            grid-template-columns: repeat(
                auto-fill,
                minmax(330px, 1fr)
            );
            gap: 20px;
        }

        .evento-card {
            overflow: hidden;
            background: #191e29;
            border: 1px solid #2d3443;
            border-radius: 10px;
            transition:
                border-color 0.2s,
                transform 0.2s,
                opacity 0.2s;
        }

        .evento-card.atualizado {
            border-color: #3b82f6;
            transform: translateY(-2px);
        }

        .evento-card.saiu {
            opacity: 0.55;
            border-color: #6b7280;
        }

        .evento-card img {
            width: 100%;
            height: 230px;
            object-fit: contain;
            background: #05070a;
        }

        .evento-dados {
            padding: 15px;
        }

        .evento-cabecalho {
            display: flex;
            justify-content: space-between;
            align-items: center;
            gap: 10px;
            margin-bottom: 12px;
        }

        .evento-cabecalho h2 {
            margin: 0;
            font-size: 18px;
        }

        .evento-status {
            padding: 5px 9px;
            border-radius: 999px;
            background: #374151;
            font-size: 12px;
            font-weight: bold;
        }

        .evento-status.entered {
            background: #166534;
            color: #dcfce7;
        }

        .evento-status.updated {
            background: #1e40af;
            color: #dbeafe;
        }

        .evento-status.exited {
            background: #4b5563;
            color: #f3f4f6;
        }

        .evento-dados p {
            margin: 6px 0;
            color: #cbd5e1;
            font-size: 14px;
        }

        .aparencia-visual,
        .contexto-cena {
            display: none;
            margin-top: 14px;
            padding-top: 12px;
            border-top: 1px solid #2d3443;
        }

        .secao-titulo {
            margin: 0 0 10px;
            font-size: 14px;
            color: #93c5fd;
        }

        .contexto-cena .secao-titulo {
            color: #86efac;
        }

        .descricao {
            line-height: 1.45;
        }

        .detalhes-pessoas {
            line-height: 1.5;
            white-space: pre-line;
        }

        .cor-amostra {
            display: inline-block;
            width: 12px;
            height: 12px;
            margin-right: 6px;
            border: 1px solid #64748b;
            border-radius: 50%;
            vertical-align: -1px;
        }

        .vazio {
            padding: 40px;
            color: #9ca3af;
            text-align: center;
            background: #191e29;
            border: 1px dashed #374151;
            border-radius: 10px;
        }

        @media (max-width: 700px) {
            header {
                align-items: flex-start;
                flex-direction: column;
                gap: 15px;
                padding: 20px;
            }

            main {
                padding: 20px;
            }

            .resumo-card {
                flex: 1;
                min-width: 140px;
            }
        }
    </style>
</head>

<body>
    <header>
        <h1>Painel de Eventos das Câmeras</h1>

        <div class="status-conexao">
            <div
                id="status-bolinha"
                class="status-bolinha"
            ></div>

            <span id="status-texto">
                Conectando...
            </span>
        </div>
    </header>

    <main>
        <section class="resumo">
            <div class="resumo-card">
                <span>Pessoas ativas</span>
                <strong id="pessoas-ativas">0</strong>
            </div>

            <div class="resumo-card">
                <span>Pessoas que saíram</span>
                <strong id="pessoas-sairam">0</strong>
            </div>

            <div class="resumo-card">
                <span>Total exibido</span>
                <strong id="total-pessoas">0</strong>
            </div>

            <div class="resumo-card">
                <span>Eventos processados</span>
                <strong id="total-eventos">0</strong>
            </div>
        </section>

        <section id="eventos">
            <div
                id="mensagem-vazia"
                class="vazio"
            >
                Aguardando eventos das câmeras...
            </div>
        </section>
    </main>

    <script>
        const statusBolinha =
            document.getElementById(
                "status-bolinha"
            );

        const statusTexto =
            document.getElementById(
                "status-texto"
            );

        const pessoasAtivasElemento =
            document.getElementById(
                "pessoas-ativas"
            );

        const pessoasSairamElemento =
            document.getElementById(
                "pessoas-sairam"
            );

        const totalPessoasElemento =
            document.getElementById(
                "total-pessoas"
            );

        const totalEventosElemento =
            document.getElementById(
                "total-eventos"
            );

        const eventosElemento =
            document.getElementById(
                "eventos"
            );

        const cartoesPorPessoa = new Map();
        const statusPorPessoa = new Map();

        let pessoasAtivas = 0;
        let pessoasSairam = 0;
        let totalPessoas = 0;
        let totalEventos = 0;
        let heartbeatId = null;

        function traduzirStatus(status) {
            const traducoes = {
                entered: "Entrou",
                updated: "Atualizado",
                suppressed: "Repetido",
                exited: "Saiu"
            };

            return (
                traducoes[status]
                || status
                || "Detectado"
            );
        }

        function atualizarResumo() {
            pessoasAtivasElemento.textContent =
                pessoasAtivas;

            pessoasSairamElemento.textContent =
                pessoasSairam;

            totalPessoasElemento.textContent =
                totalPessoas;

            totalEventosElemento.textContent =
                totalEventos;
        }

        function registrarMudancaStatus(
            pessoaId,
            novoStatus
        ) {
            const statusAnterior =
                statusPorPessoa.get(
                    pessoaId
                );

            if (!statusAnterior) {
                totalPessoas += 1;

                if (novoStatus === "exited") {
                    pessoasSairam += 1;
                } else {
                    pessoasAtivas += 1;
                }
            }

            else if (
                statusAnterior !== "exited"
                && novoStatus === "exited"
            ) {
                pessoasAtivas = Math.max(
                    0,
                    pessoasAtivas - 1
                );

                pessoasSairam += 1;
            }

            else if (
                statusAnterior === "exited"
                && novoStatus !== "exited"
            ) {
                pessoasSairam = Math.max(
                    0,
                    pessoasSairam - 1
                );

                pessoasAtivas += 1;
            }

            statusPorPessoa.set(
                pessoaId,
                novoStatus
            );

            atualizarResumo();
        }

        function conectarWebSocket() {
            const protocolo =
                window.location.protocol === "https:"
                    ? "wss"
                    : "ws";

            const endereco =
                `${protocolo}://${window.location.host}/ws/eventos`;

            const websocket =
                new WebSocket(endereco);

            websocket.onopen = function () {
                statusTexto.textContent =
                    "Conectado";

                statusBolinha.className =
                    "status-bolinha conectado";

                if (heartbeatId) {
                    clearInterval(
                        heartbeatId
                    );
                }

                heartbeatId = setInterval(
                    function () {
                        if (
                            websocket.readyState
                            === WebSocket.OPEN
                        ) {
                            websocket.send(
                                "ping"
                            );
                        }
                    },
                    20000
                );
            };

            websocket.onmessage =
                function (mensagem) {
                    try {
                        const evento =
                            JSON.parse(
                                mensagem.data
                            );

                        processarEvento(
                            evento
                        );

                    } catch (erro) {
                        console.error(
                            "Erro ao interpretar evento:",
                            erro
                        );
                    }
                };

            websocket.onclose = function () {
                statusTexto.textContent =
                    "Desconectado — reconectando...";

                statusBolinha.className =
                    "status-bolinha desconectado";

                if (heartbeatId) {
                    clearInterval(
                        heartbeatId
                    );

                    heartbeatId = null;
                }

                setTimeout(
                    conectarWebSocket,
                    2000
                );
            };

            websocket.onerror = function () {
                websocket.close();
            };
        }

        function processarEvento(evento) {
            /*
             * Atualização parcial gerada por uma
             * captura repetida e suprimida.
             *
             * Não aumenta os contadores e não muda
             * o status do cartão.
             */
            if (
                evento.status
                === "appearance_updated"
            ) {
                atualizarSomenteAparenciaEContexto(
                    evento
                );

                return;
            }

            if (
                evento.status
                === "suppressed"
            ) {
                return;
            }

            const pessoaId =
                evento.pessoa_id
                || evento.evento_id;

            if (!pessoaId) {
                console.warn(
                    "Evento sem identificador:",
                    evento
                );

                return;
            }

            const cartaoExistente =
                cartoesPorPessoa.get(
                    pessoaId
                );

            if (
                evento.status === "exited"
                && !cartaoExistente
            ) {
                return;
            }

            registrarMudancaStatus(
                pessoaId,
                evento.status || "entered"
            );

            totalEventos += 1;
            atualizarResumo();

            if (cartaoExistente) {
                atualizarCartao(
                    cartaoExistente,
                    evento
                );

                return;
            }

            criarCartao(
                pessoaId,
                evento
            );
        }

        function obterCamposCard(card) {
            return {
                imagem: card.querySelector(
                    '[data-campo="imagem"]'
                ),

                camera: card.querySelector(
                    '[data-campo="camera"]'
                ),

                status: card.querySelector(
                    '[data-campo="status"]'
                ),

                tipo: card.querySelector(
                    '[data-campo="tipo"]'
                ),

                horario: card.querySelector(
                    '[data-campo="horario"]'
                ),

                pessoaId: card.querySelector(
                    '[data-campo="pessoa-id"]'
                ),

                quantidade: card.querySelector(
                    '[data-campo="quantidade"]'
                ),

                aparencia: card.querySelector(
                    '[data-campo="aparencia"]'
                ),

                corRoupa: card.querySelector(
                    '[data-campo="cor-roupa"]'
                ),

                posicao: card.querySelector(
                    '[data-campo="posicao"]'
                ),

                tamanho: card.querySelector(
                    '[data-campo="tamanho"]'
                ),

                percentual: card.querySelector(
                    '[data-campo="percentual"]'
                ),

                quantidadeAmostras: card.querySelector(
                    '[data-campo="quantidade-amostras"]'
                ),

                descricaoAparencia: card.querySelector(
                    '[data-campo="descricao-aparencia"]'
                ),

                contexto: card.querySelector(
                    '[data-campo="contexto"]'
                ),

                contextoQuantidade: card.querySelector(
                    '[data-campo="contexto-quantidade"]'
                ),

                contextoDistribuicao: card.querySelector(
                    '[data-campo="contexto-distribuicao"]'
                ),

                contextoProximidades: card.querySelector(
                    '[data-campo="contexto-proximidades"]'
                ),

                contextoPessoas: card.querySelector(
                    '[data-campo="contexto-pessoas"]'
                ),

                contextoDescricao: card.querySelector(
                    '[data-campo="contexto-descricao"]'
                )
            };
        }

        function atualizarSomenteAparenciaEContexto(
            evento
        ) {
            const pessoaId =
                evento.pessoa_id
                || evento.evento_id;

            const card =
                cartoesPorPessoa.get(
                    pessoaId
                );

            if (!card) {
                console.warn(
                    "Atualização recebida sem cartão:",
                    pessoaId
                );

                return;
            }

            const campos =
                obterCamposCard(card);

            campos.quantidade.textContent =
                `Detecções: ${
                    evento.quantidade_deteccoes
                    || 1
                }`;

            if (evento.aparencia) {
                preencherAparencia(
                    evento.aparencia,
                    campos
                );
            }

            if (evento.contexto_cena) {
                preencherContextoCena(
                    evento.contexto_cena,
                    campos
                );
            }

            card.classList.add(
                "atualizado"
            );

            setTimeout(
                function () {
                    card.classList.remove(
                        "atualizado"
                    );
                },
                1000
            );
        }

        function criarElemento(
            tag,
            campo,
            classe = ""
        ) {
            const elemento =
                document.createElement(tag);

            if (campo) {
                elemento.dataset.campo =
                    campo;
            }

            if (classe) {
                elemento.className =
                    classe;
            }

            return elemento;
        }

        function criarCartao(
            pessoaId,
            evento
        ) {
            const mensagemVazia =
                document.getElementById(
                    "mensagem-vazia"
                );

            if (mensagemVazia) {
                mensagemVazia.remove();
            }

            const card =
                criarElemento(
                    "article",
                    "",
                    "evento-card"
                );

            card.dataset.pessoaId =
                pessoaId;

            const imagem =
                criarElemento(
                    "img",
                    "imagem"
                );

            imagem.alt =
                "Recorte da pessoa";

            const dados =
                criarElemento(
                    "div",
                    "",
                    "evento-dados"
                );

            const cabecalho =
                criarElemento(
                    "div",
                    "",
                    "evento-cabecalho"
                );

            const titulo =
                criarElemento(
                    "h2",
                    "camera"
                );

            const status =
                criarElemento(
                    "span",
                    "status",
                    "evento-status"
                );

            cabecalho.appendChild(
                titulo
            );

            cabecalho.appendChild(
                status
            );

            const tipo =
                criarElemento(
                    "p",
                    "tipo"
                );

            const horario =
                criarElemento(
                    "p",
                    "horario"
                );

            const identificadorPessoa =
                criarElemento(
                    "p",
                    "pessoa-id"
                );

            const quantidade =
                criarElemento(
                    "p",
                    "quantidade"
                );

            const aparencia =
                criarElemento(
                    "section",
                    "aparencia",
                    "aparencia-visual"
                );

            const aparenciaTitulo =
                criarElemento(
                    "h3",
                    "",
                    "secao-titulo"
                );

            aparenciaTitulo.textContent =
                "Aparência estável";

            const corRoupa =
                criarElemento(
                    "p",
                    "cor-roupa"
                );

            const posicao =
                criarElemento(
                    "p",
                    "posicao"
                );

            const tamanho =
                criarElemento(
                    "p",
                    "tamanho"
                );

            const percentual =
                criarElemento(
                    "p",
                    "percentual"
                );

            const quantidadeAmostras =
                criarElemento(
                    "p",
                    "quantidade-amostras"
                );

            const descricaoAparencia =
                criarElemento(
                    "p",
                    "descricao-aparencia",
                    "descricao"
                );

            aparencia.appendChild(
                aparenciaTitulo
            );

            aparencia.appendChild(
                corRoupa
            );

            aparencia.appendChild(
                posicao
            );

            aparencia.appendChild(
                tamanho
            );

            aparencia.appendChild(
                percentual
            );

            aparencia.appendChild(
                quantidadeAmostras
            );

            aparencia.appendChild(
                descricaoAparencia
            );

            const contexto =
                criarElemento(
                    "section",
                    "contexto",
                    "contexto-cena"
                );

            const contextoTitulo =
                criarElemento(
                    "h3",
                    "",
                    "secao-titulo"
                );

            contextoTitulo.textContent =
                "Contexto da cena";

            const contextoQuantidade =
                criarElemento(
                    "p",
                    "contexto-quantidade"
                );

            const contextoDistribuicao =
                criarElemento(
                    "p",
                    "contexto-distribuicao"
                );

            const contextoProximidades =
                criarElemento(
                    "p",
                    "contexto-proximidades"
                );

            const contextoPessoas =
                criarElemento(
                    "p",
                    "contexto-pessoas",
                    "detalhes-pessoas"
                );

            const contextoDescricao =
                criarElemento(
                    "p",
                    "contexto-descricao",
                    "descricao"
                );

            contexto.appendChild(
                contextoTitulo
            );

            contexto.appendChild(
                contextoQuantidade
            );

            contexto.appendChild(
                contextoDistribuicao
            );

            contexto.appendChild(
                contextoProximidades
            );

            contexto.appendChild(
                contextoPessoas
            );

            contexto.appendChild(
                contextoDescricao
            );

            dados.appendChild(
                cabecalho
            );

            dados.appendChild(
                tipo
            );

            dados.appendChild(
                horario
            );

            dados.appendChild(
                identificadorPessoa
            );

            dados.appendChild(
                quantidade
            );

            dados.appendChild(
                aparencia
            );

            dados.appendChild(
                contexto
            );

            card.appendChild(
                imagem
            );

            card.appendChild(
                dados
            );

            eventosElemento.prepend(
                card
            );

            cartoesPorPessoa.set(
                pessoaId,
                card
            );

            atualizarCartao(
                card,
                evento
            );

            limitarQuantidadeCartoes();
        }

        function preencherAparencia(
            aparencia,
            campos
        ) {
            campos.aparencia.style.display =
                "block";

            const rgb =
                aparencia.rgb_medio;

            let amostraCor = "";

            if (
                Array.isArray(rgb)
                && rgb.length === 3
            ) {
                amostraCor =
                    `<span
                        class="cor-amostra"
                        style="
                            background-color:
                            rgb(
                                ${rgb[0]},
                                ${rgb[1]},
                                ${rgb[2]}
                            );
                        "
                    ></span>`;
            }

            campos.corRoupa.innerHTML =
                `${amostraCor}Cor predominante: ${
                    aparencia
                        .cor_roupa_predominante
                    || "-"
                }`;

            campos.posicao.textContent =
                `Posição atual: ${
                    aparencia.posicao_atual
                    || "-"
                }`;

            campos.tamanho.textContent =
                `Tamanho predominante: ${
                    aparencia
                        .tamanho_predominante
                    || "-"
                }`;

            campos.percentual.textContent =
                `Área média ocupada: ${
                    aparencia
                        .percentual_medio_quadro
                    ?? "-"
                }%`;

            campos.quantidadeAmostras.textContent =
                `Amostras analisadas: ${
                    aparencia
                        .quantidade_amostras
                    || 1
                }`;

            campos.descricaoAparencia.textContent =
                `Descrição estável: ${
                    aparencia.descricao
                    || "-"
                }`;
        }

        function montarDistribuicao(
            contexto
        ) {
            const partes = [];

            if (contexto.pessoas_esquerda) {
                partes.push(
                    `${contexto.pessoas_esquerda} à esquerda`
                );
            }

            if (contexto.pessoas_centro) {
                partes.push(
                    `${contexto.pessoas_centro} no centro`
                );
            }

            if (contexto.pessoas_direita) {
                partes.push(
                    `${contexto.pessoas_direita} à direita`
                );
            }

            if (partes.length === 0) {
                return "sem distribuição";
            }

            return partes.join(", ");
        }

        function preencherContextoCena(
            contexto,
            campos
        ) {
            campos.contexto.style.display =
                "block";

            campos.contextoQuantidade.textContent =
                `Pessoas identificadas: ${
                    contexto.quantidade_pessoas
                    ?? 0
                }`;

            campos.contextoDistribuicao.textContent =
                `Distribuição: ${
                    montarDistribuicao(
                        contexto
                    )
                }`;

            campos.contextoProximidades.textContent =
                `Pares muito próximos: ${
                    contexto.pares_muito_proximos
                    ?? 0
                } | Pares próximos: ${
                    contexto.pares_proximos
                    ?? 0
                } | Pares separados: ${
                    contexto.pares_separados
                    ?? 0
                }`;

            const pessoas =
                Array.isArray(contexto.pessoas)
                    ? contexto.pessoas
                    : [];

            if (pessoas.length > 0) {
                const detalhes =
                    pessoas
                    .slice(0, 8)
                    .map(
                        function (pessoa) {
                            return (
                                `Pessoa ${pessoa.indice}: `
                                + `${pessoa.posicao_horizontal || "-"}, `
                                + `${pessoa.posicao_vertical || "-"}, `
                                + `tamanho ${pessoa.tamanho_no_quadro || "-"}, `
                                + `${pessoa.percentual_quadro ?? "-"}% do quadro`
                            );
                        }
                    );

                if (pessoas.length > 8) {
                    detalhes.push(
                        `E mais ${
                            pessoas.length - 8
                        } pessoa(s).`
                    );
                }

                campos.contextoPessoas.textContent =
                    "Detalhes:\n"
                    + detalhes.join("\n");

------------------------------------------------------------
ARQUIVO: app/static/index_backup_antes_movimento.html
------------------------------------------------------------
<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">

    <meta
        name="viewport"
        content="width=device-width, initial-scale=1.0"
    >

    <title>Painel de Eventos</title>

    <style>
        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            font-family: Arial, sans-serif;
            background: #10131a;
            color: #f3f4f6;
        }

        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px 30px;
            background: #191e29;
            border-bottom: 1px solid #2d3443;
        }

        h1 {
            margin: 0;
            font-size: 24px;
        }

        .status-conexao {
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .status-bolinha {
            width: 12px;
            height: 12px;
            border-radius: 50%;
            background: #f59e0b;
        }

        .status-bolinha.conectado {
            background: #22c55e;
        }

        .status-bolinha.desconectado {
            background: #ef4444;
        }

        main {
            padding: 30px;
        }

        .resumo {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
            margin-bottom: 25px;
        }

        .resumo-card {
            min-width: 180px;
            padding: 18px;
            background: #191e29;
            border: 1px solid #2d3443;
            border-radius: 10px;
        }

        .resumo-card span {
            display: block;
            color: #9ca3af;
            font-size: 14px;
        }

        .resumo-card strong {
            display: block;
            margin-top: 8px;
            font-size: 28px;
        }

        #eventos {
            display: grid;
            grid-template-columns: repeat(
                auto-fill,
                minmax(400px, 1fr)
            );
            gap: 20px;
        }

        .evento-card {
            overflow: hidden;
            background: #191e29;
            border: 1px solid #2d3443;
            border-radius: 10px;
            transition:
                border-color 0.2s,
                transform 0.2s,
                opacity 0.2s;
        }

        .evento-card.atualizado {
            border-color: #3b82f6;
            transform: translateY(-2px);
        }

        .evento-card.saiu {
            opacity: 0.55;
            border-color: #6b7280;
        }

        .imagens-evento {
            display: flex;
            flex-direction: column;
            background: #05070a;
        }

        .imagem-bloco {
            display: none;
            border-bottom: 1px solid #2d3443;
        }

        .imagem-bloco:last-child {
            border-bottom: none;
        }

        .imagem-titulo {
            display: flex;
            justify-content: space-between;
            align-items: center;
            min-height: 38px;
            padding: 9px 12px;
            background: #111827;
            color: #cbd5e1;
            font-size: 13px;
            font-weight: bold;
        }

        .imagem-contagem {
            color: #86efac;
            font-size: 12px;
        }

        .imagem-evento {
            display: block;
            width: 100%;
            background: #05070a;
        }

        .imagem-recorte {
            height: 260px;
            object-fit: contain;
        }

        .imagem-cena {
            aspect-ratio: 16 / 9;
            max-height: 440px;
            object-fit: contain;
        }

        .evento-dados {
            padding: 15px;
        }

        .evento-cabecalho {
            display: flex;
            justify-content: space-between;
            align-items: center;
            gap: 10px;
            margin-bottom: 12px;
        }

        .evento-cabecalho h2 {
            margin: 0;
            font-size: 18px;
        }

        .evento-status {
            padding: 5px 9px;
            border-radius: 999px;
            background: #374151;
            font-size: 12px;
            font-weight: bold;
        }

        .evento-status.entered {
            background: #166534;
            color: #dcfce7;
        }

        .evento-status.updated {
            background: #1e40af;
            color: #dbeafe;
        }

        .evento-status.exited {
            background: #4b5563;
            color: #f3f4f6;
        }

        .evento-dados p {
            margin: 6px 0;
            color: #cbd5e1;
            font-size: 14px;
        }

        .aparencia-visual,
        .contexto-cena {
            display: none;
            margin-top: 14px;
            padding-top: 12px;
            border-top: 1px solid #2d3443;
        }

        .secao-titulo {
            margin: 0 0 10px;
            font-size: 14px;
            color: #93c5fd;
        }

        .contexto-cena .secao-titulo {
            color: #86efac;
        }

        .descricao {
            line-height: 1.45;
        }

        .detalhes-pessoas {
            line-height: 1.5;
            white-space: pre-line;
        }

        .cor-amostra {
            display: inline-block;
            width: 12px;
            height: 12px;
            margin-right: 6px;
            border: 1px solid #64748b;
            border-radius: 50%;
            vertical-align: -1px;
        }

        .vazio {
            padding: 40px;
            color: #9ca3af;
            text-align: center;
            background: #191e29;
            border: 1px dashed #374151;
            border-radius: 10px;
        }

        @media (max-width: 700px) {
            header {
                align-items: flex-start;
                flex-direction: column;
                gap: 15px;
                padding: 20px;
            }

            main {
                padding: 20px;
            }

            #eventos {
                grid-template-columns: 1fr;
            }

            .resumo-card {
                flex: 1;
                min-width: 140px;
            }

            .imagem-recorte {
                height: 230px;
            }
        }
    </style>
</head>

<body>
    <header>
        <h1>Painel de Eventos das Câmeras</h1>

        <div class="status-conexao">
            <div
                id="status-bolinha"
                class="status-bolinha"
            ></div>

            <span id="status-texto">
                Conectando...
            </span>
        </div>
    </header>

    <main>
        <section class="resumo">
            <div class="resumo-card">
                <span>Pessoas ativas</span>
                <strong id="pessoas-ativas">0</strong>
            </div>

            <div class="resumo-card">
                <span>Pessoas que saíram</span>
                <strong id="pessoas-sairam">0</strong>
            </div>

            <div class="resumo-card">
                <span>Total exibido</span>
                <strong id="total-pessoas">0</strong>
            </div>

            <div class="resumo-card">
                <span>Eventos processados</span>
                <strong id="total-eventos">0</strong>
            </div>
        </section>

        <section id="eventos">
            <div
                id="mensagem-vazia"
                class="vazio"
            >
                Aguardando eventos das câmeras...
            </div>
        </section>
    </main>

    <script>
        const statusBolinha =
            document.getElementById(
                "status-bolinha"
            );

        const statusTexto =
            document.getElementById(
                "status-texto"
            );

        const pessoasAtivasElemento =
            document.getElementById(
                "pessoas-ativas"
            );

        const pessoasSairamElemento =
            document.getElementById(
                "pessoas-sairam"
            );

        const totalPessoasElemento =
            document.getElementById(
                "total-pessoas"
            );

        const totalEventosElemento =
            document.getElementById(
                "total-eventos"
            );

        const eventosElemento =
            document.getElementById(
                "eventos"
            );

        const cartoesPorPessoa =
            new Map();

        const statusPorPessoa =
            new Map();

        let pessoasAtivas = 0;
        let pessoasSairam = 0;
        let totalPessoas = 0;
        let totalEventos = 0;
        let heartbeatId = null;

        function traduzirStatus(status) {
            const traducoes = {
                entered: "Entrou",
                updated: "Atualizado",
                suppressed: "Repetido",
                exited: "Saiu"
            };

            return (
                traducoes[status]
                || status
                || "Detectado"
            );
        }

        function atualizarResumo() {
            pessoasAtivasElemento.textContent =
                pessoasAtivas;

            pessoasSairamElemento.textContent =
                pessoasSairam;

            totalPessoasElemento.textContent =
                totalPessoas;

            totalEventosElemento.textContent =
                totalEventos;
        }

        function registrarMudancaStatus(
            pessoaId,
            novoStatus
        ) {
            const statusAnterior =
                statusPorPessoa.get(
                    pessoaId
                );

            if (!statusAnterior) {
                totalPessoas += 1;

                if (novoStatus === "exited") {
                    pessoasSairam += 1;
                } else {
                    pessoasAtivas += 1;
                }
            }

            else if (
                statusAnterior !== "exited"
                && novoStatus === "exited"
            ) {
                pessoasAtivas = Math.max(
                    0,
                    pessoasAtivas - 1
                );

                pessoasSairam += 1;
            }

            else if (
                statusAnterior === "exited"
                && novoStatus !== "exited"
            ) {
                pessoasSairam = Math.max(
                    0,
                    pessoasSairam - 1
                );

                pessoasAtivas += 1;
            }

            statusPorPessoa.set(
                pessoaId,
                novoStatus
            );

            atualizarResumo();
        }

        function conectarWebSocket() {
            const protocolo =
                window.location.protocol === "https:"
                    ? "wss"
                    : "ws";

            const endereco =
                `${protocolo}://${window.location.host}/ws/eventos`;

            const websocket =
                new WebSocket(endereco);

            websocket.onopen = function () {
                statusTexto.textContent =
                    "Conectado";

                statusBolinha.className =
                    "status-bolinha conectado";

                if (heartbeatId) {
                    clearInterval(
                        heartbeatId
                    );
                }

                heartbeatId = setInterval(
                    function () {
                        if (
                            websocket.readyState
                            === WebSocket.OPEN
                        ) {
                            websocket.send(
                                "ping"
                            );
                        }
                    },
                    20000
                );
            };

            websocket.onmessage =
                function (mensagem) {
                    try {
                        const evento =
                            JSON.parse(
                                mensagem.data
                            );

                        processarEvento(
                            evento
                        );

                    } catch (erro) {
                        console.error(
                            "Erro ao interpretar evento:",
                            erro
                        );
                    }
                };

            websocket.onclose = function () {
                statusTexto.textContent =
                    "Desconectado — reconectando...";

                statusBolinha.className =
                    "status-bolinha desconectado";

                if (heartbeatId) {
                    clearInterval(
                        heartbeatId
                    );

                    heartbeatId = null;
                }

                setTimeout(
                    conectarWebSocket,
                    2000
                );
            };

            websocket.onerror = function () {
                websocket.close();
            };
        }

        function processarEvento(evento) {
            if (
                evento.status
                === "appearance_updated"
            ) {
                atualizarEventoParcial(
                    evento
                );

                return;
            }

            if (
                evento.status
                === "suppressed"
            ) {
                return;
            }

            const pessoaId =
                evento.pessoa_id
                || evento.evento_id;

            if (!pessoaId) {
                console.warn(
                    "Evento sem identificador:",
                    evento
                );

                return;
            }

            const cartaoExistente =
                cartoesPorPessoa.get(
                    pessoaId
                );

            if (
                evento.status === "exited"
                && !cartaoExistente
            ) {
                return;
            }

            registrarMudancaStatus(
                pessoaId,
                evento.status || "entered"
            );

            totalEventos += 1;
            atualizarResumo();

            if (cartaoExistente) {
                atualizarCartao(
                    cartaoExistente,
                    evento
                );

                return;
            }

            criarCartao(
                pessoaId,
                evento
            );
        }

        function criarElemento(
            tag,
            campo,
            classe = ""
        ) {
            const elemento =
                document.createElement(
                    tag
                );

            if (campo) {
                elemento.dataset.campo =
                    campo;
            }

            if (classe) {
                elemento.className =
                    classe;
            }

            return elemento;
        }

        function obterCamposCard(card) {
            return {
                blocoRecorte: card.querySelector(
                    '[data-campo="bloco-recorte"]'
                ),

                imagemRecorte: card.querySelector(
                    '[data-campo="imagem-recorte"]'
                ),

                blocoCena: card.querySelector(
                    '[data-campo="bloco-cena"]'
                ),

                tituloCena: card.querySelector(
                    '[data-campo="titulo-cena"]'
                ),

                quantidadeBoxes: card.querySelector(
                    '[data-campo="quantidade-boxes"]'
                ),

                imagemCena: card.querySelector(
                    '[data-campo="imagem-cena"]'
                ),

                camera: card.querySelector(
                    '[data-campo="camera"]'
                ),

                status: card.querySelector(
                    '[data-campo="status"]'
                ),

                tipo: card.querySelector(
                    '[data-campo="tipo"]'
                ),

                horario: card.querySelector(
                    '[data-campo="horario"]'
                ),

                pessoaId: card.querySelector(
                    '[data-campo="pessoa-id"]'
                ),

                quantidade: card.querySelector(
                    '[data-campo="quantidade"]'
                ),

                aparencia: card.querySelector(
                    '[data-campo="aparencia"]'
                ),

                corRoupa: card.querySelector(
                    '[data-campo="cor-roupa"]'
                ),

                posicao: card.querySelector(
                    '[data-campo="posicao"]'
                ),

                tamanho: card.querySelector(
                    '[data-campo="tamanho"]'
                ),

                percentual: card.querySelector(
                    '[data-campo="percentual"]'
                ),

                quantidadeAmostras: card.querySelector(
                    '[data-campo="quantidade-amostras"]'
                ),

                descricaoAparencia: card.querySelector(
                    '[data-campo="descricao-aparencia"]'
                ),

                contexto: card.querySelector(
                    '[data-campo="contexto"]'
                ),

                contextoQuantidade: card.querySelector(
                    '[data-campo="contexto-quantidade"]'
                ),

                contextoDistribuicao: card.querySelector(
                    '[data-campo="contexto-distribuicao"]'
                ),

                contextoProximidades: card.querySelector(
                    '[data-campo="contexto-proximidades"]'
                ),

                contextoPessoas: card.querySelector(
                    '[data-campo="contexto-pessoas"]'
                ),

                contextoDescricao: card.querySelector(
                    '[data-campo="contexto-descricao"]'
                )
            };
        }

        function atualizarEventoParcial(
            evento
        ) {
            const pessoaId =
                evento.pessoa_id
                || evento.evento_id;

            const card =
                cartoesPorPessoa.get(
                    pessoaId
                );

            if (!card) {
                console.warn(
                    "Atualização recebida sem cartão:",
                    pessoaId
                );

                return;
            }

            const campos =
                obterCamposCard(card);

            campos.quantidade.textContent =
                `Detecções: ${
                    evento.quantidade_deteccoes
                    || 1
                }`;

            if (evento.aparencia) {
                preencherAparencia(
                    evento.aparencia,
                    campos
                );
            }

            if (evento.contexto_cena) {
                preencherContextoCena(
                    evento.contexto_cena,
                    campos
                );
            }

            if (evento.imagem_cena) {
                preencherCenaCompleta(
                    evento.imagem_cena,
                    evento.quantidade_boxes_cena,
                    campos
                );
            }

            card.classList.add(
                "atualizado"
            );

            setTimeout(
                function () {
                    card.classList.remove(
                        "atualizado"
                    );
                },
                1000
            );
        }

        function criarCartao(
            pessoaId,
            evento
        ) {
            const mensagemVazia =
                document.getElementById(
                    "mensagem-vazia"
                );

            if (mensagemVazia) {
                mensagemVazia.remove();
            }

            const card =
                criarElemento(
                    "article",
                    "",
                    "evento-card"
                );

            card.dataset.pessoaId =
                pessoaId;

            const imagensEvento =
                criarElemento(
                    "section",
                    "",
                    "imagens-evento"
                );

            const blocoRecorte =
                criarElemento(
                    "div",
                    "bloco-recorte",
                    "imagem-bloco"
                );

            const tituloRecorte =
                criarElemento(
                    "div",
                    "",
                    "imagem-titulo"
                );

            tituloRecorte.textContent =
                "Recorte individual";

            const imagemRecorte =
                criarElemento(
                    "img",
                    "imagem-recorte",
                    "imagem-evento imagem-recorte"
                );

            imagemRecorte.alt =
                "Recorte individual da pessoa";

            blocoRecorte.appendChild(
                tituloRecorte
            );

            blocoRecorte.appendChild(
                imagemRecorte
            );

            const blocoCena =
                criarElemento(
                    "div",
                    "bloco-cena",
                    "imagem-bloco"
                );

            const cabecalhoCena =
                criarElemento(
                    "div",
                    "",
                    "imagem-titulo"
                );

            const tituloCena =
                criarElemento(
                    "span",
                    "titulo-cena"
                );

            tituloCena.textContent =
                "Cena completa";

            const quantidadeBoxes =
                criarElemento(
                    "span",
                    "quantidade-boxes",
                    "imagem-contagem"
                );

            quantidadeBoxes.textContent =
                "0 detecções";

            cabecalhoCena.appendChild(
                tituloCena
            );

            cabecalhoCena.appendChild(
                quantidadeBoxes
            );

            const imagemCena =
                criarElemento(
                    "img",
                    "imagem-cena",
                    "imagem-evento imagem-cena"
                );

            imagemCena.alt =
                "Cena completa com as detecções";

            blocoCena.appendChild(
                cabecalhoCena
            );

            blocoCena.appendChild(
                imagemCena
            );

            imagensEvento.appendChild(
                blocoRecorte
            );

            imagensEvento.appendChild(
                blocoCena
            );

            const dados =
                criarElemento(
                    "div",
                    "",
                    "evento-dados"
                );

            const cabecalho =
                criarElemento(
                    "div",
                    "",
                    "evento-cabecalho"
                );

            const titulo =
                criarElemento(
                    "h2",
                    "camera"
                );

            const status =
                criarElemento(
                    "span",
                    "status",
                    "evento-status"
                );

            cabecalho.appendChild(
                titulo
            );

            cabecalho.appendChild(
                status
            );

            const tipo =
                criarElemento(
                    "p",
                    "tipo"
                );

            const horario =
                criarElemento(
                    "p",
                    "horario"
                );

            const identificadorPessoa =
                criarElemento(
                    "p",
                    "pessoa-id"
                );

            const quantidade =
                criarElemento(
                    "p",
                    "quantidade"
                );

            const aparencia =
                criarElemento(
                    "section",
                    "aparencia",
                    "aparencia-visual"
                );

            const aparenciaTitulo =
                criarElemento(
                    "h3",
                    "",
                    "secao-titulo"
                );

            aparenciaTitulo.textContent =
                "Aparência estável";

            const corRoupa =
                criarElemento(
                    "p",
                    "cor-roupa"
                );

            const posicao =
                criarElemento(
                    "p",
                    "posicao"
                );

            const tamanho =
                criarElemento(
                    "p",
                    "tamanho"
                );

            const percentual =
                criarElemento(
                    "p",
                    "percentual"
                );

            const quantidadeAmostras =
                criarElemento(
                    "p",
                    "quantidade-amostras"
                );

            const descricaoAparencia =
                criarElemento(
                    "p",
                    "descricao-aparencia",
                    "descricao"
                );

            aparencia.appendChild(
                aparenciaTitulo
            );

            aparencia.appendChild(
                corRoupa
            );

            aparencia.appendChild(
                posicao
            );

            aparencia.appendChild(
                tamanho
            );

            aparencia.appendChild(
                percentual
            );

            aparencia.appendChild(
                quantidadeAmostras
            );

            aparencia.appendChild(
                descricaoAparencia
            );

            const contexto =
                criarElemento(
                    "section",
                    "contexto",
                    "contexto-cena"
                );

            const contextoTitulo =
                criarElemento(
                    "h3",
                    "",
                    "secao-titulo"
                );

            contextoTitulo.textContent =
                "Contexto da cena";

            const contextoQuantidade =
                criarElemento(
                    "p",
                    "contexto-quantidade"
                );

            const contextoDistribuicao =
                criarElemento(
                    "p",
                    "contexto-distribuicao"
                );

            const contextoProximidades =
                criarElemento(
                    "p",
                    "contexto-proximidades"
                );

            const contextoPessoas =
                criarElemento(
                    "p",
                    "contexto-pessoas",
                    "detalhes-pessoas"
                );

            const contextoDescricao =
                criarElemento(
                    "p",
                    "contexto-descricao",
                    "descricao"
                );

            contexto.appendChild(
                contextoTitulo
            );

            contexto.appendChild(
                contextoQuantidade
            );

            contexto.appendChild(
                contextoDistribuicao
            );

            contexto.appendChild(
                contextoProximidades
            );

            contexto.appendChild(
                contextoPessoas
            );

            contexto.appendChild(
                contextoDescricao
            );

            dados.appendChild(
                cabecalho
            );

            dados.appendChild(
                tipo
            );

            dados.appendChild(
                horario
            );

            dados.appendChild(
                identificadorPessoa
            );

            dados.appendChild(
                quantidade
            );

            dados.appendChild(
                aparencia
            );

            dados.appendChild(
                contexto
            );

            card.appendChild(
                imagensEvento
            );

            card.appendChild(
                dados
            );

            eventosElemento.prepend(
                card
            );

            cartoesPorPessoa.set(
                pessoaId,

------------------------------------------------------------
ARQUIVO: app/static/index_backup_atualizacao_visual.html
------------------------------------------------------------
<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">

    <meta
        name="viewport"
        content="width=device-width, initial-scale=1.0"
    >

    <title>Painel de Eventos</title>

    <style>
        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            font-family: Arial, sans-serif;
            background: #10131a;
            color: #f3f4f6;
        }

        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px 30px;
            background: #191e29;
            border-bottom: 1px solid #2d3443;
        }

        h1 {
            margin: 0;
            font-size: 24px;
        }

        .status-conexao {
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .status-bolinha {
            width: 12px;
            height: 12px;
            border-radius: 50%;
            background: #f59e0b;
        }

        .status-bolinha.conectado {
            background: #22c55e;
        }

        .status-bolinha.desconectado {
            background: #ef4444;
        }

        main {
            padding: 30px;
        }

        .resumo {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
            margin-bottom: 25px;
        }

        .resumo-card {
            min-width: 180px;
            padding: 18px;
            background: #191e29;
            border: 1px solid #2d3443;
            border-radius: 10px;
        }

        .resumo-card span {
            display: block;
            color: #9ca3af;
            font-size: 14px;
        }

        .resumo-card strong {
            display: block;
            margin-top: 8px;
            font-size: 28px;
        }

        #eventos {
            display: grid;
            grid-template-columns: repeat(
                auto-fill,
                minmax(310px, 1fr)
            );
            gap: 20px;
        }

        .evento-card {
            overflow: hidden;
            background: #191e29;
            border: 1px solid #2d3443;
            border-radius: 10px;
            transition:
                border-color 0.2s,
                transform 0.2s,
                opacity 0.2s;
        }

        .evento-card.atualizado {
            border-color: #3b82f6;
            transform: translateY(-2px);
        }

        .evento-card.saiu {
            opacity: 0.55;
            border-color: #6b7280;
        }

        .evento-card img {
            width: 100%;
            height: 230px;
            object-fit: contain;
            background: #05070a;
        }

        .evento-dados {
            padding: 15px;
        }

        .evento-cabecalho {
            display: flex;
            justify-content: space-between;
            align-items: center;
            gap: 10px;
            margin-bottom: 12px;
        }

        .evento-cabecalho h2 {
            margin: 0;
            font-size: 18px;
        }

        .evento-status {
            padding: 5px 9px;
            border-radius: 999px;
            background: #374151;
            font-size: 12px;
            font-weight: bold;
        }

        .evento-status.entered {
            background: #166534;
            color: #dcfce7;
        }

        .evento-status.updated {
            background: #1e40af;
            color: #dbeafe;
        }

        .evento-status.exited {
            background: #4b5563;
            color: #f3f4f6;
        }

        .evento-dados p {
            margin: 6px 0;
            color: #cbd5e1;
            font-size: 14px;
        }

        .aparencia-visual {
            display: none;
            margin-top: 14px;
            padding-top: 12px;
            border-top: 1px solid #2d3443;
        }

        .aparencia-titulo {
            margin: 0 0 10px;
            font-size: 14px;
            color: #93c5fd;
        }

        .descricao {
            line-height: 1.45;
        }

        .cor-amostra {
            display: inline-block;
            width: 12px;
            height: 12px;
            margin-right: 6px;
            border: 1px solid #64748b;
            border-radius: 50%;
            vertical-align: -1px;
        }

        .vazio {
            padding: 40px;
            color: #9ca3af;
            text-align: center;
            background: #191e29;
            border: 1px dashed #374151;
            border-radius: 10px;
        }

        @media (max-width: 700px) {
            header {
                align-items: flex-start;
                flex-direction: column;
                gap: 15px;
                padding: 20px;
            }

            main {
                padding: 20px;
            }

            .resumo-card {
                flex: 1;
                min-width: 140px;
            }
        }
    </style>
</head>

<body>
    <header>
        <h1>Painel de Eventos das Câmeras</h1>

        <div class="status-conexao">
            <div
                id="status-bolinha"
                class="status-bolinha"
            ></div>

            <span id="status-texto">
                Conectando...
            </span>
        </div>
    </header>

    <main>
        <section class="resumo">
            <div class="resumo-card">
                <span>Pessoas ativas</span>
                <strong id="pessoas-ativas">0</strong>
            </div>

            <div class="resumo-card">
                <span>Pessoas que saíram</span>
                <strong id="pessoas-sairam">0</strong>
            </div>

            <div class="resumo-card">
                <span>Total exibido</span>
                <strong id="total-pessoas">0</strong>
            </div>

            <div class="resumo-card">
                <span>Eventos processados</span>
                <strong id="total-eventos">0</strong>
            </div>
        </section>

        <section id="eventos">
            <div
                id="mensagem-vazia"
                class="vazio"
            >
                Aguardando eventos das câmeras...
            </div>
        </section>
    </main>

    <script>
        const statusBolinha =
            document.getElementById(
                "status-bolinha"
            );

        const statusTexto =
            document.getElementById(
                "status-texto"
            );

        const pessoasAtivasElemento =
            document.getElementById(
                "pessoas-ativas"
            );

        const pessoasSairamElemento =
            document.getElementById(
                "pessoas-sairam"
            );

        const totalPessoasElemento =
            document.getElementById(
                "total-pessoas"
            );

        const totalEventosElemento =
            document.getElementById(
                "total-eventos"
            );

        const eventosElemento =
            document.getElementById(
                "eventos"
            );

        const cartoesPorPessoa =
            new Map();

        const statusPorPessoa =
            new Map();

        let pessoasAtivas = 0;
        let pessoasSairam = 0;
        let totalPessoas = 0;
        let totalEventos = 0;
        let heartbeatId = null;

        function traduzirStatus(status) {
            const traducoes = {
                entered: "Entrou",
                updated: "Atualizado",
                suppressed: "Repetido",
                exited: "Saiu"
            };

            return (
                traducoes[status]
                || status
                || "Detectado"
            );
        }

        function atualizarResumo() {
            pessoasAtivasElemento.textContent =
                pessoasAtivas;

            pessoasSairamElemento.textContent =
                pessoasSairam;

            totalPessoasElemento.textContent =
                totalPessoas;

            totalEventosElemento.textContent =
                totalEventos;
        }

        function registrarMudancaStatus(
            pessoaId,
            novoStatus
        ) {
            const statusAnterior =
                statusPorPessoa.get(
                    pessoaId
                );

            if (!statusAnterior) {
                totalPessoas += 1;

                if (novoStatus === "exited") {
                    pessoasSairam += 1;
                } else {
                    pessoasAtivas += 1;
                }
            }

            else if (
                statusAnterior !== "exited"
                && novoStatus === "exited"
            ) {
                pessoasAtivas = Math.max(
                    0,
                    pessoasAtivas - 1
                );

                pessoasSairam += 1;
            }

            else if (
                statusAnterior === "exited"
                && novoStatus !== "exited"
            ) {
                pessoasSairam = Math.max(
                    0,
                    pessoasSairam - 1
                );

                pessoasAtivas += 1;
            }

            statusPorPessoa.set(
                pessoaId,
                novoStatus
            );

            atualizarResumo();
        }

        function conectarWebSocket() {
            const protocolo =
                window.location.protocol === "https:"
                    ? "wss"
                    : "ws";

            const endereco =
                `${protocolo}://${window.location.host}/ws/eventos`;

            const websocket =
                new WebSocket(endereco);

            websocket.onopen = function () {
                statusTexto.textContent =
                    "Conectado";

                statusBolinha.className =
                    "status-bolinha conectado";

                if (heartbeatId) {
                    clearInterval(
                        heartbeatId
                    );
                }

                heartbeatId = setInterval(
                    function () {
                        if (
                            websocket.readyState
                            === WebSocket.OPEN
                        ) {
                            websocket.send(
                                "ping"
                            );
                        }
                    },
                    20000
                );
            };

            websocket.onmessage =
                function (mensagem) {
                    try {
                        const evento =
                            JSON.parse(
                                mensagem.data
                            );

                        processarEvento(
                            evento
                        );

                    } catch (erro) {
                        console.error(
                            "Erro ao interpretar evento:",
                            erro
                        );
                    }
                };

            websocket.onclose = function () {
                statusTexto.textContent =
                    "Desconectado — reconectando...";

                statusBolinha.className =
                    "status-bolinha desconectado";

                if (heartbeatId) {
                    clearInterval(
                        heartbeatId
                    );

                    heartbeatId = null;
                }

                setTimeout(
                    conectarWebSocket,
                    2000
                );
            };

            websocket.onerror = function () {
                websocket.close();
            };
        }

        function processarEvento(evento) {
            if (
                evento.status
                === "suppressed"
            ) {
                return;
            }

            const pessoaId =
                evento.pessoa_id
                || evento.evento_id;

            if (!pessoaId) {
                console.warn(
                    "Evento sem identificador",
                    evento
                );

                return;
            }

            const cartaoExistente =
                cartoesPorPessoa.get(
                    pessoaId
                );

            if (
                evento.status === "exited"
                && !cartaoExistente
            ) {
                return;
            }

            registrarMudancaStatus(
                pessoaId,
                evento.status || "entered"
            );

            totalEventos += 1;
            atualizarResumo();

            if (cartaoExistente) {
                atualizarCartao(
                    cartaoExistente,
                    evento
                );

                return;
            }

            criarCartao(
                pessoaId,
                evento
            );
        }

        function criarCartao(
            pessoaId,
            evento
        ) {
            const mensagemVazia =
                document.getElementById(
                    "mensagem-vazia"
                );

            if (mensagemVazia) {
                mensagemVazia.remove();
            }

            const card =
                document.createElement(
                    "article"
                );

            card.className =
                "evento-card";

            card.dataset.pessoaId =
                pessoaId;

            const imagem =
                document.createElement(
                    "img"
                );

            imagem.alt =
                "Recorte da pessoa";

            imagem.dataset.campo =
                "imagem";

            const dados =
                document.createElement(
                    "div"
                );

            dados.className =
                "evento-dados";

            const cabecalho =
                document.createElement(
                    "div"
                );

            cabecalho.className =
                "evento-cabecalho";

            const titulo =
                document.createElement(
                    "h2"
                );

            titulo.dataset.campo =
                "camera";

            const status =
                document.createElement(
                    "span"
                );

            status.dataset.campo =
                "status";

            status.className =
                "evento-status";

            const tipo =
                document.createElement(
                    "p"
                );

            tipo.dataset.campo =
                "tipo";

            const horario =
                document.createElement(
                    "p"
                );

            horario.dataset.campo =
                "horario";

            const identificadorPessoa =
                document.createElement(
                    "p"
                );

            identificadorPessoa.dataset.campo =
                "pessoa-id";

            const quantidade =
                document.createElement(
                    "p"
                );

            quantidade.dataset.campo =
                "quantidade";

            const aparencia =
                document.createElement(
                    "section"
                );

            aparencia.className =
                "aparencia-visual";

            aparencia.dataset.campo =
                "aparencia";

            const aparenciaTitulo =
                document.createElement(
                    "h3"
                );

            aparenciaTitulo.className =
                "aparencia-titulo";

            aparenciaTitulo.textContent =
                "Aparência estável";

            const corRoupa =
                document.createElement(
                    "p"
                );

            corRoupa.dataset.campo =
                "cor-roupa";

            const posicao =
                document.createElement(
                    "p"
                );

            posicao.dataset.campo =
                "posicao";

            const tamanho =
                document.createElement(
                    "p"
                );

            tamanho.dataset.campo =
                "tamanho";

            const percentual =
                document.createElement(
                    "p"
                );

            percentual.dataset.campo =
                "percentual";

            const quantidadeAmostras =
                document.createElement(
                    "p"
                );

            quantidadeAmostras.dataset.campo =
                "quantidade-amostras";

            const descricao =
                document.createElement(
                    "p"
                );

            descricao.dataset.campo =
                "descricao";

            descricao.className =
                "descricao";

            cabecalho.appendChild(
                titulo
            );

            cabecalho.appendChild(
                status
            );

            dados.appendChild(
                cabecalho
            );

            dados.appendChild(tipo);
            dados.appendChild(horario);

            dados.appendChild(
                identificadorPessoa
            );

            dados.appendChild(
                quantidade
            );

            aparencia.appendChild(
                aparenciaTitulo
            );

            aparencia.appendChild(
                corRoupa
            );

            aparencia.appendChild(
                posicao
            );

            aparencia.appendChild(
                tamanho
            );

            aparencia.appendChild(
                percentual
            );

            aparencia.appendChild(
                quantidadeAmostras
            );

            aparencia.appendChild(
                descricao
            );

            dados.appendChild(
                aparencia
            );

            card.appendChild(imagem);
            card.appendChild(dados);

            eventosElemento.prepend(
                card
            );

            cartoesPorPessoa.set(
                pessoaId,
                card
            );

            atualizarCartao(
                card,
                evento
            );

            limitarQuantidadeCartoes();
        }

        function atualizarCartao(
            card,
            evento
        ) {
            const imagem =
                card.querySelector(
                    '[data-campo="imagem"]'
                );

            const titulo =
                card.querySelector(
                    '[data-campo="camera"]'
                );

            const status =
                card.querySelector(
                    '[data-campo="status"]'
                );

            const tipo =
                card.querySelector(
                    '[data-campo="tipo"]'
                );

            const horario =
                card.querySelector(
                    '[data-campo="horario"]'
                );

            const pessoaId =
                card.querySelector(
                    '[data-campo="pessoa-id"]'
                );

            const quantidade =
                card.querySelector(
                    '[data-campo="quantidade"]'
                );

            const aparenciaVisual =
                card.querySelector(
                    '[data-campo="aparencia"]'
                );

            const corRoupa =
                card.querySelector(
                    '[data-campo="cor-roupa"]'
                );

            const posicao =
                card.querySelector(
                    '[data-campo="posicao"]'
                );

            const tamanho =
                card.querySelector(
                    '[data-campo="tamanho"]'
                );

            const percentual =
                card.querySelector(
                    '[data-campo="percentual"]'
                );

            const quantidadeAmostras =
                card.querySelector(
                    '[data-campo="quantidade-amostras"]'
                );

            const descricao =
                card.querySelector(
                    '[data-campo="descricao"]'
                );

            if (evento.imagem) {
                imagem.src =
                    `data:image/jpeg;base64,${evento.imagem}`;
            }

            if (evento.camera) {
                titulo.textContent =
                    evento.camera;
            }

            status.textContent =
                traduzirStatus(
                    evento.status
                );

            status.className =
                `evento-status ${evento.status || ""}`;

            if (evento.tipo) {
                tipo.textContent =
                    `Evento: ${evento.tipo}`;
            }

            if (
                evento.status === "exited"
            ) {
                horario.textContent =
                    `Saída: ${evento.datetime || "-"}`;
            } else {
                horario.textContent =
                    `Última captura: ${evento.datetime || "-"}`;
            }

            pessoaId.textContent =
                `Pessoa: ${evento.pessoa_id || "-"}`;

            quantidade.textContent =
                `Detecções: ${
                    evento.quantidade_deteccoes
                    || 1
                }`;

            if (evento.aparencia) {
                aparenciaVisual.style.display =
                    "block";

                const rgb =
                    evento.aparencia.rgb_medio;

                let amostraCor = "";

                if (
                    Array.isArray(rgb)
                    && rgb.length === 3
                ) {
                    amostraCor =
                        `<span
                            class="cor-amostra"
                            style="
                                background-color:
                                rgb(
                                    ${rgb[0]},
                                    ${rgb[1]},
                                    ${rgb[2]}
                                );
                            "
                        ></span>`;
                }

                corRoupa.innerHTML =
                    `${amostraCor}Cor predominante: ${
                        evento.aparencia
                            .cor_roupa_predominante
                        || "-"
                    }`;

                posicao.textContent =
                    `Posição atual: ${
                        evento.aparencia
                            .posicao_atual
                        || "-"
                    }`;

                tamanho.textContent =
                    `Tamanho predominante: ${
                        evento.aparencia
                            .tamanho_predominante
                        || "-"
                    }`;

                percentual.textContent =
                    `Área média ocupada: ${
                        evento.aparencia
                            .percentual_medio_quadro
                        ?? "-"
                    }%`;

                quantidadeAmostras.textContent =
                    `Amostras analisadas: ${
                        evento.aparencia
                            .quantidade_amostras
                        || 1
                    }`;

                descricao.textContent =
                    `Descrição estável: ${
                        evento.aparencia
                            .descricao
                        || "-"
                    }`;
            }

            card.classList.remove(
                "atualizado",
                "saiu"
            );

            if (
                evento.status === "updated"
            ) {
                card.classList.add(
                    "atualizado"
                );

                eventosElemento.prepend(
                    card
                );

                setTimeout(
                    function () {
                        card.classList.remove(
                            "atualizado"
                        );
                    },
                    1500
                );
            }

            if (
                evento.status === "exited"
            ) {
                card.classList.add(
                    "saiu"
                );
            }
        }

        function limitarQuantidadeCartoes() {
            while (
                eventosElemento.children.length
                > 50
            ) {
                const ultimo =
                    eventosElemento
                    .lastElementChild;

                if (!ultimo) {
                    break;
                }

                const pessoaId =
                    ultimo.dataset.pessoaId;

                if (pessoaId) {
                    const statusRemovido =
                        statusPorPessoa.get(
                            pessoaId
                        );

                    if (
                        statusRemovido
                        === "exited"
                    ) {
                        pessoasSairam =
                            Math.max(
                                0,
                                pessoasSairam - 1
                            );
                    }

                    else if (
                        statusRemovido
                    ) {
                        pessoasAtivas =
                            Math.max(
                                0,
                                pessoasAtivas - 1
                            );
                    }

                    totalPessoas =
                        Math.max(
                            0,
                            totalPessoas - 1
                        );

                    cartoesPorPessoa.delete(
                        pessoaId
                    );

                    statusPorPessoa.delete(
                        pessoaId
                    );
                }

                ultimo.remove();
            }

            atualizarResumo();
        }

        atualizarResumo();
        conectarWebSocket();
    </script>
</body>
</html>
------------------------------------------------------------
ARQUIVO: app/static/index_backup_recorte.html
------------------------------------------------------------
<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">

    <meta
        name="viewport"
        content="width=device-width, initial-scale=1.0"
    >

    <title>Painel de Eventos</title>

    <style>
        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            font-family: Arial, sans-serif;
            background: #10131a;
            color: #f3f4f6;
        }

        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px 30px;
            background: #191e29;
            border-bottom: 1px solid #2d3443;
        }

        h1 {
            margin: 0;
            font-size: 24px;
        }

        .status-conexao {
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .status-bolinha {
            width: 12px;
            height: 12px;
            border-radius: 50%;
            background: #f59e0b;
        }

        .status-bolinha.conectado {
            background: #22c55e;
        }

        .status-bolinha.desconectado {
            background: #ef4444;
        }

        main {
            padding: 30px;
        }

        .resumo {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
            margin-bottom: 25px;
        }

        .resumo-card {
            min-width: 180px;
            padding: 18px;
            background: #191e29;
            border: 1px solid #2d3443;
            border-radius: 10px;
        }

        .resumo-card span {
            display: block;
            color: #9ca3af;
            font-size: 14px;
        }

        .resumo-card strong {
            display: block;
            margin-top: 8px;
            font-size: 28px;
        }

        #eventos {
            display: grid;
            grid-template-columns: repeat(
                auto-fill,
                minmax(400px, 1fr)
            );
            gap: 20px;
        }

        .evento-card {
            overflow: hidden;
            background: #191e29;
            border: 1px solid #2d3443;
            border-radius: 10px;
            transition:
                border-color 0.2s,
                transform 0.2s,
                opacity 0.2s;
        }

        .evento-card.atualizado {
            border-color: #3b82f6;
            transform: translateY(-2px);
        }

        .evento-card.saiu {
            opacity: 0.55;
            border-color: #6b7280;
        }

        .imagens-evento {
            display: flex;
            flex-direction: column;
            background: #05070a;
        }

        .imagem-bloco {
            display: none;
            border-bottom: 1px solid #2d3443;
        }

        .imagem-bloco:last-child {
            border-bottom: none;
        }

        .imagem-titulo {
            display: flex;
            justify-content: space-between;
            align-items: center;
            min-height: 38px;
            padding: 9px 12px;
            background: #111827;
            color: #cbd5e1;
            font-size: 13px;
            font-weight: bold;
        }

        .imagem-contagem {
            color: #86efac;
            font-size: 12px;
        }

        .imagem-evento {
            display: block;
            width: 100%;
            background: #05070a;
        }

        .imagem-recorte {
            height: 260px;
            object-fit: contain;
        }

        .imagem-cena {
            aspect-ratio: 16 / 9;
            max-height: 440px;
            object-fit: contain;
        }

        .evento-dados {
            padding: 15px;
        }

        .evento-cabecalho {
            display: flex;
            justify-content: space-between;
            align-items: center;
            gap: 10px;
            margin-bottom: 12px;
        }

        .evento-cabecalho h2 {
            margin: 0;
            font-size: 18px;
        }

        .evento-status {
            padding: 5px 9px;
            border-radius: 999px;
            background: #374151;
            font-size: 12px;
            font-weight: bold;
        }

        .evento-status.entered {
            background: #166534;
            color: #dcfce7;
        }

        .evento-status.updated {
            background: #1e40af;
            color: #dbeafe;
        }

        .evento-status.exited {
            background: #4b5563;
            color: #f3f4f6;
        }

        .evento-dados p {
            margin: 6px 0;
            color: #cbd5e1;
            font-size: 14px;
        }

        .aparencia-visual,
        .movimento-pessoa,
        .contexto-cena {
            display: none;
            margin-top: 14px;
            padding-top: 12px;
            border-top: 1px solid #2d3443;
        }

        .secao-titulo {
            margin: 0 0 10px;
            font-size: 14px;
            color: #93c5fd;
        }

        .movimento-pessoa .secao-titulo {
            color: #fbbf24;
        }

        .contexto-cena .secao-titulo {
            color: #86efac;
        }

        .descricao {
            line-height: 1.45;
        }

        .detalhes-pessoas {
            line-height: 1.5;
            white-space: pre-line;
        }

        .cor-amostra {
            display: inline-block;
            width: 12px;
            height: 12px;
            margin-right: 6px;
            border: 1px solid #64748b;
            border-radius: 50%;
            vertical-align: -1px;
        }

        .vazio {
            padding: 40px;
            color: #9ca3af;
            text-align: center;
            background: #191e29;
            border: 1px dashed #374151;
            border-radius: 10px;
        }

        @media (max-width: 700px) {
            header {
                align-items: flex-start;
                flex-direction: column;
                gap: 15px;
                padding: 20px;
            }

            main {
                padding: 20px;
            }

            #eventos {
                grid-template-columns: 1fr;
            }

            .resumo-card {
                flex: 1;
                min-width: 140px;
            }

            .imagem-recorte {
                height: 230px;
            }
        }
    </style>
</head>

<body>
    <header>
        <h1>Painel de Eventos das Câmeras</h1>

        <div class="status-conexao">
            <div
                id="status-bolinha"
                class="status-bolinha"
            ></div>

            <span id="status-texto">
                Conectando...
            </span>
        </div>
    </header>

    <main>
        <section class="resumo">
            <div class="resumo-card">
                <span>Pessoas ativas</span>
                <strong id="pessoas-ativas">0</strong>
            </div>

            <div class="resumo-card">
                <span>Pessoas que saíram</span>
                <strong id="pessoas-sairam">0</strong>
            </div>

            <div class="resumo-card">
                <span>Total exibido</span>
                <strong id="total-pessoas">0</strong>
            </div>

            <div class="resumo-card">
                <span>Eventos processados</span>
                <strong id="total-eventos">0</strong>
            </div>
        </section>

        <section id="eventos">
            <div
                id="mensagem-vazia"
                class="vazio"
            >
                Aguardando eventos das câmeras...
            </div>
        </section>
    </main>

    <script>
        const statusBolinha =
            document.getElementById(
                "status-bolinha"
            );

        const statusTexto =
            document.getElementById(
                "status-texto"
            );

        const pessoasAtivasElemento =
            document.getElementById(
                "pessoas-ativas"
            );

        const pessoasSairamElemento =
            document.getElementById(
                "pessoas-sairam"
            );

        const totalPessoasElemento =
            document.getElementById(
                "total-pessoas"
            );

        const totalEventosElemento =
            document.getElementById(
                "total-eventos"
            );

        const eventosElemento =
            document.getElementById(
                "eventos"
            );

        const cartoesPorPessoa =
            new Map();

        const statusPorPessoa =
            new Map();

        let pessoasAtivas = 0;
        let pessoasSairam = 0;
        let totalPessoas = 0;
        let totalEventos = 0;
        let heartbeatId = null;

        function traduzirStatus(status) {
            const traducoes = {
                entered: "Entrou",
                updated: "Atualizado",
                suppressed: "Repetido",
                exited: "Saiu"
            };

            return (
                traducoes[status]
                || status
                || "Detectado"
            );
        }

        function atualizarResumo() {
            pessoasAtivasElemento.textContent =
                pessoasAtivas;

            pessoasSairamElemento.textContent =
                pessoasSairam;

            totalPessoasElemento.textContent =
                totalPessoas;

            totalEventosElemento.textContent =
                totalEventos;
        }

        function registrarMudancaStatus(
            pessoaId,
            novoStatus
        ) {
            const statusAnterior =
                statusPorPessoa.get(
                    pessoaId
                );

            if (!statusAnterior) {
                totalPessoas += 1;

                if (novoStatus === "exited") {
                    pessoasSairam += 1;
                } else {
                    pessoasAtivas += 1;
                }
            }

            else if (
                statusAnterior !== "exited"
                && novoStatus === "exited"
            ) {
                pessoasAtivas = Math.max(
                    0,
                    pessoasAtivas - 1
                );

                pessoasSairam += 1;
            }

            else if (
                statusAnterior === "exited"
                && novoStatus !== "exited"
            ) {
                pessoasSairam = Math.max(
                    0,
                    pessoasSairam - 1
                );

                pessoasAtivas += 1;
            }

            statusPorPessoa.set(
                pessoaId,
                novoStatus
            );

            atualizarResumo();
        }

        function conectarWebSocket() {
            const protocolo =
                window.location.protocol === "https:"
                    ? "wss"
                    : "ws";

            const endereco =
                `${protocolo}://${window.location.host}/ws/eventos`;

            const websocket =
                new WebSocket(endereco);

            websocket.onopen = function () {
                statusTexto.textContent =
                    "Conectado";

                statusBolinha.className =
                    "status-bolinha conectado";

                if (heartbeatId) {
                    clearInterval(
                        heartbeatId
                    );
                }

                heartbeatId = setInterval(
                    function () {
                        if (
                            websocket.readyState
                            === WebSocket.OPEN
                        ) {
                            websocket.send(
                                "ping"
                            );
                        }
                    },
                    20000
                );
            };

            websocket.onmessage =
                function (mensagem) {
                    try {
                        const evento =
                            JSON.parse(
                                mensagem.data
                            );

                        processarEvento(
                            evento
                        );

                    } catch (erro) {
                        console.error(
                            "Erro ao interpretar evento:",
                            erro
                        );
                    }
                };

            websocket.onclose = function () {
                statusTexto.textContent =
                    "Desconectado — reconectando...";

                statusBolinha.className =
                    "status-bolinha desconectado";

                if (heartbeatId) {
                    clearInterval(
                        heartbeatId
                    );

                    heartbeatId = null;
                }

                setTimeout(
                    conectarWebSocket,
                    2000
                );
            };

            websocket.onerror = function () {
                websocket.close();
            };
        }

        function processarEvento(evento) {
            if (
                evento.status
                === "appearance_updated"
            ) {
                atualizarEventoParcial(
                    evento
                );

                return;
            }

            if (
                evento.status
                === "suppressed"
            ) {
                return;
            }

            const pessoaId =
                evento.pessoa_id
                || evento.evento_id;

            if (!pessoaId) {
                console.warn(
                    "Evento sem identificador:",
                    evento
                );

                return;
            }

            const cartaoExistente =
                cartoesPorPessoa.get(
                    pessoaId
                );

            if (
                evento.status === "exited"
                && !cartaoExistente
            ) {
                return;
            }

            registrarMudancaStatus(
                pessoaId,
                evento.status || "entered"
            );

            totalEventos += 1;
            atualizarResumo();

            if (cartaoExistente) {
                atualizarCartao(
                    cartaoExistente,
                    evento
                );

                return;
            }

            criarCartao(
                pessoaId,
                evento
            );
        }

        function criarElemento(
            tag,
            campo,
            classe = ""
        ) {
            const elemento =
                document.createElement(
                    tag
                );

            if (campo) {
                elemento.dataset.campo =
                    campo;
            }

            if (classe) {
                elemento.className =
                    classe;
            }

            return elemento;
        }

        function obterCamposCard(card) {
            return {
                blocoRecorte: card.querySelector(
                    '[data-campo="bloco-recorte"]'
                ),

                imagemRecorte: card.querySelector(
                    '[data-campo="imagem-recorte"]'
                ),

                blocoCena: card.querySelector(
                    '[data-campo="bloco-cena"]'
                ),

                tituloCena: card.querySelector(
                    '[data-campo="titulo-cena"]'
                ),

                quantidadeBoxes: card.querySelector(
                    '[data-campo="quantidade-boxes"]'
                ),

                imagemCena: card.querySelector(
                    '[data-campo="imagem-cena"]'
                ),

                camera: card.querySelector(
                    '[data-campo="camera"]'
                ),

                status: card.querySelector(
                    '[data-campo="status"]'
                ),

                tipo: card.querySelector(
                    '[data-campo="tipo"]'
                ),

                horario: card.querySelector(
                    '[data-campo="horario"]'
                ),

                pessoaId: card.querySelector(
                    '[data-campo="pessoa-id"]'
                ),

                quantidade: card.querySelector(
                    '[data-campo="quantidade"]'
                ),

                aparencia: card.querySelector(
                    '[data-campo="aparencia"]'
                ),

                corRoupa: card.querySelector(
                    '[data-campo="cor-roupa"]'
                ),

                posicao: card.querySelector(
                    '[data-campo="posicao"]'
                ),

                tamanho: card.querySelector(
                    '[data-campo="tamanho"]'
                ),

                percentual: card.querySelector(
                    '[data-campo="percentual"]'
                ),

                quantidadeAmostras: card.querySelector(
                    '[data-campo="quantidade-amostras"]'
                ),

                descricaoAparencia: card.querySelector(
                    '[data-campo="descricao-aparencia"]'
                ),

                movimento: card.querySelector(
                    '[data-campo="movimento"]'
                ),

                movimentoHorizontal: card.querySelector(
                    '[data-campo="movimento-horizontal"]'
                ),

                movimentoVertical: card.querySelector(
                    '[data-campo="movimento-vertical"]'
                ),

                movimentoTendencia: card.querySelector(
                    '[data-campo="movimento-tendencia"]'
                ),

                movimentoVelocidade: card.querySelector(
                    '[data-campo="movimento-velocidade"]'
                ),

                movimentoDistancia: card.querySelector(
                    '[data-campo="movimento-distancia"]'
                ),

                movimentoTempo: card.querySelector(
                    '[data-campo="movimento-tempo"]'
                ),

                movimentoAmostras: card.querySelector(
                    '[data-campo="movimento-amostras"]'
                ),

                movimentoDescricao: card.querySelector(
                    '[data-campo="movimento-descricao"]'
                ),

                contexto: card.querySelector(
                    '[data-campo="contexto"]'
                ),

                contextoQuantidade: card.querySelector(
                    '[data-campo="contexto-quantidade"]'
                ),

                contextoDistribuicao: card.querySelector(
                    '[data-campo="contexto-distribuicao"]'
                ),

                contextoProximidades: card.querySelector(
                    '[data-campo="contexto-proximidades"]'
                ),

                contextoPessoas: card.querySelector(
                    '[data-campo="contexto-pessoas"]'
                ),

                contextoDescricao: card.querySelector(
                    '[data-campo="contexto-descricao"]'
                )
            };
        }

        function atualizarEventoParcial(
            evento
        ) {
            const pessoaId =
                evento.pessoa_id
                || evento.evento_id;

            const card =
                cartoesPorPessoa.get(
                    pessoaId
                );

            if (!card) {
                console.warn(
                    "Atualização recebida sem cartão:",
                    pessoaId
                );

                return;
            }

            const campos =
                obterCamposCard(card);

            campos.quantidade.textContent =
                `Detecções: ${
                    evento.quantidade_deteccoes
                    || 1
                }`;

            if (evento.aparencia) {
                preencherAparencia(
                    evento.aparencia,
                    campos
                );
            }

            if (evento.movimento) {
                preencherMovimento(
                    evento.movimento,
                    campos
                );
            }

            if (evento.contexto_cena) {
                preencherContextoCena(
                    evento.contexto_cena,
                    campos
                );
            }

            if (evento.imagem_cena) {
                preencherCenaCompleta(
                    evento.imagem_cena,
                    evento.quantidade_boxes_cena,
                    campos
                );
            }

            card.classList.add(
                "atualizado"
            );

            setTimeout(
                function () {
                    card.classList.remove(
                        "atualizado"
                    );
                },
                1000
            );
        }

        function criarCartao(
            pessoaId,
            evento
        ) {
            const mensagemVazia =
                document.getElementById(
                    "mensagem-vazia"
                );

            if (mensagemVazia) {
                mensagemVazia.remove();
            }

            const card =
                criarElemento(
                    "article",
                    "",
                    "evento-card"
                );

            card.dataset.pessoaId =
                pessoaId;

            const imagensEvento =
                criarElemento(
                    "section",
                    "",
                    "imagens-evento"
                );

            const blocoRecorte =
                criarElemento(
                    "div",
                    "bloco-recorte",
                    "imagem-bloco"
                );

            const tituloRecorte =
                criarElemento(
                    "div",
                    "",
                    "imagem-titulo"
                );

            tituloRecorte.textContent =
                "Recorte individual";

            const imagemRecorte =
                criarElemento(
                    "img",
                    "imagem-recorte",
                    "imagem-evento imagem-recorte"
                );

            imagemRecorte.alt =
                "Recorte individual da pessoa";

            blocoRecorte.appendChild(
                tituloRecorte
            );

            blocoRecorte.appendChild(
                imagemRecorte
            );

            const blocoCena =
                criarElemento(
                    "div",
                    "bloco-cena",
                    "imagem-bloco"
                );

            const cabecalhoCena =
                criarElemento(
                    "div",
                    "",
                    "imagem-titulo"
                );

            const tituloCena =
                criarElemento(
                    "span",
                    "titulo-cena"
                );

            tituloCena.textContent =
                "Cena completa";

            const quantidadeBoxes =
                criarElemento(
                    "span",
                    "quantidade-boxes",
                    "imagem-contagem"
                );

            quantidadeBoxes.textContent =
                "0 detecções";

            cabecalhoCena.appendChild(
                tituloCena
            );

            cabecalhoCena.appendChild(
                quantidadeBoxes
            );

            const imagemCena =
                criarElemento(
                    "img",
                    "imagem-cena",
                    "imagem-evento imagem-cena"
                );

            imagemCena.alt =
                "Cena completa com as detecções";

            blocoCena.appendChild(
                cabecalhoCena
            );

            blocoCena.appendChild(
                imagemCena
            );

            imagensEvento.appendChild(
                blocoRecorte
            );

            imagensEvento.appendChild(
                blocoCena
            );

            const dados =
                criarElemento(
                    "div",
                    "",
                    "evento-dados"
                );

            const cabecalho =
                criarElemento(
                    "div",
                    "",
                    "evento-cabecalho"
                );

            const titulo =
                criarElemento(
                    "h2",
                    "camera"
                );

            const status =
                criarElemento(
                    "span",
                    "status",
                    "evento-status"
                );

            cabecalho.appendChild(
                titulo
            );

            cabecalho.appendChild(
                status
            );

            const tipo =
                criarElemento(
                    "p",
                    "tipo"
                );

            const horario =
                criarElemento(
                    "p",
                    "horario"
                );

            const identificadorPessoa =
                criarElemento(
                    "p",
                    "pessoa-id"
                );

            const quantidade =
                criarElemento(
                    "p",
                    "quantidade"
                );

            const aparencia =
                criarElemento(
                    "section",
                    "aparencia",
                    "aparencia-visual"
                );

            const aparenciaTitulo =
                criarElemento(
                    "h3",
                    "",
                    "secao-titulo"
                );

            aparenciaTitulo.textContent =
                "Aparência estável";

            const corRoupa =
                criarElemento(
                    "p",
                    "cor-roupa"
                );

            const posicao =
                criarElemento(
                    "p",
                    "posicao"
                );

            const tamanho =
                criarElemento(
                    "p",
                    "tamanho"
                );

            const percentual =
                criarElemento(
                    "p",
                    "percentual"
                );

            const quantidadeAmostras =
                criarElemento(
                    "p",
                    "quantidade-amostras"
                );

            const descricaoAparencia =
                criarElemento(
                    "p",
                    "descricao-aparencia",
                    "descricao"
                );

            aparencia.appendChild(
                aparenciaTitulo
            );

            aparencia.appendChild(
                corRoupa
            );

            aparencia.appendChild(
                posicao
            );

            aparencia.appendChild(
                tamanho
            );

            aparencia.appendChild(
                percentual
            );

            aparencia.appendChild(
                quantidadeAmostras
            );

            aparencia.appendChild(
                descricaoAparencia
            );

            const movimento =
                criarElemento(
                    "section",
                    "movimento",
                    "movimento-pessoa"
                );

            const movimentoTitulo =
                criarElemento(
                    "h3",
                    "",
                    "secao-titulo"
                );

            movimentoTitulo.textContent =
                "Movimento e permanência";

            const movimentoHorizontal =
                criarElemento(
                    "p",
                    "movimento-horizontal"
                );

            const movimentoVertical =
                criarElemento(
                    "p",
                    "movimento-vertical"
                );

            const movimentoTendencia =
                criarElemento(
                    "p",
                    "movimento-tendencia"
                );

            const movimentoVelocidade =
                criarElemento(
                    "p",
                    "movimento-velocidade"
                );

            const movimentoDistancia =
                criarElemento(
                    "p",
                    "movimento-distancia"
                );

            const movimentoTempo =
                criarElemento(
                    "p",
                    "movimento-tempo"
                );

            const movimentoAmostras =
                criarElemento(
                    "p",
                    "movimento-amostras"
                );

            const movimentoDescricao =
                criarElemento(
                    "p",
                    "movimento-descricao",
                    "descricao"
                );

            movimento.appendChild(

------------------------------------------------------------
ARQUIVO: app/static/index.html
------------------------------------------------------------
<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">

    <meta
        name="viewport"
        content="width=device-width, initial-scale=1.0"
    >

    <title>Painel de Eventos</title>

    <style>
        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            font-family: Arial, sans-serif;
            background: #10131a;
            color: #f3f4f6;
        }

        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px 30px;
            background: #191e29;
            border-bottom: 1px solid #2d3443;
        }

        h1 {
            margin: 0;
            font-size: 24px;
        }

        .status-conexao {
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .status-bolinha {
            width: 12px;
            height: 12px;
            border-radius: 50%;
            background: #f59e0b;
        }

        .status-bolinha.conectado {
            background: #22c55e;
        }

        .status-bolinha.desconectado {
            background: #ef4444;
        }

        main {
            padding: 30px;
        }

        .resumo {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
            margin-bottom: 25px;
        }

        .resumo-card {
            min-width: 180px;
            padding: 18px;
            background: #191e29;
            border: 1px solid #2d3443;
            border-radius: 10px;
        }

        .resumo-card span {
            display: block;
            color: #9ca3af;
            font-size: 14px;
        }

        .resumo-card strong {
            display: block;
            margin-top: 8px;
            font-size: 28px;
        }

        #eventos {
            display: grid;
            grid-template-columns: repeat(
                auto-fill,
                minmax(400px, 1fr)
            );
            gap: 20px;
        }

        .evento-card {
            overflow: hidden;
            background: #191e29;
            border: 1px solid #2d3443;
            border-radius: 10px;
            transition:
                border-color 0.2s,
                transform 0.2s,
                opacity 0.2s;
        }

        .evento-card.atualizado {
            border-color: #3b82f6;
            transform: translateY(-2px);
        }

        .evento-card.saiu {
            opacity: 0.55;
            border-color: #6b7280;
        }

        .imagens-evento {
            display: flex;
            flex-direction: column;
            background: #05070a;
        }

        .imagem-bloco {
            display: none;
            border-bottom: 1px solid #2d3443;
        }

        .imagem-bloco:last-child {
            border-bottom: none;
        }

        .imagem-titulo {
            display: flex;
            justify-content: space-between;
            align-items: center;
            min-height: 38px;
            padding: 9px 12px;
            background: #111827;
            color: #cbd5e1;
            font-size: 13px;
            font-weight: bold;
        }

        .imagem-contagem {
            color: #86efac;
            font-size: 12px;
        }

        .imagem-evento {
            display: block;
            width: 100%;
            background: #05070a;
        }

        .imagem-recorte {
            height: 260px;
            object-fit: contain;
        }

        .imagem-cena {
            aspect-ratio: 16 / 9;
            max-height: 440px;
            object-fit: contain;
        }

        .evento-dados {
            padding: 15px;
        }

        .evento-cabecalho {
            display: flex;
            justify-content: space-between;
            align-items: center;
            gap: 10px;
            margin-bottom: 12px;
        }

        .evento-cabecalho h2 {
            margin: 0;
            font-size: 18px;
        }

        .evento-status {
            padding: 5px 9px;
            border-radius: 999px;
            background: #374151;
            font-size: 12px;
            font-weight: bold;
        }

        .evento-status.entered {
            background: #166534;
            color: #dcfce7;
        }

        .evento-status.updated {
            background: #1e40af;
            color: #dbeafe;
        }

        .evento-status.exited {
            background: #4b5563;
            color: #f3f4f6;
        }

        .evento-dados p {
            margin: 6px 0;
            color: #cbd5e1;
            font-size: 14px;
        }

        .aparencia-visual,
        .movimento-pessoa,
        .contexto-cena {
            display: none;
            margin-top: 14px;
            padding-top: 12px;
            border-top: 1px solid #2d3443;
        }

        .secao-titulo {
            margin: 0 0 10px;
            font-size: 14px;
            color: #93c5fd;
        }

        .movimento-pessoa .secao-titulo {
            color: #fbbf24;
        }

        .contexto-cena .secao-titulo {
            color: #86efac;
        }

        .descricao {
            line-height: 1.45;
        }

        .detalhes-pessoas {
            line-height: 1.5;
            white-space: pre-line;
        }

        .cor-amostra {
            display: inline-block;
            width: 12px;
            height: 12px;
            margin-right: 6px;
            border: 1px solid #64748b;
            border-radius: 50%;
            vertical-align: -1px;
        }

        .vazio {
            padding: 40px;
            color: #9ca3af;
            text-align: center;
            background: #191e29;
            border: 1px dashed #374151;
            border-radius: 10px;
        }

        @media (max-width: 700px) {
            header {
                align-items: flex-start;
                flex-direction: column;
                gap: 15px;
                padding: 20px;
            }

            main {
                padding: 20px;
            }

            #eventos {
                grid-template-columns: 1fr;
            }

            .resumo-card {
                flex: 1;
                min-width: 140px;
            }

            .imagem-recorte {
                height: 230px;
            }
        }
    </style>
</head>

<body>
    <header>
        <h1>Painel de Eventos das Câmeras</h1>

        <div class="status-conexao">
            <div
                id="status-bolinha"
                class="status-bolinha"
            ></div>

            <span id="status-texto">
                Conectando...
            </span>
        </div>
    </header>

    <main>
        <section class="resumo">
            <div class="resumo-card">
                <span>Pessoas ativas</span>
                <strong id="pessoas-ativas">0</strong>
            </div>

            <div class="resumo-card">
                <span>Pessoas que saíram</span>
                <strong id="pessoas-sairam">0</strong>
            </div>

            <div class="resumo-card">
                <span>Total exibido</span>
                <strong id="total-pessoas">0</strong>
            </div>

            <div class="resumo-card">
                <span>Eventos processados</span>
                <strong id="total-eventos">0</strong>
            </div>
        </section>

        <section id="eventos">
            <div
                id="mensagem-vazia"
                class="vazio"
            >
                Aguardando eventos das câmeras...
            </div>
        </section>
    </main>

    <script>
        const statusBolinha =
            document.getElementById(
                "status-bolinha"
            );

        const statusTexto =
            document.getElementById(
                "status-texto"
            );

        const pessoasAtivasElemento =
            document.getElementById(
                "pessoas-ativas"
            );

        const pessoasSairamElemento =
            document.getElementById(
                "pessoas-sairam"
            );

        const totalPessoasElemento =
            document.getElementById(
                "total-pessoas"
            );

        const totalEventosElemento =
            document.getElementById(
                "total-eventos"
            );

        const eventosElemento =
            document.getElementById(
                "eventos"
            );

        const cartoesPorPessoa =
            new Map();

        const statusPorPessoa =
            new Map();

        let pessoasAtivas = 0;
        let pessoasSairam = 0;
        let totalPessoas = 0;
        let totalEventos = 0;
        let heartbeatId = null;

        function traduzirStatus(status) {
            const traducoes = {
                entered: "Entrou",
                updated: "Atualizado",
                suppressed: "Repetido",
                exited: "Saiu"
            };

            return (
                traducoes[status]
                || status
                || "Detectado"
            );
        }

        function atualizarResumo() {
            pessoasAtivasElemento.textContent =
                pessoasAtivas;

            pessoasSairamElemento.textContent =
                pessoasSairam;

            totalPessoasElemento.textContent =
                totalPessoas;

            totalEventosElemento.textContent =
                totalEventos;
        }

        function registrarMudancaStatus(
            pessoaId,
            novoStatus
        ) {
            const statusAnterior =
                statusPorPessoa.get(
                    pessoaId
                );

            if (!statusAnterior) {
                totalPessoas += 1;

                if (novoStatus === "exited") {
                    pessoasSairam += 1;
                } else {
                    pessoasAtivas += 1;
                }
            }

            else if (
                statusAnterior !== "exited"
                && novoStatus === "exited"
            ) {
                pessoasAtivas = Math.max(
                    0,
                    pessoasAtivas - 1
                );

                pessoasSairam += 1;
            }

            else if (
                statusAnterior === "exited"
                && novoStatus !== "exited"
            ) {
                pessoasSairam = Math.max(
                    0,
                    pessoasSairam - 1
                );

                pessoasAtivas += 1;
            }

            statusPorPessoa.set(
                pessoaId,
                novoStatus
            );

            atualizarResumo();
        }

        function conectarWebSocket() {
            const protocolo =
                window.location.protocol === "https:"
                    ? "wss"
                    : "ws";

            const endereco =
                `${protocolo}://${window.location.host}/ws/eventos`;

            const websocket =
                new WebSocket(endereco);

            websocket.onopen = function () {
                statusTexto.textContent =
                    "Conectado";

                statusBolinha.className =
                    "status-bolinha conectado";

                if (heartbeatId) {
                    clearInterval(
                        heartbeatId
                    );
                }

                heartbeatId = setInterval(
                    function () {
                        if (
                            websocket.readyState
                            === WebSocket.OPEN
                        ) {
                            websocket.send(
                                "ping"
                            );
                        }
                    },
                    20000
                );
            };

            websocket.onmessage =
                function (mensagem) {
                    try {
                        const evento =
                            JSON.parse(
                                mensagem.data
                            );

                        processarEvento(
                            evento
                        );

                    } catch (erro) {
                        console.error(
                            "Erro ao interpretar evento:",
                            erro
                        );
                    }
                };

            websocket.onclose = function () {
                statusTexto.textContent =
                    "Desconectado — reconectando...";

                statusBolinha.className =
                    "status-bolinha desconectado";

                if (heartbeatId) {
                    clearInterval(
                        heartbeatId
                    );

                    heartbeatId = null;
                }

                setTimeout(
                    conectarWebSocket,
                    2000
                );
            };

            websocket.onerror = function () {
                websocket.close();
            };
        }

        function processarEvento(evento) {
            if (
                evento.status
                === "appearance_updated"
            ) {
                atualizarEventoParcial(
                    evento
                );

                return;
            }

            if (
                evento.status
                === "suppressed"
            ) {
                return;
            }

            const pessoaId =
                evento.pessoa_id
                || evento.evento_id;

            if (!pessoaId) {
                console.warn(
                    "Evento sem identificador:",
                    evento
                );

                return;
            }

            const cartaoExistente =
                cartoesPorPessoa.get(
                    pessoaId
                );

            if (
                evento.status === "exited"
                && !cartaoExistente
            ) {
                return;
            }

            registrarMudancaStatus(
                pessoaId,
                evento.status || "entered"
            );

            totalEventos += 1;
            atualizarResumo();

            if (cartaoExistente) {
                atualizarCartao(
                    cartaoExistente,
                    evento
                );

                return;
            }

            criarCartao(
                pessoaId,
                evento
            );
        }

        function criarElemento(
            tag,
            campo,
            classe = ""
        ) {
            const elemento =
                document.createElement(
                    tag
                );

            if (campo) {
                elemento.dataset.campo =
                    campo;
            }

            if (classe) {
                elemento.className =
                    classe;
            }

            return elemento;
        }

        function obterCamposCard(card) {
            return {
                blocoRecorte: card.querySelector(
                    '[data-campo="bloco-recorte"]'
                ),

                imagemRecorte: card.querySelector(
                    '[data-campo="imagem-recorte"]'
                ),

                blocoCena: card.querySelector(
                    '[data-campo="bloco-cena"]'
                ),

                tituloCena: card.querySelector(
                    '[data-campo="titulo-cena"]'
                ),

                quantidadeBoxes: card.querySelector(
                    '[data-campo="quantidade-boxes"]'
                ),

                imagemCena: card.querySelector(
                    '[data-campo="imagem-cena"]'
                ),

                camera: card.querySelector(
                    '[data-campo="camera"]'
                ),

                status: card.querySelector(
                    '[data-campo="status"]'
                ),

                tipo: card.querySelector(
                    '[data-campo="tipo"]'
                ),

                horario: card.querySelector(
                    '[data-campo="horario"]'
                ),

                pessoaId: card.querySelector(
                    '[data-campo="pessoa-id"]'
                ),

                quantidade: card.querySelector(
                    '[data-campo="quantidade"]'
                ),

                aparencia: card.querySelector(
                    '[data-campo="aparencia"]'
                ),

                corRoupa: card.querySelector(
                    '[data-campo="cor-roupa"]'
                ),

                posicao: card.querySelector(
                    '[data-campo="posicao"]'
                ),

                tamanho: card.querySelector(
                    '[data-campo="tamanho"]'
                ),

                percentual: card.querySelector(
                    '[data-campo="percentual"]'
                ),

                quantidadeAmostras: card.querySelector(
                    '[data-campo="quantidade-amostras"]'
                ),

                descricaoAparencia: card.querySelector(
                    '[data-campo="descricao-aparencia"]'
                ),

                movimento: card.querySelector(
                    '[data-campo="movimento"]'
                ),

                movimentoHorizontal: card.querySelector(
                    '[data-campo="movimento-horizontal"]'
                ),

                movimentoVertical: card.querySelector(
                    '[data-campo="movimento-vertical"]'
                ),

                movimentoTendencia: card.querySelector(
                    '[data-campo="movimento-tendencia"]'
                ),

                movimentoVelocidade: card.querySelector(
                    '[data-campo="movimento-velocidade"]'
                ),

                movimentoDistancia: card.querySelector(
                    '[data-campo="movimento-distancia"]'
                ),

                movimentoTempo: card.querySelector(
                    '[data-campo="movimento-tempo"]'
                ),

                movimentoAmostras: card.querySelector(
                    '[data-campo="movimento-amostras"]'
                ),

                movimentoDescricao: card.querySelector(
                    '[data-campo="movimento-descricao"]'
                ),

                contexto: card.querySelector(
                    '[data-campo="contexto"]'
                ),

                contextoQuantidade: card.querySelector(
                    '[data-campo="contexto-quantidade"]'
                ),

                contextoDistribuicao: card.querySelector(
                    '[data-campo="contexto-distribuicao"]'
                ),

                contextoProximidades: card.querySelector(
                    '[data-campo="contexto-proximidades"]'
                ),

                contextoPessoas: card.querySelector(
                    '[data-campo="contexto-pessoas"]'
                ),

                contextoDescricao: card.querySelector(
                    '[data-campo="contexto-descricao"]'
                )
            };
        }

        function atualizarEventoParcial(
            evento
        ) {
            const pessoaId =
                evento.pessoa_id
                || evento.evento_id;

            const card =
                cartoesPorPessoa.get(
                    pessoaId
                );

            if (!card) {
                console.warn(
                    "Atualização recebida sem cartão:",
                    pessoaId
                );

                return;
            }

            const campos =
                obterCamposCard(card);

            campos.quantidade.textContent =
                `Detecções: ${
                    evento.quantidade_deteccoes
                    || 1
                }`;

            if (evento.imagem) {
                preencherRecorte(
                    evento.imagem,
                    campos
                );
            }

            if (evento.aparencia) {
                preencherAparencia(
                    evento.aparencia,
                    campos
                );
            }

            if (evento.movimento) {
                preencherMovimento(
                    evento.movimento,
                    campos
                );
            }

            if (evento.contexto_cena) {
                preencherContextoCena(
                    evento.contexto_cena,
                    campos
                );
            }

            if (evento.imagem_cena) {
                preencherCenaCompleta(
                    evento.imagem_cena,
                    evento.quantidade_boxes_cena,
                    campos
                );
            }

            card.classList.add(
                "atualizado"
            );

            setTimeout(
                function () {
                    card.classList.remove(
                        "atualizado"
                    );
                },
                1000
            );
        }

        function criarCartao(
            pessoaId,
            evento
        ) {
            const mensagemVazia =
                document.getElementById(
                    "mensagem-vazia"
                );

            if (mensagemVazia) {
                mensagemVazia.remove();
            }

            const card =
                criarElemento(
                    "article",
                    "",
                    "evento-card"
                );

            card.dataset.pessoaId =
                pessoaId;

            const imagensEvento =
                criarElemento(
                    "section",
                    "",
                    "imagens-evento"
                );

            const blocoRecorte =
                criarElemento(
                    "div",
                    "bloco-recorte",
                    "imagem-bloco"
                );

            const tituloRecorte =
                criarElemento(
                    "div",
                    "",
                    "imagem-titulo"
                );

            tituloRecorte.textContent =
                "Recorte individual";

            const imagemRecorte =
                criarElemento(
                    "img",
                    "imagem-recorte",
                    "imagem-evento imagem-recorte"
                );

            imagemRecorte.alt =
                "Recorte individual da pessoa";

            blocoRecorte.appendChild(
                tituloRecorte
            );

            blocoRecorte.appendChild(
                imagemRecorte
            );

            const blocoCena =
                criarElemento(
                    "div",
                    "bloco-cena",
                    "imagem-bloco"
                );

            const cabecalhoCena =
                criarElemento(
                    "div",
                    "",
                    "imagem-titulo"
                );

            const tituloCena =
                criarElemento(
                    "span",
                    "titulo-cena"
                );

            tituloCena.textContent =
                "Cena completa";

            const quantidadeBoxes =
                criarElemento(
                    "span",
                    "quantidade-boxes",
                    "imagem-contagem"
                );

            quantidadeBoxes.textContent =
                "0 detecções";

            cabecalhoCena.appendChild(
                tituloCena
            );

            cabecalhoCena.appendChild(
                quantidadeBoxes
            );

            const imagemCena =
                criarElemento(
                    "img",
                    "imagem-cena",
                    "imagem-evento imagem-cena"
                );

            imagemCena.alt =
                "Cena completa com as detecções";

            blocoCena.appendChild(
                cabecalhoCena
            );

            blocoCena.appendChild(
                imagemCena
            );

            imagensEvento.appendChild(
                blocoRecorte
            );

            imagensEvento.appendChild(
                blocoCena
            );

            const dados =
                criarElemento(
                    "div",
                    "",
                    "evento-dados"
                );

            const cabecalho =
                criarElemento(
                    "div",
                    "",
                    "evento-cabecalho"
                );

            const titulo =
                criarElemento(
                    "h2",
                    "camera"
                );

            const status =
                criarElemento(
                    "span",
                    "status",
                    "evento-status"
                );

            cabecalho.appendChild(
                titulo
            );

            cabecalho.appendChild(
                status
            );

            const tipo =
                criarElemento(
                    "p",
                    "tipo"
                );

            const horario =
                criarElemento(
                    "p",
                    "horario"
                );

            const identificadorPessoa =
                criarElemento(
                    "p",
                    "pessoa-id"
                );

            const quantidade =
                criarElemento(
                    "p",
                    "quantidade"
                );

            const aparencia =
                criarElemento(
                    "section",
                    "aparencia",
                    "aparencia-visual"
                );

            const aparenciaTitulo =
                criarElemento(
                    "h3",
                    "",
                    "secao-titulo"
                );

            aparenciaTitulo.textContent =
                "Aparência estável";

            const corRoupa =
                criarElemento(
                    "p",
                    "cor-roupa"
                );

            const posicao =
                criarElemento(
                    "p",
                    "posicao"
                );

            const tamanho =
                criarElemento(
                    "p",
                    "tamanho"
                );

            const percentual =
                criarElemento(
                    "p",
                    "percentual"
                );

            const quantidadeAmostras =
                criarElemento(
                    "p",
                    "quantidade-amostras"
                );

            const descricaoAparencia =
                criarElemento(
                    "p",
                    "descricao-aparencia",
                    "descricao"
                );

            aparencia.appendChild(
                aparenciaTitulo
            );

            aparencia.appendChild(
                corRoupa
            );

            aparencia.appendChild(
                posicao
            );

            aparencia.appendChild(
                tamanho
            );

            aparencia.appendChild(
                percentual
            );

            aparencia.appendChild(
                quantidadeAmostras
            );

            aparencia.appendChild(
                descricaoAparencia
            );

            const movimento =
                criarElemento(
                    "section",
                    "movimento",
                    "movimento-pessoa"
                );

            const movimentoTitulo =
                criarElemento(
                    "h3",
                    "",
                    "secao-titulo"
                );

            movimentoTitulo.textContent =
                "Movimento e permanência";

            const movimentoHorizontal =
                criarElemento(
                    "p",
                    "movimento-horizontal"
                );

            const movimentoVertical =
                criarElemento(
                    "p",
                    "movimento-vertical"
                );

            const movimentoTendencia =
                criarElemento(
                    "p",
                    "movimento-tendencia"
                );

            const movimentoVelocidade =
                criarElemento(
                    "p",
                    "movimento-velocidade"
                );

            const movimentoDistancia =
                criarElemento(
                    "p",
                    "movimento-distancia"
                );

            const movimentoTempo =
                criarElemento(
                    "p",
                    "movimento-tempo"
                );

            const movimentoAmostras =
                criarElemento(
                    "p",
                    "movimento-amostras"
                );

            const movimentoDescricao =

===== ARQUIVOS DO PAINEL =====

------------------------------------------------------------
ARQUIVO: app/services/appearance_memory.py
------------------------------------------------------------
import asyncio
from collections import Counter
from dataclasses import asdict, dataclass, field

from app.services.scene_analyzer import (
    PersonVisualAnalysis,
)


@dataclass(frozen=True)
class StableAppearance:
    cor_roupa_predominante: str

    rgb_medio: tuple[
        int,
        int,
        int,
    ]

    posicao_atual: str
    tamanho_predominante: str
    percentual_medio_quadro: float
    quantidade_amostras: int
    descricao: str

    def to_dict(self) -> dict:
        dados = asdict(self)

        dados["rgb_medio"] = list(
            self.rgb_medio
        )

        return dados


@dataclass
class _AppearanceState:
    cores: Counter[str] = field(
        default_factory=Counter
    )

    tamanhos: Counter[str] = field(
        default_factory=Counter
    )

    soma_vermelho: int = 0
    soma_verde: int = 0
    soma_azul: int = 0

    soma_percentual: float = 0.0
    quantidade_amostras: int = 0

    ultima_cor: str = "indefinida"

    # Cor apresentada como resultado estável.
    # É separada da última leitura para evitar
    # alternância causada por iluminação e sombra.
    cor_estavel: str = "indefinida"

    ultima_posicao: str = "centro"
    ultimo_tamanho: str = "medio"


class AppearanceMemory:
    def __init__(self) -> None:
        self._sessoes: dict[
            str,
            _AppearanceState,
        ] = {}

        self._lock = asyncio.Lock()

    @property
    def quantidade_sessoes(self) -> int:
        return len(self._sessoes)

    async def registrar(
        self,
        pessoa_id: str,
        analise: PersonVisualAnalysis,
    ) -> StableAppearance:
        if not pessoa_id:
            raise ValueError(
                "pessoa_id não pode ser vazio"
            )

        async with self._lock:
            estado = self._sessoes.get(
                pessoa_id
            )

            if estado is None:
                estado = _AppearanceState()

                self._sessoes[
                    pessoa_id
                ] = estado

            estado.cores[
                analise.cor_roupa_aproximada
            ] += 1

            estado.tamanhos[
                analise.tamanho_no_quadro
            ] += 1

            vermelho, verde, azul = (
                analise.rgb_representativo
            )

            estado.soma_vermelho += vermelho
            estado.soma_verde += verde
            estado.soma_azul += azul

            estado.soma_percentual += (
                analise.percentual_quadro
            )

            estado.quantidade_amostras += 1

            estado.ultima_cor = (
                analise.cor_roupa_aproximada
            )

            estado.ultima_posicao = (
                analise.posicao_horizontal
            )

            estado.ultimo_tamanho = (
                analise.tamanho_no_quadro
            )

            return self._montar_resultado(
                estado
            )

    async def obter(
        self,
        pessoa_id: str,
    ) -> StableAppearance | None:
        async with self._lock:
            estado = self._sessoes.get(
                pessoa_id
            )

            if estado is None:
                return None

            return self._montar_resultado(
                estado
            )

    async def finalizar(
        self,
        pessoa_id: str,
    ) -> StableAppearance | None:
        """
        Retorna a aparência final e remove a sessão
        daquela pessoa da memória.
        """

        async with self._lock:
            estado = self._sessoes.pop(
                pessoa_id,
                None,
            )

            if estado is None:
                return None

            return self._montar_resultado(
                estado
            )

    async def limpar(self) -> None:
        async with self._lock:
            self._sessoes.clear()

    def _montar_resultado(
        self,
        estado: _AppearanceState,
    ) -> StableAppearance:
        quantidade = max(
            1,
            estado.quantidade_amostras,
        )

        cor_calculada = (
            self._selecionar_cor_predominante(
                estado
            )
        )

        maior_quantidade_cor = max(
            estado.cores.values(),
            default=0,
        )

        # Confirma a cor depois de duas leituras
        # iguais. Com mais amostras, exige pelo menos
        # 60% de concordância.
        cor_consistente = (
            quantidade >= 2
            and maior_quantidade_cor >= 2
            and (
                maior_quantidade_cor * 5
                >= quantidade * 3
            )
        )

        # A roupa não muda durante uma passagem curta.
        # Depois que a cor fica estável, leituras
        # isoladas diferentes não apagam o resultado.
        if (
            estado.cor_estavel
            != "indefinida"
        ):
            cor_predominante = (
                estado.cor_estavel
            )

        elif cor_consistente:
            cor_predominante = (
                cor_calculada
            )

            estado.cor_estavel = (
                cor_predominante
            )

        else:
            cor_predominante = (
                "indefinida"
            )

        tamanho_predominante = (
            self._selecionar_predominante(
                contagem=estado.tamanhos,
                valor_mais_recente=(
                    estado.ultimo_tamanho
                ),
            )
        )

        rgb_medio = (
            round(
                estado.soma_vermelho
                / quantidade
            ),
            round(
                estado.soma_verde
                / quantidade
            ),
            round(
                estado.soma_azul
                / quantidade
            ),
        )

        percentual_medio = round(
            estado.soma_percentual
            / quantidade,
            2,
        )

        descricao = self._montar_descricao(
            cor=cor_predominante,
            posicao=estado.ultima_posicao,
            tamanho=tamanho_predominante,
            quantidade=quantidade,
        )

        return StableAppearance(
            cor_roupa_predominante=(
                cor_predominante
            ),
            rgb_medio=rgb_medio,
            posicao_atual=(
                estado.ultima_posicao
            ),
            tamanho_predominante=(
                tamanho_predominante
            ),
            percentual_medio_quadro=(
                percentual_medio
            ),
            quantidade_amostras=quantidade,
            descricao=descricao,
        )

    def _selecionar_cor_predominante(
        self,
        estado: _AppearanceState,
    ) -> str:
        """
        Estabiliza a classificação de roupas escuras
        que alternam entre preta e uma cor escura
        devido à iluminação, distância ou sombra.
        """

        contagem = estado.cores

        if not contagem:
            return estado.ultima_cor

        resultado_padrao = (
            self._selecionar_predominante(
                contagem=contagem,
                valor_mais_recente=(
                    estado.ultima_cor
                ),
            )
        )

        cores_escuras = (
            "azul-escura",
            "verde-escura",
            "vermelha-escura",
            "roxa-escura",
        )

        quantidade_preta = contagem.get(
            "preta",
            0,
        )

        candidatas = [
            (
                cor,
                contagem.get(cor, 0),
            )
            for cor in cores_escuras
            if contagem.get(cor, 0) > 0
        ]

        if (
            quantidade_preta == 0
            or not candidatas
        ):
            return resultado_padrao

        maior_quantidade_escura = max(
            quantidade
            for _, quantidade in candidatas
        )

        empatadas = [
            cor
            for cor, quantidade in candidatas
            if (
                quantidade
                == maior_quantidade_escura
            )
        ]

        if estado.cor_estavel in empatadas:
            cor_escura = estado.cor_estavel
        elif estado.ultima_cor in empatadas:
            cor_escura = estado.ultima_cor
        else:
            cor_escura = empatadas[0]

        cores_do_conflito = (
            set(cores_escuras)
            | {"preta"}
        )

        maior_outra_cor = max(
            (
                quantidade
                for cor, quantidade
                in contagem.items()
                if cor not in cores_do_conflito
            ),
            default=0,
        )

        if maior_outra_cor > max(
            quantidade_preta,
            maior_quantidade_escura,
        ):
            return resultado_padrao

        # Já estabilizou em azul-escura, verde-escura
        # ou outra cor escura. Um quadro preto isolado
        # não deve alterar imediatamente o resultado.
        if estado.cor_estavel in cores_escuras:
            cor_estavel = estado.cor_estavel

            quantidade_cor_estavel = (
                contagem.get(
                    cor_estavel,
                    0,
                )
            )

            # Permite mudar entre cores escuras apenas
            # quando a nova cor estiver duas amostras
            # à frente da atual.
            if (
                cor_escura != cor_estavel
                and maior_quantidade_escura
                >= quantidade_cor_estavel + 2
            ):
                cor_estavel = cor_escura
                quantidade_cor_estavel = (
                    maior_quantidade_escura
                )

            # Volta para preta somente quando as
            # leituras pretas estiverem duas amostras
            # à frente da cor escura estabilizada.
            if (
                quantidade_preta
                >= quantidade_cor_estavel + 2
            ):
                return "preta"

            return cor_estavel

        # Estava estabilizado como preto. Exige duas
        # leituras da cor escura e empate ou vantagem
        # para realizar a mudança.
        if estado.cor_estavel == "preta":
            if (
                maior_quantidade_escura >= 2
                and maior_quantidade_escura
                >= quantidade_preta
            ):
                return cor_escura

            return "preta"

        # Estado inicial, antes de uma cor estável.
        if (
            maior_quantidade_escura >= 2
            and maior_quantidade_escura
            >= quantidade_preta
        ):
            return cor_escura

        return resultado_padrao


    @staticmethod
    def _selecionar_predominante(
        contagem: Counter[str],
        valor_mais_recente: str,
    ) -> str:
        if not contagem:
            return valor_mais_recente

        maior_quantidade = max(
            contagem.values()
        )

        empatados = [
            valor
            for valor, quantidade
            in contagem.items()
            if quantidade == maior_quantidade
        ]

        # Em caso de empate, utiliza o valor
        # observado mais recentemente.
        if valor_mais_recente in empatados:
            return valor_mais_recente

        return empatados[0]

    @staticmethod
    def _montar_descricao(
        cor: str,
        posicao: str,
        tamanho: str,
        quantidade: int,
    ) -> str:
        posicoes = {
            "esquerda": "à esquerda",
            "centro": "no centro",
            "direita": "à direita",
        }

        tamanhos = {
            "pequeno": "pequeno",
            "medio": "médio",
            "grande": "grande",
        }

        posicao_formatada = posicoes.get(
            posicao,
            posicao,
        )

        tamanho_formatado = tamanhos.get(
            tamanho,
            tamanho,
        )

        return (
            "Pessoa com roupa predominantemente "
            f"{cor}, localizada atualmente "
            f"{posicao_formatada} da cena, "
            f"com tamanho aparente "
            f"{tamanho_formatado}. "
            f"Resultado baseado em "
            f"{quantidade} amostra(s)."
        )


appearance_memory = AppearanceMemory()
------------------------------------------------------------
ARQUIVO: app/services/camera_event_pipeline_backup_amostras_repetidas.py
------------------------------------------------------------
import asyncio
import logging
from datetime import datetime, timezone

from app.adapters.cameras.factory import (
    camera_adapter_factory,
)
from app.core.config import settings
from app.domain.models.camera_event import (
    RawCameraPackage,
)
from app.messaging.kafka_producer import (
    KafkaPublisher,
)
from app.services.alarm_detection_payload import (
    montar_alarm_detection_message,
)
from app.services.appearance_memory import (
    appearance_memory,
)
from app.services.event_hub import event_hub
from app.services.person_tracker import (
    DetectionBox,
    person_tracker,
)
from app.services.scene_analyzer import (
    analisar_pessoa,
)


logger = logging.getLogger(__name__)


class CameraEventPipeline:
    def __init__(
        self,
        publisher: KafkaPublisher,
        topic: str,
        maxsize: int = 1000,
        worker_count: int = 4,
    ) -> None:
        self.publisher = publisher
        self.topic = topic
        self.worker_count = worker_count

        self.queue: asyncio.Queue[
            tuple[RawCameraPackage, bytes]
        ] = asyncio.Queue(maxsize=maxsize)

        self._worker_tasks: list[
            asyncio.Task
        ] = []

        self._exit_task: (
            asyncio.Task | None
        ) = None

        self._iniciado = False

    @property
    def tamanho_fila(self) -> int:
        return self.queue.qsize()

    @property
    def capacidade_fila(self) -> int:
        return self.queue.maxsize

    async def start(self) -> None:
        if self._iniciado:
            return

        if settings.kafka_enabled:
            await self.publisher.start()
        else:
            logger.warning(
                "Kafka desativado: eventos serão "
                "processados, mas não publicados."
            )

        self._worker_tasks = [
            asyncio.create_task(
                self._worker(numero),
                name=f"camera-worker-{numero}",
            )
            for numero in range(
                1,
                self.worker_count + 1,
            )
        ]

        self._exit_task = asyncio.create_task(
            self._monitorar_saidas(),
            name="person-exit-monitor",
        )

        self._iniciado = True

        logger.info(
            "Pipeline iniciado com %s workers",
            self.worker_count,
        )

    async def stop(self) -> None:
        if not self._iniciado:
            return

        try:
            await asyncio.wait_for(
                self.queue.join(),
                timeout=10,
            )

        except TimeoutError:
            logger.warning(
                "Encerrando com %s itens na fila",
                self.queue.qsize(),
            )

        tarefas: list[asyncio.Task] = [
            *self._worker_tasks,
        ]

        if self._exit_task is not None:
            tarefas.append(
                self._exit_task
            )

        for tarefa in tarefas:
            tarefa.cancel()

        if tarefas:
            await asyncio.gather(
                *tarefas,
                return_exceptions=True,
            )

        if self.publisher.iniciado:
            await self.publisher.stop()

        await appearance_memory.limpar()

        self._worker_tasks.clear()
        self._exit_task = None
        self._iniciado = False

        logger.info(
            "Pipeline encerrado"
        )

    def adicionar(
        self,
        pacote: RawCameraPackage,
        body: bytes,
    ) -> None:
        self.queue.put_nowait(
            (pacote, body)
        )

    async def _monitorar_saidas(
        self,
    ) -> None:
        logger.info(
            "Monitor automático de saídas iniciado"
        )

        while True:
            try:
                await asyncio.sleep(1)

                saidas = (
                    await person_tracker
                    .coletar_saidas()
                )

                for saida in saidas:
                    aparencia_final = (
                        await appearance_memory
                        .finalizar(
                            saida.pessoa_id
                        )
                    )

                    momento_saida = (
                        datetime.now(
                            timezone.utc
                        )
                        .isoformat()
                        .replace(
                            "+00:00",
                            "Z",
                        )
                    )

                    evento_painel = {
                        "evento_id": (
                            saida.evento_id
                        ),
                        "pessoa_id": (
                            saida.pessoa_id
                        ),
                        "status": "exited",
                        "quantidade_deteccoes": (
                            saida
                            .quantidade_deteccoes
                        ),
                        "camera": saida.camera,
                        "tipo": None,
                        "datetime": momento_saida,
                        "imagem": None,
                        "aparencia": (
                            aparencia_final.to_dict()
                            if aparencia_final
                            is not None
                            else None
                        ),
                    }

                    await event_hub.publicar(
                        evento_painel
                    )

                    logger.info(
                        "[RASTREAMENTO] Pessoa saiu: "
                        "pessoa=%s camera=%s "
                        "deteccoes=%s amostras=%s "
                        "paineis=%s",
                        saida.pessoa_id,
                        saida.camera,
                        (
                            saida
                            .quantidade_deteccoes
                        ),
                        (
                            aparencia_final
                            .quantidade_amostras
                            if aparencia_final
                            is not None
                            else 0
                        ),
                        event_hub.total_conexoes,
                    )

            except asyncio.CancelledError:
                logger.info(
                    "Monitor automático de "
                    "saídas encerrado"
                )
                raise

            except Exception:
                logger.exception(
                    "Erro no monitor automático "
                    "de saídas"
                )

    async def _worker(
        self,
        numero: int,
    ) -> None:
        logger.info(
            "Worker %s iniciado",
            numero,
        )

        while True:
            pacote, body = (
                await self.queue.get()
            )

            try:
                adapter = (
                    camera_adapter_factory
                    .encontrar_adapter(
                        content_type=(
                            pacote.content_type
                        ),
                        body=body,
                    )
                )

                logger.info(
                    "[%s] Worker %s usando %s",
                    pacote.evento_id,
                    numero,
                    adapter.__class__.__name__,
                )

                evento = await asyncio.to_thread(
                    adapter.normalizar,
                    pacote,
                    body,
                )

                if evento.imagem is None:
                    logger.info(
                        "[%s] Evento ignorado: "
                        "não possui imagem",
                        pacote.evento_id,
                    )
                    continue

                bounding_box = (
                    evento
                    .bounding_box_escolhida
                )

                if bounding_box is None:
                    logger.info(
                        "[%s] Evento ignorado: "
                        "não possui bounding box",
                        pacote.evento_id,
                    )
                    continue

                camera = (
                    evento.nome_camera
                    or evento.camera_id
                    or evento.ip_camera
                    or "Camera desconhecida"
                )

                caixa_rastreamento = (
                    DetectionBox(
                        x=bounding_box.x,
                        y=bounding_box.y,
                        largura=(
                            bounding_box.largura
                        ),
                        altura=(
                            bounding_box.altura
                        ),
                    )
                )

                decisao = (
                    await person_tracker.registrar(
                        camera=camera,
                        evento_id=(
                            evento.evento_id
                        ),
                        bbox=(
                            caixa_rastreamento
                        ),
                    )
                )

                logger.info(
                    "[%s] Rastreamento: "
                    "pessoa=%s status=%s "
                    "deteccoes=%s",
                    pacote.evento_id,
                    decisao.pessoa_id,
                    decisao.status,
                    (
                        decisao
                        .quantidade_deteccoes
                    ),
                )

                # Eventos repetidos em intervalos
                # curtos param aqui.
                if not decisao.deve_processar:
                    logger.info(
                        "[%s] Evento repetido "
                        "suprimido: pessoa=%s",
                        pacote.evento_id,
                        decisao.pessoa_id,
                    )
                    continue

                aparencia_estavel = None

                try:
                    caminho_original = (
                        evento
                        .imagem
                        .caminho_original
                    )

                    if caminho_original:
                        analise_visual = (
                            await asyncio.to_thread(
                                analisar_pessoa,
                                caminho_imagem=(
                                    caminho_original
                                ),
                                x=bounding_box.x,
                                y=bounding_box.y,
                                largura=(
                                    bounding_box
                                    .largura
                                ),
                                altura=(
                                    bounding_box
                                    .altura
                                ),
                            )
                        )

                        aparencia_estavel = (
                            await appearance_memory
                            .registrar(
                                pessoa_id=(
                                    decisao.pessoa_id
                                ),
                                analise=(
                                    analise_visual
                                ),
                            )
                        )

                        logger.info(
                            "[%s] Aparência estável: "
                            "pessoa=%s cor=%s "
                            "posição=%s tamanho=%s "
                            "área_média=%s%% "
                            "amostras=%s",
                            pacote.evento_id,
                            decisao.pessoa_id,
                            (
                                aparencia_estavel
                                .cor_roupa_predominante
                            ),
                            (
                                aparencia_estavel
                                .posicao_atual
                            ),
                            (
                                aparencia_estavel
                                .tamanho_predominante
                            ),
                            (
                                aparencia_estavel
                                .percentual_medio_quadro
                            ),
                            (
                                aparencia_estavel
                                .quantidade_amostras
                            ),
                        )

                except Exception:
                    logger.exception(
                        "[%s] Não foi possível "
                        "estabilizar a aparência",
                        pacote.evento_id,
                    )

                try:
                    mensagem = (
                        await asyncio.to_thread(
                            montar_alarm_detection_message,
                            evento,
                        )
                    )

                except ValueError as erro:
                    logger.info(
                        "[%s] Evento ignorado: %s",
                        pacote.evento_id,
                        erro,
                    )
                    continue

                evento_painel = {
                    "evento_id": (
                        evento.evento_id
                    ),
                    "pessoa_id": (
                        decisao.pessoa_id
                    ),
                    "status": decisao.status,
                    "quantidade_deteccoes": (
                        decisao
                        .quantidade_deteccoes
                    ),
                    "camera": (
                        mensagem.device.name
                    ),
                    "tipo": (
                        mensagem.event.type
                    ),
                    "datetime": (
                        mensagem.event.datetime
                    ),
                    "imagem": (
                        mensagem
                        .event
                        .images
                        .detection
                    ),
                    "aparencia": (
                        aparencia_estavel.to_dict()
                        if aparencia_estavel
                        is not None
                        else None
                    ),
                }

                await event_hub.publicar(
                    evento_painel
                )

                logger.info(
                    "[%s] Evento enviado para "
                    "%s painel(is)",
                    pacote.evento_id,
                    event_hub.total_conexoes,
                )

                if not settings.kafka_enabled:
                    logger.info(
                        "[%s] Payload pronto; "
                        "Kafka desativado",
                        pacote.evento_id,
                    )
                    continue

                # O payload do Kafka permanece
                # no formato solicitado.
                resultado = (
                    await self.publisher.publicar(
                        topico=self.topic,
                        evento_id=(
                            evento.evento_id
                        ),
                        dados=(
                            mensagem.model_dump(
                                mode="json"
                            )
                        ),
                    )
                )

                logger.info(
                    "[%s] Publicado em %s "
                    "p=%s offset=%s",
                    pacote.evento_id,
                    resultado["topico"],
                    resultado["particao"],
                    resultado["offset"],
                )

            except asyncio.CancelledError:
                raise

            except Exception:
                logger.exception(
                    "[%s] Erro no processamento",
                    pacote.evento_id,
                )

            finally:
                self.queue.task_done()
------------------------------------------------------------
ARQUIVO: app/services/camera_event_pipeline_backup_analise_visual.py
------------------------------------------------------------
import asyncio
import logging
from datetime import datetime, timezone

from app.adapters.cameras.factory import camera_adapter_factory
from app.core.config import settings
from app.domain.models.camera_event import RawCameraPackage
from app.messaging.kafka_producer import KafkaPublisher
from app.services.alarm_detection_payload import (
    montar_alarm_detection_message,
)
from app.services.event_hub import event_hub
from app.services.person_tracker import (
    DetectionBox,
    person_tracker,
)


logger = logging.getLogger(__name__)


class CameraEventPipeline:
    def __init__(
        self,
        publisher: KafkaPublisher,
        topic: str,
        maxsize: int = 1000,
        worker_count: int = 4,
    ) -> None:
        self.publisher = publisher
        self.topic = topic
        self.worker_count = worker_count

        self.queue: asyncio.Queue[
            tuple[RawCameraPackage, bytes]
        ] = asyncio.Queue(maxsize=maxsize)

        self._worker_tasks: list[asyncio.Task] = []
        self._exit_task: asyncio.Task | None = None
        self._iniciado = False

    @property
    def tamanho_fila(self) -> int:
        return self.queue.qsize()

    @property
    def capacidade_fila(self) -> int:
        return self.queue.maxsize

    async def start(self) -> None:
        if self._iniciado:
            return

        if settings.kafka_enabled:
            await self.publisher.start()
        else:
            logger.warning(
                "Kafka desativado: eventos serão processados, "
                "mas não publicados."
            )

        self._worker_tasks = [
            asyncio.create_task(
                self._worker(numero),
                name=f"camera-worker-{numero}",
            )
            for numero in range(
                1,
                self.worker_count + 1,
            )
        ]

        self._exit_task = asyncio.create_task(
            self._monitorar_saidas(),
            name="person-exit-monitor",
        )

        self._iniciado = True

        logger.info(
            "Pipeline iniciado com %s workers",
            self.worker_count,
        )

    async def stop(self) -> None:
        if not self._iniciado:
            return

        try:
            await asyncio.wait_for(
                self.queue.join(),
                timeout=10,
            )

        except TimeoutError:
            logger.warning(
                "Encerrando com %s itens na fila",
                self.queue.qsize(),
            )

        tarefas: list[asyncio.Task] = [
            *self._worker_tasks,
        ]

        if self._exit_task is not None:
            tarefas.append(self._exit_task)

        for tarefa in tarefas:
            tarefa.cancel()

        if tarefas:
            await asyncio.gather(
                *tarefas,
                return_exceptions=True,
            )

        if self.publisher.iniciado:
            await self.publisher.stop()

        self._worker_tasks.clear()
        self._exit_task = None
        self._iniciado = False

        logger.info("Pipeline encerrado")

    def adicionar(
        self,
        pacote: RawCameraPackage,
        body: bytes,
    ) -> None:
        self.queue.put_nowait(
            (pacote, body)
        )

    async def _monitorar_saidas(self) -> None:
        """
        Verifica a cada segundo quais pessoas estão há mais
        de oito segundos sem receber uma nova detecção.
        """

        logger.info(
            "Monitor automático de saídas iniciado"
        )

        while True:
            try:
                await asyncio.sleep(1)

                saidas = (
                    await person_tracker.coletar_saidas()
                )

                for saida in saidas:
                    momento_saida = (
                        datetime.now(timezone.utc)
                        .isoformat()
                        .replace("+00:00", "Z")
                    )

                    evento_painel = {
                        "evento_id": saida.evento_id,
                        "pessoa_id": saida.pessoa_id,
                        "status": "exited",
                        "quantidade_deteccoes": (
                            saida.quantidade_deteccoes
                        ),
                        "camera": saida.camera,
                        "tipo": None,
                        "datetime": momento_saida,
                        "imagem": None,
                    }

                    await event_hub.publicar(
                        evento_painel
                    )

                    logger.info(
                        "[RASTREAMENTO] Pessoa saiu: "
                        "pessoa=%s camera=%s "
                        "deteccoes=%s paineis=%s",
                        saida.pessoa_id,
                        saida.camera,
                        saida.quantidade_deteccoes,
                        event_hub.total_conexoes,
                    )

            except asyncio.CancelledError:
                logger.info(
                    "Monitor automático de saídas encerrado"
                )
                raise

            except Exception:
                logger.exception(
                    "Erro no monitor automático de saídas"
                )

    async def _worker(
        self,
        numero: int,
    ) -> None:
        logger.info(
            "Worker %s iniciado",
            numero,
        )

        while True:
            pacote, body = await self.queue.get()

            try:
                adapter = (
                    camera_adapter_factory.encontrar_adapter(
                        content_type=pacote.content_type,
                        body=body,
                    )
                )

                logger.info(
                    "[%s] Worker %s usando %s",
                    pacote.evento_id,
                    numero,
                    adapter.__class__.__name__,
                )

                evento = await asyncio.to_thread(
                    adapter.normalizar,
                    pacote,
                    body,
                )

                if evento.imagem is None:
                    logger.info(
                        "[%s] Evento ignorado: "
                        "não possui imagem",
                        pacote.evento_id,
                    )
                    continue

                bounding_box = (
                    evento.bounding_box_escolhida
                )

                if bounding_box is None:
                    logger.info(
                        "[%s] Evento ignorado: "
                        "não possui bounding box",
                        pacote.evento_id,
                    )
                    continue

                camera = (
                    evento.nome_camera
                    or evento.camera_id
                    or evento.ip_camera
                    or "Camera desconhecida"
                )

                caixa_rastreamento = DetectionBox(
                    x=bounding_box.x,
                    y=bounding_box.y,
                    largura=bounding_box.largura,
                    altura=bounding_box.altura,
                )

                decisao = await person_tracker.registrar(
                    camera=camera,
                    evento_id=evento.evento_id,
                    bbox=caixa_rastreamento,
                )

                logger.info(
                    "[%s] Rastreamento: "
                    "pessoa=%s status=%s "
                    "deteccoes=%s",
                    pacote.evento_id,
                    decisao.pessoa_id,
                    decisao.status,
                    decisao.quantidade_deteccoes,
                )

                # Eventos repetidos em menos de cinco segundos
                # não geram Base64, painel ou Kafka.
                if not decisao.deve_processar:
                    logger.info(
                        "[%s] Evento repetido suprimido: "
                        "pessoa=%s",
                        pacote.evento_id,
                        decisao.pessoa_id,
                    )
                    continue

                try:
                    mensagem = await asyncio.to_thread(
                        montar_alarm_detection_message,
                        evento,
                    )

                except ValueError as erro:
                    logger.info(
                        "[%s] Evento ignorado: %s",
                        pacote.evento_id,
                        erro,
                    )
                    continue

                evento_painel = {
                    "evento_id": evento.evento_id,
                    "pessoa_id": decisao.pessoa_id,
                    "status": decisao.status,
                    "quantidade_deteccoes": (
                        decisao.quantidade_deteccoes
                    ),
                    "camera": mensagem.device.name,
                    "tipo": mensagem.event.type,
                    "datetime": mensagem.event.datetime,
                    "imagem": (
                        mensagem.event.images.detection
                    ),
                }

                await event_hub.publicar(
                    evento_painel
                )

                logger.info(
                    "[%s] Evento enviado para "
                    "%s painel(is)",
                    pacote.evento_id,
                    event_hub.total_conexoes,
                )

                if not settings.kafka_enabled:
                    logger.info(
                        "[%s] Payload pronto; "
                        "Kafka desativado",
                        pacote.evento_id,
                    )
                    continue

                resultado = (
                    await self.publisher.publicar(
                        topico=self.topic,
                        evento_id=evento.evento_id,
                        dados=mensagem.model_dump(
                            mode="json"
                        ),
                    )
                )

                logger.info(
                    "[%s] Publicado em %s "
                    "p=%s offset=%s",
                    pacote.evento_id,
                    resultado["topico"],
                    resultado["particao"],
                    resultado["offset"],
                )

            except asyncio.CancelledError:
                raise

            except Exception:
                logger.exception(
                    "[%s] Erro no processamento",
                    pacote.evento_id,
                )

            finally:
                self.queue.task_done()
------------------------------------------------------------
ARQUIVO: app/services/camera_event_pipeline_backup_antes_analise.py
------------------------------------------------------------
import asyncio
import logging
from datetime import datetime, timezone

from app.adapters.cameras.factory import camera_adapter_factory
from app.core.config import settings
from app.domain.models.camera_event import RawCameraPackage
from app.messaging.kafka_producer import KafkaPublisher
from app.services.alarm_detection_payload import (
    montar_alarm_detection_message,
)
from app.services.event_hub import event_hub
from app.services.person_tracker import (
    DetectionBox,
    person_tracker,
)


logger = logging.getLogger(__name__)


class CameraEventPipeline:
    def __init__(
        self,
        publisher: KafkaPublisher,
        topic: str,
        maxsize: int = 1000,
        worker_count: int = 4,
    ) -> None:
        self.publisher = publisher
        self.topic = topic
        self.worker_count = worker_count

        self.queue: asyncio.Queue[
            tuple[RawCameraPackage, bytes]
        ] = asyncio.Queue(maxsize=maxsize)

        self._worker_tasks: list[asyncio.Task] = []
        self._exit_task: asyncio.Task | None = None
        self._iniciado = False

    @property
    def tamanho_fila(self) -> int:
        return self.queue.qsize()

    @property
    def capacidade_fila(self) -> int:
        return self.queue.maxsize

    async def start(self) -> None:
        if self._iniciado:
            return

        if settings.kafka_enabled:
            await self.publisher.start()
        else:
            logger.warning(
                "Kafka desativado: eventos serão processados, "
                "mas não publicados."
            )

        self._worker_tasks = [
            asyncio.create_task(
                self._worker(numero),
                name=f"camera-worker-{numero}",
            )
            for numero in range(
                1,
                self.worker_count + 1,
            )
        ]

        self._exit_task = asyncio.create_task(
            self._monitorar_saidas(),
            name="person-exit-monitor",
        )

        self._iniciado = True

        logger.info(
            "Pipeline iniciado com %s workers",
            self.worker_count,
        )

    async def stop(self) -> None:
        if not self._iniciado:
            return

        try:
            await asyncio.wait_for(
                self.queue.join(),
                timeout=10,
            )

        except TimeoutError:
            logger.warning(
                "Encerrando com %s itens na fila",
                self.queue.qsize(),
            )

        tarefas: list[asyncio.Task] = [
            *self._worker_tasks,
        ]

        if self._exit_task is not None:
            tarefas.append(self._exit_task)

        for tarefa in tarefas:
            tarefa.cancel()

        if tarefas:
            await asyncio.gather(
                *tarefas,
                return_exceptions=True,
            )

        if self.publisher.iniciado:
            await self.publisher.stop()

        self._worker_tasks.clear()
        self._exit_task = None
        self._iniciado = False

        logger.info("Pipeline encerrado")

    def adicionar(
        self,
        pacote: RawCameraPackage,
        body: bytes,
    ) -> None:
        self.queue.put_nowait(
            (pacote, body)
        )

    async def _monitorar_saidas(self) -> None:
        """
        Verifica a cada segundo quais pessoas estão há mais
        de oito segundos sem receber uma nova detecção.
        """

        logger.info(
            "Monitor automático de saídas iniciado"
        )

        while True:
            try:
                await asyncio.sleep(1)

                saidas = (
                    await person_tracker.coletar_saidas()
                )

                for saida in saidas:
                    momento_saida = (
                        datetime.now(timezone.utc)
                        .isoformat()
                        .replace("+00:00", "Z")
                    )

                    evento_painel = {
                        "evento_id": saida.evento_id,
                        "pessoa_id": saida.pessoa_id,
                        "status": "exited",
                        "quantidade_deteccoes": (
                            saida.quantidade_deteccoes
                        ),
                        "camera": saida.camera,
                        "tipo": None,
                        "datetime": momento_saida,
                        "imagem": None,
                    }

                    await event_hub.publicar(
                        evento_painel
                    )

                    logger.info(
                        "[RASTREAMENTO] Pessoa saiu: "
                        "pessoa=%s camera=%s "
                        "deteccoes=%s paineis=%s",
                        saida.pessoa_id,
                        saida.camera,
                        saida.quantidade_deteccoes,
                        event_hub.total_conexoes,
                    )

            except asyncio.CancelledError:
                logger.info(
                    "Monitor automático de saídas encerrado"
                )
                raise

            except Exception:
                logger.exception(
                    "Erro no monitor automático de saídas"
                )

    async def _worker(
        self,
        numero: int,
    ) -> None:
        logger.info(
            "Worker %s iniciado",
            numero,
        )

        while True:
            pacote, body = await self.queue.get()

            try:
                adapter = (
                    camera_adapter_factory.encontrar_adapter(
                        content_type=pacote.content_type,
                        body=body,
                    )
                )

                logger.info(
                    "[%s] Worker %s usando %s",
                    pacote.evento_id,
                    numero,
                    adapter.__class__.__name__,
                )

                evento = await asyncio.to_thread(
                    adapter.normalizar,
                    pacote,
                    body,
                )

                if evento.imagem is None:
                    logger.info(
                        "[%s] Evento ignorado: "
                        "não possui imagem",
                        pacote.evento_id,
                    )
                    continue

                bounding_box = (
                    evento.bounding_box_escolhida
                )

                if bounding_box is None:
                    logger.info(
                        "[%s] Evento ignorado: "
                        "não possui bounding box",
                        pacote.evento_id,
                    )
                    continue

                camera = (
                    evento.nome_camera
                    or evento.camera_id
                    or evento.ip_camera
                    or "Camera desconhecida"
                )

                caixa_rastreamento = DetectionBox(
                    x=bounding_box.x,
                    y=bounding_box.y,
                    largura=bounding_box.largura,
                    altura=bounding_box.altura,
                )

                decisao = await person_tracker.registrar(
                    camera=camera,
                    evento_id=evento.evento_id,
                    bbox=caixa_rastreamento,
                )

                logger.info(
                    "[%s] Rastreamento: "
                    "pessoa=%s status=%s "
                    "deteccoes=%s",
                    pacote.evento_id,
                    decisao.pessoa_id,
                    decisao.status,
                    decisao.quantidade_deteccoes,
                )

                # Eventos repetidos em menos de cinco segundos
                # não geram Base64, painel ou Kafka.
                if not decisao.deve_processar:
                    logger.info(
                        "[%s] Evento repetido suprimido: "
                        "pessoa=%s",
                        pacote.evento_id,
                        decisao.pessoa_id,
                    )
                    continue

                try:
                    mensagem = await asyncio.to_thread(
                        montar_alarm_detection_message,
                        evento,
                    )

                except ValueError as erro:
                    logger.info(
                        "[%s] Evento ignorado: %s",
                        pacote.evento_id,
                        erro,
                    )
                    continue

                evento_painel = {
                    "evento_id": evento.evento_id,
                    "pessoa_id": decisao.pessoa_id,
                    "status": decisao.status,
                    "quantidade_deteccoes": (
                        decisao.quantidade_deteccoes
                    ),
                    "camera": mensagem.device.name,
                    "tipo": mensagem.event.type,
                    "datetime": mensagem.event.datetime,
                    "imagem": (
                        mensagem.event.images.detection
                    ),
                }

                await event_hub.publicar(
                    evento_painel
                )

                logger.info(
                    "[%s] Evento enviado para "
                    "%s painel(is)",
                    pacote.evento_id,
                    event_hub.total_conexoes,
                )

                if not settings.kafka_enabled:
                    logger.info(
                        "[%s] Payload pronto; "
                        "Kafka desativado",
                        pacote.evento_id,
                    )
                    continue

                resultado = (
                    await self.publisher.publicar(
                        topico=self.topic,
                        evento_id=evento.evento_id,
                        dados=mensagem.model_dump(
                            mode="json"
                        ),
                    )
                )

                logger.info(
                    "[%s] Publicado em %s "
                    "p=%s offset=%s",
                    pacote.evento_id,
                    resultado["topico"],
                    resultado["particao"],
                    resultado["offset"],
                )

            except asyncio.CancelledError:
                raise

            except Exception:
                logger.exception(
                    "[%s] Erro no processamento",
                    pacote.evento_id,
                )

            finally:
                self.queue.task_done()
------------------------------------------------------------
ARQUIVO: app/services/camera_event_pipeline_backup_antes_movimento.py
------------------------------------------------------------
import asyncio
import logging
from datetime import datetime, timezone

from app.adapters.cameras.factory import camera_adapter_factory
from app.core.config import settings
from app.domain.models.camera_event import BoundingBox, CameraEvent, RawCameraPackage
from app.messaging.kafka_producer import KafkaPublisher
from app.services.alarm_detection_payload import (
    arquivo_para_base64,
    montar_alarm_detection_message,
)
from app.services.appearance_memory import appearance_memory
from app.services.event_hub import event_hub
from app.services.person_tracker import (
    DetectionBox,
    TrackingDecision,
    person_tracker,
)
from app.services.scene_analyzer import analisar_pessoa
from app.services.scene_context_analyzer import analisar_contexto_cena
from app.services.scene_renderer import renderizar_cena_com_boxes

logger = logging.getLogger(__name__)


class CameraEventPipeline:
    def __init__(
        self,
        publisher: KafkaPublisher,
        topic: str,
        maxsize: int = 1000,
        worker_count: int = 4,
    ) -> None:
        self.publisher = publisher
        self.topic = topic
        self.worker_count = worker_count
        self.queue: asyncio.Queue[tuple[RawCameraPackage, bytes]] = asyncio.Queue(
            maxsize=maxsize
        )
        self._worker_tasks: list[asyncio.Task] = []
        self._exit_task: asyncio.Task | None = None
        self._iniciado = False

    @property
    def tamanho_fila(self) -> int:
        return self.queue.qsize()

    @property
    def capacidade_fila(self) -> int:
        return self.queue.maxsize

    async def start(self) -> None:
        if self._iniciado:
            return

        if settings.kafka_enabled:
            await self.publisher.start()
        else:
            logger.warning(
                "Kafka desativado: eventos serão processados, mas não publicados."
            )

        self._worker_tasks = [
            asyncio.create_task(
                self._worker(numero),
                name=f"camera-worker-{numero}",
            )
            for numero in range(1, self.worker_count + 1)
        ]
        self._exit_task = asyncio.create_task(
            self._monitorar_saidas(),
            name="person-exit-monitor",
        )
        self._iniciado = True
        logger.info("Pipeline iniciado com %s workers", self.worker_count)

    async def stop(self) -> None:
        if not self._iniciado:
            return

        try:
            await asyncio.wait_for(self.queue.join(), timeout=10)
        except TimeoutError:
            logger.warning("Encerrando com %s itens na fila", self.queue.qsize())

        tarefas: list[asyncio.Task] = [*self._worker_tasks]
        if self._exit_task is not None:
            tarefas.append(self._exit_task)

        for tarefa in tarefas:
            tarefa.cancel()

        if tarefas:
            await asyncio.gather(*tarefas, return_exceptions=True)

        if self.publisher.iniciado:
            await self.publisher.stop()

        await appearance_memory.limpar()
        await person_tracker.limpar()

        self._worker_tasks.clear()
        self._exit_task = None
        self._iniciado = False
        logger.info("Pipeline encerrado")

    def adicionar(self, pacote: RawCameraPackage, body: bytes) -> None:
        self.queue.put_nowait((pacote, body))

    async def _monitorar_saidas(self) -> None:
        logger.info("Monitor automático de saídas iniciado")

        while True:
            try:
                await asyncio.sleep(1)
                saidas = await person_tracker.coletar_saidas()

                for saida in saidas:
                    aparencia_final = await appearance_memory.finalizar(
                        saida.pessoa_id
                    )
                    momento_saida = (
                        datetime.now(timezone.utc)
                        .isoformat()
                        .replace("+00:00", "Z")
                    )
                    evento_painel = {
                        "evento_id": saida.evento_id,
                        "pessoa_id": saida.pessoa_id,
                        "status": "exited",
                        "quantidade_deteccoes": saida.quantidade_deteccoes,
                        "camera": saida.camera,
                        "tipo": None,
                        "datetime": momento_saida,
                        "imagem": None,
                        "aparencia": (
                            aparencia_final.to_dict()
                            if aparencia_final is not None
                            else None
                        ),
                        "contexto_cena": None,
                        "imagem_cena": None,
                        "quantidade_boxes_cena": None,
                    }
                    await event_hub.publicar(evento_painel)
                    logger.info(
                        "[RASTREAMENTO] Pessoa saiu: pessoa=%s camera=%s "
                        "deteccoes=%s amostras=%s paineis=%s",
                        saida.pessoa_id,
                        saida.camera,
                        saida.quantidade_deteccoes,
                        (
                            aparencia_final.quantidade_amostras
                            if aparencia_final is not None
                            else 0
                        ),
                        event_hub.total_conexoes,
                    )

            except asyncio.CancelledError:
                logger.info("Monitor automático de saídas encerrado")
                raise
            except Exception:
                logger.exception("Erro no monitor automático de saídas")

    def _selecionar_boxes_pessoas(
        self,
        evento: CameraEvent,
    ) -> list[BoundingBox]:
        """Remove caixas inválidas, duplicadas e a caixa da imagem inteira."""

        if evento.imagem is None:
            return []

        largura_imagem = evento.imagem.largura
        altura_imagem = evento.imagem.altura
        if not largura_imagem or not altura_imagem:
            return []

        area_imagem = largura_imagem * altura_imagem
        assinaturas: set[tuple[int, int, int, int]] = set()
        caixas_validas: list[BoundingBox] = []

        for caixa in evento.bounding_boxes or []:
            if caixa.largura <= 0 or caixa.altura <= 0:
                continue

            x1 = max(0, min(caixa.x, largura_imagem - 1))
            y1 = max(0, min(caixa.y, altura_imagem - 1))
            x2 = max(x1 + 1, min(caixa.x2, largura_imagem))
            y2 = max(y1 + 1, min(caixa.y2, altura_imagem))
            largura = x2 - x1
            altura = y2 - y1
            percentual = largura * altura / area_imagem * 100

            if percentual >= 60 or percentual < 0.05:
                continue

            assinatura = (x1, y1, x2, y2)
            if assinatura in assinaturas:
                continue
            assinaturas.add(assinatura)

            if (
                x1 == caixa.x
                and y1 == caixa.y
                and largura == caixa.largura
                and altura == caixa.altura
            ):
                caixa_ajustada = caixa
            else:
                caixa_ajustada = caixa.model_copy(
                    update={
                        "x": x1,
                        "y": y1,
                        "largura": largura,
                        "altura": altura,
                        "x2": x2,
                        "y2": y2,
                    }
                )

            caixas_validas.append(caixa_ajustada)

        if not caixas_validas and evento.bounding_box_escolhida is not None:
            caixas_validas.append(evento.bounding_box_escolhida)

        caixas_validas.sort(
            key=lambda caixa: (
                caixa.x + caixa.largura / 2,
                caixa.y + caixa.altura / 2,
            )
        )
        return caixas_validas

    async def _analisar_aparencia(
        self,
        evento: CameraEvent,
        bounding_box: BoundingBox,
        pessoa_id: str,
        pacote_id: str,
    ):
        try:
            if evento.imagem is None or not evento.imagem.caminho_original:
                logger.info(
                    "[%s] Imagem sem caminho original; aparência não analisada",
                    pacote_id,
                )
                return None

            analise_visual = await asyncio.to_thread(
                analisar_pessoa,
                caminho_imagem=evento.imagem.caminho_original,
                x=bounding_box.x,
                y=bounding_box.y,
                largura=bounding_box.largura,
                altura=bounding_box.altura,
            )
            aparencia_estavel = await appearance_memory.registrar(
                pessoa_id=pessoa_id,
                analise=analise_visual,
            )
            logger.info(
                "[%s] Aparência estável: pessoa=%s cor=%s posição=%s "
                "tamanho=%s área_média=%s%% amostras=%s",
                pacote_id,
                pessoa_id,
                aparencia_estavel.cor_roupa_predominante,
                aparencia_estavel.posicao_atual,
                aparencia_estavel.tamanho_predominante,
                aparencia_estavel.percentual_medio_quadro,
                aparencia_estavel.quantidade_amostras,
            )
            return aparencia_estavel
        except Exception:
            logger.exception(
                "[%s] Não foi possível analisar a aparência da pessoa=%s",
                pacote_id,
                pessoa_id,
            )
            return None

    async def _analisar_contexto_cena(
        self,
        evento: CameraEvent,
        bounding_boxes: list[BoundingBox],
        pacote_id: str,
    ):
        try:
            if evento.imagem is None:
                return None

            largura_imagem = evento.imagem.largura
            altura_imagem = evento.imagem.altura
            if not largura_imagem or not altura_imagem:
                logger.info(
                    "[%s] Contexto não analisado: dimensões ausentes",
                    pacote_id,
                )
                return None

            contexto = await asyncio.to_thread(
                analisar_contexto_cena,
                largura_imagem=largura_imagem,
                altura_imagem=altura_imagem,
                bounding_boxes=bounding_boxes,
            )
            logger.info(
                "[%s] Contexto da cena: pessoas=%s esquerda=%s centro=%s "
                "direita=%s muito_proximos=%s proximos=%s separados=%s",
                pacote_id,
                contexto.quantidade_pessoas,
                contexto.pessoas_esquerda,
                contexto.pessoas_centro,
                contexto.pessoas_direita,
                contexto.pares_muito_proximos,
                contexto.pares_proximos,
                contexto.pares_separados,
            )
            logger.info("[%s] Descrição da cena: %s", pacote_id, contexto.descricao)
            return contexto
        except Exception:
            logger.exception("[%s] Não foi possível analisar a cena", pacote_id)
            return None

    async def _renderizar_cena(
        self,
        evento: CameraEvent,
        bounding_boxes: list[BoundingBox],
        pacote_id: str,
    ):
        try:
            if evento.imagem is None or not evento.imagem.caminho_original:
                return None
            if not bounding_boxes:
                return None

            resultado = await asyncio.to_thread(
                renderizar_cena_com_boxes,
                caminho_imagem=evento.imagem.caminho_original,
                bounding_boxes=bounding_boxes,
            )
            if resultado.quantidade_boxes == 0:
                return None

            logger.info(
                "[%s] Cena renderizada: boxes=%s dimensoes=%sx%s base64=%s caracteres",
                pacote_id,
                resultado.quantidade_boxes,
                resultado.largura_imagem,
                resultado.altura_imagem,
                len(resultado.imagem_base64),
            )
            return resultado
        except Exception:
            logger.exception(
                "[%s] Não foi possível renderizar a cena com boxes",
                pacote_id,
            )
            return None

    async def _publicar_atualizacao_parcial(
        self,
        camera: str,
        decisao: TrackingDecision,
        aparencia_estavel,
        contexto_cena,
        cena_renderizada,
        indice_na_cena: int,
        total_pessoas_cena: int,
        pacote_id: str,
    ) -> None:
        atualizacao_parcial = {
            "evento_id": decisao.evento_id,
            "pessoa_id": decisao.pessoa_id,
            "status": "appearance_updated",
            "quantidade_deteccoes": decisao.quantidade_deteccoes,
            "camera": camera,
            "aparencia": (
                aparencia_estavel.to_dict()
                if aparencia_estavel is not None
                else None
            ),
            "contexto_cena": (
                contexto_cena.to_dict() if contexto_cena is not None else None
            ),
            "imagem_cena": (
                cena_renderizada.imagem_base64
                if cena_renderizada is not None
                else None
            ),
            "quantidade_boxes_cena": (
                cena_renderizada.quantidade_boxes
                if cena_renderizada is not None
                else 0
            ),
            "indice_na_cena": indice_na_cena,
            "total_pessoas_cena": total_pessoas_cena,
        }
        await event_hub.publicar(atualizacao_parcial)
        logger.info(
            "[%s] Atualização parcial: pessoa=%s indice=%s/%s "
            "deteccoes=%s amostras=%s boxes=%s paineis=%s",
            pacote_id,
            decisao.pessoa_id,
            indice_na_cena,
            total_pessoas_cena,
            decisao.quantidade_deteccoes,
            (
                aparencia_estavel.quantidade_amostras
                if aparencia_estavel is not None
                else 0
            ),
            (
                cena_renderizada.quantidade_boxes
                if cena_renderizada is not None
                else 0
            ),
            event_hub.total_conexoes,
        )

    async def _processar_pessoa(
        self,
        evento: CameraEvent,
        camera: str,
        bounding_box: BoundingBox,
        decisao: TrackingDecision,
        contexto_cena,
        cena_renderizada,
        indice_na_cena: int,
        total_pessoas_cena: int,
        pacote_id: str,
        imagem_background_base64: str | None,
    ) -> str | None:
        aparencia_estavel = await self._analisar_aparencia(
            evento=evento,
            bounding_box=bounding_box,
            pessoa_id=decisao.pessoa_id,
            pacote_id=pacote_id,
        )

        if not decisao.deve_processar:
            await self._publicar_atualizacao_parcial(
                camera=camera,
                decisao=decisao,
                aparencia_estavel=aparencia_estavel,
                contexto_cena=contexto_cena,
                cena_renderizada=cena_renderizada,
                indice_na_cena=indice_na_cena,
                total_pessoas_cena=total_pessoas_cena,
                pacote_id=pacote_id,
            )
            logger.info(
                "[%s] Evento repetido suprimido: pessoa=%s indice=%s/%s amostras=%s",
                pacote_id,
                decisao.pessoa_id,
                indice_na_cena,
                total_pessoas_cena,
                (
                    aparencia_estavel.quantidade_amostras
                    if aparencia_estavel is not None
                    else 0
                ),
            )
            return imagem_background_base64

        if evento.imagem is None or not evento.imagem.caminho_original:
            logger.info("[%s] Pessoa ignorada: caminho original ausente", pacote_id)
            return imagem_background_base64

        if imagem_background_base64 is None:
            imagem_background_base64 = await asyncio.to_thread(
                arquivo_para_base64,
                evento.imagem.caminho_original,
            )

        try:
            mensagem = await asyncio.to_thread(
                montar_alarm_detection_message,
                evento,
                bounding_box=bounding_box,
                evento_id=decisao.evento_id,
                imagem_background_base64=imagem_background_base64,
            )
        except ValueError as erro:
            logger.info(
                "[%s] Pessoa ignorada: pessoa=%s erro=%s",
                pacote_id,
                decisao.pessoa_id,
                erro,
            )
            return imagem_background_base64

        evento_painel = {
            "evento_id": decisao.evento_id,
            "pessoa_id": decisao.pessoa_id,
            "status": decisao.status,
            "quantidade_deteccoes": decisao.quantidade_deteccoes,
            "camera": mensagem.device.name,
            "tipo": mensagem.event.type,
            "datetime": mensagem.event.datetime,
            "imagem": mensagem.event.images.detection,
            "aparencia": (
                aparencia_estavel.to_dict()
                if aparencia_estavel is not None
                else None
            ),
            "contexto_cena": (
                contexto_cena.to_dict() if contexto_cena is not None else None
            ),
            "imagem_cena": (
                cena_renderizada.imagem_base64
                if cena_renderizada is not None
                else None
            ),
            "quantidade_boxes_cena": (
                cena_renderizada.quantidade_boxes
                if cena_renderizada is not None
                else 0
            ),
            "indice_na_cena": indice_na_cena,
            "total_pessoas_cena": total_pessoas_cena,
        }
        await event_hub.publicar(evento_painel)
        logger.info(
            "[%s] Pessoa enviada ao painel: pessoa=%s status=%s indice=%s/%s paineis=%s",
            pacote_id,
            decisao.pessoa_id,
            decisao.status,
            indice_na_cena,
            total_pessoas_cena,
            event_hub.total_conexoes,
        )

        if not settings.kafka_enabled:
            logger.info(
                "[%s] Payload pronto; Kafka desativado: pessoa=%s",
                pacote_id,
                decisao.pessoa_id,
            )
            return imagem_background_base64

        resultado = await self.publisher.publicar(
            topico=self.topic,
            evento_id=decisao.evento_id,
            dados=mensagem.model_dump(mode="json"),
        )
        logger.info(
            "[%s] Pessoa publicada em %s p=%s offset=%s pessoa=%s indice=%s/%s",
            pacote_id,
            resultado["topico"],
            resultado["particao"],
            resultado["offset"],
            decisao.pessoa_id,
            indice_na_cena,
            total_pessoas_cena,
        )
        return imagem_background_base64

    async def _worker(self, numero: int) -> None:
        logger.info("Worker %s iniciado", numero)

        while True:
            pacote, body = await self.queue.get()

            try:
                adapter = camera_adapter_factory.encontrar_adapter(
                    content_type=pacote.content_type,
                    body=body,
                )
                logger.info(
                    "[%s] Worker %s usando %s",
                    pacote.evento_id,
                    numero,
                    adapter.__class__.__name__,
                )

                evento = await asyncio.to_thread(adapter.normalizar, pacote, body)
                if evento.imagem is None:
                    logger.info(
                        "[%s] Evento ignorado: não possui imagem",
                        pacote.evento_id,
                    )
                    continue

                bounding_boxes = self._selecionar_boxes_pessoas(evento)
                if not bounding_boxes:
                    logger.info(
                        "[%s] Evento ignorado: não possui boxes válidas de pessoas",
                        pacote.evento_id,
                    )
                    continue

                camera = (
                    evento.nome_camera
                    or evento.camera_id
                    or evento.ip_camera
                    or "Camera desconhecida"
                )
                caixas_rastreamento = [
                    DetectionBox(
                        x=caixa.x,
                        y=caixa.y,
                        largura=caixa.largura,
                        altura=caixa.altura,
                    )
                    for caixa in bounding_boxes
                ]
                decisoes = await person_tracker.registrar_lote(
                    camera=camera,
                    evento_id=evento.evento_id,
                    bboxes=caixas_rastreamento,
                )

                if len(decisoes) != len(bounding_boxes):
                    raise RuntimeError(
                        "Quantidade de decisões diferente da quantidade de boxes"
                    )

                total_pessoas = len(bounding_boxes)
                logger.info(
                    "[%s] Rastreamento em lote: pessoas=%s ids=%s",
                    pacote.evento_id,
                    total_pessoas,
                    [
                        (decisao.pessoa_id, decisao.status)
                        for decisao in decisoes
                    ],
                )

                contexto_cena = await self._analisar_contexto_cena(
                    evento=evento,
                    bounding_boxes=bounding_boxes,
                    pacote_id=pacote.evento_id,
                )
                cena_renderizada = await self._renderizar_cena(
                    evento=evento,
                    bounding_boxes=bounding_boxes,
                    pacote_id=pacote.evento_id,
                )

                imagem_background_base64: str | None = None
                for indice, (bounding_box, decisao) in enumerate(
                    zip(bounding_boxes, decisoes, strict=True),
                    start=1,
                ):
                    imagem_background_base64 = await self._processar_pessoa(
                        evento=evento,
                        camera=camera,
                        bounding_box=bounding_box,
                        decisao=decisao,
                        contexto_cena=contexto_cena,
                        cena_renderizada=cena_renderizada,
                        indice_na_cena=indice,
                        total_pessoas_cena=total_pessoas,
                        pacote_id=pacote.evento_id,
                        imagem_background_base64=imagem_background_base64,
                    )

            except asyncio.CancelledError:
                raise
            except Exception:
                logger.exception("[%s] Erro no processamento", pacote.evento_id)
            finally:
                self.queue.task_done()

------------------------------------------------------------
ARQUIVO: app/services/camera_event_pipeline_backup_antes_multiplas_pessoas.py
------------------------------------------------------------
import asyncio
import logging
from datetime import datetime, timezone

from app.adapters.cameras.factory import (
    camera_adapter_factory,
)
from app.core.config import settings
from app.domain.models.camera_event import (
    RawCameraPackage,
)
from app.messaging.kafka_producer import (
    KafkaPublisher,
)
from app.services.alarm_detection_payload import (
    montar_alarm_detection_message,
)
from app.services.appearance_memory import (
    appearance_memory,
)
from app.services.event_hub import event_hub
from app.services.person_tracker import (
    DetectionBox,
    person_tracker,
)
from app.services.scene_analyzer import (
    analisar_pessoa,
)
from app.services.scene_context_analyzer import (
    analisar_contexto_cena,
)
from app.services.scene_renderer import (
    renderizar_cena_com_boxes,
)


logger = logging.getLogger(__name__)


class CameraEventPipeline:
    def __init__(
        self,
        publisher: KafkaPublisher,
        topic: str,
        maxsize: int = 1000,
        worker_count: int = 4,
    ) -> None:
        self.publisher = publisher
        self.topic = topic
        self.worker_count = worker_count

        self.queue: asyncio.Queue[
            tuple[RawCameraPackage, bytes]
        ] = asyncio.Queue(maxsize=maxsize)

        self._worker_tasks: list[asyncio.Task] = []
        self._exit_task: asyncio.Task | None = None
        self._iniciado = False

    @property
    def tamanho_fila(self) -> int:
        return self.queue.qsize()

    @property
    def capacidade_fila(self) -> int:
        return self.queue.maxsize

    async def start(self) -> None:
        if self._iniciado:
            return

        if settings.kafka_enabled:
            await self.publisher.start()
        else:
            logger.warning(
                "Kafka desativado: eventos serão "
                "processados, mas não publicados."
            )

        self._worker_tasks = [
            asyncio.create_task(
                self._worker(numero),
                name=f"camera-worker-{numero}",
            )
            for numero in range(
                1,
                self.worker_count + 1,
            )
        ]

        self._exit_task = asyncio.create_task(
            self._monitorar_saidas(),
            name="person-exit-monitor",
        )

        self._iniciado = True

        logger.info(
            "Pipeline iniciado com %s workers",
            self.worker_count,
        )

    async def stop(self) -> None:
        if not self._iniciado:
            return

        try:
            await asyncio.wait_for(
                self.queue.join(),
                timeout=10,
            )

        except TimeoutError:
            logger.warning(
                "Encerrando com %s itens na fila",
                self.queue.qsize(),
            )

        tarefas: list[asyncio.Task] = [
            *self._worker_tasks,
        ]

        if self._exit_task is not None:
            tarefas.append(
                self._exit_task
            )

        for tarefa in tarefas:
            tarefa.cancel()

        if tarefas:
            await asyncio.gather(
                *tarefas,
                return_exceptions=True,
            )

        if self.publisher.iniciado:
            await self.publisher.stop()

        await appearance_memory.limpar()

        self._worker_tasks.clear()
        self._exit_task = None
        self._iniciado = False

        logger.info(
            "Pipeline encerrado"
        )

    def adicionar(
        self,
        pacote: RawCameraPackage,
        body: bytes,
    ) -> None:
        self.queue.put_nowait(
            (pacote, body)
        )

    async def _monitorar_saidas(
        self,
    ) -> None:
        logger.info(
            "Monitor automático de saídas iniciado"
        )

        while True:
            try:
                await asyncio.sleep(1)

                saidas = (
                    await person_tracker
                    .coletar_saidas()
                )

                for saida in saidas:
                    aparencia_final = (
                        await appearance_memory
                        .finalizar(
                            saida.pessoa_id
                        )
                    )

                    momento_saida = (
                        datetime.now(
                            timezone.utc
                        )
                        .isoformat()
                        .replace(
                            "+00:00",
                            "Z",
                        )
                    )

                    evento_painel = {
                        "evento_id": saida.evento_id,
                        "pessoa_id": saida.pessoa_id,
                        "status": "exited",
                        "quantidade_deteccoes": (
                            saida.quantidade_deteccoes
                        ),
                        "camera": saida.camera,
                        "tipo": None,
                        "datetime": momento_saida,
                        "imagem": None,
                        "aparencia": (
                            aparencia_final.to_dict()
                            if aparencia_final is not None
                            else None
                        ),
                        "contexto_cena": None,

                        # O painel manterá a última
                        # cena recebida no cartão.
                        "imagem_cena": None,
                        "quantidade_boxes_cena": None,
                    }

                    await event_hub.publicar(
                        evento_painel
                    )

                    logger.info(
                        "[RASTREAMENTO] Pessoa saiu: "
                        "pessoa=%s camera=%s "
                        "deteccoes=%s amostras=%s "
                        "paineis=%s",
                        saida.pessoa_id,
                        saida.camera,
                        saida.quantidade_deteccoes,
                        (
                            aparencia_final
                            .quantidade_amostras
                            if aparencia_final is not None
                            else 0
                        ),
                        event_hub.total_conexoes,
                    )

            except asyncio.CancelledError:
                logger.info(
                    "Monitor automático de "
                    "saídas encerrado"
                )
                raise

            except Exception:
                logger.exception(
                    "Erro no monitor automático "
                    "de saídas"
                )

    async def _analisar_aparencia(
        self,
        evento,
        bounding_box,
        pessoa_id: str,
        pacote_id: str,
    ):
        try:
            if evento.imagem is None:
                return None

            caminho_original = (
                evento.imagem.caminho_original
            )

            if not caminho_original:
                logger.info(
                    "[%s] Imagem sem caminho "
                    "original; aparência não analisada",
                    pacote_id,
                )
                return None

            analise_visual = await asyncio.to_thread(
                analisar_pessoa,
                caminho_imagem=caminho_original,
                x=bounding_box.x,
                y=bounding_box.y,
                largura=bounding_box.largura,
                altura=bounding_box.altura,
            )

            aparencia_estavel = (
                await appearance_memory.registrar(
                    pessoa_id=pessoa_id,
                    analise=analise_visual,
                )
            )

            logger.info(
                "[%s] Aparência estável: "
                "pessoa=%s cor=%s posição=%s "
                "tamanho=%s área_média=%s%% "
                "amostras=%s",
                pacote_id,
                pessoa_id,
                (
                    aparencia_estavel
                    .cor_roupa_predominante
                ),
                aparencia_estavel.posicao_atual,
                (
                    aparencia_estavel
                    .tamanho_predominante
                ),
                (
                    aparencia_estavel
                    .percentual_medio_quadro
                ),
                (
                    aparencia_estavel
                    .quantidade_amostras
                ),
            )

            return aparencia_estavel

        except Exception:
            logger.exception(
                "[%s] Não foi possível analisar "
                "ou estabilizar a aparência",
                pacote_id,
            )

            return None

    async def _analisar_contexto_cena(
        self,
        evento,
        pacote_id: str,
    ):
        try:
            if evento.imagem is None:
                return None

            largura_imagem = (
                evento.imagem.largura
            )

            altura_imagem = (
                evento.imagem.altura
            )

            if (
                not largura_imagem
                or not altura_imagem
            ):
                logger.info(
                    "[%s] Contexto não analisado: "
                    "dimensões da imagem ausentes",
                    pacote_id,
                )
                return None

            bounding_boxes = (
                evento.bounding_boxes
                or []
            )

            contexto = await asyncio.to_thread(
                analisar_contexto_cena,
                largura_imagem=largura_imagem,
                altura_imagem=altura_imagem,
                bounding_boxes=bounding_boxes,
            )

            logger.info(
                "[%s] Contexto da cena: "
                "pessoas=%s esquerda=%s "
                "centro=%s direita=%s "
                "muito_proximos=%s "
                "proximos=%s separados=%s",
                pacote_id,
                contexto.quantidade_pessoas,
                contexto.pessoas_esquerda,
                contexto.pessoas_centro,
                contexto.pessoas_direita,
                contexto.pares_muito_proximos,
                contexto.pares_proximos,
                contexto.pares_separados,
            )

            logger.info(
                "[%s] Descrição da cena: %s",
                pacote_id,
                contexto.descricao,
            )

            return contexto

        except Exception:
            logger.exception(
                "[%s] Não foi possível analisar "
                "o contexto da cena",
                pacote_id,
            )

            return None

    async def _renderizar_cena(
        self,
        evento,
        pacote_id: str,
    ):
        """
        Desenha as bounding boxes reais da câmera
        sobre a imagem original.

        A imagem resultante é enviada somente ao
        painel e não altera o payload do Kafka.
        """

        try:
            if evento.imagem is None:
                return None

            caminho_original = (
                evento.imagem.caminho_original
            )

            if not caminho_original:
                logger.info(
                    "[%s] Cena não renderizada: "
                    "caminho original ausente",
                    pacote_id,
                )
                return None

            bounding_boxes = (
                evento.bounding_boxes
                or []
            )

            if not bounding_boxes:
                logger.info(
                    "[%s] Cena não renderizada: "
                    "nenhuma bounding box recebida",
                    pacote_id,
                )
                return None

            resultado = await asyncio.to_thread(
                renderizar_cena_com_boxes,
                caminho_imagem=caminho_original,
                bounding_boxes=bounding_boxes,
            )

            if resultado.quantidade_boxes == 0:
                logger.info(
                    "[%s] Cena não renderizada: "
                    "nenhuma bounding box válida",
                    pacote_id,
                )
                return None

            logger.info(
                "[%s] Cena renderizada: "
                "boxes=%s dimensoes=%sx%s "
                "base64=%s caracteres",
                pacote_id,
                resultado.quantidade_boxes,
                resultado.largura_imagem,
                resultado.altura_imagem,
                len(resultado.imagem_base64),
            )

            return resultado

        except Exception:
            logger.exception(
                "[%s] Não foi possível renderizar "
                "a cena com bounding boxes",
                pacote_id,
            )

            return None

    async def _worker(
        self,
        numero: int,
    ) -> None:
        logger.info(
            "Worker %s iniciado",
            numero,
        )

        while True:
            pacote, body = (
                await self.queue.get()
            )

            try:
                adapter = (
                    camera_adapter_factory
                    .encontrar_adapter(
                        content_type=(
                            pacote.content_type
                        ),
                        body=body,
                    )
                )

                logger.info(
                    "[%s] Worker %s usando %s",
                    pacote.evento_id,
                    numero,
                    adapter.__class__.__name__,
                )

                evento = await asyncio.to_thread(
                    adapter.normalizar,
                    pacote,
                    body,
                )

                if evento.imagem is None:
                    logger.info(
                        "[%s] Evento ignorado: "
                        "não possui imagem",
                        pacote.evento_id,
                    )
                    continue

                bounding_box = (
                    evento.bounding_box_escolhida
                )

                if bounding_box is None:
                    logger.info(
                        "[%s] Evento ignorado: "
                        "não possui bounding box",
                        pacote.evento_id,
                    )
                    continue

                camera = (
                    evento.nome_camera
                    or evento.camera_id
                    or evento.ip_camera
                    or "Camera desconhecida"
                )

                caixa_rastreamento = DetectionBox(
                    x=bounding_box.x,
                    y=bounding_box.y,
                    largura=bounding_box.largura,
                    altura=bounding_box.altura,
                )

                decisao = await person_tracker.registrar(
                    camera=camera,
                    evento_id=evento.evento_id,
                    bbox=caixa_rastreamento,
                )

                logger.info(
                    "[%s] Rastreamento: "
                    "pessoa=%s status=%s "
                    "deteccoes=%s",
                    pacote.evento_id,
                    decisao.pessoa_id,
                    decisao.status,
                    decisao.quantidade_deteccoes,
                )

                aparencia_estavel = (
                    await self._analisar_aparencia(
                        evento=evento,
                        bounding_box=bounding_box,
                        pessoa_id=decisao.pessoa_id,
                        pacote_id=pacote.evento_id,
                    )
                )

                contexto_cena = (
                    await self._analisar_contexto_cena(
                        evento=evento,
                        pacote_id=pacote.evento_id,
                    )
                )

                cena_renderizada = (
                    await self._renderizar_cena(
                        evento=evento,
                        pacote_id=pacote.evento_id,
                    )
                )

                # Evento repetido:
                # atualiza o cartão, mas não vai
                # novamente para o Kafka.
                if not decisao.deve_processar:
                    atualizacao_parcial = {
                        "evento_id": evento.evento_id,
                        "pessoa_id": decisao.pessoa_id,
                        "status": "appearance_updated",
                        "quantidade_deteccoes": (
                            decisao.quantidade_deteccoes
                        ),
                        "camera": camera,
                        "aparencia": (
                            aparencia_estavel.to_dict()
                            if aparencia_estavel is not None
                            else None
                        ),
                        "contexto_cena": (
                            contexto_cena.to_dict()
                            if contexto_cena is not None
                            else None
                        ),
                        "imagem_cena": (
                            cena_renderizada.imagem_base64
                            if cena_renderizada is not None
                            else None
                        ),
                        "quantidade_boxes_cena": (
                            cena_renderizada.quantidade_boxes
                            if cena_renderizada is not None
                            else 0
                        ),
                    }

                    await event_hub.publicar(
                        atualizacao_parcial
                    )

                    logger.info(
                        "[%s] Aparência, contexto e "
                        "cena atualizados em "
                        "%s painel(is): pessoa=%s "
                        "deteccoes=%s amostras=%s "
                        "boxes=%s",
                        pacote.evento_id,
                        event_hub.total_conexoes,
                        decisao.pessoa_id,
                        decisao.quantidade_deteccoes,
                        (
                            aparencia_estavel
                            .quantidade_amostras
                            if aparencia_estavel is not None
                            else 0
                        ),
                        (
                            cena_renderizada
                            .quantidade_boxes
                            if cena_renderizada is not None
                            else 0
                        ),
                    )

                    logger.info(
                        "[%s] Evento repetido "
                        "suprimido: pessoa=%s "
                        "amostras=%s",
                        pacote.evento_id,
                        decisao.pessoa_id,
                        (
                            aparencia_estavel
                            .quantidade_amostras
                            if aparencia_estavel is not None
                            else 0
                        ),
                    )

                    continue

                try:
                    mensagem = await asyncio.to_thread(
                        montar_alarm_detection_message,
                        evento,
                    )

                except ValueError as erro:
                    logger.info(
                        "[%s] Evento ignorado: %s",
                        pacote.evento_id,
                        erro,
                    )
                    continue

                evento_painel = {
                    "evento_id": evento.evento_id,
                    "pessoa_id": decisao.pessoa_id,
                    "status": decisao.status,
                    "quantidade_deteccoes": (
                        decisao.quantidade_deteccoes
                    ),
                    "camera": mensagem.device.name,
                    "tipo": mensagem.event.type,
                    "datetime": mensagem.event.datetime,

                    # Recorte individual da pessoa.
                    "imagem": (
                        mensagem
                        .event
                        .images
                        .detection
                    ),

                    "aparencia": (
                        aparencia_estavel.to_dict()
                        if aparencia_estavel is not None
                        else None
                    ),

                    "contexto_cena": (
                        contexto_cena.to_dict()
                        if contexto_cena is not None
                        else None
                    ),

                    # Imagem completa com todas as
                    # bounding boxes válidas.
                    "imagem_cena": (
                        cena_renderizada.imagem_base64
                        if cena_renderizada is not None
                        else None
                    ),

                    "quantidade_boxes_cena": (
                        cena_renderizada.quantidade_boxes
                        if cena_renderizada is not None
                        else 0
                    ),
                }

                await event_hub.publicar(
                    evento_painel
                )

                logger.info(
                    "[%s] Evento enviado para "
                    "%s painel(is)",
                    pacote.evento_id,
                    event_hub.total_conexoes,
                )

                if not settings.kafka_enabled:
                    logger.info(
                        "[%s] Payload pronto; "
                        "Kafka desativado",
                        pacote.evento_id,
                    )
                    continue

                # O formato do Kafka permanece igual.
                # imagem_cena e contexto_cena ficam
                # apenas no painel/WebSocket.
                resultado = await self.publisher.publicar(
                    topico=self.topic,
                    evento_id=evento.evento_id,
                    dados=mensagem.model_dump(
                        mode="json"
                    ),
                )

                logger.info(
                    "[%s] Publicado em %s "
                    "p=%s offset=%s",
                    pacote.evento_id,
                    resultado["topico"],
                    resultado["particao"],
                    resultado["offset"],
                )

            except asyncio.CancelledError:
                raise

            except Exception:
                logger.exception(
                    "[%s] Erro no processamento",
                    pacote.evento_id,
                )

            finally:
                self.queue.task_done()
------------------------------------------------------------
ARQUIVO: app/services/camera_event_pipeline_backup_antes_saida.py
------------------------------------------------------------
import asyncio
import logging

from app.adapters.cameras.factory import camera_adapter_factory
from app.core.config import settings
from app.domain.models.camera_event import RawCameraPackage
from app.messaging.kafka_producer import KafkaPublisher
from app.services.alarm_detection_payload import (
    montar_alarm_detection_message,
)
from app.services.event_hub import event_hub
from app.services.person_tracker import (
    DetectionBox,
    person_tracker,
)


logger = logging.getLogger(__name__)


class CameraEventPipeline:
    def __init__(
        self,
        publisher: KafkaPublisher,
        topic: str,
        maxsize: int = 1000,
        worker_count: int = 4,
    ) -> None:
        self.publisher = publisher
        self.topic = topic
        self.worker_count = worker_count

        self.queue: asyncio.Queue[
            tuple[RawCameraPackage, bytes]
        ] = asyncio.Queue(maxsize=maxsize)

        self._worker_tasks: list[asyncio.Task] = []
        self._iniciado = False

    @property
    def tamanho_fila(self) -> int:
        return self.queue.qsize()

    @property
    def capacidade_fila(self) -> int:
        return self.queue.maxsize

    async def start(self) -> None:
        if self._iniciado:
            return

        if settings.kafka_enabled:
            await self.publisher.start()
        else:
            logger.warning(
                "Kafka desativado: eventos serão processados, "
                "mas não publicados."
            )

        self._worker_tasks = [
            asyncio.create_task(
                self._worker(numero),
                name=f"camera-worker-{numero}",
            )
            for numero in range(
                1,
                self.worker_count + 1,
            )
        ]

        self._iniciado = True

        logger.info(
            "Pipeline iniciado com %s workers",
            self.worker_count,
        )

    async def stop(self) -> None:
        if not self._iniciado:
            return

        try:
            await asyncio.wait_for(
                self.queue.join(),
                timeout=10,
            )

        except TimeoutError:
            logger.warning(
                "Encerrando com %s itens na fila",
                self.queue.qsize(),
            )

        for task in self._worker_tasks:
            task.cancel()

        await asyncio.gather(
            *self._worker_tasks,
            return_exceptions=True,
        )

        if self.publisher.iniciado:
            await self.publisher.stop()

        self._worker_tasks.clear()
        self._iniciado = False

        logger.info("Pipeline encerrado")

    def adicionar(
        self,
        pacote: RawCameraPackage,
        body: bytes,
    ) -> None:
        self.queue.put_nowait(
            (pacote, body)
        )

    async def _worker(
        self,
        numero: int,
    ) -> None:
        logger.info(
            "Worker %s iniciado",
            numero,
        )

        while True:
            pacote, body = await self.queue.get()

            try:
                adapter = (
                    camera_adapter_factory.encontrar_adapter(
                        content_type=pacote.content_type,
                        body=body,
                    )
                )

                logger.info(
                    "[%s] Worker %s usando %s",
                    pacote.evento_id,
                    numero,
                    adapter.__class__.__name__,
                )

                evento = await asyncio.to_thread(
                    adapter.normalizar,
                    pacote,
                    body,
                )

                # Eventos sem imagem não entram no rastreamento.
                if evento.imagem is None:
                    logger.info(
                        "[%s] Evento ignorado: "
                        "não possui imagem",
                        pacote.evento_id,
                    )
                    continue

                bounding_box = (
                    evento.bounding_box_escolhida
                )

                if bounding_box is None:
                    logger.info(
                        "[%s] Evento ignorado: "
                        "não possui bounding box",
                        pacote.evento_id,
                    )
                    continue

                camera = (
                    evento.nome_camera
                    or evento.camera_id
                    or evento.ip_camera
                    or "Camera desconhecida"
                )

                # Remove sessões que estão há mais de
                # 8 segundos sem novas detecções.
                saidas = (
                    await person_tracker.coletar_saidas()
                )

                for saida in saidas:
                    logger.info(
                        "[RASTREAMENTO] Pessoa saiu: "
                        "pessoa=%s camera=%s "
                        "deteccoes=%s",
                        saida.pessoa_id,
                        saida.camera,
                        saida.quantidade_deteccoes,
                    )

                caixa_rastreamento = DetectionBox(
                    x=bounding_box.x,
                    y=bounding_box.y,
                    largura=bounding_box.largura,
                    altura=bounding_box.altura,
                )

                decisao = await person_tracker.registrar(
                    camera=camera,
                    evento_id=evento.evento_id,
                    bbox=caixa_rastreamento,
                )

                logger.info(
                    "[%s] Rastreamento: "
                    "pessoa=%s status=%s "
                    "deteccoes=%s",
                    pacote.evento_id,
                    decisao.pessoa_id,
                    decisao.status,
                    decisao.quantidade_deteccoes,
                )

                # Eventos repetidos em menos de 5 segundos
                # param aqui. Não geram Base64, painel ou Kafka.
                if not decisao.deve_processar:
                    logger.info(
                        "[%s] Evento repetido suprimido: "
                        "pessoa=%s",
                        pacote.evento_id,
                        decisao.pessoa_id,
                    )
                    continue

                try:
                    mensagem = await asyncio.to_thread(
                        montar_alarm_detection_message,
                        evento,
                    )

                except ValueError as erro:
                    logger.info(
                        "[%s] Evento ignorado: %s",
                        pacote.evento_id,
                        erro,
                    )
                    continue

                evento_painel = {
                    "evento_id": evento.evento_id,
                    "pessoa_id": decisao.pessoa_id,
                    "status": decisao.status,
                    "quantidade_deteccoes": (
                        decisao.quantidade_deteccoes
                    ),
                    "camera": mensagem.device.name,
                    "tipo": mensagem.event.type,
                    "datetime": mensagem.event.datetime,
                    "imagem": (
                        mensagem.event.images.detection
                    ),
                }

                await event_hub.publicar(
                    evento_painel
                )

                logger.info(
                    "[%s] Evento enviado para "
                    "%s painel(is)",
                    pacote.evento_id,
                    event_hub.total_conexoes,
                )

                if not settings.kafka_enabled:
                    logger.info(
                        "[%s] Payload pronto; "
                        "Kafka desativado",
                        pacote.evento_id,
                    )
                    continue

                resultado = (
                    await self.publisher.publicar(
                        topico=self.topic,
                        evento_id=evento.evento_id,
                        dados=mensagem.model_dump(
                            mode="json"
                        ),
                    )
                )

                logger.info(
                    "[%s] Publicado em %s "
                    "p=%s offset=%s",
                    pacote.evento_id,
                    resultado["topico"],
                    resultado["particao"],
                    resultado["offset"],
                )

            except Exception:
                logger.exception(
                    "[%s] Erro no processamento",
                    pacote.evento_id,
                )

            finally:
                self.queue.task_done()
------------------------------------------------------------
ARQUIVO: app/services/camera_event_pipeline_backup_atualizacao_visual.py
------------------------------------------------------------
import asyncio
import logging
from datetime import datetime, timezone

from app.adapters.cameras.factory import (
    camera_adapter_factory,
)
from app.core.config import settings
from app.domain.models.camera_event import (
    RawCameraPackage,
)
from app.messaging.kafka_producer import (
    KafkaPublisher,
)
from app.services.alarm_detection_payload import (
    montar_alarm_detection_message,
)
from app.services.appearance_memory import (
    appearance_memory,
)
from app.services.event_hub import event_hub
from app.services.person_tracker import (
    DetectionBox,
    person_tracker,
)
from app.services.scene_analyzer import (
    analisar_pessoa,
)


logger = logging.getLogger(__name__)


class CameraEventPipeline:
    def __init__(
        self,
        publisher: KafkaPublisher,
        topic: str,
        maxsize: int = 1000,
        worker_count: int = 4,
    ) -> None:
        self.publisher = publisher
        self.topic = topic
        self.worker_count = worker_count

        self.queue: asyncio.Queue[
            tuple[RawCameraPackage, bytes]
        ] = asyncio.Queue(maxsize=maxsize)

        self._worker_tasks: list[asyncio.Task] = []
        self._exit_task: asyncio.Task | None = None
        self._iniciado = False

    @property
    def tamanho_fila(self) -> int:
        return self.queue.qsize()

    @property
    def capacidade_fila(self) -> int:
        return self.queue.maxsize

    async def start(self) -> None:
        if self._iniciado:
            return

        if settings.kafka_enabled:
            await self.publisher.start()
        else:
            logger.warning(
                "Kafka desativado: eventos serão "
                "processados, mas não publicados."
            )

        self._worker_tasks = [
            asyncio.create_task(
                self._worker(numero),
                name=f"camera-worker-{numero}",
            )
            for numero in range(
                1,
                self.worker_count + 1,
            )
        ]

        self._exit_task = asyncio.create_task(
            self._monitorar_saidas(),
            name="person-exit-monitor",
        )

        self._iniciado = True

        logger.info(
            "Pipeline iniciado com %s workers",
            self.worker_count,
        )

    async def stop(self) -> None:
        if not self._iniciado:
            return

        try:
            await asyncio.wait_for(
                self.queue.join(),
                timeout=10,
            )

        except TimeoutError:
            logger.warning(
                "Encerrando com %s itens na fila",
                self.queue.qsize(),
            )

        tarefas: list[asyncio.Task] = [
            *self._worker_tasks,
        ]

        if self._exit_task is not None:
            tarefas.append(self._exit_task)

        for tarefa in tarefas:
            tarefa.cancel()

        if tarefas:
            await asyncio.gather(
                *tarefas,
                return_exceptions=True,
            )

        if self.publisher.iniciado:
            await self.publisher.stop()

        await appearance_memory.limpar()

        self._worker_tasks.clear()
        self._exit_task = None
        self._iniciado = False

        logger.info("Pipeline encerrado")

    def adicionar(
        self,
        pacote: RawCameraPackage,
        body: bytes,
    ) -> None:
        self.queue.put_nowait(
            (pacote, body)
        )

    async def _monitorar_saidas(self) -> None:
        """
        Verifica a cada segundo quais pessoas estão
        há pelo menos 15 segundos sem nova imagem.
        """

        logger.info(
            "Monitor automático de saídas iniciado"
        )

        while True:
            try:
                await asyncio.sleep(1)

                saidas = (
                    await person_tracker.coletar_saidas()
                )

                for saida in saidas:
                    aparencia_final = (
                        await appearance_memory.finalizar(
                            saida.pessoa_id
                        )
                    )

                    momento_saida = (
                        datetime.now(timezone.utc)
                        .isoformat()
                        .replace("+00:00", "Z")
                    )

                    evento_painel = {
                        "evento_id": saida.evento_id,
                        "pessoa_id": saida.pessoa_id,
                        "status": "exited",
                        "quantidade_deteccoes": (
                            saida.quantidade_deteccoes
                        ),
                        "camera": saida.camera,
                        "tipo": None,
                        "datetime": momento_saida,
                        "imagem": None,
                        "aparencia": (
                            aparencia_final.to_dict()
                            if aparencia_final is not None
                            else None
                        ),
                    }

                    await event_hub.publicar(
                        evento_painel
                    )

                    logger.info(
                        "[RASTREAMENTO] Pessoa saiu: "
                        "pessoa=%s camera=%s "
                        "deteccoes=%s amostras=%s "
                        "paineis=%s",
                        saida.pessoa_id,
                        saida.camera,
                        saida.quantidade_deteccoes,
                        (
                            aparencia_final
                            .quantidade_amostras
                            if aparencia_final is not None
                            else 0
                        ),
                        event_hub.total_conexoes,
                    )

            except asyncio.CancelledError:
                logger.info(
                    "Monitor automático de saídas "
                    "encerrado"
                )
                raise

            except Exception:
                logger.exception(
                    "Erro no monitor automático "
                    "de saídas"
                )

    async def _analisar_aparencia(
        self,
        evento,
        bounding_box,
        pessoa_id: str,
        pacote_id: str,
    ):
        """
        Analisa a imagem e registra uma nova amostra
        visual para a mesma pessoa.

        Esta função também é executada para eventos
        repetidos que serão suprimidos.
        """

        try:
            if evento.imagem is None:
                return None

            caminho_original = (
                evento.imagem.caminho_original
            )

            if not caminho_original:
                logger.info(
                    "[%s] Imagem sem caminho original; "
                    "aparência não analisada",
                    pacote_id,
                )
                return None

            analise_visual = await asyncio.to_thread(
                analisar_pessoa,
                caminho_imagem=caminho_original,
                x=bounding_box.x,
                y=bounding_box.y,
                largura=bounding_box.largura,
                altura=bounding_box.altura,
            )

            aparencia_estavel = (
                await appearance_memory.registrar(
                    pessoa_id=pessoa_id,
                    analise=analise_visual,
                )
            )

            logger.info(
                "[%s] Aparência estável: "
                "pessoa=%s cor=%s posição=%s "
                "tamanho=%s área_média=%s%% "
                "amostras=%s",
                pacote_id,
                pessoa_id,
                (
                    aparencia_estavel
                    .cor_roupa_predominante
                ),
                aparencia_estavel.posicao_atual,
                (
                    aparencia_estavel
                    .tamanho_predominante
                ),
                (
                    aparencia_estavel
                    .percentual_medio_quadro
                ),
                (
                    aparencia_estavel
                    .quantidade_amostras
                ),
            )

            return aparencia_estavel

        except Exception:
            logger.exception(
                "[%s] Não foi possível analisar "
                "ou estabilizar a aparência",
                pacote_id,
            )

            return None

    async def _worker(
        self,
        numero: int,
    ) -> None:
        logger.info(
            "Worker %s iniciado",
            numero,
        )

        while True:
            pacote, body = await self.queue.get()

            try:
                adapter = (
                    camera_adapter_factory
                    .encontrar_adapter(
                        content_type=(
                            pacote.content_type
                        ),
                        body=body,
                    )
                )

                logger.info(
                    "[%s] Worker %s usando %s",
                    pacote.evento_id,
                    numero,
                    adapter.__class__.__name__,
                )

                evento = await asyncio.to_thread(
                    adapter.normalizar,
                    pacote,
                    body,
                )

                if evento.imagem is None:
                    logger.info(
                        "[%s] Evento ignorado: "
                        "não possui imagem",
                        pacote.evento_id,
                    )
                    continue

                bounding_box = (
                    evento.bounding_box_escolhida
                )

                if bounding_box is None:
                    logger.info(
                        "[%s] Evento ignorado: "
                        "não possui bounding box",
                        pacote.evento_id,
                    )
                    continue

                camera = (
                    evento.nome_camera
                    or evento.camera_id
                    or evento.ip_camera
                    or "Camera desconhecida"
                )

                caixa_rastreamento = DetectionBox(
                    x=bounding_box.x,
                    y=bounding_box.y,
                    largura=bounding_box.largura,
                    altura=bounding_box.altura,
                )

                decisao = await person_tracker.registrar(
                    camera=camera,
                    evento_id=evento.evento_id,
                    bbox=caixa_rastreamento,
                )

                logger.info(
                    "[%s] Rastreamento: "
                    "pessoa=%s status=%s "
                    "deteccoes=%s",
                    pacote.evento_id,
                    decisao.pessoa_id,
                    decisao.status,
                    decisao.quantidade_deteccoes,
                )

                # A análise acontece antes da supressão.
                # Assim, imagens repetidas contam como
                # amostras da aparência da mesma pessoa.
                aparencia_estavel = (
                    await self._analisar_aparencia(
                        evento=evento,
                        bounding_box=bounding_box,
                        pessoa_id=decisao.pessoa_id,
                        pacote_id=pacote.evento_id,
                    )
                )

                # O evento repetido atualiza a memória,
                # mas não gera Base64, painel ou Kafka.
                if not decisao.deve_processar:
                    logger.info(
                        "[%s] Evento repetido suprimido: "
                        "pessoa=%s amostras=%s",
                        pacote.evento_id,
                        decisao.pessoa_id,
                        (
                            aparencia_estavel
                            .quantidade_amostras
                            if aparencia_estavel is not None
                            else 0
                        ),
                    )
                    continue

                try:
                    mensagem = await asyncio.to_thread(
                        montar_alarm_detection_message,
                        evento,
                    )

                except ValueError as erro:
                    logger.info(
                        "[%s] Evento ignorado: %s",
                        pacote.evento_id,
                        erro,
                    )
                    continue

                evento_painel = {
                    "evento_id": evento.evento_id,
                    "pessoa_id": decisao.pessoa_id,
                    "status": decisao.status,
                    "quantidade_deteccoes": (
                        decisao.quantidade_deteccoes
                    ),
                    "camera": mensagem.device.name,
                    "tipo": mensagem.event.type,
                    "datetime": mensagem.event.datetime,
                    "imagem": (
                        mensagem
                        .event
                        .images
                        .detection
                    ),
                    "aparencia": (
                        aparencia_estavel.to_dict()
                        if aparencia_estavel is not None
                        else None
                    ),
                }

                await event_hub.publicar(
                    evento_painel
                )

                logger.info(
                    "[%s] Evento enviado para "
                    "%s painel(is)",
                    pacote.evento_id,
                    event_hub.total_conexoes,
                )

                if not settings.kafka_enabled:
                    logger.info(
                        "[%s] Payload pronto; "
                        "Kafka desativado",
                        pacote.evento_id,
                    )
                    continue

                resultado = (
                    await self.publisher.publicar(
                        topico=self.topic,
                        evento_id=evento.evento_id,
                        dados=mensagem.model_dump(
                            mode="json"
                        ),
                    )
                )

                logger.info(
                    "[%s] Publicado em %s "
                    "p=%s offset=%s",
                    pacote.evento_id,
                    resultado["topico"],
                    resultado["particao"],
                    resultado["offset"],
                )

            except asyncio.CancelledError:
                raise

            except Exception:
                logger.exception(
                    "[%s] Erro no processamento",
                    pacote.evento_id,
                )

            finally:
                self.queue.task_done()
------------------------------------------------------------
ARQUIVO: app/services/camera_event_pipeline_backup_cena_renderizada.py
------------------------------------------------------------
import asyncio
import logging
from datetime import datetime, timezone

from app.adapters.cameras.factory import (
    camera_adapter_factory,
)
from app.core.config import settings
from app.domain.models.camera_event import (
    RawCameraPackage,
)
from app.messaging.kafka_producer import (
    KafkaPublisher,
)
from app.services.alarm_detection_payload import (
    montar_alarm_detection_message,
)
from app.services.appearance_memory import (
    appearance_memory,
)
from app.services.event_hub import event_hub
from app.services.person_tracker import (
    DetectionBox,
    person_tracker,
)
from app.services.scene_analyzer import (
    analisar_pessoa,
)
from app.services.scene_context_analyzer import (
    analisar_contexto_cena,
)


logger = logging.getLogger(__name__)


class CameraEventPipeline:
    def __init__(
        self,
        publisher: KafkaPublisher,
        topic: str,
        maxsize: int = 1000,
        worker_count: int = 4,
    ) -> None:
        self.publisher = publisher
        self.topic = topic
        self.worker_count = worker_count

        self.queue: asyncio.Queue[
            tuple[RawCameraPackage, bytes]
        ] = asyncio.Queue(maxsize=maxsize)

        self._worker_tasks: list[
            asyncio.Task
        ] = []

        self._exit_task: (
            asyncio.Task | None
        ) = None

        self._iniciado = False

    @property
    def tamanho_fila(self) -> int:
        return self.queue.qsize()

    @property
    def capacidade_fila(self) -> int:
        return self.queue.maxsize

    async def start(self) -> None:
        if self._iniciado:
            return

        if settings.kafka_enabled:
            await self.publisher.start()
        else:
            logger.warning(
                "Kafka desativado: eventos serão "
                "processados, mas não publicados."
            )

        self._worker_tasks = [
            asyncio.create_task(
                self._worker(numero),
                name=f"camera-worker-{numero}",
            )
            for numero in range(
                1,
                self.worker_count + 1,
            )
        ]

        self._exit_task = asyncio.create_task(
            self._monitorar_saidas(),
            name="person-exit-monitor",
        )

        self._iniciado = True

        logger.info(
            "Pipeline iniciado com %s workers",
            self.worker_count,
        )

    async def stop(self) -> None:
        if not self._iniciado:
            return

        try:
            await asyncio.wait_for(
                self.queue.join(),
                timeout=10,
            )

        except TimeoutError:
            logger.warning(
                "Encerrando com %s itens na fila",
                self.queue.qsize(),
            )

        tarefas: list[asyncio.Task] = [
            *self._worker_tasks,
        ]

        if self._exit_task is not None:
            tarefas.append(
                self._exit_task
            )

        for tarefa in tarefas:
            tarefa.cancel()

        if tarefas:
            await asyncio.gather(
                *tarefas,
                return_exceptions=True,
            )

        if self.publisher.iniciado:
            await self.publisher.stop()

        await appearance_memory.limpar()

        self._worker_tasks.clear()
        self._exit_task = None
        self._iniciado = False

        logger.info(
            "Pipeline encerrado"
        )

    def adicionar(
        self,
        pacote: RawCameraPackage,
        body: bytes,
    ) -> None:
        self.queue.put_nowait(
            (pacote, body)
        )

    async def _monitorar_saidas(
        self,
    ) -> None:
        """
        Verifica quais pessoas estão há pelo menos
        15 segundos sem receber uma nova imagem.
        """

        logger.info(
            "Monitor automático de saídas iniciado"
        )

        while True:
            try:
                await asyncio.sleep(1)

                saidas = (
                    await person_tracker
                    .coletar_saidas()
                )

                for saida in saidas:
                    aparencia_final = (
                        await appearance_memory
                        .finalizar(
                            saida.pessoa_id
                        )
                    )

                    momento_saida = (
                        datetime.now(
                            timezone.utc
                        )
                        .isoformat()
                        .replace(
                            "+00:00",
                            "Z",
                        )
                    )

                    evento_painel = {
                        "evento_id": (
                            saida.evento_id
                        ),
                        "pessoa_id": (
                            saida.pessoa_id
                        ),
                        "status": "exited",
                        "quantidade_deteccoes": (
                            saida
                            .quantidade_deteccoes
                        ),
                        "camera": saida.camera,
                        "tipo": None,
                        "datetime": momento_saida,
                        "imagem": None,
                        "aparencia": (
                            aparencia_final.to_dict()
                            if aparencia_final
                            is not None
                            else None
                        ),
                        "contexto_cena": None,
                    }

                    await event_hub.publicar(
                        evento_painel
                    )

                    logger.info(
                        "[RASTREAMENTO] Pessoa saiu: "
                        "pessoa=%s camera=%s "
                        "deteccoes=%s amostras=%s "
                        "paineis=%s",
                        saida.pessoa_id,
                        saida.camera,
                        (
                            saida
                            .quantidade_deteccoes
                        ),
                        (
                            aparencia_final
                            .quantidade_amostras
                            if aparencia_final
                            is not None
                            else 0
                        ),
                        event_hub.total_conexoes,
                    )

            except asyncio.CancelledError:
                logger.info(
                    "Monitor automático de "
                    "saídas encerrado"
                )
                raise

            except Exception:
                logger.exception(
                    "Erro no monitor automático "
                    "de saídas"
                )

    async def _analisar_aparencia(
        self,
        evento,
        bounding_box,
        pessoa_id: str,
        pacote_id: str,
    ):
        """
        Analisa a imagem e registra uma amostra
        visual para a pessoa.

        Também é executado para eventos repetidos
        que serão suprimidos.
        """

        try:
            if evento.imagem is None:
                return None

            caminho_original = (
                evento
                .imagem
                .caminho_original
            )

            if not caminho_original:
                logger.info(
                    "[%s] Imagem sem caminho "
                    "original; aparência não "
                    "analisada",
                    pacote_id,
                )

                return None

            analise_visual = (
                await asyncio.to_thread(
                    analisar_pessoa,
                    caminho_imagem=(
                        caminho_original
                    ),
                    x=bounding_box.x,
                    y=bounding_box.y,
                    largura=(
                        bounding_box.largura
                    ),
                    altura=(
                        bounding_box.altura
                    ),
                )
            )

            aparencia_estavel = (
                await appearance_memory
                .registrar(
                    pessoa_id=pessoa_id,
                    analise=analise_visual,
                )
            )

            logger.info(
                "[%s] Aparência estável: "
                "pessoa=%s cor=%s posição=%s "
                "tamanho=%s área_média=%s%% "
                "amostras=%s",
                pacote_id,
                pessoa_id,
                (
                    aparencia_estavel
                    .cor_roupa_predominante
                ),
                (
                    aparencia_estavel
                    .posicao_atual
                ),
                (
                    aparencia_estavel
                    .tamanho_predominante
                ),
                (
                    aparencia_estavel
                    .percentual_medio_quadro
                ),
                (
                    aparencia_estavel
                    .quantidade_amostras
                ),
            )

            return aparencia_estavel

        except Exception:
            logger.exception(
                "[%s] Não foi possível analisar "
                "ou estabilizar a aparência",
                pacote_id,
            )

            return None

    async def _analisar_contexto_cena(
        self,
        evento,
        pacote_id: str,
    ):
        """
        Analisa todas as bounding boxes válidas
        recebidas no evento da câmera.
        """

        try:
            if evento.imagem is None:
                return None

            largura_imagem = (
                evento.imagem.largura
            )

            altura_imagem = (
                evento.imagem.altura
            )

            if (
                not largura_imagem
                or not altura_imagem
            ):
                logger.info(
                    "[%s] Contexto não analisado: "
                    "dimensões da imagem ausentes",
                    pacote_id,
                )

                return None

            bounding_boxes = (
                evento.bounding_boxes
                or []
            )

            contexto = (
                await asyncio.to_thread(
                    analisar_contexto_cena,
                    largura_imagem=(
                        largura_imagem
                    ),
                    altura_imagem=(
                        altura_imagem
                    ),
                    bounding_boxes=(
                        bounding_boxes
                    ),
                )
            )

            logger.info(
                "[%s] Contexto da cena: "
                "pessoas=%s esquerda=%s "
                "centro=%s direita=%s "
                "muito_proximos=%s "
                "proximos=%s separados=%s",
                pacote_id,
                contexto.quantidade_pessoas,
                contexto.pessoas_esquerda,
                contexto.pessoas_centro,
                contexto.pessoas_direita,
                (
                    contexto
                    .pares_muito_proximos
                ),
                contexto.pares_proximos,
                contexto.pares_separados,
            )

            logger.info(
                "[%s] Descrição da cena: %s",
                pacote_id,
                contexto.descricao,
            )

            return contexto

        except Exception:
            logger.exception(
                "[%s] Não foi possível analisar "
                "o contexto da cena",
                pacote_id,
            )

            return None

    async def _worker(
        self,
        numero: int,
    ) -> None:
        logger.info(
            "Worker %s iniciado",
            numero,
        )

        while True:
            pacote, body = (
                await self.queue.get()
            )

            try:
                adapter = (
                    camera_adapter_factory
                    .encontrar_adapter(
                        content_type=(
                            pacote.content_type
                        ),
                        body=body,
                    )
                )

                logger.info(
                    "[%s] Worker %s usando %s",
                    pacote.evento_id,
                    numero,
                    adapter.__class__.__name__,
                )

                evento = (
                    await asyncio.to_thread(
                        adapter.normalizar,
                        pacote,
                        body,
                    )
                )

                if evento.imagem is None:
                    logger.info(
                        "[%s] Evento ignorado: "
                        "não possui imagem",
                        pacote.evento_id,
                    )
                    continue

                bounding_box = (
                    evento
                    .bounding_box_escolhida
                )

                if bounding_box is None:
                    logger.info(
                        "[%s] Evento ignorado: "
                        "não possui bounding box",
                        pacote.evento_id,
                    )
                    continue

                camera = (
                    evento.nome_camera
                    or evento.camera_id
                    or evento.ip_camera
                    or "Camera desconhecida"
                )

                caixa_rastreamento = (
                    DetectionBox(
                        x=bounding_box.x,
                        y=bounding_box.y,
                        largura=(
                            bounding_box.largura
                        ),
                        altura=(
                            bounding_box.altura
                        ),
                    )
                )

                decisao = (
                    await person_tracker
                    .registrar(
                        camera=camera,
                        evento_id=(
                            evento.evento_id
                        ),
                        bbox=(
                            caixa_rastreamento
                        ),
                    )
                )

                logger.info(
                    "[%s] Rastreamento: "
                    "pessoa=%s status=%s "
                    "deteccoes=%s",
                    pacote.evento_id,
                    decisao.pessoa_id,
                    decisao.status,
                    (
                        decisao
                        .quantidade_deteccoes
                    ),
                )

                aparencia_estavel = (
                    await self
                    ._analisar_aparencia(
                        evento=evento,
                        bounding_box=(
                            bounding_box
                        ),
                        pessoa_id=(
                            decisao.pessoa_id
                        ),
                        pacote_id=(
                            pacote.evento_id
                        ),
                    )
                )

                contexto_cena = (
                    await self
                    ._analisar_contexto_cena(
                        evento=evento,
                        pacote_id=(
                            pacote.evento_id
                        ),
                    )
                )

                # A captura repetida não vai ao
                # Kafka e não é contada como um
                # evento novo no painel.
                #
                # Ela envia somente uma atualização
                # parcial da aparência e do contexto.
                if not decisao.deve_processar:
                    if (
                        aparencia_estavel
                        is not None
                        or contexto_cena
                        is not None
                    ):
                        atualizacao_aparencia = {
                            "evento_id": (
                                evento.evento_id
                            ),
                            "pessoa_id": (
                                decisao.pessoa_id
                            ),
                            "status": (
                                "appearance_updated"
                            ),
                            "quantidade_deteccoes": (
                                decisao
                                .quantidade_deteccoes
                            ),
                            "camera": camera,
                            "aparencia": (
                                aparencia_estavel
                                .to_dict()
                                if aparencia_estavel
                                is not None
                                else None
                            ),
                            "contexto_cena": (
                                contexto_cena
                                .to_dict()
                                if contexto_cena
                                is not None
                                else None
                            ),
                        }

                        await event_hub.publicar(
                            atualizacao_aparencia
                        )

                        logger.info(
                            "[%s] Aparência e "
                            "contexto atualizados em "
                            "%s painel(is): "
                            "pessoa=%s deteccoes=%s "
                            "amostras=%s",
                            pacote.evento_id,
                            (
                                event_hub
                                .total_conexoes
                            ),
                            decisao.pessoa_id,
                            (
                                decisao
                                .quantidade_deteccoes
                            ),
                            (
                                aparencia_estavel
                                .quantidade_amostras
                                if aparencia_estavel
                                is not None
                                else 0
                            ),
                        )

                    logger.info(
                        "[%s] Evento repetido "
                        "suprimido: pessoa=%s "
                        "amostras=%s",
                        pacote.evento_id,
                        decisao.pessoa_id,
                        (
                            aparencia_estavel
                            .quantidade_amostras
                            if aparencia_estavel
                            is not None
                            else 0
                        ),
                    )

                    continue

                try:
                    mensagem = (
                        await asyncio.to_thread(
                            montar_alarm_detection_message,
                            evento,
                        )
                    )

                except ValueError as erro:
                    logger.info(
                        "[%s] Evento ignorado: %s",
                        pacote.evento_id,
                        erro,
                    )
                    continue

                evento_painel = {
                    "evento_id": (
                        evento.evento_id
                    ),
                    "pessoa_id": (
                        decisao.pessoa_id
                    ),
                    "status": decisao.status,
                    "quantidade_deteccoes": (
                        decisao
                        .quantidade_deteccoes
                    ),
                    "camera": (
                        mensagem.device.name
                    ),
                    "tipo": (
                        mensagem.event.type
                    ),
                    "datetime": (
                        mensagem.event.datetime
                    ),
                    "imagem": (
                        mensagem
                        .event
                        .images
                        .detection
                    ),
                    "aparencia": (
                        aparencia_estavel.to_dict()
                        if aparencia_estavel
                        is not None
                        else None
                    ),
                    "contexto_cena": (
                        contexto_cena.to_dict()
                        if contexto_cena
                        is not None
                        else None
                    ),
                }

                await event_hub.publicar(
                    evento_painel
                )

                logger.info(
                    "[%s] Evento enviado para "
                    "%s painel(is)",
                    pacote.evento_id,
                    event_hub.total_conexoes,
                )

                if not settings.kafka_enabled:
                    logger.info(
                        "[%s] Payload pronto; "
                        "Kafka desativado",
                        pacote.evento_id,
                    )
                    continue

                # A análise de contexto não é
                # adicionada ao Kafka nesta etapa.
                # O formato solicitado pelo
                # supervisor permanece igual.
                resultado = (
                    await self.publisher.publicar(
                        topico=self.topic,
                        evento_id=(
                            evento.evento_id
                        ),
                        dados=(
                            mensagem.model_dump(
                                mode="json"
                            )
                        ),
                    )
                )

                logger.info(
                    "[%s] Publicado em %s "
                    "p=%s offset=%s",
                    pacote.evento_id,
                    resultado["topico"],
                    resultado["particao"],
                    resultado["offset"],
                )

            except asyncio.CancelledError:
                raise

            except Exception:
                logger.exception(
                    "[%s] Erro no processamento",
                    pacote.evento_id,
                )

            finally:
                self.queue.task_done()
------------------------------------------------------------
ARQUIVO: app/services/camera_event_pipeline_backup_contexto_cena.py
------------------------------------------------------------
import asyncio
import logging
from datetime import datetime, timezone

from app.adapters.cameras.factory import (
    camera_adapter_factory,
)
from app.core.config import settings
from app.domain.models.camera_event import (
    RawCameraPackage,
)
from app.messaging.kafka_producer import (
    KafkaPublisher,
)
from app.services.alarm_detection_payload import (
    montar_alarm_detection_message,
)
from app.services.appearance_memory import (
    appearance_memory,
)
from app.services.event_hub import event_hub
from app.services.person_tracker import (
    DetectionBox,
    person_tracker,
)
from app.services.scene_analyzer import (
    analisar_pessoa,
)


logger = logging.getLogger(__name__)


class CameraEventPipeline:
    def __init__(
        self,
        publisher: KafkaPublisher,
        topic: str,
        maxsize: int = 1000,
        worker_count: int = 4,
    ) -> None:
        self.publisher = publisher
        self.topic = topic
        self.worker_count = worker_count

        self.queue: asyncio.Queue[
            tuple[RawCameraPackage, bytes]
        ] = asyncio.Queue(maxsize=maxsize)

        self._worker_tasks: list[asyncio.Task] = []
        self._exit_task: asyncio.Task | None = None
        self._iniciado = False

    @property
    def tamanho_fila(self) -> int:
        return self.queue.qsize()

    @property
    def capacidade_fila(self) -> int:
        return self.queue.maxsize

    async def start(self) -> None:
        if self._iniciado:
            return

        if settings.kafka_enabled:
            await self.publisher.start()
        else:
            logger.warning(
                "Kafka desativado: eventos serão "
                "processados, mas não publicados."
            )

        self._worker_tasks = [
            asyncio.create_task(
                self._worker(numero),
                name=f"camera-worker-{numero}",
            )
            for numero in range(
                1,
                self.worker_count + 1,
            )
        ]

        self._exit_task = asyncio.create_task(
            self._monitorar_saidas(),
            name="person-exit-monitor",
        )

        self._iniciado = True

        logger.info(
            "Pipeline iniciado com %s workers",
            self.worker_count,
        )

    async def stop(self) -> None:
        if not self._iniciado:
            return

        try:
            await asyncio.wait_for(
                self.queue.join(),
                timeout=10,
            )

        except TimeoutError:
            logger.warning(
                "Encerrando com %s itens na fila",
                self.queue.qsize(),
            )

        tarefas: list[asyncio.Task] = [
            *self._worker_tasks,
        ]

        if self._exit_task is not None:
            tarefas.append(self._exit_task)

        for tarefa in tarefas:
            tarefa.cancel()

        if tarefas:
            await asyncio.gather(
                *tarefas,
                return_exceptions=True,
            )

        if self.publisher.iniciado:
            await self.publisher.stop()

        await appearance_memory.limpar()

        self._worker_tasks.clear()
        self._exit_task = None
        self._iniciado = False

        logger.info("Pipeline encerrado")

    def adicionar(
        self,
        pacote: RawCameraPackage,
        body: bytes,
    ) -> None:
        self.queue.put_nowait(
            (pacote, body)
        )

    async def _monitorar_saidas(self) -> None:
        """
        Verifica quais pessoas estão há pelo menos
        15 segundos sem receber uma nova imagem.
        """

        logger.info(
            "Monitor automático de saídas iniciado"
        )

        while True:
            try:
                await asyncio.sleep(1)

                saidas = (
                    await person_tracker.coletar_saidas()
                )

                for saida in saidas:
                    aparencia_final = (
                        await appearance_memory.finalizar(
                            saida.pessoa_id
                        )
                    )

                    momento_saida = (
                        datetime.now(timezone.utc)
                        .isoformat()
                        .replace("+00:00", "Z")
                    )

                    evento_painel = {
                        "evento_id": saida.evento_id,
                        "pessoa_id": saida.pessoa_id,
                        "status": "exited",
                        "quantidade_deteccoes": (
                            saida.quantidade_deteccoes
                        ),
                        "camera": saida.camera,
                        "tipo": None,
                        "datetime": momento_saida,
                        "imagem": None,
                        "aparencia": (
                            aparencia_final.to_dict()
                            if aparencia_final is not None
                            else None
                        ),
                    }

                    await event_hub.publicar(
                        evento_painel
                    )

                    logger.info(
                        "[RASTREAMENTO] Pessoa saiu: "
                        "pessoa=%s camera=%s "
                        "deteccoes=%s amostras=%s "
                        "paineis=%s",
                        saida.pessoa_id,
                        saida.camera,
                        saida.quantidade_deteccoes,
                        (
                            aparencia_final
                            .quantidade_amostras
                            if aparencia_final is not None
                            else 0
                        ),
                        event_hub.total_conexoes,
                    )

            except asyncio.CancelledError:
                logger.info(
                    "Monitor automático de saídas "
                    "encerrado"
                )
                raise

            except Exception:
                logger.exception(
                    "Erro no monitor automático "
                    "de saídas"
                )

    async def _analisar_aparencia(
        self,
        evento,
        bounding_box,
        pessoa_id: str,
        pacote_id: str,
    ):
        """
        Analisa a imagem e registra uma nova amostra
        visual para a pessoa.

        Esta função também é executada para eventos
        repetidos que serão suprimidos.
        """

        try:
            if evento.imagem is None:
                return None

            caminho_original = (
                evento.imagem.caminho_original
            )

            if not caminho_original:
                logger.info(
                    "[%s] Imagem sem caminho original; "
                    "aparência não analisada",
                    pacote_id,
                )
                return None

            analise_visual = await asyncio.to_thread(
                analisar_pessoa,
                caminho_imagem=caminho_original,
                x=bounding_box.x,
                y=bounding_box.y,
                largura=bounding_box.largura,
                altura=bounding_box.altura,
            )

            aparencia_estavel = (
                await appearance_memory.registrar(
                    pessoa_id=pessoa_id,
                    analise=analise_visual,
                )
            )

            logger.info(
                "[%s] Aparência estável: "
                "pessoa=%s cor=%s posição=%s "
                "tamanho=%s área_média=%s%% "
                "amostras=%s",
                pacote_id,
                pessoa_id,
                (
                    aparencia_estavel
                    .cor_roupa_predominante
                ),
                aparencia_estavel.posicao_atual,
                (
                    aparencia_estavel
                    .tamanho_predominante
                ),
                (
                    aparencia_estavel
                    .percentual_medio_quadro
                ),
                (
                    aparencia_estavel
                    .quantidade_amostras
                ),
            )

            return aparencia_estavel

        except Exception:
            logger.exception(
                "[%s] Não foi possível analisar "
                "ou estabilizar a aparência",
                pacote_id,
            )

            return None

    async def _worker(
        self,
        numero: int,
    ) -> None:
        logger.info(
            "Worker %s iniciado",
            numero,
        )

        while True:
            pacote, body = await self.queue.get()

            try:
                adapter = (
                    camera_adapter_factory
                    .encontrar_adapter(
                        content_type=(
                            pacote.content_type
                        ),
                        body=body,
                    )
                )

                logger.info(
                    "[%s] Worker %s usando %s",
                    pacote.evento_id,
                    numero,
                    adapter.__class__.__name__,
                )

                evento = await asyncio.to_thread(
                    adapter.normalizar,
                    pacote,
                    body,
                )

                if evento.imagem is None:
                    logger.info(
                        "[%s] Evento ignorado: "
                        "não possui imagem",
                        pacote.evento_id,
                    )
                    continue

                bounding_box = (
                    evento.bounding_box_escolhida
                )

                if bounding_box is None:
                    logger.info(
                        "[%s] Evento ignorado: "
                        "não possui bounding box",
                        pacote.evento_id,
                    )
                    continue

                camera = (
                    evento.nome_camera
                    or evento.camera_id
                    or evento.ip_camera
                    or "Camera desconhecida"
                )

                caixa_rastreamento = DetectionBox(
                    x=bounding_box.x,
                    y=bounding_box.y,
                    largura=bounding_box.largura,
                    altura=bounding_box.altura,
                )

                decisao = await person_tracker.registrar(
                    camera=camera,
                    evento_id=evento.evento_id,
                    bbox=caixa_rastreamento,
                )

                logger.info(
                    "[%s] Rastreamento: "
                    "pessoa=%s status=%s "
                    "deteccoes=%s",
                    pacote.evento_id,
                    decisao.pessoa_id,
                    decisao.status,
                    decisao.quantidade_deteccoes,
                )

                # A aparência é analisada antes da
                # verificação de supressão.
                aparencia_estavel = (
                    await self._analisar_aparencia(
                        evento=evento,
                        bounding_box=bounding_box,
                        pessoa_id=decisao.pessoa_id,
                        pacote_id=pacote.evento_id,
                    )
                )

                # A captura repetida não vai ao Kafka
                # e não é contada como um evento novo.
                #
                # Ela envia somente uma atualização
                # parcial da aparência para o cartão.
                if not decisao.deve_processar:
                    if aparencia_estavel is not None:
                        atualizacao_aparencia = {
                            "evento_id": (
                                evento.evento_id
                            ),
                            "pessoa_id": (
                                decisao.pessoa_id
                            ),
                            "status": (
                                "appearance_updated"
                            ),
                            "quantidade_deteccoes": (
                                decisao
                                .quantidade_deteccoes
                            ),
                            "camera": camera,
                            "aparencia": (
                                aparencia_estavel
                                .to_dict()
                            ),
                        }

                        await event_hub.publicar(
                            atualizacao_aparencia
                        )

                        logger.info(
                            "[%s] Aparência atualizada "
                            "em %s painel(is): "
                            "pessoa=%s deteccoes=%s "
                            "amostras=%s",
                            pacote.evento_id,
                            event_hub.total_conexoes,
                            decisao.pessoa_id,
                            (
                                decisao
                                .quantidade_deteccoes
                            ),
                            (
                                aparencia_estavel
                                .quantidade_amostras
                            ),
                        )

                    logger.info(
                        "[%s] Evento repetido "
                        "suprimido: pessoa=%s "
                        "amostras=%s",
                        pacote.evento_id,
                        decisao.pessoa_id,
                        (
                            aparencia_estavel
                            .quantidade_amostras
                            if aparencia_estavel is not None
                            else 0
                        ),
                    )

                    continue

                try:
                    mensagem = await asyncio.to_thread(
                        montar_alarm_detection_message,
                        evento,
                    )

                except ValueError as erro:
                    logger.info(
                        "[%s] Evento ignorado: %s",
                        pacote.evento_id,
                        erro,
                    )
                    continue

                evento_painel = {
                    "evento_id": evento.evento_id,
                    "pessoa_id": decisao.pessoa_id,
                    "status": decisao.status,
                    "quantidade_deteccoes": (
                        decisao.quantidade_deteccoes
                    ),
                    "camera": mensagem.device.name,
                    "tipo": mensagem.event.type,
                    "datetime": mensagem.event.datetime,
                    "imagem": (
                        mensagem
                        .event
                        .images
                        .detection
                    ),
                    "aparencia": (
                        aparencia_estavel.to_dict()
                        if aparencia_estavel is not None
                        else None
                    ),
                }

                await event_hub.publicar(
                    evento_painel
                )

                logger.info(
                    "[%s] Evento enviado para "
                    "%s painel(is)",
                    pacote.evento_id,
                    event_hub.total_conexoes,
                )

                if not settings.kafka_enabled:
                    logger.info(
                        "[%s] Payload pronto; "
                        "Kafka desativado",
                        pacote.evento_id,
                    )
                    continue

                resultado = (
                    await self.publisher.publicar(
                        topico=self.topic,
                        evento_id=evento.evento_id,
                        dados=mensagem.model_dump(
                            mode="json"
                        ),
                    )
                )

                logger.info(
                    "[%s] Publicado em %s "
                    "p=%s offset=%s",
                    pacote.evento_id,
                    resultado["topico"],
                    resultado["particao"],
                    resultado["offset"],
                )

            except asyncio.CancelledError:
                raise

            except Exception:
                logger.exception(
                    "[%s] Erro no processamento",
                    pacote.evento_id,
                )

            finally:
                self.queue.task_done()
------------------------------------------------------------
ARQUIVO: app/services/camera_event_pipeline_backup_recorte.py
------------------------------------------------------------
import asyncio
import logging
from datetime import datetime, timezone

from app.adapters.cameras.factory import camera_adapter_factory
from app.core.config import settings
from app.domain.models.camera_event import BoundingBox, CameraEvent, RawCameraPackage
from app.messaging.kafka_producer import KafkaPublisher
from app.services.alarm_detection_payload import (
    arquivo_para_base64,
    montar_alarm_detection_message,
)
from app.services.appearance_memory import appearance_memory
from app.services.event_hub import event_hub
from app.services.person_tracker import (
    DetectionBox,
    TrackingDecision,
    person_tracker,
)
from app.services.person_movement import person_movement_memory
from app.services.scene_analyzer import analisar_pessoa
from app.services.scene_context_analyzer import analisar_contexto_cena
from app.services.scene_renderer import renderizar_cena_com_boxes

logger = logging.getLogger(__name__)


class CameraEventPipeline:
    def __init__(
        self,
        publisher: KafkaPublisher,
        topic: str,
        maxsize: int = 1000,
        worker_count: int = 4,
    ) -> None:
        self.publisher = publisher
        self.topic = topic
        self.worker_count = worker_count
        self.queue: asyncio.Queue[tuple[RawCameraPackage, bytes]] = asyncio.Queue(
            maxsize=maxsize
        )
        self._worker_tasks: list[asyncio.Task] = []
        self._exit_task: asyncio.Task | None = None
        self._iniciado = False

    @property
    def tamanho_fila(self) -> int:
        return self.queue.qsize()

    @property
    def capacidade_fila(self) -> int:
        return self.queue.maxsize

    async def start(self) -> None:
        if self._iniciado:
            return

        if settings.kafka_enabled:
            await self.publisher.start()
        else:
            logger.warning(
                "Kafka desativado: eventos serão processados, mas não publicados."
            )

        self._worker_tasks = [
            asyncio.create_task(
                self._worker(numero),
                name=f"camera-worker-{numero}",
            )
            for numero in range(1, self.worker_count + 1)
        ]
        self._exit_task = asyncio.create_task(
            self._monitorar_saidas(),
            name="person-exit-monitor",
        )
        self._iniciado = True
        logger.info("Pipeline iniciado com %s workers", self.worker_count)

    async def stop(self) -> None:
        if not self._iniciado:
            return

        try:
            await asyncio.wait_for(self.queue.join(), timeout=10)
        except TimeoutError:
            logger.warning("Encerrando com %s itens na fila", self.queue.qsize())

        tarefas: list[asyncio.Task] = [*self._worker_tasks]
        if self._exit_task is not None:
            tarefas.append(self._exit_task)

        for tarefa in tarefas:
            tarefa.cancel()

        if tarefas:
            await asyncio.gather(*tarefas, return_exceptions=True)

        if self.publisher.iniciado:
            await self.publisher.stop()

        await appearance_memory.limpar()
        await person_movement_memory.limpar()
        await person_tracker.limpar()

        self._worker_tasks.clear()
        self._exit_task = None
        self._iniciado = False
        logger.info("Pipeline encerrado")

    def adicionar(self, pacote: RawCameraPackage, body: bytes) -> None:
        self.queue.put_nowait((pacote, body))

    async def _monitorar_saidas(self) -> None:
        logger.info("Monitor automático de saídas iniciado")

        while True:
            try:
                await asyncio.sleep(1)
                saidas = await person_tracker.coletar_saidas()

                for saida in saidas:
                    aparencia_final = await appearance_memory.finalizar(
                        saida.pessoa_id
                    )
                    movimento_final = await person_movement_memory.finalizar(
                        saida.pessoa_id
                    )
                    momento_saida = (
                        datetime.now(timezone.utc)
                        .isoformat()
                        .replace("+00:00", "Z")
                    )
                    evento_painel = {
                        "evento_id": saida.evento_id,
                        "pessoa_id": saida.pessoa_id,
                        "status": "exited",
                        "quantidade_deteccoes": saida.quantidade_deteccoes,
                        "camera": saida.camera,
                        "tipo": None,
                        "datetime": momento_saida,
                        "imagem": None,
                        "aparencia": (
                            aparencia_final.to_dict()
                            if aparencia_final is not None
                            else None
                        ),
                        "movimento": (
                            movimento_final.to_dict()
                            if movimento_final is not None
                            else None
                        ),
                        "contexto_cena": None,
                        "imagem_cena": None,
                        "quantidade_boxes_cena": None,
                    }
                    await event_hub.publicar(evento_painel)
                    logger.info(
                        "[RASTREAMENTO] Pessoa saiu: pessoa=%s camera=%s "
                        "deteccoes=%s amostras_visuais=%s amostras_movimento=%s "
                        "tempo_observado=%ss paineis=%s",
                        saida.pessoa_id,
                        saida.camera,
                        saida.quantidade_deteccoes,
                        (
                            aparencia_final.quantidade_amostras
                            if aparencia_final is not None
                            else 0
                        ),
                        (
                            movimento_final.quantidade_amostras
                            if movimento_final is not None
                            else 0
                        ),
                        (
                            movimento_final.tempo_observado_segundos
                            if movimento_final is not None
                            else 0
                        ),
                        event_hub.total_conexoes,
                    )

            except asyncio.CancelledError:
                logger.info("Monitor automático de saídas encerrado")
                raise
            except Exception:
                logger.exception("Erro no monitor automático de saídas")

    def _selecionar_boxes_pessoas(
        self,
        evento: CameraEvent,
    ) -> list[BoundingBox]:
        """Remove caixas inválidas, duplicadas e a caixa da imagem inteira."""

        if evento.imagem is None:
            return []

        largura_imagem = evento.imagem.largura
        altura_imagem = evento.imagem.altura
        if not largura_imagem or not altura_imagem:
            return []

        area_imagem = largura_imagem * altura_imagem
        assinaturas: set[tuple[int, int, int, int]] = set()
        caixas_validas: list[BoundingBox] = []

        for caixa in evento.bounding_boxes or []:
            if caixa.largura <= 0 or caixa.altura <= 0:
                continue

            x1 = max(0, min(caixa.x, largura_imagem - 1))
            y1 = max(0, min(caixa.y, altura_imagem - 1))
            x2 = max(x1 + 1, min(caixa.x2, largura_imagem))
            y2 = max(y1 + 1, min(caixa.y2, altura_imagem))
            largura = x2 - x1
            altura = y2 - y1
            percentual = largura * altura / area_imagem * 100

            if percentual >= 60 or percentual < 0.05:
                continue

            assinatura = (x1, y1, x2, y2)
            if assinatura in assinaturas:
                continue
            assinaturas.add(assinatura)

            if (
                x1 == caixa.x
                and y1 == caixa.y
                and largura == caixa.largura
                and altura == caixa.altura
            ):
                caixa_ajustada = caixa
            else:
                caixa_ajustada = caixa.model_copy(
                    update={
                        "x": x1,
                        "y": y1,
                        "largura": largura,
                        "altura": altura,
                        "x2": x2,
                        "y2": y2,
                    }
                )

            caixas_validas.append(caixa_ajustada)

        if not caixas_validas and evento.bounding_box_escolhida is not None:
            caixas_validas.append(evento.bounding_box_escolhida)

        caixas_validas.sort(
            key=lambda caixa: (
                caixa.x + caixa.largura / 2,
                caixa.y + caixa.altura / 2,
            )
        )
        return caixas_validas

    async def _analisar_aparencia(
        self,
        evento: CameraEvent,
        bounding_box: BoundingBox,
        pessoa_id: str,
        pacote_id: str,
    ):
        try:
            if evento.imagem is None or not evento.imagem.caminho_original:
                logger.info(
                    "[%s] Imagem sem caminho original; aparência não analisada",
                    pacote_id,
                )
                return None

            analise_visual = await asyncio.to_thread(
                analisar_pessoa,
                caminho_imagem=evento.imagem.caminho_original,
                x=bounding_box.x,
                y=bounding_box.y,
                largura=bounding_box.largura,
                altura=bounding_box.altura,
            )
            aparencia_estavel = await appearance_memory.registrar(
                pessoa_id=pessoa_id,
                analise=analise_visual,
            )
            logger.info(
                "[%s] Aparência estável: pessoa=%s cor=%s posição=%s "
                "tamanho=%s área_média=%s%% amostras=%s",
                pacote_id,
                pessoa_id,
                aparencia_estavel.cor_roupa_predominante,
                aparencia_estavel.posicao_atual,
                aparencia_estavel.tamanho_predominante,
                aparencia_estavel.percentual_medio_quadro,
                aparencia_estavel.quantidade_amostras,
            )
            return aparencia_estavel
        except Exception:
            logger.exception(
                "[%s] Não foi possível analisar a aparência da pessoa=%s",
                pacote_id,
                pessoa_id,
            )
            return None

    async def _analisar_contexto_cena(
        self,
        evento: CameraEvent,
        bounding_boxes: list[BoundingBox],
        pacote_id: str,
    ):
        try:
            if evento.imagem is None:
                return None

            largura_imagem = evento.imagem.largura
            altura_imagem = evento.imagem.altura
            if not largura_imagem or not altura_imagem:
                logger.info(
                    "[%s] Contexto não analisado: dimensões ausentes",
                    pacote_id,
                )
                return None

            contexto = await asyncio.to_thread(
                analisar_contexto_cena,
                largura_imagem=largura_imagem,
                altura_imagem=altura_imagem,
                bounding_boxes=bounding_boxes,
            )
            logger.info(
                "[%s] Contexto da cena: pessoas=%s esquerda=%s centro=%s "
                "direita=%s muito_proximos=%s proximos=%s separados=%s",
                pacote_id,
                contexto.quantidade_pessoas,
                contexto.pessoas_esquerda,
                contexto.pessoas_centro,
                contexto.pessoas_direita,
                contexto.pares_muito_proximos,
                contexto.pares_proximos,
                contexto.pares_separados,
            )
            logger.info("[%s] Descrição da cena: %s", pacote_id, contexto.descricao)
            return contexto
        except Exception:
            logger.exception("[%s] Não foi possível analisar a cena", pacote_id)
            return None

    async def _renderizar_cena(
        self,
        evento: CameraEvent,
        bounding_boxes: list[BoundingBox],
        pacote_id: str,
    ):
        try:
            if evento.imagem is None or not evento.imagem.caminho_original:
                return None
            if not bounding_boxes:
                return None

            resultado = await asyncio.to_thread(
                renderizar_cena_com_boxes,
                caminho_imagem=evento.imagem.caminho_original,
                bounding_boxes=bounding_boxes,
            )
            if resultado.quantidade_boxes == 0:
                return None

            logger.info(
                "[%s] Cena renderizada: boxes=%s dimensoes=%sx%s base64=%s caracteres",
                pacote_id,
                resultado.quantidade_boxes,
                resultado.largura_imagem,
                resultado.altura_imagem,
                len(resultado.imagem_base64),
            )
            return resultado
        except Exception:
            logger.exception(
                "[%s] Não foi possível renderizar a cena com boxes",
                pacote_id,
            )
            return None

    async def _publicar_atualizacao_parcial(
        self,
        camera: str,
        decisao: TrackingDecision,
        aparencia_estavel,
        movimento,
        contexto_cena,
        cena_renderizada,
        indice_na_cena: int,
        total_pessoas_cena: int,
        pacote_id: str,
    ) -> None:
        atualizacao_parcial = {
            "evento_id": decisao.evento_id,
            "pessoa_id": decisao.pessoa_id,
            "status": "appearance_updated",
            "quantidade_deteccoes": decisao.quantidade_deteccoes,
            "camera": camera,
            "aparencia": (
                aparencia_estavel.to_dict()
                if aparencia_estavel is not None
                else None
            ),
            "movimento": (
                movimento.to_dict()
                if movimento is not None
                else None
            ),
            "contexto_cena": (
                contexto_cena.to_dict() if contexto_cena is not None else None
            ),
            "imagem_cena": (
                cena_renderizada.imagem_base64
                if cena_renderizada is not None
                else None
            ),
            "quantidade_boxes_cena": (
                cena_renderizada.quantidade_boxes
                if cena_renderizada is not None
                else 0
            ),
            "indice_na_cena": indice_na_cena,
            "total_pessoas_cena": total_pessoas_cena,
        }
        await event_hub.publicar(atualizacao_parcial)
        logger.info(
            "[%s] Atualização parcial: pessoa=%s indice=%s/%s "
            "deteccoes=%s amostras=%s movimento=%s/%s velocidade=%spx_s "
            "boxes=%s paineis=%s",
            pacote_id,
            decisao.pessoa_id,
            indice_na_cena,
            total_pessoas_cena,
            decisao.quantidade_deteccoes,
            (
                aparencia_estavel.quantidade_amostras
                if aparencia_estavel is not None
                else 0
            ),
            (
                movimento.movimento_horizontal
                if movimento is not None
                else "-"
            ),
            (
                movimento.movimento_vertical
                if movimento is not None
                else "-"
            ),
            (
                movimento.velocidade_pixels_segundo
                if movimento is not None
                else 0
            ),
            (
                cena_renderizada.quantidade_boxes
                if cena_renderizada is not None
                else 0
            ),
            event_hub.total_conexoes,
        )

    async def _processar_pessoa(
        self,
        evento: CameraEvent,
        camera: str,
        bounding_box: BoundingBox,
        decisao: TrackingDecision,
        contexto_cena,
        cena_renderizada,
        indice_na_cena: int,
        total_pessoas_cena: int,
        pacote_id: str,
        imagem_background_base64: str | None,
    ) -> str | None:
        aparencia_estavel = await self._analisar_aparencia(
            evento=evento,
            bounding_box=bounding_box,
            pessoa_id=decisao.pessoa_id,
            pacote_id=pacote_id,
        )

        movimento = await person_movement_memory.registrar(
            pessoa_id=decisao.pessoa_id,
            bbox=decisao.bbox,
        )
        logger.info(
            "[%s] Movimento: pessoa=%s horizontal=%s vertical=%s "
            "tendencia=%s velocidade=%spx_s distancia_total=%spx "
            "tempo=%ss amostras=%s",
            pacote_id,
            decisao.pessoa_id,
            movimento.movimento_horizontal,
            movimento.movimento_vertical,
            movimento.tendencia_distancia,
            movimento.velocidade_pixels_segundo,
            movimento.distancia_total_pixels,
            movimento.tempo_observado_segundos,
            movimento.quantidade_amostras,
        )

        if not decisao.deve_processar:
            await self._publicar_atualizacao_parcial(
                camera=camera,
                decisao=decisao,
                aparencia_estavel=aparencia_estavel,
                movimento=movimento,
                contexto_cena=contexto_cena,
                cena_renderizada=cena_renderizada,
                indice_na_cena=indice_na_cena,
                total_pessoas_cena=total_pessoas_cena,
                pacote_id=pacote_id,
            )
            logger.info(
                "[%s] Evento repetido suprimido: pessoa=%s indice=%s/%s amostras=%s",
                pacote_id,
                decisao.pessoa_id,
                indice_na_cena,
                total_pessoas_cena,
                (
                    aparencia_estavel.quantidade_amostras
                    if aparencia_estavel is not None
                    else 0
                ),
            )
            return imagem_background_base64

        if evento.imagem is None or not evento.imagem.caminho_original:
            logger.info("[%s] Pessoa ignorada: caminho original ausente", pacote_id)
            return imagem_background_base64

        if imagem_background_base64 is None:
            imagem_background_base64 = await asyncio.to_thread(
                arquivo_para_base64,
                evento.imagem.caminho_original,
            )

        try:
            mensagem = await asyncio.to_thread(
                montar_alarm_detection_message,
                evento,
                bounding_box=bounding_box,
                evento_id=decisao.evento_id,
                imagem_background_base64=imagem_background_base64,
            )
        except ValueError as erro:
            logger.info(
                "[%s] Pessoa ignorada: pessoa=%s erro=%s",
                pacote_id,
                decisao.pessoa_id,
                erro,
            )
            return imagem_background_base64

        evento_painel = {
            "evento_id": decisao.evento_id,
            "pessoa_id": decisao.pessoa_id,
            "status": decisao.status,
            "quantidade_deteccoes": decisao.quantidade_deteccoes,
            "camera": mensagem.device.name,
            "tipo": mensagem.event.type,
            "datetime": mensagem.event.datetime,
            "imagem": mensagem.event.images.detection,
            "aparencia": (
                aparencia_estavel.to_dict()
                if aparencia_estavel is not None
                else None
            ),
            "movimento": movimento.to_dict(),
            "contexto_cena": (
                contexto_cena.to_dict() if contexto_cena is not None else None
            ),
            "imagem_cena": (
                cena_renderizada.imagem_base64
                if cena_renderizada is not None
                else None
            ),
            "quantidade_boxes_cena": (
                cena_renderizada.quantidade_boxes
                if cena_renderizada is not None
                else 0
            ),
            "indice_na_cena": indice_na_cena,
            "total_pessoas_cena": total_pessoas_cena,
        }
        await event_hub.publicar(evento_painel)
        logger.info(
            "[%s] Pessoa enviada ao painel: pessoa=%s status=%s indice=%s/%s paineis=%s",
            pacote_id,
            decisao.pessoa_id,
            decisao.status,
            indice_na_cena,
            total_pessoas_cena,
            event_hub.total_conexoes,
        )

        if not settings.kafka_enabled:
            logger.info(
                "[%s] Payload pronto; Kafka desativado: pessoa=%s",
                pacote_id,
                decisao.pessoa_id,
            )
            return imagem_background_base64

        resultado = await self.publisher.publicar(
            topico=self.topic,
            evento_id=decisao.evento_id,
            dados=mensagem.model_dump(mode="json"),
        )
        logger.info(
            "[%s] Pessoa publicada em %s p=%s offset=%s pessoa=%s indice=%s/%s",
            pacote_id,
            resultado["topico"],
            resultado["particao"],
            resultado["offset"],
            decisao.pessoa_id,
            indice_na_cena,
            total_pessoas_cena,
        )
        return imagem_background_base64

    async def _worker(self, numero: int) -> None:
        logger.info("Worker %s iniciado", numero)

        while True:
            pacote, body = await self.queue.get()

            try:
                adapter = camera_adapter_factory.encontrar_adapter(
                    content_type=pacote.content_type,
                    body=body,
                )
                logger.info(
                    "[%s] Worker %s usando %s",
                    pacote.evento_id,
                    numero,
                    adapter.__class__.__name__,
                )

                evento = await asyncio.to_thread(adapter.normalizar, pacote, body)
                if evento.imagem is None:
                    logger.info(
                        "[%s] Evento ignorado: não possui imagem",
                        pacote.evento_id,
                    )
                    continue

                bounding_boxes = self._selecionar_boxes_pessoas(evento)
                if not bounding_boxes:
                    logger.info(
                        "[%s] Evento ignorado: não possui boxes válidas de pessoas",
                        pacote.evento_id,
                    )
                    continue

                camera = (
                    evento.nome_camera
                    or evento.camera_id
                    or evento.ip_camera
                    or "Camera desconhecida"
                )
                caixas_rastreamento = [
                    DetectionBox(
                        x=caixa.x,
                        y=caixa.y,
                        largura=caixa.largura,
                        altura=caixa.altura,
                    )
                    for caixa in bounding_boxes
                ]
                decisoes = await person_tracker.registrar_lote(
                    camera=camera,
                    evento_id=evento.evento_id,
                    bboxes=caixas_rastreamento,
                )

                if len(decisoes) != len(bounding_boxes):
                    raise RuntimeError(
                        "Quantidade de decisões diferente da quantidade de boxes"
                    )

                total_pessoas = len(bounding_boxes)
                logger.info(
                    "[%s] Rastreamento em lote: pessoas=%s ids=%s",
                    pacote.evento_id,
                    total_pessoas,
                    [
                        (decisao.pessoa_id, decisao.status)
                        for decisao in decisoes
                    ],
                )

                contexto_cena = await self._analisar_contexto_cena(
                    evento=evento,
                    bounding_boxes=bounding_boxes,
                    pacote_id=pacote.evento_id,
                )
                cena_renderizada = await self._renderizar_cena(
                    evento=evento,
                    bounding_boxes=bounding_boxes,
                    pacote_id=pacote.evento_id,
                )

                imagem_background_base64: str | None = None
                for indice, (bounding_box, decisao) in enumerate(
                    zip(bounding_boxes, decisoes, strict=True),
                    start=1,
                ):
                    imagem_background_base64 = await self._processar_pessoa(
                        evento=evento,
                        camera=camera,
                        bounding_box=bounding_box,
                        decisao=decisao,
                        contexto_cena=contexto_cena,
                        cena_renderizada=cena_renderizada,
                        indice_na_cena=indice,
                        total_pessoas_cena=total_pessoas,
                        pacote_id=pacote.evento_id,
                        imagem_background_base64=imagem_background_base64,
                    )

            except asyncio.CancelledError:
                raise
            except Exception:
                logger.exception("[%s] Erro no processamento", pacote.evento_id)
            finally:
                self.queue.task_done()

------------------------------------------------------------
ARQUIVO: app/services/camera_event_pipeline_melhorado.py
------------------------------------------------------------
import asyncio
import logging
from datetime import datetime, timezone

from app.adapters.cameras.factory import camera_adapter_factory
from app.core.config import settings
from app.domain.models.camera_event import BoundingBox, CameraEvent, RawCameraPackage
from app.messaging.kafka_producer_melhorado import KafkaPublisher
from app.services.alarm_detection_payload import (
    arquivo_para_base64,
    montar_alarm_detection_message,
    recortar_deteccao_base64,
)
from app.services.appearance_memory import appearance_memory
from app.services.event_hub import event_hub
from app.services.person_tracker import (
    DetectionBox,
    TrackingDecision,
    person_tracker,
)
from app.services.person_movement import person_movement_memory
from app.services.person_detector_yolo import (
    combinar_bounding_boxes,
    detectar_pessoas_yolo,
)
from app.services.scene_analyzer import analisar_pessoa
from app.services.scene_context_analyzer import analisar_contexto_cena
from app.services.scene_renderer import renderizar_cena_com_boxes

logger = logging.getLogger(__name__)


class CameraEventPipeline:
    def __init__(
        self,
        publisher: KafkaPublisher,
        topic: str,
        maxsize: int = 1000,
        worker_count: int = 4,
    ) -> None:
        self.publisher = publisher
        self.topic = topic
        self.worker_count = worker_count
        self.queue: asyncio.Queue[tuple[RawCameraPackage, bytes]] = asyncio.Queue(
            maxsize=maxsize
        )
        self._worker_tasks: list[asyncio.Task] = []
        self._exit_task: asyncio.Task | None = None
        self._iniciado = False

    @property
    def tamanho_fila(self) -> int:
        return self.queue.qsize()

    @property
    def capacidade_fila(self) -> int:
        return self.queue.maxsize

    async def start(self) -> None:
        if self._iniciado:
            return

        if settings.kafka_enabled:
            await self.publisher.start()
        else:
            logger.warning(
                "Kafka desativado: eventos serão processados, mas não publicados."
            )

        self._worker_tasks = [
            asyncio.create_task(
                self._worker(numero),
                name=f"camera-worker-{numero}",
            )
            for numero in range(1, self.worker_count + 1)
        ]
        self._exit_task = asyncio.create_task(
            self._monitorar_saidas(),
            name="person-exit-monitor",
        )
        self._iniciado = True
        logger.info("Pipeline iniciado com %s workers", self.worker_count)

    async def stop(self) -> None:
        if not self._iniciado:
            return

        try:
            await asyncio.wait_for(self.queue.join(), timeout=10)
        except TimeoutError:
            logger.warning("Encerrando com %s itens na fila", self.queue.qsize())

        tarefas: list[asyncio.Task] = [*self._worker_tasks]
        if self._exit_task is not None:
            tarefas.append(self._exit_task)

        for tarefa in tarefas:
            tarefa.cancel()

        if tarefas:
            await asyncio.gather(*tarefas, return_exceptions=True)

        if self.publisher.iniciado:
            await self.publisher.stop()

        await appearance_memory.limpar()
        await person_movement_memory.limpar()
        await person_tracker.limpar()

        self._worker_tasks.clear()
        self._exit_task = None
        self._iniciado = False
        logger.info("Pipeline encerrado")

    def adicionar(self, pacote: RawCameraPackage, body: bytes) -> None:
        self.queue.put_nowait((pacote, body))

    async def _monitorar_saidas(self) -> None:
        logger.info("Monitor automático de saídas iniciado")

        while True:
            try:
                await asyncio.sleep(1)
                saidas = await person_tracker.coletar_saidas()

                for saida in saidas:
                    aparencia_final = await appearance_memory.finalizar(
                        saida.pessoa_id
                    )
                    movimento_final = await person_movement_memory.finalizar(
                        saida.pessoa_id
                    )
                    momento_saida = (
                        datetime.now(timezone.utc)
                        .isoformat()
                        .replace("+00:00", "Z")
                    )
                    evento_painel = {
                        "evento_id": saida.evento_id,
                        "pessoa_id": saida.pessoa_id,
                        "status": "exited",
                        "quantidade_deteccoes": saida.quantidade_deteccoes,
                        "camera": saida.camera,
                        "tipo": None,
                        "datetime": momento_saida,
                        "attributes": None,
                        "imagem": None,
                        "aparencia": (
                            aparencia_final.to_dict()
                            if aparencia_final is not None
                            else None
                        ),
                        "movimento": (
                            movimento_final.to_dict()
                            if movimento_final is not None
                            else None
                        ),
                        "contexto_cena": None,
                        "imagem_cena": None,
                        "quantidade_boxes_cena": None,
                    }
                    await event_hub.publicar(evento_painel)
                    logger.info(
                        "[RASTREAMENTO] Pessoa saiu: pessoa=%s camera=%s "
                        "deteccoes=%s amostras_visuais=%s amostras_movimento=%s "
                        "tempo_observado=%ss paineis=%s",
                        saida.pessoa_id,
                        saida.camera,
                        saida.quantidade_deteccoes,
                        (
                            aparencia_final.quantidade_amostras
                            if aparencia_final is not None
                            else 0
                        ),
                        (
                            movimento_final.quantidade_amostras
                            if movimento_final is not None
                            else 0
                        ),
                        (
                            movimento_final.tempo_observado_segundos
                            if movimento_final is not None
                            else 0
                        ),
                        event_hub.total_conexoes,
                    )

            except asyncio.CancelledError:
                logger.info("Monitor automático de saídas encerrado")
                raise
            except Exception:
                logger.exception("Erro no monitor automático de saídas")

    def _selecionar_boxes_pessoas(
        self,
        evento: CameraEvent,
    ) -> list[BoundingBox]:
        """Remove caixas inválidas, duplicadas e a caixa da imagem inteira."""

        if evento.imagem is None:
            return []

        largura_imagem = evento.imagem.largura
        altura_imagem = evento.imagem.altura
        if not largura_imagem or not altura_imagem:
            return []

        area_imagem = largura_imagem * altura_imagem
        assinaturas: set[tuple[int, int, int, int]] = set()
        caixas_validas: list[BoundingBox] = []

        for caixa in evento.bounding_boxes or []:
            if caixa.largura <= 0 or caixa.altura <= 0:
                continue

            x1 = max(0, min(caixa.x, largura_imagem - 1))
            y1 = max(0, min(caixa.y, altura_imagem - 1))
            x2 = max(x1 + 1, min(caixa.x2, largura_imagem))
            y2 = max(y1 + 1, min(caixa.y2, altura_imagem))
            largura = x2 - x1
            altura = y2 - y1
            percentual = largura * altura / area_imagem * 100

            if percentual >= 60 or percentual < 0.05:
                continue

            assinatura = (x1, y1, x2, y2)
            if assinatura in assinaturas:
                continue
            assinaturas.add(assinatura)

            if (
                x1 == caixa.x
                and y1 == caixa.y
                and largura == caixa.largura
                and altura == caixa.altura
            ):
                caixa_ajustada = caixa
            else:
                caixa_ajustada = caixa.model_copy(
                    update={
                        "x": x1,
                        "y": y1,
                        "largura": largura,
                        "altura": altura,
                        "x2": x2,
                        "y2": y2,
                    }
                )

            caixas_validas.append(caixa_ajustada)

        if not caixas_validas and evento.bounding_box_escolhida is not None:
            caixas_validas.append(evento.bounding_box_escolhida)

        caixas_validas.sort(
            key=lambda caixa: (
                caixa.x + caixa.largura / 2,
                caixa.y + caixa.altura / 2,
            )
        )
        return caixas_validas

    async def _analisar_aparencia(
        self,
        evento: CameraEvent,
        bounding_box: BoundingBox,
        pessoa_id: str,
        pacote_id: str,
    ):
        try:
            if evento.imagem is None or not evento.imagem.caminho_original:
                logger.info(
                    "[%s] Imagem sem caminho original; aparência não analisada",
                    pacote_id,
                )
                return None

            percentual_caixa = (
                float(
                    bounding_box.proporcao_imagem
                    or 0.0
                )
                * 100
            )

            # Uma pessoa muito pequena não possui
            # pixels de roupa suficientes para uma
            # classificação confiável.
            if (
                bounding_box.largura < 60
                or bounding_box.altura < 120
                or percentual_caixa < 1.00
            ):
                logger.info(
                    "[%s] Aparência ignorada: "
                    "caixa pequena pessoa=%s "
                    "dimensoes=%sx%s area=%.2f%%",
                    pacote_id,
                    pessoa_id,
                    bounding_box.largura,
                    bounding_box.altura,
                    percentual_caixa,
                )

                return await appearance_memory.obter(
                    pessoa_id
                )

            analise_visual = await asyncio.to_thread(
                analisar_pessoa,
                caminho_imagem=evento.imagem.caminho_original,
                x=bounding_box.x,
                y=bounding_box.y,
                largura=bounding_box.largura,
                altura=bounding_box.altura,
            )
            # Uma leitura ambígua não entra na
            # votação da memória. Caso já exista uma
            # aparência confiável, ela é preservada.
            if (
                analise_visual.cor_roupa_aproximada
                == "escura-indefinida"
            ):
                logger.info(
                    "[%s] Cor escura indefinida: "
                    "pessoa=%s rgb=%s; "
                    "mantendo aparência anterior",
                    pacote_id,
                    pessoa_id,
                    analise_visual.rgb_representativo,
                )

                return await appearance_memory.obter(
                    pessoa_id
                )

            aparencia_estavel = await appearance_memory.registrar(
                pessoa_id=pessoa_id,
                analise=analise_visual,
            )
            logger.info(
                "[%s] Aparência estável: pessoa=%s cor=%s posição=%s "
                "tamanho=%s área_média=%s%% amostras=%s",
                pacote_id,
                pessoa_id,
                aparencia_estavel.cor_roupa_predominante,
                aparencia_estavel.posicao_atual,
                aparencia_estavel.tamanho_predominante,
                aparencia_estavel.percentual_medio_quadro,
                aparencia_estavel.quantidade_amostras,
            )
            return aparencia_estavel
        except Exception:
            logger.exception(
                "[%s] Não foi possível analisar a aparência da pessoa=%s",
                pacote_id,
                pessoa_id,
            )
            return None

    async def _analisar_contexto_cena(
        self,
        evento: CameraEvent,
        bounding_boxes: list[BoundingBox],
        pacote_id: str,
    ):
        try:
            if evento.imagem is None:
                return None

            largura_imagem = evento.imagem.largura
            altura_imagem = evento.imagem.altura
            if not largura_imagem or not altura_imagem:
                logger.info(
                    "[%s] Contexto não analisado: dimensões ausentes",
                    pacote_id,
                )
                return None

            contexto = await asyncio.to_thread(
                analisar_contexto_cena,
                largura_imagem=largura_imagem,
                altura_imagem=altura_imagem,
                bounding_boxes=bounding_boxes,
            )
            logger.info(
                "[%s] Contexto da cena: pessoas=%s esquerda=%s centro=%s "
                "direita=%s muito_proximos=%s proximos=%s separados=%s",
                pacote_id,
                contexto.quantidade_pessoas,
                contexto.pessoas_esquerda,
                contexto.pessoas_centro,
                contexto.pessoas_direita,
                contexto.pares_muito_proximos,
                contexto.pares_proximos,
                contexto.pares_separados,
            )
            logger.info("[%s] Descrição da cena: %s", pacote_id, contexto.descricao)
            return contexto
        except Exception:
            logger.exception("[%s] Não foi possível analisar a cena", pacote_id)
            return None

    async def _renderizar_cena(
        self,
        evento: CameraEvent,
        bounding_boxes: list[BoundingBox],
        pacote_id: str,
    ):
        try:
            if evento.imagem is None or not evento.imagem.caminho_original:
                return None
            if not bounding_boxes:
                return None

            resultado = await asyncio.to_thread(
                renderizar_cena_com_boxes,
                caminho_imagem=evento.imagem.caminho_original,
                bounding_boxes=bounding_boxes,
            )
            if resultado.quantidade_boxes == 0:
                return None

            logger.info(
                "[%s] Cena renderizada: boxes=%s dimensoes=%sx%s base64=%s caracteres",
                pacote_id,
                resultado.quantidade_boxes,
                resultado.largura_imagem,
                resultado.altura_imagem,
                len(resultado.imagem_base64),
            )
            return resultado
        except Exception:
            logger.exception(
                "[%s] Não foi possível renderizar a cena com boxes",
                pacote_id,
            )
            return None

    async def _publicar_atualizacao_parcial(
        self,
        camera: str,
        decisao: TrackingDecision,
        aparencia_estavel,
        movimento,
        contexto_cena,
        cena_renderizada,
        imagem_recorte_base64: str | None,
        indice_na_cena: int,
        total_pessoas_cena: int,
        pacote_id: str,
    ) -> None:
        atualizacao_parcial = {
            "evento_id": decisao.evento_id,
            "pessoa_id": decisao.pessoa_id,
            "status": "appearance_updated",
            "quantidade_deteccoes": decisao.quantidade_deteccoes,
            "camera": camera,
            "imagem": imagem_recorte_base64,
            "aparencia": (
                aparencia_estavel.to_dict()
                if aparencia_estavel is not None
                else None
            ),
            "movimento": (
                movimento.to_dict()
                if movimento is not None
                else None
            ),
            "contexto_cena": (
                contexto_cena.to_dict() if contexto_cena is not None else None
            ),
            "imagem_cena": (
                cena_renderizada.imagem_base64
                if cena_renderizada is not None
                else None
            ),
            "quantidade_boxes_cena": (
                cena_renderizada.quantidade_boxes
                if cena_renderizada is not None
                else 0
            ),
            "indice_na_cena": indice_na_cena,
            "total_pessoas_cena": total_pessoas_cena,
        }
        await event_hub.publicar(atualizacao_parcial)
        logger.info(
            "[%s] Atualização parcial: pessoa=%s indice=%s/%s "
            "deteccoes=%s amostras=%s movimento=%s/%s velocidade=%spx_s "
            "boxes=%s paineis=%s",
            pacote_id,
            decisao.pessoa_id,
            indice_na_cena,
            total_pessoas_cena,
            decisao.quantidade_deteccoes,
            (
                aparencia_estavel.quantidade_amostras
                if aparencia_estavel is not None
                else 0
            ),
            (
                movimento.movimento_horizontal
                if movimento is not None
                else "-"
            ),
            (
                movimento.movimento_vertical
                if movimento is not None
                else "-"
            ),
            (
                movimento.velocidade_pixels_segundo
                if movimento is not None
                else 0
            ),
            (
                cena_renderizada.quantidade_boxes
                if cena_renderizada is not None
                else 0
            ),
            event_hub.total_conexoes,
        )

    async def _processar_pessoa(
        self,
        evento: CameraEvent,
        camera: str,
        bounding_box: BoundingBox,
        decisao: TrackingDecision,
        contexto_cena,
        cena_renderizada,
        indice_na_cena: int,
        total_pessoas_cena: int,
        pacote_id: str,
        imagem_background_base64: str | None,
    ) -> str | None:
        aparencia_estavel = await self._analisar_aparencia(
            evento=evento,
            bounding_box=bounding_box,
            pessoa_id=decisao.pessoa_id,
            pacote_id=pacote_id,
        )

        movimento = await person_movement_memory.registrar(
            pessoa_id=decisao.pessoa_id,
            bbox=decisao.bbox,
        )
        logger.info(
            "[%s] Movimento: pessoa=%s horizontal=%s vertical=%s "
            "tendencia=%s velocidade=%spx_s distancia_total=%spx "
            "tempo=%ss amostras=%s",
            pacote_id,
            decisao.pessoa_id,
            movimento.movimento_horizontal,
            movimento.movimento_vertical,
            movimento.tendencia_distancia,
            movimento.velocidade_pixels_segundo,
            movimento.distancia_total_pixels,
            movimento.tempo_observado_segundos,
            movimento.quantidade_amostras,
        )

        if not decisao.deve_processar:
            imagem_recorte_base64 = None

            if (
                evento.imagem is not None
                and evento.imagem.caminho_original
            ):
                try:
                    imagem_recorte_base64 = (
                        await asyncio.to_thread(
                            recortar_deteccao_base64,
                            evento.imagem.caminho_original,
                            bounding_box,
                        )
                    )
                except Exception:
                    logger.exception(
                        "[%s] Não foi possível atualizar "
                        "o recorte da pessoa=%s",
                        pacote_id,
                        decisao.pessoa_id,
                    )

            await self._publicar_atualizacao_parcial(
                camera=camera,
                decisao=decisao,
                aparencia_estavel=aparencia_estavel,
                movimento=movimento,
                contexto_cena=contexto_cena,
                cena_renderizada=cena_renderizada,
                imagem_recorte_base64=(
                    imagem_recorte_base64
                ),
                indice_na_cena=indice_na_cena,
                total_pessoas_cena=total_pessoas_cena,
                pacote_id=pacote_id,
            )
            logger.info(
                "[%s] Evento repetido suprimido: pessoa=%s indice=%s/%s amostras=%s",
                pacote_id,
                decisao.pessoa_id,
                indice_na_cena,
                total_pessoas_cena,
                (
                    aparencia_estavel.quantidade_amostras
                    if aparencia_estavel is not None
                    else 0
                ),
            )
            return imagem_background_base64

        if evento.imagem is None or not evento.imagem.caminho_original:
            logger.info("[%s] Pessoa ignorada: caminho original ausente", pacote_id)
            return imagem_background_base64

        if imagem_background_base64 is None:
            imagem_background_base64 = await asyncio.to_thread(
                arquivo_para_base64,
                evento.imagem.caminho_original,
            )

        try:
            mensagem = await asyncio.to_thread(
                montar_alarm_detection_message,
                evento,
                bounding_box=bounding_box,
                evento_id=decisao.evento_id,
                imagem_background_base64=imagem_background_base64,
            )
        except ValueError as erro:
            logger.info(
                "[%s] Pessoa ignorada: pessoa=%s erro=%s",
                pacote_id,
                decisao.pessoa_id,
                erro,
            )
            return imagem_background_base64

        evento_painel = {
            "evento_id": decisao.evento_id,
            "pessoa_id": decisao.pessoa_id,
            "status": decisao.status,
            "quantidade_deteccoes": decisao.quantidade_deteccoes,
            "camera": mensagem.device.name,
            "tipo": mensagem.event.type,
            "datetime": mensagem.event.datetime,
            "attributes": (
                mensagem.event.attributes.model_dump(
                    mode="json"
                )
                if mensagem.event.attributes is not None
                else None
            ),
            "imagem": mensagem.event.images.detection,
            "aparencia": (
                aparencia_estavel.to_dict()
                if aparencia_estavel is not None
                else None
            ),
            "movimento": movimento.to_dict(),
            "contexto_cena": (
                contexto_cena.to_dict() if contexto_cena is not None else None
            ),
            "imagem_cena": (
                cena_renderizada.imagem_base64
                if cena_renderizada is not None
                else None
            ),
            "quantidade_boxes_cena": (
                cena_renderizada.quantidade_boxes
                if cena_renderizada is not None
                else 0
            ),
            "indice_na_cena": indice_na_cena,
            "total_pessoas_cena": total_pessoas_cena,
        }
        await event_hub.publicar(evento_painel)
        logger.info(
            "[%s] Pessoa enviada ao painel: pessoa=%s status=%s indice=%s/%s paineis=%s",
            pacote_id,
            decisao.pessoa_id,
            decisao.status,
            indice_na_cena,
            total_pessoas_cena,
            event_hub.total_conexoes,
        )

        if not settings.kafka_enabled:
            logger.info(
                "[%s] Payload pronto; Kafka desativado: pessoa=%s",
                pacote_id,
                decisao.pessoa_id,
            )
            return imagem_background_base64

        resultado = await self.publisher.publicar(
            topico=self.topic,
            evento_id=decisao.evento_id,
            dados=mensagem.model_dump(
                mode="json",
                by_alias=True,
                exclude_none=True,
            ),
        )
        logger.info(
            "[%s] Pessoa publicada em %s p=%s offset=%s pessoa=%s indice=%s/%s",
            pacote_id,
            resultado["topico"],
            resultado["particao"],
            resultado["offset"],
            decisao.pessoa_id,
            indice_na_cena,
            total_pessoas_cena,
        )
        return imagem_background_base64

    async def _worker(self, numero: int) -> None:
        logger.info("Worker %s iniciado", numero)

        while True:
            pacote, body = await self.queue.get()

            try:
                adapter = camera_adapter_factory.encontrar_adapter(
                    content_type=pacote.content_type,
                    body=body,
                )
                logger.info(
                    "[%s] Worker %s usando %s",
                    pacote.evento_id,
                    numero,
                    adapter.__class__.__name__,
                )

                evento = await asyncio.to_thread(adapter.normalizar, pacote, body)

                logger.info(
                    "[%s] OBJETO NORMALIZADO (%s):\n%s",
                    pacote.evento_id,
                    evento.fabricante,
                    evento.model_dump_json(indent=2, by_alias=True, exclude_none=True),
                )

                if evento.imagem is None:
                    logger.info(
                        "[%s] Evento ignorado: não possui imagem",
                        pacote.evento_id,
                    )
                    continue

                bounding_boxes_camera = (
                    self._selecionar_boxes_pessoas(
                        evento
                    )
                )

                bounding_boxes_yolo: list[
                    BoundingBox
                ] = []

                if evento.imagem.caminho_original:
                    try:
                        bounding_boxes_yolo = (
                            await asyncio.to_thread(
                                detectar_pessoas_yolo,
                                evento.imagem.caminho_original,
                            )
                        )
                    except Exception:
                        logger.exception(
                            "[%s] Falha no YOLO; "
                            "usando caixas da câmera",
                            pacote.evento_id,
                        )

                bounding_boxes = combinar_bounding_boxes(
                    caixas_camera=bounding_boxes_camera,
                    caixas_yolo=bounding_boxes_yolo,
                )

                logger.info(
                    "[%s] Boxes: câmera=%s "
                    "yolo=%s final=%s",
                    pacote.evento_id,
                    len(bounding_boxes_camera),
                    len(bounding_boxes_yolo),
                    len(bounding_boxes),
                )
                if not bounding_boxes:
                    logger.info(
                        "[%s] Evento ignorado: não possui boxes válidas de pessoas",
                        pacote.evento_id,
                    )
                    continue

                camera = (
                    evento.nome_camera
                    or evento.camera_id
                    or evento.ip_camera
                    or "Camera desconhecida"
                )
                caixas_rastreamento = [
                    DetectionBox(
                        x=caixa.x,
                        y=caixa.y,
                        largura=caixa.largura,
                        altura=caixa.altura,
                    )
                    for caixa in bounding_boxes
                ]
                decisoes = await person_tracker.registrar_lote(
                    camera=camera,
                    evento_id=evento.evento_id,
                    bboxes=caixas_rastreamento,
                )

                if len(decisoes) != len(bounding_boxes):
                    raise RuntimeError(
                        "Quantidade de decisões diferente da quantidade de boxes"
                    )

                total_pessoas = len(bounding_boxes)
                logger.info(
                    "[%s] Rastreamento em lote: pessoas=%s ids=%s",
                    pacote.evento_id,
                    total_pessoas,
                    [
                        (decisao.pessoa_id, decisao.status)
                        for decisao in decisoes
                    ],
                )

                contexto_cena = await self._analisar_contexto_cena(
                    evento=evento,
                    bounding_boxes=bounding_boxes,
                    pacote_id=pacote.evento_id,
                )
                cena_renderizada = await self._renderizar_cena(
                    evento=evento,
                    bounding_boxes=bounding_boxes,
                    pacote_id=pacote.evento_id,
                )

                imagem_background_base64: str | None = None
                for indice, (bounding_box, decisao) in enumerate(
                    zip(bounding_boxes, decisoes, strict=True),
                    start=1,
                ):
                    imagem_background_base64 = await self._processar_pessoa(
                        evento=evento,
                        camera=camera,
                        bounding_box=bounding_box,
                        decisao=decisao,
                        contexto_cena=contexto_cena,
                        cena_renderizada=cena_renderizada,
                        indice_na_cena=indice,
                        total_pessoas_cena=total_pessoas,
                        pacote_id=pacote.evento_id,
                        imagem_background_base64=imagem_background_base64,
                    )

            except asyncio.CancelledError:
                raise
            except Exception:
                logger.exception("[%s] Erro no processamento", pacote.evento_id)
            finally:
                self.queue.task_done()

------------------------------------------------------------
ARQUIVO: app/services/camera_event_pipeline.py
------------------------------------------------------------
import asyncio
import logging
from datetime import datetime, timezone

from app.adapters.cameras.factory import camera_adapter_factory
from app.core.config import settings
from app.domain.models.camera_event import BoundingBox, CameraEvent, RawCameraPackage
from app.messaging.kafka_producer import KafkaPublisher
from app.services.alarm_detection_payload import (
    arquivo_para_base64,
    montar_alarm_detection_message,
    recortar_deteccao_base64,
)
from app.services.appearance_memory import appearance_memory
from app.services.event_hub import event_hub
from app.services.person_tracker import (
    DetectionBox,
    TrackingDecision,
    person_tracker,
)
from app.services.person_movement import person_movement_memory
from app.services.scene_analyzer import analisar_pessoa
from app.services.scene_context_analyzer import analisar_contexto_cena
from app.services.scene_renderer import renderizar_cena_com_boxes

logger = logging.getLogger(__name__)


class CameraEventPipeline:
    def __init__(
        self,
        publisher: KafkaPublisher,
        topic: str,
        maxsize: int = 1000,
        worker_count: int = 4,
    ) -> None:
        self.publisher = publisher
        self.topic = topic
        self.worker_count = worker_count
        self.queue: asyncio.Queue[tuple[RawCameraPackage, bytes]] = asyncio.Queue(
            maxsize=maxsize
        )
        self._worker_tasks: list[asyncio.Task] = []
        self._exit_task: asyncio.Task | None = None
        self._iniciado = False

    @property
    def tamanho_fila(self) -> int:
        return self.queue.qsize()

    @property
    def capacidade_fila(self) -> int:
        return self.queue.maxsize

    async def start(self) -> None:
        if self._iniciado:
            return

        if settings.kafka_enabled:
            await self.publisher.start()
        else:
            logger.warning(
                "Kafka desativado: eventos serão processados, mas não publicados."
            )

        self._worker_tasks = [
            asyncio.create_task(
                self._worker(numero),
                name=f"camera-worker-{numero}",
            )
            for numero in range(1, self.worker_count + 1)
        ]
        self._exit_task = asyncio.create_task(
            self._monitorar_saidas(),
            name="person-exit-monitor",
        )
        self._iniciado = True
        logger.info("Pipeline iniciado com %s workers", self.worker_count)

    async def stop(self) -> None:
        if not self._iniciado:
            return

        try:
            await asyncio.wait_for(self.queue.join(), timeout=10)
        except TimeoutError:
            logger.warning("Encerrando com %s itens na fila", self.queue.qsize())

        tarefas: list[asyncio.Task] = [*self._worker_tasks]
        if self._exit_task is not None:
            tarefas.append(self._exit_task)

        for tarefa in tarefas:
            tarefa.cancel()

        if tarefas:
            await asyncio.gather(*tarefas, return_exceptions=True)

        if self.publisher.iniciado:
            await self.publisher.stop()

        await appearance_memory.limpar()
        await person_movement_memory.limpar()
        await person_tracker.limpar()

        self._worker_tasks.clear()
        self._exit_task = None
        self._iniciado = False
        logger.info("Pipeline encerrado")

    def adicionar(self, pacote: RawCameraPackage, body: bytes) -> None:
        self.queue.put_nowait((pacote, body))

    async def _monitorar_saidas(self) -> None:
        logger.info("Monitor automático de saídas iniciado")

        while True:
            try:
                await asyncio.sleep(1)
                saidas = await person_tracker.coletar_saidas()

                for saida in saidas:
                    aparencia_final = await appearance_memory.finalizar(
                        saida.pessoa_id
                    )
                    movimento_final = await person_movement_memory.finalizar(
                        saida.pessoa_id
                    )
                    momento_saida = (
                        datetime.now(timezone.utc)
                        .isoformat()
                        .replace("+00:00", "Z")
                    )
                    evento_painel = {
                        "evento_id": saida.evento_id,
                        "pessoa_id": saida.pessoa_id,
                        "status": "exited",
                        "quantidade_deteccoes": saida.quantidade_deteccoes,
                        "camera": saida.camera,
                        "tipo": None,
                        "datetime": momento_saida,
                        "attributes": None,
                        "imagem": None,
                        "aparencia": (
                            aparencia_final.to_dict()
                            if aparencia_final is not None
                            else None
                        ),
                        "movimento": (
                            movimento_final.to_dict()
                            if movimento_final is not None
                            else None
                        ),
                        "contexto_cena": None,
                        "imagem_cena": None,
                        "quantidade_boxes_cena": None,
                    }
                    await event_hub.publicar(evento_painel)
                    logger.info(
                        "[RASTREAMENTO] Pessoa saiu: pessoa=%s camera=%s "
                        "deteccoes=%s amostras_visuais=%s amostras_movimento=%s "
                        "tempo_observado=%ss paineis=%s",
                        saida.pessoa_id,
                        saida.camera,
                        saida.quantidade_deteccoes,
                        (
                            aparencia_final.quantidade_amostras
                            if aparencia_final is not None
                            else 0
                        ),
                        (
                            movimento_final.quantidade_amostras
                            if movimento_final is not None
                            else 0
                        ),
                        (
                            movimento_final.tempo_observado_segundos
                            if movimento_final is not None
                            else 0
                        ),
                        event_hub.total_conexoes,
                    )

            except asyncio.CancelledError:
                logger.info("Monitor automático de saídas encerrado")
                raise
            except Exception:
                logger.exception("Erro no monitor automático de saídas")

    def _selecionar_boxes_pessoas(
        self,
        evento: CameraEvent,
    ) -> list[BoundingBox]:
        """Remove caixas inválidas, duplicadas e a caixa da imagem inteira."""

        if evento.imagem is None:
            return []

        largura_imagem = evento.imagem.largura
        altura_imagem = evento.imagem.altura
        if not largura_imagem or not altura_imagem:
            return []

        area_imagem = largura_imagem * altura_imagem
        assinaturas: set[tuple[int, int, int, int]] = set()
        caixas_validas: list[BoundingBox] = []

        for caixa in evento.bounding_boxes or []:
            if caixa.largura <= 0 or caixa.altura <= 0:
                continue

            x1 = max(0, min(caixa.x, largura_imagem - 1))
            y1 = max(0, min(caixa.y, altura_imagem - 1))
            x2 = max(x1 + 1, min(caixa.x2, largura_imagem))
            y2 = max(y1 + 1, min(caixa.y2, altura_imagem))
            largura = x2 - x1
            altura = y2 - y1
            percentual = largura * altura / area_imagem * 100

            if percentual >= 60 or percentual < 0.05:
                continue

            assinatura = (x1, y1, x2, y2)
            if assinatura in assinaturas:
                continue
            assinaturas.add(assinatura)

            if (
                x1 == caixa.x
                and y1 == caixa.y
                and largura == caixa.largura
                and altura == caixa.altura
            ):
                caixa_ajustada = caixa
            else:
                caixa_ajustada = caixa.model_copy(
                    update={
                        "x": x1,
                        "y": y1,
                        "largura": largura,
                        "altura": altura,
                        "x2": x2,
                        "y2": y2,
                    }
                )

            caixas_validas.append(caixa_ajustada)

        if not caixas_validas and evento.bounding_box_escolhida is not None:
            caixas_validas.append(evento.bounding_box_escolhida)

        caixas_validas.sort(
            key=lambda caixa: (
                caixa.x + caixa.largura / 2,
                caixa.y + caixa.altura / 2,
            )
        )
        return caixas_validas

    async def _analisar_aparencia(
        self,
        evento: CameraEvent,
        bounding_box: BoundingBox,
        pessoa_id: str,
        pacote_id: str,
    ):
        try:
            if evento.imagem is None or not evento.imagem.caminho_original:
                logger.info(
                    "[%s] Imagem sem caminho original; aparência não analisada",
                    pacote_id,
                )
                return None

            percentual_caixa = (
                float(
                    bounding_box.proporcao_imagem
                    or 0.0
                )
                * 100
            )

            # Uma pessoa muito pequena não possui
            # pixels de roupa suficientes para uma
            # classificação confiável.
            if (
                bounding_box.largura < 60
                or bounding_box.altura < 120
                or percentual_caixa < 1.00
            ):
                logger.info(
                    "[%s] Aparência ignorada: "
                    "caixa pequena pessoa=%s "
                    "dimensoes=%sx%s area=%.2f%%",
                    pacote_id,
                    pessoa_id,
                    bounding_box.largura,
                    bounding_box.altura,
                    percentual_caixa,
                )

                return await appearance_memory.obter(
                    pessoa_id
                )

            analise_visual = await asyncio.to_thread(
                analisar_pessoa,
                caminho_imagem=evento.imagem.caminho_original,
                x=bounding_box.x,
                y=bounding_box.y,
                largura=bounding_box.largura,
                altura=bounding_box.altura,
            )
            # Uma leitura ambígua não entra na
            # votação da memória. Caso já exista uma
            # aparência confiável, ela é preservada.
            if (
                analise_visual.cor_roupa_aproximada
                == "escura-indefinida"
            ):
                logger.info(
                    "[%s] Cor escura indefinida: "
                    "pessoa=%s rgb=%s; "
                    "mantendo aparência anterior",
                    pacote_id,
                    pessoa_id,
                    analise_visual.rgb_representativo,
                )

                return await appearance_memory.obter(
                    pessoa_id
                )

            aparencia_estavel = await appearance_memory.registrar(
                pessoa_id=pessoa_id,
                analise=analise_visual,
            )
            logger.info(
                "[%s] Aparência estável: pessoa=%s cor=%s posição=%s "
                "tamanho=%s área_média=%s%% amostras=%s",
                pacote_id,
                pessoa_id,
                aparencia_estavel.cor_roupa_predominante,
                aparencia_estavel.posicao_atual,
                aparencia_estavel.tamanho_predominante,
                aparencia_estavel.percentual_medio_quadro,
                aparencia_estavel.quantidade_amostras,
            )
            return aparencia_estavel
        except Exception:
            logger.exception(
                "[%s] Não foi possível analisar a aparência da pessoa=%s",
                pacote_id,
                pessoa_id,
            )
            return None

    async def _analisar_contexto_cena(
        self,
        evento: CameraEvent,
        bounding_boxes: list[BoundingBox],
        pacote_id: str,
    ):
        try:
            if evento.imagem is None:
                return None

            largura_imagem = evento.imagem.largura
            altura_imagem = evento.imagem.altura
            if not largura_imagem or not altura_imagem:
                logger.info(
                    "[%s] Contexto não analisado: dimensões ausentes",
                    pacote_id,
                )
                return None

            contexto = await asyncio.to_thread(
                analisar_contexto_cena,
                largura_imagem=largura_imagem,
                altura_imagem=altura_imagem,
                bounding_boxes=bounding_boxes,
            )
            logger.info(
                "[%s] Contexto da cena: pessoas=%s esquerda=%s centro=%s "
                "direita=%s muito_proximos=%s proximos=%s separados=%s",
                pacote_id,
                contexto.quantidade_pessoas,
                contexto.pessoas_esquerda,
                contexto.pessoas_centro,
                contexto.pessoas_direita,
                contexto.pares_muito_proximos,
                contexto.pares_proximos,
                contexto.pares_separados,
            )
            logger.info("[%s] Descrição da cena: %s", pacote_id, contexto.descricao)
            return contexto
        except Exception:
            logger.exception("[%s] Não foi possível analisar a cena", pacote_id)
            return None

    async def _renderizar_cena(
        self,
        evento: CameraEvent,
        bounding_boxes: list[BoundingBox],
        pacote_id: str,
    ):
        try:
            if evento.imagem is None or not evento.imagem.caminho_original:
                return None
            if not bounding_boxes:
                return None

            resultado = await asyncio.to_thread(
                renderizar_cena_com_boxes,
                caminho_imagem=evento.imagem.caminho_original,
                bounding_boxes=bounding_boxes,
            )
            if resultado.quantidade_boxes == 0:
                return None

            logger.info(
                "[%s] Cena renderizada: boxes=%s dimensoes=%sx%s base64=%s caracteres",
                pacote_id,
                resultado.quantidade_boxes,
                resultado.largura_imagem,
                resultado.altura_imagem,
                len(resultado.imagem_base64),
            )
            return resultado
        except Exception:
            logger.exception(
                "[%s] Não foi possível renderizar a cena com boxes",
                pacote_id,
            )
            return None

    async def _publicar_atualizacao_parcial(
        self,
        camera: str,
        decisao: TrackingDecision,
        aparencia_estavel,
        movimento,
        contexto_cena,
        cena_renderizada,
        imagem_recorte_base64: str | None,
        indice_na_cena: int,
        total_pessoas_cena: int,
        pacote_id: str,
    ) -> None:
        atualizacao_parcial = {
            "evento_id": decisao.evento_id,
            "pessoa_id": decisao.pessoa_id,
            "status": "appearance_updated",
            "quantidade_deteccoes": decisao.quantidade_deteccoes,
            "camera": camera,
            "imagem": imagem_recorte_base64,
            "aparencia": (
                aparencia_estavel.to_dict()
                if aparencia_estavel is not None
                else None
            ),
            "movimento": (
                movimento.to_dict()
                if movimento is not None
                else None
            ),
            "contexto_cena": (
                contexto_cena.to_dict() if contexto_cena is not None else None
            ),
            "imagem_cena": (
                cena_renderizada.imagem_base64
                if cena_renderizada is not None
                else None
            ),
            "quantidade_boxes_cena": (
                cena_renderizada.quantidade_boxes
                if cena_renderizada is not None
                else 0
            ),
            "indice_na_cena": indice_na_cena,
            "total_pessoas_cena": total_pessoas_cena,
        }
        await event_hub.publicar(atualizacao_parcial)
        logger.info(
            "[%s] Atualização parcial: pessoa=%s indice=%s/%s "
            "deteccoes=%s amostras=%s movimento=%s/%s velocidade=%spx_s "
            "boxes=%s paineis=%s",
            pacote_id,
            decisao.pessoa_id,
            indice_na_cena,
            total_pessoas_cena,
            decisao.quantidade_deteccoes,
            (
                aparencia_estavel.quantidade_amostras
                if aparencia_estavel is not None
                else 0
            ),
            (
                movimento.movimento_horizontal
                if movimento is not None
                else "-"
            ),
            (
                movimento.movimento_vertical
                if movimento is not None
                else "-"
            ),
            (
                movimento.velocidade_pixels_segundo
                if movimento is not None
                else 0
            ),
            (
                cena_renderizada.quantidade_boxes
                if cena_renderizada is not None
                else 0
            ),
            event_hub.total_conexoes,
        )

    async def _processar_pessoa(
        self,
        evento: CameraEvent,
        camera: str,
        bounding_box: BoundingBox,
        decisao: TrackingDecision,
        contexto_cena,
        cena_renderizada,
        indice_na_cena: int,
        total_pessoas_cena: int,
        pacote_id: str,
        imagem_background_base64: str | None,
    ) -> str | None:
        aparencia_estavel = await self._analisar_aparencia(
            evento=evento,
            bounding_box=bounding_box,
            pessoa_id=decisao.pessoa_id,
            pacote_id=pacote_id,
        )

        movimento = await person_movement_memory.registrar(
            pessoa_id=decisao.pessoa_id,
            bbox=decisao.bbox,
        )
        logger.info(
            "[%s] Movimento: pessoa=%s horizontal=%s vertical=%s "
            "tendencia=%s velocidade=%spx_s distancia_total=%spx "
            "tempo=%ss amostras=%s",
            pacote_id,
            decisao.pessoa_id,
            movimento.movimento_horizontal,
            movimento.movimento_vertical,
            movimento.tendencia_distancia,
            movimento.velocidade_pixels_segundo,
            movimento.distancia_total_pixels,
            movimento.tempo_observado_segundos,
            movimento.quantidade_amostras,
        )

        if not decisao.deve_processar:
            imagem_recorte_base64 = None

            if (
                evento.imagem is not None
                and evento.imagem.caminho_original
            ):
                try:
                    imagem_recorte_base64 = (
                        await asyncio.to_thread(
                            recortar_deteccao_base64,
                            evento.imagem.caminho_original,
                            bounding_box,
                        )
                    )
                except Exception:
                    logger.exception(
                        "[%s] Não foi possível atualizar "
                        "o recorte da pessoa=%s",
                        pacote_id,
                        decisao.pessoa_id,
                    )

            await self._publicar_atualizacao_parcial(
                camera=camera,
                decisao=decisao,
                aparencia_estavel=aparencia_estavel,
                movimento=movimento,
                contexto_cena=contexto_cena,
                cena_renderizada=cena_renderizada,
                imagem_recorte_base64=(
                    imagem_recorte_base64
                ),
                indice_na_cena=indice_na_cena,
                total_pessoas_cena=total_pessoas_cena,
                pacote_id=pacote_id,
            )
            logger.info(
                "[%s] Evento repetido suprimido: pessoa=%s indice=%s/%s amostras=%s",
                pacote_id,
                decisao.pessoa_id,
                indice_na_cena,
                total_pessoas_cena,
                (
                    aparencia_estavel.quantidade_amostras
                    if aparencia_estavel is not None
                    else 0
                ),
            )
            return imagem_background_base64

        if evento.imagem is None or not evento.imagem.caminho_original:
            logger.info("[%s] Pessoa ignorada: caminho original ausente", pacote_id)
            return imagem_background_base64

        if imagem_background_base64 is None:
            imagem_background_base64 = await asyncio.to_thread(
                arquivo_para_base64,
                evento.imagem.caminho_original,
            )

        try:
            mensagem = await asyncio.to_thread(
                montar_alarm_detection_message,
                evento,
                bounding_box=bounding_box,
                evento_id=decisao.evento_id,
                imagem_background_base64=imagem_background_base64,
            )
        except ValueError as erro:
            logger.info(
                "[%s] Pessoa ignorada: pessoa=%s erro=%s",
                pacote_id,
                decisao.pessoa_id,
                erro,
            )
            return imagem_background_base64

        evento_painel = {
            "evento_id": decisao.evento_id,
            "pessoa_id": decisao.pessoa_id,
            "status": decisao.status,
            "quantidade_deteccoes": decisao.quantidade_deteccoes,
            "camera": mensagem.device.name,
            "tipo": mensagem.event.type,
            "datetime": mensagem.event.datetime,
            "attributes": (
                mensagem.event.attributes.model_dump(
                    mode="json"
                )
                if mensagem.event.attributes is not None
                else None
            ),
            "imagem": mensagem.event.images.detection,
            "aparencia": (
                aparencia_estavel.to_dict()
                if aparencia_estavel is not None
                else None
            ),
            "movimento": movimento.to_dict(),
            "contexto_cena": (
                contexto_cena.to_dict() if contexto_cena is not None else None
            ),
            "imagem_cena": (
                cena_renderizada.imagem_base64
                if cena_renderizada is not None
                else None
            ),
            "quantidade_boxes_cena": (
                cena_renderizada.quantidade_boxes
                if cena_renderizada is not None
                else 0
            ),
            "indice_na_cena": indice_na_cena,
            "total_pessoas_cena": total_pessoas_cena,
        }
        await event_hub.publicar(evento_painel)
        logger.info(
            "[%s] Pessoa enviada ao painel: pessoa=%s status=%s indice=%s/%s paineis=%s",
            pacote_id,
            decisao.pessoa_id,
            decisao.status,
            indice_na_cena,
            total_pessoas_cena,
            event_hub.total_conexoes,
        )

        if not settings.kafka_enabled:
            logger.info(
                "[%s] Payload pronto; Kafka desativado: pessoa=%s",
                pacote_id,
                decisao.pessoa_id,
            )
            return imagem_background_base64

        resultado = await self.publisher.publicar(
            topico=self.topic,
            evento_id=decisao.evento_id,
            dados=mensagem.model_dump(
                mode="json",
                by_alias=True,
                exclude_none=True,
            ),
        )
        logger.info(
            "[%s] Pessoa publicada em %s p=%s offset=%s pessoa=%s indice=%s/%s",
            pacote_id,
            resultado["topico"],
            resultado["particao"],
            resultado["offset"],
            decisao.pessoa_id,
            indice_na_cena,
            total_pessoas_cena,
        )
        return imagem_background_base64

    async def _worker(self, numero: int) -> None:
        logger.info("Worker %s iniciado", numero)

        while True:
            pacote, body = await self.queue.get()

            try:
                adapter = camera_adapter_factory.encontrar_adapter(
                    content_type=pacote.content_type,
                    body=body,
                )
                logger.info(
                    "[%s] Worker %s usando %s",
                    pacote.evento_id,
                    numero,
                    adapter.__class__.__name__,
                )

                evento = await asyncio.to_thread(adapter.normalizar, pacote, body)

                logger.info(
                    "[%s] OBJETO NORMALIZADO (%s):\n%s",
                    pacote.evento_id,
                    evento.fabricante,
                    evento.model_dump_json(indent=2, by_alias=True, exclude_none=True),
                )

                if evento.imagem is None:
                    logger.info(
                        "[%s] Evento ignorado: não possui imagem",
                        pacote.evento_id,
                    )
                    continue

                bounding_boxes = self._selecionar_boxes_pessoas(evento)
                if not bounding_boxes:
                    logger.info(
                        "[%s] Evento ignorado: não possui boxes válidas de pessoas",
                        pacote.evento_id,
                    )
                    continue

                camera = (
                    evento.nome_camera
                    or evento.camera_id
                    or evento.ip_camera
                    or "Camera desconhecida"
                )
                caixas_rastreamento = [
                    DetectionBox(
                        x=caixa.x,
                        y=caixa.y,
                        largura=caixa.largura,
                        altura=caixa.altura,
                    )
                    for caixa in bounding_boxes
                ]
                decisoes = await person_tracker.registrar_lote(
                    camera=camera,
                    evento_id=evento.evento_id,
                    bboxes=caixas_rastreamento,
                )

                if len(decisoes) != len(bounding_boxes):
                    raise RuntimeError(
                        "Quantidade de decisões diferente da quantidade de boxes"
                    )

                total_pessoas = len(bounding_boxes)
                logger.info(
                    "[%s] Rastreamento em lote: pessoas=%s ids=%s",
                    pacote.evento_id,
                    total_pessoas,
                    [
                        (decisao.pessoa_id, decisao.status)
                        for decisao in decisoes
                    ],
                )

                contexto_cena = await self._analisar_contexto_cena(
                    evento=evento,
                    bounding_boxes=bounding_boxes,
                    pacote_id=pacote.evento_id,
                )
                cena_renderizada = await self._renderizar_cena(
                    evento=evento,
                    bounding_boxes=bounding_boxes,
                    pacote_id=pacote.evento_id,
                )

                imagem_background_base64: str | None = None
                for indice, (bounding_box, decisao) in enumerate(
                    zip(bounding_boxes, decisoes, strict=True),
                    start=1,
                ):
                    imagem_background_base64 = await self._processar_pessoa(
                        evento=evento,
                        camera=camera,
                        bounding_box=bounding_box,
                        decisao=decisao,
                        contexto_cena=contexto_cena,
                        cena_renderizada=cena_renderizada,
                        indice_na_cena=indice,
                        total_pessoas_cena=total_pessoas,
                        pacote_id=pacote.evento_id,
                        imagem_background_base64=imagem_background_base64,
                    )

            except asyncio.CancelledError:
                raise
            except Exception:
                logger.exception("[%s] Erro no processamento", pacote.evento_id)
            finally:
                self.queue.task_done()

------------------------------------------------------------
ARQUIVO: app/services/event_hub.py
------------------------------------------------------------
import asyncio
import logging
from typing import Any

from fastapi import WebSocket


logger = logging.getLogger(__name__)


class EventHub:
    """
    Mantém os navegadores conectados e envia eventos em tempo real.
    """

    def __init__(self) -> None:
        self._conexoes: list[WebSocket] = []
        self._lock = asyncio.Lock()

    @property
    def total_conexoes(self) -> int:
        return len(self._conexoes)

    async def conectar(self, websocket: WebSocket) -> None:
        await websocket.accept()

        async with self._lock:
            self._conexoes.append(websocket)

        logger.info(
            "Painel conectado. Total: %s",
            self.total_conexoes,
        )

    async def desconectar(self, websocket: WebSocket) -> None:
        async with self._lock:
            if websocket in self._conexoes:
                self._conexoes.remove(websocket)

        logger.info(
            "Painel desconectado. Total: %s",
            self.total_conexoes,
        )

    async def publicar(
        self,
        evento: dict[str, Any],
    ) -> None:
        async with self._lock:
            conexoes = list(self._conexoes)

        desconectadas: list[WebSocket] = []

        for websocket in conexoes:
            try:
                await websocket.send_json(evento)

            except Exception:
                desconectadas.append(websocket)

        for websocket in desconectadas:
            await self.desconectar(websocket)


event_hub = EventHub()
------------------------------------------------------------
ARQUIVO: app/services/person_movement.py
------------------------------------------------------------
from __future__ import annotations

import asyncio
import math
import time
from dataclasses import asdict, dataclass
from typing import Literal

from app.services.person_tracker import DetectionBox


MovimentoHorizontal = Literal[
    "inicial",
    "parado",
    "esquerda",
    "direita",
]

MovimentoVertical = Literal[
    "inicial",
    "parado",
    "cima",
    "baixo",
]

TendenciaDistancia = Literal[
    "inicial",
    "estavel",
    "aproximando",
    "afastando",
]


@dataclass(frozen=True)
class PersonMovementAnalysis:
    pessoa_id: str

    movimento_horizontal: MovimentoHorizontal
    movimento_vertical: MovimentoVertical
    tendencia_distancia: TendenciaDistancia

    deslocamento_x: float
    deslocamento_y: float
    distancia_pixels: float

    velocidade_pixels_segundo: float
    variacao_area_percentual: float

    distancia_total_pixels: float
    tempo_observado_segundos: float
    quantidade_amostras: int

    descricao: str

    def to_dict(self) -> dict:
        return asdict(self)


@dataclass
class _MovementState:
    pessoa_id: str

    primeira_amostra_em: float
    ultima_amostra_em: float

    ultima_bbox: DetectionBox

    quantidade_amostras: int
    distancia_total_pixels: float

    ultima_analise: PersonMovementAnalysis


class PersonMovementMemory:
    def __init__(
        self,
        limite_movimento_percentual: float = 0.05,
        limite_movimento_minimo_pixels: float = 6.0,
        limite_variacao_area_percentual: float = 12.0,
    ) -> None:
        if limite_movimento_percentual < 0:
            raise ValueError(
                "limite_movimento_percentual "
                "não pode ser negativo"
            )

        if limite_movimento_minimo_pixels < 0:
            raise ValueError(
                "limite_movimento_minimo_pixels "
                "não pode ser negativo"
            )

        if limite_variacao_area_percentual < 0:
            raise ValueError(
                "limite_variacao_area_percentual "
                "não pode ser negativo"
            )

        self.limite_movimento_percentual = (
            limite_movimento_percentual
        )

        self.limite_movimento_minimo_pixels = (
            limite_movimento_minimo_pixels
        )

        self.limite_variacao_area_percentual = (
            limite_variacao_area_percentual
        )

        self._estados: dict[
            str,
            _MovementState,
        ] = {}

        self._lock = asyncio.Lock()

    async def registrar(
        self,
        pessoa_id: str,
        bbox: DetectionBox,
        agora: float | None = None,
    ) -> PersonMovementAnalysis:
        if not pessoa_id.strip():
            raise ValueError(
                "pessoa_id não pode ser vazio"
            )

        if (
            bbox.largura <= 0
            or bbox.altura <= 0
        ):
            raise ValueError(
                "A bounding box deve possuir "
                "largura e altura positivas"
            )

        momento = (
            time.monotonic()
            if agora is None
            else agora
        )

        async with self._lock:
            estado = self._estados.get(
                pessoa_id
            )

            if estado is None:
                analise = self._criar_primeira_analise(
                    pessoa_id=pessoa_id,
                )

                self._estados[pessoa_id] = (
                    _MovementState(
                        pessoa_id=pessoa_id,
                        primeira_amostra_em=momento,
                        ultima_amostra_em=momento,
                        ultima_bbox=bbox,
                        quantidade_amostras=1,
                        distancia_total_pixels=0.0,
                        ultima_analise=analise,
                    )
                )

                return analise

            analise = self._calcular_movimento(
                estado=estado,
                bbox=bbox,
                agora=momento,
            )

            estado.ultima_amostra_em = momento
            estado.ultima_bbox = bbox
            estado.quantidade_amostras += 1

            estado.distancia_total_pixels = (
                analise.distancia_total_pixels
            )

            estado.ultima_analise = analise

            return analise

    async def obter(
        self,
        pessoa_id: str,
    ) -> PersonMovementAnalysis | None:
        async with self._lock:
            estado = self._estados.get(
                pessoa_id
            )

            if estado is None:
                return None

            return estado.ultima_analise

    async def finalizar(
        self,
        pessoa_id: str,
    ) -> PersonMovementAnalysis | None:
        async with self._lock:
            estado = self._estados.pop(
                pessoa_id,
                None,
            )

            if estado is None:
                return None

            return estado.ultima_analise

    async def limpar(self) -> None:
        async with self._lock:
            self._estados.clear()

    @property
    def quantidade_pessoas(self) -> int:
        return len(self._estados)

    def _criar_primeira_analise(
        self,
        pessoa_id: str,
    ) -> PersonMovementAnalysis:
        return PersonMovementAnalysis(
            pessoa_id=pessoa_id,
            movimento_horizontal="inicial",
            movimento_vertical="inicial",
            tendencia_distancia="inicial",
            deslocamento_x=0.0,
            deslocamento_y=0.0,
            distancia_pixels=0.0,
            velocidade_pixels_segundo=0.0,
            variacao_area_percentual=0.0,
            distancia_total_pixels=0.0,
            tempo_observado_segundos=0.0,
            quantidade_amostras=1,
            descricao=(
                "Primeira posição da pessoa "
                "registrada."
            ),
        )

    def _calcular_movimento(
        self,
        estado: _MovementState,
        bbox: DetectionBox,
        agora: float,
    ) -> PersonMovementAnalysis:
        bbox_anterior = estado.ultima_bbox

        deslocamento_x = (
            bbox.centro_x
            - bbox_anterior.centro_x
        )

        deslocamento_y = (
            bbox.centro_y
            - bbox_anterior.centro_y
        )

        distancia_pixels = math.hypot(
            deslocamento_x,
            deslocamento_y,
        )

        intervalo_segundos = max(
            0.001,
            agora
            - estado.ultima_amostra_em,
        )

        velocidade = (
            distancia_pixels
            / intervalo_segundos
        )

        limite_horizontal = max(
            self.limite_movimento_minimo_pixels,
            max(
                bbox.largura,
                bbox_anterior.largura,
            )
            * self.limite_movimento_percentual,
        )

        limite_vertical = max(
            self.limite_movimento_minimo_pixels,
            max(
                bbox.altura,
                bbox_anterior.altura,
            )
            * self.limite_movimento_percentual,
        )

        movimento_horizontal = (
            self._classificar_movimento_horizontal(
                deslocamento_x=deslocamento_x,
                limite=limite_horizontal,
            )
        )

        movimento_vertical = (
            self._classificar_movimento_vertical(
                deslocamento_y=deslocamento_y,
                limite=limite_vertical,
            )
        )

        area_anterior = max(
            1,
            bbox_anterior.area,
        )

        variacao_area_percentual = (
            (
                bbox.area
                - area_anterior
            )
            / area_anterior
            * 100
        )

        tendencia_distancia = (
            self._classificar_tendencia_distancia(
                variacao_area_percentual
            )
        )

        quantidade_amostras = (
            estado.quantidade_amostras
            + 1
        )

        distancia_total = (
            estado.distancia_total_pixels
            + distancia_pixels
        )

        tempo_observado = max(
            0.0,
            agora
            - estado.primeira_amostra_em,
        )

        descricao = self._montar_descricao(
            movimento_horizontal=(
                movimento_horizontal
            ),
            movimento_vertical=(
                movimento_vertical
            ),
            tendencia_distancia=(
                tendencia_distancia
            ),
            velocidade=velocidade,
        )

        return PersonMovementAnalysis(
            pessoa_id=estado.pessoa_id,
            movimento_horizontal=(
                movimento_horizontal
            ),
            movimento_vertical=(
                movimento_vertical
            ),
            tendencia_distancia=(
                tendencia_distancia
            ),
            deslocamento_x=round(
                deslocamento_x,
                2,
            ),
            deslocamento_y=round(
                deslocamento_y,
                2,
            ),
            distancia_pixels=round(
                distancia_pixels,
                2,
            ),
            velocidade_pixels_segundo=round(
                velocidade,
                2,
            ),
            variacao_area_percentual=round(
                variacao_area_percentual,
                2,
            ),
            distancia_total_pixels=round(
                distancia_total,
                2,
            ),
            tempo_observado_segundos=round(
                tempo_observado,
                2,
            ),
            quantidade_amostras=(
                quantidade_amostras
            ),
            descricao=descricao,
        )

    def _classificar_movimento_horizontal(
        self,
        deslocamento_x: float,
        limite: float,
    ) -> MovimentoHorizontal:
        if abs(deslocamento_x) < limite:
            return "parado"

        if deslocamento_x > 0:
            return "direita"

        return "esquerda"

    def _classificar_movimento_vertical(
        self,
        deslocamento_y: float,
        limite: float,
    ) -> MovimentoVertical:
        if abs(deslocamento_y) < limite:
            return "parado"

        if deslocamento_y > 0:
            return "baixo"

        return "cima"

    def _classificar_tendencia_distancia(
        self,
        variacao_area_percentual: float,
    ) -> TendenciaDistancia:
        limite = (
            self
            .limite_variacao_area_percentual
        )

        if variacao_area_percentual >= limite:
            return "aproximando"

        if variacao_area_percentual <= -limite:
            return "afastando"

        return "estavel"

    def _montar_descricao(
        self,
        movimento_horizontal: MovimentoHorizontal,
        movimento_vertical: MovimentoVertical,
        tendencia_distancia: TendenciaDistancia,
        velocidade: float,
    ) -> str:
        movimentos: list[str] = []

        if movimento_horizontal == "direita":
            movimentos.append(
                "para a direita"
            )

        elif movimento_horizontal == "esquerda":
            movimentos.append(
                "para a esquerda"
            )

        if movimento_vertical == "cima":
            movimentos.append(
                "para cima"
            )

        elif movimento_vertical == "baixo":
            movimentos.append(
                "para baixo"
            )

        if not movimentos:
            descricao_movimento = (
                "Pessoa praticamente parada"
            )

        else:
            descricao_movimento = (
                "Pessoa se movimentando "
                + " e ".join(movimentos)
            )

        tendencias = {
            "inicial": "",
            "estavel": (
                " mantendo distância "
                "aparente estável"
            ),
            "aproximando": (
                " e aparentemente se aproximando "
                "da câmera"
            ),
            "afastando": (
                " e aparentemente se afastando "
                "da câmera"
            ),
        }

        return (
            f"{descricao_movimento}"
            f"{tendencias[tendencia_distancia]}, "
            f"com velocidade aproximada de "
            f"{velocidade:.2f} pixels por segundo."
        )


person_movement_memory = (
    PersonMovementMemory()
)
------------------------------------------------------------
ARQUIVO: app/services/person_tracker_backup_antes_15s.py
------------------------------------------------------------
import asyncio
import math
import time
import uuid
from dataclasses import dataclass
from typing import Literal


TrackingStatus = Literal[
    "entered",
    "updated",
    "suppressed",
    "exited",
]


@dataclass(frozen=True)
class DetectionBox:
    x: int
    y: int
    largura: int
    altura: int

    @property
    def x2(self) -> int:
        return self.x + self.largura

    @property
    def y2(self) -> int:
        return self.y + self.altura

    @property
    def centro_x(self) -> float:
        return self.x + self.largura / 2

    @property
    def centro_y(self) -> float:
        return self.y + self.altura / 2

    @property
    def area(self) -> int:
        return max(0, self.largura) * max(
            0,
            self.altura,
        )

    def iou(self, outra: "DetectionBox") -> float:
        """
        Calcula a sobreposição entre duas caixas.

        0.0 significa nenhuma sobreposição.
        1.0 significa caixas iguais.
        """

        intersecao_x1 = max(self.x, outra.x)
        intersecao_y1 = max(self.y, outra.y)
        intersecao_x2 = min(self.x2, outra.x2)
        intersecao_y2 = min(self.y2, outra.y2)

        largura_intersecao = max(
            0,
            intersecao_x2 - intersecao_x1,
        )

        altura_intersecao = max(
            0,
            intersecao_y2 - intersecao_y1,
        )

        area_intersecao = (
            largura_intersecao * altura_intersecao
        )

        area_uniao = (
            self.area
            + outra.area
            - area_intersecao
        )

        if area_uniao <= 0:
            return 0.0

        return area_intersecao / area_uniao

    def distancia_centros(
        self,
        outra: "DetectionBox",
    ) -> float:
        diferenca_x = self.centro_x - outra.centro_x
        diferenca_y = self.centro_y - outra.centro_y

        return math.hypot(
            diferenca_x,
            diferenca_y,
        )


@dataclass
class TrackedPerson:
    pessoa_id: str
    camera: str
    primeira_deteccao: float
    ultima_deteccao: float
    ultimo_processamento: float
    bbox: DetectionBox
    quantidade_deteccoes: int
    ultimo_evento_id: str


@dataclass(frozen=True)
class TrackingDecision:
    pessoa_id: str
    camera: str
    evento_id: str
    status: TrackingStatus
    deve_processar: bool
    quantidade_deteccoes: int
    bbox: DetectionBox


class PersonTracker:
    def __init__(
        self,
        intervalo_reprocessamento: float = 5.0,
        tempo_para_saida: float = 8.0,
        limite_iou: float = 0.25,
        limite_distancia: float = 0.75,
    ) -> None:
        if intervalo_reprocessamento <= 0:
            raise ValueError(
                "intervalo_reprocessamento deve ser positivo"
            )

        if tempo_para_saida <= 0:
            raise ValueError(
                "tempo_para_saida deve ser positivo"
            )

        self.intervalo_reprocessamento = (
            intervalo_reprocessamento
        )

        self.tempo_para_saida = tempo_para_saida
        self.limite_iou = limite_iou
        self.limite_distancia = limite_distancia

        self._pessoas: dict[str, TrackedPerson] = {}
        self._lock = asyncio.Lock()

    @property
    def quantidade_ativas(self) -> int:
        return len(self._pessoas)

    async def registrar(
        self,
        camera: str,
        evento_id: str,
        bbox: DetectionBox,
        agora: float | None = None,
    ) -> TrackingDecision:
        momento = (
            time.monotonic()
            if agora is None
            else agora
        )

        async with self._lock:
            pessoa = self._encontrar_pessoa(
                camera=camera,
                bbox=bbox,
            )

            if pessoa is None:
                pessoa = self._criar_pessoa(
                    camera=camera,
                    evento_id=evento_id,
                    bbox=bbox,
                    agora=momento,
                )

                return TrackingDecision(
                    pessoa_id=pessoa.pessoa_id,
                    camera=camera,
                    evento_id=evento_id,
                    status="entered",
                    deve_processar=True,
                    quantidade_deteccoes=1,
                    bbox=bbox,
                )

            pessoa.ultima_deteccao = momento
            pessoa.bbox = bbox
            pessoa.ultimo_evento_id = evento_id
            pessoa.quantidade_deteccoes += 1

            tempo_desde_processamento = (
                momento
                - pessoa.ultimo_processamento
            )

            if (
                tempo_desde_processamento
                >= self.intervalo_reprocessamento
            ):
                pessoa.ultimo_processamento = momento

                return TrackingDecision(
                    pessoa_id=pessoa.pessoa_id,
                    camera=camera,
                    evento_id=evento_id,
                    status="updated",
                    deve_processar=True,
                    quantidade_deteccoes=(
                        pessoa.quantidade_deteccoes
                    ),
                    bbox=bbox,
                )

            return TrackingDecision(
                pessoa_id=pessoa.pessoa_id,
                camera=camera,
                evento_id=evento_id,
                status="suppressed",
                deve_processar=False,
                quantidade_deteccoes=(
                    pessoa.quantidade_deteccoes
                ),
                bbox=bbox,
            )

    async def coletar_saidas(
        self,
        agora: float | None = None,
    ) -> list[TrackingDecision]:
        momento = (
            time.monotonic()
            if agora is None
            else agora
        )

        async with self._lock:
            pessoas_encerradas: list[
                TrackingDecision
            ] = []

            ids_para_remover: list[str] = []

            for pessoa_id, pessoa in self._pessoas.items():
                tempo_sem_deteccao = (
                    momento
                    - pessoa.ultima_deteccao
                )

                if (
                    tempo_sem_deteccao
                    < self.tempo_para_saida
                ):
                    continue

                pessoas_encerradas.append(
                    TrackingDecision(
                        pessoa_id=pessoa.pessoa_id,
                        camera=pessoa.camera,
                        evento_id=(
                            pessoa.ultimo_evento_id
                        ),
                        status="exited",
                        deve_processar=False,
                        quantidade_deteccoes=(
                            pessoa.quantidade_deteccoes
                        ),
                        bbox=pessoa.bbox,
                    )
                )

                ids_para_remover.append(pessoa_id)

            for pessoa_id in ids_para_remover:
                self._pessoas.pop(
                    pessoa_id,
                    None,
                )

            return pessoas_encerradas

    def _encontrar_pessoa(
        self,
        camera: str,
        bbox: DetectionBox,
    ) -> TrackedPerson | None:
        melhor_pessoa: TrackedPerson | None = None
        melhor_pontuacao = -1.0

        for pessoa in self._pessoas.values():
            if pessoa.camera != camera:
                continue

            sobreposicao = bbox.iou(pessoa.bbox)

            distancia = bbox.distancia_centros(
                pessoa.bbox
            )

            maior_dimensao = max(
                bbox.largura,
                bbox.altura,
                pessoa.bbox.largura,
                pessoa.bbox.altura,
                1,
            )

            distancia_normalizada = (
                distancia / maior_dimensao
            )

            caixas_proximas = (
                distancia_normalizada
                <= self.limite_distancia
            )

            caixas_sobrepostas = (
                sobreposicao >= self.limite_iou
            )

            if not (
                caixas_proximas
                or caixas_sobrepostas
            ):
                continue

            pontuacao_distancia = max(
                0.0,
                1.0 - distancia_normalizada,
            )

            pontuacao = (
                sobreposicao
                + pontuacao_distancia
            )

            if pontuacao > melhor_pontuacao:
                melhor_pontuacao = pontuacao
                melhor_pessoa = pessoa

        return melhor_pessoa

    def _criar_pessoa(
        self,
        camera: str,
        evento_id: str,
        bbox: DetectionBox,
        agora: float,
    ) -> TrackedPerson:
        camera_normalizada = "".join(
            caractere.lower()
            if caractere.isalnum()
            else "-"
            for caractere in camera
        ).strip("-")

        if not camera_normalizada:
            camera_normalizada = "camera"

        pessoa_id = (
            f"{camera_normalizada}-"
            f"{uuid.uuid4().hex[:8]}"
        )

        pessoa = TrackedPerson(
            pessoa_id=pessoa_id,
            camera=camera,
            primeira_deteccao=agora,
            ultima_deteccao=agora,
            ultimo_processamento=agora,
            bbox=bbox,
            quantidade_deteccoes=1,
            ultimo_evento_id=evento_id,
        )

        self._pessoas[pessoa_id] = pessoa

        return pessoa


person_tracker = PersonTracker(
    intervalo_reprocessamento=5.0,
    tempo_para_saida=8.0,
)
------------------------------------------------------------
ARQUIVO: app/services/person_tracker_backup_antes_continuidade.py
------------------------------------------------------------
import asyncio
import math
import time
import uuid
from dataclasses import dataclass
from typing import Literal


TrackingStatus = Literal[
    "entered",
    "updated",
    "suppressed",
    "exited",
]


@dataclass(frozen=True)
class DetectionBox:
    x: int
    y: int
    largura: int
    altura: int

    @property
    def x2(self) -> int:
        return self.x + self.largura

    @property
    def y2(self) -> int:
        return self.y + self.altura

    @property
    def centro_x(self) -> float:
        return self.x + self.largura / 2

    @property
    def centro_y(self) -> float:
        return self.y + self.altura / 2

    @property
    def area(self) -> int:
        return (
            max(0, self.largura)
            * max(0, self.altura)
        )

    def iou(
        self,
        outra: "DetectionBox",
    ) -> float:
        """
        Calcula a sobreposição entre duas caixas.

        0.0 significa nenhuma sobreposição.
        1.0 significa caixas iguais.
        """

        intersecao_x1 = max(
            self.x,
            outra.x,
        )

        intersecao_y1 = max(
            self.y,
            outra.y,
        )

        intersecao_x2 = min(
            self.x2,
            outra.x2,
        )

        intersecao_y2 = min(
            self.y2,
            outra.y2,
        )

        largura_intersecao = max(
            0,
            intersecao_x2 - intersecao_x1,
        )

        altura_intersecao = max(
            0,
            intersecao_y2 - intersecao_y1,
        )

        area_intersecao = (
            largura_intersecao
            * altura_intersecao
        )

        area_uniao = (
            self.area
            + outra.area
            - area_intersecao
        )

        if area_uniao <= 0:
            return 0.0

        return (
            area_intersecao
            / area_uniao
        )

    def distancia_centros(
        self,
        outra: "DetectionBox",
    ) -> float:
        diferenca_x = (
            self.centro_x
            - outra.centro_x
        )

        diferenca_y = (
            self.centro_y
            - outra.centro_y
        )

        return math.hypot(
            diferenca_x,
            diferenca_y,
        )


@dataclass
class TrackedPerson:
    pessoa_id: str
    camera: str

    primeira_deteccao: float
    ultima_deteccao: float
    ultimo_processamento: float

    bbox: DetectionBox
    quantidade_deteccoes: int
    ultimo_evento_id: str


@dataclass(frozen=True)
class TrackingDecision:
    pessoa_id: str
    camera: str
    evento_id: str

    status: TrackingStatus
    deve_processar: bool

    quantidade_deteccoes: int
    bbox: DetectionBox


class PersonTracker:
    def __init__(
        self,
        intervalo_reprocessamento: float = 5.0,
        tempo_para_saida: float = 15.0,
        limite_iou: float = 0.25,
        limite_distancia: float = 0.75,
    ) -> None:
        if intervalo_reprocessamento <= 0:
            raise ValueError(
                "intervalo_reprocessamento "
                "deve ser positivo"
            )

        if tempo_para_saida <= 0:
            raise ValueError(
                "tempo_para_saida deve ser positivo"
            )

        if limite_iou < 0:
            raise ValueError(
                "limite_iou não pode ser negativo"
            )

        if limite_distancia < 0:
            raise ValueError(
                "limite_distancia não pode "
                "ser negativo"
            )

        self.intervalo_reprocessamento = (
            intervalo_reprocessamento
        )

        self.tempo_para_saida = (
            tempo_para_saida
        )

        self.limite_iou = limite_iou
        self.limite_distancia = limite_distancia

        self._pessoas: dict[
            str,
            TrackedPerson,
        ] = {}

        self._lock = asyncio.Lock()

    @property
    def quantidade_ativas(self) -> int:
        return len(self._pessoas)

    async def registrar(
        self,
        camera: str,
        evento_id: str,
        bbox: DetectionBox,
        agora: float | None = None,
    ) -> TrackingDecision:
        """
        Mantém compatibilidade com o pipeline antigo,
        que registra somente uma pessoa por chamada.
        """

        decisoes = await self.registrar_lote(
            camera=camera,
            evento_id=evento_id,
            bboxes=[bbox],
            agora=agora,
        )

        return decisoes[0]

    async def registrar_lote(
        self,
        camera: str,
        evento_id: str,
        bboxes: list[DetectionBox],
        agora: float | None = None,
    ) -> list[TrackingDecision]:
        """
        Registra todas as detecções de um mesmo quadro.

        Cada pessoa ativa pode ser associada a apenas
        uma bounding box do quadro atual.

        Cada bounding box também pode ser associada
        a apenas uma pessoa ativa.
        """

        if not bboxes:
            return []

        for bbox in bboxes:
            if (
                bbox.largura <= 0
                or bbox.altura <= 0
            ):
                raise ValueError(
                    "Todas as bounding boxes devem "
                    "possuir largura e altura positivas"
                )

        momento = (
            time.monotonic()
            if agora is None
            else agora
        )

        async with self._lock:
            associacoes = (
                self._associar_deteccoes_em_lote(
                    camera=camera,
                    bboxes=bboxes,
                )
            )

            total_deteccoes = len(bboxes)

            decisoes: list[
                TrackingDecision
            ] = []

            for indice, bbox in enumerate(
                bboxes
            ):
                evento_individual_id = (
                    self._criar_evento_individual_id(
                        evento_id=evento_id,
                        indice=indice,
                        total=total_deteccoes,
                    )
                )

                pessoa = associacoes.get(
                    indice
                )

                if pessoa is None:
                    pessoa = self._criar_pessoa(
                        camera=camera,
                        evento_id=(
                            evento_individual_id
                        ),
                        bbox=bbox,
                        agora=momento,
                    )

                    decisoes.append(
                        TrackingDecision(
                            pessoa_id=(
                                pessoa.pessoa_id
                            ),
                            camera=camera,
                            evento_id=(
                                evento_individual_id
                            ),
                            status="entered",
                            deve_processar=True,
                            quantidade_deteccoes=1,
                            bbox=bbox,
                        )
                    )

                    continue

                decisao = (
                    self._atualizar_pessoa(
                        pessoa=pessoa,
                        evento_id=(
                            evento_individual_id
                        ),
                        bbox=bbox,
                        agora=momento,
                    )
                )

                decisoes.append(
                    decisao
                )

            return decisoes

    async def coletar_saidas(
        self,
        agora: float | None = None,
    ) -> list[TrackingDecision]:
        momento = (
            time.monotonic()
            if agora is None
            else agora
        )

        async with self._lock:
            pessoas_encerradas: list[
                TrackingDecision
            ] = []

            ids_para_remover: list[str] = []

            for (
                pessoa_id,
                pessoa,
            ) in self._pessoas.items():
                tempo_sem_deteccao = (
                    momento
                    - pessoa.ultima_deteccao
                )

                if (
                    tempo_sem_deteccao
                    < self.tempo_para_saida
                ):
                    continue

                pessoas_encerradas.append(
                    TrackingDecision(
                        pessoa_id=(
                            pessoa.pessoa_id
                        ),
                        camera=pessoa.camera,
                        evento_id=(
                            pessoa.ultimo_evento_id
                        ),
                        status="exited",
                        deve_processar=False,
                        quantidade_deteccoes=(
                            pessoa
                            .quantidade_deteccoes
                        ),
                        bbox=pessoa.bbox,
                    )
                )

                ids_para_remover.append(
                    pessoa_id
                )

            for pessoa_id in ids_para_remover:
                self._pessoas.pop(
                    pessoa_id,
                    None,
                )

            return pessoas_encerradas

    async def limpar(self) -> None:
        async with self._lock:
            self._pessoas.clear()

    def _associar_deteccoes_em_lote(
        self,
        camera: str,
        bboxes: list[DetectionBox],
    ) -> dict[int, TrackedPerson]:
        """
        Cria todas as combinações possíveis entre
        as caixas atuais e as pessoas ativas.

        Depois seleciona as melhores combinações,
        sem reutilizar pessoa ou bounding box.
        """

        candidatos: list[
            tuple[
                float,
                int,
                str,
            ]
        ] = []

        for indice, bbox in enumerate(
            bboxes
        ):
            for pessoa in self._pessoas.values():
                if pessoa.camera != camera:
                    continue

                pontuacao = (
                    self._calcular_pontuacao(
                        bbox=bbox,
                        pessoa=pessoa,
                    )
                )

                if pontuacao is None:
                    continue

                candidatos.append(
                    (
                        pontuacao,
                        indice,
                        pessoa.pessoa_id,
                    )
                )

        # Maior pontuação primeiro.
        #
        # O índice e o pessoa_id deixam o resultado
        # determinístico quando há empate.
        candidatos.sort(
            key=lambda item: (
                -item[0],
                item[1],
                item[2],
            )
        )

        deteccoes_utilizadas: set[int] = set()
        pessoas_utilizadas: set[str] = set()

        associacoes: dict[
            int,
            TrackedPerson,
        ] = {}

        for (
            _,
            indice,
            pessoa_id,
        ) in candidatos:
            if indice in deteccoes_utilizadas:
                continue

            if pessoa_id in pessoas_utilizadas:
                continue

            pessoa = self._pessoas.get(
                pessoa_id
            )

            if pessoa is None:
                continue

            associacoes[indice] = pessoa

            deteccoes_utilizadas.add(
                indice
            )

            pessoas_utilizadas.add(
                pessoa_id
            )

        return associacoes

    def _calcular_pontuacao(
        self,
        bbox: DetectionBox,
        pessoa: TrackedPerson,
    ) -> float | None:
        sobreposicao = bbox.iou(
            pessoa.bbox
        )

        distancia = bbox.distancia_centros(
            pessoa.bbox
        )

        maior_dimensao = max(
            bbox.largura,
            bbox.altura,
            pessoa.bbox.largura,
            pessoa.bbox.altura,
            1,
        )

        distancia_normalizada = (
            distancia
            / maior_dimensao
        )

        caixas_proximas = (
            distancia_normalizada
            <= self.limite_distancia
        )

        caixas_sobrepostas = (
            sobreposicao
            >= self.limite_iou
        )

        if not (
            caixas_proximas
            or caixas_sobrepostas
        ):
            return None

        pontuacao_distancia = max(
            0.0,
            1.0 - distancia_normalizada,
        )

        return (
            sobreposicao
            + pontuacao_distancia
        )

    def _atualizar_pessoa(
        self,
        pessoa: TrackedPerson,
        evento_id: str,
        bbox: DetectionBox,
        agora: float,
    ) -> TrackingDecision:
        pessoa.ultima_deteccao = agora
        pessoa.bbox = bbox
        pessoa.ultimo_evento_id = evento_id
        pessoa.quantidade_deteccoes += 1

        tempo_desde_processamento = (
            agora
            - pessoa.ultimo_processamento
        )

        if (
            tempo_desde_processamento
            >= self.intervalo_reprocessamento
        ):
            pessoa.ultimo_processamento = agora

            return TrackingDecision(
                pessoa_id=pessoa.pessoa_id,
                camera=pessoa.camera,
                evento_id=evento_id,
                status="updated",
                deve_processar=True,
                quantidade_deteccoes=(
                    pessoa.quantidade_deteccoes
                ),
                bbox=bbox,
            )

        return TrackingDecision(
            pessoa_id=pessoa.pessoa_id,
            camera=pessoa.camera,
            evento_id=evento_id,
            status="suppressed",
            deve_processar=False,
            quantidade_deteccoes=(
                pessoa.quantidade_deteccoes
            ),
            bbox=bbox,
        )

    def _encontrar_pessoa(
        self,
        camera: str,
        bbox: DetectionBox,
    ) -> TrackedPerson | None:
        """
        Mantido para compatibilidade e testes
        individuais.
        """

        melhor_pessoa: (
            TrackedPerson | None
        ) = None

        melhor_pontuacao = -1.0

        for pessoa in self._pessoas.values():
            if pessoa.camera != camera:
                continue

            pontuacao = (
                self._calcular_pontuacao(
                    bbox=bbox,
                    pessoa=pessoa,
                )
            )

            if pontuacao is None:
                continue

            if pontuacao > melhor_pontuacao:
                melhor_pontuacao = pontuacao
                melhor_pessoa = pessoa

        return melhor_pessoa

    def _criar_pessoa(
        self,
        camera: str,
        evento_id: str,
        bbox: DetectionBox,
        agora: float,
    ) -> TrackedPerson:
        camera_normalizada = "".join(
            caractere.lower()
            if caractere.isalnum()
            else "-"
            for caractere in camera
        ).strip("-")

        if not camera_normalizada:
            camera_normalizada = "camera"

        pessoa_id = (
            f"{camera_normalizada}-"
            f"{uuid.uuid4().hex[:8]}"
        )

        pessoa = TrackedPerson(
            pessoa_id=pessoa_id,
            camera=camera,
            primeira_deteccao=agora,
            ultima_deteccao=agora,
            ultimo_processamento=agora,
            bbox=bbox,
            quantidade_deteccoes=1,
            ultimo_evento_id=evento_id,
        )

        self._pessoas[
            pessoa_id
        ] = pessoa

        return pessoa

    def _criar_evento_individual_id(
        self,
        evento_id: str,
        indice: int,
        total: int,
    ) -> str:
        """
        Um quadro com uma única pessoa mantém o ID
        original.

        Quadros com várias pessoas recebem IDs como:

        evento123-01
        evento123-02
        evento123-03
        """

        if total <= 1:
            return evento_id

        return (
            f"{evento_id}-"
            f"{indice + 1:02d}"
        )


person_tracker = PersonTracker(
    intervalo_reprocessamento=5.0,
    tempo_para_saida=15.0,
)
------------------------------------------------------------
ARQUIVO: app/services/person_tracker_backup_antes_lote.py
------------------------------------------------------------
import asyncio
import math
import time
import uuid
from dataclasses import dataclass
from typing import Literal


TrackingStatus = Literal[
    "entered",
    "updated",
    "suppressed",
    "exited",
]


@dataclass(frozen=True)
class DetectionBox:
    x: int
    y: int
    largura: int
    altura: int

    @property
    def x2(self) -> int:
        return self.x + self.largura

    @property
    def y2(self) -> int:
        return self.y + self.altura

    @property
    def centro_x(self) -> float:
        return self.x + self.largura / 2

    @property
    def centro_y(self) -> float:
        return self.y + self.altura / 2

    @property
    def area(self) -> int:
        return max(0, self.largura) * max(
            0,
            self.altura,
        )

    def iou(self, outra: "DetectionBox") -> float:
        """
        Calcula a sobreposição entre duas caixas.

        0.0 significa nenhuma sobreposição.
        1.0 significa caixas iguais.
        """

        intersecao_x1 = max(self.x, outra.x)
        intersecao_y1 = max(self.y, outra.y)
        intersecao_x2 = min(self.x2, outra.x2)
        intersecao_y2 = min(self.y2, outra.y2)

        largura_intersecao = max(
            0,
            intersecao_x2 - intersecao_x1,
        )

        altura_intersecao = max(
            0,
            intersecao_y2 - intersecao_y1,
        )

        area_intersecao = (
            largura_intersecao * altura_intersecao
        )

        area_uniao = (
            self.area
            + outra.area
            - area_intersecao
        )

        if area_uniao <= 0:
            return 0.0

        return area_intersecao / area_uniao

    def distancia_centros(
        self,
        outra: "DetectionBox",
    ) -> float:
        diferenca_x = self.centro_x - outra.centro_x
        diferenca_y = self.centro_y - outra.centro_y

        return math.hypot(
            diferenca_x,
            diferenca_y,
        )


@dataclass
class TrackedPerson:
    pessoa_id: str
    camera: str
    primeira_deteccao: float
    ultima_deteccao: float
    ultimo_processamento: float
    bbox: DetectionBox
    quantidade_deteccoes: int
    ultimo_evento_id: str


@dataclass(frozen=True)
class TrackingDecision:
    pessoa_id: str
    camera: str
    evento_id: str
    status: TrackingStatus
    deve_processar: bool
    quantidade_deteccoes: int
    bbox: DetectionBox


class PersonTracker:
    def __init__(
        self,
        intervalo_reprocessamento: float = 5.0,
        tempo_para_saida: float = 8.0,
        limite_iou: float = 0.25,
        limite_distancia: float = 0.75,
    ) -> None:
        if intervalo_reprocessamento <= 0:
            raise ValueError(
                "intervalo_reprocessamento deve ser positivo"
            )

        if tempo_para_saida <= 0:
            raise ValueError(
                "tempo_para_saida deve ser positivo"
            )

        self.intervalo_reprocessamento = (
            intervalo_reprocessamento
        )

        self.tempo_para_saida = tempo_para_saida
        self.limite_iou = limite_iou
        self.limite_distancia = limite_distancia

        self._pessoas: dict[str, TrackedPerson] = {}
        self._lock = asyncio.Lock()

    @property
    def quantidade_ativas(self) -> int:
        return len(self._pessoas)

    async def registrar(
        self,
        camera: str,
        evento_id: str,
        bbox: DetectionBox,
        agora: float | None = None,
    ) -> TrackingDecision:
        momento = (
            time.monotonic()
            if agora is None
            else agora
        )

        async with self._lock:
            pessoa = self._encontrar_pessoa(
                camera=camera,
                bbox=bbox,
            )

            if pessoa is None:
                pessoa = self._criar_pessoa(
                    camera=camera,
                    evento_id=evento_id,
                    bbox=bbox,
                    agora=momento,
                )

                return TrackingDecision(
                    pessoa_id=pessoa.pessoa_id,
                    camera=camera,
                    evento_id=evento_id,
                    status="entered",
                    deve_processar=True,
                    quantidade_deteccoes=1,
                    bbox=bbox,
                )

            pessoa.ultima_deteccao = momento
            pessoa.bbox = bbox
            pessoa.ultimo_evento_id = evento_id
            pessoa.quantidade_deteccoes += 1

            tempo_desde_processamento = (
                momento
                - pessoa.ultimo_processamento
            )

            if (
                tempo_desde_processamento
                >= self.intervalo_reprocessamento
            ):
                pessoa.ultimo_processamento = momento

                return TrackingDecision(
                    pessoa_id=pessoa.pessoa_id,
                    camera=camera,
                    evento_id=evento_id,
                    status="updated",
                    deve_processar=True,
                    quantidade_deteccoes=(
                        pessoa.quantidade_deteccoes
                    ),
                    bbox=bbox,
                )

            return TrackingDecision(
                pessoa_id=pessoa.pessoa_id,
                camera=camera,
                evento_id=evento_id,
                status="suppressed",
                deve_processar=False,
                quantidade_deteccoes=(
                    pessoa.quantidade_deteccoes
                ),
                bbox=bbox,
            )

    async def coletar_saidas(
        self,
        agora: float | None = None,
    ) -> list[TrackingDecision]:
        momento = (
            time.monotonic()
            if agora is None
            else agora
        )

        async with self._lock:
            pessoas_encerradas: list[
                TrackingDecision
            ] = []

            ids_para_remover: list[str] = []

            for pessoa_id, pessoa in self._pessoas.items():
                tempo_sem_deteccao = (
                    momento
                    - pessoa.ultima_deteccao
                )

                if (
                    tempo_sem_deteccao
                    < self.tempo_para_saida
                ):
                    continue

                pessoas_encerradas.append(
                    TrackingDecision(
                        pessoa_id=pessoa.pessoa_id,
                        camera=pessoa.camera,
                        evento_id=(
                            pessoa.ultimo_evento_id
                        ),
                        status="exited",
                        deve_processar=False,
                        quantidade_deteccoes=(
                            pessoa.quantidade_deteccoes
                        ),
                        bbox=pessoa.bbox,
                    )
                )

                ids_para_remover.append(pessoa_id)

            for pessoa_id in ids_para_remover:
                self._pessoas.pop(
                    pessoa_id,
                    None,
                )

            return pessoas_encerradas

    def _encontrar_pessoa(
        self,
        camera: str,
        bbox: DetectionBox,
    ) -> TrackedPerson | None:
        melhor_pessoa: TrackedPerson | None = None
        melhor_pontuacao = -1.0

        for pessoa in self._pessoas.values():
            if pessoa.camera != camera:
                continue

            sobreposicao = bbox.iou(pessoa.bbox)

            distancia = bbox.distancia_centros(
                pessoa.bbox
            )

            maior_dimensao = max(
                bbox.largura,
                bbox.altura,
                pessoa.bbox.largura,
                pessoa.bbox.altura,
                1,
            )

            distancia_normalizada = (
                distancia / maior_dimensao
            )

            caixas_proximas = (
                distancia_normalizada
                <= self.limite_distancia
            )

            caixas_sobrepostas = (
                sobreposicao >= self.limite_iou
            )

            if not (
                caixas_proximas
                or caixas_sobrepostas
            ):
                continue

            pontuacao_distancia = max(
                0.0,
                1.0 - distancia_normalizada,
            )

            pontuacao = (
                sobreposicao
                + pontuacao_distancia
            )

            if pontuacao > melhor_pontuacao:
                melhor_pontuacao = pontuacao
                melhor_pessoa = pessoa

        return melhor_pessoa

    def _criar_pessoa(
        self,
        camera: str,
        evento_id: str,
        bbox: DetectionBox,
        agora: float,
    ) -> TrackedPerson:
        camera_normalizada = "".join(
            caractere.lower()
            if caractere.isalnum()
            else "-"
            for caractere in camera
        ).strip("-")

        if not camera_normalizada:
            camera_normalizada = "camera"

        pessoa_id = (
            f"{camera_normalizada}-"
            f"{uuid.uuid4().hex[:8]}"
        )

        pessoa = TrackedPerson(
            pessoa_id=pessoa_id,
            camera=camera,
            primeira_deteccao=agora,
            ultima_deteccao=agora,
            ultimo_processamento=agora,
            bbox=bbox,
            quantidade_deteccoes=1,
            ultimo_evento_id=evento_id,
        )

        self._pessoas[pessoa_id] = pessoa

        return pessoa


person_tracker = PersonTracker(
    intervalo_reprocessamento=5.0,
    tempo_para_saida=15.0,
)
------------------------------------------------------------
ARQUIVO: app/services/person_tracker.py
------------------------------------------------------------
import asyncio
import math
import time
import uuid
from dataclasses import dataclass
from typing import Literal


TrackingStatus = Literal[
    "entered",
    "updated",
    "suppressed",
    "exited",
]


@dataclass(frozen=True)
class DetectionBox:
    x: int
    y: int
    largura: int
    altura: int

    @property
    def x2(self) -> int:
        return self.x + self.largura

    @property
    def y2(self) -> int:
        return self.y + self.altura

    @property
    def centro_x(self) -> float:
        return self.x + self.largura / 2

    @property
    def centro_y(self) -> float:
        return self.y + self.altura / 2

    @property
    def area(self) -> int:
        return (
            max(0, self.largura)
            * max(0, self.altura)
        )

    def iou(
        self,
        outra: "DetectionBox",
    ) -> float:
        """
        Calcula a sobreposição entre duas caixas.

        0.0 significa nenhuma sobreposição.
        1.0 significa caixas iguais.
        """

        intersecao_x1 = max(
            self.x,
            outra.x,
        )

        intersecao_y1 = max(
            self.y,
            outra.y,
        )

        intersecao_x2 = min(
            self.x2,
            outra.x2,
        )

        intersecao_y2 = min(
            self.y2,
            outra.y2,
        )

        largura_intersecao = max(
            0,
            intersecao_x2 - intersecao_x1,
        )

        altura_intersecao = max(
            0,
            intersecao_y2 - intersecao_y1,
        )

        area_intersecao = (
            largura_intersecao
            * altura_intersecao
        )

        area_uniao = (
            self.area
            + outra.area
            - area_intersecao
        )

        if area_uniao <= 0:
            return 0.0

        return area_intersecao / area_uniao

    def distancia_centros(
        self,
        outra: "DetectionBox",
    ) -> float:
        diferenca_x = (
            self.centro_x
            - outra.centro_x
        )

        diferenca_y = (
            self.centro_y
            - outra.centro_y
        )

        return math.hypot(
            diferenca_x,
            diferenca_y,
        )


@dataclass
class TrackedPerson:
    pessoa_id: str
    camera: str

    primeira_deteccao: float
    ultima_deteccao: float
    ultimo_processamento: float

    bbox: DetectionBox
    quantidade_deteccoes: int
    ultimo_evento_id: str


@dataclass(frozen=True)
class TrackingDecision:
    pessoa_id: str
    camera: str
    evento_id: str

    status: TrackingStatus
    deve_processar: bool

    quantidade_deteccoes: int
    bbox: DetectionBox


class PersonTracker:
    def __init__(
        self,
        intervalo_reprocessamento: float = 5.0,
        tempo_para_saida: float = 15.0,
        limite_iou: float = 0.25,
        limite_distancia: float = 0.75,
        janela_continuidade_flexivel: float = 4.0,
        limite_distancia_flexivel: float = 2.5,
        limite_razao_area_flexivel: float = 3.0,
        limite_velocidade_normalizada: float = 1.25,
    ) -> None:
        if intervalo_reprocessamento <= 0:
            raise ValueError(
                "intervalo_reprocessamento "
                "deve ser positivo"
            )

        if tempo_para_saida <= 0:
            raise ValueError(
                "tempo_para_saida deve ser positivo"
            )

        if limite_iou < 0:
            raise ValueError(
                "limite_iou não pode ser negativo"
            )

        if limite_distancia < 0:
            raise ValueError(
                "limite_distancia não pode ser negativo"
            )

        if janela_continuidade_flexivel <= 0:
            raise ValueError(
                "janela_continuidade_flexivel "
                "deve ser positiva"
            )

        if (
            limite_distancia_flexivel
            < limite_distancia
        ):
            raise ValueError(
                "limite_distancia_flexivel deve ser "
                "maior ou igual ao limite_distancia"
            )

        if limite_razao_area_flexivel < 1:
            raise ValueError(
                "limite_razao_area_flexivel deve ser "
                "maior ou igual a 1"
            )

        if limite_velocidade_normalizada <= 0:
            raise ValueError(
                "limite_velocidade_normalizada "
                "deve ser positivo"
            )

        self.intervalo_reprocessamento = (
            intervalo_reprocessamento
        )

        self.tempo_para_saida = tempo_para_saida
        self.limite_iou = limite_iou
        self.limite_distancia = limite_distancia

        self.janela_continuidade_flexivel = (
            janela_continuidade_flexivel
        )

        self.limite_distancia_flexivel = (
            limite_distancia_flexivel
        )

        self.limite_razao_area_flexivel = (
            limite_razao_area_flexivel
        )

        self.limite_velocidade_normalizada = (
            limite_velocidade_normalizada
        )

        self._pessoas: dict[
            str,
            TrackedPerson,
        ] = {}

        self._lock = asyncio.Lock()

    @property
    def quantidade_ativas(self) -> int:
        return len(self._pessoas)

    async def registrar(
        self,
        camera: str,
        evento_id: str,
        bbox: DetectionBox,
        agora: float | None = None,
    ) -> TrackingDecision:
        """
        Mantém compatibilidade com chamadas que
        enviam somente uma bounding box.
        """

        decisoes = await self.registrar_lote(
            camera=camera,
            evento_id=evento_id,
            bboxes=[bbox],
            agora=agora,
        )

        return decisoes[0]

    async def registrar_lote(
        self,
        camera: str,
        evento_id: str,
        bboxes: list[DetectionBox],
        agora: float | None = None,
    ) -> list[TrackingDecision]:
        """
        Registra todas as pessoas encontradas
        no mesmo quadro.

        Cada pessoa ativa pode ser associada
        somente a uma bounding box do quadro.
        """

        if not bboxes:
            return []

        for bbox in bboxes:
            if (
                bbox.largura <= 0
                or bbox.altura <= 0
            ):
                raise ValueError(
                    "Todas as bounding boxes devem "
                    "possuir largura e altura positivas"
                )

        momento = (
            time.monotonic()
            if agora is None
            else agora
        )

        async with self._lock:
            associacoes = (
                self._associar_deteccoes_em_lote(
                    camera=camera,
                    bboxes=bboxes,
                )
            )

            self._aplicar_continuidade_flexivel(
                camera=camera,
                bboxes=bboxes,
                associacoes=associacoes,
                agora=momento,
            )

            total_deteccoes = len(bboxes)

            decisoes: list[
                TrackingDecision
            ] = []

            for indice, bbox in enumerate(
                bboxes
            ):
                evento_individual_id = (
                    self._criar_evento_individual_id(
                        evento_id=evento_id,
                        indice=indice,
                        total=total_deteccoes,
                    )
                )

                pessoa = associacoes.get(
                    indice
                )

                if pessoa is None:
                    pessoa = self._criar_pessoa(
                        camera=camera,
                        evento_id=(
                            evento_individual_id
                        ),
                        bbox=bbox,
                        agora=momento,
                    )

                    decisoes.append(
                        TrackingDecision(
                            pessoa_id=(
                                pessoa.pessoa_id
                            ),
                            camera=camera,
                            evento_id=(
                                evento_individual_id
                            ),
                            status="entered",
                            deve_processar=True,
                            quantidade_deteccoes=1,
                            bbox=bbox,
                        )
                    )

                    continue

                decisao = self._atualizar_pessoa(
                    pessoa=pessoa,
                    evento_id=(
                        evento_individual_id
                    ),
                    bbox=bbox,
                    agora=momento,
                )

                decisoes.append(
                    decisao
                )

            return decisoes

    async def coletar_saidas(
        self,
        agora: float | None = None,
    ) -> list[TrackingDecision]:
        momento = (
            time.monotonic()
            if agora is None
            else agora
        )

        async with self._lock:
            pessoas_encerradas: list[
                TrackingDecision
            ] = []

            ids_para_remover: list[str] = []

            for (
                pessoa_id,
                pessoa,
            ) in self._pessoas.items():
                tempo_sem_deteccao = (
                    momento
                    - pessoa.ultima_deteccao
                )

                if (
                    tempo_sem_deteccao
                    < self.tempo_para_saida
                ):
                    continue

                pessoas_encerradas.append(
                    TrackingDecision(
                        pessoa_id=(
                            pessoa.pessoa_id
                        ),
                        camera=pessoa.camera,
                        evento_id=(
                            pessoa.ultimo_evento_id
                        ),
                        status="exited",
                        deve_processar=False,
                        quantidade_deteccoes=(
                            pessoa
                            .quantidade_deteccoes
                        ),
                        bbox=pessoa.bbox,
                    )
                )

                ids_para_remover.append(
                    pessoa_id
                )

            for pessoa_id in ids_para_remover:
                self._pessoas.pop(
                    pessoa_id,
                    None,
                )

            return pessoas_encerradas

    async def limpar(self) -> None:
        async with self._lock:
            self._pessoas.clear()

    def _associar_deteccoes_em_lote(
        self,
        camera: str,
        bboxes: list[DetectionBox],
    ) -> dict[int, TrackedPerson]:
        """
        Busca as melhores associações entre as
        bounding boxes e as pessoas ativas.

        Uma pessoa não pode ser usada duas vezes
        dentro do mesmo quadro.
        """

        candidatos: list[
            tuple[
                float,
                int,
                str,
            ]
        ] = []

        for indice, bbox in enumerate(
            bboxes
        ):
            for pessoa in self._pessoas.values():
                if pessoa.camera != camera:
                    continue

                pontuacao = (
                    self._calcular_pontuacao(
                        bbox=bbox,
                        pessoa=pessoa,
                    )
                )

                if pontuacao is None:
                    continue

                candidatos.append(
                    (
                        pontuacao,
                        indice,
                        pessoa.pessoa_id,
                    )
                )

        candidatos.sort(
            key=lambda item: (
                -item[0],
                item[1],
                item[2],
            )
        )

        deteccoes_utilizadas: set[int] = set()
        pessoas_utilizadas: set[str] = set()

        associacoes: dict[
            int,
            TrackedPerson,
        ] = {}

        for (
            _,
            indice,
            pessoa_id,
        ) in candidatos:
            if indice in deteccoes_utilizadas:
                continue

            if pessoa_id in pessoas_utilizadas:
                continue

            pessoa = self._pessoas.get(
                pessoa_id
            )

            if pessoa is None:
                continue

            associacoes[indice] = pessoa

            deteccoes_utilizadas.add(
                indice
            )

            pessoas_utilizadas.add(
                pessoa_id
            )

        return associacoes

    def _aplicar_continuidade_flexivel(
        self,
        camera: str,
        bboxes: list[DetectionBox],
        associacoes: dict[int, TrackedPerson],
        agora: float,
    ) -> None:
        """
        Corrige o caso em que uma pessoa atravessa
        rapidamente a imagem e a nova caixa fica
        distante da caixa anterior.

        Esta regra somente é aplicada quando:

        - existe uma única bounding box;
        - existe uma única pessoa ativa na câmera;
        - a associação normal falhou;
        - o intervalo entre as imagens é curto;
        - o tamanho das caixas continua plausível.

        Assim, a regra não interfere no rastreamento
        em lote de várias pessoas.
        """

        if len(bboxes) != 1:
            return

        if associacoes:
            return

        pessoas_camera = [
            pessoa
            for pessoa in self._pessoas.values()
            if pessoa.camera == camera
        ]

        if len(pessoas_camera) != 1:
            return

        pessoa = pessoas_camera[0]
        bbox = bboxes[0]

        continuidade_valida = (
            self._pode_usar_continuidade_flexivel(
                pessoa=pessoa,
                bbox=bbox,
                agora=agora,
            )
        )

        if continuidade_valida:
            associacoes[0] = pessoa

    def _pode_usar_continuidade_flexivel(
        self,
        pessoa: TrackedPerson,
        bbox: DetectionBox,
        agora: float,
    ) -> bool:
        tempo_sem_deteccao = max(
            0.0,
            agora - pessoa.ultima_deteccao,
        )

        if (
            tempo_sem_deteccao
            > self.janela_continuidade_flexivel
        ):
            return False

        menor_area = max(
            1,
            min(
                bbox.area,
                pessoa.bbox.area,
            ),
        )

        maior_area = max(
            bbox.area,
            pessoa.bbox.area,
        )

        razao_area = (
            maior_area
            / menor_area
        )

        if (
            razao_area
            > self.limite_razao_area_flexivel
        ):
            return False

        distancia = bbox.distancia_centros(
            pessoa.bbox
        )

        maior_dimensao = max(
            bbox.largura,
            bbox.altura,
            pessoa.bbox.largura,
            pessoa.bbox.altura,
            1,
        )

        distancia_normalizada = (
            distancia
            / maior_dimensao
        )

        limite_dinamico = min(
            self.limite_distancia_flexivel,
            max(
                self.limite_distancia,
                1.25
                + tempo_sem_deteccao * 0.45,
            ),
        )

        if (
            distancia_normalizada
            > limite_dinamico
        ):
            return False

        velocidade_normalizada = (
            distancia_normalizada
            / max(
                tempo_sem_deteccao,
                0.25,
            )
        )

        if (
            velocidade_normalizada
            > self.limite_velocidade_normalizada
        ):
            return False

        return True

    def _calcular_pontuacao(
        self,
        bbox: DetectionBox,
        pessoa: TrackedPerson,
    ) -> float | None:
        sobreposicao = bbox.iou(
            pessoa.bbox
        )

        distancia = bbox.distancia_centros(
            pessoa.bbox
        )

        maior_dimensao = max(
            bbox.largura,
            bbox.altura,
            pessoa.bbox.largura,
            pessoa.bbox.altura,
            1,
        )

        distancia_normalizada = (
            distancia
            / maior_dimensao
        )

        caixas_proximas = (
            distancia_normalizada
            <= self.limite_distancia
        )

        caixas_sobrepostas = (
            sobreposicao
            >= self.limite_iou
        )

        if not (
            caixas_proximas
            or caixas_sobrepostas
        ):
            return None

        pontuacao_distancia = max(
            0.0,
            1.0 - distancia_normalizada,
        )

        return (
            sobreposicao
            + pontuacao_distancia
        )

    def _atualizar_pessoa(
        self,
        pessoa: TrackedPerson,
        evento_id: str,
        bbox: DetectionBox,
        agora: float,
    ) -> TrackingDecision:
        pessoa.ultima_deteccao = agora
        pessoa.bbox = bbox
        pessoa.ultimo_evento_id = evento_id
        pessoa.quantidade_deteccoes += 1

        tempo_desde_processamento = (
            agora
            - pessoa.ultimo_processamento
        )

        if (
            tempo_desde_processamento
            >= self.intervalo_reprocessamento
        ):
            pessoa.ultimo_processamento = agora

            return TrackingDecision(
                pessoa_id=pessoa.pessoa_id,
                camera=pessoa.camera,
                evento_id=evento_id,
                status="updated",
                deve_processar=True,
                quantidade_deteccoes=(
                    pessoa.quantidade_deteccoes
                ),
                bbox=bbox,
            )

        return TrackingDecision(
            pessoa_id=pessoa.pessoa_id,
            camera=pessoa.camera,
            evento_id=evento_id,
            status="suppressed",
            deve_processar=False,
            quantidade_deteccoes=(
                pessoa.quantidade_deteccoes
            ),
            bbox=bbox,
        )

    def _encontrar_pessoa(
        self,
        camera: str,
        bbox: DetectionBox,
    ) -> TrackedPerson | None:
        """
        Mantido para compatibilidade com testes
        individuais.
        """

        melhor_pessoa: (
            TrackedPerson | None
        ) = None

        melhor_pontuacao = -1.0

        for pessoa in self._pessoas.values():
            if pessoa.camera != camera:
                continue

            pontuacao = (
                self._calcular_pontuacao(
                    bbox=bbox,
                    pessoa=pessoa,
                )
            )

            if pontuacao is None:
                continue

            if pontuacao > melhor_pontuacao:
                melhor_pontuacao = pontuacao
                melhor_pessoa = pessoa

        return melhor_pessoa

    def _criar_pessoa(
        self,
        camera: str,
        evento_id: str,
        bbox: DetectionBox,
        agora: float,
    ) -> TrackedPerson:
        camera_normalizada = "".join(
            caractere.lower()
            if caractere.isalnum()
            else "-"
            for caractere in camera
        ).strip("-")

        if not camera_normalizada:
            camera_normalizada = "camera"

        pessoa_id = (
            f"{camera_normalizada}-"
            f"{uuid.uuid4().hex[:8]}"
        )

        pessoa = TrackedPerson(
            pessoa_id=pessoa_id,
            camera=camera,
            primeira_deteccao=agora,
            ultima_deteccao=agora,
            ultimo_processamento=agora,
            bbox=bbox,
            quantidade_deteccoes=1,
            ultimo_evento_id=evento_id,
        )

        self._pessoas[
            pessoa_id
        ] = pessoa

        return pessoa

    def _criar_evento_individual_id(
        self,
        evento_id: str,
        indice: int,
        total: int,
    ) -> str:
        """
        Um quadro com uma pessoa mantém o ID original.

        Quadros com várias pessoas recebem IDs como:

        evento123-01
        evento123-02
        """

        if total <= 1:
            return evento_id

        return (
            f"{evento_id}-"
            f"{indice + 1:02d}"
        )


person_tracker = PersonTracker(
    intervalo_reprocessamento=5.0,
    tempo_para_saida=15.0,
    limite_iou=0.25,
    limite_distancia=0.75,
    janela_continuidade_flexivel=12.0,
    limite_distancia_flexivel=2.5,
    limite_razao_area_flexivel=12.0,
    limite_velocidade_normalizada=1.25,
)
------------------------------------------------------------
ARQUIVO: app/services/scene_analyzer_backup_antes_cores.py
------------------------------------------------------------
from __future__ import annotations

import colorsys
import math
from dataclasses import asdict, dataclass
from pathlib import Path
from statistics import median
from typing import Literal

from PIL import Image, ImageFilter


PosicaoHorizontal = Literal[
    "esquerda",
    "centro",
    "direita",
]

TamanhoNoQuadro = Literal[
    "pequeno",
    "medio",
    "grande",
]


@dataclass(frozen=True)
class PersonVisualAnalysis:
    cor_roupa_aproximada: str

    rgb_representativo: tuple[
        int,
        int,
        int,
    ]

    posicao_horizontal: PosicaoHorizontal
    tamanho_no_quadro: TamanhoNoQuadro
    percentual_quadro: float
    descricao: str

    def to_dict(self) -> dict:
        dados = asdict(self)

        dados["rgb_representativo"] = list(
            self.rgb_representativo
        )

        return dados


CORES_REFERENCIA: dict[
    str,
    tuple[int, int, int],
] = {
    "vermelha": (190, 45, 45),
    "laranja": (220, 115, 35),
    "amarela": (220, 200, 45),
    "verde": (55, 145, 70),
    "azul": (55, 95, 180),
    "roxa": (125, 65, 155),
    "rosa": (215, 105, 150),
    "marrom": (110, 70, 45),
    "bege": (195, 170, 125),
}


def analisar_pessoa(
    caminho_imagem: str | Path,
    x: int,
    y: int,
    largura: int,
    altura: int,
) -> PersonVisualAnalysis:
    caminho = Path(caminho_imagem)

    if not caminho.is_file():
        raise FileNotFoundError(
            f"Imagem não encontrada: {caminho}"
        )

    if largura <= 0 or altura <= 0:
        raise ValueError(
            "A bounding box deve possuir largura "
            "e altura positivas"
        )

    with Image.open(caminho) as imagem_aberta:
        imagem = imagem_aberta.convert("RGB")

        largura_imagem, altura_imagem = (
            imagem.size
        )

        x1 = max(
            0,
            min(
                int(x),
                largura_imagem - 1,
            ),
        )

        y1 = max(
            0,
            min(
                int(y),
                altura_imagem - 1,
            ),
        )

        x2 = max(
            x1 + 1,
            min(
                int(x + largura),
                largura_imagem,
            ),
        )

        y2 = max(
            y1 + 1,
            min(
                int(y + altura),
                altura_imagem,
            ),
        )

        largura_pessoa = x2 - x1
        altura_pessoa = y2 - y1

        regiao_roupa = _recortar_regiao_roupa(
            imagem=imagem,
            x1=x1,
            y1=y1,
            largura=largura_pessoa,
            altura=altura_pessoa,
        )

        rgb = _obter_cor_representativa(
            regiao_roupa
        )

        cor = _classificar_cor(
            rgb
        )

        posicao = _classificar_posicao(
            centro_x=(
                x1 + largura_pessoa / 2
            ),
            largura_imagem=largura_imagem,
        )

        percentual_quadro = round(
            (
                largura_pessoa
                * altura_pessoa
            )
            / (
                largura_imagem
                * altura_imagem
            )
            * 100,
            2,
        )

        tamanho = _classificar_tamanho(
            percentual_quadro
        )

        descricao = _montar_descricao(
            cor=cor,
            posicao=posicao,
            tamanho=tamanho,
        )

        return PersonVisualAnalysis(
            cor_roupa_aproximada=cor,
            rgb_representativo=rgb,
            posicao_horizontal=posicao,
            tamanho_no_quadro=tamanho,
            percentual_quadro=percentual_quadro,
            descricao=descricao,
        )


def _recortar_regiao_roupa(
    imagem: Image.Image,
    x1: int,
    y1: int,
    largura: int,
    altura: int,
) -> Image.Image:
    """
    Recorta somente a parte central do tronco.

    O recorte evita:

    - cabeça e rosto;
    - braços e mãos;
    - pernas;
    - bordas da bounding box;
    - parte do fundo ao redor da pessoa.
    """

    roupa_x1 = int(
        x1 + largura * 0.25
    )

    roupa_x2 = int(
        x1 + largura * 0.75
    )

    roupa_y1 = int(
        y1 + altura * 0.27
    )

    roupa_y2 = int(
        y1 + altura * 0.64
    )

    roupa_x2 = max(
        roupa_x1 + 1,
        roupa_x2,
    )

    roupa_y2 = max(
        roupa_y1 + 1,
        roupa_y2,
    )

    return imagem.crop(
        (
            roupa_x1,
            roupa_y1,
            roupa_x2,
            roupa_y2,
        )
    )


def _obter_cor_representativa(
    regiao: Image.Image,
) -> tuple[int, int, int]:
    """
    Calcula uma cor robusta para a roupa.

    Em vez de pegar simplesmente a cor que mais
    aparece, o método:

    - reduz a imagem;
    - remove ruídos;
    - descarta parte dos reflexos mais claros;
    - calcula a mediana de cada canal RGB.

    A mediana sofre menos influência da iluminação,
    do fundo e de pequenos reflexos.
    """

    regiao_reduzida = regiao.resize(
        (64, 64),
        Image.Resampling.LANCZOS,
    )

    regiao_suavizada = (
        regiao_reduzida.filter(
            ImageFilter.MedianFilter(
                size=3
            )
        )
    )

    pixels = list(
        regiao_suavizada.getdata()
    )

    if not pixels:
        return (0, 0, 0)

    luminancias = sorted(
        _calcular_luminancia(pixel)
        for pixel in pixels
    )

    indice_limite = min(
        len(luminancias) - 1,
        int(
            len(luminancias) * 0.85
        ),
    )

    limite_luminancia = (
        luminancias[indice_limite]
    )

    # Descarta aproximadamente os 15% de pixels
    # mais claros. Normalmente são reflexos, teto,
    # paredes ou fundo entrando na bounding box.
    candidatos = [
        pixel
        for pixel in pixels
        if (
            _calcular_luminancia(pixel)
            <= limite_luminancia
        )
    ]

    quantidade_minima = max(
        100,
        len(pixels) // 3,
    )

    if (
        len(candidatos)
        < quantidade_minima
    ):
        candidatos = pixels

    vermelho = int(
        round(
            median(
                pixel[0]
                for pixel in candidatos
            )
        )
    )

    verde = int(
        round(
            median(
                pixel[1]
                for pixel in candidatos
            )
        )
    )

    azul = int(
        round(
            median(
                pixel[2]
                for pixel in candidatos
            )
        )
    )

    return (
        vermelho,
        verde,
        azul,
    )


def _calcular_luminancia(
    rgb: tuple[int, int, int],
) -> float:
    """
    Calcula a luminosidade percebida pelo olho
    humano.

    O canal verde possui maior peso porque o olho
    humano é mais sensível a ele.
    """

    vermelho, verde, azul = rgb

    return (
        vermelho * 0.2126
        + verde * 0.7152
        + azul * 0.0722
    )


def _classificar_cor(
    rgb: tuple[int, int, int],
) -> str:
    vermelho, verde, azul = rgb

    r = vermelho / 255
    g = verde / 255
    b = azul / 255

    _, saturacao, brilho = (
        colorsys.rgb_to_hsv(
            r,
            g,
            b,
        )
    )

    luminancia = _calcular_luminancia(
        rgb
    )

    maior_canal = max(
        vermelho,
        verde,
        azul,
    )

    menor_canal = min(
        vermelho,
        verde,
        azul,
    )

    diferenca_canais = (
        maior_canal
        - menor_canal
    )

   
    if brilho <= 0.32:
        return "preta"

   
    if (
        luminancia <= 88
        and saturacao <= 0.52
    ):
        return "preta"

    
    if (
        brilho <= 0.42
        and diferenca_canais <= 45
    ):
        return "preta"

    if (
        luminancia <= 105
        and diferenca_canais <= 32
    ):
        return "preta"

    # Branco: alta luminosidade e pouca saturação.
    if (
        luminancia >= 218
        and saturacao <= 0.18
    ):
        return "branca"

    # Tons neutros sem cor forte.
    if saturacao <= 0.18:
        return "cinza"

    melhor_cor = "indefinida"
    menor_distancia = math.inf

    for nome, referencia in (
        CORES_REFERENCIA.items()
    ):
        distancia = math.sqrt(
            (
                vermelho
                - referencia[0]
            ) ** 2
            + (
                verde
                - referencia[1]
            ) ** 2
            + (
                azul
                - referencia[2]
            ) ** 2
        )

        if distancia < menor_distancia:
            menor_distancia = distancia
            melhor_cor = nome

    return melhor_cor


def _classificar_posicao(
    centro_x: float,
    largura_imagem: int,
) -> PosicaoHorizontal:
    proporcao = (
        centro_x
        / largura_imagem
    )

    if proporcao < 0.34:
        return "esquerda"

    if proporcao < 0.67:
        return "centro"

    return "direita"


def _classificar_tamanho(
    percentual_quadro: float,
) -> TamanhoNoQuadro:
    if percentual_quadro < 3:
        return "pequeno"

    if percentual_quadro < 12:
        return "medio"

    return "grande"


def _montar_descricao(
    cor: str,
    posicao: PosicaoHorizontal,
    tamanho: TamanhoNoQuadro,
) -> str:
    descricoes_posicao = {
        "esquerda": "à esquerda",
        "centro": "no centro",
        "direita": "à direita",
    }

    descricoes_tamanho = {
        "pequeno": (
            "ocupando uma pequena parte "
            "do enquadramento"
        ),
        "medio": (
            "ocupando uma parte intermediária "
            "do enquadramento"
        ),
        "grande": (
            "ocupando uma grande parte "
            "do enquadramento"
        ),
    }

    return (
        "Pessoa com roupa predominantemente "
        f"{cor}, localizada "
        f"{descricoes_posicao[posicao]} da cena, "
        f"{descricoes_tamanho[tamanho]}."
    )
------------------------------------------------------------
ARQUIVO: app/services/scene_analyzer_backup_antes_cor_preta.py
------------------------------------------------------------
from __future__ import annotations

import colorsys
import math
from collections import Counter
from dataclasses import asdict, dataclass
from pathlib import Path
from typing import Literal

from PIL import Image


PosicaoHorizontal = Literal[
    "esquerda",
    "centro",
    "direita",
]

TamanhoNoQuadro = Literal[
    "pequeno",
    "medio",
    "grande",
]


@dataclass(frozen=True)
class PersonVisualAnalysis:
    cor_roupa_aproximada: str

    rgb_representativo: tuple[
        int,
        int,
        int,
    ]

    posicao_horizontal: PosicaoHorizontal
    tamanho_no_quadro: TamanhoNoQuadro
    percentual_quadro: float
    descricao: str

    def to_dict(self) -> dict:
        dados = asdict(self)

        dados["rgb_representativo"] = list(
            self.rgb_representativo
        )

        return dados


CORES_REFERENCIA: dict[
    str,
    tuple[int, int, int],
] = {
    "vermelha": (190, 45, 45),
    "laranja": (220, 115, 35),
    "amarela": (220, 200, 45),
    "verde": (55, 145, 70),
    "azul": (55, 95, 180),
    "roxa": (125, 65, 155),
    "rosa": (215, 105, 150),
    "marrom": (110, 70, 45),
    "bege": (195, 170, 125),
}


def analisar_pessoa(
    caminho_imagem: str | Path,
    x: int,
    y: int,
    largura: int,
    altura: int,
) -> PersonVisualAnalysis:
    caminho = Path(caminho_imagem)

    if not caminho.exists():
        raise FileNotFoundError(
            f"Imagem não encontrada: {caminho}"
        )

    if largura <= 0 or altura <= 0:
        raise ValueError(
            "A bounding box deve possuir largura "
            "e altura positivas"
        )

    with Image.open(caminho) as imagem_aberta:
        imagem = imagem_aberta.convert("RGB")

        largura_imagem, altura_imagem = (
            imagem.size
        )

        x1 = max(
            0,
            min(x, largura_imagem - 1),
        )

        y1 = max(
            0,
            min(y, altura_imagem - 1),
        )

        x2 = max(
            x1 + 1,
            min(
                x + largura,
                largura_imagem,
            ),
        )

        y2 = max(
            y1 + 1,
            min(
                y + altura,
                altura_imagem,
            ),
        )

        largura_pessoa = x2 - x1
        altura_pessoa = y2 - y1

        regiao_roupa = _recortar_regiao_roupa(
            imagem=imagem,
            x1=x1,
            y1=y1,
            largura=largura_pessoa,
            altura=altura_pessoa,
        )

        rgb = _obter_cor_representativa(
            regiao_roupa
        )

        cor = _classificar_cor(rgb)

        posicao = _classificar_posicao(
            centro_x=(
                x1 + largura_pessoa / 2
            ),
            largura_imagem=largura_imagem,
        )

        percentual_quadro = round(
            (
                largura_pessoa
                * altura_pessoa
            )
            / (
                largura_imagem
                * altura_imagem
            )
            * 100,
            2,
        )

        tamanho = _classificar_tamanho(
            percentual_quadro
        )

        descricao = _montar_descricao(
            cor=cor,
            posicao=posicao,
            tamanho=tamanho,
        )

        return PersonVisualAnalysis(
            cor_roupa_aproximada=cor,
            rgb_representativo=rgb,
            posicao_horizontal=posicao,
            tamanho_no_quadro=tamanho,
            percentual_quadro=percentual_quadro,
            descricao=descricao,
        )


def _recortar_regiao_roupa(
    imagem: Image.Image,
    x1: int,
    y1: int,
    largura: int,
    altura: int,
) -> Image.Image:
    """
    Recorta a região central do tronco.

    Evita usar toda a bounding box, pois ela pode
    conter bastante fundo, cabeça e pernas.
    """

    roupa_x1 = int(
        x1 + largura * 0.20
    )

    roupa_x2 = int(
        x1 + largura * 0.80
    )

    roupa_y1 = int(
        y1 + altura * 0.22
    )

    roupa_y2 = int(
        y1 + altura * 0.62
    )

    roupa_x2 = max(
        roupa_x1 + 1,
        roupa_x2,
    )

    roupa_y2 = max(
        roupa_y1 + 1,
        roupa_y2,
    )

    return imagem.crop(
        (
            roupa_x1,
            roupa_y1,
            roupa_x2,
            roupa_y2,
        )
    )


def _obter_cor_representativa(
    regiao: Image.Image,
) -> tuple[int, int, int]:
    """
    Reduz a região e encontra a cor mais frequente.
    """

    regiao_reduzida = regiao.resize(
        (48, 48),
        Image.Resampling.LANCZOS,
    )

    regiao_quantizada = (
        regiao_reduzida
        .quantize(
            colors=6,
            method=Image.Quantize.MEDIANCUT,
        )
        .convert("RGB")
    )

    contagem = Counter(
        regiao_quantizada.getdata()
    )

    cor, _ = contagem.most_common(1)[0]

    return (
        int(cor[0]),
        int(cor[1]),
        int(cor[2]),
    )


def _classificar_cor(
    rgb: tuple[int, int, int],
) -> str:
    vermelho, verde, azul = rgb

    r = vermelho / 255
    g = verde / 255
    b = azul / 255

    _, saturacao, brilho = (
        colorsys.rgb_to_hsv(
            r,
            g,
            b,
        )
    )

    if brilho <= 0.20:
        return "preta"

    if (
        saturacao <= 0.14
        and brilho >= 0.84
    ):
        return "branca"

    if saturacao <= 0.18:
        return "cinza"

    melhor_cor = "indefinida"
    menor_distancia = math.inf

    for nome, referencia in (
        CORES_REFERENCIA.items()
    ):
        distancia = math.sqrt(
            (
                vermelho
                - referencia[0]
            ) ** 2
            + (
                verde
                - referencia[1]
            ) ** 2
            + (
                azul
                - referencia[2]
            ) ** 2
        )

        if distancia < menor_distancia:
            menor_distancia = distancia
            melhor_cor = nome

    return melhor_cor


def _classificar_posicao(
    centro_x: float,
    largura_imagem: int,
) -> PosicaoHorizontal:
    proporcao = (
        centro_x / largura_imagem
    )

    if proporcao < 0.34:
        return "esquerda"

    if proporcao < 0.67:
        return "centro"

    return "direita"


def _classificar_tamanho(
    percentual_quadro: float,
) -> TamanhoNoQuadro:
    if percentual_quadro < 3:
        return "pequeno"

    if percentual_quadro < 12:
        return "medio"

    return "grande"


def _montar_descricao(
    cor: str,
    posicao: PosicaoHorizontal,
    tamanho: TamanhoNoQuadro,
) -> str:
    descricoes_tamanho = {
        "pequeno": (
            "ocupando uma pequena parte "
            "do enquadramento"
        ),
        "medio": (
            "ocupando uma parte intermediária "
            "do enquadramento"
        ),
        "grande": (
            "ocupando uma grande parte "
            "do enquadramento"
        ),
    }

    return (
        "Pessoa com roupa predominantemente "
        f"{cor}, localizada no {posicao} da cena, "
        f"{descricoes_tamanho[tamanho]}."
    )
------------------------------------------------------------
ARQUIVO: app/services/scene_analyzer.py
------------------------------------------------------------
from __future__ import annotations

import colorsys
from dataclasses import asdict, dataclass
from pathlib import Path
from statistics import median
from typing import Literal

from PIL import Image, ImageFilter

from app.services.person_detector_yolo import (
    obter_rgb_roupa_segmentada,
)


PosicaoHorizontal = Literal[
    "esquerda",
    "centro",
    "direita",
]

TamanhoNoQuadro = Literal[
    "pequeno",
    "medio",
    "grande",
]


@dataclass(frozen=True)
class PersonVisualAnalysis:
    cor_roupa_aproximada: str

    rgb_representativo: tuple[
        int,
        int,
        int,
    ]

    posicao_horizontal: PosicaoHorizontal
    tamanho_no_quadro: TamanhoNoQuadro
    percentual_quadro: float
    descricao: str

    def to_dict(self) -> dict:
        dados = asdict(self)

        dados["rgb_representativo"] = list(
            self.rgb_representativo
        )

        return dados


def analisar_pessoa(
    caminho_imagem: str | Path,
    x: int,
    y: int,
    largura: int,
    altura: int,
) -> PersonVisualAnalysis:
    caminho = Path(caminho_imagem)

    if not caminho.is_file():
        raise FileNotFoundError(
            f"Imagem não encontrada: {caminho}"
        )

    if largura <= 0 or altura <= 0:
        raise ValueError(
            "A bounding box deve possuir largura "
            "e altura positivas"
        )

    with Image.open(caminho) as imagem_aberta:
        imagem = imagem_aberta.convert("RGB")

        largura_imagem, altura_imagem = (
            imagem.size
        )

        x1 = max(
            0,
            min(
                int(x),
                largura_imagem - 1,
            ),
        )

        y1 = max(
            0,
            min(
                int(y),
                altura_imagem - 1,
            ),
        )

        x2 = max(
            x1 + 1,
            min(
                int(x + largura),
                largura_imagem,
            ),
        )

        y2 = max(
            y1 + 1,
            min(
                int(y + altura),
                altura_imagem,
            ),
        )

        largura_pessoa = x2 - x1
        altura_pessoa = y2 - y1

        rgb = obter_rgb_roupa_segmentada(
            caminho_imagem=caminho,
            x=x1,
            y=y1,
            largura=largura_pessoa,
            altura=altura_pessoa,
        )

        # Fallback: mantém o método antigo quando o
        # YOLO não encontra uma máscara correspondente.
        if rgb is None:
            regiao_roupa = _recortar_regiao_roupa(
                imagem=imagem,
                x1=x1,
                y1=y1,
                largura=largura_pessoa,
                altura=altura_pessoa,
            )

            rgb = _obter_cor_representativa(
                regiao_roupa
            )

        cor = classificar_cor_rgb(
            rgb
        )

        posicao = _classificar_posicao(
            centro_x=(
                x1 + largura_pessoa / 2
            ),
            largura_imagem=largura_imagem,
        )

        percentual_quadro = round(
            (
                largura_pessoa
                * altura_pessoa
            )
            / (
                largura_imagem
                * altura_imagem
            )
            * 100,
            2,
        )

        tamanho = _classificar_tamanho(
            percentual_quadro
        )

        descricao = _montar_descricao(
            cor=cor,
            posicao=posicao,
            tamanho=tamanho,
        )

        return PersonVisualAnalysis(
            cor_roupa_aproximada=cor,
            rgb_representativo=rgb,
            posicao_horizontal=posicao,
            tamanho_no_quadro=tamanho,
            percentual_quadro=percentual_quadro,
            descricao=descricao,
        )


def _recortar_regiao_roupa(
    imagem: Image.Image,
    x1: int,
    y1: int,
    largura: int,
    altura: int,
) -> Image.Image:
    """
    Recorta a região central do tronco.

    A área é mais estreita que a bounding box para
    reduzir fundo, braços, rosto, mãos e calça.
    """

    roupa_x1 = int(
        x1 + largura * 0.27
    )

    roupa_x2 = int(
        x1 + largura * 0.73
    )

    roupa_y1 = int(
        y1 + altura * 0.25
    )

    roupa_y2 = int(
        y1 + altura * 0.58
    )

    roupa_x2 = max(
        roupa_x1 + 1,
        roupa_x2,
    )

    roupa_y2 = max(
        roupa_y1 + 1,
        roupa_y2,
    )

    return imagem.crop(
        (
            roupa_x1,
            roupa_y1,
            roupa_x2,
            roupa_y2,
        )
    )


def _obter_cor_representativa(
    regiao: Image.Image,
) -> tuple[int, int, int]:
    """
    Calcula uma cor robusta da roupa.

    O método anterior descartava apenas os pixels
    claros, deixando o resultado artificialmente
    escuro. Agora são removidas pequenas parcelas
    das sombras mais escuras e dos reflexos mais
    claros antes de calcular a mediana RGB.
    """

    regiao_reduzida = regiao.resize(
        (64, 64),
        Image.Resampling.LANCZOS,
    )

    regiao_suavizada = (
        regiao_reduzida.filter(
            ImageFilter.MedianFilter(
                size=3
            )
        )
    )

    pixels = list(
        regiao_suavizada.getdata()
    )

    if not pixels:
        return (0, 0, 0)

    pixels_ordenados = sorted(
        pixels,
        key=_calcular_luminancia,
    )

    quantidade = len(
        pixels_ordenados
    )

    corte = int(
        quantidade * 0.08
    )

    if (
        corte > 0
        and quantidade - corte > corte
    ):
        candidatos = pixels_ordenados[
            corte:quantidade - corte
        ]
    else:
        candidatos = pixels_ordenados

    quantidade_minima = max(
        100,
        quantidade // 3,
    )

    if (
        len(candidatos)
        < quantidade_minima
    ):
        candidatos = pixels

    vermelho = int(
        round(
            median(
                pixel[0]
                for pixel in candidatos
            )
        )
    )

    verde = int(
        round(
            median(
                pixel[1]
                for pixel in candidatos
            )
        )
    )

    azul = int(
        round(
            median(
                pixel[2]
                for pixel in candidatos
            )
        )
    )

    return (
        vermelho,
        verde,
        azul,
    )


def _calcular_luminancia(
    rgb: tuple[int, int, int],
) -> float:
    vermelho, verde, azul = rgb

    return (
        vermelho * 0.2126
        + verde * 0.7152
        + azul * 0.0722
    )


def classificar_cor_rgb(
    rgb: tuple[int, int, int],
) -> str:
    """
    Classifica uma cor RGB usando HSV, luminância
    e diferença entre os canais.

    A regra evita chamar qualquer cor escura de
    preta. Tons escuros com saturação perceptível
    são classificados como azul-escura,
    verde-escura, vermelha-escura ou roxa-escura.
    """

    vermelho, verde, azul = (
        max(0, min(255, int(canal)))
        for canal in rgb
    )

    r = vermelho / 255
    g = verde / 255
    b = azul / 255

    matiz, saturacao, brilho = (
        colorsys.rgb_to_hsv(
            r,
            g,
            b,
        )
    )

    matiz_graus = (
        matiz * 360
    )

    luminancia = _calcular_luminancia(
        (
            vermelho,
            verde,
            azul,
        )
    )

    maior_canal = max(
        vermelho,
        verde,
        azul,
    )

    menor_canal = min(
        vermelho,
        verde,
        azul,
    )

    diferenca_canais = (
        maior_canal
        - menor_canal
    )

    # Zona de baixa iluminação em que preto e
    # azul-marinho ficam visualmente indistinguíveis.
    #
    # Nessa faixa, pequenas diferenças entre canais
    # podem ser apenas ruído, compressão JPEG ou
    # iluminação da câmera. É mais seguro não
    # inventar uma cor.
    if (
        brilho <= 0.20
        and maior_canal <= 55
        and 0.16 < saturacao < 0.38
        and 5 < diferenca_canais < 18
    ):
        return "escura-indefinida"

    canais_ordenados = sorted(
        (
            vermelho,
            verde,
            azul,
        ),
        reverse=True,
    )

    segundo_maior_canal = (
        canais_ordenados[1]
    )

    vantagem_canal_dominante = (
        maior_canal
        - segundo_maior_canal
    )

    # Em regiões extremamente escuras, a matiz HSV
    # fica instável e pequenos ruídos da câmera podem
    # transformar preto em azul, verde ou vermelho.
    #
    # Só preserva uma cor muito escura quando existe
    # sinal cromático suficiente nos pixels.
    if brilho <= 0.18:
        cor_escura_perceptivel = (
            maior_canal >= 34
            and saturacao >= 0.20
            and diferenca_canais >= 8
            and vantagem_canal_dominante >= 5
        )

        if not cor_escura_perceptivel:
            return "preta"

    # Preto verdadeiro ou preto iluminado.
    if (
        brilho <= 0.12
        or (
            brilho <= 0.28
            and diferenca_canais <= 5
        )
        or (
            luminancia <= 58
            and saturacao <= 0.14
        )
    ):
        return "preta"

    # Branco e tons neutros.
    if (
        luminancia >= 218
        and saturacao <= 0.18
    ):
        return "branca"

    if saturacao <= 0.16:
        if luminancia < 135:
            return "cinza-escura"

        return "cinza"

    # Marrom e bege ficam na faixa de laranja,
    # mas são separados pelo brilho.
    if (
        15 <= matiz_graus < 48
        and saturacao >= 0.22
    ):
        if brilho <= 0.62:
            return "marrom"

        if (
            saturacao <= 0.48
            and brilho >= 0.66
        ):
            return "bege"

        return "laranja"

    cor_base = _classificar_por_matiz(
        matiz_graus
    )

    # Preserva a cor de roupas escuras saturadas.
    if brilho <= 0.42:
        cores_escuras = {
            "vermelha": "vermelha-escura",
            "verde": "verde-escura",
            "azul": "azul-escura",
            "roxa": "roxa-escura",
        }

        return cores_escuras.get(
            cor_base,
            cor_base,
        )

    return cor_base


def _classificar_por_matiz(
    matiz_graus: float,
) -> str:
    if (
        matiz_graus < 15
        or matiz_graus >= 345
    ):
        return "vermelha"

    if matiz_graus < 48:
        return "laranja"

    if matiz_graus < 72:
        return "amarela"

    if matiz_graus < 170:
        return "verde"

    if matiz_graus < 200:
        return "azul"

    if matiz_graus < 260:
        return "azul"

    if matiz_graus < 315:
        return "roxa"

    return "rosa"


def _classificar_cor(
    rgb: tuple[int, int, int],
) -> str:
    """
    Mantém compatibilidade com testes ou imports
    antigos que utilizavam a função privada.
    """

    return classificar_cor_rgb(
        rgb
    )


def _classificar_posicao(
    centro_x: float,
    largura_imagem: int,
) -> PosicaoHorizontal:
    proporcao = (
        centro_x
        / largura_imagem
    )

    if proporcao < 0.34:
        return "esquerda"

    if proporcao < 0.67:
        return "centro"

    return "direita"


def _classificar_tamanho(
    percentual_quadro: float,
) -> TamanhoNoQuadro:
    if percentual_quadro < 3:
        return "pequeno"

    if percentual_quadro < 12:
        return "medio"

    return "grande"


def _montar_descricao(
    cor: str,
    posicao: PosicaoHorizontal,
    tamanho: TamanhoNoQuadro,
) -> str:
    descricoes_posicao = {
        "esquerda": "à esquerda",
        "centro": "no centro",
        "direita": "à direita",
    }

    descricoes_tamanho = {
        "pequeno": (
            "ocupando uma pequena parte "
            "do enquadramento"
        ),
        "medio": (
            "ocupando uma parte intermediária "
            "do enquadramento"
        ),
        "grande": (
            "ocupando uma grande parte "
            "do enquadramento"
        ),
    }

    return (
        "Pessoa com roupa predominantemente "
        f"{cor}, localizada "
        f"{descricoes_posicao[posicao]} da cena, "
        f"{descricoes_tamanho[tamanho]}."
    )

------------------------------------------------------------
ARQUIVO: app/static/index_backup_analise_visual.html
------------------------------------------------------------
<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">

    <meta
        name="viewport"
        content="width=device-width, initial-scale=1.0"
    >

    <title>Painel de Eventos</title>

    <style>
        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            font-family: Arial, sans-serif;
            background: #10131a;
            color: #f3f4f6;
        }

        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px 30px;
            background: #191e29;
            border-bottom: 1px solid #2d3443;
        }

        h1 {
            margin: 0;
            font-size: 24px;
        }

        .status-conexao {
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .status-bolinha {
            width: 12px;
            height: 12px;
            border-radius: 50%;
            background: #f59e0b;
        }

        .status-bolinha.conectado {
            background: #22c55e;
        }

        .status-bolinha.desconectado {
            background: #ef4444;
        }

        main {
            padding: 30px;
        }

        .resumo {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
            margin-bottom: 25px;
        }

        .resumo-card {
            min-width: 180px;
            padding: 18px;
            background: #191e29;
            border: 1px solid #2d3443;
            border-radius: 10px;
        }

        .resumo-card span {
            display: block;
            color: #9ca3af;
            font-size: 14px;
        }

        .resumo-card strong {
            display: block;
            margin-top: 8px;
            font-size: 28px;
        }

        #eventos {
            display: grid;
            grid-template-columns: repeat(
                auto-fill,
                minmax(280px, 1fr)
            );
            gap: 20px;
        }

        .evento-card {
            overflow: hidden;
            background: #191e29;
            border: 1px solid #2d3443;
            border-radius: 10px;
            transition:
                border-color 0.2s,
                transform 0.2s,
                opacity 0.2s;
        }

        .evento-card.atualizado {
            border-color: #3b82f6;
            transform: translateY(-2px);
        }

        .evento-card.saiu {
            opacity: 0.55;
            border-color: #6b7280;
        }

        .evento-card img {
            width: 100%;
            height: 230px;
            object-fit: contain;
            background: #05070a;
        }

        .evento-dados {
            padding: 15px;
        }

        .evento-cabecalho {
            display: flex;
            justify-content: space-between;
            align-items: center;
            gap: 10px;
            margin-bottom: 12px;
        }

        .evento-cabecalho h2 {
            margin: 0;
            font-size: 18px;
        }

        .evento-status {
            padding: 5px 9px;
            border-radius: 999px;
            background: #374151;
            font-size: 12px;
            font-weight: bold;
        }

        .evento-status.entered {
            background: #166534;
            color: #dcfce7;
        }

        .evento-status.updated {
            background: #1e40af;
            color: #dbeafe;
        }

        .evento-status.exited {
            background: #4b5563;
            color: #f3f4f6;
        }

        .evento-dados p {
            margin: 6px 0;
            color: #cbd5e1;
            font-size: 14px;
        }

        .vazio {
            padding: 40px;
            color: #9ca3af;
            text-align: center;
            background: #191e29;
            border: 1px dashed #374151;
            border-radius: 10px;
        }

        @media (max-width: 700px) {
            header {
                align-items: flex-start;
                flex-direction: column;
                gap: 15px;
                padding: 20px;
            }

            main {
                padding: 20px;
            }

            .resumo-card {
                flex: 1;
                min-width: 140px;
            }
        }
    </style>
</head>

<body>
    <header>
        <h1>Painel de Eventos das Câmeras</h1>

        <div class="status-conexao">
            <div
                id="status-bolinha"
                class="status-bolinha"
            ></div>

            <span id="status-texto">
                Conectando...
            </span>
        </div>
    </header>

    <main>
        <section class="resumo">
            <div class="resumo-card">
                <span>Pessoas ativas</span>
                <strong id="pessoas-ativas">0</strong>
            </div>

            <div class="resumo-card">
                <span>Pessoas que saíram</span>
                <strong id="pessoas-sairam">0</strong>
            </div>

            <div class="resumo-card">
                <span>Total exibido</span>
                <strong id="total-pessoas">0</strong>
            </div>

            <div class="resumo-card">
                <span>Eventos processados</span>
                <strong id="total-eventos">0</strong>
            </div>
        </section>

        <section id="eventos">
            <div id="mensagem-vazia" class="vazio">
                Aguardando eventos das câmeras...
            </div>
        </section>
    </main>

    <script>
        const statusBolinha =
            document.getElementById("status-bolinha");

        const statusTexto =
            document.getElementById("status-texto");

        const pessoasAtivasElemento =
            document.getElementById("pessoas-ativas");

        const pessoasSairamElemento =
            document.getElementById("pessoas-sairam");

        const totalPessoasElemento =
            document.getElementById("total-pessoas");

        const totalEventosElemento =
            document.getElementById("total-eventos");

        const eventosElemento =
            document.getElementById("eventos");

        /*
         * Guarda o cartão e o último estado de cada pessoa.
         */
        const cartoesPorPessoa = new Map();
        const statusPorPessoa = new Map();

        let pessoasAtivas = 0;
        let pessoasSairam = 0;
        let totalPessoas = 0;
        let totalEventos = 0;
        let heartbeatId = null;

        function traduzirStatus(status) {
            const traducoes = {
                entered: "Entrou",
                updated: "Atualizado",
                suppressed: "Repetido",
                exited: "Saiu"
            };

            return (
                traducoes[status]
                || status
                || "Detectado"
            );
        }

        function atualizarResumo() {
            pessoasAtivasElemento.textContent =
                pessoasAtivas;

            pessoasSairamElemento.textContent =
                pessoasSairam;

            totalPessoasElemento.textContent =
                totalPessoas;

            totalEventosElemento.textContent =
                totalEventos;
        }

        function registrarMudancaStatus(
            pessoaId,
            novoStatus
        ) {
            const statusAnterior =
                statusPorPessoa.get(pessoaId);

            /*
             * Primeira mensagem recebida dessa pessoa.
             */
            if (!statusAnterior) {
                totalPessoas += 1;

                if (novoStatus === "exited") {
                    pessoasSairam += 1;
                } else {
                    pessoasAtivas += 1;
                }
            }

            /*
             * A pessoa estava ativa e agora saiu.
             */
            else if (
                statusAnterior !== "exited"
                && novoStatus === "exited"
            ) {
                pessoasAtivas = Math.max(
                    0,
                    pessoasAtivas - 1
                );

                pessoasSairam += 1;
            }

            /*
             * Segurança caso o mesmo identificador volte
             * a receber um evento ativo.
             */
            else if (
                statusAnterior === "exited"
                && novoStatus !== "exited"
            ) {
                pessoasSairam = Math.max(
                    0,
                    pessoasSairam - 1
                );

                pessoasAtivas += 1;
            }

            statusPorPessoa.set(
                pessoaId,
                novoStatus
            );

            atualizarResumo();
        }

        function conectarWebSocket() {
            const protocolo =
                window.location.protocol === "https:"
                    ? "wss"
                    : "ws";

            const endereco =
                `${protocolo}://${window.location.host}/ws/eventos`;

            const websocket =
                new WebSocket(endereco);

            websocket.onopen = function () {
                statusTexto.textContent =
                    "Conectado";

                statusBolinha.className =
                    "status-bolinha conectado";

                if (heartbeatId) {
                    clearInterval(heartbeatId);
                }

                heartbeatId = setInterval(
                    function () {
                        if (
                            websocket.readyState
                            === WebSocket.OPEN
                        ) {
                            websocket.send("ping");
                        }
                    },
                    20000
                );
            };

            websocket.onmessage = function (mensagem) {
                try {
                    const evento =
                        JSON.parse(mensagem.data);

                    processarEvento(evento);
                } catch (erro) {
                    console.error(
                        "Erro ao interpretar evento:",
                        erro
                    );
                }
            };

            websocket.onclose = function () {
                statusTexto.textContent =
                    "Desconectado — reconectando...";

                statusBolinha.className =
                    "status-bolinha desconectado";

                if (heartbeatId) {
                    clearInterval(heartbeatId);
                    heartbeatId = null;
                }

                setTimeout(
                    conectarWebSocket,
                    2000
                );
            };

            websocket.onerror = function () {
                websocket.close();
            };
        }

        function processarEvento(evento) {
            /*
             * O backend já bloqueia eventos repetidos.
             * Esta verificação é uma proteção adicional.
             */
            if (evento.status === "suppressed") {
                return;
            }

            const pessoaId =
                evento.pessoa_id
                || evento.evento_id;

            if (!pessoaId) {
                console.warn(
                    "Evento recebido sem identificador",
                    evento
                );

                return;
            }

            const cartaoExistente =
                cartoesPorPessoa.get(pessoaId);

            /*
             * Não cria cartão vazio quando o painel
             * conecta somente depois da saída da pessoa.
             */
            if (
                evento.status === "exited"
                && !cartaoExistente
            ) {
                return;
            }

            const novoStatus =
                evento.status || "entered";

            registrarMudancaStatus(
                pessoaId,
                novoStatus
            );

            totalEventos += 1;
            atualizarResumo();

            if (cartaoExistente) {
                atualizarCartao(
                    cartaoExistente,
                    evento
                );

                return;
            }

            criarCartao(
                pessoaId,
                evento
            );
        }

        function criarCartao(
            pessoaId,
            evento
        ) {
            const mensagemVazia =
                document.getElementById(
                    "mensagem-vazia"
                );

            if (mensagemVazia) {
                mensagemVazia.remove();
            }

            const card =
                document.createElement("article");

            card.className = "evento-card";
            card.dataset.pessoaId = pessoaId;

            const imagem =
                document.createElement("img");

            imagem.alt = "Recorte da pessoa";
            imagem.dataset.campo = "imagem";

            const dados =
                document.createElement("div");

            dados.className = "evento-dados";

            const cabecalho =
                document.createElement("div");

            cabecalho.className =
                "evento-cabecalho";

            const titulo =
                document.createElement("h2");

            titulo.dataset.campo = "camera";

            const status =
                document.createElement("span");

            status.dataset.campo = "status";
            status.className = "evento-status";

            const tipo =
                document.createElement("p");

            tipo.dataset.campo = "tipo";

            const horario =
                document.createElement("p");

            horario.dataset.campo = "horario";

            const identificadorPessoa =
                document.createElement("p");

            identificadorPessoa.dataset.campo =
                "pessoa-id";

            const quantidade =
                document.createElement("p");

            quantidade.dataset.campo =
                "quantidade";

            cabecalho.appendChild(titulo);
            cabecalho.appendChild(status);

            dados.appendChild(cabecalho);
            dados.appendChild(tipo);
            dados.appendChild(horario);
            dados.appendChild(
                identificadorPessoa
            );
            dados.appendChild(quantidade);

            card.appendChild(imagem);
            card.appendChild(dados);

            eventosElemento.prepend(card);

            cartoesPorPessoa.set(
                pessoaId,
                card
            );

            atualizarCartao(
                card,
                evento
            );

            limitarQuantidadeCartoes();
        }

        function atualizarCartao(
            card,
            evento
        ) {
            const imagem =
                card.querySelector(
                    '[data-campo="imagem"]'
                );

            const titulo =
                card.querySelector(
                    '[data-campo="camera"]'
                );

            const status =
                card.querySelector(
                    '[data-campo="status"]'
                );

            const tipo =
                card.querySelector(
                    '[data-campo="tipo"]'
                );

            const horario =
                card.querySelector(
                    '[data-campo="horario"]'
                );

            const pessoaId =
                card.querySelector(
                    '[data-campo="pessoa-id"]'
                );

            const quantidade =
                card.querySelector(
                    '[data-campo="quantidade"]'
                );

            /*
             * O evento de saída não envia uma imagem.
             * A última captura permanece no cartão.
             */
            if (evento.imagem) {
                imagem.src =
                    `data:image/jpeg;base64,${evento.imagem}`;
            }

            if (evento.camera) {
                titulo.textContent =
                    evento.camera;
            }

            status.textContent =
                traduzirStatus(evento.status);

            status.className =
                `evento-status ${evento.status || ""}`;

            /*
             * O evento de saída não possui um novo tipo.
             * Mantemos o último tipo exibido.
             */
            if (evento.tipo) {
                tipo.textContent =
                    `Evento: ${evento.tipo}`;
            }

            if (evento.status === "exited") {
                horario.textContent =
                    `Saída: ${evento.datetime || "-"}`;
            } else {
                horario.textContent =
                    `Última captura: ${
                        evento.datetime || "-"
                    }`;
            }

            pessoaId.textContent =
                `Pessoa: ${
                    evento.pessoa_id || "-"
                }`;

            quantidade.textContent =
                `Detecções: ${
                    evento.quantidade_deteccoes || 1
                }`;

            card.classList.remove(
                "atualizado",
                "saiu"
            );

            if (evento.status === "updated") {
                card.classList.add(
                    "atualizado"
                );

                /*
                 * Move a pessoa atualizada para o início.
                 */
                eventosElemento.prepend(card);

                setTimeout(
                    function () {
                        card.classList.remove(
                            "atualizado"
                        );
                    },
                    1500
                );
            }

            if (evento.status === "exited") {
                card.classList.add("saiu");
            }
        }

        function limitarQuantidadeCartoes() {
            while (
                eventosElemento.children.length > 50
            ) {
                const ultimo =
                    eventosElemento.lastElementChild;

                if (!ultimo) {
                    break;
                }

                const pessoaId =
                    ultimo.dataset.pessoaId;

                if (pessoaId) {
                    const statusRemovido =
                        statusPorPessoa.get(pessoaId);

                    if (statusRemovido === "exited") {
                        pessoasSairam = Math.max(
                            0,
                            pessoasSairam - 1
                        );
                    } else if (statusRemovido) {
                        pessoasAtivas = Math.max(
                            0,
                            pessoasAtivas - 1
                        );
                    }

                    totalPessoas = Math.max(
                        0,
                        totalPessoas - 1
                    );

                    cartoesPorPessoa.delete(
                        pessoaId
                    );

                    statusPorPessoa.delete(
                        pessoaId
                    );
                }

                ultimo.remove();
            }

            atualizarResumo();
        }

        atualizarResumo();
        conectarWebSocket();
    </script>
</body>
</html>
------------------------------------------------------------
ARQUIVO: app/static/index_backup_antes_analise.html
------------------------------------------------------------
<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">

    <meta
        name="viewport"
        content="width=device-width, initial-scale=1.0"
    >

    <title>Painel de Eventos</title>

    <style>
        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            font-family: Arial, sans-serif;
            background: #10131a;
            color: #f3f4f6;
        }

        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px 30px;
            background: #191e29;
            border-bottom: 1px solid #2d3443;
        }

        h1 {
            margin: 0;
            font-size: 24px;
        }

        .status-conexao {
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .status-bolinha {
            width: 12px;
            height: 12px;
            border-radius: 50%;
            background: #f59e0b;
        }

        .status-bolinha.conectado {
            background: #22c55e;
        }

        .status-bolinha.desconectado {
            background: #ef4444;
        }

        main {
            padding: 30px;
        }

        .resumo {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
            margin-bottom: 25px;
        }

        .resumo-card {
            min-width: 180px;
            padding: 18px;
            background: #191e29;
            border: 1px solid #2d3443;
            border-radius: 10px;
        }

        .resumo-card span {
            display: block;
            color: #9ca3af;
            font-size: 14px;
        }

        .resumo-card strong {
            display: block;
            margin-top: 8px;
            font-size: 28px;
        }

        #eventos {
            display: grid;
            grid-template-columns: repeat(
                auto-fill,
                minmax(280px, 1fr)
            );
            gap: 20px;
        }

        .evento-card {
            overflow: hidden;
            background: #191e29;
            border: 1px solid #2d3443;
            border-radius: 10px;
            transition:
                border-color 0.2s,
                transform 0.2s,
                opacity 0.2s;
        }

        .evento-card.atualizado {
            border-color: #3b82f6;
            transform: translateY(-2px);
        }

        .evento-card.saiu {
            opacity: 0.55;
            border-color: #6b7280;
        }

        .evento-card img {
            width: 100%;
            height: 230px;
            object-fit: contain;
            background: #05070a;
        }

        .evento-dados {
            padding: 15px;
        }

        .evento-cabecalho {
            display: flex;
            justify-content: space-between;
            align-items: center;
            gap: 10px;
            margin-bottom: 12px;
        }

        .evento-cabecalho h2 {
            margin: 0;
            font-size: 18px;
        }

        .evento-status {
            padding: 5px 9px;
            border-radius: 999px;
            background: #374151;
            font-size: 12px;
            font-weight: bold;
        }

        .evento-status.entered {
            background: #166534;
            color: #dcfce7;
        }

        .evento-status.updated {
            background: #1e40af;
            color: #dbeafe;
        }

        .evento-status.exited {
            background: #4b5563;
            color: #f3f4f6;
        }

        .evento-dados p {
            margin: 6px 0;
            color: #cbd5e1;
            font-size: 14px;
        }

        .vazio {
            padding: 40px;
            color: #9ca3af;
            text-align: center;
            background: #191e29;
            border: 1px dashed #374151;
            border-radius: 10px;
        }

        @media (max-width: 700px) {
            header {
                align-items: flex-start;
                flex-direction: column;
                gap: 15px;
                padding: 20px;
            }

            main {
                padding: 20px;
            }

            .resumo-card {
                flex: 1;
                min-width: 140px;
            }
        }
    </style>
</head>

<body>
    <header>
        <h1>Painel de Eventos das Câmeras</h1>

        <div class="status-conexao">
            <div
                id="status-bolinha"
                class="status-bolinha"
            ></div>

            <span id="status-texto">
                Conectando...
            </span>
        </div>
    </header>

    <main>
        <section class="resumo">
            <div class="resumo-card">
                <span>Pessoas ativas</span>
                <strong id="pessoas-ativas">0</strong>
            </div>

            <div class="resumo-card">
                <span>Pessoas que saíram</span>
                <strong id="pessoas-sairam">0</strong>
            </div>

            <div class="resumo-card">
                <span>Total exibido</span>
                <strong id="total-pessoas">0</strong>
            </div>

            <div class="resumo-card">
                <span>Eventos processados</span>
                <strong id="total-eventos">0</strong>
            </div>
        </section>

        <section id="eventos">
            <div id="mensagem-vazia" class="vazio">
                Aguardando eventos das câmeras...
            </div>
        </section>
    </main>

    <script>
        const statusBolinha =
            document.getElementById("status-bolinha");

        const statusTexto =
            document.getElementById("status-texto");

        const pessoasAtivasElemento =
            document.getElementById("pessoas-ativas");

        const pessoasSairamElemento =
            document.getElementById("pessoas-sairam");

        const totalPessoasElemento =
            document.getElementById("total-pessoas");

        const totalEventosElemento =
            document.getElementById("total-eventos");

        const eventosElemento =
            document.getElementById("eventos");

        /*
         * Guarda o cartão e o último estado de cada pessoa.
         */
        const cartoesPorPessoa = new Map();
        const statusPorPessoa = new Map();

        let pessoasAtivas = 0;
        let pessoasSairam = 0;
        let totalPessoas = 0;
        let totalEventos = 0;
        let heartbeatId = null;

        function traduzirStatus(status) {
            const traducoes = {
                entered: "Entrou",
                updated: "Atualizado",
                suppressed: "Repetido",
                exited: "Saiu"
            };

            return (
                traducoes[status]
                || status
                || "Detectado"
            );
        }

        function atualizarResumo() {
            pessoasAtivasElemento.textContent =
                pessoasAtivas;

            pessoasSairamElemento.textContent =
                pessoasSairam;

            totalPessoasElemento.textContent =
                totalPessoas;

            totalEventosElemento.textContent =
                totalEventos;
        }

        function registrarMudancaStatus(
            pessoaId,
            novoStatus
        ) {
            const statusAnterior =
                statusPorPessoa.get(pessoaId);

            /*
             * Primeira mensagem recebida dessa pessoa.
             */
            if (!statusAnterior) {
                totalPessoas += 1;

                if (novoStatus === "exited") {
                    pessoasSairam += 1;
                } else {
                    pessoasAtivas += 1;
                }
            }

            /*
             * A pessoa estava ativa e agora saiu.
             */
            else if (
                statusAnterior !== "exited"
                && novoStatus === "exited"
            ) {
                pessoasAtivas = Math.max(
                    0,
                    pessoasAtivas - 1
                );

                pessoasSairam += 1;
            }

            /*
             * Segurança caso o mesmo identificador volte
             * a receber um evento ativo.
             */
            else if (
                statusAnterior === "exited"
                && novoStatus !== "exited"
            ) {
                pessoasSairam = Math.max(
                    0,
                    pessoasSairam - 1
                );

                pessoasAtivas += 1;
            }

            statusPorPessoa.set(
                pessoaId,
                novoStatus
            );

            atualizarResumo();
        }

        function conectarWebSocket() {
            const protocolo =
                window.location.protocol === "https:"
                    ? "wss"
                    : "ws";

            const endereco =
                `${protocolo}://${window.location.host}/ws/eventos`;

            const websocket =
                new WebSocket(endereco);

            websocket.onopen = function () {
                statusTexto.textContent =
                    "Conectado";

                statusBolinha.className =
                    "status-bolinha conectado";

                if (heartbeatId) {
                    clearInterval(heartbeatId);
                }

                heartbeatId = setInterval(
                    function () {
                        if (
                            websocket.readyState
                            === WebSocket.OPEN
                        ) {
                            websocket.send("ping");
                        }
                    },
                    20000
                );
            };

            websocket.onmessage = function (mensagem) {
                try {
                    const evento =
                        JSON.parse(mensagem.data);

                    processarEvento(evento);
                } catch (erro) {
                    console.error(
                        "Erro ao interpretar evento:",
                        erro
                    );
                }
            };

            websocket.onclose = function () {
                statusTexto.textContent =
                    "Desconectado — reconectando...";

                statusBolinha.className =
                    "status-bolinha desconectado";

                if (heartbeatId) {
                    clearInterval(heartbeatId);
                    heartbeatId = null;
                }

                setTimeout(
                    conectarWebSocket,
                    2000
                );
            };

            websocket.onerror = function () {
                websocket.close();
            };
        }

        function processarEvento(evento) {
            /*
             * O backend já bloqueia eventos repetidos.
             * Esta verificação é uma proteção adicional.
             */
            if (evento.status === "suppressed") {
                return;
            }

            const pessoaId =
                evento.pessoa_id
                || evento.evento_id;

            if (!pessoaId) {
                console.warn(
                    "Evento recebido sem identificador",
                    evento
                );

                return;
            }

            const cartaoExistente =
                cartoesPorPessoa.get(pessoaId);

            /*
             * Não cria cartão vazio quando o painel
             * conecta somente depois da saída da pessoa.
             */
            if (
                evento.status === "exited"
                && !cartaoExistente
            ) {
                return;
            }

            const novoStatus =
                evento.status || "entered";

            registrarMudancaStatus(
                pessoaId,
                novoStatus
            );

            totalEventos += 1;
            atualizarResumo();

            if (cartaoExistente) {
                atualizarCartao(
                    cartaoExistente,
                    evento
                );

                return;
            }

            criarCartao(
                pessoaId,
                evento
            );
        }

        function criarCartao(
            pessoaId,
            evento
        ) {
            const mensagemVazia =
                document.getElementById(
                    "mensagem-vazia"
                );

            if (mensagemVazia) {
                mensagemVazia.remove();
            }

            const card =
                document.createElement("article");

            card.className = "evento-card";
            card.dataset.pessoaId = pessoaId;

            const imagem =
                document.createElement("img");

            imagem.alt = "Recorte da pessoa";
            imagem.dataset.campo = "imagem";

            const dados =
                document.createElement("div");

            dados.className = "evento-dados";

            const cabecalho =
                document.createElement("div");

            cabecalho.className =
                "evento-cabecalho";

            const titulo =
                document.createElement("h2");

            titulo.dataset.campo = "camera";

            const status =
                document.createElement("span");

            status.dataset.campo = "status";
            status.className = "evento-status";

            const tipo =
                document.createElement("p");

            tipo.dataset.campo = "tipo";

            const horario =
                document.createElement("p");

            horario.dataset.campo = "horario";

            const identificadorPessoa =
                document.createElement("p");

            identificadorPessoa.dataset.campo =
                "pessoa-id";

            const quantidade =
                document.createElement("p");

            quantidade.dataset.campo =
                "quantidade";

            cabecalho.appendChild(titulo);
            cabecalho.appendChild(status);

            dados.appendChild(cabecalho);
            dados.appendChild(tipo);
            dados.appendChild(horario);
            dados.appendChild(
                identificadorPessoa
            );
            dados.appendChild(quantidade);

            card.appendChild(imagem);
            card.appendChild(dados);

            eventosElemento.prepend(card);

            cartoesPorPessoa.set(
                pessoaId,
                card
            );

            atualizarCartao(
                card,
                evento
            );

            limitarQuantidadeCartoes();
        }

        function atualizarCartao(
            card,
            evento
        ) {
            const imagem =
                card.querySelector(
                    '[data-campo="imagem"]'
                );

            const titulo =
                card.querySelector(
                    '[data-campo="camera"]'
                );

            const status =
                card.querySelector(
                    '[data-campo="status"]'
                );

            const tipo =
                card.querySelector(
                    '[data-campo="tipo"]'
                );

            const horario =
                card.querySelector(
                    '[data-campo="horario"]'
                );

            const pessoaId =
                card.querySelector(
                    '[data-campo="pessoa-id"]'
                );

            const quantidade =
                card.querySelector(
                    '[data-campo="quantidade"]'
                );

            /*
             * O evento de saída não envia uma imagem.
             * A última captura permanece no cartão.
             */
            if (evento.imagem) {
                imagem.src =
                    `data:image/jpeg;base64,${evento.imagem}`;
            }

            if (evento.camera) {
                titulo.textContent =
                    evento.camera;
            }

            status.textContent =
                traduzirStatus(evento.status);

            status.className =
                `evento-status ${evento.status || ""}`;

            /*
             * O evento de saída não possui um novo tipo.
             * Mantemos o último tipo exibido.
             */
            if (evento.tipo) {
                tipo.textContent =
                    `Evento: ${evento.tipo}`;
            }

            if (evento.status === "exited") {
                horario.textContent =
                    `Saída: ${evento.datetime || "-"}`;
            } else {
                horario.textContent =
                    `Última captura: ${
                        evento.datetime || "-"
                    }`;
            }

            pessoaId.textContent =
                `Pessoa: ${
                    evento.pessoa_id || "-"
                }`;

            quantidade.textContent =
                `Detecções: ${
                    evento.quantidade_deteccoes || 1
                }`;

            card.classList.remove(
                "atualizado",
                "saiu"
            );

            if (evento.status === "updated") {
                card.classList.add(
                    "atualizado"
                );

                /*
                 * Move a pessoa atualizada para o início.
                 */
                eventosElemento.prepend(card);

                setTimeout(
                    function () {
                        card.classList.remove(
                            "atualizado"
                        );
                    },
                    1500
                );
            }

            if (evento.status === "exited") {
                card.classList.add("saiu");
            }
        }

        function limitarQuantidadeCartoes() {
            while (
                eventosElemento.children.length > 50
            ) {
                const ultimo =
                    eventosElemento.lastElementChild;

                if (!ultimo) {
                    break;
                }

                const pessoaId =
                    ultimo.dataset.pessoaId;

                if (pessoaId) {
                    const statusRemovido =
                        statusPorPessoa.get(pessoaId);

                    if (statusRemovido === "exited") {
                        pessoasSairam = Math.max(
                            0,
                            pessoasSairam - 1
                        );
                    } else if (statusRemovido) {
                        pessoasAtivas = Math.max(
                            0,
                            pessoasAtivas - 1
                        );
                    }

                    totalPessoas = Math.max(
                        0,
                        totalPessoas - 1
                    );

                    cartoesPorPessoa.delete(
                        pessoaId
                    );

                    statusPorPessoa.delete(
                        pessoaId
                    );
                }

                ultimo.remove();
            }

            atualizarResumo();
        }

        atualizarResumo();
        conectarWebSocket();
    </script>
</body>
</html>
------------------------------------------------------------
ARQUIVO: app/static/index_backup_antes_atualizacao.html
------------------------------------------------------------
<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">

    <meta
        name="viewport"
        content="width=device-width, initial-scale=1.0"
    >

    <title>Painel de Eventos</title>

    <style>
        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            font-family: Arial, sans-serif;
            background: #10131a;
            color: #f3f4f6;
        }

        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px 30px;
            background: #191e29;
            border-bottom: 1px solid #2d3443;
        }

        h1 {
            margin: 0;
            font-size: 24px;
        }

        .status {
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .status-bolinha {
            width: 12px;
            height: 12px;
            border-radius: 50%;
            background: #f59e0b;
        }

        .status-bolinha.conectado {
            background: #22c55e;
        }

        .status-bolinha.desconectado {
            background: #ef4444;
        }

        main {
            padding: 30px;
        }

        .resumo {
            display: flex;
            gap: 20px;
            margin-bottom: 25px;
        }

        .resumo-card {
            min-width: 180px;
            padding: 18px;
            background: #191e29;
            border: 1px solid #2d3443;
            border-radius: 10px;
        }

        .resumo-card span {
            display: block;
            color: #9ca3af;
            font-size: 14px;
        }

        .resumo-card strong {
            display: block;
            margin-top: 8px;
            font-size: 28px;
        }

        #eventos {
            display: grid;
            grid-template-columns: repeat(
                auto-fill,
                minmax(280px, 1fr)
            );
            gap: 20px;
        }

        .evento-card {
            overflow: hidden;
            background: #191e29;
            border: 1px solid #2d3443;
            border-radius: 10px;
        }

        .evento-card img {
            width: 100%;
            height: 230px;
            object-fit: contain;
            background: #05070a;
        }

        .evento-dados {
            padding: 15px;
        }

        .evento-dados h2 {
            margin: 0 0 12px;
            font-size: 18px;
        }

        .evento-dados p {
            margin: 6px 0;
            color: #cbd5e1;
            font-size: 14px;
        }

        .vazio {
            padding: 40px;
            color: #9ca3af;
            text-align: center;
            background: #191e29;
            border: 1px dashed #374151;
            border-radius: 10px;
        }
    </style>
</head>

<body>
    <header>
        <h1>Painel de Eventos das Câmeras</h1>

        <div class="status">
            <div
                id="status-bolinha"
                class="status-bolinha"
            ></div>

            <span id="status-texto">
                Conectando...
            </span>
        </div>
    </header>

    <main>
        <section class="resumo">
            <div class="resumo-card">
                <span>Eventos recebidos</span>
                <strong id="total-eventos">0</strong>
            </div>
        </section>

        <section id="eventos">
            <div id="mensagem-vazia" class="vazio">
                Aguardando eventos das câmeras...
            </div>
        </section>
    </main>

    <script>
        const statusBolinha =
            document.getElementById("status-bolinha");

        const statusTexto =
            document.getElementById("status-texto");

        const totalEventosElemento =
            document.getElementById("total-eventos");

        const eventosElemento =
            document.getElementById("eventos");

        let totalEventos = 0;

        function conectarWebSocket() {
            const protocolo =
                window.location.protocol === "https:"
                    ? "wss"
                    : "ws";

            const endereco =
                `${protocolo}://${window.location.host}/ws/eventos`;

            const websocket = new WebSocket(endereco);

            websocket.onopen = function () {
                statusTexto.textContent = "Conectado";

                statusBolinha.className =
                    "status-bolinha conectado";
            };

            websocket.onmessage = function (mensagem) {
                const evento = JSON.parse(mensagem.data);

                adicionarEvento(evento);
            };

            websocket.onclose = function () {
                statusTexto.textContent =
                    "Desconectado — reconectando...";

                statusBolinha.className =
                    "status-bolinha desconectado";

                setTimeout(conectarWebSocket, 2000);
            };

            websocket.onerror = function () {
                websocket.close();
            };

            setInterval(function () {
                if (
                    websocket.readyState ===
                    WebSocket.OPEN
                ) {
                    websocket.send("ping");
                }
            }, 20000);
        }

        function adicionarEvento(evento) {
            const mensagemVazia =
                document.getElementById("mensagem-vazia");

            if (mensagemVazia) {
                mensagemVazia.remove();
            }

            totalEventos += 1;
            totalEventosElemento.textContent = totalEventos;

            const card = document.createElement("article");
            card.className = "evento-card";

            const imagem = document.createElement("img");

            if (evento.imagem) {
                imagem.src =
                    `data:image/jpeg;base64,${evento.imagem}`;
            }

            imagem.alt = "Recorte da detecção";

            const dados = document.createElement("div");
            dados.className = "evento-dados";

            const titulo = document.createElement("h2");
            titulo.textContent =
                evento.camera || "Câmera não identificada";

            const tipo = document.createElement("p");
            tipo.textContent =
                `Evento: ${evento.tipo || "desconhecido"}`;

            const horario = document.createElement("p");
            horario.textContent =
                `Horário: ${evento.datetime || "-"}`;

            const identificador =
                document.createElement("p");

            identificador.textContent =
                `ID: ${evento.evento_id || "-"}`;

            dados.appendChild(titulo);
            dados.appendChild(tipo);
            dados.appendChild(horario);
            dados.appendChild(identificador);

            card.appendChild(imagem);
            card.appendChild(dados);

            eventosElemento.prepend(card);

            while (eventosElemento.children.length > 50) {
                eventosElemento.lastElementChild.remove();
            }
        }

        conectarWebSocket();
    </script>
</body>
</html>
------------------------------------------------------------
ARQUIVO: app/static/index_backup_antes_contadores.html
------------------------------------------------------------
<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">

    <meta
        name="viewport"
        content="width=device-width, initial-scale=1.0"
    >

    <title>Painel de Eventos</title>

    <style>
        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            font-family: Arial, sans-serif;
            background: #10131a;
            color: #f3f4f6;
        }

        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px 30px;
            background: #191e29;
            border-bottom: 1px solid #2d3443;
        }

        h1 {
            margin: 0;
            font-size: 24px;
        }

        .status-conexao {
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .status-bolinha {
            width: 12px;
            height: 12px;
            border-radius: 50%;
            background: #f59e0b;
        }

        .status-bolinha.conectado {
            background: #22c55e;
        }

        .status-bolinha.desconectado {
            background: #ef4444;
        }

        main {
            padding: 30px;
        }

        .resumo {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
            margin-bottom: 25px;
        }

        .resumo-card {
            min-width: 180px;
            padding: 18px;
            background: #191e29;
            border: 1px solid #2d3443;
            border-radius: 10px;
        }

        .resumo-card span {
            display: block;
            color: #9ca3af;
            font-size: 14px;
        }

        .resumo-card strong {
            display: block;
            margin-top: 8px;
            font-size: 28px;
        }

        #eventos {
            display: grid;
            grid-template-columns: repeat(
                auto-fill,
                minmax(280px, 1fr)
            );
            gap: 20px;
        }

        .evento-card {
            overflow: hidden;
            background: #191e29;
            border: 1px solid #2d3443;
            border-radius: 10px;
            transition:
                border-color 0.2s,
                transform 0.2s,
                opacity 0.2s;
        }

        .evento-card.atualizado {
            border-color: #3b82f6;
            transform: translateY(-2px);
        }

        .evento-card.saiu {
            opacity: 0.55;
            border-color: #6b7280;
        }

        .evento-card img {
            width: 100%;
            height: 230px;
            object-fit: contain;
            background: #05070a;
        }

        .evento-dados {
            padding: 15px;
        }

        .evento-cabecalho {
            display: flex;
            justify-content: space-between;
            align-items: center;
            gap: 10px;
            margin-bottom: 12px;
        }

        .evento-cabecalho h2 {
            margin: 0;
            font-size: 18px;
        }

        .evento-status {
            padding: 5px 9px;
            border-radius: 999px;
            background: #374151;
            font-size: 12px;
            font-weight: bold;
        }

        .evento-status.entered {
            background: #166534;
            color: #dcfce7;
        }

        .evento-status.updated {
            background: #1e40af;
            color: #dbeafe;
        }

        .evento-status.exited {
            background: #4b5563;
            color: #f3f4f6;
        }

        .evento-dados p {
            margin: 6px 0;
            color: #cbd5e1;
            font-size: 14px;
        }

        .vazio {
            padding: 40px;
            color: #9ca3af;
            text-align: center;
            background: #191e29;
            border: 1px dashed #374151;
            border-radius: 10px;
        }
    </style>
</head>

<body>
    <header>
        <h1>Painel de Eventos das Câmeras</h1>

        <div class="status-conexao">
            <div
                id="status-bolinha"
                class="status-bolinha"
            ></div>

            <span id="status-texto">
                Conectando...
            </span>
        </div>
    </header>

    <main>
        <section class="resumo">
            <div class="resumo-card">
                <span>Pessoas exibidas</span>
                <strong id="total-pessoas">0</strong>
            </div>

            <div class="resumo-card">
                <span>Eventos processados</span>
                <strong id="total-eventos">0</strong>
            </div>
        </section>

        <section id="eventos">
            <div id="mensagem-vazia" class="vazio">
                Aguardando eventos das câmeras...
            </div>
        </section>
    </main>

    <script>
        const statusBolinha =
            document.getElementById("status-bolinha");

        const statusTexto =
            document.getElementById("status-texto");

        const totalPessoasElemento =
            document.getElementById("total-pessoas");

        const totalEventosElemento =
            document.getElementById("total-eventos");

        const eventosElemento =
            document.getElementById("eventos");

        const cartoesPorPessoa = new Map();

        let totalPessoas = 0;
        let totalEventos = 0;
        let heartbeatId = null;

        function traduzirStatus(status) {
            const traducoes = {
                entered: "Entrou",
                updated: "Atualizado",
                suppressed: "Repetido",
                exited: "Saiu"
            };

            return (
                traducoes[status]
                || status
                || "Detectado"
            );
        }

        function conectarWebSocket() {
            const protocolo =
                window.location.protocol === "https:"
                    ? "wss"
                    : "ws";

            const endereco =
                `${protocolo}://${window.location.host}/ws/eventos`;

            const websocket =
                new WebSocket(endereco);

            websocket.onopen = function () {
                statusTexto.textContent =
                    "Conectado";

                statusBolinha.className =
                    "status-bolinha conectado";

                if (heartbeatId) {
                    clearInterval(heartbeatId);
                }

                heartbeatId = setInterval(
                    function () {
                        if (
                            websocket.readyState
                            === WebSocket.OPEN
                        ) {
                            websocket.send("ping");
                        }
                    },
                    20000
                );
            };

            websocket.onmessage = function (mensagem) {
                const evento =
                    JSON.parse(mensagem.data);

                processarEvento(evento);
            };

            websocket.onclose = function () {
                statusTexto.textContent =
                    "Desconectado — reconectando...";

                statusBolinha.className =
                    "status-bolinha desconectado";

                if (heartbeatId) {
                    clearInterval(heartbeatId);
                    heartbeatId = null;
                }

                setTimeout(
                    conectarWebSocket,
                    2000
                );
            };

            websocket.onerror = function () {
                websocket.close();
            };
        }

        function processarEvento(evento) {
            if (evento.status === "suppressed") {
                return;
            }

            const pessoaId =
                evento.pessoa_id
                || evento.evento_id;

            const cartaoExistente =
                cartoesPorPessoa.get(pessoaId);

            /*
             * Não cria um cartão vazio caso o painel tenha
             * conectado somente depois da saída da pessoa.
             */
            if (
                evento.status === "exited"
                && !cartaoExistente
            ) {
                return;
            }

            totalEventos += 1;

            totalEventosElemento.textContent =
                totalEventos;

            if (cartaoExistente) {
                atualizarCartao(
                    cartaoExistente,
                    evento
                );

                return;
            }

            criarCartao(
                pessoaId,
                evento
            );
        }

        function criarCartao(
            pessoaId,
            evento
        ) {
            const mensagemVazia =
                document.getElementById(
                    "mensagem-vazia"
                );

            if (mensagemVazia) {
                mensagemVazia.remove();
            }

            const card =
                document.createElement("article");

            card.className = "evento-card";
            card.dataset.pessoaId = pessoaId;

            const imagem =
                document.createElement("img");

            imagem.alt = "Recorte da pessoa";
            imagem.dataset.campo = "imagem";

            const dados =
                document.createElement("div");

            dados.className = "evento-dados";

            const cabecalho =
                document.createElement("div");

            cabecalho.className =
                "evento-cabecalho";

            const titulo =
                document.createElement("h2");

            titulo.dataset.campo = "camera";

            const status =
                document.createElement("span");

            status.dataset.campo = "status";
            status.className = "evento-status";

            const tipo =
                document.createElement("p");

            tipo.dataset.campo = "tipo";

            const horario =
                document.createElement("p");

            horario.dataset.campo = "horario";

            const identificadorPessoa =
                document.createElement("p");

            identificadorPessoa.dataset.campo =
                "pessoa-id";

            const quantidade =
                document.createElement("p");

            quantidade.dataset.campo =
                "quantidade";

            cabecalho.appendChild(titulo);
            cabecalho.appendChild(status);

            dados.appendChild(cabecalho);
            dados.appendChild(tipo);
            dados.appendChild(horario);
            dados.appendChild(
                identificadorPessoa
            );
            dados.appendChild(quantidade);

            card.appendChild(imagem);
            card.appendChild(dados);

            eventosElemento.prepend(card);

            cartoesPorPessoa.set(
                pessoaId,
                card
            );

            totalPessoas += 1;

            totalPessoasElemento.textContent =
                totalPessoas;

            atualizarCartao(
                card,
                evento
            );

            limitarQuantidadeCartoes();
        }

        function atualizarCartao(
            card,
            evento
        ) {
            const imagem =
                card.querySelector(
                    '[data-campo="imagem"]'
                );

            const titulo =
                card.querySelector(
                    '[data-campo="camera"]'
                );

            const status =
                card.querySelector(
                    '[data-campo="status"]'
                );

            const tipo =
                card.querySelector(
                    '[data-campo="tipo"]'
                );

            const horario =
                card.querySelector(
                    '[data-campo="horario"]'
                );

            const pessoaId =
                card.querySelector(
                    '[data-campo="pessoa-id"]'
                );

            const quantidade =
                card.querySelector(
                    '[data-campo="quantidade"]'
                );

            /*
             * No evento de saída não existe uma nova imagem.
             * Por isso, a última imagem continua no cartão.
             */
            if (evento.imagem) {
                imagem.src =
                    `data:image/jpeg;base64,${evento.imagem}`;
            }

            if (evento.camera) {
                titulo.textContent =
                    evento.camera;
            }

            status.textContent =
                traduzirStatus(evento.status);

            status.className =
                `evento-status ${evento.status || ""}`;

            /*
             * No evento de saída o tipo não é enviado.
             * O painel mantém o último tipo mostrado.
             */
            if (evento.tipo) {
                tipo.textContent =
                    `Evento: ${evento.tipo}`;
            }

            if (evento.status === "exited") {
                horario.textContent =
                    `Saída: ${evento.datetime || "-"}`;
            } else {
                horario.textContent =
                    `Última captura: ${
                        evento.datetime || "-"
                    }`;
            }

            pessoaId.textContent =
                `Pessoa: ${
                    evento.pessoa_id || "-"
                }`;

            quantidade.textContent =
                `Detecções: ${
                    evento.quantidade_deteccoes || 1
                }`;

            card.classList.remove(
                "atualizado",
                "saiu"
            );

            if (evento.status === "updated") {
                card.classList.add(
                    "atualizado"
                );

                eventosElemento.prepend(card);

                setTimeout(
                    function () {
                        card.classList.remove(
                            "atualizado"
                        );
                    },
                    1500
                );
            }

            if (evento.status === "exited") {
                card.classList.add("saiu");
            }
        }

        function limitarQuantidadeCartoes() {
            while (
                eventosElemento.children.length > 50
            ) {
                const ultimo =
                    eventosElemento.lastElementChild;

                const pessoaId =
                    ultimo.dataset.pessoaId;

                if (pessoaId) {
                    cartoesPorPessoa.delete(
                        pessoaId
                    );
                }

                ultimo.remove();

                totalPessoas = Math.max(
                    0,
                    totalPessoas - 1
                );

                totalPessoasElemento.textContent =
                    totalPessoas;
            }
        }

        conectarWebSocket();
    </script>
</body>
</html>
------------------------------------------------------------
ARQUIVO: app/static/index_backup_antes_contexto_cena.html
------------------------------------------------------------
<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">

    <meta
        name="viewport"
        content="width=device-width, initial-scale=1.0"
    >

    <title>Painel de Eventos</title>

    <style>
        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            font-family: Arial, sans-serif;
            background: #10131a;
            color: #f3f4f6;
        }

        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px 30px;
            background: #191e29;
            border-bottom: 1px solid #2d3443;
        }

        h1 {
            margin: 0;
            font-size: 24px;
        }

        .status-conexao {
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .status-bolinha {
            width: 12px;
            height: 12px;
            border-radius: 50%;
            background: #f59e0b;
        }

        .status-bolinha.conectado {
            background: #22c55e;
        }

        .status-bolinha.desconectado {
            background: #ef4444;
        }

        main {
            padding: 30px;
        }

        .resumo {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
            margin-bottom: 25px;
        }

        .resumo-card {
            min-width: 180px;
            padding: 18px;
            background: #191e29;
            border: 1px solid #2d3443;
            border-radius: 10px;
        }

        .resumo-card span {
            display: block;
            color: #9ca3af;
            font-size: 14px;
        }

        .resumo-card strong {
            display: block;
            margin-top: 8px;
            font-size: 28px;
        }

        #eventos {
            display: grid;
            grid-template-columns: repeat(
                auto-fill,
                minmax(310px, 1fr)
            );
            gap: 20px;
        }

        .evento-card {
            overflow: hidden;
            background: #191e29;
            border: 1px solid #2d3443;
            border-radius: 10px;
            transition:
                border-color 0.2s,
                transform 0.2s,
                opacity 0.2s;
        }

        .evento-card.atualizado {
            border-color: #3b82f6;
            transform: translateY(-2px);
        }

        .evento-card.saiu {
            opacity: 0.55;
            border-color: #6b7280;
        }

        .evento-card img {
            width: 100%;
            height: 230px;
            object-fit: contain;
            background: #05070a;
        }

        .evento-dados {
            padding: 15px;
        }

        .evento-cabecalho {
            display: flex;
            justify-content: space-between;
            align-items: center;
            gap: 10px;
            margin-bottom: 12px;
        }

        .evento-cabecalho h2 {
            margin: 0;
            font-size: 18px;
        }

        .evento-status {
            padding: 5px 9px;
            border-radius: 999px;
            background: #374151;
            font-size: 12px;
            font-weight: bold;
        }

        .evento-status.entered {
            background: #166534;
            color: #dcfce7;
        }

        .evento-status.updated {
            background: #1e40af;
            color: #dbeafe;
        }

        .evento-status.exited {
            background: #4b5563;
            color: #f3f4f6;
        }

        .evento-dados p {
            margin: 6px 0;
            color: #cbd5e1;
            font-size: 14px;
        }

        .aparencia-visual {
            display: none;
            margin-top: 14px;
            padding-top: 12px;
            border-top: 1px solid #2d3443;
        }

        .aparencia-titulo {
            margin: 0 0 10px;
            font-size: 14px;
            color: #93c5fd;
        }

        .descricao {
            line-height: 1.45;
        }

        .cor-amostra {
            display: inline-block;
            width: 12px;
            height: 12px;
            margin-right: 6px;
            border: 1px solid #64748b;
            border-radius: 50%;
            vertical-align: -1px;
        }

        .vazio {
            padding: 40px;
            color: #9ca3af;
            text-align: center;
            background: #191e29;
            border: 1px dashed #374151;
            border-radius: 10px;
        }

        @media (max-width: 700px) {
            header {
                align-items: flex-start;
                flex-direction: column;
                gap: 15px;
                padding: 20px;
            }

            main {
                padding: 20px;
            }

            .resumo-card {
                flex: 1;
                min-width: 140px;
            }
        }
    </style>
</head>

<body>
    <header>
        <h1>Painel de Eventos das Câmeras</h1>

        <div class="status-conexao">
            <div
                id="status-bolinha"
                class="status-bolinha"
            ></div>

            <span id="status-texto">
                Conectando...
            </span>
        </div>
    </header>

    <main>
        <section class="resumo">
            <div class="resumo-card">
                <span>Pessoas ativas</span>
                <strong id="pessoas-ativas">0</strong>
            </div>

            <div class="resumo-card">
                <span>Pessoas que saíram</span>
                <strong id="pessoas-sairam">0</strong>
            </div>

            <div class="resumo-card">
                <span>Total exibido</span>
                <strong id="total-pessoas">0</strong>
            </div>

            <div class="resumo-card">
                <span>Eventos processados</span>
                <strong id="total-eventos">0</strong>
            </div>
        </section>

        <section id="eventos">
            <div
                id="mensagem-vazia"
                class="vazio"
            >
                Aguardando eventos das câmeras...
            </div>
        </section>
    </main>

    <script>
        const statusBolinha =
            document.getElementById(
                "status-bolinha"
            );

        const statusTexto =
            document.getElementById(
                "status-texto"
            );

        const pessoasAtivasElemento =
            document.getElementById(
                "pessoas-ativas"
            );

        const pessoasSairamElemento =
            document.getElementById(
                "pessoas-sairam"
            );

        const totalPessoasElemento =
            document.getElementById(
                "total-pessoas"
            );

        const totalEventosElemento =
            document.getElementById(
                "total-eventos"
            );

        const eventosElemento =
            document.getElementById(
                "eventos"
            );

        const cartoesPorPessoa =
            new Map();

        const statusPorPessoa =
            new Map();

        let pessoasAtivas = 0;
        let pessoasSairam = 0;
        let totalPessoas = 0;
        let totalEventos = 0;
        let heartbeatId = null;

        function traduzirStatus(status) {
            const traducoes = {
                entered: "Entrou",
                updated: "Atualizado",
                suppressed: "Repetido",
                exited: "Saiu"
            };

            return (
                traducoes[status]
                || status
                || "Detectado"
            );
        }

        function atualizarResumo() {
            pessoasAtivasElemento.textContent =
                pessoasAtivas;

            pessoasSairamElemento.textContent =
                pessoasSairam;

            totalPessoasElemento.textContent =
                totalPessoas;

            totalEventosElemento.textContent =
                totalEventos;
        }

        function registrarMudancaStatus(
            pessoaId,
            novoStatus
        ) {
            const statusAnterior =
                statusPorPessoa.get(
                    pessoaId
                );

            if (!statusAnterior) {
                totalPessoas += 1;

                if (novoStatus === "exited") {
                    pessoasSairam += 1;
                } else {
                    pessoasAtivas += 1;
                }
            }

            else if (
                statusAnterior !== "exited"
                && novoStatus === "exited"
            ) {
                pessoasAtivas = Math.max(
                    0,
                    pessoasAtivas - 1
                );

                pessoasSairam += 1;
            }

            else if (
                statusAnterior === "exited"
                && novoStatus !== "exited"
            ) {
                pessoasSairam = Math.max(
                    0,
                    pessoasSairam - 1
                );

                pessoasAtivas += 1;
            }

            statusPorPessoa.set(
                pessoaId,
                novoStatus
            );

            atualizarResumo();
        }

        function conectarWebSocket() {
            const protocolo =
                window.location.protocol === "https:"
                    ? "wss"
                    : "ws";

            const endereco =
                `${protocolo}://${window.location.host}/ws/eventos`;

            const websocket =
                new WebSocket(endereco);

            websocket.onopen = function () {
                statusTexto.textContent =
                    "Conectado";

                statusBolinha.className =
                    "status-bolinha conectado";

                if (heartbeatId) {
                    clearInterval(
                        heartbeatId
                    );
                }

                heartbeatId = setInterval(
                    function () {
                        if (
                            websocket.readyState
                            === WebSocket.OPEN
                        ) {
                            websocket.send(
                                "ping"
                            );
                        }
                    },
                    20000
                );
            };

            websocket.onmessage =
                function (mensagem) {
                    try {
                        const evento =
                            JSON.parse(
                                mensagem.data
                            );

                        processarEvento(evento);

                    } catch (erro) {
                        console.error(
                            "Erro ao interpretar evento:",
                            erro
                        );
                    }
                };

            websocket.onclose = function () {
                statusTexto.textContent =
                    "Desconectado — reconectando...";

                statusBolinha.className =
                    "status-bolinha desconectado";

                if (heartbeatId) {
                    clearInterval(
                        heartbeatId
                    );

                    heartbeatId = null;
                }

                setTimeout(
                    conectarWebSocket,
                    2000
                );
            };

            websocket.onerror = function () {
                websocket.close();
            };
        }

        function processarEvento(evento) {
            /*
             * Atualização parcial gerada por uma
             * captura que foi suprimida.
             *
             * Não altera os contadores e não muda
             * o status Entrou, Atualizado ou Saiu.
             */
            if (
                evento.status
                === "appearance_updated"
            ) {
                atualizarSomenteAparencia(
                    evento
                );

                return;
            }

            if (
                evento.status
                === "suppressed"
            ) {
                return;
            }

            const pessoaId =
                evento.pessoa_id
                || evento.evento_id;

            if (!pessoaId) {
                console.warn(
                    "Evento sem identificador",
                    evento
                );

                return;
            }

            const cartaoExistente =
                cartoesPorPessoa.get(
                    pessoaId
                );

            if (
                evento.status === "exited"
                && !cartaoExistente
            ) {
                return;
            }

            registrarMudancaStatus(
                pessoaId,
                evento.status || "entered"
            );

            totalEventos += 1;
            atualizarResumo();

            if (cartaoExistente) {
                atualizarCartao(
                    cartaoExistente,
                    evento
                );

                return;
            }

            criarCartao(
                pessoaId,
                evento
            );
        }

        function atualizarSomenteAparencia(
            evento
        ) {
            const pessoaId =
                evento.pessoa_id
                || evento.evento_id;

            const card =
                cartoesPorPessoa.get(
                    pessoaId
                );

            /*
             * Não cria um cartão novo quando o painel
             * não recebeu o evento inicial da pessoa.
             */
            if (!card) {
                console.warn(
                    "Atualização recebida sem cartão:",
                    pessoaId
                );

                return;
            }

            const quantidade =
                card.querySelector(
                    '[data-campo="quantidade"]'
                );

            const aparenciaVisual =
                card.querySelector(
                    '[data-campo="aparencia"]'
                );

            const corRoupa =
                card.querySelector(
                    '[data-campo="cor-roupa"]'
                );

            const posicao =
                card.querySelector(
                    '[data-campo="posicao"]'
                );

            const tamanho =
                card.querySelector(
                    '[data-campo="tamanho"]'
                );

            const percentual =
                card.querySelector(
                    '[data-campo="percentual"]'
                );

            const quantidadeAmostras =
                card.querySelector(
                    '[data-campo="quantidade-amostras"]'
                );

            const descricao =
                card.querySelector(
                    '[data-campo="descricao"]'
                );

            quantidade.textContent =
                `Detecções: ${
                    evento.quantidade_deteccoes
                    || 1
                }`;

            if (evento.aparencia) {
                aparenciaVisual.style.display =
                    "block";

                preencherAparencia(
                    evento.aparencia,
                    corRoupa,
                    posicao,
                    tamanho,
                    percentual,
                    quantidadeAmostras,
                    descricao
                );
            }

            /*
             * Faz o cartão piscar sem alterar
             * o texto atual do status.
             */
            card.classList.add(
                "atualizado"
            );

            setTimeout(
                function () {
                    card.classList.remove(
                        "atualizado"
                    );
                },
                1000
            );
        }

        function criarCartao(
            pessoaId,
            evento
        ) {
            const mensagemVazia =
                document.getElementById(
                    "mensagem-vazia"
                );

            if (mensagemVazia) {
                mensagemVazia.remove();
            }

            const card =
                document.createElement(
                    "article"
                );

            card.className =
                "evento-card";

            card.dataset.pessoaId =
                pessoaId;

            const imagem =
                document.createElement(
                    "img"
                );

            imagem.alt =
                "Recorte da pessoa";

            imagem.dataset.campo =
                "imagem";

            const dados =
                document.createElement(
                    "div"
                );

            dados.className =
                "evento-dados";

            const cabecalho =
                document.createElement(
                    "div"
                );

            cabecalho.className =
                "evento-cabecalho";

            const titulo =
                document.createElement(
                    "h2"
                );

            titulo.dataset.campo =
                "camera";

            const status =
                document.createElement(
                    "span"
                );

            status.dataset.campo =
                "status";

            status.className =
                "evento-status";

            const tipo =
                document.createElement(
                    "p"
                );

            tipo.dataset.campo =
                "tipo";

            const horario =
                document.createElement(
                    "p"
                );

            horario.dataset.campo =
                "horario";

            const identificadorPessoa =
                document.createElement(
                    "p"
                );

            identificadorPessoa.dataset.campo =
                "pessoa-id";

            const quantidade =
                document.createElement(
                    "p"
                );

            quantidade.dataset.campo =
                "quantidade";

            const aparencia =
                document.createElement(
                    "section"
                );

            aparencia.className =
                "aparencia-visual";

            aparencia.dataset.campo =
                "aparencia";

            const aparenciaTitulo =
                document.createElement(
                    "h3"
                );

            aparenciaTitulo.className =
                "aparencia-titulo";

            aparenciaTitulo.textContent =
                "Aparência estável";

            const corRoupa =
                document.createElement(
                    "p"
                );

            corRoupa.dataset.campo =
                "cor-roupa";

            const posicao =
                document.createElement(
                    "p"
                );

            posicao.dataset.campo =
                "posicao";

            const tamanho =
                document.createElement(
                    "p"
                );

            tamanho.dataset.campo =
                "tamanho";

            const percentual =
                document.createElement(
                    "p"
                );

            percentual.dataset.campo =
                "percentual";

            const quantidadeAmostras =
                document.createElement(
                    "p"
                );

            quantidadeAmostras.dataset.campo =
                "quantidade-amostras";

            const descricao =
                document.createElement(
                    "p"
                );

            descricao.dataset.campo =
                "descricao";

            descricao.className =
                "descricao";

            cabecalho.appendChild(titulo);
            cabecalho.appendChild(status);

            dados.appendChild(cabecalho);
            dados.appendChild(tipo);
            dados.appendChild(horario);

            dados.appendChild(
                identificadorPessoa
            );

            dados.appendChild(
                quantidade
            );

            aparencia.appendChild(
                aparenciaTitulo
            );

            aparencia.appendChild(
                corRoupa
            );

            aparencia.appendChild(
                posicao
            );

            aparencia.appendChild(
                tamanho
            );

            aparencia.appendChild(
                percentual
            );

            aparencia.appendChild(
                quantidadeAmostras
            );

            aparencia.appendChild(
                descricao
            );

            dados.appendChild(
                aparencia
            );

            card.appendChild(imagem);
            card.appendChild(dados);

            eventosElemento.prepend(
                card
            );

            cartoesPorPessoa.set(
                pessoaId,
                card
            );

            atualizarCartao(
                card,
                evento
            );

            limitarQuantidadeCartoes();
        }

        function preencherAparencia(
            aparencia,
            corRoupa,
            posicao,
            tamanho,
            percentual,
            quantidadeAmostras,
            descricao
        ) {
            const rgb =
                aparencia.rgb_medio;

            let amostraCor = "";

            if (
                Array.isArray(rgb)
                && rgb.length === 3
            ) {
                amostraCor =
                    `<span
                        class="cor-amostra"
                        style="
                            background-color:
                            rgb(
                                ${rgb[0]},
                                ${rgb[1]},
                                ${rgb[2]}
                            );
                        "
                    ></span>`;
            }

            corRoupa.innerHTML =
                `${amostraCor}Cor predominante: ${
                    aparencia
                        .cor_roupa_predominante
                    || "-"
                }`;

            posicao.textContent =
                `Posição atual: ${
                    aparencia.posicao_atual
                    || "-"
                }`;

            tamanho.textContent =
                `Tamanho predominante: ${
                    aparencia
                        .tamanho_predominante
                    || "-"
                }`;

            percentual.textContent =
                `Área média ocupada: ${
                    aparencia
                        .percentual_medio_quadro
                    ?? "-"
                }%`;

            quantidadeAmostras.textContent =
                `Amostras analisadas: ${
                    aparencia
                        .quantidade_amostras
                    || 1
                }`;

            descricao.textContent =
                `Descrição estável: ${
                    aparencia.descricao
                    || "-"
                }`;
        }

        function atualizarCartao(
            card,
            evento
        ) {
            const imagem =
                card.querySelector(
                    '[data-campo="imagem"]'
                );

            const titulo =
                card.querySelector(
                    '[data-campo="camera"]'
                );

            const status =
                card.querySelector(
                    '[data-campo="status"]'
                );

            const tipo =
                card.querySelector(
                    '[data-campo="tipo"]'
                );

            const horario =
                card.querySelector(
                    '[data-campo="horario"]'
                );

            const pessoaId =
                card.querySelector(
                    '[data-campo="pessoa-id"]'
                );

            const quantidade =
                card.querySelector(
                    '[data-campo="quantidade"]'
                );

            const aparenciaVisual =
                card.querySelector(
                    '[data-campo="aparencia"]'
                );

            const corRoupa =
                card.querySelector(
                    '[data-campo="cor-roupa"]'
                );

            const posicao =
                card.querySelector(
                    '[data-campo="posicao"]'
                );

            const tamanho =
                card.querySelector(
                    '[data-campo="tamanho"]'
                );

            const percentual =
                card.querySelector(
                    '[data-campo="percentual"]'
                );

            const quantidadeAmostras =
                card.querySelector(
                    '[data-campo="quantidade-amostras"]'
                );

            const descricao =
                card.querySelector(
                    '[data-campo="descricao"]'
                );

            if (evento.imagem) {
                imagem.src =
                    `data:image/jpeg;base64,${evento.imagem}`;
            }

            if (evento.camera) {
                titulo.textContent =
                    evento.camera;
            }

            status.textContent =
                traduzirStatus(
                    evento.status
                );

            status.className =
                `evento-status ${evento.status || ""}`;

            if (evento.tipo) {
                tipo.textContent =
                    `Evento: ${evento.tipo}`;
            }

            if (
                evento.status === "exited"
            ) {
                horario.textContent =
                    `Saída: ${
                        evento.datetime || "-"
                    }`;
            } else {
                horario.textContent =
                    `Última captura: ${
                        evento.datetime || "-"
                    }`;
            }

            pessoaId.textContent =
                `Pessoa: ${
                    evento.pessoa_id || "-"
                }`;

            quantidade.textContent =
                `Detecções: ${
                    evento.quantidade_deteccoes
                    || 1
                }`;

            if (evento.aparencia) {
                aparenciaVisual.style.display =
                    "block";

                preencherAparencia(
                    evento.aparencia,
                    corRoupa,
                    posicao,
                    tamanho,
                    percentual,
                    quantidadeAmostras,
                    descricao
                );
            }

            card.classList.remove(
                "atualizado",
                "saiu"
            );

            if (
                evento.status === "updated"
            ) {
                card.classList.add(
                    "atualizado"
                );

                eventosElemento.prepend(
                    card
                );

                setTimeout(
                    function () {
                        card.classList.remove(
                            "atualizado"
                        );
                    },
                    1500
                );
            }

            if (
                evento.status === "exited"
            ) {
                card.classList.add(
                    "saiu"
                );
            }
        }

        function limitarQuantidadeCartoes() {
            while (
                eventosElemento.children.length
                > 50
            ) {
                const ultimo =
                    eventosElemento
                    .lastElementChild;

                if (!ultimo) {
                    break;
                }

                const pessoaId =
                    ultimo.dataset.pessoaId;

                if (pessoaId) {
                    const statusRemovido =
                        statusPorPessoa.get(
                            pessoaId
                        );

                    if (
                        statusRemovido
                        === "exited"
                    ) {
                        pessoasSairam =
                            Math.max(
                                0,
                                pessoasSairam - 1
                            );
                    }

                    else if (
                        statusRemovido
                    ) {
                        pessoasAtivas =
                            Math.max(
                                0,
                                pessoasAtivas - 1
                            );
                    }

                    totalPessoas =
                        Math.max(
                            0,
                            totalPessoas - 1
                        );

                    cartoesPorPessoa.delete(
                        pessoaId
                    );

                    statusPorPessoa.delete(
                        pessoaId
                    );
                }

                ultimo.remove();
            }

            atualizarResumo();
        }

        atualizarResumo();
        conectarWebSocket();
    </script>
</body>
</html>
------------------------------------------------------------
ARQUIVO: app/static/index_backup_antes_duas_imagens.html
------------------------------------------------------------
<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">

    <meta
        name="viewport"
        content="width=device-width, initial-scale=1.0"
    >

    <title>Painel de Eventos</title>

    <style>
        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            font-family: Arial, sans-serif;
            background: #10131a;
            color: #f3f4f6;
        }

        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px 30px;
            background: #191e29;
            border-bottom: 1px solid #2d3443;
        }

        h1 {
            margin: 0;
            font-size: 24px;
        }

        .status-conexao {
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .status-bolinha {
            width: 12px;
            height: 12px;
            border-radius: 50%;
            background: #f59e0b;
        }

        .status-bolinha.conectado {
            background: #22c55e;
        }

        .status-bolinha.desconectado {
            background: #ef4444;
        }

        main {
            padding: 30px;
        }

        .resumo {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
            margin-bottom: 25px;
        }

        .resumo-card {
            min-width: 180px;
            padding: 18px;
            background: #191e29;
            border: 1px solid #2d3443;
            border-radius: 10px;
        }

        .resumo-card span {
            display: block;
            color: #9ca3af;
            font-size: 14px;
        }

        .resumo-card strong {
            display: block;
            margin-top: 8px;
            font-size: 28px;
        }

        #eventos {
            display: grid;
            grid-template-columns: repeat(
                auto-fill,
                minmax(330px, 1fr)
            );
            gap: 20px;
        }

        .evento-card {
            overflow: hidden;
            background: #191e29;
            border: 1px solid #2d3443;
            border-radius: 10px;
            transition:
                border-color 0.2s,
                transform 0.2s,
                opacity 0.2s;
        }

        .evento-card.atualizado {
            border-color: #3b82f6;
            transform: translateY(-2px);
        }

        .evento-card.saiu {
            opacity: 0.55;
            border-color: #6b7280;
        }

        .evento-card img {
            width: 100%;
            height: 230px;
            object-fit: contain;
            background: #05070a;
        }

        .evento-dados {
            padding: 15px;
        }

        .evento-cabecalho {
            display: flex;
            justify-content: space-between;
            align-items: center;
            gap: 10px;
            margin-bottom: 12px;
        }

        .evento-cabecalho h2 {
            margin: 0;
            font-size: 18px;
        }

        .evento-status {
            padding: 5px 9px;
            border-radius: 999px;
            background: #374151;
            font-size: 12px;
            font-weight: bold;
        }

        .evento-status.entered {
            background: #166534;
            color: #dcfce7;
        }

        .evento-status.updated {
            background: #1e40af;
            color: #dbeafe;
        }

        .evento-status.exited {
            background: #4b5563;
            color: #f3f4f6;
        }

        .evento-dados p {
            margin: 6px 0;
            color: #cbd5e1;
            font-size: 14px;
        }

        .aparencia-visual,
        .contexto-cena {
            display: none;
            margin-top: 14px;
            padding-top: 12px;
            border-top: 1px solid #2d3443;
        }

        .secao-titulo {
            margin: 0 0 10px;
            font-size: 14px;
            color: #93c5fd;
        }

        .contexto-cena .secao-titulo {
            color: #86efac;
        }

        .descricao {
            line-height: 1.45;
        }

        .detalhes-pessoas {
            line-height: 1.5;
            white-space: pre-line;
        }

        .cor-amostra {
            display: inline-block;
            width: 12px;
            height: 12px;
            margin-right: 6px;
            border: 1px solid #64748b;
            border-radius: 50%;
            vertical-align: -1px;
        }

        .vazio {
            padding: 40px;
            color: #9ca3af;
            text-align: center;
            background: #191e29;
            border: 1px dashed #374151;
            border-radius: 10px;
        }

        @media (max-width: 700px) {
            header {
                align-items: flex-start;
                flex-direction: column;
                gap: 15px;
                padding: 20px;
            }

            main {
                padding: 20px;
            }

            .resumo-card {
                flex: 1;
                min-width: 140px;
            }
        }
    </style>
</head>

<body>
    <header>
        <h1>Painel de Eventos das Câmeras</h1>

        <div class="status-conexao">
            <div
                id="status-bolinha"
                class="status-bolinha"
            ></div>

            <span id="status-texto">
                Conectando...
            </span>
        </div>
    </header>

    <main>
        <section class="resumo">
            <div class="resumo-card">
                <span>Pessoas ativas</span>
                <strong id="pessoas-ativas">0</strong>
            </div>

            <div class="resumo-card">
                <span>Pessoas que saíram</span>
                <strong id="pessoas-sairam">0</strong>
            </div>

            <div class="resumo-card">
                <span>Total exibido</span>
                <strong id="total-pessoas">0</strong>
            </div>

            <div class="resumo-card">
                <span>Eventos processados</span>
                <strong id="total-eventos">0</strong>
            </div>
        </section>

        <section id="eventos">
            <div
                id="mensagem-vazia"
                class="vazio"
            >
                Aguardando eventos das câmeras...
            </div>
        </section>
    </main>

    <script>
        const statusBolinha =
            document.getElementById(
                "status-bolinha"
            );

        const statusTexto =
            document.getElementById(
                "status-texto"
            );

        const pessoasAtivasElemento =
            document.getElementById(
                "pessoas-ativas"
            );

        const pessoasSairamElemento =
            document.getElementById(
                "pessoas-sairam"
            );

        const totalPessoasElemento =
            document.getElementById(
                "total-pessoas"
            );

        const totalEventosElemento =
            document.getElementById(
                "total-eventos"
            );

        const eventosElemento =
            document.getElementById(
                "eventos"
            );

        const cartoesPorPessoa = new Map();
        const statusPorPessoa = new Map();

        let pessoasAtivas = 0;
        let pessoasSairam = 0;
        let totalPessoas = 0;
        let totalEventos = 0;
        let heartbeatId = null;

        function traduzirStatus(status) {
            const traducoes = {
                entered: "Entrou",
                updated: "Atualizado",
                suppressed: "Repetido",
                exited: "Saiu"
            };

            return (
                traducoes[status]
                || status
                || "Detectado"
            );
        }

        function atualizarResumo() {
            pessoasAtivasElemento.textContent =
                pessoasAtivas;

            pessoasSairamElemento.textContent =
                pessoasSairam;

            totalPessoasElemento.textContent =
                totalPessoas;

            totalEventosElemento.textContent =
                totalEventos;
        }

        function registrarMudancaStatus(
            pessoaId,
            novoStatus
        ) {
            const statusAnterior =
                statusPorPessoa.get(
                    pessoaId
                );

            if (!statusAnterior) {
                totalPessoas += 1;

                if (novoStatus === "exited") {
                    pessoasSairam += 1;
                } else {
                    pessoasAtivas += 1;
                }
            }

            else if (
                statusAnterior !== "exited"
                && novoStatus === "exited"
            ) {
                pessoasAtivas = Math.max(
                    0,
                    pessoasAtivas - 1
                );

                pessoasSairam += 1;
            }

            else if (
                statusAnterior === "exited"
                && novoStatus !== "exited"
            ) {
                pessoasSairam = Math.max(
                    0,
                    pessoasSairam - 1
                );

                pessoasAtivas += 1;
            }

            statusPorPessoa.set(
                pessoaId,
                novoStatus
            );

            atualizarResumo();
        }

        function conectarWebSocket() {
            const protocolo =
                window.location.protocol === "https:"
                    ? "wss"
                    : "ws";

            const endereco =
                `${protocolo}://${window.location.host}/ws/eventos`;

            const websocket =
                new WebSocket(endereco);

            websocket.onopen = function () {
                statusTexto.textContent =
                    "Conectado";

                statusBolinha.className =
                    "status-bolinha conectado";

                if (heartbeatId) {
                    clearInterval(
                        heartbeatId
                    );
                }

                heartbeatId = setInterval(
                    function () {
                        if (
                            websocket.readyState
                            === WebSocket.OPEN
                        ) {
                            websocket.send(
                                "ping"
                            );
                        }
                    },
                    20000
                );
            };

            websocket.onmessage =
                function (mensagem) {
                    try {
                        const evento =
                            JSON.parse(
                                mensagem.data
                            );

                        processarEvento(
                            evento
                        );

                    } catch (erro) {
                        console.error(
                            "Erro ao interpretar evento:",
                            erro
                        );
                    }
                };

            websocket.onclose = function () {
                statusTexto.textContent =
                    "Desconectado — reconectando...";

                statusBolinha.className =
                    "status-bolinha desconectado";

                if (heartbeatId) {
                    clearInterval(
                        heartbeatId
                    );

                    heartbeatId = null;
                }

                setTimeout(
                    conectarWebSocket,
                    2000
                );
            };

            websocket.onerror = function () {
                websocket.close();
            };
        }

        function processarEvento(evento) {
            /*
             * Atualização parcial gerada por uma
             * captura repetida e suprimida.
             *
             * Não aumenta os contadores e não muda
             * o status do cartão.
             */
            if (
                evento.status
                === "appearance_updated"
            ) {
                atualizarSomenteAparenciaEContexto(
                    evento
                );

                return;
            }

            if (
                evento.status
                === "suppressed"
            ) {
                return;
            }

            const pessoaId =
                evento.pessoa_id
                || evento.evento_id;

            if (!pessoaId) {
                console.warn(
                    "Evento sem identificador:",
                    evento
                );

                return;
            }

            const cartaoExistente =
                cartoesPorPessoa.get(
                    pessoaId
                );

            if (
                evento.status === "exited"
                && !cartaoExistente
            ) {
                return;
            }

            registrarMudancaStatus(
                pessoaId,
                evento.status || "entered"
            );

            totalEventos += 1;
            atualizarResumo();

            if (cartaoExistente) {
                atualizarCartao(
                    cartaoExistente,
                    evento
                );

                return;
            }

            criarCartao(
                pessoaId,
                evento
            );
        }

        function obterCamposCard(card) {
            return {
                imagem: card.querySelector(
                    '[data-campo="imagem"]'
                ),

                camera: card.querySelector(
                    '[data-campo="camera"]'
                ),

                status: card.querySelector(
                    '[data-campo="status"]'
                ),

                tipo: card.querySelector(
                    '[data-campo="tipo"]'
                ),

                horario: card.querySelector(
                    '[data-campo="horario"]'
                ),

                pessoaId: card.querySelector(
                    '[data-campo="pessoa-id"]'
                ),

                quantidade: card.querySelector(
                    '[data-campo="quantidade"]'
                ),

                aparencia: card.querySelector(
                    '[data-campo="aparencia"]'
                ),

                corRoupa: card.querySelector(
                    '[data-campo="cor-roupa"]'
                ),

                posicao: card.querySelector(
                    '[data-campo="posicao"]'
                ),

                tamanho: card.querySelector(
                    '[data-campo="tamanho"]'
                ),

                percentual: card.querySelector(
                    '[data-campo="percentual"]'
                ),

                quantidadeAmostras: card.querySelector(
                    '[data-campo="quantidade-amostras"]'
                ),

                descricaoAparencia: card.querySelector(
                    '[data-campo="descricao-aparencia"]'
                ),

                contexto: card.querySelector(
                    '[data-campo="contexto"]'
                ),

                contextoQuantidade: card.querySelector(
                    '[data-campo="contexto-quantidade"]'
                ),

                contextoDistribuicao: card.querySelector(
                    '[data-campo="contexto-distribuicao"]'
                ),

                contextoProximidades: card.querySelector(
                    '[data-campo="contexto-proximidades"]'
                ),

                contextoPessoas: card.querySelector(
                    '[data-campo="contexto-pessoas"]'
                ),

                contextoDescricao: card.querySelector(
                    '[data-campo="contexto-descricao"]'
                )
            };
        }

        function atualizarSomenteAparenciaEContexto(
            evento
        ) {
            const pessoaId =
                evento.pessoa_id
                || evento.evento_id;

            const card =
                cartoesPorPessoa.get(
                    pessoaId
                );

            if (!card) {
                console.warn(
                    "Atualização recebida sem cartão:",
                    pessoaId
                );

                return;
            }

            const campos =
                obterCamposCard(card);

            campos.quantidade.textContent =
                `Detecções: ${
                    evento.quantidade_deteccoes
                    || 1
                }`;

            if (evento.aparencia) {
                preencherAparencia(
                    evento.aparencia,
                    campos
                );
            }

            if (evento.contexto_cena) {
                preencherContextoCena(
                    evento.contexto_cena,
                    campos
                );
            }

            card.classList.add(
                "atualizado"
            );

            setTimeout(
                function () {
                    card.classList.remove(
                        "atualizado"
                    );
                },
                1000
            );
        }

        function criarElemento(
            tag,
            campo,
            classe = ""
        ) {
            const elemento =
                document.createElement(tag);

            if (campo) {
                elemento.dataset.campo =
                    campo;
            }

            if (classe) {
                elemento.className =
                    classe;
            }

            return elemento;
        }

        function criarCartao(
            pessoaId,
            evento
        ) {
            const mensagemVazia =
                document.getElementById(
                    "mensagem-vazia"
                );

            if (mensagemVazia) {
                mensagemVazia.remove();
            }

            const card =
                criarElemento(
                    "article",
                    "",
                    "evento-card"
                );

            card.dataset.pessoaId =
                pessoaId;

            const imagem =
                criarElemento(
                    "img",
                    "imagem"
                );

            imagem.alt =
                "Recorte da pessoa";

            const dados =
                criarElemento(
                    "div",
                    "",
                    "evento-dados"
                );

            const cabecalho =
                criarElemento(
                    "div",
                    "",
                    "evento-cabecalho"
                );

            const titulo =
                criarElemento(
                    "h2",
                    "camera"
                );

            const status =
                criarElemento(
                    "span",
                    "status",
                    "evento-status"
                );

            cabecalho.appendChild(
                titulo
            );

            cabecalho.appendChild(
                status
            );

            const tipo =
                criarElemento(
                    "p",
                    "tipo"
                );

            const horario =
                criarElemento(
                    "p",
                    "horario"
                );

            const identificadorPessoa =
                criarElemento(
                    "p",
                    "pessoa-id"
                );

            const quantidade =
                criarElemento(
                    "p",
                    "quantidade"
                );

            const aparencia =
                criarElemento(
                    "section",
                    "aparencia",
                    "aparencia-visual"
                );

            const aparenciaTitulo =
                criarElemento(
                    "h3",
                    "",
                    "secao-titulo"
                );

            aparenciaTitulo.textContent =
                "Aparência estável";

            const corRoupa =
                criarElemento(
                    "p",
                    "cor-roupa"
                );

            const posicao =
                criarElemento(
                    "p",
                    "posicao"
                );

            const tamanho =
                criarElemento(
                    "p",
                    "tamanho"
                );

            const percentual =
                criarElemento(
                    "p",
                    "percentual"
                );

            const quantidadeAmostras =
                criarElemento(
                    "p",
                    "quantidade-amostras"
                );

            const descricaoAparencia =
                criarElemento(
                    "p",
                    "descricao-aparencia",
                    "descricao"
                );

            aparencia.appendChild(
                aparenciaTitulo
            );

            aparencia.appendChild(
                corRoupa
            );

            aparencia.appendChild(
                posicao
            );

            aparencia.appendChild(
                tamanho
            );

            aparencia.appendChild(
                percentual
            );

            aparencia.appendChild(
                quantidadeAmostras
            );

            aparencia.appendChild(
                descricaoAparencia
            );

            const contexto =
                criarElemento(
                    "section",
                    "contexto",
                    "contexto-cena"
                );

            const contextoTitulo =
                criarElemento(
                    "h3",
                    "",
                    "secao-titulo"
                );

            contextoTitulo.textContent =
                "Contexto da cena";

            const contextoQuantidade =
                criarElemento(
                    "p",
                    "contexto-quantidade"
                );

            const contextoDistribuicao =
                criarElemento(
                    "p",
                    "contexto-distribuicao"
                );

            const contextoProximidades =
                criarElemento(
                    "p",
                    "contexto-proximidades"
                );

            const contextoPessoas =
                criarElemento(
                    "p",
                    "contexto-pessoas",
                    "detalhes-pessoas"
                );

            const contextoDescricao =
                criarElemento(
                    "p",
                    "contexto-descricao",
                    "descricao"
                );

            contexto.appendChild(
                contextoTitulo
            );

            contexto.appendChild(
                contextoQuantidade
            );

            contexto.appendChild(
                contextoDistribuicao
            );

            contexto.appendChild(
                contextoProximidades
            );

            contexto.appendChild(
                contextoPessoas
            );

            contexto.appendChild(
                contextoDescricao
            );

            dados.appendChild(
                cabecalho
            );

            dados.appendChild(
                tipo
            );

            dados.appendChild(
                horario
            );

            dados.appendChild(
                identificadorPessoa
            );

            dados.appendChild(
                quantidade
            );

            dados.appendChild(
                aparencia
            );

            dados.appendChild(
                contexto
            );

            card.appendChild(
                imagem
            );

            card.appendChild(
                dados
            );

            eventosElemento.prepend(
                card
            );

            cartoesPorPessoa.set(
                pessoaId,
                card
            );

            atualizarCartao(
                card,
                evento
            );

            limitarQuantidadeCartoes();
        }

        function preencherAparencia(
            aparencia,
            campos
        ) {
            campos.aparencia.style.display =
                "block";

            const rgb =
                aparencia.rgb_medio;

            let amostraCor = "";

            if (
                Array.isArray(rgb)
                && rgb.length === 3
            ) {
                amostraCor =
                    `<span
                        class="cor-amostra"
                        style="
                            background-color:
                            rgb(
                                ${rgb[0]},
                                ${rgb[1]},
                                ${rgb[2]}
                            );
                        "
                    ></span>`;
            }

            campos.corRoupa.innerHTML =
                `${amostraCor}Cor predominante: ${
                    aparencia
                        .cor_roupa_predominante
                    || "-"
                }`;

            campos.posicao.textContent =
                `Posição atual: ${
                    aparencia.posicao_atual
                    || "-"
                }`;

            campos.tamanho.textContent =
                `Tamanho predominante: ${
                    aparencia
                        .tamanho_predominante
                    || "-"
                }`;

            campos.percentual.textContent =
                `Área média ocupada: ${
                    aparencia
                        .percentual_medio_quadro
                    ?? "-"
                }%`;

            campos.quantidadeAmostras.textContent =
                `Amostras analisadas: ${
                    aparencia
                        .quantidade_amostras
                    || 1
                }`;

            campos.descricaoAparencia.textContent =
                `Descrição estável: ${
                    aparencia.descricao
                    || "-"
                }`;
        }

        function montarDistribuicao(
            contexto
        ) {
            const partes = [];

            if (contexto.pessoas_esquerda) {
                partes.push(
                    `${contexto.pessoas_esquerda} à esquerda`
                );
            }

            if (contexto.pessoas_centro) {
                partes.push(
                    `${contexto.pessoas_centro} no centro`
                );
            }

            if (contexto.pessoas_direita) {
                partes.push(
                    `${contexto.pessoas_direita} à direita`
                );
            }

            if (partes.length === 0) {
                return "sem distribuição";
            }

            return partes.join(", ");
        }

        function preencherContextoCena(
            contexto,
            campos
        ) {
            campos.contexto.style.display =
                "block";

            campos.contextoQuantidade.textContent =
                `Pessoas identificadas: ${
                    contexto.quantidade_pessoas
                    ?? 0
                }`;

            campos.contextoDistribuicao.textContent =
                `Distribuição: ${
                    montarDistribuicao(
                        contexto
                    )
                }`;

            campos.contextoProximidades.textContent =
                `Pares muito próximos: ${
                    contexto.pares_muito_proximos
                    ?? 0
                } | Pares próximos: ${
                    contexto.pares_proximos
                    ?? 0
                } | Pares separados: ${
                    contexto.pares_separados
                    ?? 0
                }`;

            const pessoas =
                Array.isArray(contexto.pessoas)
                    ? contexto.pessoas
                    : [];

            if (pessoas.length > 0) {
                const detalhes =
                    pessoas
                    .slice(0, 8)
                    .map(
                        function (pessoa) {
                            return (
                                `Pessoa ${pessoa.indice}: `
                                + `${pessoa.posicao_horizontal || "-"}, `
                                + `${pessoa.posicao_vertical || "-"}, `
                                + `tamanho ${pessoa.tamanho_no_quadro || "-"}, `
                                + `${pessoa.percentual_quadro ?? "-"}% do quadro`
                            );
                        }
                    );

                if (pessoas.length > 8) {
                    detalhes.push(
                        `E mais ${
                            pessoas.length - 8
                        } pessoa(s).`
                    );
                }

                campos.contextoPessoas.textContent =
                    "Detalhes:\n"
                    + detalhes.join("\n");

            } else {
                campos.contextoPessoas.textContent =
                    "Detalhes: nenhuma pessoa válida.";
            }

            campos.contextoDescricao.textContent =
                `Descrição da cena: ${
                    contexto.descricao
                    || "-"
                }`;
        }

        function atualizarCartao(
            card,
            evento
        ) {
            const campos =
                obterCamposCard(card);

            if (evento.imagem) {
                campos.imagem.src =
                    `data:image/jpeg;base64,${evento.imagem}`;
            }

            if (evento.camera) {
                campos.camera.textContent =
                    evento.camera;
            }

            campos.status.textContent =
                traduzirStatus(
                    evento.status
                );

            campos.status.className =
                `evento-status ${evento.status || ""}`;

            if (evento.tipo) {
                campos.tipo.textContent =
                    `Evento: ${evento.tipo}`;
            }

            if (
                evento.status === "exited"
            ) {
                campos.horario.textContent =
                    `Saída: ${
                        evento.datetime || "-"
                    }`;

            } else {
                campos.horario.textContent =
                    `Última captura: ${
                        evento.datetime || "-"
                    }`;
            }

            campos.pessoaId.textContent =
                `Pessoa: ${
                    evento.pessoa_id || "-"
                }`;

            campos.quantidade.textContent =
                `Detecções: ${
                    evento.quantidade_deteccoes
                    || 1
                }`;

            if (evento.aparencia) {
                preencherAparencia(
                    evento.aparencia,
                    campos
                );
            }

            /*
             * No evento de saída, contexto_cena é
             * nulo. Dessa forma, o último contexto
             * exibido permanece no cartão.
             */
            if (evento.contexto_cena) {
                preencherContextoCena(
                    evento.contexto_cena,
                    campos
                );
            }

            card.classList.remove(
                "atualizado",
                "saiu"
            );

            if (
                evento.status === "updated"
            ) {
                card.classList.add(
                    "atualizado"
                );

                eventosElemento.prepend(
                    card
                );

                setTimeout(
                    function () {
                        card.classList.remove(
                            "atualizado"
                        );
                    },
                    1500
                );
            }

            if (
                evento.status === "exited"
            ) {
                card.classList.add(
                    "saiu"
                );
            }
        }

        function limitarQuantidadeCartoes() {
            while (
                eventosElemento.children.length
                > 50
            ) {
                const ultimo =
                    eventosElemento
                    .lastElementChild;

                if (!ultimo) {
                    break;
                }

                const pessoaId =
                    ultimo.dataset.pessoaId;

                if (pessoaId) {
                    const statusRemovido =
                        statusPorPessoa.get(
                            pessoaId
                        );

                    if (
                        statusRemovido
                        === "exited"
                    ) {
                        pessoasSairam =
                            Math.max(
                                0,
                                pessoasSairam - 1
                            );
                    }

                    else if (statusRemovido) {
                        pessoasAtivas =
                            Math.max(
                                0,
                                pessoasAtivas - 1
                            );
                    }

                    totalPessoas =
                        Math.max(
                            0,
                            totalPessoas - 1
                        );

                    cartoesPorPessoa.delete(
                        pessoaId
                    );

                    statusPorPessoa.delete(
                        pessoaId
                    );
                }

                ultimo.remove();
            }

            atualizarResumo();
        }

        atualizarResumo();
        conectarWebSocket();
    </script>
</body>
</html>
------------------------------------------------------------
ARQUIVO: app/static/index_backup_antes_movimento.html
------------------------------------------------------------
<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">

    <meta
        name="viewport"
        content="width=device-width, initial-scale=1.0"
    >

    <title>Painel de Eventos</title>

    <style>
        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            font-family: Arial, sans-serif;
            background: #10131a;
            color: #f3f4f6;
        }

        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px 30px;
            background: #191e29;
            border-bottom: 1px solid #2d3443;
        }

        h1 {
            margin: 0;
            font-size: 24px;
        }

        .status-conexao {
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .status-bolinha {
            width: 12px;
            height: 12px;
            border-radius: 50%;
            background: #f59e0b;
        }

        .status-bolinha.conectado {
            background: #22c55e;
        }

        .status-bolinha.desconectado {
            background: #ef4444;
        }

        main {
            padding: 30px;
        }

        .resumo {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
            margin-bottom: 25px;
        }

        .resumo-card {
            min-width: 180px;
            padding: 18px;
            background: #191e29;
            border: 1px solid #2d3443;
            border-radius: 10px;
        }

        .resumo-card span {
            display: block;
            color: #9ca3af;
            font-size: 14px;
        }

        .resumo-card strong {
            display: block;
            margin-top: 8px;
            font-size: 28px;
        }

        #eventos {
            display: grid;
            grid-template-columns: repeat(
                auto-fill,
                minmax(400px, 1fr)
            );
            gap: 20px;
        }

        .evento-card {
            overflow: hidden;
            background: #191e29;
            border: 1px solid #2d3443;
            border-radius: 10px;
            transition:
                border-color 0.2s,
                transform 0.2s,
                opacity 0.2s;
        }

        .evento-card.atualizado {
            border-color: #3b82f6;
            transform: translateY(-2px);
        }

        .evento-card.saiu {
            opacity: 0.55;
            border-color: #6b7280;
        }

        .imagens-evento {
            display: flex;
            flex-direction: column;
            background: #05070a;
        }

        .imagem-bloco {
            display: none;
            border-bottom: 1px solid #2d3443;
        }

        .imagem-bloco:last-child {
            border-bottom: none;
        }

        .imagem-titulo {
            display: flex;
            justify-content: space-between;
            align-items: center;
            min-height: 38px;
            padding: 9px 12px;
            background: #111827;
            color: #cbd5e1;
            font-size: 13px;
            font-weight: bold;
        }

        .imagem-contagem {
            color: #86efac;
            font-size: 12px;
        }

        .imagem-evento {
            display: block;
            width: 100%;
            background: #05070a;
        }

        .imagem-recorte {
            height: 260px;
            object-fit: contain;
        }

        .imagem-cena {
            aspect-ratio: 16 / 9;
            max-height: 440px;
            object-fit: contain;
        }

        .evento-dados {
            padding: 15px;
        }

        .evento-cabecalho {
            display: flex;
            justify-content: space-between;
            align-items: center;
            gap: 10px;
            margin-bottom: 12px;
        }

        .evento-cabecalho h2 {
            margin: 0;
            font-size: 18px;
        }

        .evento-status {
            padding: 5px 9px;
            border-radius: 999px;
            background: #374151;
            font-size: 12px;
            font-weight: bold;
        }

        .evento-status.entered {
            background: #166534;
            color: #dcfce7;
        }

        .evento-status.updated {
            background: #1e40af;
            color: #dbeafe;
        }

        .evento-status.exited {
            background: #4b5563;
            color: #f3f4f6;
        }

        .evento-dados p {
            margin: 6px 0;
            color: #cbd5e1;
            font-size: 14px;
        }

        .aparencia-visual,
        .contexto-cena {
            display: none;
            margin-top: 14px;
            padding-top: 12px;
            border-top: 1px solid #2d3443;
        }

        .secao-titulo {
            margin: 0 0 10px;
            font-size: 14px;
            color: #93c5fd;
        }

        .contexto-cena .secao-titulo {
            color: #86efac;
        }

        .descricao {
            line-height: 1.45;
        }

        .detalhes-pessoas {
            line-height: 1.5;
            white-space: pre-line;
        }

        .cor-amostra {
            display: inline-block;
            width: 12px;
            height: 12px;
            margin-right: 6px;
            border: 1px solid #64748b;
            border-radius: 50%;
            vertical-align: -1px;
        }

        .vazio {
            padding: 40px;
            color: #9ca3af;
            text-align: center;
            background: #191e29;
            border: 1px dashed #374151;
            border-radius: 10px;
        }

        @media (max-width: 700px) {
            header {
                align-items: flex-start;
                flex-direction: column;
                gap: 15px;
                padding: 20px;
            }

            main {
                padding: 20px;
            }

            #eventos {
                grid-template-columns: 1fr;
            }

            .resumo-card {
                flex: 1;
                min-width: 140px;
            }

            .imagem-recorte {
                height: 230px;
            }
        }
    </style>
</head>

<body>
    <header>
        <h1>Painel de Eventos das Câmeras</h1>

        <div class="status-conexao">
            <div
                id="status-bolinha"
                class="status-bolinha"
            ></div>

            <span id="status-texto">
                Conectando...
            </span>
        </div>
    </header>

    <main>
        <section class="resumo">
            <div class="resumo-card">
                <span>Pessoas ativas</span>
                <strong id="pessoas-ativas">0</strong>
            </div>

            <div class="resumo-card">
                <span>Pessoas que saíram</span>
                <strong id="pessoas-sairam">0</strong>
            </div>

            <div class="resumo-card">
                <span>Total exibido</span>
                <strong id="total-pessoas">0</strong>
            </div>

            <div class="resumo-card">
                <span>Eventos processados</span>
                <strong id="total-eventos">0</strong>
            </div>
        </section>

        <section id="eventos">
            <div
                id="mensagem-vazia"
                class="vazio"
            >
                Aguardando eventos das câmeras...
            </div>
        </section>
    </main>

    <script>
        const statusBolinha =
            document.getElementById(
                "status-bolinha"
            );

        const statusTexto =
            document.getElementById(
                "status-texto"
            );

        const pessoasAtivasElemento =
            document.getElementById(
                "pessoas-ativas"
            );

        const pessoasSairamElemento =
            document.getElementById(
                "pessoas-sairam"
            );

        const totalPessoasElemento =
            document.getElementById(
                "total-pessoas"
            );

        const totalEventosElemento =
            document.getElementById(
                "total-eventos"
            );

        const eventosElemento =
            document.getElementById(
                "eventos"
            );

        const cartoesPorPessoa =
            new Map();

        const statusPorPessoa =
            new Map();

        let pessoasAtivas = 0;
        let pessoasSairam = 0;
        let totalPessoas = 0;
        let totalEventos = 0;
        let heartbeatId = null;

        function traduzirStatus(status) {
            const traducoes = {
                entered: "Entrou",
                updated: "Atualizado",
                suppressed: "Repetido",
                exited: "Saiu"
            };

            return (
                traducoes[status]
                || status
                || "Detectado"
            );
        }

        function atualizarResumo() {
            pessoasAtivasElemento.textContent =
                pessoasAtivas;

            pessoasSairamElemento.textContent =
                pessoasSairam;

            totalPessoasElemento.textContent =
                totalPessoas;

            totalEventosElemento.textContent =
                totalEventos;
        }

        function registrarMudancaStatus(
            pessoaId,
            novoStatus
        ) {
            const statusAnterior =
                statusPorPessoa.get(
                    pessoaId
                );

            if (!statusAnterior) {
                totalPessoas += 1;

                if (novoStatus === "exited") {
                    pessoasSairam += 1;
                } else {
                    pessoasAtivas += 1;
                }
            }

            else if (
                statusAnterior !== "exited"
                && novoStatus === "exited"
            ) {
                pessoasAtivas = Math.max(
                    0,
                    pessoasAtivas - 1
                );

                pessoasSairam += 1;
            }

            else if (
                statusAnterior === "exited"
                && novoStatus !== "exited"
            ) {
                pessoasSairam = Math.max(
                    0,
                    pessoasSairam - 1
                );

                pessoasAtivas += 1;
            }

            statusPorPessoa.set(
                pessoaId,
                novoStatus
            );

            atualizarResumo();
        }

        function conectarWebSocket() {
            const protocolo =
                window.location.protocol === "https:"
                    ? "wss"
                    : "ws";

            const endereco =
                `${protocolo}://${window.location.host}/ws/eventos`;

            const websocket =
                new WebSocket(endereco);

            websocket.onopen = function () {
                statusTexto.textContent =
                    "Conectado";

                statusBolinha.className =
                    "status-bolinha conectado";

                if (heartbeatId) {
                    clearInterval(
                        heartbeatId
                    );
                }

                heartbeatId = setInterval(
                    function () {
                        if (
                            websocket.readyState
                            === WebSocket.OPEN
                        ) {
                            websocket.send(
                                "ping"
                            );
                        }
                    },
                    20000
                );
            };

            websocket.onmessage =
                function (mensagem) {
                    try {
                        const evento =
                            JSON.parse(
                                mensagem.data
                            );

                        processarEvento(
                            evento
                        );

                    } catch (erro) {
                        console.error(
                            "Erro ao interpretar evento:",
                            erro
                        );
                    }
                };

            websocket.onclose = function () {
                statusTexto.textContent =
                    "Desconectado — reconectando...";

                statusBolinha.className =
                    "status-bolinha desconectado";

                if (heartbeatId) {
                    clearInterval(
                        heartbeatId
                    );

                    heartbeatId = null;
                }

                setTimeout(
                    conectarWebSocket,
                    2000
                );
            };

            websocket.onerror = function () {
                websocket.close();
            };
        }

        function processarEvento(evento) {
            if (
                evento.status
                === "appearance_updated"
            ) {
                atualizarEventoParcial(
                    evento
                );

                return;
            }

            if (
                evento.status
                === "suppressed"
            ) {
                return;
            }

            const pessoaId =
                evento.pessoa_id
                || evento.evento_id;

            if (!pessoaId) {
                console.warn(
                    "Evento sem identificador:",
                    evento
                );

                return;
            }

            const cartaoExistente =
                cartoesPorPessoa.get(
                    pessoaId
                );

            if (
                evento.status === "exited"
                && !cartaoExistente
            ) {
                return;
            }

            registrarMudancaStatus(
                pessoaId,
                evento.status || "entered"
            );

            totalEventos += 1;
            atualizarResumo();

            if (cartaoExistente) {
                atualizarCartao(
                    cartaoExistente,
                    evento
                );

                return;
            }

            criarCartao(
                pessoaId,
                evento
            );
        }

        function criarElemento(
            tag,
            campo,
            classe = ""
        ) {
            const elemento =
                document.createElement(
                    tag
                );

            if (campo) {
                elemento.dataset.campo =
                    campo;
            }

            if (classe) {
                elemento.className =
                    classe;
            }

            return elemento;
        }

        function obterCamposCard(card) {
            return {
                blocoRecorte: card.querySelector(
                    '[data-campo="bloco-recorte"]'
                ),

                imagemRecorte: card.querySelector(
                    '[data-campo="imagem-recorte"]'
                ),

                blocoCena: card.querySelector(
                    '[data-campo="bloco-cena"]'
                ),

                tituloCena: card.querySelector(
                    '[data-campo="titulo-cena"]'
                ),

                quantidadeBoxes: card.querySelector(
                    '[data-campo="quantidade-boxes"]'
                ),

                imagemCena: card.querySelector(
                    '[data-campo="imagem-cena"]'
                ),

                camera: card.querySelector(
                    '[data-campo="camera"]'
                ),

                status: card.querySelector(
                    '[data-campo="status"]'
                ),

                tipo: card.querySelector(
                    '[data-campo="tipo"]'
                ),

                horario: card.querySelector(
                    '[data-campo="horario"]'
                ),

                pessoaId: card.querySelector(
                    '[data-campo="pessoa-id"]'
                ),

                quantidade: card.querySelector(
                    '[data-campo="quantidade"]'
                ),

                aparencia: card.querySelector(
                    '[data-campo="aparencia"]'
                ),

                corRoupa: card.querySelector(
                    '[data-campo="cor-roupa"]'
                ),

                posicao: card.querySelector(
                    '[data-campo="posicao"]'
                ),

                tamanho: card.querySelector(
                    '[data-campo="tamanho"]'
                ),

                percentual: card.querySelector(
                    '[data-campo="percentual"]'
                ),

                quantidadeAmostras: card.querySelector(
                    '[data-campo="quantidade-amostras"]'
                ),

                descricaoAparencia: card.querySelector(
                    '[data-campo="descricao-aparencia"]'
                ),

                contexto: card.querySelector(
                    '[data-campo="contexto"]'
                ),

                contextoQuantidade: card.querySelector(
                    '[data-campo="contexto-quantidade"]'
                ),

                contextoDistribuicao: card.querySelector(
                    '[data-campo="contexto-distribuicao"]'
                ),

                contextoProximidades: card.querySelector(
                    '[data-campo="contexto-proximidades"]'
                ),

                contextoPessoas: card.querySelector(
                    '[data-campo="contexto-pessoas"]'
                ),

                contextoDescricao: card.querySelector(
                    '[data-campo="contexto-descricao"]'
                )
            };
        }

        function atualizarEventoParcial(
            evento
        ) {
            const pessoaId =
                evento.pessoa_id
                || evento.evento_id;

            const card =
                cartoesPorPessoa.get(
                    pessoaId
                );

            if (!card) {
                console.warn(
                    "Atualização recebida sem cartão:",
                    pessoaId
                );

                return;
            }

            const campos =
                obterCamposCard(card);

            campos.quantidade.textContent =
                `Detecções: ${
                    evento.quantidade_deteccoes
                    || 1
                }`;

            if (evento.aparencia) {
                preencherAparencia(
                    evento.aparencia,
                    campos
                );
            }

            if (evento.contexto_cena) {
                preencherContextoCena(
                    evento.contexto_cena,
                    campos
                );
            }

            if (evento.imagem_cena) {
                preencherCenaCompleta(
                    evento.imagem_cena,
                    evento.quantidade_boxes_cena,
                    campos
                );
            }

            card.classList.add(
                "atualizado"
            );

            setTimeout(
                function () {
                    card.classList.remove(
                        "atualizado"
                    );
                },
                1000
            );
        }

        function criarCartao(
            pessoaId,
            evento
        ) {
            const mensagemVazia =
                document.getElementById(
                    "mensagem-vazia"
                );

            if (mensagemVazia) {
                mensagemVazia.remove();
            }

            const card =
                criarElemento(
                    "article",
                    "",
                    "evento-card"
                );

            card.dataset.pessoaId =
                pessoaId;

            const imagensEvento =
                criarElemento(
                    "section",
                    "",
                    "imagens-evento"
                );

            const blocoRecorte =
                criarElemento(
                    "div",
                    "bloco-recorte",
                    "imagem-bloco"
                );

            const tituloRecorte =
                criarElemento(
                    "div",
                    "",
                    "imagem-titulo"
                );

            tituloRecorte.textContent =
                "Recorte individual";

            const imagemRecorte =
                criarElemento(
                    "img",
                    "imagem-recorte",
                    "imagem-evento imagem-recorte"
                );

            imagemRecorte.alt =
                "Recorte individual da pessoa";

            blocoRecorte.appendChild(
                tituloRecorte
            );

            blocoRecorte.appendChild(
                imagemRecorte
            );

            const blocoCena =
                criarElemento(
                    "div",
                    "bloco-cena",
                    "imagem-bloco"
                );

            const cabecalhoCena =
                criarElemento(
                    "div",
                    "",
                    "imagem-titulo"
                );

            const tituloCena =
                criarElemento(
                    "span",
                    "titulo-cena"
                );

            tituloCena.textContent =
                "Cena completa";

            const quantidadeBoxes =
                criarElemento(
                    "span",
                    "quantidade-boxes",
                    "imagem-contagem"
                );

            quantidadeBoxes.textContent =
                "0 detecções";

            cabecalhoCena.appendChild(
                tituloCena
            );

            cabecalhoCena.appendChild(
                quantidadeBoxes
            );

            const imagemCena =
                criarElemento(
                    "img",
                    "imagem-cena",
                    "imagem-evento imagem-cena"
                );

            imagemCena.alt =
                "Cena completa com as detecções";

            blocoCena.appendChild(
                cabecalhoCena
            );

            blocoCena.appendChild(
                imagemCena
            );

            imagensEvento.appendChild(
                blocoRecorte
            );

            imagensEvento.appendChild(
                blocoCena
            );

            const dados =
                criarElemento(
                    "div",
                    "",
                    "evento-dados"
                );

            const cabecalho =
                criarElemento(
                    "div",
                    "",
                    "evento-cabecalho"
                );

            const titulo =
                criarElemento(
                    "h2",
                    "camera"
                );

            const status =
                criarElemento(
                    "span",
                    "status",
                    "evento-status"
                );

            cabecalho.appendChild(
                titulo
            );

            cabecalho.appendChild(
                status
            );

            const tipo =
                criarElemento(
                    "p",
                    "tipo"
                );

            const horario =
                criarElemento(
                    "p",
                    "horario"
                );

            const identificadorPessoa =
                criarElemento(
                    "p",
                    "pessoa-id"
                );

            const quantidade =
                criarElemento(
                    "p",
                    "quantidade"
                );

            const aparencia =
                criarElemento(
                    "section",
                    "aparencia",
                    "aparencia-visual"
                );

            const aparenciaTitulo =
                criarElemento(
                    "h3",
                    "",
                    "secao-titulo"
                );

            aparenciaTitulo.textContent =
                "Aparência estável";

            const corRoupa =
                criarElemento(
                    "p",
                    "cor-roupa"
                );

            const posicao =
                criarElemento(
                    "p",
                    "posicao"
                );

            const tamanho =
                criarElemento(
                    "p",
                    "tamanho"
                );

            const percentual =
                criarElemento(
                    "p",
                    "percentual"
                );

            const quantidadeAmostras =
                criarElemento(
                    "p",
                    "quantidade-amostras"
                );

            const descricaoAparencia =
                criarElemento(
                    "p",
                    "descricao-aparencia",
                    "descricao"
                );

            aparencia.appendChild(
                aparenciaTitulo
            );

            aparencia.appendChild(
                corRoupa
            );

            aparencia.appendChild(
                posicao
            );

            aparencia.appendChild(
                tamanho
            );

            aparencia.appendChild(
                percentual
            );

            aparencia.appendChild(
                quantidadeAmostras
            );

            aparencia.appendChild(
                descricaoAparencia
            );

            const contexto =
                criarElemento(
                    "section",
                    "contexto",
                    "contexto-cena"
                );

            const contextoTitulo =
                criarElemento(
                    "h3",
                    "",
                    "secao-titulo"
                );

            contextoTitulo.textContent =
                "Contexto da cena";

            const contextoQuantidade =
                criarElemento(
                    "p",
                    "contexto-quantidade"
                );

            const contextoDistribuicao =
                criarElemento(
                    "p",
                    "contexto-distribuicao"
                );

            const contextoProximidades =
                criarElemento(
                    "p",
                    "contexto-proximidades"
                );

            const contextoPessoas =
                criarElemento(
                    "p",
                    "contexto-pessoas",
                    "detalhes-pessoas"
                );

            const contextoDescricao =
                criarElemento(
                    "p",
                    "contexto-descricao",
                    "descricao"
                );

            contexto.appendChild(
                contextoTitulo
            );

            contexto.appendChild(
                contextoQuantidade
            );

            contexto.appendChild(
                contextoDistribuicao
            );

            contexto.appendChild(
                contextoProximidades
            );

            contexto.appendChild(
                contextoPessoas
            );

            contexto.appendChild(
                contextoDescricao
            );

            dados.appendChild(
                cabecalho
            );

            dados.appendChild(
                tipo
            );

            dados.appendChild(
                horario
            );

            dados.appendChild(
                identificadorPessoa
            );

            dados.appendChild(
                quantidade
            );

            dados.appendChild(
                aparencia
            );

            dados.appendChild(
                contexto
            );

            card.appendChild(
                imagensEvento
            );

            card.appendChild(
                dados
            );

            eventosElemento.prepend(
                card
            );

            cartoesPorPessoa.set(
                pessoaId,
                card
            );

            atualizarCartao(
                card,
                evento
            );

            limitarQuantidadeCartoes();
        }

        function preencherRecorte(
            imagemBase64,
            campos
        ) {
            if (!imagemBase64) {
                return;
            }

            campos.blocoRecorte.style.display =
                "block";

            campos.imagemRecorte.src =
                `data:image/jpeg;base64,${imagemBase64}`;
        }

        function preencherCenaCompleta(
            imagemBase64,
            quantidadeBoxes,
            campos
        ) {
            if (!imagemBase64) {
                return;
            }

            const quantidade =
                Number(
                    quantidadeBoxes || 0
                );

            campos.blocoCena.style.display =
                "block";

            campos.imagemCena.src =
                `data:image/jpeg;base64,${imagemBase64}`;

            campos.tituloCena.textContent =
                "Cena completa com detecções";

            if (quantidade === 1) {
                campos.quantidadeBoxes.textContent =
                    "1 detecção";
            } else {
                campos.quantidadeBoxes.textContent =
                    `${quantidade} detecções`;
            }
        }

        function preencherAparencia(
            aparencia,
            campos
        ) {
            campos.aparencia.style.display =
                "block";

            const rgb =
                aparencia.rgb_medio;

            let amostraCor = "";

            if (
                Array.isArray(rgb)
                && rgb.length === 3
            ) {
                amostraCor =
                    `<span
                        class="cor-amostra"
                        style="
                            background-color:
                            rgb(
                                ${rgb[0]},
                                ${rgb[1]},
                                ${rgb[2]}
                            );
                        "
                    ></span>`;
            }

            campos.corRoupa.innerHTML =
                `${amostraCor}Cor predominante: ${
                    aparencia
                        .cor_roupa_predominante
                    || "-"
                }`;

            campos.posicao.textContent =
                `Posição atual: ${
                    aparencia.posicao_atual
                    || "-"
                }`;

            campos.tamanho.textContent =
                `Tamanho predominante: ${
                    aparencia
                        .tamanho_predominante
                    || "-"
                }`;

            campos.percentual.textContent =
                `Área média ocupada: ${
                    aparencia
                        .percentual_medio_quadro
                    ?? "-"
                }%`;

            campos.quantidadeAmostras.textContent =
                `Amostras analisadas: ${
                    aparencia
                        .quantidade_amostras
                    || 1
                }`;

            campos.descricaoAparencia.textContent =
                `Descrição estável: ${
                    aparencia.descricao
                    || "-"
                }`;
        }

        function montarDistribuicao(
            contexto
        ) {
            const partes = [];

            if (contexto.pessoas_esquerda) {
                partes.push(
                    `${contexto.pessoas_esquerda} à esquerda`
                );
            }

            if (contexto.pessoas_centro) {
                partes.push(
                    `${contexto.pessoas_centro} no centro`
                );
            }

            if (contexto.pessoas_direita) {
                partes.push(
                    `${contexto.pessoas_direita} à direita`
                );
            }

            if (partes.length === 0) {
                return "sem distribuição";
            }

            return partes.join(", ");
        }

        function preencherContextoCena(
            contexto,
            campos
        ) {
            campos.contexto.style.display =
                "block";

            campos.contextoQuantidade.textContent =
                `Pessoas identificadas: ${
                    contexto.quantidade_pessoas
                    ?? 0
                }`;

            campos.contextoDistribuicao.textContent =
                `Distribuição: ${
                    montarDistribuicao(
                        contexto
                    )
                }`;

            campos.contextoProximidades.textContent =
                `Pares muito próximos: ${
                    contexto.pares_muito_proximos
                    ?? 0
                } | Pares próximos: ${
                    contexto.pares_proximos
                    ?? 0
                } | Pares separados: ${
                    contexto.pares_separados
                    ?? 0
                }`;

            const pessoas =
                Array.isArray(
                    contexto.pessoas
                )
                    ? contexto.pessoas
                    : [];

            if (pessoas.length > 0) {
                const detalhes =
                    pessoas
                    .slice(0, 8)
                    .map(
                        function (pessoa) {
                            return (
                                `Pessoa ${pessoa.indice}: `
                                + `${pessoa.posicao_horizontal || "-"}, `
                                + `${pessoa.posicao_vertical || "-"}, `
                                + `tamanho ${pessoa.tamanho_no_quadro || "-"}, `
                                + `${pessoa.percentual_quadro ?? "-"}% do quadro`
                            );
                        }
                    );

                if (pessoas.length > 8) {
                    detalhes.push(
                        `E mais ${
                            pessoas.length - 8
                        } pessoa(s).`
                    );
                }

                campos.contextoPessoas.textContent =
                    "Detalhes:\n"
                    + detalhes.join("\n");

            } else {
                campos.contextoPessoas.textContent =
                    "Detalhes: nenhuma pessoa válida.";
            }

            campos.contextoDescricao.textContent =
                `Descrição da cena: ${
                    contexto.descricao
                    || "-"
                }`;
        }

        function atualizarCartao(
            card,
            evento
        ) {
            const campos =
                obterCamposCard(card);

            if (evento.imagem) {
                preencherRecorte(
                    evento.imagem,
                    campos
                );
            }

            if (evento.imagem_cena) {
                preencherCenaCompleta(
                    evento.imagem_cena,
                    evento.quantidade_boxes_cena,
                    campos
                );
            }

            if (evento.camera) {
                campos.camera.textContent =
                    evento.camera;
            }

            campos.status.textContent =
                traduzirStatus(
                    evento.status
                );

            campos.status.className =
                `evento-status ${evento.status || ""}`;

            if (evento.tipo) {
                campos.tipo.textContent =
                    `Evento: ${evento.tipo}`;
            }

            if (
                evento.status === "exited"
            ) {
                campos.horario.textContent =
                    `Saída: ${
                        evento.datetime || "-"
                    }`;

            } else {
                campos.horario.textContent =
                    `Última captura: ${
                        evento.datetime || "-"
                    }`;
            }

            campos.pessoaId.textContent =
                `Pessoa: ${
                    evento.pessoa_id || "-"
                }`;

            campos.quantidade.textContent =
                `Detecções: ${

------------------------------------------------------------
ARQUIVO: app/static/index_backup_atualizacao_visual.html
------------------------------------------------------------
<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">

    <meta
        name="viewport"
        content="width=device-width, initial-scale=1.0"
    >

    <title>Painel de Eventos</title>

    <style>
        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            font-family: Arial, sans-serif;
            background: #10131a;
            color: #f3f4f6;
        }

        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px 30px;
            background: #191e29;
            border-bottom: 1px solid #2d3443;
        }

        h1 {
            margin: 0;
            font-size: 24px;
        }

        .status-conexao {
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .status-bolinha {
            width: 12px;
            height: 12px;
            border-radius: 50%;
            background: #f59e0b;
        }

        .status-bolinha.conectado {
            background: #22c55e;
        }

        .status-bolinha.desconectado {
            background: #ef4444;
        }

        main {
            padding: 30px;
        }

        .resumo {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
            margin-bottom: 25px;
        }

        .resumo-card {
            min-width: 180px;
            padding: 18px;
            background: #191e29;
            border: 1px solid #2d3443;
            border-radius: 10px;
        }

        .resumo-card span {
            display: block;
            color: #9ca3af;
            font-size: 14px;
        }

        .resumo-card strong {
            display: block;
            margin-top: 8px;
            font-size: 28px;
        }

        #eventos {
            display: grid;
            grid-template-columns: repeat(
                auto-fill,
                minmax(310px, 1fr)
            );
            gap: 20px;
        }

        .evento-card {
            overflow: hidden;
            background: #191e29;
            border: 1px solid #2d3443;
            border-radius: 10px;
            transition:
                border-color 0.2s,
                transform 0.2s,
                opacity 0.2s;
        }

        .evento-card.atualizado {
            border-color: #3b82f6;
            transform: translateY(-2px);
        }

        .evento-card.saiu {
            opacity: 0.55;
            border-color: #6b7280;
        }

        .evento-card img {
            width: 100%;
            height: 230px;
            object-fit: contain;
            background: #05070a;
        }

        .evento-dados {
            padding: 15px;
        }

        .evento-cabecalho {
            display: flex;
            justify-content: space-between;
            align-items: center;
            gap: 10px;
            margin-bottom: 12px;
        }

        .evento-cabecalho h2 {
            margin: 0;
            font-size: 18px;
        }

        .evento-status {
            padding: 5px 9px;
            border-radius: 999px;
            background: #374151;
            font-size: 12px;
            font-weight: bold;
        }

        .evento-status.entered {
            background: #166534;
            color: #dcfce7;
        }

        .evento-status.updated {
            background: #1e40af;
            color: #dbeafe;
        }

        .evento-status.exited {
            background: #4b5563;
            color: #f3f4f6;
        }

        .evento-dados p {
            margin: 6px 0;
            color: #cbd5e1;
            font-size: 14px;
        }

        .aparencia-visual {
            display: none;
            margin-top: 14px;
            padding-top: 12px;
            border-top: 1px solid #2d3443;
        }

        .aparencia-titulo {
            margin: 0 0 10px;
            font-size: 14px;
            color: #93c5fd;
        }

        .descricao {
            line-height: 1.45;
        }

        .cor-amostra {
            display: inline-block;
            width: 12px;
            height: 12px;
            margin-right: 6px;
            border: 1px solid #64748b;
            border-radius: 50%;
            vertical-align: -1px;
        }

        .vazio {
            padding: 40px;
            color: #9ca3af;
            text-align: center;
            background: #191e29;
            border: 1px dashed #374151;
            border-radius: 10px;
        }

        @media (max-width: 700px) {
            header {
                align-items: flex-start;
                flex-direction: column;
                gap: 15px;
                padding: 20px;
            }

            main {
                padding: 20px;
            }

            .resumo-card {
                flex: 1;
                min-width: 140px;
            }
        }
    </style>
</head>

<body>
    <header>
        <h1>Painel de Eventos das Câmeras</h1>

        <div class="status-conexao">
            <div
                id="status-bolinha"
                class="status-bolinha"
            ></div>

            <span id="status-texto">
                Conectando...
            </span>
        </div>
    </header>

    <main>
        <section class="resumo">
            <div class="resumo-card">
                <span>Pessoas ativas</span>
                <strong id="pessoas-ativas">0</strong>
            </div>

            <div class="resumo-card">
                <span>Pessoas que saíram</span>
                <strong id="pessoas-sairam">0</strong>
            </div>

            <div class="resumo-card">
                <span>Total exibido</span>
                <strong id="total-pessoas">0</strong>
            </div>

            <div class="resumo-card">
                <span>Eventos processados</span>
                <strong id="total-eventos">0</strong>
            </div>
        </section>

        <section id="eventos">
            <div
                id="mensagem-vazia"
                class="vazio"
            >
                Aguardando eventos das câmeras...
            </div>
        </section>
    </main>

    <script>
        const statusBolinha =
            document.getElementById(
                "status-bolinha"
            );

        const statusTexto =
            document.getElementById(
                "status-texto"
            );

        const pessoasAtivasElemento =
            document.getElementById(
                "pessoas-ativas"
            );

        const pessoasSairamElemento =
            document.getElementById(
                "pessoas-sairam"
            );

        const totalPessoasElemento =
            document.getElementById(
                "total-pessoas"
            );

        const totalEventosElemento =
            document.getElementById(
                "total-eventos"
            );

        const eventosElemento =
            document.getElementById(
                "eventos"
            );

        const cartoesPorPessoa =
            new Map();

        const statusPorPessoa =
            new Map();

        let pessoasAtivas = 0;
        let pessoasSairam = 0;
        let totalPessoas = 0;
        let totalEventos = 0;
        let heartbeatId = null;

        function traduzirStatus(status) {
            const traducoes = {
                entered: "Entrou",
                updated: "Atualizado",
                suppressed: "Repetido",
                exited: "Saiu"
            };

            return (
                traducoes[status]
                || status
                || "Detectado"
            );
        }

        function atualizarResumo() {
            pessoasAtivasElemento.textContent =
                pessoasAtivas;

            pessoasSairamElemento.textContent =
                pessoasSairam;

            totalPessoasElemento.textContent =
                totalPessoas;

            totalEventosElemento.textContent =
                totalEventos;
        }

        function registrarMudancaStatus(
            pessoaId,
            novoStatus
        ) {
            const statusAnterior =
                statusPorPessoa.get(
                    pessoaId
                );

            if (!statusAnterior) {
                totalPessoas += 1;

                if (novoStatus === "exited") {
                    pessoasSairam += 1;
                } else {
                    pessoasAtivas += 1;
                }
            }

            else if (
                statusAnterior !== "exited"
                && novoStatus === "exited"
            ) {
                pessoasAtivas = Math.max(
                    0,
                    pessoasAtivas - 1
                );

                pessoasSairam += 1;
            }

            else if (
                statusAnterior === "exited"
                && novoStatus !== "exited"
            ) {
                pessoasSairam = Math.max(
                    0,
                    pessoasSairam - 1
                );

                pessoasAtivas += 1;
            }

            statusPorPessoa.set(
                pessoaId,
                novoStatus
            );

            atualizarResumo();
        }

        function conectarWebSocket() {
            const protocolo =
                window.location.protocol === "https:"
                    ? "wss"
                    : "ws";

            const endereco =
                `${protocolo}://${window.location.host}/ws/eventos`;

            const websocket =
                new WebSocket(endereco);

            websocket.onopen = function () {
                statusTexto.textContent =
                    "Conectado";

                statusBolinha.className =
                    "status-bolinha conectado";

                if (heartbeatId) {
                    clearInterval(
                        heartbeatId
                    );
                }

                heartbeatId = setInterval(
                    function () {
                        if (
                            websocket.readyState
                            === WebSocket.OPEN
                        ) {
                            websocket.send(
                                "ping"
                            );
                        }
                    },
                    20000
                );
            };

            websocket.onmessage =
                function (mensagem) {
                    try {
                        const evento =
                            JSON.parse(
                                mensagem.data
                            );

                        processarEvento(
                            evento
                        );

                    } catch (erro) {
                        console.error(
                            "Erro ao interpretar evento:",
                            erro
                        );
                    }
                };

            websocket.onclose = function () {
                statusTexto.textContent =
                    "Desconectado — reconectando...";

                statusBolinha.className =
                    "status-bolinha desconectado";

                if (heartbeatId) {
                    clearInterval(
                        heartbeatId
                    );

                    heartbeatId = null;
                }

                setTimeout(
                    conectarWebSocket,
                    2000
                );
            };

            websocket.onerror = function () {
                websocket.close();
            };
        }

        function processarEvento(evento) {
            if (
                evento.status
                === "suppressed"
            ) {
                return;
            }

            const pessoaId =
                evento.pessoa_id
                || evento.evento_id;

            if (!pessoaId) {
                console.warn(
                    "Evento sem identificador",
                    evento
                );

                return;
            }

            const cartaoExistente =
                cartoesPorPessoa.get(
                    pessoaId
                );

            if (
                evento.status === "exited"
                && !cartaoExistente
            ) {
                return;
            }

            registrarMudancaStatus(
                pessoaId,
                evento.status || "entered"
            );

            totalEventos += 1;
            atualizarResumo();

            if (cartaoExistente) {
                atualizarCartao(
                    cartaoExistente,
                    evento
                );

                return;
            }

            criarCartao(
                pessoaId,
                evento
            );
        }

        function criarCartao(
            pessoaId,
            evento
        ) {
            const mensagemVazia =
                document.getElementById(
                    "mensagem-vazia"
                );

            if (mensagemVazia) {
                mensagemVazia.remove();
            }

            const card =
                document.createElement(
                    "article"
                );

            card.className =
                "evento-card";

            card.dataset.pessoaId =
                pessoaId;

            const imagem =
                document.createElement(
                    "img"
                );

            imagem.alt =
                "Recorte da pessoa";

            imagem.dataset.campo =
                "imagem";

            const dados =
                document.createElement(
                    "div"
                );

            dados.className =
                "evento-dados";

            const cabecalho =
                document.createElement(
                    "div"
                );

            cabecalho.className =
                "evento-cabecalho";

            const titulo =
                document.createElement(
                    "h2"
                );

            titulo.dataset.campo =
                "camera";

            const status =
                document.createElement(
                    "span"
                );

            status.dataset.campo =
                "status";

            status.className =
                "evento-status";

            const tipo =
                document.createElement(
                    "p"
                );

            tipo.dataset.campo =
                "tipo";

            const horario =
                document.createElement(
                    "p"
                );

            horario.dataset.campo =
                "horario";

            const identificadorPessoa =
                document.createElement(
                    "p"
                );

            identificadorPessoa.dataset.campo =
                "pessoa-id";

            const quantidade =
                document.createElement(
                    "p"
                );

            quantidade.dataset.campo =
                "quantidade";

            const aparencia =
                document.createElement(
                    "section"
                );

            aparencia.className =
                "aparencia-visual";

            aparencia.dataset.campo =
                "aparencia";

            const aparenciaTitulo =
                document.createElement(
                    "h3"
                );

            aparenciaTitulo.className =
                "aparencia-titulo";

            aparenciaTitulo.textContent =
                "Aparência estável";

            const corRoupa =
                document.createElement(
                    "p"
                );

            corRoupa.dataset.campo =
                "cor-roupa";

            const posicao =
                document.createElement(
                    "p"
                );

            posicao.dataset.campo =
                "posicao";

            const tamanho =
                document.createElement(
                    "p"
                );

            tamanho.dataset.campo =
                "tamanho";

            const percentual =
                document.createElement(
                    "p"
                );

            percentual.dataset.campo =
                "percentual";

            const quantidadeAmostras =
                document.createElement(
                    "p"
                );

            quantidadeAmostras.dataset.campo =
                "quantidade-amostras";

            const descricao =
                document.createElement(
                    "p"
                );

            descricao.dataset.campo =
                "descricao";

            descricao.className =
                "descricao";

            cabecalho.appendChild(
                titulo
            );

            cabecalho.appendChild(
                status
            );

            dados.appendChild(
                cabecalho
            );

            dados.appendChild(tipo);
            dados.appendChild(horario);

            dados.appendChild(
                identificadorPessoa
            );

            dados.appendChild(
                quantidade
            );

            aparencia.appendChild(
                aparenciaTitulo
            );

            aparencia.appendChild(
                corRoupa
            );

            aparencia.appendChild(
                posicao
            );

            aparencia.appendChild(
                tamanho
            );

            aparencia.appendChild(
                percentual
            );

            aparencia.appendChild(
                quantidadeAmostras
            );

            aparencia.appendChild(
                descricao
            );

            dados.appendChild(
                aparencia
            );

            card.appendChild(imagem);
            card.appendChild(dados);

            eventosElemento.prepend(
                card
            );

            cartoesPorPessoa.set(
                pessoaId,
                card
            );

            atualizarCartao(
                card,
                evento
            );

            limitarQuantidadeCartoes();
        }

        function atualizarCartao(
            card,
            evento
        ) {
            const imagem =
                card.querySelector(
                    '[data-campo="imagem"]'
                );

            const titulo =
                card.querySelector(
                    '[data-campo="camera"]'
                );

            const status =
                card.querySelector(
                    '[data-campo="status"]'
                );

            const tipo =
                card.querySelector(
                    '[data-campo="tipo"]'
                );

            const horario =
                card.querySelector(
                    '[data-campo="horario"]'
                );

            const pessoaId =
                card.querySelector(
                    '[data-campo="pessoa-id"]'
                );

            const quantidade =
                card.querySelector(
                    '[data-campo="quantidade"]'
                );

            const aparenciaVisual =
                card.querySelector(
                    '[data-campo="aparencia"]'
                );

            const corRoupa =
                card.querySelector(
                    '[data-campo="cor-roupa"]'
                );

            const posicao =
                card.querySelector(
                    '[data-campo="posicao"]'
                );

            const tamanho =
                card.querySelector(
                    '[data-campo="tamanho"]'
                );

            const percentual =
                card.querySelector(
                    '[data-campo="percentual"]'
                );

            const quantidadeAmostras =
                card.querySelector(
                    '[data-campo="quantidade-amostras"]'
                );

            const descricao =
                card.querySelector(
                    '[data-campo="descricao"]'
                );

            if (evento.imagem) {
                imagem.src =
                    `data:image/jpeg;base64,${evento.imagem}`;
            }

            if (evento.camera) {
                titulo.textContent =
                    evento.camera;
            }

            status.textContent =
                traduzirStatus(
                    evento.status
                );

            status.className =
                `evento-status ${evento.status || ""}`;

            if (evento.tipo) {
                tipo.textContent =
                    `Evento: ${evento.tipo}`;
            }

            if (
                evento.status === "exited"
            ) {
                horario.textContent =
                    `Saída: ${evento.datetime || "-"}`;
            } else {
                horario.textContent =
                    `Última captura: ${evento.datetime || "-"}`;
            }

            pessoaId.textContent =
                `Pessoa: ${evento.pessoa_id || "-"}`;

            quantidade.textContent =
                `Detecções: ${
                    evento.quantidade_deteccoes
                    || 1
                }`;

            if (evento.aparencia) {
                aparenciaVisual.style.display =
                    "block";

                const rgb =
                    evento.aparencia.rgb_medio;

                let amostraCor = "";

                if (
                    Array.isArray(rgb)
                    && rgb.length === 3
                ) {
                    amostraCor =
                        `<span
                            class="cor-amostra"
                            style="
                                background-color:
                                rgb(
                                    ${rgb[0]},
                                    ${rgb[1]},
                                    ${rgb[2]}
                                );
                            "
                        ></span>`;
                }

                corRoupa.innerHTML =
                    `${amostraCor}Cor predominante: ${
                        evento.aparencia
                            .cor_roupa_predominante
                        || "-"
                    }`;

                posicao.textContent =
                    `Posição atual: ${
                        evento.aparencia
                            .posicao_atual
                        || "-"
                    }`;

                tamanho.textContent =
                    `Tamanho predominante: ${
                        evento.aparencia
                            .tamanho_predominante
                        || "-"
                    }`;

                percentual.textContent =
                    `Área média ocupada: ${
                        evento.aparencia
                            .percentual_medio_quadro
                        ?? "-"
                    }%`;

                quantidadeAmostras.textContent =
                    `Amostras analisadas: ${
                        evento.aparencia
                            .quantidade_amostras
                        || 1
                    }`;

                descricao.textContent =
                    `Descrição estável: ${
                        evento.aparencia
                            .descricao
                        || "-"
                    }`;
            }

            card.classList.remove(
                "atualizado",
                "saiu"
            );

            if (
                evento.status === "updated"
            ) {
                card.classList.add(
                    "atualizado"
                );

                eventosElemento.prepend(
                    card
                );

                setTimeout(
                    function () {
                        card.classList.remove(
                            "atualizado"
                        );
                    },
                    1500
                );
            }

            if (
                evento.status === "exited"
            ) {
                card.classList.add(
                    "saiu"
                );
            }
        }

        function limitarQuantidadeCartoes() {
            while (
                eventosElemento.children.length
                > 50
            ) {
                const ultimo =
                    eventosElemento
                    .lastElementChild;

                if (!ultimo) {
                    break;
                }

                const pessoaId =
                    ultimo.dataset.pessoaId;

                if (pessoaId) {
                    const statusRemovido =
                        statusPorPessoa.get(
                            pessoaId
                        );

                    if (
                        statusRemovido
                        === "exited"
                    ) {
                        pessoasSairam =
                            Math.max(
                                0,
                                pessoasSairam - 1
                            );
                    }

                    else if (
                        statusRemovido
                    ) {
                        pessoasAtivas =
                            Math.max(
                                0,
                                pessoasAtivas - 1
                            );
                    }

                    totalPessoas =
                        Math.max(
                            0,
                            totalPessoas - 1
                        );

                    cartoesPorPessoa.delete(
                        pessoaId
                    );

                    statusPorPessoa.delete(
                        pessoaId
                    );
                }

                ultimo.remove();
            }

            atualizarResumo();
        }

        atualizarResumo();
        conectarWebSocket();
    </script>
</body>
</html>
------------------------------------------------------------
ARQUIVO: app/static/index_backup_recorte.html
------------------------------------------------------------
<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">

    <meta
        name="viewport"
        content="width=device-width, initial-scale=1.0"
    >

    <title>Painel de Eventos</title>

    <style>
        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            font-family: Arial, sans-serif;
            background: #10131a;
            color: #f3f4f6;
        }

        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px 30px;
            background: #191e29;
            border-bottom: 1px solid #2d3443;
        }

        h1 {
            margin: 0;
            font-size: 24px;
        }

        .status-conexao {
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .status-bolinha {
            width: 12px;
            height: 12px;
            border-radius: 50%;
            background: #f59e0b;
        }

        .status-bolinha.conectado {
            background: #22c55e;
        }

        .status-bolinha.desconectado {
            background: #ef4444;
        }

        main {
            padding: 30px;
        }

        .resumo {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
            margin-bottom: 25px;
        }

        .resumo-card {
            min-width: 180px;
            padding: 18px;
            background: #191e29;
            border: 1px solid #2d3443;
            border-radius: 10px;
        }

        .resumo-card span {
            display: block;
            color: #9ca3af;
            font-size: 14px;
        }

        .resumo-card strong {
            display: block;
            margin-top: 8px;
            font-size: 28px;
        }

        #eventos {
            display: grid;
            grid-template-columns: repeat(
                auto-fill,
                minmax(400px, 1fr)
            );
            gap: 20px;
        }

        .evento-card {
            overflow: hidden;
            background: #191e29;
            border: 1px solid #2d3443;
            border-radius: 10px;
            transition:
                border-color 0.2s,
                transform 0.2s,
                opacity 0.2s;
        }

        .evento-card.atualizado {
            border-color: #3b82f6;
            transform: translateY(-2px);
        }

        .evento-card.saiu {
            opacity: 0.55;
            border-color: #6b7280;
        }

        .imagens-evento {
            display: flex;
            flex-direction: column;
            background: #05070a;
        }

        .imagem-bloco {
            display: none;
            border-bottom: 1px solid #2d3443;
        }

        .imagem-bloco:last-child {
            border-bottom: none;
        }

        .imagem-titulo {
            display: flex;
            justify-content: space-between;
            align-items: center;
            min-height: 38px;
            padding: 9px 12px;
            background: #111827;
            color: #cbd5e1;
            font-size: 13px;
            font-weight: bold;
        }

        .imagem-contagem {
            color: #86efac;
            font-size: 12px;
        }

        .imagem-evento {
            display: block;
            width: 100%;
            background: #05070a;
        }

        .imagem-recorte {
            height: 260px;
            object-fit: contain;
        }

        .imagem-cena {
            aspect-ratio: 16 / 9;
            max-height: 440px;
            object-fit: contain;
        }

        .evento-dados {
            padding: 15px;
        }

        .evento-cabecalho {
            display: flex;
            justify-content: space-between;
            align-items: center;
            gap: 10px;
            margin-bottom: 12px;
        }

        .evento-cabecalho h2 {
            margin: 0;
            font-size: 18px;
        }

        .evento-status {
            padding: 5px 9px;
            border-radius: 999px;
            background: #374151;
            font-size: 12px;
            font-weight: bold;
        }

        .evento-status.entered {
            background: #166534;
            color: #dcfce7;
        }

        .evento-status.updated {
            background: #1e40af;
            color: #dbeafe;
        }

        .evento-status.exited {
            background: #4b5563;
            color: #f3f4f6;
        }

        .evento-dados p {
            margin: 6px 0;
            color: #cbd5e1;
            font-size: 14px;
        }

        .aparencia-visual,
        .movimento-pessoa,
        .contexto-cena {
            display: none;
            margin-top: 14px;
            padding-top: 12px;
            border-top: 1px solid #2d3443;
        }

        .secao-titulo {
            margin: 0 0 10px;
            font-size: 14px;
            color: #93c5fd;
        }

        .movimento-pessoa .secao-titulo {
            color: #fbbf24;
        }

        .contexto-cena .secao-titulo {
            color: #86efac;
        }

        .descricao {
            line-height: 1.45;
        }

        .detalhes-pessoas {
            line-height: 1.5;
            white-space: pre-line;
        }

        .cor-amostra {
            display: inline-block;
            width: 12px;
            height: 12px;
            margin-right: 6px;
            border: 1px solid #64748b;
            border-radius: 50%;
            vertical-align: -1px;
        }

        .vazio {
            padding: 40px;
            color: #9ca3af;
            text-align: center;
            background: #191e29;
            border: 1px dashed #374151;
            border-radius: 10px;
        }

        @media (max-width: 700px) {
            header {
                align-items: flex-start;
                flex-direction: column;
                gap: 15px;
                padding: 20px;
            }

            main {
                padding: 20px;
            }

            #eventos {
                grid-template-columns: 1fr;
            }

            .resumo-card {
                flex: 1;
                min-width: 140px;
            }

            .imagem-recorte {
                height: 230px;
            }
        }
    </style>
</head>

<body>
    <header>
        <h1>Painel de Eventos das Câmeras</h1>

        <div class="status-conexao">
            <div
                id="status-bolinha"
                class="status-bolinha"
            ></div>

            <span id="status-texto">
                Conectando...
            </span>
        </div>
    </header>

    <main>
        <section class="resumo">
            <div class="resumo-card">
                <span>Pessoas ativas</span>
                <strong id="pessoas-ativas">0</strong>
            </div>

            <div class="resumo-card">
                <span>Pessoas que saíram</span>
                <strong id="pessoas-sairam">0</strong>
            </div>

            <div class="resumo-card">
                <span>Total exibido</span>
                <strong id="total-pessoas">0</strong>
            </div>

            <div class="resumo-card">
                <span>Eventos processados</span>
                <strong id="total-eventos">0</strong>
            </div>
        </section>

        <section id="eventos">
            <div
                id="mensagem-vazia"
                class="vazio"
            >
                Aguardando eventos das câmeras...
            </div>
        </section>
    </main>

    <script>
        const statusBolinha =
            document.getElementById(
                "status-bolinha"
            );

        const statusTexto =
            document.getElementById(
                "status-texto"
            );

        const pessoasAtivasElemento =
            document.getElementById(
                "pessoas-ativas"
            );

        const pessoasSairamElemento =
            document.getElementById(
                "pessoas-sairam"
            );

        const totalPessoasElemento =
            document.getElementById(
                "total-pessoas"
            );

        const totalEventosElemento =
            document.getElementById(
                "total-eventos"
            );

        const eventosElemento =
            document.getElementById(
                "eventos"
            );

        const cartoesPorPessoa =
            new Map();

        const statusPorPessoa =
            new Map();

        let pessoasAtivas = 0;
        let pessoasSairam = 0;
        let totalPessoas = 0;
        let totalEventos = 0;
        let heartbeatId = null;

        function traduzirStatus(status) {
            const traducoes = {
                entered: "Entrou",
                updated: "Atualizado",
                suppressed: "Repetido",
                exited: "Saiu"
            };

            return (
                traducoes[status]
                || status
                || "Detectado"
            );
        }

        function atualizarResumo() {
            pessoasAtivasElemento.textContent =
                pessoasAtivas;

            pessoasSairamElemento.textContent =
                pessoasSairam;

            totalPessoasElemento.textContent =
                totalPessoas;

            totalEventosElemento.textContent =
                totalEventos;
        }

        function registrarMudancaStatus(
            pessoaId,
            novoStatus
        ) {
            const statusAnterior =
                statusPorPessoa.get(
                    pessoaId
                );

            if (!statusAnterior) {
                totalPessoas += 1;

                if (novoStatus === "exited") {
                    pessoasSairam += 1;
                } else {
                    pessoasAtivas += 1;
                }
            }

            else if (
                statusAnterior !== "exited"
                && novoStatus === "exited"
            ) {
                pessoasAtivas = Math.max(
                    0,
                    pessoasAtivas - 1
                );

                pessoasSairam += 1;
            }

            else if (
                statusAnterior === "exited"
                && novoStatus !== "exited"
            ) {
                pessoasSairam = Math.max(
                    0,
                    pessoasSairam - 1
                );

                pessoasAtivas += 1;
            }

            statusPorPessoa.set(
                pessoaId,
                novoStatus
            );

            atualizarResumo();
        }

        function conectarWebSocket() {
            const protocolo =
                window.location.protocol === "https:"
                    ? "wss"
                    : "ws";

            const endereco =
                `${protocolo}://${window.location.host}/ws/eventos`;

            const websocket =
                new WebSocket(endereco);

            websocket.onopen = function () {
                statusTexto.textContent =
                    "Conectado";

                statusBolinha.className =
                    "status-bolinha conectado";

                if (heartbeatId) {
                    clearInterval(
                        heartbeatId
                    );
                }

                heartbeatId = setInterval(
                    function () {
                        if (
                            websocket.readyState
                            === WebSocket.OPEN
                        ) {
                            websocket.send(
                                "ping"
                            );
                        }
                    },
                    20000
                );
            };

            websocket.onmessage =
                function (mensagem) {
                    try {
                        const evento =
                            JSON.parse(
                                mensagem.data
                            );

                        processarEvento(
                            evento
                        );

                    } catch (erro) {
                        console.error(
                            "Erro ao interpretar evento:",
                            erro
                        );
                    }
                };

            websocket.onclose = function () {
                statusTexto.textContent =
                    "Desconectado — reconectando...";

                statusBolinha.className =
                    "status-bolinha desconectado";

                if (heartbeatId) {
                    clearInterval(
                        heartbeatId
                    );

                    heartbeatId = null;
                }

                setTimeout(
                    conectarWebSocket,
                    2000
                );
            };

            websocket.onerror = function () {
                websocket.close();
            };
        }

        function processarEvento(evento) {
            if (
                evento.status
                === "appearance_updated"
            ) {
                atualizarEventoParcial(
                    evento
                );

                return;
            }

            if (
                evento.status
                === "suppressed"
            ) {
                return;
            }

            const pessoaId =
                evento.pessoa_id
                || evento.evento_id;

            if (!pessoaId) {
                console.warn(
                    "Evento sem identificador:",
                    evento
                );

                return;
            }

            const cartaoExistente =
                cartoesPorPessoa.get(
                    pessoaId
                );

            if (
                evento.status === "exited"
                && !cartaoExistente
            ) {
                return;
            }

            registrarMudancaStatus(
                pessoaId,
                evento.status || "entered"
            );

            totalEventos += 1;
            atualizarResumo();

            if (cartaoExistente) {
                atualizarCartao(
                    cartaoExistente,
                    evento
                );

                return;
            }

            criarCartao(
                pessoaId,
                evento
            );
        }

        function criarElemento(
            tag,
            campo,
            classe = ""
        ) {
            const elemento =
                document.createElement(
                    tag
                );

            if (campo) {
                elemento.dataset.campo =
                    campo;
            }

            if (classe) {
                elemento.className =
                    classe;
            }

            return elemento;
        }

        function obterCamposCard(card) {
            return {
                blocoRecorte: card.querySelector(
                    '[data-campo="bloco-recorte"]'
                ),

                imagemRecorte: card.querySelector(
                    '[data-campo="imagem-recorte"]'
                ),

                blocoCena: card.querySelector(
                    '[data-campo="bloco-cena"]'
                ),

                tituloCena: card.querySelector(
                    '[data-campo="titulo-cena"]'
                ),

                quantidadeBoxes: card.querySelector(
                    '[data-campo="quantidade-boxes"]'
                ),

                imagemCena: card.querySelector(
                    '[data-campo="imagem-cena"]'
                ),

                camera: card.querySelector(
                    '[data-campo="camera"]'
                ),

                status: card.querySelector(
                    '[data-campo="status"]'
                ),

                tipo: card.querySelector(
                    '[data-campo="tipo"]'
                ),

                horario: card.querySelector(
                    '[data-campo="horario"]'
                ),

                pessoaId: card.querySelector(
                    '[data-campo="pessoa-id"]'
                ),

                quantidade: card.querySelector(
                    '[data-campo="quantidade"]'
                ),

                aparencia: card.querySelector(
                    '[data-campo="aparencia"]'
                ),

                corRoupa: card.querySelector(
                    '[data-campo="cor-roupa"]'
                ),

                posicao: card.querySelector(
                    '[data-campo="posicao"]'
                ),

                tamanho: card.querySelector(
                    '[data-campo="tamanho"]'
                ),

                percentual: card.querySelector(
                    '[data-campo="percentual"]'
                ),

                quantidadeAmostras: card.querySelector(
                    '[data-campo="quantidade-amostras"]'
                ),

                descricaoAparencia: card.querySelector(
                    '[data-campo="descricao-aparencia"]'
                ),

                movimento: card.querySelector(
                    '[data-campo="movimento"]'
                ),

                movimentoHorizontal: card.querySelector(
                    '[data-campo="movimento-horizontal"]'
                ),

                movimentoVertical: card.querySelector(
                    '[data-campo="movimento-vertical"]'
                ),

                movimentoTendencia: card.querySelector(
                    '[data-campo="movimento-tendencia"]'
                ),

                movimentoVelocidade: card.querySelector(
                    '[data-campo="movimento-velocidade"]'
                ),

                movimentoDistancia: card.querySelector(
                    '[data-campo="movimento-distancia"]'
                ),

                movimentoTempo: card.querySelector(
                    '[data-campo="movimento-tempo"]'
                ),

                movimentoAmostras: card.querySelector(
                    '[data-campo="movimento-amostras"]'
                ),

                movimentoDescricao: card.querySelector(
                    '[data-campo="movimento-descricao"]'
                ),

                contexto: card.querySelector(
                    '[data-campo="contexto"]'
                ),

                contextoQuantidade: card.querySelector(
                    '[data-campo="contexto-quantidade"]'
                ),

                contextoDistribuicao: card.querySelector(
                    '[data-campo="contexto-distribuicao"]'
                ),

                contextoProximidades: card.querySelector(
                    '[data-campo="contexto-proximidades"]'
                ),

                contextoPessoas: card.querySelector(
                    '[data-campo="contexto-pessoas"]'
                ),

                contextoDescricao: card.querySelector(
                    '[data-campo="contexto-descricao"]'
                )
            };
        }

        function atualizarEventoParcial(
            evento
        ) {
            const pessoaId =
                evento.pessoa_id
                || evento.evento_id;

            const card =
                cartoesPorPessoa.get(
                    pessoaId
                );

            if (!card) {
                console.warn(
                    "Atualização recebida sem cartão:",
                    pessoaId
                );

                return;
            }

            const campos =
                obterCamposCard(card);

            campos.quantidade.textContent =
                `Detecções: ${
                    evento.quantidade_deteccoes
                    || 1
                }`;

            if (evento.aparencia) {
                preencherAparencia(
                    evento.aparencia,
                    campos
                );
            }

            if (evento.movimento) {
                preencherMovimento(
                    evento.movimento,
                    campos
                );
            }

            if (evento.contexto_cena) {
                preencherContextoCena(
                    evento.contexto_cena,
                    campos
                );
            }

            if (evento.imagem_cena) {
                preencherCenaCompleta(
                    evento.imagem_cena,
                    evento.quantidade_boxes_cena,
                    campos
                );
            }

            card.classList.add(
                "atualizado"
            );

            setTimeout(
                function () {
                    card.classList.remove(
                        "atualizado"
                    );
                },
                1000
            );
        }

        function criarCartao(
            pessoaId,
            evento
        ) {
            const mensagemVazia =
                document.getElementById(
                    "mensagem-vazia"
                );

            if (mensagemVazia) {
                mensagemVazia.remove();
            }

            const card =
                criarElemento(
                    "article",
                    "",
                    "evento-card"
                );

            card.dataset.pessoaId =
                pessoaId;

            const imagensEvento =
                criarElemento(
                    "section",
                    "",
                    "imagens-evento"
                );

            const blocoRecorte =
                criarElemento(
                    "div",
                    "bloco-recorte",
                    "imagem-bloco"
                );

            const tituloRecorte =
                criarElemento(
                    "div",
                    "",
                    "imagem-titulo"
                );

            tituloRecorte.textContent =
                "Recorte individual";

            const imagemRecorte =
                criarElemento(
                    "img",
                    "imagem-recorte",
                    "imagem-evento imagem-recorte"
                );

            imagemRecorte.alt =
                "Recorte individual da pessoa";

            blocoRecorte.appendChild(
                tituloRecorte
            );

            blocoRecorte.appendChild(
                imagemRecorte
            );

            const blocoCena =
                criarElemento(
                    "div",
                    "bloco-cena",
                    "imagem-bloco"
                );

            const cabecalhoCena =
                criarElemento(
                    "div",
                    "",
                    "imagem-titulo"
                );

            const tituloCena =
                criarElemento(
                    "span",
                    "titulo-cena"
                );

            tituloCena.textContent =
                "Cena completa";

            const quantidadeBoxes =
                criarElemento(
                    "span",
                    "quantidade-boxes",
                    "imagem-contagem"
                );

            quantidadeBoxes.textContent =
                "0 detecções";

            cabecalhoCena.appendChild(
                tituloCena
            );

            cabecalhoCena.appendChild(
                quantidadeBoxes
            );

            const imagemCena =
                criarElemento(
                    "img",
                    "imagem-cena",
                    "imagem-evento imagem-cena"
                );

            imagemCena.alt =
                "Cena completa com as detecções";

            blocoCena.appendChild(
                cabecalhoCena
            );

            blocoCena.appendChild(
                imagemCena
            );

            imagensEvento.appendChild(
                blocoRecorte
            );

            imagensEvento.appendChild(
                blocoCena
            );

            const dados =
                criarElemento(
                    "div",
                    "",
                    "evento-dados"
                );

            const cabecalho =
                criarElemento(
                    "div",
                    "",
                    "evento-cabecalho"
                );

            const titulo =
                criarElemento(
                    "h2",
                    "camera"
                );

            const status =
                criarElemento(
                    "span",
                    "status",
                    "evento-status"
                );

            cabecalho.appendChild(
                titulo
            );

            cabecalho.appendChild(
                status
            );

            const tipo =
                criarElemento(
                    "p",
                    "tipo"
                );

            const horario =
                criarElemento(
                    "p",
                    "horario"
                );

            const identificadorPessoa =
                criarElemento(
                    "p",
                    "pessoa-id"
                );

            const quantidade =
                criarElemento(
                    "p",
                    "quantidade"
                );

            const aparencia =
                criarElemento(
                    "section",
                    "aparencia",
                    "aparencia-visual"
                );

            const aparenciaTitulo =
                criarElemento(
                    "h3",
                    "",
                    "secao-titulo"
                );

            aparenciaTitulo.textContent =
                "Aparência estável";

            const corRoupa =
                criarElemento(
                    "p",
                    "cor-roupa"
                );

            const posicao =
                criarElemento(
                    "p",
                    "posicao"
                );

            const tamanho =
                criarElemento(
                    "p",
                    "tamanho"
                );

            const percentual =
                criarElemento(
                    "p",
                    "percentual"
                );

            const quantidadeAmostras =
                criarElemento(
                    "p",
                    "quantidade-amostras"
                );

            const descricaoAparencia =
                criarElemento(
                    "p",
                    "descricao-aparencia",
                    "descricao"
                );

            aparencia.appendChild(
                aparenciaTitulo
            );

            aparencia.appendChild(
                corRoupa
            );

            aparencia.appendChild(
                posicao
            );

            aparencia.appendChild(
                tamanho
            );

            aparencia.appendChild(
                percentual
            );

            aparencia.appendChild(
                quantidadeAmostras
            );

            aparencia.appendChild(
                descricaoAparencia
            );

            const movimento =
                criarElemento(
                    "section",
                    "movimento",
                    "movimento-pessoa"
                );

            const movimentoTitulo =
                criarElemento(
                    "h3",
                    "",
                    "secao-titulo"
                );

            movimentoTitulo.textContent =
                "Movimento e permanência";

            const movimentoHorizontal =
                criarElemento(
                    "p",
                    "movimento-horizontal"
                );

            const movimentoVertical =
                criarElemento(
                    "p",
                    "movimento-vertical"
                );

            const movimentoTendencia =
                criarElemento(
                    "p",
                    "movimento-tendencia"
                );

            const movimentoVelocidade =
                criarElemento(
                    "p",
                    "movimento-velocidade"
                );

            const movimentoDistancia =
                criarElemento(
                    "p",
                    "movimento-distancia"
                );

            const movimentoTempo =
                criarElemento(
                    "p",
                    "movimento-tempo"
                );

            const movimentoAmostras =
                criarElemento(
                    "p",
                    "movimento-amostras"
                );

            const movimentoDescricao =
                criarElemento(
                    "p",
                    "movimento-descricao",
                    "descricao"
                );

            movimento.appendChild(
                movimentoTitulo
            );

            movimento.appendChild(
                movimentoHorizontal
            );

            movimento.appendChild(
                movimentoVertical
            );

            movimento.appendChild(
                movimentoTendencia
            );

            movimento.appendChild(
                movimentoVelocidade
            );

            movimento.appendChild(
                movimentoDistancia
            );

            movimento.appendChild(
                movimentoTempo
            );

            movimento.appendChild(
                movimentoAmostras
            );

            movimento.appendChild(
                movimentoDescricao
            );

            const contexto =
                criarElemento(
                    "section",
                    "contexto",
                    "contexto-cena"
                );

            const contextoTitulo =
                criarElemento(
                    "h3",
                    "",
                    "secao-titulo"
                );

            contextoTitulo.textContent =
                "Contexto da cena";

            const contextoQuantidade =
                criarElemento(
                    "p",
                    "contexto-quantidade"
                );

            const contextoDistribuicao =
                criarElemento(
                    "p",
                    "contexto-distribuicao"
                );

            const contextoProximidades =
                criarElemento(
                    "p",
                    "contexto-proximidades"
                );

            const contextoPessoas =
                criarElemento(
                    "p",
                    "contexto-pessoas",
                    "detalhes-pessoas"
                );

            const contextoDescricao =
                criarElemento(
                    "p",
                    "contexto-descricao",
                    "descricao"
                );

            contexto.appendChild(
                contextoTitulo
            );

            contexto.appendChild(
                contextoQuantidade
            );

            contexto.appendChild(
                contextoDistribuicao
            );

            contexto.appendChild(
                contextoProximidades
            );

            contexto.appendChild(
                contextoPessoas
            );

            contexto.appendChild(
                contextoDescricao
            );

            dados.appendChild(
                cabecalho
            );

            dados.appendChild(
                tipo
            );

            dados.appendChild(
                horario
            );

            dados.appendChild(
                identificadorPessoa
            );

            dados.appendChild(
                quantidade
            );

            dados.appendChild(
                aparencia
            );

            dados.appendChild(
                movimento
            );

            dados.appendChild(
                contexto
            );

            card.appendChild(
                imagensEvento
            );

            card.appendChild(
                dados
            );

            eventosElemento.prepend(
                card
            );

            cartoesPorPessoa.set(
                pessoaId,
                card
            );

            atualizarCartao(
                card,
                evento
            );

            limitarQuantidadeCartoes();
        }

        function preencherRecorte(
            imagemBase64,
            campos
        ) {
            if (!imagemBase64) {
                return;
            }

            campos.blocoRecorte.style.display =
                "block";

            campos.imagemRecorte.src =
                `data:image/jpeg;base64,${imagemBase64}`;
        }

        function preencherCenaCompleta(
            imagemBase64,
            quantidadeBoxes,
            campos
        ) {
            if (!imagemBase64) {
                return;
            }

            const quantidade =
                Number(
                    quantidadeBoxes || 0
                );

            campos.blocoCena.style.display =
                "block";

            campos.imagemCena.src =
                `data:image/jpeg;base64,${imagemBase64}`;

            campos.tituloCena.textContent =
                "Cena completa com detecções";

            if (quantidade === 1) {
                campos.quantidadeBoxes.textContent =
                    "1 detecção";
            } else {
                campos.quantidadeBoxes.textContent =
                    `${quantidade} detecções`;
            }
        }

        function preencherAparencia(
            aparencia,
            campos
        ) {
            campos.aparencia.style.display =
                "block";

            const rgb =
                aparencia.rgb_medio;

            let amostraCor = "";

            if (
                Array.isArray(rgb)
                && rgb.length === 3
            ) {
                amostraCor =
                    `<span
                        class="cor-amostra"
                        style="
                            background-color:
                            rgb(
                                ${rgb[0]},
                                ${rgb[1]},
                                ${rgb[2]}
                            );
                        "
                    ></span>`;
            }

            campos.corRoupa.innerHTML =
                `${amostraCor}Cor predominante: ${
                    aparencia
                        .cor_roupa_predominante
                    || "-"
                }`;

            campos.posicao.textContent =
                `Posição atual: ${
                    aparencia.posicao_atual
                    || "-"
                }`;

            campos.tamanho.textContent =
                `Tamanho predominante: ${
                    aparencia
                        .tamanho_predominante
                    || "-"
                }`;

            campos.percentual.textContent =
                `Área média ocupada: ${
                    aparencia
                        .percentual_medio_quadro
                    ?? "-"
                }%`;

            campos.quantidadeAmostras.textContent =
                `Amostras analisadas: ${
                    aparencia
                        .quantidade_amostras
                    || 1
                }`;

            campos.descricaoAparencia.textContent =
                `Descrição estável: ${
                    aparencia.descricao
                    || "-"
                }`;
        }

        function formatarTextoMovimento(
            valor
        ) {
            if (
                valor === null
                || valor === undefined
                || valor === ""
            ) {
                return "-";
            }

            const texto = String(valor)
                .replaceAll("_", " ");

            return (
                texto.charAt(0).toUpperCase()
                + texto.slice(1)

------------------------------------------------------------
ARQUIVO: app/static/index.html
------------------------------------------------------------
<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">

    <meta
        name="viewport"
        content="width=device-width, initial-scale=1.0"
    >

    <title>Painel de Eventos</title>

    <style>
        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            font-family: Arial, sans-serif;
            background: #10131a;
            color: #f3f4f6;
        }

        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px 30px;
            background: #191e29;
            border-bottom: 1px solid #2d3443;
        }

        h1 {
            margin: 0;
            font-size: 24px;
        }

        .status-conexao {
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .status-bolinha {
            width: 12px;
            height: 12px;
            border-radius: 50%;
            background: #f59e0b;
        }

        .status-bolinha.conectado {
            background: #22c55e;
        }

        .status-bolinha.desconectado {
            background: #ef4444;
        }

        main {
            padding: 30px;
        }

        .resumo {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
            margin-bottom: 25px;
        }

        .resumo-card {
            min-width: 180px;
            padding: 18px;
            background: #191e29;
            border: 1px solid #2d3443;
            border-radius: 10px;
        }

        .resumo-card span {
            display: block;
            color: #9ca3af;
            font-size: 14px;
        }

        .resumo-card strong {
            display: block;
            margin-top: 8px;
            font-size: 28px;
        }

        #eventos {
            display: grid;
            grid-template-columns: repeat(
                auto-fill,
                minmax(400px, 1fr)
            );
            gap: 20px;
        }

        .evento-card {
            overflow: hidden;
            background: #191e29;
            border: 1px solid #2d3443;
            border-radius: 10px;
            transition:
                border-color 0.2s,
                transform 0.2s,
                opacity 0.2s;
        }

        .evento-card.atualizado {
            border-color: #3b82f6;
            transform: translateY(-2px);
        }

        .evento-card.saiu {
            opacity: 0.55;
            border-color: #6b7280;
        }

        .imagens-evento {
            display: flex;
            flex-direction: column;
            background: #05070a;
        }

        .imagem-bloco {
            display: none;
            border-bottom: 1px solid #2d3443;
        }

        .imagem-bloco:last-child {
            border-bottom: none;
        }

        .imagem-titulo {
            display: flex;
            justify-content: space-between;
            align-items: center;
            min-height: 38px;
            padding: 9px 12px;
            background: #111827;
            color: #cbd5e1;
            font-size: 13px;
            font-weight: bold;
        }

        .imagem-contagem {
            color: #86efac;
            font-size: 12px;
        }

        .imagem-evento {
            display: block;
            width: 100%;
            background: #05070a;
        }

        .imagem-recorte {
            height: 260px;
            object-fit: contain;
        }

        .imagem-cena {
            aspect-ratio: 16 / 9;
            max-height: 440px;
            object-fit: contain;
        }

        .evento-dados {
            padding: 15px;
        }

        .evento-cabecalho {
            display: flex;
            justify-content: space-between;
            align-items: center;
            gap: 10px;
            margin-bottom: 12px;
        }

        .evento-cabecalho h2 {
            margin: 0;
            font-size: 18px;
        }

        .evento-status {
            padding: 5px 9px;
            border-radius: 999px;
            background: #374151;
            font-size: 12px;
            font-weight: bold;
        }

        .evento-status.entered {
            background: #166534;
            color: #dcfce7;
        }

        .evento-status.updated {
            background: #1e40af;
            color: #dbeafe;
        }

        .evento-status.exited {
            background: #4b5563;
            color: #f3f4f6;
        }

        .evento-dados p {
            margin: 6px 0;
            color: #cbd5e1;
            font-size: 14px;
        }

        .aparencia-visual,
        .movimento-pessoa,
        .contexto-cena {
            display: none;
            margin-top: 14px;
            padding-top: 12px;
            border-top: 1px solid #2d3443;
        }

        .secao-titulo {
            margin: 0 0 10px;
            font-size: 14px;
            color: #93c5fd;
        }

        .movimento-pessoa .secao-titulo {
            color: #fbbf24;
        }

        .contexto-cena .secao-titulo {
            color: #86efac;
        }

        .descricao {
            line-height: 1.45;
        }

        .detalhes-pessoas {
            line-height: 1.5;
            white-space: pre-line;
        }

        .cor-amostra {
            display: inline-block;
            width: 12px;
            height: 12px;
            margin-right: 6px;
            border: 1px solid #64748b;
            border-radius: 50%;
            vertical-align: -1px;
        }

        .vazio {
            padding: 40px;
            color: #9ca3af;
            text-align: center;
            background: #191e29;
            border: 1px dashed #374151;
            border-radius: 10px;
        }

        @media (max-width: 700px) {
            header {
                align-items: flex-start;
                flex-direction: column;
                gap: 15px;
                padding: 20px;
            }

            main {
                padding: 20px;
            }

            #eventos {
                grid-template-columns: 1fr;
            }

            .resumo-card {
                flex: 1;
                min-width: 140px;
            }

            .imagem-recorte {
                height: 230px;
            }
        }
    </style>
</head>

<body>
    <header>
        <h1>Painel de Eventos das Câmeras</h1>

        <div class="status-conexao">
            <div
                id="status-bolinha"
                class="status-bolinha"
            ></div>

            <span id="status-texto">
                Conectando...
            </span>
        </div>
    </header>

    <main>
        <section class="resumo">
            <div class="resumo-card">
                <span>Pessoas ativas</span>
                <strong id="pessoas-ativas">0</strong>
            </div>

            <div class="resumo-card">
                <span>Pessoas que saíram</span>
                <strong id="pessoas-sairam">0</strong>
            </div>

            <div class="resumo-card">
                <span>Total exibido</span>
                <strong id="total-pessoas">0</strong>
            </div>

            <div class="resumo-card">
                <span>Eventos processados</span>
                <strong id="total-eventos">0</strong>
            </div>
        </section>

        <section id="eventos">
            <div
                id="mensagem-vazia"
                class="vazio"
            >
                Aguardando eventos das câmeras...
            </div>
        </section>
    </main>

    <script>
        const statusBolinha =
            document.getElementById(
                "status-bolinha"
            );

        const statusTexto =
            document.getElementById(
                "status-texto"
            );

        const pessoasAtivasElemento =
            document.getElementById(
                "pessoas-ativas"
            );

        const pessoasSairamElemento =
            document.getElementById(
                "pessoas-sairam"
            );

        const totalPessoasElemento =
            document.getElementById(
                "total-pessoas"
            );

        const totalEventosElemento =
            document.getElementById(
                "total-eventos"
            );

        const eventosElemento =
            document.getElementById(
                "eventos"
            );

        const cartoesPorPessoa =
            new Map();

        const statusPorPessoa =
            new Map();

        let pessoasAtivas = 0;
        let pessoasSairam = 0;
        let totalPessoas = 0;
        let totalEventos = 0;
        let heartbeatId = null;

        function traduzirStatus(status) {
            const traducoes = {
                entered: "Entrou",
                updated: "Atualizado",
                suppressed: "Repetido",
                exited: "Saiu"
            };

            return (
                traducoes[status]
                || status
                || "Detectado"
            );
        }

        function atualizarResumo() {
            pessoasAtivasElemento.textContent =
                pessoasAtivas;

            pessoasSairamElemento.textContent =
                pessoasSairam;

            totalPessoasElemento.textContent =
                totalPessoas;

            totalEventosElemento.textContent =
                totalEventos;
        }

        function registrarMudancaStatus(
            pessoaId,
            novoStatus
        ) {
            const statusAnterior =
                statusPorPessoa.get(
                    pessoaId
                );

            if (!statusAnterior) {
                totalPessoas += 1;

                if (novoStatus === "exited") {
                    pessoasSairam += 1;
                } else {
                    pessoasAtivas += 1;
                }
            }

            else if (
                statusAnterior !== "exited"
                && novoStatus === "exited"
            ) {
                pessoasAtivas = Math.max(
                    0,
                    pessoasAtivas - 1
                );

                pessoasSairam += 1;
            }

            else if (
                statusAnterior === "exited"
                && novoStatus !== "exited"
            ) {
                pessoasSairam = Math.max(
                    0,
                    pessoasSairam - 1
                );

                pessoasAtivas += 1;
            }

            statusPorPessoa.set(
                pessoaId,
                novoStatus
            );

            atualizarResumo();
        }

        function conectarWebSocket() {
            const protocolo =
                window.location.protocol === "https:"
                    ? "wss"
                    : "ws";

            const endereco =
                `${protocolo}://${window.location.host}/ws/eventos`;

            const websocket =
                new WebSocket(endereco);

            websocket.onopen = function () {
                statusTexto.textContent =
                    "Conectado";

                statusBolinha.className =
                    "status-bolinha conectado";

                if (heartbeatId) {
                    clearInterval(
                        heartbeatId
                    );
                }

                heartbeatId = setInterval(
                    function () {
                        if (
                            websocket.readyState
                            === WebSocket.OPEN
                        ) {
                            websocket.send(
                                "ping"
                            );
                        }
                    },
                    20000
                );
            };

            websocket.onmessage =
                function (mensagem) {
                    try {
                        const evento =
                            JSON.parse(
                                mensagem.data
                            );

                        processarEvento(
                            evento
                        );

                    } catch (erro) {
                        console.error(
                            "Erro ao interpretar evento:",
                            erro
                        );
                    }
                };

            websocket.onclose = function () {
                statusTexto.textContent =
                    "Desconectado — reconectando...";

                statusBolinha.className =
                    "status-bolinha desconectado";

                if (heartbeatId) {
                    clearInterval(
                        heartbeatId
                    );

                    heartbeatId = null;
                }

                setTimeout(
                    conectarWebSocket,
                    2000
                );
            };

            websocket.onerror = function () {
                websocket.close();
            };
        }

        function processarEvento(evento) {
            if (
                evento.status
                === "appearance_updated"
            ) {
                atualizarEventoParcial(
                    evento
                );

                return;
            }

            if (
                evento.status
                === "suppressed"
            ) {
                return;
            }

            const pessoaId =
                evento.pessoa_id
                || evento.evento_id;

            if (!pessoaId) {
                console.warn(
                    "Evento sem identificador:",
                    evento
                );

                return;
            }

            const cartaoExistente =
                cartoesPorPessoa.get(
                    pessoaId
                );

            if (
                evento.status === "exited"
                && !cartaoExistente
            ) {
                return;
            }

            registrarMudancaStatus(
                pessoaId,
                evento.status || "entered"
            );

            totalEventos += 1;
            atualizarResumo();

            if (cartaoExistente) {
                atualizarCartao(
                    cartaoExistente,
                    evento
                );

                return;
            }

            criarCartao(
                pessoaId,
                evento
            );
        }

        function criarElemento(
            tag,
            campo,
            classe = ""
        ) {
            const elemento =
                document.createElement(
                    tag
                );

            if (campo) {
                elemento.dataset.campo =
                    campo;
            }

            if (classe) {
                elemento.className =
                    classe;
            }

            return elemento;
        }

        function obterCamposCard(card) {
            return {
                blocoRecorte: card.querySelector(
                    '[data-campo="bloco-recorte"]'
                ),

                imagemRecorte: card.querySelector(
                    '[data-campo="imagem-recorte"]'
                ),

                blocoCena: card.querySelector(
                    '[data-campo="bloco-cena"]'
                ),

                tituloCena: card.querySelector(
                    '[data-campo="titulo-cena"]'
                ),

                quantidadeBoxes: card.querySelector(
                    '[data-campo="quantidade-boxes"]'
                ),

                imagemCena: card.querySelector(
                    '[data-campo="imagem-cena"]'
                ),

                camera: card.querySelector(
                    '[data-campo="camera"]'
                ),

                status: card.querySelector(
                    '[data-campo="status"]'
                ),

                tipo: card.querySelector(
                    '[data-campo="tipo"]'
                ),

                horario: card.querySelector(
                    '[data-campo="horario"]'
                ),

                pessoaId: card.querySelector(
                    '[data-campo="pessoa-id"]'
                ),

                quantidade: card.querySelector(
                    '[data-campo="quantidade"]'
                ),

                aparencia: card.querySelector(
                    '[data-campo="aparencia"]'
                ),

                corRoupa: card.querySelector(
                    '[data-campo="cor-roupa"]'
                ),

                posicao: card.querySelector(
                    '[data-campo="posicao"]'
                ),

                tamanho: card.querySelector(
                    '[data-campo="tamanho"]'
                ),

                percentual: card.querySelector(
                    '[data-campo="percentual"]'
                ),

                quantidadeAmostras: card.querySelector(
                    '[data-campo="quantidade-amostras"]'
                ),

                descricaoAparencia: card.querySelector(
                    '[data-campo="descricao-aparencia"]'
                ),

                movimento: card.querySelector(
                    '[data-campo="movimento"]'
                ),

                movimentoHorizontal: card.querySelector(
                    '[data-campo="movimento-horizontal"]'
                ),

                movimentoVertical: card.querySelector(
                    '[data-campo="movimento-vertical"]'
                ),

                movimentoTendencia: card.querySelector(
                    '[data-campo="movimento-tendencia"]'
                ),

                movimentoVelocidade: card.querySelector(
                    '[data-campo="movimento-velocidade"]'
                ),

                movimentoDistancia: card.querySelector(
                    '[data-campo="movimento-distancia"]'
                ),

                movimentoTempo: card.querySelector(
                    '[data-campo="movimento-tempo"]'
                ),

                movimentoAmostras: card.querySelector(
                    '[data-campo="movimento-amostras"]'
                ),

                movimentoDescricao: card.querySelector(
                    '[data-campo="movimento-descricao"]'
                ),

                contexto: card.querySelector(
                    '[data-campo="contexto"]'
                ),

                contextoQuantidade: card.querySelector(
                    '[data-campo="contexto-quantidade"]'
                ),

                contextoDistribuicao: card.querySelector(
                    '[data-campo="contexto-distribuicao"]'
                ),

                contextoProximidades: card.querySelector(
                    '[data-campo="contexto-proximidades"]'
                ),

                contextoPessoas: card.querySelector(
                    '[data-campo="contexto-pessoas"]'
                ),

                contextoDescricao: card.querySelector(
                    '[data-campo="contexto-descricao"]'
                )
            };
        }

        function atualizarEventoParcial(
            evento
        ) {
            const pessoaId =
                evento.pessoa_id
                || evento.evento_id;

            const card =
                cartoesPorPessoa.get(
                    pessoaId
                );

            if (!card) {
                console.warn(
                    "Atualização recebida sem cartão:",
                    pessoaId
                );

                return;
            }

            const campos =
                obterCamposCard(card);

            campos.quantidade.textContent =
                `Detecções: ${
                    evento.quantidade_deteccoes
                    || 1
                }`;

            if (evento.imagem) {
                preencherRecorte(
                    evento.imagem,
                    campos
                );
            }

            if (evento.aparencia) {
                preencherAparencia(
                    evento.aparencia,
                    campos
                );
            }

            if (evento.movimento) {
                preencherMovimento(
                    evento.movimento,
                    campos
                );
            }

            if (evento.contexto_cena) {
                preencherContextoCena(
                    evento.contexto_cena,
                    campos
                );
            }

            if (evento.imagem_cena) {
                preencherCenaCompleta(
                    evento.imagem_cena,
                    evento.quantidade_boxes_cena,
                    campos
                );
            }

            card.classList.add(
                "atualizado"
            );

            setTimeout(
                function () {
                    card.classList.remove(
                        "atualizado"
                    );
                },
                1000
            );
        }

        function criarCartao(
            pessoaId,
            evento
        ) {
            const mensagemVazia =
                document.getElementById(
                    "mensagem-vazia"
                );

            if (mensagemVazia) {
                mensagemVazia.remove();
            }

            const card =
                criarElemento(
                    "article",
                    "",
                    "evento-card"
                );

            card.dataset.pessoaId =
                pessoaId;

            const imagensEvento =
                criarElemento(
                    "section",
                    "",
                    "imagens-evento"
                );

            const blocoRecorte =
                criarElemento(
                    "div",
                    "bloco-recorte",
                    "imagem-bloco"
                );

            const tituloRecorte =
                criarElemento(
                    "div",
                    "",
                    "imagem-titulo"
                );

            tituloRecorte.textContent =
                "Recorte individual";

            const imagemRecorte =
                criarElemento(
                    "img",
                    "imagem-recorte",
                    "imagem-evento imagem-recorte"
                );

            imagemRecorte.alt =
                "Recorte individual da pessoa";

            blocoRecorte.appendChild(
                tituloRecorte
            );

            blocoRecorte.appendChild(
                imagemRecorte
            );

            const blocoCena =
                criarElemento(
                    "div",
                    "bloco-cena",
                    "imagem-bloco"
                );

            const cabecalhoCena =
                criarElemento(
                    "div",
                    "",
                    "imagem-titulo"
                );

            const tituloCena =
                criarElemento(
                    "span",
                    "titulo-cena"
                );

            tituloCena.textContent =
                "Cena completa";

            const quantidadeBoxes =
                criarElemento(
                    "span",
                    "quantidade-boxes",
                    "imagem-contagem"
                );

            quantidadeBoxes.textContent =
                "0 detecções";

            cabecalhoCena.appendChild(
                tituloCena
            );

            cabecalhoCena.appendChild(
                quantidadeBoxes
            );

            const imagemCena =
                criarElemento(
                    "img",
                    "imagem-cena",
                    "imagem-evento imagem-cena"
                );

            imagemCena.alt =
                "Cena completa com as detecções";

            blocoCena.appendChild(
                cabecalhoCena
            );

            blocoCena.appendChild(
                imagemCena
            );

            imagensEvento.appendChild(
                blocoRecorte
            );

            imagensEvento.appendChild(
                blocoCena
            );

            const dados =
                criarElemento(
                    "div",
                    "",
                    "evento-dados"
                );

            const cabecalho =
                criarElemento(
                    "div",
                    "",
                    "evento-cabecalho"
                );

            const titulo =
                criarElemento(
                    "h2",
                    "camera"
                );

            const status =
                criarElemento(
                    "span",
                    "status",
                    "evento-status"
                );

            cabecalho.appendChild(
                titulo
            );

            cabecalho.appendChild(
                status
            );

            const tipo =
                criarElemento(
                    "p",
                    "tipo"
                );

            const horario =
                criarElemento(
                    "p",
                    "horario"
                );

            const identificadorPessoa =
                criarElemento(
                    "p",
                    "pessoa-id"
                );

            const quantidade =
                criarElemento(
                    "p",
                    "quantidade"
                );

            const aparencia =
                criarElemento(
                    "section",
                    "aparencia",
                    "aparencia-visual"
                );

            const aparenciaTitulo =
                criarElemento(
                    "h3",
                    "",
                    "secao-titulo"
                );

            aparenciaTitulo.textContent =
                "Aparência estável";

            const corRoupa =
                criarElemento(
                    "p",
                    "cor-roupa"
                );

            const posicao =
                criarElemento(
                    "p",
                    "posicao"
                );

            const tamanho =
                criarElemento(
                    "p",
                    "tamanho"
                );

            const percentual =
                criarElemento(
                    "p",
                    "percentual"
                );

            const quantidadeAmostras =
                criarElemento(
                    "p",
                    "quantidade-amostras"
                );

            const descricaoAparencia =
                criarElemento(
                    "p",
                    "descricao-aparencia",
                    "descricao"
                );

            aparencia.appendChild(
                aparenciaTitulo
            );

            aparencia.appendChild(
                corRoupa
            );

            aparencia.appendChild(
                posicao
            );

            aparencia.appendChild(
                tamanho
            );

            aparencia.appendChild(
                percentual
            );

            aparencia.appendChild(
                quantidadeAmostras
            );

            aparencia.appendChild(
                descricaoAparencia
            );

            const movimento =
                criarElemento(
                    "section",
                    "movimento",
                    "movimento-pessoa"
                );

            const movimentoTitulo =
                criarElemento(
                    "h3",
                    "",
                    "secao-titulo"
                );

            movimentoTitulo.textContent =
                "Movimento e permanência";

            const movimentoHorizontal =
                criarElemento(
                    "p",
                    "movimento-horizontal"
                );

            const movimentoVertical =
                criarElemento(
                    "p",
                    "movimento-vertical"
                );

            const movimentoTendencia =
                criarElemento(
                    "p",
                    "movimento-tendencia"
                );

            const movimentoVelocidade =
                criarElemento(
                    "p",
                    "movimento-velocidade"
                );

            const movimentoDistancia =
                criarElemento(
                    "p",
                    "movimento-distancia"
                );

            const movimentoTempo =
                criarElemento(
                    "p",
                    "movimento-tempo"
                );

            const movimentoAmostras =
                criarElemento(
                    "p",
                    "movimento-amostras"
                );

            const movimentoDescricao =
                criarElemento(
                    "p",
                    "movimento-descricao",
                    "descricao"
                );

            movimento.appendChild(
                movimentoTitulo
            );

            movimento.appendChild(
                movimentoHorizontal
            );

            movimento.appendChild(
                movimentoVertical
            );

            movimento.appendChild(
                movimentoTendencia
            );

            movimento.appendChild(
                movimentoVelocidade
            );

            movimento.appendChild(
                movimentoDistancia
            );

            movimento.appendChild(
                movimentoTempo
            );

            movimento.appendChild(
                movimentoAmostras
            );

            movimento.appendChild(
                movimentoDescricao
            );

            const contexto =
                criarElemento(
                    "section",
                    "contexto",
                    "contexto-cena"
                );

            const contextoTitulo =
                criarElemento(
                    "h3",
                    "",
                    "secao-titulo"
                );

            contextoTitulo.textContent =
                "Contexto da cena";

            const contextoQuantidade =
                criarElemento(
                    "p",
                    "contexto-quantidade"
                );

            const contextoDistribuicao =
                criarElemento(
                    "p",
                    "contexto-distribuicao"
                );

            const contextoProximidades =
                criarElemento(
                    "p",
                    "contexto-proximidades"
                );

            const contextoPessoas =
                criarElemento(
                    "p",
                    "contexto-pessoas",
                    "detalhes-pessoas"
                );

            const contextoDescricao =
                criarElemento(
                    "p",
                    "contexto-descricao",
                    "descricao"
                );

            contexto.appendChild(
                contextoTitulo
            );

            contexto.appendChild(
                contextoQuantidade
            );

            contexto.appendChild(
                contextoDistribuicao
            );

            contexto.appendChild(
                contextoProximidades
            );

            contexto.appendChild(
                contextoPessoas
            );

            contexto.appendChild(
                contextoDescricao
            );

            dados.appendChild(
                cabecalho
            );

            dados.appendChild(
                tipo
            );

            dados.appendChild(
                horario
            );

            dados.appendChild(
                identificadorPessoa
            );

            dados.appendChild(
                quantidade
            );

            dados.appendChild(
                aparencia
            );

            dados.appendChild(
                movimento
            );

            dados.appendChild(
                contexto
            );

            card.appendChild(
                imagensEvento
            );

            card.appendChild(
                dados
            );

            eventosElemento.prepend(
                card
            );

            cartoesPorPessoa.set(
                pessoaId,
                card
            );

            atualizarCartao(
                card,
                evento
            );

            limitarQuantidadeCartoes();
        }

        function preencherRecorte(
            imagemBase64,
            campos
        ) {
            if (!imagemBase64) {
                return;
            }

            campos.blocoRecorte.style.display =
                "block";

            campos.imagemRecorte.src =
                `data:image/jpeg;base64,${imagemBase64}`;
        }

        function preencherCenaCompleta(
            imagemBase64,
            quantidadeBoxes,
            campos
        ) {
            if (!imagemBase64) {
                return;
            }

            const quantidade =
                Number(
                    quantidadeBoxes || 0
                );

            campos.blocoCena.style.display =
                "block";

            campos.imagemCena.src =
                `data:image/jpeg;base64,${imagemBase64}`;

            campos.tituloCena.textContent =
                "Cena completa com detecções";

            if (quantidade === 1) {
                campos.quantidadeBoxes.textContent =
                    "1 detecção";
            } else {
                campos.quantidadeBoxes.textContent =
                    `${quantidade} detecções`;
            }
        }

        function preencherAparencia(
            aparencia,
            campos
        ) {
            campos.aparencia.style.display =
                "block";

            const rgb =
                aparencia.rgb_medio;

            let amostraCor = "";

            if (
                Array.isArray(rgb)
                && rgb.length === 3
            ) {
                amostraCor =
                    `<span
                        class="cor-amostra"
                        style="
                            background-color:
                            rgb(
                                ${rgb[0]},
                                ${rgb[1]},
                                ${rgb[2]}
                            );
                        "
                    ></span>`;
            }

            campos.corRoupa.innerHTML =
                `${amostraCor}Cor predominante: ${
                    aparencia
                        .cor_roupa_predominante
                    || "-"
                }`;

            campos.posicao.textContent =
                `Posição atual: ${
                    aparencia.posicao_atual
                    || "-"
                }`;

            campos.tamanho.textContent =
                `Tamanho predominante: ${
                    aparencia
                        .tamanho_predominante
                    || "-"
                }`;

            campos.percentual.textContent =
                `Área média ocupada: ${
                    aparencia
                        .percentual_medio_quadro
                    ?? "-"
                }%`;

            campos.quantidadeAmostras.textContent =
                `Amostras analisadas: ${
                    aparencia
                        .quantidade_amostras
                    || 1
                }`;

            campos.descricaoAparencia.textContent =
                `Descrição estável: ${
                    aparencia.descricao
                    || "-"
                }`;
        }

        function formatarTextoMovimento(
            valor
        ) {
            if (
                valor === null
                || valor === undefined
                || valor === ""
            ) {
                return "-";
            }

===== EVENTOS RECENTES DO RASTREAMENTO =====
2026-08-04 14:42:06,301 | INFO | app.services.camera_event_pipeline | [a0d9c8c3b85a] Rastreamento em lote: pessoas=1 ids=[('camera-01-11512b12', 'entered')]
2026-08-04 14:42:11,025 | INFO | app.services.camera_event_pipeline | [a0d9c8c3b85a] Aparência estável: pessoa=camera-01-11512b12 cor=indefinida posição=esquerda tamanho=pequeno área_média=2.57% amostras=1
2026-08-04 14:42:11,045 | INFO | app.services.camera_event_pipeline | [a0d9c8c3b85a] Pessoa enviada ao painel: pessoa=camera-01-11512b12 status=entered indice=1/1 paineis=0
2026-08-04 14:42:13,016 | INFO | app.services.camera_event_pipeline | [5ea7823f73d0] Rastreamento em lote: pessoas=1 ids=[('dahua-canal-0-992bc98b', 'entered')]
2026-08-04 14:42:13,545 | INFO | app.services.camera_event_pipeline | [3af4f6347a9a] Rastreamento em lote: pessoas=1 ids=[('camera-01-11512b12', 'updated')]
2026-08-04 14:42:13,827 | INFO | app.services.camera_event_pipeline | [5ea7823f73d0] Aparência estável: pessoa=dahua-canal-0-992bc98b cor=indefinida posição=centro tamanho=medio área_média=3.68% amostras=1
2026-08-04 14:42:13,892 | INFO | app.services.camera_event_pipeline | [5ea7823f73d0] Pessoa enviada ao painel: pessoa=dahua-canal-0-992bc98b status=entered indice=1/1 paineis=0
2026-08-04 14:42:14,002 | INFO | app.services.camera_event_pipeline | [6180e7be34b3] Rastreamento em lote: pessoas=1 ids=[('dahua-canal-0-992bc98b', 'suppressed')]
2026-08-04 14:42:14,440 | INFO | app.services.camera_event_pipeline | [3af4f6347a9a] Aparência estável: pessoa=camera-01-11512b12 cor=indefinida posição=esquerda tamanho=pequeno área_média=2.46% amostras=2
2026-08-04 14:42:14,464 | INFO | app.services.camera_event_pipeline | [3af4f6347a9a] Pessoa enviada ao painel: pessoa=camera-01-11512b12 status=updated indice=1/1 paineis=0
2026-08-04 14:42:14,920 | INFO | app.services.camera_event_pipeline | [6180e7be34b3] Aparência estável: pessoa=dahua-canal-0-992bc98b cor=indefinida posição=centro tamanho=pequeno área_média=2.69% amostras=2
2026-08-04 14:42:14,964 | INFO | app.services.camera_event_pipeline | [6180e7be34b3] Atualização parcial: pessoa=dahua-canal-0-992bc98b indice=1/1 deteccoes=2 amostras=2 movimento=esquerda/cima velocidade=105.67px_s boxes=1 paineis=0
2026-08-04 14:42:14,964 | INFO | app.services.camera_event_pipeline | [6180e7be34b3] Evento repetido suprimido: pessoa=dahua-canal-0-992bc98b indice=1/1 amostras=2
2026-08-04 14:42:14,989 | INFO | app.services.camera_event_pipeline | [3397a9773344] Rastreamento em lote: pessoas=1 ids=[('dahua-canal-0-992bc98b', 'suppressed')]
2026-08-04 14:42:15,732 | INFO | app.services.camera_event_pipeline | [3397a9773344] Aparência estável: pessoa=dahua-canal-0-992bc98b cor=azul posição=direita tamanho=medio área_média=2.95% amostras=3
2026-08-04 14:42:15,835 | INFO | app.services.camera_event_pipeline | [3397a9773344] Atualização parcial: pessoa=dahua-canal-0-992bc98b indice=1/1 deteccoes=3 amostras=3 movimento=direita/baixo velocidade=439.74px_s boxes=1 paineis=1
2026-08-04 14:42:15,835 | INFO | app.services.camera_event_pipeline | [3397a9773344] Evento repetido suprimido: pessoa=dahua-canal-0-992bc98b indice=1/1 amostras=3
2026-08-04 14:42:17,711 | INFO | app.services.camera_event_pipeline | [13d10b6d5aae] Rastreamento em lote: pessoas=1 ids=[('camera-01-11512b12', 'suppressed')]
2026-08-04 14:42:18,304 | INFO | app.services.camera_event_pipeline | [13d10b6d5aae] Cor escura indefinida: pessoa=camera-01-11512b12 rgb=(35, 39, 45); mantendo aparência anterior
2026-08-04 14:42:18,358 | INFO | app.services.camera_event_pipeline | [13d10b6d5aae] Atualização parcial: pessoa=camera-01-11512b12 indice=1/1 deteccoes=3 amostras=2 movimento=direita/cima velocidade=36.28px_s boxes=1 paineis=1
2026-08-04 14:42:18,358 | INFO | app.services.camera_event_pipeline | [13d10b6d5aae] Evento repetido suprimido: pessoa=camera-01-11512b12 indice=1/1 amostras=2
2026-08-04 14:42:27,230 | INFO | app.services.camera_event_pipeline | [3e0d0ce39be9] Rastreamento em lote: pessoas=1 ids=[('camera-01-11512b12', 'updated')]
2026-08-04 14:42:27,883 | INFO | app.services.camera_event_pipeline | [3e0d0ce39be9] Aparência estável: pessoa=camera-01-11512b12 cor=azul-escura posição=esquerda tamanho=pequeno área_média=2.27% amostras=3
2026-08-04 14:42:27,925 | INFO | app.services.camera_event_pipeline | [3e0d0ce39be9] Pessoa enviada ao painel: pessoa=camera-01-11512b12 status=updated indice=1/1 paineis=1
2026-08-04 14:42:35,099 | INFO | app.services.camera_event_pipeline | [0d3e4dc08f3b] Rastreamento em lote: pessoas=1 ids=[('camera-01-11512b12', 'updated')]
2026-08-04 14:42:35,820 | INFO | app.services.camera_event_pipeline | [0d3e4dc08f3b] Aparência estável: pessoa=camera-01-11512b12 cor=azul-escura posição=centro tamanho=pequeno área_média=2.22% amostras=4
2026-08-04 14:42:35,886 | INFO | app.services.camera_event_pipeline | [0d3e4dc08f3b] Pessoa enviada ao painel: pessoa=camera-01-11512b12 status=updated indice=1/1 paineis=1
2026-08-04 14:42:35,932 | INFO | app.services.camera_event_pipeline | [98a642319ecc] Rastreamento em lote: pessoas=1 ids=[('dahua-canal-0-2f23eaee', 'entered')]
2026-08-04 14:42:36,829 | INFO | app.services.camera_event_pipeline | [98a642319ecc] Aparência estável: pessoa=dahua-canal-0-2f23eaee cor=indefinida posição=centro tamanho=pequeno área_média=1.73% amostras=1
2026-08-04 14:42:36,987 | INFO | app.services.camera_event_pipeline | [98a642319ecc] Pessoa enviada ao painel: pessoa=dahua-canal-0-2f23eaee status=entered indice=1/1 paineis=1
2026-08-04 14:42:37,007 | INFO | app.services.camera_event_pipeline | [1193a240f420] Rastreamento em lote: pessoas=1 ids=[('dahua-canal-0-2f23eaee', 'suppressed')]
2026-08-04 14:42:37,832 | INFO | app.services.camera_event_pipeline | [1193a240f420] Aparência estável: pessoa=dahua-canal-0-2f23eaee cor=azul posição=centro tamanho=pequeno área_média=1.54% amostras=2
2026-08-04 14:42:37,942 | INFO | app.services.camera_event_pipeline | [1193a240f420] Atualização parcial: pessoa=dahua-canal-0-2f23eaee indice=1/1 deteccoes=2 amostras=2 movimento=esquerda/cima velocidade=425.74px_s boxes=1 paineis=1
2026-08-04 14:42:37,943 | INFO | app.services.camera_event_pipeline | [1193a240f420] Evento repetido suprimido: pessoa=dahua-canal-0-2f23eaee indice=1/1 amostras=2
2026-08-04 14:42:40,859 | INFO | app.services.camera_event_pipeline | [6c87ffe10217] Rastreamento em lote: pessoas=1 ids=[('camera-01-11512b12', 'updated')]
2026-08-04 14:42:41,448 | INFO | app.services.camera_event_pipeline | [6c87ffe10217] Aparência estável: pessoa=camera-01-11512b12 cor=azul-escura posição=centro tamanho=pequeno área_média=2.29% amostras=5
2026-08-04 14:42:41,494 | INFO | app.services.camera_event_pipeline | [6c87ffe10217] Pessoa enviada ao painel: pessoa=camera-01-11512b12 status=updated indice=1/1 paineis=1
2026-08-04 14:42:45,648 | INFO | app.services.camera_event_pipeline | [bcd5d34a4748] Rastreamento em lote: pessoas=1 ids=[('camera-01-11512b12', 'suppressed')]
2026-08-04 14:42:46,156 | INFO | app.services.camera_event_pipeline | [bcd5d34a4748] Aparência estável: pessoa=camera-01-11512b12 cor=azul-escura posição=centro tamanho=pequeno área_média=2.57% amostras=6
2026-08-04 14:42:46,195 | INFO | app.services.camera_event_pipeline | [bcd5d34a4748] Atualização parcial: pessoa=camera-01-11512b12 indice=1/1 deteccoes=7 amostras=6 movimento=parado/cima velocidade=20.82px_s boxes=1 paineis=1
2026-08-04 14:42:46,195 | INFO | app.services.camera_event_pipeline | [bcd5d34a4748] Evento repetido suprimido: pessoa=camera-01-11512b12 indice=1/1 amostras=6
2026-08-04 14:42:48,969 | INFO | app.services.camera_event_pipeline | [5b870cccc05b] Rastreamento em lote: pessoas=1 ids=[('dahua-canal-0-2f23eaee', 'updated')]
2026-08-04 14:42:49,645 | INFO | app.services.camera_event_pipeline | [5b870cccc05b] Aparência estável: pessoa=dahua-canal-0-2f23eaee cor=azul posição=centro tamanho=pequeno área_média=2.65% amostras=3
2026-08-04 14:42:49,762 | INFO | app.services.camera_event_pipeline | [5b870cccc05b] Pessoa enviada ao painel: pessoa=dahua-canal-0-2f23eaee status=updated indice=1/1 paineis=1
2026-08-04 14:42:50,488 | INFO | app.services.camera_event_pipeline | [0d12de830565] Rastreamento em lote: pessoas=1 ids=[('camera-01-11512b12', 'updated')]
2026-08-04 14:42:51,020 | INFO | app.services.camera_event_pipeline | [0d12de830565] Aparência estável: pessoa=camera-01-11512b12 cor=azul-escura posição=centro tamanho=pequeno área_média=2.5% amostras=7
2026-08-04 14:42:51,068 | INFO | app.services.camera_event_pipeline | [0d12de830565] Pessoa enviada ao painel: pessoa=camera-01-11512b12 status=updated indice=1/1 paineis=1
2026-08-04 14:42:53,903 | INFO | app.services.camera_event_pipeline | [1ada4c06726b] Rastreamento em lote: pessoas=1 ids=[('dahua-canal-0-2f23eaee', 'suppressed')]
2026-08-04 14:42:54,615 | INFO | app.services.camera_event_pipeline | [1ada4c06726b] Aparência estável: pessoa=dahua-canal-0-2f23eaee cor=azul posição=esquerda tamanho=pequeno área_média=2.26% amostras=4
2026-08-04 14:42:54,739 | INFO | app.services.camera_event_pipeline | [1ada4c06726b] Atualização parcial: pessoa=dahua-canal-0-2f23eaee indice=1/1 deteccoes=4 amostras=4 movimento=esquerda/cima velocidade=215.56px_s boxes=1 paineis=1
2026-08-04 14:42:54,739 | INFO | app.services.camera_event_pipeline | [1ada4c06726b] Evento repetido suprimido: pessoa=dahua-canal-0-2f23eaee indice=1/1 amostras=4
2026-08-04 14:42:54,945 | INFO | app.services.camera_event_pipeline | [5e095e0b3d80] Rastreamento em lote: pessoas=1 ids=[('camera-01-11512b12', 'suppressed')]
2026-08-04 14:42:55,424 | INFO | app.services.camera_event_pipeline | [5e095e0b3d80] Aparência estável: pessoa=camera-01-11512b12 cor=azul-escura posição=centro tamanho=pequeno área_média=2.47% amostras=8
2026-08-04 14:42:55,465 | INFO | app.services.camera_event_pipeline | [5e095e0b3d80] Atualização parcial: pessoa=camera-01-11512b12 indice=1/1 deteccoes=9 amostras=8 movimento=direita/cima velocidade=47.79px_s boxes=1 paineis=1
2026-08-04 14:42:55,465 | INFO | app.services.camera_event_pipeline | [5e095e0b3d80] Evento repetido suprimido: pessoa=camera-01-11512b12 indice=1/1 amostras=8
2026-08-04 14:42:55,953 | INFO | app.services.camera_event_pipeline | [fef204dd0723] Rastreamento em lote: pessoas=1 ids=[('dahua-canal-0-2f23eaee', 'updated')]
2026-08-04 14:42:56,613 | INFO | app.services.camera_event_pipeline | [fef204dd0723] Cor escura indefinida: pessoa=dahua-canal-0-2f23eaee rgb=(38, 32, 30); mantendo aparência anterior
2026-08-04 14:42:56,727 | INFO | app.services.camera_event_pipeline | [fef204dd0723] Pessoa enviada ao painel: pessoa=dahua-canal-0-2f23eaee status=updated indice=1/1 paineis=1
2026-08-04 14:42:57,011 | INFO | app.services.camera_event_pipeline | [c1c0522d17e8] Rastreamento em lote: pessoas=1 ids=[('camera-01-8487879c', 'entered')]
2026-08-04 14:42:57,550 | INFO | app.services.camera_event_pipeline | [c1c0522d17e8] Aparência estável: pessoa=camera-01-8487879c cor=indefinida posição=esquerda tamanho=medio área_média=3.21% amostras=1
2026-08-04 14:42:57,588 | INFO | app.services.camera_event_pipeline | [c1c0522d17e8] Pessoa enviada ao painel: pessoa=camera-01-8487879c status=entered indice=1/1 paineis=1
2026-08-04 14:42:58,952 | INFO | app.services.camera_event_pipeline | [679f80dfcd81] Rastreamento em lote: pessoas=1 ids=[('dahua-canal-0-2d489324', 'entered')]
2026-08-04 14:42:59,633 | INFO | app.services.camera_event_pipeline | [679f80dfcd81] Aparência estável: pessoa=dahua-canal-0-2d489324 cor=indefinida posição=centro tamanho=pequeno área_média=1.94% amostras=1
2026-08-04 14:42:59,736 | INFO | app.services.camera_event_pipeline | [679f80dfcd81] Pessoa enviada ao painel: pessoa=dahua-canal-0-2d489324 status=entered indice=1/1 paineis=1
2026-08-04 14:43:17,253 | INFO | app.services.camera_event_pipeline | [af8f0faf483b] Rastreamento em lote: pessoas=1 ids=[('camera-01-8c189150', 'entered')]
2026-08-04 14:43:17,297 | INFO | app.services.camera_event_pipeline | [af8f0faf483b] Aparência ignorada: caixa pequena pessoa=camera-01-8c189150 dimensoes=58x142 area=0.89%
2026-08-04 14:43:17,341 | INFO | app.services.camera_event_pipeline | [af8f0faf483b] Pessoa enviada ao painel: pessoa=camera-01-8c189150 status=entered indice=1/1 paineis=1
2026-08-04 14:43:20,882 | INFO | app.services.camera_event_pipeline | [754d38b645d8] Rastreamento em lote: pessoas=1 ids=[('dahua-canal-0-31050c1d', 'entered')]
2026-08-04 14:43:21,576 | INFO | app.services.camera_event_pipeline | [754d38b645d8] Aparência estável: pessoa=dahua-canal-0-31050c1d cor=indefinida posição=direita tamanho=medio área_média=3.11% amostras=1
2026-08-04 14:43:21,703 | INFO | app.services.camera_event_pipeline | [754d38b645d8] Pessoa enviada ao painel: pessoa=dahua-canal-0-31050c1d status=entered indice=1/1 paineis=1
2026-08-04 14:43:30,930 | INFO | app.services.camera_event_pipeline | [4750234ef1ff] Rastreamento em lote: pessoas=1 ids=[('dahua-canal-0-31050c1d', 'updated')]
2026-08-04 14:43:31,049 | INFO | app.services.camera_event_pipeline | [5249886085bd] Rastreamento em lote: pessoas=1 ids=[('camera-01-1640b345', 'entered')]
2026-08-04 14:43:31,718 | INFO | app.services.camera_event_pipeline | [4750234ef1ff] Aparência estável: pessoa=dahua-canal-0-31050c1d cor=indefinida posição=centro tamanho=medio área_média=5.04% amostras=2
2026-08-04 14:43:31,860 | INFO | app.services.camera_event_pipeline | [4750234ef1ff] Pessoa enviada ao painel: pessoa=dahua-canal-0-31050c1d status=updated indice=1/1 paineis=1
2026-08-04 14:43:32,158 | INFO | app.services.camera_event_pipeline | [5249886085bd] Aparência estável: pessoa=camera-01-1640b345 cor=indefinida posição=centro tamanho=pequeno área_média=2.3% amostras=1
2026-08-04 14:43:32,199 | INFO | app.services.camera_event_pipeline | [5249886085bd] Pessoa enviada ao painel: pessoa=camera-01-1640b345 status=entered indice=1/1 paineis=1
2026-08-04 14:43:37,939 | INFO | app.services.camera_event_pipeline | [39fe0f65b0a5] Rastreamento em lote: pessoas=1 ids=[('dahua-canal-0-31050c1d', 'updated')]
2026-08-04 14:43:38,685 | INFO | app.services.camera_event_pipeline | [39fe0f65b0a5] Aparência estável: pessoa=dahua-canal-0-31050c1d cor=azul posição=direita tamanho=medio área_média=4.46% amostras=3
2026-08-04 14:43:38,820 | INFO | app.services.camera_event_pipeline | [39fe0f65b0a5] Pessoa enviada ao painel: pessoa=dahua-canal-0-31050c1d status=updated indice=1/1 paineis=1
2026-08-04 14:43:43,883 | INFO | app.services.camera_event_pipeline | [cdd24b464aa7] Rastreamento em lote: pessoas=1 ids=[('dahua-canal-0-31050c1d', 'updated')]
2026-08-04 14:43:44,546 | INFO | app.services.camera_event_pipeline | [cdd24b464aa7] Aparência estável: pessoa=dahua-canal-0-31050c1d cor=azul posição=esquerda tamanho=medio área_média=4.2% amostras=4
2026-08-04 14:43:44,651 | INFO | app.services.camera_event_pipeline | [cdd24b464aa7] Pessoa enviada ao painel: pessoa=dahua-canal-0-31050c1d status=updated indice=1/1 paineis=1
2026-08-04 14:43:44,898 | INFO | app.services.camera_event_pipeline | [724cc35bc344] Rastreamento em lote: pessoas=1 ids=[('dahua-canal-0-fc63164b', 'entered')]
2026-08-04 14:43:45,515 | INFO | app.services.camera_event_pipeline | [724cc35bc344] Aparência estável: pessoa=dahua-canal-0-fc63164b cor=indefinida posição=direita tamanho=medio área_média=3.53% amostras=1
2026-08-04 14:43:45,618 | INFO | app.services.camera_event_pipeline | [724cc35bc344] Pessoa enviada ao painel: pessoa=dahua-canal-0-fc63164b status=entered indice=1/1 paineis=1
2026-08-04 14:43:51,653 | INFO | app.services.camera_event_pipeline | [ded82a5f8a8f] Rastreamento em lote: pessoas=1 ids=[('camera-01-ba436115', 'entered')]
2026-08-04 14:43:52,223 | INFO | app.services.camera_event_pipeline | [ded82a5f8a8f] Aparência estável: pessoa=camera-01-ba436115 cor=indefinida posição=centro tamanho=medio área_média=3.41% amostras=1
2026-08-04 14:43:52,266 | INFO | app.services.camera_event_pipeline | [ded82a5f8a8f] Pessoa enviada ao painel: pessoa=camera-01-ba436115 status=entered indice=1/1 paineis=1
2026-08-04 14:43:59,636 | INFO | app.services.camera_event_pipeline | [fe867370096c] Rastreamento em lote: pessoas=1 ids=[('camera-01-ba436115', 'updated')]
2026-08-04 14:44:00,363 | INFO | app.services.camera_event_pipeline | [fe867370096c] Aparência estável: pessoa=camera-01-ba436115 cor=indefinida posição=esquerda tamanho=medio área_média=3.33% amostras=2
2026-08-04 14:44:00,413 | INFO | app.services.camera_event_pipeline | [fe867370096c] Pessoa enviada ao painel: pessoa=camera-01-ba436115 status=updated indice=1/1 paineis=1
2026-08-04 14:44:07,399 | INFO | app.services.camera_event_pipeline | [b2e40ed8a645] Rastreamento em lote: pessoas=1 ids=[('camera-01-ba436115', 'updated')]
2026-08-04 14:44:07,886 | INFO | app.services.camera_event_pipeline | [b2e40ed8a645] Aparência estável: pessoa=camera-01-ba436115 cor=azul-escura posição=esquerda tamanho=medio área_média=3.04% amostras=3
2026-08-04 14:44:07,931 | INFO | app.services.camera_event_pipeline | [b2e40ed8a645] Pessoa enviada ao painel: pessoa=camera-01-ba436115 status=updated indice=1/1 paineis=1
2026-08-04 14:44:08,269 | INFO | app.services.camera_event_pipeline | [8fe14051275f] Rastreamento em lote: pessoas=1 ids=[('camera-01-8e1d889a', 'entered')]
2026-08-04 14:44:08,809 | INFO | app.services.camera_event_pipeline | [8fe14051275f] Aparência estável: pessoa=camera-01-8e1d889a cor=indefinida posição=centro tamanho=pequeno área_média=2.14% amostras=1
2026-08-04 14:44:08,845 | INFO | app.services.camera_event_pipeline | [8fe14051275f] Pessoa enviada ao painel: pessoa=camera-01-8e1d889a status=entered indice=1/1 paineis=1
2026-08-04 14:44:10,075 | INFO | app.services.camera_event_pipeline | [44258f8a7794] Rastreamento em lote: pessoas=1 ids=[('camera-01-8e1d889a', 'suppressed')]
2026-08-04 14:44:10,542 | INFO | app.services.camera_event_pipeline | [44258f8a7794] Aparência estável: pessoa=camera-01-8e1d889a cor=indefinida posição=centro tamanho=pequeno área_média=1.94% amostras=2
2026-08-04 14:44:10,579 | INFO | app.services.camera_event_pipeline | [44258f8a7794] Atualização parcial: pessoa=camera-01-8e1d889a indice=1/1 deteccoes=2 amostras=2 movimento=direita/cima velocidade=17.53px_s boxes=1 paineis=1
2026-08-04 14:44:10,579 | INFO | app.services.camera_event_pipeline | [44258f8a7794] Evento repetido suprimido: pessoa=camera-01-8e1d889a indice=1/1 amostras=2

===== QUANTIDADE DE IDS DIFERENTES =====
     37 camera-01-11512b12
     21 dahua-canal-0-2f23eaee
     17 dahua-canal-0-31050c1d
     13 dahua-canal-0-992bc98b
     12 camera-01-ba436115
      8 camera-01-8e1d889a
      5 dahua-canal-0-fc63164b
      5 dahua-canal-0-2d489324
      5 camera-01-8c189150
      5 camera-01-8487879c
      5 camera-01-1640b345
